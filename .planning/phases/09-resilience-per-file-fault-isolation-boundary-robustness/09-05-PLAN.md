---
phase: 09-resilience-per-file-fault-isolation-boundary-robustness
plan: 05
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - packages/angular-typechecker/src/core/run-typecheck.ts
  - packages/angular-typechecker/src/core/diagnostic-codes.ts
  - packages/angular-typechecker/src/executors/angular-typecheck/executor.ts
  - packages/angular-typechecker/src/core/run-typecheck.spec.ts
  - packages/angular-typechecker/src/core/fault-isolation.integration.spec.ts
  - packages/angular-typechecker/src/executors/angular-typecheck/executor.spec.ts
autonomous: true
requirements: [RES-02]
tags: [angular, compiler-cli, diagnostics, resilience, reporting]

must_haves:
  truths:
    - "When a TCB-generation FatalDiagnosticError suppresses surviving files' template diagnostics, the engine surfaces a LOUD, visible notice naming the offending file -- the incompleteness is NEVER silent (RES-02 reframe; 09-RES-02-DECISION.md)."
    - "Detection is PURE (a CoreResult field set in core by scanning the reported diagnostics for the TCB-generation FatalDiagnosticError code(s) verified at v22.0.4); the notice TEXT is rendered by the adapter (executor logger.warn), never by core (eslint bans no-console/process in **/src/core/**)."
    - "The notice fires on the existing fixtures/fault-isolation poison (NG3004 IMPORT_GENERATION_FAILURE) and does NOT fire on clean / ordinarily-erroring programs (no false positives) -- proven by unit + real-compiler integration + executor tests."
    - "The Phase-8 infra-vs-type policy is untouched: a non-fatal/infra throw still surfaces as UNKNOWN_ERROR_CODE 500 -> TypecheckInfrastructureError (D-05); the notice is orthogonal signalling, not a reclassification."
  artifacts:
    - path: "packages/angular-typechecker/src/core/run-typecheck.ts"
      provides: "A pure CoreResult field flagging a TCB-generation-Fatal template-abort + the offending file (set in finalize by scanning reported diagnostics)"
      contains: "CoreResult"
    - path: "packages/angular-typechecker/src/executors/angular-typecheck/executor.ts"
      provides: "A loud logger.warn notice rendered when the CoreResult flag is set"
      contains: "logger"
  key_links:
    - from: "run-typecheck.ts finalize"
      to: "diagnostic-codes.ts TCB-generation FatalDiagnosticError code(s)"
      via: "scan reported diagnostics for the fatal code; populate the CoreResult field"
      pattern: "diagnostic-codes"
    - from: "executor.ts"
      to: "the CoreResult template-abort field"
      via: "logger.warn naming the offending file when set"
      pattern: "logger\\.(warn|error)"
---

<objective>
Complete the reframed RES-02 contract (09-RES-02-DECISION.md, Option A): when a TCB-GENERATION-phase `FatalDiagnosticError` (e.g. NG3004 `IMPORT_GENERATION_FAILURE`) aborts the shared shim priming -- so surviving files' Angular TEMPLATE/extended (NG8xxx) diagnostics are suppressed -- the engine MUST surface a LOUD, visible notice naming the offending file, so the (deferred-to-REP-RES-02b) incompleteness is NEVER silent.

Pure-core detection (a `CoreResult` field), adapter-rendered notice (executor `logger.warn`). The poison in `fixtures/fault-isolation/` (NG3004) is the test vehicle; clean fixtures must NOT trigger it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-CONTEXT.md
@.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-RES-02-DECISION.md
@.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-RESEARCH.md
@.planning/phases/08-correctness-completeness-fixes/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Detect a TCB-generation-Fatal template abort in core and surface a loud notice in the executor</name>
  <files>packages/angular-typechecker/src/core/run-typecheck.ts, packages/angular-typechecker/src/core/diagnostic-codes.ts, packages/angular-typechecker/src/executors/angular-typecheck/executor.ts, packages/angular-typechecker/src/core/run-typecheck.spec.ts, packages/angular-typechecker/src/core/fault-isolation.integration.spec.ts, packages/angular-typechecker/src/executors/angular-typecheck/executor.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/run-typecheck.ts (the CoreResult interface + finalize where diagnostics are sorted/counted -- the natural home for a pure detection field)
    - packages/angular-typechecker/src/core/diagnostic-codes.ts (the existing NG-code helpers / negative-encoding; add the TCB-generation FatalDiagnosticError code constant(s) here)
    - packages/angular-typechecker/src/core/gather-diagnostics.ts (the HYBRID gatherer + its header documenting the abort behavior)
    - packages/angular-typechecker/src/executors/angular-typecheck/executor.ts (the adapter that owns I/O -- where logger.warn belongs; see the existing infra-error catch/message)
    - packages/angular-typechecker/src/core/fault-isolation.integration.spec.ts and packages/angular-typechecker/src/executors/angular-typecheck/executor.spec.ts (the test idioms to extend)
    - .planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-RES-02-DECISION.md (WHY this notice exists; the exact contract)
    - .planning/phases/08-correctness-completeness-fixes/08-CONTEXT.md (D-05/D-06: the infra-vs-type policy the notice must NOT disturb)
  </read_first>
  <action>
    VERIFY at Angular v22.0.4 (read `node_modules/@angular/compiler-cli` or the local clone `D:\projects\github\angular\angular` via `git show v22.0.4:`) which `FatalDiagnosticError` ErrorCode(s) are thrown from the TCB-generation / template-type-check path and reach the `NgCompiler.getDiagnostics`/`getDiagnosticsForFile` `isFatalDiagnosticError` catch (NG3004 IMPORT_GENERATION_FAILURE is the confirmed one; check whether the sibling structural classes 3001/3003 also reach that catch). Add a named constant (and the NG-encoded value via the existing diagnostic-codes helper) for the detected TCB-generation-Fatal code(s) in `diagnostic-codes.ts` with a `// angular-typechecker: vendored -- <reason>` style note if it mirrors a compiler value.

    In `run-typecheck.ts`: add a PURE, optional field to `CoreResult` (name at your discretion, e.g. `templateCheckAborted?: { code: number; fileName: string | undefined }` -- or a boolean + file). Populate it in `finalize` by scanning the REPORTED (post-filter, post-sort) diagnostics for a TCB-generation-Fatal code; carry the offending diagnostic's `file?.fileName` when present. No `process`/`console` (core is PURE). Do NOT change the infra-500 re-throw path (D-05) -- the notice is additive signalling on the normal result path.

    In `executor.ts`: when the field is set, emit a LOUD `logger.warn` (distinct from the existing infra `logger.error`) naming the offending file and stating that surviving files' Angular template/extended (NG8xxx) diagnostics may be suppressed until that fatal is fixed, with a re-run hint. Keep the existing success/infra behavior unchanged.

    Tests: (a) `run-typecheck.spec.ts` unit -- given a synthesized reported set containing the TCB-gen-Fatal code, finalize sets the field with the file; given a clean set, it stays unset. (b) `fault-isolation.integration.spec.ts` (extend) -- the real-compiler run against `fixtures/fault-isolation/tsconfig.app.json` sets the field (the NG3004 poison), and a clean fixture run does not. (c) `executor.spec.ts` (extend) -- when the result field is set, the executor calls `logger.warn` with the file; when unset, it does not. ASCII-only.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `diagnostic-codes.ts` declares the TCB-generation FatalDiagnosticError code constant(s), verified against v22.0.4.
    - `CoreResult` has a new optional pure field flagging a TCB-generation-Fatal template abort + the offending file; populated in `finalize` with no `console`/`process` use.
    - `executor.ts` emits a `logger.warn` (not error) naming the file when the field is set; success and infra (`UNKNOWN_ERROR_CODE` 500 -> TypecheckInfrastructureError) paths are unchanged.
    - The field is SET on the `fixtures/fault-isolation` poison run and UNSET on clean/ordinarily-erroring runs (proven by unit + integration + executor tests).
    - `npx nx test angular-typechecker` exits 0 (full suite, no regressions); `npx nx build angular-typechecker` exits 0; `npx nx lint angular-typechecker` exits 0 (pure-core eslint satisfied -- no console/process added under src/core).
  </acceptance_criteria>
  <done>A TCB-generation Fatal now produces a loud, file-named notice via the executor, the suppression is never silent, detection is pure-core + adapter-rendered, and the full suite + build + lint are green.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-09-05 | Repudiation / silent incompleteness (the tool under-reports without telling anyone) | gatherer abort path + executor output | mitigate | The loud notice converts a SILENT template-diagnostic suppression into an explicit, file-named warning, so a CI/agent consumer cannot mistake a TCB-gen-Fatal run for a complete template check. Closes the only completeness objection to the RES-02 Option-A reframe. |
| T-09-04 | Tampering with verdict meaning | core detection field | mitigate | Detection is additive on the normal result path and does NOT touch the infra-500 -> TypecheckInfrastructureError classification (Phase-8 D-05); the notice is a warning, not a reclassification. |
| T-09-SC | Tampering | npm installs | accept | No package installs (pure source edits). |
</threat_model>

<verification>
- `npx nx test angular-typechecker` exits 0 (full suite, incl. the new notice unit + integration + executor tests).
- `npx nx build angular-typechecker` exits 0 (drift guard).
- `npx nx lint angular-typechecker` exits 0 (no console/process under src/core).
- The notice fires on the poison fixture and not on clean fixtures.
</verification>

<success_criteria>
- The reframed SC2 loud-notice clause is delivered: a TCB-generation Fatal surfaces a loud, file-named notice; the incompleteness is never silent.
- Pure-core detection + adapter-rendered notice; infra-vs-type policy preserved; no new package.
</success_criteria>

<output>
Create `.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-05-SUMMARY.md` when done.
</output>
