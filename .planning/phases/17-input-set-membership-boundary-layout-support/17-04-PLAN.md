---
phase: 17-input-set-membership-boundary-layout-support
plan: 04
type: execute
wave: 3
depends_on: ["17-03"]
files_modified:
  - packages/angular-typechecker/src/core/evaluate-result.ts
  - packages/angular-typechecker/src/core/evaluate-result.spec.ts
  - packages/angular-typechecker/src/core/exit-codes.ts
  - packages/angular-typechecker/src/core/exit-codes.spec.ts
autonomous: true
requirements: [SB-04]

must_haves:
  truths:
    - "evaluateResult fails when errorCount > 0 OR suppressedInGraphErrorCount > 0 OR (gatesWarnings AND (warningCount > maxWarnings OR suppressedInGraphWarningCount > 0)) OR templateCheckAborted present OR a zero-root-names leaf is recorded"
    - "The warning-severity decision is LATE-BOUND in evaluateResult with the real maxWarnings (not baked in core)"
    - "evaluateResult returns an outcome discriminant: clean | type-error | coverage-incomplete | warnings-exceeded"
    - "toExitCode maps coverage-incomplete to exit 1 (ngc parity), reusing 1"
    - "A clean run (all counts 0, no abort, no zero-root-names leaf) stays success:true / outcome clean / exit 0"
  artifacts:
    - path: "packages/angular-typechecker/src/core/evaluate-result.ts"
      provides: "late-bound coverage-incomplete gate + outcome discriminant"
      contains: "coverage-incomplete"
    - path: "packages/angular-typechecker/src/core/exit-codes.ts"
      provides: "coverage-incomplete -> exit 1"
  key_links:
    - from: "CoreResult.suppressedInGraphErrorCount / templateCheckAborted / skippedReferences(zero-root-names)"
      to: "evaluateResult { success:false, outcome:'coverage-incomplete' }"
      via: "the coverage-incomplete gate"
      pattern: "suppressedInGraph"
---

<objective>
Wire the split counter into the pure verdict so a dropped first-party diagnostic can never
coexist with a green verdict (the SB-04 charter floor, D-06). Add a LATE-BOUND, per-category
coverage-incomplete gate to `evaluateResult` (the live Nx path) and mirror it in `toExitCode`
(the deferred-CLI scaffold), plus an `outcome` discriminant
(`clean | type-error | coverage-incomplete | warnings-exceeded`).

Purpose: today neither function reads any suppressed count -- an unwired field is
"surface-only by accident" (a live silent pass once the CLI ships). The warning decision MUST
be late-bound because `finalize` never receives `maxWarnings`; baking "warnings never count"
in core is a silent false pass under `maxWarnings: 0`. HARD by default (D-06).
Output: the coverage-incomplete verdict + exit policy + their unit tiers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

READ-FIRST source-of-truth (D-06 is the authoritative verdict design):
@.planning/phases/17-input-set-membership-boundary-layout-support/17-DECISION-input-set-boundary.md
@.planning/phases/17-input-set-membership-boundary-layout-support/17-CONTEXT.md
@.planning/phases/17-input-set-membership-boundary-layout-support/17-RESEARCH.md

The files being modified + the CoreResult contract (17-03 output):
@packages/angular-typechecker/src/core/evaluate-result.ts
@packages/angular-typechecker/src/core/exit-codes.ts
@packages/angular-typechecker/src/core/run-typecheck.ts

Prior-plan SUMMARY (delivered CoreResult fields):
@.planning/phases/17-input-set-membership-boundary-layout-support/17-03-SUMMARY.md

<interfaces>
```typescript
// evaluate-result.ts -- expanded Pick + outcome discriminant.
export type Outcome = 'clean' | 'type-error' | 'coverage-incomplete' | 'warnings-exceeded';

export function evaluateResult(
  result: Pick<
    CoreResult,
    | 'errorCount'
    | 'warningCount'
    | 'suppressedInGraphErrorCount'
    | 'suppressedInGraphWarningCount'
    | 'templateCheckAborted'
    | 'skippedReferences'
  >,
  options?: EvaluateOptions, // { maxWarnings?: number }
): { success: boolean; outcome: Outcome };

// exit-codes.ts
export function toExitCode(
  input: Pick<CoreResult, 'errorCount' | 'suppressedInGraphErrorCount'> | TypecheckInfrastructureError,
): 0 | 1 | 2;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: late-bound coverage-incomplete gate + outcome in evaluateResult</name>
  <files>packages/angular-typechecker/src/core/evaluate-result.ts, packages/angular-typechecker/src/core/evaluate-result.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/evaluate-result.ts (the current gate: errorCount-first, then the maxWarnings `gatesWarnings` block; the `EvaluateOptions` shape; the defensive NaN/negative handling)
    - packages/angular-typechecker/src/core/evaluate-result.spec.ts (the 2-field-literal idiom -- extend it)
    - packages/angular-typechecker/src/core/run-typecheck.ts (CoreResult: the new suppressedInGraph* fields; templateCheckAborted?; skippedReferences? with reason 'zero-root-names'; SkippedReference type)
    - 17-DECISION-input-set-boundary.md ("Severity-mirror has a silent-pass hole UNLESS late-bound"; FM-9 fold; zero-rootNames leaf into coverage-incomplete) + 17-CONTEXT.md D-06
  </read_first>
  <behavior>
    - errorCount > 0 -> { success:false, outcome:'type-error' } (checked FIRST; errors always win the label).
    - else suppressedInGraphErrorCount > 0 -> { success:false, outcome:'coverage-incomplete' }.
    - else templateCheckAborted present -> { success:false, outcome:'coverage-incomplete' } (FM-9: whole-program TCB abort suppressed survivors' NG8xxx).
    - else a skippedReferences entry with reason === 'zero-root-names' -> { success:false, outcome:'coverage-incomplete' } (a first-party leaf resolved zero files; a clean sibling must not read the whole solution clean).
    - else gatesWarnings AND warningCount > maxWarnings -> { success:false, outcome:'warnings-exceeded' } (existing EXE-05 gate, preserved).
    - else gatesWarnings AND suppressedInGraphWarningCount > 0 -> { success:false, outcome:'coverage-incomplete' } (a dropped in-graph warning under a warning-gating config).
    - else -> { success:true, outcome:'clean' }.
    - gatesWarnings = maxWarnings is a finite number >= 0 (existing defensive NaN/negative handling preserved).
  </behavior>
  <action>
    Expand the `evaluateResult` parameter `Pick` to include `suppressedInGraphErrorCount`, `suppressedInGraphWarningCount`, `templateCheckAborted`, and `skippedReferences` (import the `SkippedReference`/`CoreResult` types as needed). Return `{ success, outcome }` where `outcome` is the `Outcome` union. Implement the ordered decision in the `<behavior>` block: the two ALWAYS-fail coverage triggers (suppressedInGraphErrorCount, templateCheckAborted, zero-root-names leaf) are unconditional; the warning-suppression trigger is gated by `gatesWarnings` (LATE-BOUND with the real `maxWarnings` -- this is the load-bearing D-06 fix, do NOT pre-collapse the warning decision in core). Preserve the existing errors-always-fail and warnings-exceeded semantics exactly. Extend `evaluate-result.spec.ts`: keep every existing assertion (add `.outcome` where useful) and add coverage-incomplete cases -- suppressedInGraphErrorCount>0 with errorCount 0 fails as coverage-incomplete; templateCheckAborted present fails as coverage-incomplete; a zero-root-names skippedReferences entry fails as coverage-incomplete; suppressedInGraphWarningCount>0 fails ONLY when maxWarnings gates (0), passes clean when maxWarnings unset (the late-binding proof); a Suggestion/Message-only drop (counts 0) stays clean.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- evaluate-result</automated>
  </verify>
  <acceptance_criteria>
    - `evaluateResult` returns `{ success, outcome }`; outcome is one of clean/type-error/coverage-incomplete/warnings-exceeded.
    - suppressedInGraphWarningCount>0 fails under maxWarnings:0 but PASSES clean with maxWarnings unset (late-binding proven).
    - templateCheckAborted present and a zero-root-names skip each yield coverage-incomplete.
    - Every prior evaluate-result.spec assertion still passes; `npx nx test angular-typechecker -- evaluate-result` green.
  </acceptance_criteria>
  <done>The verdict fails on any first-party coverage loss, late-binds the warning decision, and reports a discriminated outcome.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: coverage-incomplete exit code in toExitCode</name>
  <files>packages/angular-typechecker/src/core/exit-codes.ts, packages/angular-typechecker/src/core/exit-codes.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/exit-codes.ts (the current 2/1/0 ngc-parity policy; the `Pick<CoreResult,'errorCount'> | TypecheckInfrastructureError` input; the LAYERING note -- run-typecheck must not import this)
    - packages/angular-typechecker/src/core/exit-codes.spec.ts (the 2-field-literal + typed-error idiom)
    - 17-RESEARCH.md "Open Question 2" (reuse 1 vs a distinct code -> reuse 1) + Code Examples (exit-codes snippet)
  </read_first>
  <behavior>
    - TypecheckInfrastructureError -> 2 (unchanged).
    - errorCount > 0 -> 1 (unchanged).
    - suppressedInGraphErrorCount > 0 -> 1 (coverage-incomplete; reuse 1, ngc parity).
    - else 0.
  </behavior>
  <action>
    Expand the `toExitCode` input `Pick` to include `suppressedInGraphErrorCount` and add the `suppressedInGraphErrorCount > 0 -> 1` branch after the errorCount branch. Reuse exit code `1` (Open Q 2 resolution -- a distinct code is not worth breaking ngc parity; the live Nx path already fails via evaluateResult). Add a `// ponytail:` note that the maxWarnings-gated suppressed-warning case and the templateCheckAborted / zero-root-names coverage triggers are handled by the LIVE `evaluateResult` path; mirror them here when the deferred CLI gains a live consumer + a maxWarnings option (toExitCode has no live consumer today). Extend `exit-codes.spec.ts`: `{ errorCount: 0, suppressedInGraphErrorCount: 2 }` -> 1; `{ errorCount: 0, suppressedInGraphErrorCount: 0 }` -> 0; the infra + errorCount branches unchanged.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- exit-codes</automated>
  </verify>
  <acceptance_criteria>
    - `toExitCode({ errorCount: 0, suppressedInGraphErrorCount: 2 })` returns 1.
    - The infra (2) and errorCount (1) and clean (0) branches are unchanged.
    - The ponytail note documents the deferred maxWarnings/abort/zero-root triggers.
    - `npx nx test angular-typechecker -- exit-codes` green.
  </acceptance_criteria>
  <done>Coverage-incomplete maps to exit 1 in the deferred-CLI policy, with the deferred triggers documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CoreResult counts -> pass/fail verdict + exit code | the verdict is the security control: it must never report clean while a first-party diagnostic was dropped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | Repudiation (false clean verdict) | an unwired suppressed count / a baked-in warning decision | mitigate | evaluateResult AND toExitCode both READ the suppressed counts; the warning decision is late-bound with the real maxWarnings (no silent pass under maxWarnings:0); HARD by default |
| T-17-10 | Tampering (whole-program abort hides survivors' errors) | templateCheckAborted not verdict-affecting | mitigate | FM-9 fold: templateCheckAborted present -> coverage-incomplete (success:false) |
| T-17-11 | Repudiation (a zero-file leaf reads clean) | zero-root-names leaf advisory-only | mitigate | a skippedReferences zero-root-names entry -> coverage-incomplete; a clean sibling can no longer make the solution read clean |
| T-17-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | N/A -- no package installs this phase |
</threat_model>

<verification>
- `npx nx test angular-typechecker -- evaluate-result exit-codes` green.
- Per wave merge: full suite + build + lint + format:check (CI gates).
</verification>

<success_criteria>
- evaluateResult fails on any first-party coverage loss with a late-bound warning decision + outcome discriminant.
- toExitCode maps coverage-incomplete (suppressedInGraphErrorCount>0) to 1.
- Clean runs remain success:true / clean / 0.
</success_criteria>

<output>
Create `.planning/phases/17-input-set-membership-boundary-layout-support/17-04-SUMMARY.md` when done.
</output>
