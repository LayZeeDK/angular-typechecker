---
phase: 06-full-e2e-matrix-ci
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
autonomous: true
requirements: [CI-01]
user_setup: []

must_haves:
  truths:
    - "The release.yml publish job carries an `if: startsWith(github.ref, 'refs/tags/angular-typechecker@')` ref gate so it cannot fire on a non-tag ref even if a trigger is broadened later (additive defense-in-depth)"
    - "The change touches ONLY the publish job's `if:` -- the OIDC/provenance/permissions/environment model is byte-for-byte unchanged (registry-url retained, contents:read top-level, id-token:write only, npm-publish environment, NODE_AUTH_TOKEN unset, NPM_CONFIG_PROVENANCE true, all actions 40-char SHA-pinned, persist-credentials:false)"
    - "The release-hygiene regression spec stays green and `nx release --dry-run` still previews a clean version + changelog"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "the publish-job ref gate (RD-07 Option A) added to the otherwise-frozen workflow"
      contains: "startsWith(github.ref, 'refs/tags/angular-typechecker@')"
  key_links:
    - from: ".github/workflows/release.yml publish job"
      to: "the tag-ref gate (act-testable + belt-and-suspenders)"
      via: "if: startsWith(github.ref, 'refs/tags/angular-typechecker@')"
      pattern: "startsWith\\(github.ref"
    - from: ".github/workflows/release.yml"
      to: "the release-hygiene regression invariants"
      via: "the install-e2e release-hygiene.int.spec must stay green"
      pattern: "release-hygiene"
---

<objective>
Add the RD-07 (Option A, user-approved) ref gate to `release.yml`'s publish job:
`if: startsWith(github.ref, 'refs/tags/angular-typechecker@')`. This is the ONLY permitted change to
the Phase-5.1-FROZEN workflow. It is additive defense-in-depth (the publish job cannot fire on a
non-tag ref even if a trigger is broadened later), self-documents intent, and -- critically -- is
what enables `act` to discriminate tag-push (publish SELECTED) from branch-push (publish SKIPPED) in
06-05, because act does NOT evaluate `on:` filters, only the event name + `if:`.

Purpose: close the act tag-vs-branch discrimination contract (RD-05) AND harden the publish gate,
WITHOUT disturbing the OIDC/provenance/permissions/environment model that 05/05.1 proved.

Output: a one-line `if:` added to the publish job + re-verification that nothing else moved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-full-e2e-matrix-ci/06-RESEARCH.md
@.planning/phases/06-full-e2e-matrix-ci/06-CONTEXT.md
@AGENTS.md

<interfaces>
<!-- The frozen publish job shape (verified this session from .github/workflows/release.yml).
     The ONLY edit is adding the `if:` to the publish job, alongside its existing keys. -->

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: npm-publish          # required-reviewer gate (load-bearing name; matches npm TP)
    permissions:
      id-token: write                 # OIDC only; NO contents: write
    # <-- ADD HERE: if: startsWith(github.ref, 'refs/tags/angular-typechecker@')
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1 (persist-credentials: false)
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0 (registry-url retained -- REQUIRED for OIDC detection)
      ...
      - run: npx nx release publish   # NPM_CONFIG_PROVENANCE: true; NODE_AUTH_TOKEN deliberately UNSET

on:
  push: { tags: ['angular-typechecker@*'] }   # the PRIMARY real gate -- stays; the if: is belt-and-suspenders
  workflow_dispatch: {}

# release-hygiene invariants (e2e/.../install-e2e/src/release-hygiene.int.spec.ts) that MUST stay green:
#   - every `uses:` ref is a 40-char SHA   - NOT contains 'NODE_AUTH_TOKEN'
#   - contents: read present; contents: write ABSENT   - id-token: write present
#   - environment present   - persist-credentials: false   - NPM_CONFIG_PROVENANCE: true
#   - registry-url https://registry.npmjs.org/ retained   - NOT 'pull_request_target'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: add the publish-job ref gate + re-verify the frozen model is intact</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - .github/workflows/release.yml (the full frozen workflow + its threat-model header -- the `if:` goes on the publish job, between `permissions: { id-token: write }` and `steps:`; do NOT touch the trigger block, permissions, environment, setup-node registry-url, or the publish step env)
    - e2e/angular-typechecker-install-e2e/src/release-hygiene.int.spec.ts (the regression invariants this change must not break: 40-char SHA pins, NOT 'NODE_AUTH_TOKEN', contents:read / no contents:write, id-token:write, environment, persist-credentials:false, NPM_CONFIG_PROVENANCE true, registry-url retained, NOT 'pull_request_target')
    - 06-CONTEXT.md RD-07 (Option A, user-approved -- the exact gate string + the "MUST re-verify" list) and 06-RESEARCH.md (release.yml is the SHA + envelope source)
    - AGENTS.md "do NOT re-enable createRelease / git.push: false" landmine + the release mechanics (so the edit does not drift into release-config territory)
  </read_first>
  <action>
    Edit ONLY `.github/workflows/release.yml`: add the single line
    `if: startsWith(github.ref, 'refs/tags/angular-typechecker@')` to the `publish` job, at job level
    (a sibling of `runs-on`/`environment`/`permissions`/`steps`), placed for readability after the
    `permissions:` block and before `steps:`. Add a brief ASCII-only comment line directly above it
    explaining it is additive defense-in-depth + act-discrimination (do NOT introduce the bare literal
    tokens `pull_request_target` / `contents: write` / `NODE_AUTH_TOKEN` / `@vN` in the comment -- the
    release-hygiene spec does string checks; reword as in the existing threat-model comments).

    Change NOTHING ELSE: keep the `on: push: tags:` block (the PRIMARY real gate), the top-level
    `permissions: contents: read`, the publish job's `environment: npm-publish` + `permissions:
    id-token: write`, the setup-node `registry-url` (REQUIRED for OIDC detection -- do NOT drop it),
    the `NPM_CONFIG_PROVENANCE: true`, and the UNSET auth token. All `uses:` refs stay 40-char
    SHA-pinned.

    Then re-verify the frozen model is intact:
    1. The release-hygiene regression spec stays green.
    2. A `nx release --dry-run` previews a clean version + changelog and writes nothing (the AGENTS.md
       0.x-adjustment note applies; this is a sanity check, not a release).

    This task does NOT cut a release and does NOT push anything.
  </action>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && node -e "const y=require('fs').readFileSync('.github/workflows/release.yml','utf8'); const active=y.split('\n').filter(l=>!/^\s*#/.test(l)).join('\n'); const refs=[...active.matchAll(/uses:\s*\S+@(\S+)/g)].map(m=>m[1].trim()); const ok=/if:\s*startsWith\(github\.ref,\s*'refs\/tags\/angular-typechecker@'\)/.test(active) && refs.length>0 && refs.every(r=>/^[0-9a-f]{40}$/.test(r)) && !active.includes('NODE_AUTH_TOKEN') && /id-token:\s*write/.test(active) && !/contents:\s*write/.test(active) && /registry-url:\s*https:\/\/registry\.npmjs\.org/.test(active) && !active.includes('pull_request_target'); process.exit(ok?0:1)" && npx nx run angular-typechecker-install-e2e:test 2>&1 | rg -q "release-hygiene" && echo RELEASE_GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/release.yml` publish job contains exactly `if: startsWith(github.ref, 'refs/tags/angular-typechecker@')` at job level
    - the active (comment-stripped) YAML still has: every `uses:` a 40-char SHA, NO `NODE_AUTH_TOKEN`, `id-token: write`, NO `contents: write`, `registry-url: https://registry.npmjs.org/`, NO `pull_request_target`
    - the `on: push: tags: ['angular-typechecker@*']` trigger and `workflow_dispatch` are unchanged; `release.git.push:false` + `createRelease:false` in nx.json are untouched (not re-enabled)
    - `npx nx run angular-typechecker-install-e2e:test` passes (release-hygiene regression spec green)
    - `npx nx release --dry-run` previews a clean version + changelog and writes nothing (run manually; record the previewed version in the SUMMARY)
  </acceptance_criteria>
  <done>The publish-job ref gate is present; the release-hygiene spec is green; the OIDC/provenance/permissions/environment model is byte-for-byte intact; a dry-run previews clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tag-push ref -> publish job | the OIDC publish is the highest-value action in the repo; the `if:` gate is an additive ref check on top of the primary `on: push: tags:` filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Elevation | the publish job fires on a non-tag ref if a trigger is later broadened | mitigate | `if: startsWith(github.ref, 'refs/tags/angular-typechecker@')` -- the publish step is unreachable unless the ref is a release tag, independent of the `on:` block |
| T-06-04-02 | Tampering | a careless edit silently weakens the OIDC/provenance/permissions model while "just adding the if:" | mitigate | the release-hygiene regression spec asserts every invariant (SHA pins, no token, no contents:write, registry-url retained, no pull_request_target); the verify re-runs it; the change is a single job-level `if:` line |

> The gate is additive defense-in-depth and must NOT alter the OIDC/provenance/permissions/environment model -- the release-hygiene spec stays green is the proof.
</threat_model>

<verification>
- The release-hygiene regression spec is green (`nx run angular-typechecker-install-e2e:test`).
- `nx release --dry-run` previews a clean version + changelog and writes nothing.
- The act tag-vs-branch discrimination (06-05) depends on this `if:`; it is asserted there.
</verification>

<success_criteria>
- The publish job carries the RD-07 ref gate; the rest of `release.yml` is byte-for-byte frozen.
- The OIDC/provenance/permissions/environment model is provably unchanged (release-hygiene green).
- No release is cut and nothing is pushed.
</success_criteria>

<output>
Create `.planning/phases/06-full-e2e-matrix-ci/06-04-SUMMARY.md` when done.
</output>
