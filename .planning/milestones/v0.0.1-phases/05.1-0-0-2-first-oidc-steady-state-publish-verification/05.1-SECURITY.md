---
phase: 05.1-0-0-2-first-oidc-steady-state-publish-verification
slug: 0-0-2-first-oidc-steady-state-publish-verification
audit: security
audited: 2026-06-29
asvs_level: 1
block_on: high
threats_total: 9
threats_closed: 9
threats_open: 0
status: verified
created: 2026-06-29
---

# Phase 05.1 -- Security

> Per-phase security contract: threat register, accepted risks, and audit trail.
> Result: SECURED -- 9/9 declared threats CLOSED; 0 open at or above `high`.

This is a RELEASE/CI-config + docs phase. No plugin source changed; the threat
surface is the supply-chain release pipeline (`.github/workflows/release.yml`, the
`nx release` config, the live npm publish). The only delta versus the 05-05 seed is
OIDC auth replacing the one-time seed token -- which REMOVES the leaked/standing-token
vector rather than adding surface. Every threat in the PLAN.md `<threat_model>` was
verified against the IMPLEMENTED artifact (config `file:line`, the regression spec,
git lineage, or live `npm view` registry state) -- not documentation or intent.

> NOTE: `angular-typechecker@0.0.2` is ALREADY PUBLISHED LIVE on npm via tokenless
> OIDC. Independently re-confirmed during this audit: `npm view
angular-typechecker@0.0.2 --json` -> `version 0.0.2`, `dist.attestations.
provenance.predicateType = https://slsa.dev/provenance/v1`, `_npmUser = "GitHub
Actions <npm-oidc-no-reply@github.com>"` (the synthetic publisher npm assigns ONLY
> for a tokenless Trusted-Publisher publish; a token-authenticated publish records the
> human npm user), `_npmVersion 11.17.0` (>= 11.5.1 OIDC floor), `_nodeVersion
24.17.0`; `dist-tags.latest = 0.0.2`. This is a retroactive audit of a shipped,
> immutable artifact.

---

## Trust Boundaries

Reused from `05-SECURITY.md` (25/25 closed). The supply-chain surface (release.yml,
the npm publish) is UNCHANGED; the only delta is OIDC auth replacing the one-time seed
token. No new boundary is introduced.

| Boundary                                      | Description                                                                                                                                                       | Data Crossing                                        |
| --------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| GitHub OIDC id-token -> npm Trusted Publisher | The TP must trust the EXACT workflow filename (`release.yml`) + environment (`npm-publish`), not the whole repo (TanStack vector). Tokenless; no standing secret. | Short-lived OIDC id-token; npm publish authorization |
| git tag push -> privileged release run        | Trigger is tag-push (+ `workflow_dispatch`) only; NO `pull_request_target` (s1ngularity vector).                                                                  | Tag ref; privileged `id-token: write` job context    |
| published version immutability                | An npm version is irreversible (immutable, name claimed) -- hence human-gated (B-01).                                                                             | Published tarball + SLSA provenance attestation      |
| curated public artifact -> npm/GitHub         | CHANGELOG / Release notes must not leak internal GSD scopes, the work email, or private prior-art.                                                                | Public release notes / changelog text                |

---

## Threat Register

All nine declared threats have disposition `mitigate`. Each is CLOSED with concrete
evidence at the actual control point.

| Threat ID | Category               | Component                                         | Disposition | Mitigation (evidence)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Status |
| --------- | ---------------------- | ------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| T-05.1-01 | Information Disclosure | re-introduced standing npm token                  | mitigate    | release.yml is OIDC-only: `NODE_AUTH_TOKEN` appears ONLY on comment lines (`release.yml:88`), no active assignment; no `secrets.NPM*` reference active anywhere; `_authToken` only in a comment (`release.yml:58`). `NPM_SEED_TOKEN` absent from `gh secret list`. Enforced by `release-hygiene.int.spec.ts:164-173`, which runs `stripCommentLines()` BEFORE `expect(workflow).not.toContain('NODE_AUTH_TOKEN')` -- a comment can neither satisfy nor break the gate. LIVE PROOF: `npm view` `_npmUser = "GitHub Actions <npm-oidc-no-reply@github.com>"` -- the OIDC synthetic user, proving no token authenticated the 0.0.2 publish.                                                                                                                              | closed |
| T-05.1-02 | Spoofing / Tampering   | Trusted Publisher trust scope                     | mitigate    | Repo-observable half present: `release.yml:43` `environment: npm-publish` and the workflow filename `release.yml` are the EXACT strings the TP must bind to (the seed-complete comment `release.yml:85-87` records the TP scope: repo `LayZeeDK/angular-typechecker`, workflow `release.yml`, env `npm-publish`). `package.json:21-24` `repository.url = git+https://github.com/LayZeeDK/angular-typechecker.git` byte-matches the TP (LayZeeDK casing). The successful tokenless OIDC publish (`_npmUser` = OIDC user) corroborates an exact-match TP. npm-account-side TP registration is operator-attested; all repo signals are consistent and none contradict.                                                                                                   | closed |
| T-05.1-03 | Elevation of Privilege | unauthorized / accidental publish                 | mitigate    | `release.yml:43` `environment: npm-publish` (required-reviewer gate on the only irreversible action, B-01/D-10); publish job re-grants ONLY `id-token: write` (`release.yml:47`) with NO `contents: write`. Asserted by `release-hygiene.int.spec.ts:119-128` (id-token:write present, contents:write absent) + `:130-136` (environment present). Human approval occurred at execution (SUMMARY); the autonomous chain stopped at ready-to-approve. The live publish having succeeded inside that environment confirms the gate was cleared by a human.                                                                                                                                                                                                               | closed |
| T-05.1-04 | Tampering              | untrusted-PR code in a privileged run             | mitigate    | `release.yml:27-31` triggers on `push.tags: ['angular-typechecker@*']` + `workflow_dispatch` ONLY; `git grep -c "pull_request_target"` = 0. Top-level `release.yml:33-34` `permissions: contents: read`. Regression-guarded `release-hygiene.int.spec.ts:100-117` (`not.toContain('pull_request_target')` + `contents:\s*read`). Unchanged this phase.                                                                                                                                                                                                                                                                                                                                                                                                                | closed |
| T-05.1-05 | Tampering              | mutable action tag repointed to malware           | mitigate    | Both `uses:` refs are full 40-char SHA pins: `release.yml:49` `actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1`, `:52` `actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0`. Asserted `release-hygiene.int.spec.ts:138-154` (every `uses:` ref matches `^[0-9a-f]{40}$`). Freshness: `.github/dependabot.yml:7` `package-ecosystem: github-actions`. Unchanged this phase.                                                                                                                                                                                                                                                                                                                                                       | closed |
| T-05.1-06 | Repudiation            | unverifiable build origin                         | mitigate    | `release.yml:79` `NPM_CONFIG_PROVENANCE: true` + `:47` `id-token: write` + manifest `package.json:51-54` `publishConfig.provenance: true`. Asserted `release-hygiene.int.spec.ts:164-169` (`NPM_CONFIG_PROVENANCE:\s*true`). LIVE PROOF: `npm view angular-typechecker@0.0.2 --json` -> `dist.attestations.provenance.predicateType = https://slsa.dev/provenance/v1`. Provenance is independent of auth mode.                                                                                                                                                                                                                                                                                                                                                        | closed |
| T-05.1-07 | Information Disclosure | secrets/PII leak in the public changelog/Release  | mitigate    | `CHANGELOG.md:5-14` 0.0.2 entry is hand-curated in the 0.0.1 style: ASCII-only, no GSD plan-id scopes (`git grep -nE "docs\(\|\(05\.1-\|\[#[0-9]" CHANGELOG.md` = 0), no `consensus.dk` work email (`git grep -c "consensus.dk"` across CHANGELOG/package.json/README = 0). Trailing `[0.0.2]:` release-tag link present (`CHANGELOG.md:38`). No private prior-art (Connect) specifics. GitHub Release notes derive from this curated CHANGELOG.                                                                                                                                                                                                                                                                                                                      | closed |
| T-05.1-08 | Tampering              | reactive registry-url drop weakening the workflow | mitigate    | The reactive drop (D-04) was NOT applied -- `registry-url: https://registry.npmjs.org/` is RETAINED (`release.yml:62`), and the corrected inline comment (`release.yml:55-61`) documents that the real 0.0.2 failure cause was the Trusted Publisher never being saved (NOT an empty `_authToken`), and that registry-url is REQUIRED for OIDC detection on npm >= 11.5.1. Had it been applied, it would remove ONLY the non-asserted `registry-url` line: `release-hygiene.int.spec.ts` makes NO assertion on `registry-url` (confirmed by full read), so all hardening invariants (SHA pins, persist-credentials:false, contents:read, id-token:write, no token) stay enforced and the gate stays green. All hardening invariants are intact in the as-is workflow. | closed |
| T-05.1-SC | Tampering              | npm/pip/cargo installs                            | mitigate    | No package installs in this phase. `git diff angular-typechecker@0.0.1 angular-typechecker@0.0.2 -- packages/angular-typechecker/package.json` shows ONLY the `version` field changed (`0.0.1` -> `0.0.2`); the `dependencies` (`@nx/devkit 23.0.1`, `tslib ^2.3.0`) and `peerDependencies` (`@angular/compiler-cli ^22.0.0`, `typescript >=6.0.0 <6.1.0`) blocks are byte-identical. The 0.0.2 tag commit (`b5c84eb`) touches only `CHANGELOG.md` + `package.json` (version). The only `npm i -g npm@latest` is in-CI workflow text (`release.yml:71`), not a plan-time install. No [ASSUMED]/[SUS] packages -> no blocking legitimacy checkpoint required.                                                                                                          | closed |

_Status: open / closed_
_Disposition: mitigate (implementation required) / accept (documented risk) / transfer (third-party)_

---

## Accepted Risks Log

No accepted risks. All nine declared threats carry disposition `mitigate` and are
CLOSED with code/config/live-registry evidence.

---

## Operational controls -- attestation basis

T-05.1-02's Trusted Publisher trust-scope registration is an npm-account-side
operation NOT readable from the repository. It is closed with all repo-observable
signals consistent with the declared mitigation:

- The steady-state `release.yml` is OIDC-only (no active `NODE_AUTH_TOKEN`; `git grep`
  count of active assignments = 0).
- The live 0.0.2 publish authenticated as the npm OIDC synthetic user (`_npmUser =
"GitHub Actions <npm-oidc-no-reply@github.com>"`) with SLSA v1 provenance -- only
  reachable through the `id-token: write` OIDC / Trusted-Publisher path.
- The workflow names the EXACT required-reviewer environment (`npm-publish`) and EXACT
  filename (`release.yml`) the TP must bind to; `repository.url` byte-matches the TP
  (LayZeeDK casing).
- `NPM_SEED_TOKEN` is deleted (`gh secret list` empty of it).

No repo evidence CONTRADICTS the operator's attestation. If a future audit finds the
steady-state workflow re-introducing a standing token, or the published provenance
dropping, T-05.1-01 / T-05.1-06 flip to OPEN and ESCALATE.

---

## Threat-flags reconciliation (from SUMMARY)

`05.1-01-SUMMARY.md` has NO `## Threat Flags` heading (confirmed via `git grep -i
"Threat Flags"`). The phase changed release/CI config + docs only and introduced no
new network endpoint, auth path, or trust-boundary schema change beyond the
in-register supply-chain surface (T-05.1-01..08 + T-05.1-SC). The SUMMARY's documented
deviations (createRelease force-push reconciliation; the registry-url ENEEDAUTH
detour; the TP-never-saved root cause; the cosmetic always-auth strip) all map onto
existing register entries (T-05.1-08 / T-05.1-02 / T-05.1-01) and the regression spec.

**Unregistered flags:** none. No new attack surface appeared during implementation
that lacks a mapped threat ID.

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By                        |
| ---------- | ------------- | ------ | ---- | ----------------------------- |
| 2026-06-29 | 9             | 9      | 0    | gsd-security-auditor (Claude) |

---

## Sign-Off

- [x] All threats have a disposition (all `mitigate`)
- [x] Accepted risks documented in Accepted Risks Log (none -- all mitigated)
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-06-29
