---
quick_id: 260721-vm1
type: quick
slug: fix-red-ci-fallow-job-reduce-buildruleme
files_modified:
  - packages/angular-typechecker/src/core/sarif-report.ts

must_haves:
  truths:
    - "The CI `fallow` job (`npx fallow audit --format human --base origin/main`, default thresholds) no longer flags a complexity finding on `packages/angular-typechecker/src/core/sarif-report.ts` -- the `buildRuleMeta` CRAP 37.1 / 11-cyclomatic finding is gone."
    - "The refactor is STRICTLY behavior-preserving: the SARIF output is byte-identical (the drift-lock `machine-reporters-sarif.integration.spec.ts` passes unchanged)."
    - "Only `sarif-report.ts` is modified; `formatSarifReport`'s PASS-2 location handling is UNTOUCHED (that is reworked separately by gap G-35-01)."
  artifacts:
    - "packages/angular-typechecker/src/core/sarif-report.ts (buildRuleMeta split into per-family helpers)"
---

<objective>
The CI `fallow` complexity gate flags one function -- `buildRuleMeta` in
`packages/angular-typechecker/src/core/sarif-report.ts` (11 cyclomatic, 8 cognitive,
56 lines, CRAP 37.1, over fallow's default threshold) -- turning the required `ci`
aggregate red on PR #55. Reduce its complexity by extracting each family branch into a
small dedicated helper, strictly preserving behavior. This is a maintenance fix,
independent of gap G-35-01 (which reworks the file-less-location handling in a
DIFFERENT function, `formatSarifReport`).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Extract per-family metadata builders from buildRuleMeta</name>
  <files>packages/angular-typechecker/src/core/sarif-report.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/sarif-report.ts (the whole file; focus on buildRuleMeta lines ~235-290 and its four family branches: extended-diagnostics, tool, template-type-check, typescript)
    - packages/angular-typechecker/src/core/machine-reporters-sarif.integration.spec.ts (the drift-lock -- proves byte-identical SARIF; must pass unchanged)
  </read_first>
  <action>
Refactor `buildRuleMeta(record, family)` so each family branch becomes its own small
helper function, and `buildRuleMeta` becomes a thin dispatch. Concretely:

- Add four helpers, each returning a `RuleMeta`, each computing exactly what the
  corresponding branch computes today (COPY the existing branch bodies verbatim -- the
  family literal, level, shortDescription, helpUri, helpText, and ALL `??` fallbacks
  must be byte-identical):
    - `buildExtendedRuleMeta(level, ruleId)` -- the `family === 'extended-diagnostics'` branch (uses `EXTENDED_BY_RULE_ID`, `HELP_URI_BASE`, `INFORMATION_URI`).
    - `buildToolRuleMeta(level, ruleId)` -- the `family === 'tool'` branch (uses `TOOL_RULE_TEXT`, `INFORMATION_URI`).
    - `buildTemplateRuleMeta(level, ruleId)` -- the `family === 'template-type-check'` branch (uses `TEMPLATE_TYPE_CHECK_HELP_URI`).
    - `buildTypeScriptRuleMeta(level, ruleId)` -- the final `'typescript'` branch (uses `TYPESCRIPT_HELP_URI`).
  Each helper sets the `family` field to its own literal in the returned object, so the
  RuleMeta is identical to today's branch output.
- Rewrite `buildRuleMeta` to: compute `level = toSarifLevel(record.severity)` and
  `ruleId = record.code` once, then dispatch -- `if (family === 'extended-diagnostics') { return buildExtendedRuleMeta(level, ruleId); }`,
  the same for `'tool'` and `'template-type-check'`, and `return buildTypeScriptRuleMeta(level, ruleId);`
  for the `'typescript'` fall-through. This drops `buildRuleMeta`'s cyclomatic count under
  the threshold while each helper is trivially simple.
- Keep every existing comment's intent; move the per-branch explanatory comments onto
  their new helper functions. Preserve the module header and all other functions
  (`formatSarifReport`, `toSarifLevel`, `fingerprintOf`) UNCHANGED. Do NOT touch
  `formatSarifReport`'s PASS-2 location ternary (G-35-01 territory).
- Follow project conventions: braces on all control-flow bodies, blank lines around
  control flow / returns, no non-ASCII, singleQuote.

If (and only if) the extraction still does not clear fallow's threshold, fall back to a
documented `.fallowrc.jsonc` complexity entry explaining why the family dispatch is
acceptable -- but the extraction should suffice; prefer it.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker</automated>
    <automated>npx nx run-many -t lint</automated>
    <automated>npx nx format:check</automated>
    <automated>git fetch --quiet origin main; npx fallow audit --format human --base origin/main; echo "fallow exit: $?"</automated>
  </verify>
  <done>`npx fallow audit --base origin/main` reports NO complexity finding on sarif-report.ts; `nx test angular-typechecker` (incl. the drift-lock integration spec) + lint + format:check are green; only sarif-report.ts changed.</done>
  <acceptance_criteria>
    - `buildRuleMeta` is a thin family dispatch; the four family metadata builders are separate helpers with byte-identical outputs.
    - `npx nx test angular-typechecker` passes, including `machine-reporters-sarif.integration.spec.ts` UNCHANGED (proves byte-identical SARIF).
    - `npx nx run-many -t lint` (maxWarnings:0) and `npx nx format:check` pass.
    - `npx fallow audit --format human --base origin/main` no longer emits the `sarif-report.ts` complexity finding.
    - `git diff --stat` shows ONLY `packages/angular-typechecker/src/core/sarif-report.ts`; `formatSarifReport` PASS-2 location handling untouched.
  </acceptance_criteria>
</task>

</tasks>

<output>
Create `.planning/quick/260721-vm1-fix-red-ci-fallow-job-reduce-buildruleme/260721-vm1-SUMMARY.md` when done, with `status: complete` in the frontmatter.
</output>
