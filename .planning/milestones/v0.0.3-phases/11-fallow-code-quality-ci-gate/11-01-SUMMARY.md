---
phase: 11-fallow-code-quality-ci-gate
plan: 01
subsystem: infra
tags: [fallow, dead-code, ci-gate, jsonc-config, nx, dependency-hygiene]

# Dependency graph
requires:
  - phase: 10-drift-hardening-maintainability
    provides: the drift tripwire (compiler-cli-types.drift.ts) + the EmitFlags/UNKNOWN_ERROR_CODE contract-mirror shim whose import-graph-invisible nature produces the fallow false positives this config resolves
provides:
  - .fallowrc.jsonc resolving all current fallow dead-code findings (verdict pass / exit 0 on a 0-finding tree)
  - fallow@2.103.0 exact-pinned root devDependency (lockfile-pinned)
  - removal of the genuinely-unused @angular/forms prod dependency
affects: [11-02 (fallow CI job), release (root deps changed, published package surface unchanged)]

# Tech tracking
tech-stack:
  added: [fallow@2.103.0 (root devDependency)]
  patterns:
    - "Resolve-not-baseline: false positives suppressed STRUCTURALLY (entry/ignoreExports/overrides), genuine findings removed at source"
    - "One JSONC comment per config declaration naming the finding it resolves (IN-02/03/04, D-06/07/02)"
    - "Exact-pin the analyzer so a minor release cannot silently flip the gate; run via npx fallow (locked), never @latest"

key-files:
  created: [.fallowrc.jsonc]
  modified: [package.json, package-lock.json]

key-decisions:
  - "Removed @angular/forms (verified zero usage repo-wide) rather than ignoreDependencies it -- yields a fully clean 0-finding tree (D-07 verify-then-resolve -> remove)"
  - "Repo-root fixture glob fixtures/fault-isolation/** (NOT the package-prefixed path in CONTEXT.md D-04) -- the corrected path is the one that actually scopes off the 4 fixture findings"
  - "No duplicates/health blocks (D-09 absence == defaults); no IN-05/publishConfig suppression (D-08 does not reproduce in 2.103.0)"

patterns-established:
  - "Pattern 1: hand-authored .fallowrc.jsonc with documented structural suppressions as the central, auditable dead-code config (D-03)"
  - "Pattern 2: dead-code gate pinned new-only in config (audit.gate) for a deterministic verdict independent of CLI flags (D-02)"

requirements-completed: [QUAL-02, QUAL-03]

# Metrics
duration: ~12min
completed: 2026-06-30
---

# Phase 11 Plan 01: Fallow config + clean-tree resolution Summary

**Hand-authored .fallowrc.jsonc that resolves every current fallow@2.103.0 finding (drift tripwire entry, EmitFlags/UNKNOWN_ERROR_CODE shim suppression, fixture override, dev-dep off), an exact-pinned fallow root devDependency, and removal of the unused @angular/forms prod dep -- making `npx fallow audit --base origin/main` exit 0 on a 0-finding tree.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-06-30T02:33Z (approx)
- **Completed:** 2026-06-30T02:47Z (approx)
- **Tasks:** 3
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments
- `.fallowrc.jsonc` authored at repo root with all six declarations + the CORRECTED repo-root fixture glob, each carrying its documented JSONC comment.
- `fallow@2.103.0` exact-pinned as a root devDependency (no `^`/`~`); lockfile regenerated and resolves the locked version.
- `@angular/forms` removed from root `dependencies` (zero usage reconfirmed repo-wide); lockfile drops it entirely.
- **Acceptance proven:** `npx fallow audit --format json --base origin/main` exits 0 with `verdict: "pass"`, `dead_code_issues: 0`, `dead_code_has_errors: false`, `dead_code_introduced: 0`, `dead_code_inherited: 0`, `complexity_findings: 0`, `duplication_clone_groups: 0` (61 entry points; 56 Angular-plugin auto-detected + 1 manual_entry for the drift file).
- Engine regression backstop green: `npx nx run-many -t typecheck-drift test -p angular-typechecker` passed (26 test files, 147 tests) -- the `@angular/forms` removal did not regress the engine.

## Task Commits

Each task was committed atomically:

1. **Task 1: Author .fallowrc.jsonc resolving all current fallow findings** - `213ea66` (chore)
2. **Task 2: Pin fallow@2.103.0 as an exact root devDependency** - `bce8bec` (chore)
3. **Task 3: Remove unused @angular/forms, regenerate lockfile, prove the gate is clean** - `4b81ad8` (chore)

_Commit type rationale (AGENTS.md scope hygiene): this is a tooling/CI phase touching ONLY root files, NOT `packages/angular-typechecker/` source, so `chore` is used (no published-package version bump; hidden from the public changelog)._

## Files Created/Modified
- `.fallowrc.jsonc` (created) - fallow config: `entry` (drift tripwire), `ignoreExports` (UNKNOWN_ERROR_CODE), `rules.unused-dev-dependencies: off`, `overrides` (EmitFlags off on compiler-cli-types.ts; fixtures off), `audit.gate: new-only`.
- `package.json` (modified) - added `"fallow": "2.103.0"` to devDependencies (alphabetical); removed `"@angular/forms": "22.0.4"` from dependencies.
- `package-lock.json` (modified, tool-generated) - regenerated by `npm install`: adds fallow@2.103.0, drops @angular/forms.

## Decisions Made
- **@angular/forms removed (not ignored):** `git grep -n "@angular/forms" -- packages/ fixtures/ e2e/ tools/ apps/` returned exit 1 (no matches) -- zero genuine import/template/config references. The only remaining hits are root `package.json`/`package-lock.json` and `.planning/` docs that describe the removal. Per D-07 verify-then-resolve, removal is correct and yields the fully clean 0-finding tree (the `ignoreDependencies` alternative would leave a perpetual reported finding with `has_errors: true`).
- **Corrected repo-root fixture glob** `fixtures/fault-isolation/**` used (per 11-RESEARCH.md / 11-PATTERNS.md correction), NOT the package-prefixed path written in 11-CONTEXT.md D-04 -- the package-prefixed glob matches nothing and would leave the 4 introduced fixture findings live.
- **No `duplicates`/`health` blocks and no IN-05 suppression** (D-09 / D-08): absence == defaults (repo is clean on both, so defaults are zero-cost regression tripwires); IN-05 (`publishConfig.provenance`) does not reproduce in 2.103.0 because fallow analyzes the root `package.json`, not the published manifest.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- A `node fs.readFileSync` against a Git-Bash `/tmp/...` path failed with ENOENT (Git Bash maps `/tmp` to a non-existent `D:\tmp` for the Node process). This was a transient path artifact during verification only -- it did NOT affect the audit, which had already reported `fallow-exit=0`. Re-ran the capture into the session scratchpad directory and confirmed the full `verdict: "pass"` JSON. No code or config change resulted.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- The dead-code gate is provably green (verdict pass / 0 findings), so Plan 11-02 can wire the dedicated `fallow` CI job into the `ci` aggregate and add the act-compat assertion against a gate that is already clean.
- `fallow` is installed and lockfile-pinned, so CI's `npm ci` + `npx fallow` will resolve the locked 2.103.0.
- Scope boundary respected: `packages/angular-typechecker/package.json` and `ci.yml`/`act-compat.sh` were NOT touched (those belong to Plan 11-02).

## Self-Check: PASSED

- FOUND: `.fallowrc.jsonc`
- FOUND: `.planning/phases/11-fallow-code-quality-ci-gate/11-01-SUMMARY.md`
- FOUND commit `213ea66` (Task 1)
- FOUND commit `bce8bec` (Task 2)
- FOUND commit `4b81ad8` (Task 3)

---
*Phase: 11-fallow-code-quality-ci-gate*
*Completed: 2026-06-30*
