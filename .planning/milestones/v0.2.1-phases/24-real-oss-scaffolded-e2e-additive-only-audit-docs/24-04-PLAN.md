---
phase: 24-real-oss-scaffolded-e2e-additive-only-audit-docs
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/package.json
  - packages/angular-typechecker/src/package-manifest.spec.ts
  - packages/angular-typechecker/eslint.config.mjs
  - .planning/PROJECT.md
  - CLAUDE.md
autonomous: true
gap_closure: true
requirements: [ACP-02]

must_haves:
  truths:
    - "The published plugin manifest declares `nx: \"^23.0.0\"` in `dependencies` (alongside the exact-pinned `@nx/devkit`), and does NOT declare `nx` in `peerDependencies`."
    - "`nx test angular-typechecker` passes: `package-manifest.spec.ts` now asserts `nx` IS a `^23.0.0` runtime dependency (and still absent from peerDependencies), with the header comment + it-titles restated to the new contract."
    - "`nx lint angular-typechecker` passes at maxWarnings:0: `@nx/dependency-checks` does not flag the newly declared (unimported) `nx` dependency as obsolete because `ignoredDependencies` now includes `nx`."
    - "The operative Dependencies constraint in PROJECT.md and CLAUDE.md states the new rule (`nx` IS a direct `^23.0.0` dependency, because yarn does not auto-install peers) with rationale; the historical STACK-research rows carry a dated `[v0.2.1 CORRECTION: ...]` note rather than a verbatim rewrite."
    - "AGENTS.md is unchanged (the nx-dependency rule is not stated in AGENTS.md, so no code-reviewed AGENTS.md edit is required)."
  artifacts:
    - path: "packages/angular-typechecker/package.json"
      provides: "The dependency-contract fix: nx declared as a direct runtime dependency"
      contains: "\"nx\": \"^23.0.0\""
    - path: "packages/angular-typechecker/src/package-manifest.spec.ts"
      provides: "The inverted guard: asserts nx IS a ^23.0.0 dependency and NOT a peer"
      contains: "^23.0.0"
    - path: "packages/angular-typechecker/eslint.config.mjs"
      provides: "ignoredDependencies includes nx so @nx/dependency-checks stays green"
      contains: "'nx'"
    - path: ".planning/PROJECT.md"
      provides: "The flipped Dependencies constraint (new rule + rationale)"
    - path: "CLAUDE.md"
      provides: "The flipped Constraints line + dated corrections on the STACK-research rows"
  key_links:
    - from: "packages/angular-typechecker/package.json"
      to: "@nx/devkit -> require('nx/src/devkit-exports')"
      via: "nx as a direct dependency (yarn installs direct deps; it does NOT auto-install the @nx/devkit peer)"
      pattern: "\"nx\":\\s*\"\\^23\\.0\\.0\""
    - from: "packages/angular-typechecker/eslint.config.mjs"
      to: "@nx/dependency-checks obsoleteDependency check"
      via: "ignoredDependencies: ['nx', '@angular-devkit/architect', 'rxjs']"
      pattern: "ignoredDependencies"
    - from: "packages/angular-typechecker/src/package-manifest.spec.ts"
      to: "packages/angular-typechecker/package.json"
      via: "readFileSync + JSON.parse manifest.dependencies.nx assertion"
      pattern: "dependencies.*nx"
---

<objective>
Fix the confirmed root cause of the CLI x yarn `ng add` failure at the PRODUCT level: `@nx/devkit/dist/index.js` does a top-level `require("nx/src/devkit-exports")`, and `nx` is `@nx/devkit`'s peerDependency. yarn (any version) does NOT auto-install peer deps (npm 7+/pnpm 8+ do), so a plain Angular CLI yarn workspace crashes with `Cannot find module 'nx/src/devkit-exports'` on `ng add angular-typechecker` / `ng run <project>:typecheck`. Declare `nx` as a direct `dependency` pinned to `^23.0.0` so every package manager installs it, and update the two enforcement guards (`package-manifest.spec.ts`, the `@nx/dependency-checks` lint) plus the contradicting docs so the new contract is enforced and explained.

Purpose: closes the yarn UX gap surfaced by the CLI x yarn e2e (`.planning/debug/cli-yarn-e2e-wrong-version.md`, 2nd-layer root cause). This is the LOCKED user decision (deliberately overriding the prior "never declare nx" rule, which assumed an always-Nx consumer -- false for a yarn Angular CLI consumer). Range `^23.0.0` (= `>=23.0.0 <24.0.0`) matches this repo's Nx-23-only official support and cannot drag in nx 22 or 24. Precedent: `@nxext/stencil` declares `nx` as a dependency. ACP-02: adding a `dependencies` entry is additive vs `angular-typechecker@0.2.0` (no API/schema/barrel change) -- confirmed by the post-execution additive-audit re-run.

Output: the fixed manifest + inverted guards + de-contradicted PROJECT.md/CLAUDE.md.

Wave/isolation rationale: sole Wave-1 gap-closure plan (single-plan wave -> run on the MAIN checkout with real node_modules, NOT a junctioned worktree). Although it edits `package.json` dependencies, `nx@23.0.1` already lives in the dev-repo `node_modules` (transitive dep + workspace devDep), so no per-worktree `npm ci` is needed to build/test/lint. It must land BEFORE Plan 24-05 so the globalSetup-built dist the e2e installs carries `nx` in its dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/cli-yarn-e2e-wrong-version.md
@.planning/phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-ADDITIVE-AUDIT.md

<interfaces>
<!-- Exact current-state anchors so the executor edits in place with no exploration. -->

packages/angular-typechecker/package.json -- current `dependencies` block:
  "dependencies": { "@nx/devkit": "23.0.1", "tslib": "^2.3.0" }
Target: add "nx": "^23.0.0" alongside @nx/devkit. `nx` is NOT in peerDependencies and must stay out of it.

packages/angular-typechecker/src/package-manifest.spec.ts -- the two nx-absent assertion sites to INVERT:
  ~lines 80-83, describe "plugin manifest compatibility contract (CMP-01 ... / D-14)":
    it('does NOT declare nx in dependencies or peerDependencies ...') {
      expect(manifest.dependencies ?? {}).not.toHaveProperty('nx');
      expect(manifest.peerDependencies ?? {}).not.toHaveProperty('nx');
    }
  ~lines 186-189, describe "plugin manifest Angular CLI install contract (ACP-01 / NGADD-01 RF-01 / D-07)":
    it('still does NOT declare nx (the optional peers must not reintroduce an explicit nx constraint)') {
      expect(manifest.dependencies ?? {}).not.toHaveProperty('nx');
      expect(manifest.peerDependencies ?? {}).not.toHaveProperty('nx');
    }
  Header comment ~lines 16-20 currently reads "`nx` declared by NO ONE (devkit's own peer carries the consumer's nx transitively -- declaring it ourselves double-constrains)".

packages/angular-typechecker/eslint.config.mjs -- the @nx/dependency-checks block (~lines 69-98):
  ignoredDependencies: ['@angular-devkit/architect', 'rxjs']   // target: add 'nx'
  The preceding comment (~lines 86-90) currently ends "`nx` is deliberately NOT declared here (declaring it would double-constrain the consumer's Nx)."

.planning/PROJECT.md line 194 == CLAUDE.md line 22 (IDENTICAL operative text):
  "- **Dependencies**: `@nx/devkit` as a pinned `dependency` (do NOT declare `nx`; devkit's peer carries it transitively); ..."

CLAUDE.md STACK-research rows to ANNOTATE (dated correction, do NOT rewrite the historical research):
  line ~36 headline: "... you never declare `nx` yourself."
  line ~46 `@nx/devkit` table row: "... you never declare `nx`."
  line ~55 `nx` table row: "Declared by no one in your package.json -- flows in via `@nx/devkit`'s peer."
  "What NOT to Use" row: "`nx` in `dependencies` OR `peerDependencies` of the published plugin".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare nx as a ^23.0.0 dependency + flip the two enforcement guards (atomic, unit+lint verifiable)</name>
  <files>packages/angular-typechecker/package.json, packages/angular-typechecker/src/package-manifest.spec.ts, packages/angular-typechecker/eslint.config.mjs</files>
  <read_first>
    - packages/angular-typechecker/package.json (the manifest -- current `dependencies` + `peerDependencies` blocks)
    - packages/angular-typechecker/src/package-manifest.spec.ts (the guard to invert -- the two nx-absent `it(...)` sites at ~80-83 and ~186-189 + the CMP-01 header comment at ~16-20)
    - packages/angular-typechecker/eslint.config.mjs (the `@nx/dependency-checks` `ignoredDependencies` array + its comment at ~69-98)
    - .planning/debug/cli-yarn-e2e-wrong-version.md (Resolution + the 2nd-layer root cause -- WHY nx must be a direct dependency, not a peer)
  </read_first>
  <action>
    Make the three edits ATOMIC (each guard enforces the same contract, so a partial change reddens `nx test` or `nx lint`):
    (1) In `packages/angular-typechecker/package.json`, add `"nx": "^23.0.0"` to the `dependencies` object alongside `"@nx/devkit": "23.0.1"` (keep `tslib`; preserve the existing key ordering/formatting -- devkit, nx, tslib alphabetical is fine). Do NOT add `nx` to `peerDependencies` and do NOT change `@nx/devkit`'s exact `23.0.1` pin.
    (2) In `packages/angular-typechecker/src/package-manifest.spec.ts`, INVERT both nx-absent assertions. At the CMP-01 site (~80-83): change the `it` title to state that `nx` IS declared as a `^23.0.0` dependency (and still not a peer), and change the body to `expect(manifest.dependencies?.['nx']).toBe('^23.0.0');` plus `expect(manifest.peerDependencies ?? {}).not.toHaveProperty('nx');`. At the ACP-01 site (~186-189): retitle to "declares nx as a ^23.0.0 dependency; the optional peers do not move it into peerDependencies" and assert the same (`dependencies.nx === '^23.0.0'`, still not a peer). Rewrite the header comment (~16-20) so it states the NEW contract: `nx` is declared as a direct `^23.0.0` runtime dependency because `@nx/devkit`'s entrypoint `require()`s `nx/src/devkit-exports` at load and yarn does NOT auto-install the peer (npm/pnpm do) -- range `^23.0.0` pins Nx-23-only support and cannot pull nx 22/24.
    (3) In `packages/angular-typechecker/eslint.config.mjs`, add `'nx'` to the `@nx/dependency-checks` `ignoredDependencies` array (making it `['nx', '@angular-devkit/architect', 'rxjs']`). Rewrite the now-false comment segment that says "`nx` is deliberately NOT declared here" to explain: `nx` IS declared as a direct dependency (so yarn/non-peer-auto-installing consumers get it) but is a runtime-transitive requirement never imported in `src/`, so without this ignore `@nx/dependency-checks` would flag it `obsoleteDependency` and fail `nx lint` at maxWarnings:0. Leave `checkVersionMismatches: false` and the `ignoredFiles` untouched.
  </action>
  <verify>
    <automated>NX_DAEMON=false npx nx test angular-typechecker --skip-nx-cache && NX_DAEMON=false npx nx lint angular-typechecker --skip-nx-cache && NX_DAEMON=false npx nx build angular-typechecker --skip-nx-cache</automated>
  </verify>
  <acceptance_criteria>
    - `packages/angular-typechecker/package.json` `dependencies` contains `"nx": "^23.0.0"`; `peerDependencies` still has NO `nx` key; `@nx/devkit` stays `"23.0.1"`.
    - `package-manifest.spec.ts` asserts `manifest.dependencies?.['nx'] === '^23.0.0'` at both former nx-absent sites and still asserts `nx` is not a peer; the `it` titles and header comment state the new contract (no surviving "does NOT declare nx" title).
    - `eslint.config.mjs` `ignoredDependencies` includes `'nx'`; the comment no longer says nx is deliberately not declared.
    - `nx test angular-typechecker` exits 0 (the inverted manifest guard passes); `nx lint angular-typechecker` exits 0 at maxWarnings:0 (BOTH lint rules stay green: `@nx/dependency-checks` no longer flags `nx` obsolete, AND `@nx/nx-plugin-checks` -- which also runs on package.json -- stays green, since `nx@^23.0.0` is satisfied by the installed `23.0.1` and is consistent with `@nx/devkit@23.0.1`; if `@nx/nx-plugin-checks` flags a version-consistency issue, reconcile the range rather than ignore it); `nx build angular-typechecker` exits 0.
    - The built dist manifest (`dist/packages/angular-typechecker/package.json`) carries `dependencies.nx === '^23.0.0'` after the build.
  </acceptance_criteria>
  <done>The shipped manifest declares nx as a ^23.0.0 dependency, and both standing guards (unit + lint) enforce it green.</done>
</task>

<task type="auto">
  <name>Task 2: De-contradict the docs -- flip the operative Dependencies constraint + date-annotate the STACK-research rows</name>
  <files>.planning/PROJECT.md, CLAUDE.md</files>
  <read_first>
    - .planning/PROJECT.md (Constraints section, the "Dependencies" bullet ~line 194)
    - CLAUDE.md (the identical Constraints "Dependencies" line ~22; the STACK-research headline ~36; the `@nx/devkit` table row ~46; the `nx` table row ~55; the "What NOT to Use" row "`nx` in `dependencies` OR `peerDependencies` of the published plugin")
    - .planning/debug/cli-yarn-e2e-wrong-version.md (the rationale to cite in the new rule text)
  </read_first>
  <action>
    Flip the OPERATIVE rule and annotate (do not rewrite) the historical research:
    (1) In `.planning/PROJECT.md` and `CLAUDE.md`, rewrite the identical Dependencies constraint bullet (PROJECT.md ~194 / CLAUDE.md ~22) so it states the NEW rule: `@nx/devkit` pinned exact as a `dependency` AND `nx` declared as a direct `dependency` at `^23.0.0`. Give the rationale inline: `@nx/devkit`'s entrypoint `require()`s `nx/src/devkit-exports` at load, and yarn does NOT auto-install peers (npm/pnpm do), so a yarn Angular CLI consumer needs `nx` present directly; range `^23.0.0` = Nx-23-only official support (cannot pull nx 22/24). Keep the `@angular/compiler-cli` + `typescript` peers and the `@nx/dependency-checks` policing note.
    (2) In `CLAUDE.md`, annotate each of the three embedded STACK-research rows and the "What NOT to Use" nx row with a dated inline note in the exact form `[v0.2.1 CORRECTION (2026-07-12): nx is now a direct ^23.0.0 dependency -- yarn does not auto-install the @nx/devkit peer; see PROJECT.md Constraints]`. Do NOT delete or rewrite the historical research prose itself -- append the bracketed correction so the original finding stays legible as history.
    (3) Confirm `AGENTS.md` does NOT contain a "declare nx" / "never declare nx" rule (it does not -- verified). Do NOT edit `AGENTS.md` (its own rule requires a code review for any change; there is nothing to change here).
  </action>
  <verify>
    <automated>rg -q -F "^23.0.0" .planning/PROJECT.md && rg -q -F "^23.0.0" CLAUDE.md && rg -q -F "v0.2.1 CORRECTION" CLAUDE.md && git diff --quiet AGENTS.md && echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - The Dependencies constraint in BOTH PROJECT.md and CLAUDE.md states `nx` is a direct `^23.0.0` dependency with the yarn-does-not-auto-install-peers rationale; neither still reads the operative "do NOT declare `nx`".
    - CLAUDE.md carries a dated `[v0.2.1 CORRECTION (2026-07-12): ...]` note on the headline row, the `@nx/devkit` row, the `nx` row, and the "What NOT to Use" nx row; the original research text is preserved beside each note.
    - `AGENTS.md` is byte-unchanged (no nx-declaration rule exists there to flip).
  </acceptance_criteria>
  <done>PROJECT.md/CLAUDE.md no longer contradict the shipped manifest; the historical research is annotated, not erased; AGENTS.md needs no code-reviewed edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published manifest -> consumer install | Declaring `nx` as a direct dependency widens the installed dependency tree for ALL consumers (a heavy transitive package now installs directly) |
| plugin package.json -> @nx/devkit require() | `@nx/devkit` `require()`s `nx/src/devkit-exports` at load; the fix guarantees `nx` is resolvable regardless of package manager |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-08 | Tampering (supply chain) | `nx` added to `dependencies` | accept | `nx@^23.0.0` is the SAME first-party Nrwl package already pulled transitively via `@nx/devkit`'s peer (npm/pnpm auto-install it today); making it a direct dep only ensures yarn consumers get it too. Range `^23.0.0` (>=23.0.0 <24.0.0) matches the officially-supported Nx-23-only stack and cannot introduce nx 22/24. No NEW package enters the tree. |
| T-24-09 | Denial of service | version-range skew | accept | The exact-pinned `@nx/devkit@23.0.1` peer on `nx` is `">= 22 <= 24 \|\| ^23.0.0-0"`; the declared `^23.0.0` is a strict subset, so no double-constraint conflict. `@nx/dependency-checks` `checkVersionMismatches:false` avoids an autofix rewrite of the range. |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install of a NEW package occurs in this plan; it edits a manifest string + guards + docs. `nx` was already audited/present in the dependency tree (RESEARCH.md Package Legitimacy Audit -- none `[ASSUMED]`/`[SUS]`). |

No `high`+ severity threat exists in this plan's surface (a manifest string, two guards, and docs). The one supply-chain consideration (T-24-08) is a no-op on the actual installed set because `nx` is already transitively present.
</threat_model>

<verification>
- `nx test angular-typechecker` -- the inverted `package-manifest.spec.ts` guard passes (nx IS a `^23.0.0` dependency, still not a peer).
- `nx lint angular-typechecker` -- `@nx/dependency-checks` green at maxWarnings:0 with `nx` in `ignoredDependencies`.
- `nx build angular-typechecker` -- the built dist manifest carries `dependencies.nx === '^23.0.0'`.
- Post-execution gates (NOT plan tasks -- run by the orchestrator/agents after execution):
  - Re-run the ADDITIVE-ONLY audit and UPDATE `24-ADDITIVE-AUDIT.md`: confirm (do NOT assume) that adding a `dependencies` entry is additive vs `angular-typechecker@0.2.0` -- it touches no public barrel (`src/index.ts`), no schema, and no executor id, so v0.2.1 (0.2.x) still holds and v0.3.0 is NOT triggered. Record the `dependencies` delta explicitly in the audit.
  - `gsd-verifier` re-verifies the phase goal (ACP-02).
</verification>

<success_criteria>
- ACP-02: `nx` is declared as a direct `^23.0.0` dependency; the manifest guard + lint enforce it; the additive-only charter still holds (dependency addition is additive vs 0.2.0, re-confirmed in the audit).
- A yarn Angular CLI consumer running `ng add angular-typechecker` now gets `nx` installed automatically (the product fix behind Plan 24-05's yarn e2e finalization).
</success_criteria>

<output>
Create `.planning/phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-04-SUMMARY.md` when done.
</output>
