---
phase: 32-verification-docs-additive-audit
plan: 2
type: execute
wave: 2
depends_on: ['32-01']
files_modified:
  - libs/test-util/src/lib/cli-e2e.ts
  - libs/test-util/src/index.ts
  - e2e/angular-typechecker-cli-e2e/src/cli-exit-codes.e2e.spec.ts
  - e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run.e2e.spec.ts
  - e2e/angular-typechecker-install-e2e/src/install-smoke.e2e.spec.ts
autonomous: true
requirements: [VER-03]
must_haves:
  truths:
    - "D-04: the SHIPPED tarball emits valid JSON + schema-valid SARIF through ALL three adapters -- the standalone CLI --format (angular-typechecker-cli-e2e), ng run <project>:typecheck --format (angular-typechecker-ng-cli-e2e), and the Nx executor --format (angular-typechecker-install-e2e); each EXTENDS an existing e2e spec (no new project, no ci.yml edit)."
    - "D-04: each adapter asserts (a) stdout-purity -- the stdout payload PARSES cleanly with no Nx/ng chrome or advisory text inside it; (b) the SARIF validates against the 2.1.0 schema (reusing the 32-01 validateSarif dev-only validator); (c) exit-code PARITY -- the identical literal exit code across human/json/sarif for the same input, including the coverage-incomplete errorCount===0 / success===false -> 1 case (the cardinal anti-false-pass)."
    - "runShim merges stdout+stderr, so a runShimSplit variant returning SEPARATE streams is required for the CLI stdout-purity proof (parse only .stdout)."
  artifacts:
    - libs/test-util/src/lib/cli-e2e.ts (runShimSplit + the --format parity/purity assertion)
    - e2e/angular-typechecker-cli-e2e/src (CLI --format assertions)
    - e2e/angular-typechecker-ng-cli-e2e/src (ng run --format assertions)
    - e2e/angular-typechecker-install-e2e/src (Nx executor --format assertions)
  key_links:
    - "validateSarif imported from @workspace/test-util (created in 32-01) -- ONE validator across integration + e2e."
    - "evaluateResult/toExitCode remain the SOLE verdict owners; exit-code parity across formats rests on the reporters being pure (CoreResult) => string."
    - "The CLI .bin shim yields TRULY pure stdout; nx run / ng run frame stdout -- extract the payload from the first '{' and assert no advisory text is INSIDE the parsed payload."
---

<objective>
VER-03: prove the INSTALLED (Verdaccio-published tarball) package emits valid JSON +
schema-valid SARIF through all three adapters -- the standalone CLI, `ng run`, and the
Nx executor -- asserting stdout-purity and the cardinal exit-code parity across
human/json/sarif on the shipped artifact.

Purpose: the integration tier (32-01) proves the source; VER-03 proves the SHIPPED
bits behave identically through the real package-manager shim / `nx run` / `ng run`.
Exit-code parity across formats on the tarball is the milestone's signature
never-silent proof.

Output: `runShimSplit` (separate streams) + a `--format` parity/purity assertion in
`cli-e2e.ts`, and `--format` assertions added to the three existing e2e projects.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-verification-docs-additive-audit/32-CONTEXT.md
@.planning/phases/32-verification-docs-additive-audit/32-RESEARCH.md

# The e2e extension points (read for the exact helpers/specs to extend):
@libs/test-util/src/lib/cli-e2e.ts
@libs/test-util/src/lib/ng-cli-e2e.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: runShimSplit + the standalone-CLI --format parity/purity assertion (cli-e2e)</name>
  <files>libs/test-util/src/lib/cli-e2e.ts, libs/test-util/src/index.ts, e2e/angular-typechecker-cli-e2e/src/cli-exit-codes.e2e.spec.ts</files>
  <read_first>
    - libs/test-util/src/lib/cli-e2e.ts (runShim merges stdout+stderr at line 81; assertShippedBinExitCodes lines 109-184 -- the 0/1/2 contract to extend with a --format loop)
    - e2e/angular-typechecker-cli-e2e/src/cli-exit-codes.e2e.spec.ts (how the npm/pnpm/yarn specs call assertShippedBinExitCodes + runShim)
    - .planning/phases/32-verification-docs-additive-audit/32-RESEARCH.md ("THE key VER-03 implementation detail: runShim merges stdout+stderr" and "The three assertions per adapter")
  </read_first>
  <action>
    Add `runShimSplit` to `cli-e2e.ts`: identical spawnSync invocation to `runShim`
    (same cwd/env/encoding/maxBuffer/shell-on-Windows options, same `result.error`
    throw, same `result.status ?? 2`) but return a `ShimResultSplit` with SEPARATE
    `stdout` and `stderr` fields (`result.stdout ?? ''` / `result.stderr ?? ''`) --
    do NOT concatenate. Export `runShimSplit` + `ShimResultSplit` from cli-e2e.ts and
    re-export both from `libs/test-util/src/index.ts`. Keep the existing `runShim`
    (stream-merged) unchanged -- the exit-code/code-substring assertions still use it.

    Add an exported `assertMachineFormatParity(tmp, env)` to cli-e2e.ts that, over the
    already-installed cli-consumer fixture, loops `format` over `['human','json','sarif']`:
    - CLEAN fixture: `runShimSplit(tmp, 'angular-typechecker', ['-c','tsconfig.json','--format',format], env)`;
      assert `code === 0` for EVERY format (parity); for `json` assert
      `JSON.parse(split.stdout)` does not throw (stdout-purity); for `sarif` assert
      `validateSarif(split.stdout).valid === true` (import validateSarif from
      `@workspace/test-util`), passing `.errors` as the message arg.
    - PLANTED TS2322 (reuse the COMPONENT_ANCHOR/COMPONENT_INJECTION + plant + finally-restore
      idiom already in assertShippedBinExitCodes): assert `code === 1` for ALL THREE
      formats (the anti-false-pass parity) and that the json/sarif payloads still
      parse/validate.
    Assert, per run, that the parsed json/sarif payload does NOT contain the advisory
    prefix or notice text that belongs on stderr (purity: advisories go to
    `split.stderr`, never into the payload).

    Call `assertMachineFormatParity(tmp, npmEnv)` from the npm cli-exit-codes spec
    (after the existing `assertShippedBinExitCodes` block, before teardown). Do NOT
    fork assertShippedBinExitCodes -- add the parity block alongside it.
  </action>
  <verify>
    <automated>npx nx e2e angular-typechecker-cli-e2e</automated>
  </verify>
  <acceptance_criteria>
    - `runShimSplit` returns separate `stdout`/`stderr`; `JSON.parse(runShimSplit(...).stdout)` succeeds where the stream-merged `runShim` would fail on advisory chatter.
    - `assertMachineFormatParity` asserts: exit code identical across human/json/sarif for the clean run (0) AND the planted TS2322 run (1); json stdout parses; sarif stdout is schema-valid via validateSarif; no advisory text inside either payload.
    - `nx e2e angular-typechecker-cli-e2e` is green.
  </acceptance_criteria>
  <done>The shipped CLI emits parseable JSON + schema-valid SARIF with pure stdout, and returns the identical exit code across all three formats for the same input, proven on the Verdaccio-installed tarball.</done>
</task>

<task type="auto">
  <name>Task 2: ng run --format purity + exit-code parity (ng-cli-e2e)</name>
  <files>e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run.e2e.spec.ts, libs/test-util/src/lib/ng-cli-e2e.ts</files>
  <read_first>
    - libs/test-util/src/lib/ng-cli-e2e.ts (createNgRun returns { stdout, code } with stdout = merged stdout+stderr; plant; the app/lib project names + planted codes)
    - e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run.e2e.spec.ts (how the npm ng-cli spec drives createNgRun over the scaffold)
    - .planning/phases/32-verification-docs-additive-audit/32-RESEARCH.md ("stdout-purity risk for the Nx / ng adapters" -- the framing-extraction guidance + Open Question 1)
  </read_first>
  <action>
    Extend the npm ng-cli e2e spec to add a `--format` block over the scaffolded
    `ng-cli-workspace` app project. FIRST, at execution time, CAPTURE a real
    `corepack? / npx ng run ng-cli-workspace:typecheck --format json` and inspect its
    stdout framing (Open Question 1 / assumption A2 -- do NOT assume; the exact Nx 23
    framing must be observed). createNgRun currently MERGES stdout+stderr, so for the
    payload-purity assertion add a stream-split variant OR run `ng run` with an
    output style that suppresses framing (e.g. append `--output-style=static` if it
    reduces prefixes -- verify during execution) so the payload is isolable.

    Assertions over the CLEAN app project:
    - json: extract the JSON payload from stdout (slice from the first `{`, since ng
      may prepend a task-echo line), `JSON.parse` it, and assert it has
      `formatVersion` + `diagnostics[]` + `summary`; assert NO advisory/notice text
      (the `angular-typechecker:` prefix, a coverage-incomplete notice) appears INSIDE
      the parsed payload.
    - sarif: extract likewise and assert `validateSarif(payload).valid === true`
      (import from `@workspace/test-util`).
    - exit-code parity: run `ng run ...:typecheck --format human|json|sarif` on the
      SAME input and assert the returned `code` is identical across all three (0 for
      the clean project). Then plant the app-component TS2322 (reuse the
      APP_COMPONENT_ANCHOR/APP_COMPONENT_INJECTION + plant idiom) and assert the code
      is identical and non-zero across all three formats (the anti-false-pass parity);
      restore the source in a finally.
    Keep the CLI shim (Task 1) as the authoritative stdout-purity proof; here tolerate
    ng's own wrapper line but prove the PAYLOAD boundary is clean.
  </action>
  <verify>
    <automated>npx nx e2e angular-typechecker-ng-cli-e2e</automated>
  </verify>
  <acceptance_criteria>
    - The ng-cli spec extracts and parses the JSON payload, schema-validates the SARIF payload, and asserts no advisory text is inside either parsed payload.
    - Exit code is identical across human/json/sarif for the clean project (0) and for the planted TS2322 project (non-zero), proving verdict parity through `ng run`.
    - `nx e2e angular-typechecker-ng-cli-e2e` is green; the real Nx 23 `ng run` stdout framing was observed (recorded in the SUMMARY), not assumed.
  </acceptance_criteria>
  <done>ng run <project>:typecheck --format json|sarif emits a parseable/schema-valid payload with a clean payload boundary and identical exit codes across formats on the shipped tarball.</done>
</task>

<task type="auto">
  <name>Task 3: Nx executor --format purity + exit-code parity (install-e2e)</name>
  <files>e2e/angular-typechecker-install-e2e/src/install-smoke.e2e.spec.ts</files>
  <read_first>
    - e2e/angular-typechecker-install-e2e/src/install-smoke.e2e.spec.ts (the consumer-workspace fixture that drives `nx run <project>:typecheck` off the installed tarball)
    - e2e/angular-typechecker-install-e2e/src/tarball-audit.e2e.spec.ts (the install-e2e harness/inject('verdaccioUrl') pattern, if a helper is needed)
    - .planning/phases/32-verification-docs-additive-audit/32-RESEARCH.md ("Adapter -> project mapping" + "stdout-purity risk for the Nx / ng adapters")
  </read_first>
  <action>
    Extend the install-e2e consumer-workspace spec to add a `--format` block driving
    the shipped Nx executor: `nx run <project>:typecheck --format json` (and `sarif`,
    `human`) off the installed package. FIRST capture a real
    `nx run <project>:typecheck --format json` stdout at execution time and inspect
    Nx 23 framing (do NOT assume); run with `NX_DAEMON=false` and, if framing leaks
    onto stdout, `--output-style=static` (verify it reduces prefixes) so the payload
    is isolable.

    Assertions over the clean consumer project:
    - json: extract the payload (slice from the first `{`), `JSON.parse`, assert
      `formatVersion` + `diagnostics[]` + `summary`, and assert NO Nx chrome /
      advisory text is INSIDE the parsed payload.
    - sarif: extract + `validateSarif(payload).valid === true` (import from
      `@workspace/test-util`).
    - exit-code parity: run the SAME project through `--format human|json|sarif` and
      assert the process exit code is identical across all three (0 clean). Plant a
      TS2322 in the consumer project and assert the exit code is identical and
      non-zero across all three formats (the cardinal anti-false-pass parity);
      restore in a finally.
    Reuse the existing consumer-workspace fixture + Verdaccio install path -- add the
    format block, do not scaffold a new project (D-04: extend, never add).
  </action>
  <verify>
    <automated>npx nx e2e angular-typechecker-install-e2e</automated>
  </verify>
  <acceptance_criteria>
    - The install-e2e spec parses the JSON payload and schema-validates the SARIF payload emitted by the shipped Nx executor, with no Nx chrome/advisory text inside the payload.
    - Exit code is identical across human/json/sarif for the clean project (0) and the planted TS2322 project (non-zero) through `nx run`.
    - `nx e2e angular-typechecker-install-e2e` is green; the real Nx 23 executor stdout framing was observed (recorded in the SUMMARY).
  </acceptance_criteria>
  <done>The shipped Nx executor emits a parseable JSON + schema-valid SARIF payload with a clean payload boundary and identical exit codes across formats, proven on the installed tarball.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verdaccio local registry -> consumer install | e2e installs the packed tarball by name from a 127.0.0.1 registry |
| shipped bin/executor stdout -> test assertion | the payload boundary must stay pure (no advisory text on the machine payload) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-03 | Tampering / Spoofing | Verdaccio e2e harness | low | mitigate | Reuses the SHIPPED global-setup: registry bound to 127.0.0.1, publish-once, tokened install -- no new network/auth surface added this plan. |
| T-32-SC | Tampering | supply chain (this plan) | low | accept | This plan adds NO new dependency (it reuses the 32-01 dev-only ajv validator via @workspace/test-util); nothing to install. |

No high-severity threats: VER-03 extends existing e2e specs over the already-shipped
tarball; no production code, no new dependency, no new CI job.
</threat_model>

<verification>
Per the validation contract (landmine #4):
- `npx nx e2e angular-typechecker-cli-e2e` (Task 1)
- `npx nx e2e angular-typechecker-ng-cli-e2e` (Task 2)
- `npx nx e2e angular-typechecker-install-e2e` (Task 3)
- `npx nx typecheck test-util` + `npx nx lint test-util` (the runShimSplit helper)
- `npx nx typecheck angular-typechecker` + `npx nx lint angular-typechecker` + `npx nx format:check` (unchanged plugin surface)
Note: this is a single-plan wave -- run on the main checkout (real node_modules), no worktree.
</verification>

<success_criteria>
- runShimSplit returns separate streams; the CLI purity proof parses only .stdout.
- All three adapters (CLI, ng run, Nx executor) emit valid JSON + schema-valid SARIF from the SHIPPED tarball, with a clean payload boundary.
- Exit code is identical across human/json/sarif for the same input -- including the anti-false-pass planted-error case -- on every adapter.
- The three extended e2e projects are green; no new e2e project, no ci.yml edit.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `libs/test-util/src/lib/cli-e2e.ts` -- `runShimSplit` + `ShimResultSplit` + `assertMachineFormatParity` (exported from @workspace/test-util).
- `--format` parity/purity assertions in `angular-typechecker-cli-e2e` (standalone CLI adapter).
- `--format` parity/purity assertions in `angular-typechecker-ng-cli-e2e` (`ng run` adapter).
- `--format` parity/purity assertions in `angular-typechecker-install-e2e` (Nx executor adapter).
</artifacts>

<output>
Create `.planning/phases/32-verification-docs-additive-audit/32-02-SUMMARY.md` when done.
</output>
