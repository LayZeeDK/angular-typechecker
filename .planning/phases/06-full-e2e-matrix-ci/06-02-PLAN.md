---
phase: 06-full-e2e-matrix-ci
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - e2e/angular-typechecker-matrix-e2e/src/pnpm-symlink.int.spec.ts
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/pnpm-lock.yaml
  - packages/angular-typechecker/src/core/filter-diagnostics.spec.ts
  - packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "The executor type-checks the installed tarball under a pnpm symlinked node_modules layout (green) and reports an injected error (non-zero exit)"
    - "A Linux-only realpath regression-guard proves a non-realpath startsWith filter would mis-suppress an in-project diagnostic resolved through a pnpm symlink (or, with documented fallback, asserts the symlinked layout simply works)"
    - "The filter-diagnostics unit suite (all 9 CI cells) asserts mixed-case fold under useCaseSensitiveFileNames:false for in-project, out-of-project, and node_modules-segment paths, and proves the fold is gated on the flag (NOT folded under :true)"
    - "An integration assertion proves the host derives useCaseSensitiveFileNames from the real ts.sys/program host (live case-insensitive on the macOS/Windows CI legs, case-sensitive on Linux)"
  artifacts:
    - path: "e2e/angular-typechecker-matrix-e2e/src/pnpm-symlink.int.spec.ts"
      provides: "pnpm symlinked-store e2e + realpath regression-guard (Linux)"
      contains: "pnpm add"
    - path: "packages/angular-typechecker/src/core/filter-diagnostics.spec.ts"
      provides: "extended mixed-case unit coverage (D-10)"
      contains: "useCaseSensitiveFileNames"
    - path: "packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts"
      provides: "host-derived case-sensitivity integration assertion (D-10)"
      contains: "useCaseSensitiveFileNames"
  key_links:
    - from: "e2e/angular-typechecker-matrix-e2e/src/pnpm-symlink.int.spec.ts"
      to: "pnpm .pnpm/ symlinked store"
      via: "pnpm add <tgz> --no-frozen-lockfile + lstatSync/realpathSync probe before the filter assertion"
      pattern: "realpathSync"
    - from: "packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts"
      to: "the real program host"
      via: "result.program.getTsProgram().useCaseSensitiveFileNames() + ts.sys.realpath"
      pattern: "useCaseSensitiveFileNames"
---

<objective>
Backstop OUT-02 (the realpath-normalized / pnpm-symlink / case-insensitive-FS-safe boundary
filter) across the cross-platform surface that npm/Linux makes invisible. Two complementary tiers:
(1) a pnpm fixture e2e spec in the matrix-e2e project that runs the executor under pnpm's symlinked
`.pnpm/` store and acts as a realpath regression-guard (Linux-only); (2) extended mixed-case unit
cases in `filter-diagnostics.spec.ts` plus one host-derived `useCaseSensitiveFileNames` integration
assertion -- both run on ALL nine CI cells so the macOS/Windows legs are the live case-insensitive
samples.

Purpose: the OUT-02 filter was implemented in Phase 3, but its realpath-first + case-fold logic has
never been exercised under a real pnpm symlink layout or a real case-insensitive FS. The STATE
Blockers charter is explicit: "pnpm-symlink + case-insensitive FS path filtering is invisible under
npm/Linux -- the pnpm fixture + mixed-case assertion (Phase 6) is the backstop." This plan is that
backstop. The mixed-case assertion deliberately lives in the cross-OS unit + integration tier, NOT
the Linux-only e2e gate (where it would be dead code on a case-sensitive FS) -- D-10.

Output: `pnpm-symlink.int.spec.ts` (+ a committed `pnpm-lock.yaml`), extended
`filter-diagnostics.spec.ts`, and an extended `run-typecheck.integration.spec.ts`. Depends on
06-01 (the matrix-e2e project + consumer-workspace fixture must exist).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-full-e2e-matrix-ci/06-CONTEXT.md
@.planning/phases/06-full-e2e-matrix-ci/06-RESEARCH.md
@.planning/phases/06-full-e2e-matrix-ci/06-PATTERNS.md

<interfaces>
<!-- Analog shapes already read from the codebase. Use these directly. -->

filter-diagnostics.spec.ts existing idioms to extend:
- `diag(fileName, code = 2322)` literal builder (lines 14-23): returns a `ts.Diagnostic` with
  `category: 0`, `code`, `file: fileName === undefined ? undefined : ({ fileName } as ts.SourceFile)`.
- `const base = { basePath: '/ws/proj', useCaseSensitiveFileNames: true, realpath: (p) => p }` (lines 26-30).
- Seed cases to parallel: realpath-before-case-fold (lines 73-91); the existing
  `useCaseSensitiveFileNames: false` mixed-case in-project case `/WS/PROJ/src/A.ts` under `/ws/proj`
  (lines 93-103). `filterDiagnostics(...)` returns `{ kept, suppressedCount }`.
- node_modules SEGMENT idiom (lines 32-51): `/ws/proj/node_modules/x/y.d.ts` is suppressed;
  `/ws/proj/node_modules-tools/z.ts` is NOT (segment test, not substring).

run-typecheck.integration.spec.ts existing idioms to extend:
- `runTypecheck({ tsConfigPath })` returns a `CoreResult` with `diagnostics` (absolute,
  forward-slash fileNames).
- `diagnosticsOnFile(diagnostics, absolutePath)` comparator (lines 54-65) normalizes `\\`->`/`.
- `describe.each([['app tsconfig', appTsConfig], ['local-library tsconfig', libTsConfig]])` idiom
  (lines 67-70). Fixtures: gate-b-error (app + lib tsconfigs), sibling-import.
- The host-derived seam in run-typecheck.ts (06-PATTERNS.md lines 438-442):
  `useCaseSensitiveFileNames: result.program.getTsProgram().useCaseSensitiveFileNames()` and
  `realpath: (filePath) => ts.sys.realpath?.(filePath) ?? filePath`.

install-smoke harness primitives to reuse for the pnpm spec (see 06-01 interfaces for full spans):
buildCleanEnv (strips 8 NX_* + both legacy-peer-deps forms; NX_DAEMON='false', FORCE_COLOR='0'),
the pack-to-tmp beforeAll, afterAll tgz cleanup, empty-.npmrc + non-existent-userconfig honesty.

pnpm install of the tarball (RESEARCH lines 362-372):
```ts
const tmp = mkdtempSync(join(tmpdir(), 'atc-pnpm-'));
cpSync(pnpmFixtureDir, tmp, { recursive: true });
writeFileSync(join(tmp, '.npmrc'), '');
execSync(`pnpm add ${JSON.stringify(tarballPath)} --no-frozen-lockfile`, { cwd: tmp, env });
// PROBE (Linux): assert installed node_modules/<pkg> is a symlink whose realpath crosses into .pnpm/ BEFORE asserting filter behavior.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend filter-diagnostics.spec.ts with the D-10 mixed-case unit cases</name>
  <files>packages/angular-typechecker/src/core/filter-diagnostics.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/filter-diagnostics.spec.ts (THE file to extend -- the diag() builder lines 14-23, base object lines 26-30, the seed mixed-case + realpath cases lines 73-103, the node_modules-segment cases lines 32-71)
    - packages/angular-typechecker/src/core/filter-diagnostics.ts (the realpath-first -> \\->/ -> toLowerCase()-when-:false logic the cases pin)
    - .planning/phases/06-full-e2e-matrix-ci/06-PATTERNS.md (filter-diagnostics.spec.ts EXTEND section, lines 412-430)
    - .planning/phases/06-full-e2e-matrix-ci/06-RESEARCH.md (D-10 lines 167-180; Nyquist mixed-case samples lines 470-472)
  </read_first>
  <behavior>
    - Under `useCaseSensitiveFileNames: false`: a mixed-case in-project path (e.g. `/WS/PROJ/src/A.ts` under `/ws/proj`) is KEPT (kept length 1, suppressedCount 0) -- the existing seed case stays
    - Under `useCaseSensitiveFileNames: false`: a mixed-case OUT-of-project path (e.g. `/WS/SIBLING/src/B.ts` under `/ws/proj`) is SUPPRESSED (suppressedCount 1)
    - Under `useCaseSensitiveFileNames: false`: a mixed-case `node_modules` SEGMENT path (e.g. `/WS/PROJ/NODE_MODULES/x/y.d.ts`) is SUPPRESSED by segment (not folded back as in-project)
    - Under `useCaseSensitiveFileNames: true`: the SAME mixed-case in-project input (`/WS/PROJ/src/A.ts` under `/ws/proj`) is SUPPRESSED (NOT folded) -- proving the fold is gated strictly on the flag
  </behavior>
  <action>
    Add new `it('... (OUT-02)', ...)` cases to the existing `describe('filterDiagnostics', ...)`
    block, parallel to the seed mixed-case case (lines 93-103). Reuse the `diag()` literal builder
    and pass an explicit options object per case (basePath '/ws/proj', `useCaseSensitiveFileNames`
    true or false, `realpath: (p) => p`, `includeDeps: false`). Cover: mixed-case OUT-of-project
    suppressed under `false`; mixed-case `node_modules` segment suppressed under `false`; and the
    gating proof -- the same mixed-case in-project input under `useCaseSensitiveFileNames: true` is
    SUPPRESSED. Match the existing style exactly: blank line before the `expect` after the
    `filterDiagnostics(...)` call, braces always, single quotes, `it('... (OUT-02)', () => {...})`
    naming. Do NOT change `filter-diagnostics.ts` -- this is a unit-test extension only.
  </action>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && npx nx run-many -t test -p angular-typechecker 2>&1 | tee /tmp/unit.log; rg -q 'filterDiagnostics' /tmp/unit.log && rg -vq 'fail|FAIL' <(rg 'Test Files|Tests ' /tmp/unit.log) && echo UNIT_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `npx nx run-many -t test -p angular-typechecker` exits 0 with the extended filterDiagnostics cases passing
    - filter-diagnostics.spec.ts contains at least 3 NEW `(OUT-02)` cases: mixed-case out-of-project suppressed under `false`, mixed-case node_modules-segment suppressed under `false`, and mixed-case in-project SUPPRESSED under `true` (the fold-is-flag-gated proof)
    - No change to filter-diagnostics.ts (the production filter is unchanged)
    - All new cases assert on `result.kept` length and `result.suppressedCount`
  </acceptance_criteria>
  <done>The unit tier deterministically gates the OUT-02 case-fold logic on all three OS via mixed-case inputs under both case modes, proving the fold is enabled only when useCaseSensitiveFileNames is false.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the host-derived useCaseSensitiveFileNames integration assertion</name>
  <files>packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts (THE file to extend -- the runTypecheck() call shape, the diagnosticsOnFile comparator lines 54-65, the describe.each idiom lines 67-70, fixture paths lines 21-52)
    - packages/angular-typechecker/src/core/run-typecheck.ts (the host-derived seam: useCaseSensitiveFileNames from result.program.getTsProgram().useCaseSensitiveFileNames(); realpath from ts.sys.realpath -- 06-PATTERNS.md lines 438-442)
    - .planning/phases/06-full-e2e-matrix-ci/06-PATTERNS.md (run-typecheck.integration.spec.ts EXTEND section, lines 434-443)
    - .planning/phases/06-full-e2e-matrix-ci/06-RESEARCH.md (D-10 host-derived lines 374-381; Validation Architecture integration layer lines 447-448)
  </read_first>
  <behavior>
    - The integration assertion proves the in-project classification uses the host-derived case sensitivity (NOT a hard-coded constant): a mixed-case in-project fileName (or mixed-case tsConfigPath) is correctly classified in-project when the real host reports useCaseSensitiveFileNames:false (macOS/Windows CI legs) and on the case-sensitive Linux leg the same path classifies under the case-sensitive rule
    - On every leg, the existing real-compiler diagnostics for the committed fixtures (gate-b-error TS2322 + NG8109, sibling-import suppression) continue to pass unchanged
  </behavior>
  <action>
    Add ONE integration assertion to run-typecheck.integration.spec.ts that the host derives
    `useCaseSensitiveFileNames` from the real program host rather than a constant. The seam is
    already wired in run-typecheck.ts (useCaseSensitiveFileNames from
    result.program.getTsProgram().useCaseSensitiveFileNames(); realpath from ts.sys.realpath), so
    run the REAL compiler against a committed fixture and assert that an in-project diagnostic whose
    fileName is referenced with mixed case is correctly classified in-project. Reuse the
    `diagnosticsOnFile` normalized-forward-slash comparator and the `describe.each` parameterization
    idiom. On the macOS/Windows matrix legs the host-derived value is false (a live case-insensitive
    exercise); on Linux it is true (case-sensitive). The assertion must NOT hard-code a case-mode
    expectation that would fail on a particular OS -- it asserts the in-project keep is correct given
    whatever the host derives. Match the existing style: blank lines around control flow/returns,
    braces always, single quotes. Do NOT change run-typecheck.ts.
  </action>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && npx nx run-many -t test -p angular-typechecker 2>&1 | tee /tmp/integ.log; rg -q 'useCaseSensitiveFileNames' packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts && rg -q 'runTypecheck end-to-end' /tmp/integ.log && echo INTEG_RAN</automated>
  </verify>
  <acceptance_criteria>
    - `npx nx run-many -t test -p angular-typechecker` exits 0 on the dev box (Linux/case-sensitive equivalent) with the new integration assertion passing
    - run-typecheck.integration.spec.ts contains a new assertion referencing `useCaseSensitiveFileNames` and the host-derived seam (real `runTypecheck` call, not a mock)
    - The assertion is OS-agnostic in its expectation (no hard-coded case-mode that would red on macOS/Windows or Linux); it asserts correct in-project classification given the host-derived value
    - No change to run-typecheck.ts
  </acceptance_criteria>
  <done>The integration tier asserts the executor derives case sensitivity from the live program host, so the macOS/Windows CI legs become real case-insensitive exercises and the OUT-02 mixed-case property is gated where it actually bites.</done>
</task>

<task type="auto">
  <name>Task 3: Author the pnpm symlinked-store e2e spec + realpath regression-guard (Linux-validated)</name>
  <files>e2e/angular-typechecker-matrix-e2e/src/pnpm-symlink.int.spec.ts, e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/pnpm-lock.yaml</files>
  <read_first>
    - e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts (the harness to reuse: buildCleanEnv, run(), pack-to-tmp beforeAll, afterAll, empty-.npmrc honesty)
    - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/** (the fixture from 06-01 -- reuse a project for the pnpm install target)
    - .planning/phases/06-full-e2e-matrix-ci/06-PATTERNS.md (pnpm-symlink.int.spec.ts section, lines 397-408; the WARNING about Windows ln -s producing a copy)
    - .planning/phases/06-full-e2e-matrix-ci/06-RESEARCH.md (D-09 lines 154-166; B-02 resolution lines 54; Pitfall 1 lines 262-266; pnpm install code example lines 362-372; Open Question 1 lines 407-410)
  </read_first>
  <action>
    Author `e2e/angular-typechecker-matrix-e2e/src/pnpm-symlink.int.spec.ts` reusing the install-smoke
    harness verbatim (buildCleanEnv, the pack-to-tmp beforeAll, afterAll tgz cleanup, run() with
    --output-style=static, the empty-.npmrc + non-existent-userconfig honesty). Differences from the
    npm spec:
      - Install via pnpm: `execSync('pnpm add ' + JSON.stringify(tarballPath) + ' --no-frozen-lockfile', { cwd: tmp, env })`.
        Run the executor with `includeDeps: true` so pnpm's .pnpm/-symlinked store is genuinely
        traversed (D-09). Reuse a consumer-workspace project as the install target (e.g. the app or
        local-lib), green + injected-error 4-way assertion (non-zero + TS2322 + not ERR_REQUIRE_ESM +
        not 'infrastructure error').
      - Linux-only realpath PROBE BEFORE the filter assertion (Pitfall 1 / B-02): assert the
        installed node_modules/angular-typechecker path is a symlink (`lstatSync(p).isSymbolicLink()`)
        AND its `realpathSync(p)` crosses into `.pnpm/` BEFORE asserting filter behavior. If the probe
        shows the layout does NOT produce a boundary-crossing realpath (or the runner is not Linux),
        FALL BACK to asserting the symlinked layout simply WORKS (D-09 option a) and DOCUMENT inline
        that the load-bearing realpath guard is the filter-diagnostics.spec.ts unit coverage (Task 1).
      - The regression-guard: construct an in-project source resolved THROUGH a pnpm symlink such that
        a naive non-realpath `startsWith(basePath)` filter would MIS-SUPPRESS an in-project
        diagnostic; assert it is KEPT (and an out-of-project one is suppressed via realpath). Gate
        this construction behind the symlink probe so it only runs when the layout genuinely crosses a
        boundary.
      - Commit a `pnpm-lock.yaml` in fixtures/consumer-workspace matching the pinned pnpm version
        (06-03 pins `version: 11.9.0` in ci.yml); if generating the lockfile is not feasible on the
        dev box, rely on `--no-frozen-lockfile` for the tmp install and commit a minimal lockfile (or
        document the --no-frozen-lockfile reliance).

    WARNING (Pitfall 1): this symlink construction CANNOT be authored/validated on the Windows arm64
    dev box -- Git Bash `ln -s` produces a copy (isSymbolicLink:false), so the boundary-crossing
    realpath cannot be reproduced locally. The spec must SELF-GUARD with the in-spec probe and the
    documented fallback so it is correct on the Linux CI runner (the e2e gate is Linux-only) and does
    not produce a meaningless green on the dev box. ASCII-only; single quotes; never pipe nx through
    head/rg; FORCE_COLOR=0 not --no-color.
  </action>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && rg -q 'pnpm add' e2e/angular-typechecker-matrix-e2e/src/pnpm-symlink.int.spec.ts && rg -q 'isSymbolicLink' e2e/angular-typechecker-matrix-e2e/src/pnpm-symlink.int.spec.ts && rg -q 'realpathSync' e2e/angular-typechecker-matrix-e2e/src/pnpm-symlink.int.spec.ts && echo PNPM_SPEC_GUARDED</automated>
  </verify>
  <acceptance_criteria>
    - pnpm-symlink.int.spec.ts contains `pnpm add`, an in-spec symlink probe (`lstatSync(...).isSymbolicLink()` + `realpathSync`) that runs BEFORE the filter assertion, and a documented fallback to "symlinked layout works" when the boundary-crossing realpath cannot be confirmed
    - The spec reuses `buildCleanEnv` + the empty-.npmrc + non-existent-userconfig honesty pattern (B-03) and the 4-way green/injected assertion
    - The spec runs the executor with `includeDeps: true` so the .pnpm/ store is traversed (D-09)
    - A `pnpm-lock.yaml` is committed under fixtures/consumer-workspace OR the spec uses `--no-frozen-lockfile` with the reliance documented inline
    - Inline comment documents that this construction is validated on the Linux CI runner (the e2e gate is Linux-only) because Git Bash `ln -s` on the Windows dev box produces a copy
  </acceptance_criteria>
  <done>The pnpm fixture runs the executor under the real symlinked .pnpm/ store and acts as a self-guarding realpath regression-guard on Linux CI (with a documented fallback), backstopping OUT-02's pnpm-symlink safety where npm/Linux cannot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packed tarball -> tmp pnpm consumer | This repo's freshly-packed `dist/` installed via `pnpm add` into an isolated tmp dir; no third-party registry fetch |
| pnpm store (.pnpm/) -> executor | The executor traverses the symlinked store under `includeDeps:true`; symlinks resolve to this repo's own installed package |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | pnpm add of tarball in spec | accept | The installed artifact is THIS repo's own `npm pack` of `dist/` (no registry fetch); the Phase-5 tarball-audit gate already asserts no install scripts. Local fixture, no secrets. |
| T-06-04 | Tampering | committed pnpm-lock.yaml | mitigate | Lockfile (or `--no-frozen-lockfile`) pins the fixture's resolved tree to the already-vetted locked stack; clean install (no legacy-peer-deps) surfaces a real ERESOLVE rather than masking it |

No new application input-handling surface: this plan adds unit/integration test extensions + one
e2e spec only (local files, no secrets, no untrusted input crosses a boundary).
</threat_model>

<verification>
- `npx nx run-many -t test -p angular-typechecker` exits 0 with the extended filterDiagnostics cases + the host-derived integration assertion.
- pnpm-symlink.int.spec.ts contains `pnpm add`, the `isSymbolicLink`/`realpathSync` probe before the filter assertion, and the documented fallback.
- The mixed-case assertions live in the cross-OS unit + integration tier (NOT the Linux-only e2e gate) -- D-10.
</verification>

<success_criteria>
OUT-02 is backstopped across the cross-platform surface: a pnpm symlinked-store e2e fixture runs
the executor and self-guards the realpath boundary on Linux CI; extended mixed-case unit cases and
a host-derived case-sensitivity integration assertion run on all nine CI cells so the macOS/Windows
legs are live case-insensitive samples.
</success_criteria>

<output>
Create `.planning/phases/06-full-e2e-matrix-ci/06-02-SUMMARY.md` when done (record whether the pnpm
realpath guard crossed the boundary on the dev box or relied on the Linux-CI fallback + the unit
coverage).
</output>
