---
phase: 18-packaged-tarball-e2e-docs
plan: 05
type: execute
wave: 3
depends_on: ["18-01", "18-04"]
files_modified:
  - packages/angular-typechecker/README.md
  - CHANGELOG.md
  - .planning/todos/pending/wr-01-readme-coverage-incomplete.md
autonomous: true
requirements: [SB-07]

must_haves:
  truths:
    - "README has a ## Storybook section carrying the EXACT MUST coverage claim, the MUST-NOT list, and the MUST caveats from board CONSENSUS.md"
    - "The README Limitations 'reference walk is single-level' bullet is rewritten (WR-01): an in-project leaf resolving to ZERO input files is coverage-incomplete (non-clean), not an advisory-only skip; only out-of-project / duplicate / self references remain advisory"
    - "The Programmatic-API CoreResult shape comment includes the new notTypeCheckedDeclaredFiles field"
    - "CHANGELOG has a curated 0.1.2 section with a PROMINENT green->red false-pass -> true-fail callout; NO release cut (no version bump, no tag)"
    - "The WR-01 todo is moved to resolved"
  artifacts:
    - path: "packages/angular-typechecker/README.md"
      provides: "## Storybook section + Contents entry + rewritten Limitations bullet + updated CoreResult comment"
      contains: "## Storybook"
    - path: "CHANGELOG.md"
      provides: "Curated 0.1.2 section above 0.1.1 with green->red callout + ref-link footer"
      contains: "0.1.2"
  key_links:
    - from: "packages/angular-typechecker/README.md"
      to: ".planning/research/v0.1.2-storybook/board/CONSENSUS.md"
      via: "verbatim MUST/MUST-NOT/caveat coverage claim"
      pattern: "SOLUTION `tsconfig.json`"
---

<objective>
Ship SB-07: the README `## Storybook` coverage section (the exact MUST/MUST-NOT/caveat claim), the
WR-01 Limitations fix (zero-root-names is now coverage-incomplete), the updated Programmatic-API
`CoreResult` comment (the new D-01 field), and a curated CHANGELOG 0.1.2 section with the green->red
false-pass -> true-fail callout. PROSE ONLY -- do NOT run `nx release`, bump the version, or tag (D-05).

Purpose: criterion 4. The coverage claim must be exact (never "all Storybook files" / "complete
Storybook coverage" / "ensures Storybook builds") and the behavior change must read as a correctness
fix, not a regression.

Output: edited README + CHANGELOG + the WR-01 todo moved to resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-packaged-tarball-e2e-docs/18-CONTEXT.md
@.planning/phases/18-packaged-tarball-e2e-docs/18-RESEARCH.md
@.planning/phases/18-packaged-tarball-e2e-docs/18-PATTERNS.md
@.planning/research/v0.1.2-storybook/board/CONSENSUS.md
@.planning/phases/18-packaged-tarball-e2e-docs/18-01-SUMMARY.md
@.planning/phases/18-packaged-tarball-e2e-docs/18-04-SUMMARY.md

<interfaces>
<!-- README section anchors (git grep verified): ## Contents (line 28), ## Programmatic API (290),
     the CoreResult shape comment (lines 308-312), ## How it compares (328), ## Limitations (350),
     the "reference walk is single-level" bullet (358-361). -->
<!-- EXACT MUST claim (board CONSENSUS.md "Release coverage claim" / 18-RESEARCH RQ4 lines 352-366):
     "v0.1.2 runs the complete Angular type-check (TypeScript + template type-check + NG8xxx, no emit)
     on the TypeScript files the Storybook tsconfig declares -- your *.stories.ts,
     .storybook/main.ts/preview.ts, and (centralized host) the aggregated
     *.component.ts/*.directive.ts/*.ts its include reaches -- provided the typecheck target points at
     the project's SOLUTION tsconfig.json. A green verdict means every such file type-checked clean." -->
<!-- MUST NOT: "all Storybook files" / "complete Storybook coverage" unqualified; ensures Storybook builds/runs; any layout not proven on the official stack. -->
<!-- MUST caveat: .mdx never type-checked; .tsx only if jsx enabled; external templateUrl per branch 4a
     (attributes to .html, kept via relatedInformation owner mapping); Layout C not supported; pointing at
     a LEAF app/lib tsconfig EXCLUDES stories (point at the solution config); "supported" is verified
     against the FORCE-INSTALLED Storybook combination (--legacy-peer-deps/--force; nx add/pnpm can hit
     ERR_PNPM_IGNORED_BUILDS). Plus: forced @storybook/angular@10.4.6's 48 TS6 .d.ts errors are
     node_modules-attributed -> suppressed -> never leak; genuine TS6 errors in main.ts/preview.ts are real. -->
<!-- CHANGELOG green->red callout (18-RESEARCH RQ4 lines 402-407): "Behavior change (a correctness fix,
     not a regression): existing centralized-host (Layout B) builds that previously passed by SILENTLY
     dropping aggregated cross-project diagnostics will now FAIL ... a false-pass -> true-fail CORRECTION
     (permitted under 0.x semver), not a break." -->
<!-- New D-01 field name (from 18-01 SUMMARY): notTypeCheckedDeclaredFiles?: readonly string[] -- add to the CoreResult comment. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README Storybook section + Limitations WR-01 fix + CoreResult comment</name>
  <files>packages/angular-typechecker/README.md</files>
  <read_first>
    - packages/angular-typechecker/README.md (## Contents 28-42; ## Programmatic API 290-326 incl. the CoreResult comment 308-312; ## How it compares 328-348; ## Limitations 350-366 incl. the single-level bullet 358-361)
    - .planning/research/v0.1.2-storybook/board/CONSENSUS.md ("Release coverage claim" -- the verbatim MUST/MUST-NOT/caveat)
    - .planning/phases/18-packaged-tarball-e2e-docs/18-RESEARCH.md (RQ4: exact claim, WR-01 fix, README placement)
    - .planning/phases/18-packaged-tarball-e2e-docs/18-01-SUMMARY.md (the exact notTypeCheckedDeclaredFiles field name shipped)
    - packages/angular-typechecker/src/executors/typecheck/executor.ts (lines 86-99 verdictNote branching that the Limitations rewrite must match) and packages/angular-typechecker/src/core/evaluate-result.ts (lines 105-112 zero-root-names -> coverage-incomplete)
  </read_first>
  <action>
    Add a `## Storybook` section between `## How it compares` (line 328) and `## Limitations` (line 350),
    plus a `- [Storybook](#storybook)` entry in `## Contents` between the How-it-compares and Limitations
    entries. The section's spine is the EXACT MUST claim, MUST-NOT list, and MUST caveats from board
    CONSENSUS.md (see interfaces). Reference the two Nx layouts (A per-project scaffold, B centralized
    host) with one line each and state that pointing the target at the SOLUTION tsconfig is the enabling
    condition (and the DX note: the target stores `tsConfig: <solution>` and reads `references[]` at
    execute time, so adding Storybook after wiring `typecheck` yields coverage on the next run with no
    re-generation). Fold in the two docs-only D4 facts (force-install `--legacy-peer-deps`/`--force`, nx
    add/pnpm `ERR_PNPM_IGNORED_BUILDS`; forced-SB10's node_modules `.d.ts` errors are suppressed and never
    leak while genuine `main.ts`/`preview.ts` TS6 errors are real). REWRITE the "reference walk is
    single-level" Limitations bullet (WR-01): an in-project leaf resolving to ZERO input files (empty, or a
    references-only/solution tsconfig whose inner projects are not walked) yields a non-clean
    coverage-incomplete verdict; only out-of-project / duplicate / self references remain advisory (verdict
    unchanged). UPDATE the Programmatic-API `CoreResult` shape comment (lines 308-312) to add
    `notTypeCheckedDeclaredFiles?: readonly string[]`. ASCII only (`--` not em-dash), match the existing
    section prose style.
  </action>
  <verify>
    <automated>npx nx run-many -t format:check lint -p angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -c '^## Storybook' packages/angular-typechecker/README.md` == 1
    - the section contains the phrase `SOLUTION` tsconfig and states a green verdict means every declared file type-checked clean
    - the README does NOT claim "complete Storybook coverage" or "all Storybook files" (`git grep -i 'complete storybook coverage' packages/angular-typechecker/README.md` returns nothing)
    - the Limitations bullet now says a zero-input in-project leaf is coverage-incomplete (contains "coverage-incomplete"); the old "empty ... skipped with an advisory warning and do not change the verdict" wording is gone
    - the CoreResult comment contains `notTypeCheckedDeclaredFiles`
    - `format:check` and `lint` (maxWarnings:0) pass for the package
  </acceptance_criteria>
  <done>The README carries the exact coverage claim, the corrected Limitations, and the updated CoreResult comment; format + lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Curated CHANGELOG 0.1.2 section + resolve WR-01 todo</name>
  <files>CHANGELOG.md, .planning/todos/pending/wr-01-readme-coverage-incomplete.md</files>
  <read_first>
    - CHANGELOG.md (the 0.1.1 entry lines 5-35 for structure; the 0.1.0 Breaking-Changes section 44-56 for the behavior-change callout tone; the ref-link footer near the bottom)
    - .planning/phases/18-packaged-tarball-e2e-docs/18-RESEARCH.md (RQ4: the verbatim green->red callout + CHANGELOG placement; the D-05 no-release-cut rule)
    - .planning/todos/pending/wr-01-readme-coverage-incomplete.md (the folded todo to resolve)
  </read_first>
  <action>
    Add a curated `## 0.1.2 (<date>)` section ABOVE `## 0.1.1`, matching the existing entry style
    (summary paragraph + `### Features` / `### Fixes` / `### Internal` / `### Compatibility`). Content:
    the input-set-membership boundary correctness fix so `typecheck` checks the whole Storybook
    tsconfig-declared surface (stories, main.ts/preview.ts, aggregated host components) across Layout A +
    Layout B; the additive split-counter surfacing + the new `notTypeCheckedDeclaredFiles` advisory
    (`.mdx` never checked, `.tsx` only with `jsx`); the packaged-tarball Storybook e2e. Include a PROMINENT
    green->red callout (the verbatim "Behavior change (a correctness fix, not a regression)" block from
    18-RESEARCH RQ4): existing Layout-B builds that passed by silently dropping aggregated diagnostics will
    now FAIL -- a false-pass -> true-fail CORRECTION permitted under 0.x, not a break. Add the `[0.1.2]`
    ref-link line at the top of the footer (`.../releases/tag/angular-typechecker@0.1.2`). Keep scopes
    clean -- NO internal plan-ids (per AGENTS.md, the raw nx changelog leaks them; this hand-curated
    section IS the curation step). Do NOT run `nx release`, bump `package.json` version, or create a tag
    (D-05). Then move the WR-01 todo: `git mv .planning/todos/pending/wr-01-readme-coverage-incomplete.md
    .planning/todos/resolved/wr-01-readme-coverage-incomplete.md` (create the resolved dir) and set its
    frontmatter `status: resolved`.
  </action>
  <verify>
    <automated>git grep -c "## 0.1.2" CHANGELOG.md && test -f .planning/todos/resolved/wr-01-readme-coverage-incomplete.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md has a `## 0.1.2 (<date>)` section directly above `## 0.1.1`
    - it contains a green->red "false-pass -> true-fail" behavior-change callout framed as a correctness fix, not a regression
    - a `[0.1.2]:` ref-link line targeting `angular-typechecker@0.1.2` is present in the footer
    - `git grep -n -E '\b[0-9]{2}-[0-9]{2}\b' CHANGELOG.md` shows no internal plan-id scopes in the new section
    - `packages/angular-typechecker/package.json` version is UNCHANGED (no release cut); no new git tag created
    - the WR-01 todo now lives under `.planning/todos/resolved/` with `status: resolved`
  </acceptance_criteria>
  <done>The curated 0.1.2 CHANGELOG section is written with the green->red callout, no release cut, and the WR-01 todo is resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Prose-only edits to README / CHANGELOG / a planning todo. No code, no external input, no trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05-I | Information disclosure | public CHANGELOG | mitigate | Curate scopes so no internal plan-ids leak into the public changelog (AGENTS.md rule); the acceptance criteria assert no `NN-NN` scope in the new section. |
| T-18-05-R | Repudiation (accidental release) | CHANGELOG / package.json | mitigate | PROSE ONLY (D-05): assert the package version is unchanged and no tag is created, so no accidental release cut. |
</threat_model>

<verification>
- `npx nx run-many -t format:check lint -p angular-typechecker` green (maxWarnings:0).
- README `## Storybook` section present with the exact claim; Limitations WR-01 fix present; CoreResult comment updated.
- CHANGELOG 0.1.2 section present with the green->red callout; no version bump / tag (D-05).
- WR-01 todo moved to resolved.
</verification>

<success_criteria>
- The README + CHANGELOG carry the exact MUST/MUST-NOT/caveat coverage statement and the false-pass -> true-fail callout (criterion 4).
- The WR-01 Limitations inaccuracy is corrected and the todo is resolved.
- No release was cut (prose only).
</success_criteria>

<output>
Create `.planning/phases/18-packaged-tarball-e2e-docs/18-05-SUMMARY.md` when done.
</output>
