---
phase: 35-automated-code-scanning-proof
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/ci/assert-code-scanning.mjs
  - packages/angular-typechecker/src/assert-code-scanning.spec.ts
autonomous: true
requirements: [PROOF-01, PROOF-02]

must_haves:
  truths:
    - "`tools/ci/assert-code-scanning.mjs` is a lean pure-node script driving `gh api` (NOT a new marketplace action), receiving the repo, PR number, expected category, and expected family/severity set (D-03)."
    - "It captures the `upload-sarif` `sarif-id` (via env) and polls `GET code-scanning/sarifs/{id}` until `processing_status == complete` (bounded; fails on `failed`) BEFORE querying alerts (D-03a)."
    - "It queries `code-scanning/analyses` + `code-scanning/alerts` on `refs/pull/<n>/merge` with bounded retries + backoff, and fails LOUD (non-zero exit) on timeout -- never a single query (D-03b)."
    - "It asserts SET-MEMBERSHIP of the expected (family tag, severity) tuples among alerts client-filtered to `most_recent_instance.category === 'angular-typecheck-proof'`, exiting non-zero on ANY missing tuple, never asserting an exact count (D-03c, PROOF-02)."
    - "The expected set is exactly typescript/error, template-type-check/error, extended-diagnostics/warning, tool/error (D-03d)."
    - "The exported pure matcher `missingTuples(alerts, expected)` is unit-tested (subprocess, no GitHub) for the GREEN path (all tuples present -> exit 0), the RED path (a missing tuple -> non-zero exit, PROOF-02), and category isolation (right tag+severity but wrong category does NOT satisfy a tuple)."
    - "Additive CI/test-only: no published-surface change, no new runtime dependency, no version bump (D-04)."
  artifacts:
    - "tools/ci/assert-code-scanning.mjs (exported pure `missingTuples`; gh-api poll/assert CLI entry; `ASSERT_ALERTS_FILE` test seam)"
    - packages/angular-typechecker/src/assert-code-scanning.spec.ts
  key_links:
    - "upload-sarif `sarif-id` (env SARIF_ID) -> GET sarifs/{id} processing poll -> GET alerts?ref=refs/pull/<PR>/merge&tool_name -> category-filter -> missingTuples -> exit 0/1"
    - "ASSERT_ALERTS_FILE env seam -> subprocess spec feeds a canned alerts payload -> proves the set-membership decision + RED exit WITHOUT hitting GitHub (the ingestion path stays real-CI-only)"
---

<objective>
Build the lean `gh api` assertion script that continuously proves the SARIF -> Code Scanning contract landed, and its fast local unit test covering the fail-loud (RED) path. The script is the deterministic, bounded-poll, set-membership assertion that turns a broken contract into a red CI check (PROOF-02).

Purpose: PROOF-01/02's assertion half. It captures the upload's `sarif-id`, waits for GitHub to finish processing, then checks that one alert per family (matched on rule tag + severity) landed under the dedicated proof category on the PR merge-ref -- exiting non-zero on any miss or timeout.
Output: `tools/ci/assert-code-scanning.mjs` (with an exported pure matcher) + a plugin-side subprocess spec that proves the matcher's GREEN/RED/category-isolation behavior without GitHub.

## Artifacts this plan produces
- `tools/ci/assert-code-scanning.mjs` -- exported pure function `missingTuples(alerts, expected)`; a CLI entry (guarded by `process.argv[1] === fileURLToPath(import.meta.url)`) that drives `gh api`; an `ASSERT_ALERTS_FILE` env test seam
- `packages/angular-typechecker/src/assert-code-scanning.spec.ts` -- vitest subprocess test (GREEN/RED/category-isolation)
- Exported matcher symbol: `missingTuples`
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-automated-code-scanning-proof/35-CONTEXT.md
@.planning/phases/35-automated-code-scanning-proof/35-RESEARCH.md
@.planning/phases/35-automated-code-scanning-proof/35-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tools/ci/assert-code-scanning.mjs (exported matcher + gh-api poll/assert)</name>
  <files>tools/ci/assert-code-scanning.mjs</files>
  <read_first>
    - tools/ci/merge-sarif.mjs (the EXACT .mjs shape: comment header -> node:* imports -> exported pure function -> `if (process.argv[1] === fileURLToPath(import.meta.url))` CLI entry; NO shebang; JSDoc types not TS; injection-safe spawn with maxBuffer 64*1024*1024)
    - tools/ci/list-typecheck-projects.mjs (the exported-pure + throw-on-empty fail-loud precedent)
    - .planning/phases/35-automated-code-scanning-proof/35-RESEARCH.md (## Code Examples -> "Assert script skeleton" with the VERIFIED gh REST field paths; ## Architecture Patterns -> Pattern 2 category isolation, Pattern 3 deterministic wait handle; ## Common Pitfalls P3/P4/P6/P7)
    - packages/angular-typechecker/src/core/diagnostic-family.ts (READ-ONLY: the four family tag literals the EXPECTED set uses)
  </read_first>
  <action>
Create `tools/ci/assert-code-scanning.mjs` mirroring `merge-sarif.mjs` exactly (comment header explaining WHY; `node:child_process` + `node:fs` + `node:url` imports; NO shebang; JSDoc typing, not TS). Implement D-03/D-03a/D-03b/D-03c/D-03d:

Constants: `REPO = 'LayZeeDK/angular-typechecker'`, `CATEGORY = 'angular-typecheck-proof'`, `TOOL = 'angular-typechecker'`, and `EXPECTED` = the four tuples typescript/error, template-type-check/error, extended-diagnostics/warning, tool/error (D-03d). Add a comment cross-referencing that this set is locked by the 35-01 drift-lock integration spec (`machine-reporters-sarif.integration.spec.ts`) and grounded in `diagnostic-family.ts` -- the two must not drift.

Export the PURE matcher `missingTuples(alerts, expected)`: given a parsed alerts array and the expected tuples, return the tuples for which NO alert has `(alert.rule.tags || []).includes(tag) && alert.rule.severity === severity`. Pure -- no I/O, no spawn (mirrors `mergeSarifRuns` being exported). The CLI entry filters alerts by category BEFORE calling it (Pattern 2), so the matcher receives already-category-scoped alerts.

Add a small helper `ghApi(pathAndQuery)` that runs `gh` via a fixed arg array -- `execFileSync('gh', ['api', pathAndQuery], { encoding: 'utf8', maxBuffer: 64*1024*1024 })` -- and `JSON.parse`s stdout. NO `shell: true`, NO interpolated PR data (injection-safe; gh reads the token from `GH_TOKEN` in env). Add an async `sleep`.

CLI entry (guarded by `process.argv[1] === fileURLToPath(import.meta.url)`), reading config from env (NEVER argv for PR data): `PR_NUMBER` (= github.event.pull_request.number), `SARIF_ID` (= the upload step's `sarif-id`), and the OPTIONAL test seam `ASSERT_ALERTS_FILE`.

  - TEST-SEAM branch: if `ASSERT_ALERTS_FILE` is set, read + JSON.parse that file as the alerts array, client-filter it by `most_recent_instance.category === CATEGORY`, run `missingTuples`, and if non-empty THROW an Error naming the missing tuples (non-zero exit); else log success and exit 0. This branch does NOT call `gh` (the GitHub ingestion path is real-CI-only) and exists so the set-membership decision + RED exit are unit-testable locally without GitHub.

  - NORMAL branch (no ASSERT_ALERTS_FILE): compute `ref = refs/pull/${PR_NUMBER}/merge`. (1) Deterministic wait handle (D-03a): bounded loop (~20 attempts x 6s, ~2 min ceiling -- Discretion) polling `repos/${REPO}/code-scanning/sarifs/${SARIF_ID}`; return when `processing_status === 'complete'`; THROW immediately if `'failed'` (include `errors`); THROW on timeout. (2) Optional analyses cross-check (Discretion -- include it): GET `repos/${REPO}/code-scanning/analyses?ref=${ref}&tool_name=${TOOL}&sarif_id=${SARIF_ID}`; THROW if no analysis has `category === CATEGORY`. (3) Set-membership over alerts (D-03b/D-03c): bounded retry loop (~20 x 6s) GETting `repos/${REPO}/code-scanning/alerts?ref=${ref}&tool_name=${TOOL}&per_page=100`, client-filtering to `most_recent_instance.category === CATEGORY`, running `missingTuples`; return on empty-missing; retry+backoff otherwise; THROW on timeout naming the still-missing tuples. On success, log a one-line confirmation.

Fail-loud everywhere: any missing tuple, `processing_status: failed`, or timeout THROWS (non-zero exit) -- never a silent pass (PROOF-02; mirrors list-typecheck-projects.mjs's throw-on-empty). Use `refs/pull/<n>/merge` (P3) -- never the default branch view. Poll, never assert once (P4). Do NOT assert an exact alert count (P6). Do NOT edit the reporter/adapters/schema (D-04) -- the category is an upload input, not a reporter concern.
  </action>
  <verify>
    <automated>node -e "import('./tools/ci/assert-code-scanning.mjs').then(m=>{if(typeof m.missingTuples!=='function'){console.error('missingTuples not exported');process.exit(1)}const E=[{tag:'typescript',severity:'error'},{tag:'template-type-check',severity:'error'},{tag:'extended-diagnostics',severity:'warning'},{tag:'tool',severity:'error'}];const full=E.map(e=>({rule:{tags:[e.tag],severity:e.severity},most_recent_instance:{category:'angular-typecheck-proof'}}));if(m.missingTuples(full,E).length!==0){console.error('GREEN case wrong');process.exit(1)}if(m.missingTuples(full.slice(0,3),E).length!==1){console.error('RED case wrong');process.exit(1)}console.log('missingTuples GREEN+RED ok')})"</automated>
  </verify>
  <done>`assert-code-scanning.mjs` exports a pure `missingTuples` and its `gh api` CLI entry polls `sarifs/{id}` then set-membership on `refs/pull/<n>/merge`, failing loud (non-zero exit) on any miss/timeout.</done>
  <acceptance_criteria>
    - `tools/ci/assert-code-scanning.mjs` exists, has NO shebang, imports only `node:*`, and exports a pure `missingTuples(alerts, expected)`.
    - Importing the module and calling `missingTuples` returns `[]` when all four tuples are present and a non-empty array when one is missing.
    - The CLI entry reads `PR_NUMBER`/`SARIF_ID`/`ASSERT_ALERTS_FILE` from env (never argv), queries `refs/pull/<n>/merge`, polls `sarifs/{id}` before alerts, filters alerts by the `angular-typecheck-proof` category, and throws (non-zero exit) on any missing tuple/timeout/processing-failed.
    - The script uses `execFileSync('gh', ['api', ...])` (arg array, no shell, no interpolated PR data).
    - No reporter/adapter/schema/manifest file is touched.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add the matcher subprocess unit test (GREEN / RED / category-isolation)</name>
  <files>packages/angular-typechecker/src/assert-code-scanning.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/merge-sarif.spec.ts (the subprocess-test precedent AND its load-bearing comment: a plugin spec must NOT statically import a tools/ci module -- @nx/enforce-module-boundaries at maxWarnings:0 forbids the relative import, and a file:// dynamic import fails vitest's module runner; drive the real script as a subprocess instead)
    - packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts (the B3 hermetic-env subprocess model: findWorkspaceRoot, execFileSync('node', [script], { env, cwd }))
    - tools/ci/assert-code-scanning.mjs (the ASSERT_ALERTS_FILE seam + the alert shape it expects: rule.tags/rule.severity/most_recent_instance.category)
  </read_first>
  <action>
Create `packages/angular-typechecker/src/assert-code-scanning.spec.ts` -- a plugin-side vitest spec (runs under `nx test`) that drives the REAL `tools/ci/assert-code-scanning.mjs` as a SUBPROCESS via `execFileSync('node', [scriptPath], { env, encoding: 'utf8', cwd: workspaceRoot })`. Do NOT import the `.mjs` (the merge-sarif.spec.ts comment documents why: the module boundary lint + vitest module runner both block it) -- exercise it through the `ASSERT_ALERTS_FILE` env seam instead, mirroring merge-sarif.spec.ts / ci-e2e-coverage-guard.spec.ts.

Resolve the script path via `findWorkspaceRoot(dirname(fileURLToPath(import.meta.url)))` + `join(root, 'tools', 'ci', 'assert-code-scanning.mjs')`. Write canned alerts payloads to `mkdtempSync` temp files and pass their path as `ASSERT_ALERTS_FILE` (plus dummy `PR_NUMBER`/`SARIF_ID`). Assert exit codes:
  - GREEN: a payload with one alert per family (each alert `{ rule: { tags: [<family>], severity: <level> }, most_recent_instance: { category: 'angular-typecheck-proof' } }` for all four tuples) -> the subprocess exits 0 (execFileSync does NOT throw).
  - RED (PROOF-02): a payload MISSING one family (e.g. drop the `tool` alert) -> the subprocess exits non-zero (execFileSync throws; assert it throws and, if capturable, `error.status === 1` and the message names the missing tuple).
  - Category isolation (Pattern 2): a payload where an alert has the right tag+severity but `category: 'angular-typecheck/some-project'` (a dogfood category) -> that tuple is NOT satisfied -> the subprocess exits non-zero. This proves the client-side category filter is load-bearing, not cosmetic.

Add a header comment explaining this is the LOCAL, GitHub-free proof of the assert's set-membership decision + fail-loud (RED) path; the actual ingestion assertion is real-CI-only (the 35-03 job).
  </action>
  <verify>
    <automated>npx nx test angular-typechecker</automated>
  </verify>
  <done>`nx test` passes; the subprocess spec proves GREEN, RED (fail-loud), and category isolation without touching GitHub, and lint stays green (no boundary violation).</done>
  <acceptance_criteria>
    - `npx nx test angular-typechecker` passes including the new `assert-code-scanning.spec.ts`.
    - The spec drives the real `.mjs` as a subprocess (execFileSync) via the `ASSERT_ALERTS_FILE` seam; it does NOT statically import the `.mjs`.
    - GREEN (all four tuples, correct category) exits 0; RED (a missing family) exits non-zero; wrong-category alert does NOT satisfy a tuple (exits non-zero).
    - `npx nx run-many -t lint` (maxWarnings:0) stays green (no module-boundary violation from the spec).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assert script -> `gh` CLI subprocess -> GitHub REST | The script shells out to `gh`; the subprocess call must be injection-safe (fixed arg array, no shell). |
| env (PR data) -> assert script | `PR_NUMBER`/`SARIF_ID` are GitHub-controlled values passed via env, consumed as data (not interpolated into any shell). |
| `GH_TOKEN` -> `gh` | Authentication uses the ephemeral workflow token from env; no long-lived secret is read or written. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-08 | Tampering / Elevation (command injection) | assert script subprocess | high | mitigate | `execFileSync('gh', ['api', pathAndQuery])` -- fixed arg array, NO `shell: true`; PR data read from `env`, never concatenated into a shell string (V5). |
| T-35-10 | Denial of Service (flaky false red) | async ingestion timing | medium | mitigate | Deterministic wait handle (poll `sarifs/{id}` to `complete`) + bounded alerts retry/backoff; fail loud only on true timeout (D-03a/D-03b). |
| T-35-11 | Spoofing (dogfood alert masks a proof regression) | alert category isolation | medium | mitigate | Client-filter alerts to `most_recent_instance.category === 'angular-typecheck-proof'` before set-membership (Pattern 2); unit-tested by the category-isolation case. |

**Note:** The upload/token-scope/fork threats live in the 35-03 CI-job plan; this script performs read-only `gh api` GETs authenticated by the ambient workflow token.
</threat_model>

<verification>
- `npx nx test angular-typechecker` green (the matcher subprocess spec: GREEN/RED/category-isolation).
- Task-1 automated import check: `missingTuples` is exported and correct on GREEN/RED.
- `npx nx run-many -t lint` (maxWarnings:0) + `npx nx format:check` green.
- `git diff --stat` touches ONLY `tools/ci/assert-code-scanning.mjs` + `packages/angular-typechecker/src/assert-code-scanning.spec.ts` -- no reporter/adapter/schema/manifest change (D-04).
</verification>

<success_criteria>
- `tools/ci/assert-code-scanning.mjs` drives `gh api` with a bounded, deterministic poll on the PR merge-ref and asserts set-membership of the four (family tag, severity) tuples under the `angular-typecheck-proof` category, exiting non-zero on any miss or timeout (PROOF-01/02, D-03a..d).
- The exported pure `missingTuples` matcher is unit-proven for GREEN, RED (fail-loud), and category isolation without touching GitHub.
- No published surface changes; no new runtime dependency; no version bump (D-04).
</success_criteria>

<output>
Create `.planning/phases/35-automated-code-scanning-proof/35-02-SUMMARY.md` when done.
</output>
