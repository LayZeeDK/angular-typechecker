---
phase: 26-pure-cli-core-exit-code-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/cli/parse-args.ts
  - packages/angular-typechecker/src/cli/console-logger.ts
  - packages/angular-typechecker/src/cli/parse-args.spec.ts
autonomous: true
requirements: [ARGS-01, ARGS-02, ARGS-04, CLI-03, VER-01]

must_haves:
  truths:
    - "parse-args maps -c/--tsConfig (repeatable) to a typed result and rejects an unknown flag, a missing required --tsConfig, and a non-integer --max-warnings as usage errors"
    - "--help/-h and --version resolve text destined for stdout, with the version read from the real package.json manifest"
    - "-p / --project is NOT a registered flag (it surfaces as an unknown-flag usage error)"
    - "a BufferingLogger accumulates info/warn/error lines into one buffer and joins them for the returned stderr"
    - "Locked CONTEXT.md decisions covered by this plan: D-04, D-08, D-10, D-11, D-12, D-14, D-15, D-16"
  artifacts:
    - path: "packages/angular-typechecker/src/cli/parse-args.ts"
      provides: "util.parseArgs wrapper + flag validation + usage-error mapping + help/version text"
      contains: "parseArgs"
    - path: "packages/angular-typechecker/src/cli/console-logger.ts"
      provides: "BufferingLogger implementing core/logger.ts Logger"
      contains: "implements Logger"
    - path: "packages/angular-typechecker/src/cli/parse-args.spec.ts"
      provides: "unit coverage of flag mapping, single-vs-repeatable -c, and usage-error branches"
  key_links:
    - from: "packages/angular-typechecker/src/cli/parse-args.ts"
      to: "node:util parseArgs"
      via: "strict:true, allowPositionals:false, short { c: tsConfig, h: help }, multiple:true for tsConfig"
      pattern: "parseArgs"
    - from: "packages/angular-typechecker/src/cli/console-logger.ts"
      to: "../core/logger"
      via: "type-only import of Logger; implemented structurally"
      pattern: "from '../core/logger'"
---

<objective>
Build the two nx-free foundation modules the CLI's run() consumes: parse-args.ts
(the util.parseArgs wrapper + flag validation + usage-error mapping + help/version
text) and console-logger.ts (a BufferingLogger that implements the core Logger seam
and buffers notice/error lines for stderr). Author the pure unit spec for parse-args.

Purpose: These are the CONTRACTS the load-bearing run() (Plan 26-02) imports. Defining
them first means the run() executor receives a typed CliOptions result and a ready
BufferingLogger instead of discovering the shapes by exploration.

Output: parse-args.ts, console-logger.ts, and parse-args.spec.ts under
packages/angular-typechecker/src/cli/ (a NEW dir, sibling of executors/ and builders/;
@nx/js:tsc already globs src/**/*.ts so no build-config change is needed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-pure-cli-core-exit-code-wiring/26-CONTEXT.md
@.planning/phases/26-pure-cli-core-exit-code-wiring/26-RESEARCH.md
@.planning/phases/26-pure-cli-core-exit-code-wiring/26-PATTERNS.md

<interfaces>
<!-- The core seam parse-args and console-logger interact with. Use directly; no codebase exploration needed. -->

From packages/angular-typechecker/src/core/logger.ts (the interface console-logger implements):
```typescript
export interface Logger {
  info(message: string): void;
  warn(message: string): void;
  error(message: string): void; // reserved for the CLI infra path (Phase 26); no advisory uses it
}
```

Node util.parseArgs config to build (RESEARCH Code Examples, D-12 -- short is `c`, NEVER `p`):
- options.tsConfig      : { type: 'string',  short: 'c', multiple: true }
- options['max-warnings']: { type: 'string' }
- options['fail-fast']  : { type: 'boolean' }
- options['include-deps']: { type: 'boolean' }
- options.strict        : { type: 'boolean' }
- options.help          : { type: 'boolean', short: 'h' }
- options.version       : { type: 'boolean' }
- top-level: { args: argv, strict: true, allowPositionals: false }

Recommended discriminated result shape for parse-args (internal names are D-11 discretion):
- { kind: 'options', tsConfig: string[]; maxWarnings?: number; failFast: boolean; includeDeps: boolean; strict: boolean }
- { kind: 'help', text: string }
- { kind: 'version', text: string }
- { kind: 'usageError', message: string }
run() (Plan 26-02) consumes this union; keep tsConfig as the raw string[] here -- the
single-vs-array COLLAPSE (ARGS-03) happens in run() when it builds CoreOptions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parse-args.ts -- util.parseArgs wrapper, validation, usage mapping, help/version</name>
  <files>packages/angular-typechecker/src/cli/parse-args.ts</files>
  <read_first>
    - packages/angular-typechecker/src/executors/typecheck/normalize-options.ts (the pure typed knob-split SHAPE to mirror: a pure function returning a typed struct, no I/O beyond the manifest read)
    - packages/angular-typechecker/src/core/logger.ts (Logger, imported type-only by console-logger, not here)
    - packages/angular-typechecker/package.json (confirm `version` field + that main.js sits under src/, so `require('../../package.json')` from src/cli resolves the manifest two dirs up)
    - .planning/phases/26-pure-cli-core-exit-code-wiring/26-RESEARCH.md (Code Examples: parseArgs config D-12, --max-warnings validation D-08, Pitfall 4 usage->2)
  </read_first>
  <behavior>
    - `-c x` and `--tsConfig x` both yield tsConfig ['x']; `-c a -c b` yields ['a','b'] (repeatable).
    - No --tsConfig at all -> usageError (parseArgs does not enforce required; check explicitly).
    - Unknown flag (e.g. `--project x` or `-p x` or `--nonsense`) -> usageError (strict:true throws; caught).
    - Missing value for an option that needs one (e.g. `--tsConfig` with no arg) -> usageError.
    - `--max-warnings 3` -> maxWarnings 3; `--max-warnings 0` -> maxWarnings 0 (valid); `--max-warnings x`, `--max-warnings -1`, `--max-warnings 1.5` -> usageError.
    - `--help` or `-h` -> { kind: 'help', text } whose text contains `npx angular-typechecker` and the exit-code line 0/1/2; NEVER `npx atc`.
    - `--version` -> { kind: 'version', text } whose text equals the real package.json version.
    - `--fail-fast`, `--include-deps`, `--strict` map to the respective booleans (default false when absent).
  </behavior>
  <action>
    Create parse-args.ts. Import parseArgs from 'node:util'. Call parseArgs with the exact options
    object and top-level flags from the interfaces block (strict:true, allowPositionals:false, tsConfig
    short 'c' multiple:true, help short 'h'). Wrap the parseArgs call in try/catch (D-14): strict:true
    throws ERR_PARSE_ARGS_* on an unknown flag or a missing option value -- map any caught throw to a
    usageError result with a clear ASCII message that names the offending token where available. After a
    successful parse, short-circuit in this order: if values.help -> return a help result; if values.version
    -> return a version result. The version string is `require('../../package.json').version` (CJS JSON
    require works under module:nodenext; D-10). The help text MUST present the canonical uninstalled
    invocation as `npx angular-typechecker` and MUST NOT contain `npx atc` (Pitfall 5 supply-chain hazard);
    include the flag list and a single line stating exit codes 0 clean / 1 verdict-fail / 2 infra-or-usage.
    Then validate: if values.tsConfig is absent or empty -> usageError (missing required --tsConfig). Validate
    --max-warnings per D-08: if present, compute n = Number(raw) and reject !Number.isInteger(n) || n < 0 as a
    usageError with the message shape `--max-warnings expects a non-negative integer, got "<raw>"`;
    --max-warnings 0 stays valid. Otherwise return an options result carrying tsConfig (the raw string[] --
    do NOT collapse here; run() collapses per ARGS-03), maxWarnings (the validated number or undefined),
    failFast, includeDeps, strict (each `values[...] ?? false`). Import NOTHING from @nx/devkit, nx, the
    barrel ../index, or ../executors/* -- node:util + the manifest require only (D-15, nx-free by construction).
    Follow project TS style: blank lines around control flow/returns, braces on all control-flow bodies,
    ASCII-only strings.
  </action>
  <verify>
    <automated>nx build angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `nx build angular-typechecker` succeeds with parse-args.ts included.
    - parse-args.ts exports a parse function returning the discriminated union (options | help | version | usageError).
    - The parseArgs options object registers `c` (not `p`) as the short for tsConfig and does NOT register `-p`/`--project`.
    - The --help text contains the substring `npx angular-typechecker` and does NOT contain `npx atc`.
    - `git grep -n "@nx/devkit\|from 'nx\|from '../index\|from '../executors" packages/angular-typechecker/src/cli/parse-args.ts` returns nothing.
  </acceptance_criteria>
  <done>parse-args.ts compiles under the build; exports a parse function returning the discriminated union; imports only node:util + the package.json manifest (no @nx/devkit / nx / ../index / ../executors).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: console-logger.ts -- BufferingLogger implementing the core Logger</name>
  <files>packages/angular-typechecker/src/cli/console-logger.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/logger.ts (the Logger interface to implement structurally)
    - packages/angular-typechecker/src/core/emit-advisory-notices.spec.ts (the mockLogger object-of-spies shape; emitAdvisoryNotices uses only info/warn, error is the CLI infra path)
    - .planning/phases/26-pure-cli-core-exit-code-wiring/26-PATTERNS.md (Pattern 3: BufferingLogger -- all three methods route to ONE buffer)
  </read_first>
  <behavior>
    - Calling info('a'), warn('b'), error('c') in that order and reading the joined text yields 'a\nb\nc'.
    - An unused logger's joined text is the empty string ''.
    - All three methods route to the SAME buffer (everything except the report is stderr).
  </behavior>
  <action>
    Create console-logger.ts. Export a BufferingLogger class (class/getter names are D-04 discretion) that
    implements the core Logger via a type-only import from '../core/logger' (one level up: src/cli ->
    src/core). Back it with a private string[]; info/warn/error each push the message onto that one array;
    expose the buffered lines joined by '\n' via a getter (e.g. `text`) so run() (Plan 26-02) can hand the
    result to the returned stderr. Do NOT write to console or process -- the real stream write happens only in
    bin.ts (Phase 27). Import NOTHING from @nx/devkit / nx / ../index. Project TS style; ASCII-only.
  </action>
  <verify>
    <automated>nx build angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `nx build angular-typechecker` succeeds with console-logger.ts included.
    - BufferingLogger implements Logger (info/warn/error) and exposes a newline-joining text getter.
    - The Logger import from '../core/logger' is type-only.
    - `git grep -n "@nx/devkit\|from 'nx\|from '../index" packages/angular-typechecker/src/cli/console-logger.ts` returns nothing.
  </acceptance_criteria>
  <done>console-logger.ts compiles; BufferingLogger implements Logger with info/warn/error buffering into one array plus a newline-joining text getter; type-only Logger import from ../core/logger; no nx imports. (Its behavioral assertions are exercised through run() in main.spec.ts, Plan 26-02 -- no separate spec per RESEARCH Open Question 2.)</done>
</task>

<task type="auto">
  <name>Task 3: parse-args.spec.ts -- unit coverage of flag mapping + usage errors</name>
  <files>packages/angular-typechecker/src/cli/parse-args.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/cli/parse-args.ts (the module under test, from Task 1)
    - packages/angular-typechecker/src/core/emit-advisory-notices.spec.ts (the pure-function direct-call spec pattern with NO vi.mock -- parse-args needs no core stubs)
    - .planning/phases/26-pure-cli-core-exit-code-wiring/26-VALIDATION.md (the ARGS-01..05 unit-tier rows this spec must satisfy)
  </read_first>
  <action>
    Create parse-args.spec.ts as a pure Vitest unit spec (import { describe, expect, it } from 'vitest';
    direct calls, NO vi.mock -- mirror emit-advisory-notices.spec.ts). Cover: -c and --tsConfig map to
    tsConfig, repeatable -c yields the multi-element array (ARGS-02); a single -c yields a one-element array
    at this layer (ARGS-03 collapse is asserted in main.spec via the value handed to runTypecheck, NOT here);
    -p / --project and any unknown flag -> usageError result (ARGS-02 deliberately does not register -p);
    missing required --tsConfig -> usageError; missing -c value -> usageError; --max-warnings x / -1 / 1.5 ->
    usageError while --max-warnings 0 and 3 are accepted (ARGS-04 / D-08); --help and -h return a help result
    whose text contains 'npx angular-typechecker' and does NOT contain 'npx atc'; --version returns a version
    result whose text equals the imported real package.json version (ARGS-04). Assertions must be exact
    (toBe / toEqual / toContain / not.toContain), no subjective language. File name *.spec.ts (unit `test`
    target; NOT *.integration.spec.ts). Project TS style; ASCII-only.
  </action>
  <verify>
    <automated>nx test angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `nx test angular-typechecker` passes including parse-args.spec.ts.
    - There is a passing assertion for: single -c mapping, repeatable -c mapping, -p/--project unknown-flag usageError, missing required --tsConfig usageError, missing -c value usageError.
    - There is a passing assertion that --max-warnings x / -1 / 1.5 -> usageError and --max-warnings 0 / 3 accepted.
    - There is a passing assertion that --help/-h text contains 'npx angular-typechecker' and not 'npx atc', and that --version equals the imported manifest version.
  </acceptance_criteria>
  <done>nx test angular-typechecker passes including parse-args.spec.ts; every ARGS-01/02/04 behavior above has a passing assertion; the -p/--project unknown-flag case and the non-integer --max-warnings case both assert a usageError.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| argv -> parse-args | Untrusted command-line tokens cross into parse-args; the ONLY input surface in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Tampering | parse-args --max-warnings | mitigate | D-08: Number(raw) then reject !Number.isInteger(n) || n < 0 as a usageError -> exit 2; never pass a malformed number through where it could invert the verdict (evaluateResult also treats negative/NaN defensively as unset, defense-in-depth). Asserted in parse-args.spec.ts. |
| T-26-07 | Denial of Service | parse-args strict-mode throw | mitigate | Wrap parseArgs in try/catch (D-14): an unknown flag / missing value throws ERR_PARSE_ARGS_* -> mapped to a usageError, never an uncaught crash with a stack. Asserted in parse-args.spec.ts. |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | ZERO new runtime/dev dependencies this plan (ARGS-01: Node stdlib util.parseArgs only). RESEARCH Package Legitimacy Audit: no packages installed -- nothing to audit; the legitimacy gate does not apply. |
</threat_model>

<verification>
- nx build angular-typechecker succeeds (parse-args.ts + console-logger.ts compile against the real core types).
- nx test angular-typechecker passes including the new parse-args.spec.ts.
- git grep -n "@nx/devkit\|from 'nx\|from '../index\|from '../executors" packages/angular-typechecker/src/cli/parse-args.ts packages/angular-typechecker/src/cli/console-logger.ts returns nothing (nx-free by construction, D-15).
- nx lint angular-typechecker passes at maxWarnings:0; nx format:check clean.
</verification>

<success_criteria>
- parse-args.ts returns a discriminated union (options | help | version | usageError) with the D-12 flag surface (-c short, no -p), D-08 max-warnings validation, and a --help synopsis that says `npx angular-typechecker` and never `npx atc`.
- console-logger.ts exports a BufferingLogger implementing the core Logger, buffering info/warn/error into one newline-joined text.
- parse-args.spec.ts green, covering ARGS-01/02/04 mapping + usage-error branches.
- Both source files import only node:util + the package.json manifest + a type-only core Logger -- no nx / barrel / executor imports.
</success_criteria>

<output>
Create `.planning/phases/26-pure-cli-core-exit-code-wiring/26-01-SUMMARY.md` when done.
</output>
