---
phase: 11-fallow-code-quality-ci-gate
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - .github/workflows/ci.yml
  - tools/act/act-compat.sh
autonomous: true
requirements: [QUAL-01, QUAL-03]
worktree_provisioning: pattern-a   # touches NO deps; but its meaningful verification (npm ci + npx fallow) requires Plan 11-01's lockfile + .fallowrc.jsonc -> run after Wave 1 merges (depends_on 11-01)

must_haves:
  truths:
    - "A dedicated `fallow` job exists in ci.yml: path-gated (NEGATIVE `!= 'false'` form), ubuntu-latest, Node 24, `fetch-depth: 0`, runs `npx fallow audit --format json --base origin/main`"
    - "The `fallow` job is listed in the `ci` aggregate `needs:` so it is covered by the existing `contains(needs.*.result, ...)` gate"
    - "`tools/act/act-compat.sh` asserts the new job is selected on pull_request (`ci/fallow`)"
    - "`./actionlint -color` exits 0 and `bash tools/act/act-compat.sh` passes with `ci/fallow` SELECTED"
    - "ci.yml security posture is preserved: SHA-pinned actions (reused pins), top-level `contents: read` (no job re-grant), `persist-credentials: false`, no PR-metadata interpolation"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "the new `fallow` job + the one-line `ci.needs` edit"
      contains: "npx fallow audit"
    - path: "tools/act/act-compat.sh"
      provides: "the `assert_selected \"$PR_PLAN\" \"ci/fallow\" \"pull_request\"` line"
      contains: "ci/fallow"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: ".fallowrc.jsonc"
      via: "the `fallow` job's `npx fallow audit` step auto-discovers the repo-root config"
      pattern: "npx fallow audit"
    - from: ".github/workflows/ci.yml"
      to: "ci aggregate gate"
      via: "the `fallow` job in `ci.needs` is globbed by `contains(needs.*.result, ...)`"
      pattern: "needs: \\[changes, test, e2e, fallow"
    - from: "tools/act/act-compat.sh"
      to: ".github/workflows/ci.yml fallow job"
      via: "act labels the job `ci/fallow`; the assertion proves it is selected on pull_request"
      pattern: "ci/fallow"
---

<objective>
Wire the `fallow` gate into CI: add a dedicated, path-gated, SHA-pinned `fallow` job to `ci.yml` (copying the `e2e` job verbatim plus two deltas — `fetch-depth: 0` and the `npx fallow audit` run step), add it to the `ci` aggregate `needs:` (one-line edit; the `contains(needs.*.result, ...)` gate auto-includes it), and add the matching `assert_selected "$PR_PLAN" "ci/fallow" "pull_request"` line to `tools/act/act-compat.sh`.

Purpose: This makes newly-introduced dead code / duplication / over-complexity break CI LOUDLY via the single existing required status check (`ci`) — NO branch-ruleset change. It depends on Plan 11-01 because the `npm ci` + `npx fallow audit` step is only meaningful once the lockfile carries `fallow@2.103.0` and `.fallowrc.jsonc` exists.
Output: modified `.github/workflows/ci.yml` (new job + aggregate edit), modified `tools/act/act-compat.sh` (one assertion line).

Covers QUAL-01 (the path-gated SHA-pinned `fallow` CI job wired into the `ci` aggregate, `new-only` gate) and the CI/posture half of QUAL-03 (act-compat assertion, actionlint stays green, ci.yml security posture preserved, no SARIF).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Source of truth for the exact `fallow` job YAML + the aggregate edit + the act assertion (run-verified):
@.planning/phases/11-fallow-code-quality-ci-gate/11-RESEARCH.md
# Pattern map: the analog `e2e` job + `ci` aggregate + act-compat lines with file:line refs and SHA pins:
@.planning/phases/11-fallow-code-quality-ci-gate/11-PATTERNS.md
# Decisions D-10..D-15 (CI wiring, gate command, fetch-depth, path-gate, act assertion):
@.planning/phases/11-fallow-code-quality-ci-gate/11-CONTEXT.md

# The files being edited (read the FULL current content + the analog patterns to copy):
@.github/workflows/ci.yml
@tools/act/act-compat.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the path-gated, SHA-pinned `fallow` job to ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml — read the FULL file. Copy the `e2e` job (lines 123-143) verbatim as the template; note the threat-model header (lines 1-19), the top-level `permissions: contents: read` (lines 27-28), the path-gate NEGATIVE form on `e2e` (lines 124-125), and the EXACT SHA pins (`actions/checkout@93cb6e... # v5.0.1`, `actions/setup-node@a0853c... # v5.0.0`).
    - .planning/phases/11-fallow-code-quality-ci-gate/11-RESEARCH.md SS "CI wiring: the exact `fallow` job YAML" (lines 177-209) — the run-verified job block + its explanatory comment block. Use verbatim.
    - .planning/phases/11-fallow-code-quality-ci-gate/11-PATTERNS.md SS "add the `fallow` job" (the deltas: `fetch-depth: 0`, the `npx fallow audit` run step + `FALLOW_AUDIT_BASE`) + the "Action SHA pins to reuse VERBATIM" table.
    - .planning/phases/11-fallow-code-quality-ci-gate/11-CONTEXT.md D-12, D-13, D-14.
  </read_first>
  <action>
    Insert a new `fallow` job into `ci.yml` as a sibling of `e2e` (slot it AFTER `e2e`, BEFORE `act-compat`), using the run-verified YAML from 11-RESEARCH.md lines 177-209 verbatim — including its explanatory comment block above the `fallow:` key (matching the in-file commenting density of the `test`/`e2e`/`changes` jobs). The job MUST be:
    - `needs: changes` + `if: ${{ needs.changes.outputs.code != 'false' }}` — the NEGATIVE `!= 'false'` form copied verbatim from `e2e` (D-14; load-bearing so the job stays in the `act -n` plan under empty filter output, and a planning/docs-only PR path-skips it).
    - `runs-on: ubuntu-latest`, Node 24 (matching e2e/release).
    - `actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1` with BOTH `persist-credentials: false` AND `fetch-depth: 0`. The `fetch-depth: 0` is the ONLY checkout in the file that needs it (LOAD-BEARING for `new-only` base-snapshot attribution against `origin/main`'s merge-base; a shallow checkout breaks new-vs-inherited attribution — 11-RESEARCH.md Pitfall 3, D-13). Reuse the EXACT existing SHA pin; do NOT invent a new one (Dependabot keeps the pins in lockstep).
    - `actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0` with `node-version: 24`, `cache: npm` (reuse the EXACT existing pin).
    - `run: npm ci` then `run: npx fallow audit --format json --base origin/main` with `env: FALLOW_AUDIT_BASE: origin/main` (D-12 gate command; `--format json` is for the CI log, the EXIT CODE gates; `FALLOW_AUDIT_BASE` pins the base ref defensively per D-13).
    Do NOT use `--ci`/SARIF output (D-12 — would need `security-events: write`, contradicting the top-level `contents: read`). Do NOT add any `permissions:` block to the job (it re-grants NOTHING; inherits the top-level `contents: read`). Do NOT interpolate any `${{ github.event.* }}` PR metadata into a run step (fixed target ids + flags only — preserves the threat-model header guarantee).
    The threat-model header (lines 1-19) MUST remain byte-unchanged; the new job is consistent with every guarantee it states (SHA-pinned, read-only, persist-credentials:false, no PR-metadata interpolation).
  </action>
  <verify>
    <automated>git grep -n "npx fallow audit --format json --base origin/main" .github/workflows/ci.yml && git grep -n "fetch-depth: 0" .github/workflows/ci.yml && git grep -c "93cb6efe18208431cddfb8368fd83d5badbf9bfd" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "^  fallow:" .github/workflows/ci.yml` matches (the new job key exists at job indentation).
    - `git grep -n "npx fallow audit --format json --base origin/main" .github/workflows/ci.yml` matches exactly once.
    - `git grep -n "fetch-depth: 0" .github/workflows/ci.yml` matches exactly once (only the `fallow` job carries it).
    - `git grep -n "FALLOW_AUDIT_BASE: origin/main" .github/workflows/ci.yml` matches.
    - The `fallow` job uses the existing `actions/checkout@93cb6efe...# v5.0.1` and `actions/setup-node@a0853c24...# v5.0.0` pins (no new SHA introduced).
    - NO `--ci` / `sarif` token and NO `permissions:` block inside the `fallow` job; NO `${{ github.event.` interpolation in the `fallow` job run steps.
    - The threat-model header (lines 1-19) is unchanged.
  </acceptance_criteria>
  <done>The `fallow` job is added to ci.yml between `e2e` and `act-compat`, path-gated with the NEGATIVE form, SHA-pinned with the reused pins, `fetch-depth: 0` on checkout, running `npx fallow audit --format json --base origin/main` with `FALLOW_AUDIT_BASE`, no SARIF, no extra permissions, no PR-metadata interpolation, header byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add `fallow` to the `ci` aggregate `needs:` list</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml — the `ci` aggregate job (lines 193-205): the current `needs: [changes, test, e2e, act-compat, lint-workflows]` (line 194) and the gate expression `contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled')` (line 200) + the `'skipped'`-tolerance comment (lines 186-192).
    - .planning/phases/11-fallow-code-quality-ci-gate/11-RESEARCH.md SS "Aggregate `ci` job change (one line)" (lines 211-216).
    - .planning/phases/11-fallow-code-quality-ci-gate/11-PATTERNS.md SS "edit the `ci` aggregate `needs:`".
    - .planning/phases/11-fallow-code-quality-ci-gate/11-CONTEXT.md D-10.
  </read_first>
  <action>
    Edit ONLY line 194's `needs:` list to add `fallow`: change `needs: [changes, test, e2e, act-compat, lint-workflows]` to `needs: [changes, test, e2e, fallow, act-compat, lint-workflows]` (slot `fallow` after `e2e`, matching its workflow-file ordering, per D-10 / 11-RESEARCH.md:214).
    Do NOT touch the gate expression (line 200): `needs.*` already globs every job in the `needs:` list, so adding `fallow` to the list auto-includes it in the `contains(needs.*.result, ...)` gate — no gate-expression edit. `'skipped'` STAYS out of the fail set (the documented path-skip tolerance, lines 186-192), so a planning/docs-only PR that path-skips `fallow` keeps `ci` green. The single required status check stays `ci` — NO Default-branch ruleset change.
  </action>
  <verify>
    <automated>git grep -n "needs: \[changes, test, e2e, fallow, act-compat, lint-workflows\]" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "needs: \[changes, test, e2e, fallow, act-compat, lint-workflows\]" .github/workflows/ci.yml` matches exactly the `ci` aggregate `needs:` line.
    - The gate expression line (`contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled')`) is UNCHANGED (`git grep -n "contains(needs.\*.result, 'failure')" .github/workflows/ci.yml` still matches once).
    - The `ci` job id+name is still exactly `ci` (the required status check is unchanged).
  </acceptance_criteria>
  <done>`fallow` is added to the `ci` aggregate `needs:` list (after `e2e`); the gate expression and the `ci` job id/name are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add the `ci/fallow` act-compat assertion, then run the full workflow-validation suite</name>
  <files>tools/act/act-compat.sh</files>
  <read_first>
    - tools/act/act-compat.sh — the `pull_request` block (lines 110-116) with the existing `assert_selected "$PR_PLAN" "ci/<jobid>" "pull_request"` lines; the `PUSH_MAIN_PLAN` (lines 120-123) and `PUSH_TAG_PLAN`/`DISPATCH_PLAN` (lines 127-133) blocks; the `assert_selected` helper (lines 71-78).
    - .planning/phases/11-fallow-code-quality-ci-gate/11-RESEARCH.md SS "act-compat.sh: the exact assertion" (lines 224-234) — the ONE line to add + WHY not to touch the other blocks.
    - .planning/phases/11-fallow-code-quality-ci-gate/11-PATTERNS.md SS "add the `ci/fallow` assertion".
    - .planning/phases/11-fallow-code-quality-ci-gate/11-CONTEXT.md D-15.
  </read_first>
  <action>
    Add EXACTLY ONE line to the `pull_request` block of `act-compat.sh`, alongside the other `ci/*` assertions (after the `ci/e2e` line at line 112, before `ci/act-compat` at line 113): `assert_selected "$PR_PLAN" "ci/fallow" "pull_request"`. Job id `fallow` (no `name:` set) maps to act label `ci/fallow` (act labels jobs `<workflow>/<jobid>`; every existing assertion uses `ci/<jobid>`) — D-15, 11-RESEARCH.md:226-230.
    Do NOT add `ci/fallow` to the `PUSH_MAIN_PLAN` block (it asserts only a representative subset `ci/test-`/`ci/ci`; a push-main `ci/fallow` is OPTIONAL — omit for the minimal change). Do NOT add it to the `PUSH_TAG_PLAN`/`DISPATCH_PLAN` blocks (those assert only `release/publish` from `release.yml`; `fallow` is in `ci.yml`, so adding it there is noise — 11-RESEARCH.md:232). Do NOT add an `assert_absent` for it anywhere.
    After the edit, run the full workflow-validation suite to prove QUAL-01/QUAL-03 statically:
    1. `./actionlint -color` — must exit 0 (the new job + the extended `needs.*.result` graph type-check clean; the only residual research uncertainty A1 is verified here). If `actionlint` is not on PATH locally, install the pinned 1.7.7 the way the `lint-workflows` job does (ci.yml lines 172-174) or defer this specific check to the CI `lint-workflows` job and note it in the SUMMARY.
    2. `bash tools/act/act-compat.sh` — must pass with `ci/fallow SELECTED` for `pull_request` (and all existing assertions still pass). If Docker/act is not running locally, note the deferral to the CI `act-compat` job (per the Phase 6/7 precedent — act's `needs.*.result`/skipped arithmetic is only authoritative on a real run anyway) and confirm the script's static `act --validate` step still parses both workflows.
  </action>
  <verify>
    <automated>git grep -n 'assert_selected "$PR_PLAN" "ci/fallow" "pull_request"' tools/act/act-compat.sh && bash tools/act/act-compat.sh</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n 'ci/fallow' tools/act/act-compat.sh` shows exactly ONE line: `assert_selected "$PR_PLAN" "ci/fallow" "pull_request"` inside the `pull_request` block.
    - `ci/fallow` is NOT present in the `PUSH_MAIN_PLAN`, `PUSH_TAG_PLAN`, or `DISPATCH_PLAN` blocks; no `assert_absent` for `ci/fallow`.
    - `bash tools/act/act-compat.sh` exits 0 with `[PASS] pull_request: ci/fallow SELECTED` in its output (or, if act/Docker is unavailable locally, the `act --validate` guard still passes and the deferral to the CI `act-compat` job is recorded in the SUMMARY).
    - `./actionlint -color` exits 0 (or the deferral to the CI `lint-workflows` job is recorded in the SUMMARY with the reason — actionlint not provisioned on the dev box).
  </acceptance_criteria>
  <done>The single `ci/fallow` pull_request assertion is added; the other act-compat blocks are unchanged; `actionlint` and `act-compat.sh` are green (or their unavoidable local-tooling deferrals to the existing CI jobs are documented).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted PR -> ci.yml `fallow` job | a fork/untrusted PR triggers the new job on the SAFE `pull_request` (code-checkout) trigger |
| GitHub Actions runner -> repo (token) | the checkout token scope available to the new job |
| third-party actions (`checkout`, `setup-node`) -> CI | mutable-tag repoint risk on the actions the new job uses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | mutable action tag repointed to malicious code (tj-actions vector) | mitigate | The `fallow` job reuses the EXACT 40-char SHA pins already in ci.yml (`actions/checkout@93cb6e... # v5.0.1`, `actions/setup-node@a0853c... # v5.0.0`); Dependabot (github-actions ecosystem) keeps them in lockstep. No new/un-pinned action introduced. PRESERVES the header's SHA-pin guarantee (Task 1 acceptance). |
| T-11-04 | Elevation of Privilege | untrusted-PR exploitation of the new CI job | mitigate | Reuses the SAFE `pull_request` trigger (never the privileged sibling); top-level `permissions: contents: read` unchanged, the job re-grants NO write (Task 1 forbids a job `permissions:` block); `npx fallow audit` reads git + filesystem only. PRESERVES the header's least-privilege guarantee. |
| T-11-05 | Tampering | PR-metadata command injection in the new run step | mitigate | The `fallow` job's run steps use FIXED target ids + flags only (`npx fallow audit --format json --base origin/main`); NO `${{ github.event.* }}` interpolation (Task 1 acceptance forbids it). PRESERVES the header's no-PR-metadata-interpolation guarantee. |
| T-11-06 | Info Disclosure | checkout credential persisted to `.git/config` and leaked | mitigate | `persist-credentials: false` on the new job's checkout (matches every other checkout in the file). PRESERVES the header guarantee. |
| T-11-07 | Elevation of Privilege | output-channel privilege creep (SARIF needs `security-events: write`) | mitigate | Gate uses `--format json` (CI-log only) NOT `--ci`/SARIF (D-12; Task 1 forbids SARIF). No `security-events` permission is requested — the least-privilege `contents: read` posture holds. |

No package installs in this plan (the `fallow` devDep + lockfile are owned by Plan 11-01); no `[ASSUMED]`/`[SUS]` packages -> no legitimacy checkpoint task here.
</threat_model>

<verification>
- `./actionlint -color` exits 0 (ci.yml is GitHub-spec-valid incl. the extended `needs.*.result` graph) — or its local-tooling deferral to the CI `lint-workflows` job is documented.
- `bash tools/act/act-compat.sh` passes with `ci/fallow SELECTED` on `pull_request` (and all prior assertions still pass) — or its local act/Docker deferral to the CI `act-compat` job is documented, with `act --validate` still parsing both workflows.
- `git grep -n "needs: \[changes, test, e2e, fallow, act-compat, lint-workflows\]" .github/workflows/ci.yml` matches; the gate expression and `ci` job id/name are unchanged.
- ci.yml threat-model header (lines 1-19) byte-unchanged; the new job adds no `permissions:` block, no SARIF, no PR-metadata interpolation, and reuses the existing SHA pins.
- (Authoritative integration check, MANUAL — see Manual-Only in 11-VALIDATION.md) The Phase 11 PR's own real `ci` run is green with the new `fallow` job present, and a throwaway PR that introduces dead code goes red.
</verification>

<success_criteria>
QUAL-01 satisfied: a dedicated, path-gated, SHA-pinned `fallow` job (ubuntu-latest, Node 24, `fetch-depth: 0`, `new-only` config-pinned gate, `npx fallow audit --format json --base origin/main`) is wired into the `ci` aggregate `needs:` and covered by its existing `contains(needs.*.result, ...)` gate — the single required check stays `ci`, no ruleset change. The CI/posture half of QUAL-03 satisfied: `tools/act/act-compat.sh` asserts `ci/fallow` selected on pull_request, actionlint stays green, and the ci.yml security posture (SHA pins, `contents: read`, `persist-credentials: false`, no PR-metadata interpolation, no SARIF) is preserved.
</success_criteria>

<output>
Create `.planning/phases/11-fallow-code-quality-ci-gate/11-02-SUMMARY.md` when done.
</output>
