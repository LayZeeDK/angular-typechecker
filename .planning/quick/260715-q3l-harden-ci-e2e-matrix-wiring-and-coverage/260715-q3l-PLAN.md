---
phase: quick-260715-q3l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - tools/ci/list-e2e-projects.mjs
  - e2e/angular-typechecker-ng-cli-e2e/project.json
  - packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts
autonomous: true
requirements:
  - FIX-1-shell-interp
  - FIX-2-empty-discovery
  - FIX-3-registry-serialize
  - FIX-4-fallback-shrink

must_haves:
  truths:
    - "The ci.yml e2e job passes the matrix project through a PROJECT env var, never as an inline ${{ matrix.project }} in a shell run line"
    - "list-e2e-projects.mjs throws (non-zero exit) when zero e2e projects are discovered"
    - "The ng-cli-e2e e2e target sets parallelism:false"
    - "GUARD-01b asserts every e2e project whose global-setup starts a local registry sets parallelism:false"
    - "The ci.yml e2e matrix static fallback is a single-element act-plan sentinel"
    - "nx test angular-typechecker is green (all GUARD-01/01b/01c/01d/01e pass, incl. the new registry-serialization it and the execSync discovery==enumeration check)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "PROJECT env var + -p \"$PROJECT\" run lines + shrunk single-element fallback + refreshed comments"
      contains: "PROJECT: ${{ matrix.project }}"
    - path: "tools/ci/list-e2e-projects.mjs"
      provides: "Empty-discovery throw guarding against a silent zero-cell e2e matrix"
      contains: "names.length === 0"
    - path: "e2e/angular-typechecker-ng-cli-e2e/project.json"
      provides: "parallelism:false on the e2e target (serialize the 2nd Verdaccio publisher)"
      contains: "\"parallelism\": false"
    - path: "packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts"
      provides: "New GUARD-01b registry-serialization it + corrected install-e2e comment"
      contains: "startLocalRegistry"
  key_links:
    - from: ".github/workflows/ci.yml e2e run steps"
      to: "PROJECT env var"
      via: "-p \"$PROJECT\""
      pattern: "-p \\\"\\$PROJECT\\\""
    - from: "ci-e2e-coverage-guard.spec.ts new it"
      to: "e2eTargetParallelism via global-setup startLocalRegistry detection"
      via: "collectE2eFiles('global-setup.ts') + isTsComment"
      pattern: "e2eTargetParallelism"
---

<objective>
Address four triaged CI review findings on the e2e matrix wiring and its coverage guard. All four are bounded, low-risk hardening fixes with no product-code or version change.

Purpose: restore ci.yml's own command-injection threat-model invariant (fork-PR-controlled matrix value no longer interpolated into a shell), fail loud instead of silently dropping the whole tarball tier on empty discovery, serialize the second Verdaccio publisher and encode that invariant in the guard, and shrink the unguarded static matrix fallback to an honest act-plan sentinel.

Output: edits to ci.yml, the e2e discovery script, the ng-cli-e2e project.json, and the coverage-guard spec; no version mutation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@.github/workflows/ci.yml
@tools/ci/list-e2e-projects.mjs
@e2e/angular-typechecker-ng-cli-e2e/project.json
@e2e/angular-typechecker-ng-cli-e2e/src/global-setup.ts
@packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts

<interfaces>
<!-- Contracts the executor needs; already present in the guard spec. Do NOT re-derive. -->

Existing helpers in ci-e2e-coverage-guard.spec.ts (reuse, do not reimplement):
- collectE2eFiles(suffix: string): string[]   // absolute paths under e2e/ ending with suffix, skips IGNORED_DIRS
- isTsComment(line: string): boolean           // true for `//` line comments or `*` block-comment continuations
- e2eTargetParallelism(project: string): boolean | undefined  // reads e2e/<project>/project.json targets.e2e.parallelism
- enumerateE2eProjects(root): string[]         // name===dir convention

GUARD regexes that MUST keep matching after the ci.yml edits (verify by keeping the literal substrings):
- GUARD-01 / GUARD-01b: /^(?!\s*#).*\brun-many\s+-t\s+e2e\b/m   -> keep `run-many -t e2e` before `-p "$PROJECT"`
- GUARD-01c:            /^(?!\s*#).*\brun-many\s+-t\s+typecheck\b/m -> keep `run-many -t typecheck` before `-p "$PROJECT"`
- GUARD-01b matrix:     /^(?!\s*#).*fromJSON\(\s*needs\.discover\.outputs\.projects/m -> keep `fromJSON(needs.discover.outputs.projects || '[...]')`

global-setup path convention: e2e/<project>/src/global-setup.ts
  -> project name = basename(dirname(dirname(globalSetupPath)))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the ci.yml e2e job (FIX 1 shell interpolation + FIX 4 fallback shrink)</name>
  <files>.github/workflows/ci.yml</files>
  <action>
FIX 1 (command-injection hardening of the e2e job's two run steps). The matrix value derives from e2e/*/project.json `name` (fork-PR-controlled), so interpolating it into a shell command is a command-injection surface that violates the file's own threat-model comment (ci.yml:3-8, "fixed target ids + flags only ... no command-injection surface").
- In the e2e job's existing `env:` block (which already has `NX_DAEMON: false`), add a line `PROJECT: ${{ matrix.project }}`. In an `env:` block the expression is set by the runner, not passed through a shell, so this is safe.
- Change the typecheck run line from `npx nx run-many -t typecheck -p ${{ matrix.project }}` to `npx nx run-many -t typecheck -p "$PROJECT"`.
- Change the e2e run line from `npx nx run-many -t e2e -p ${{ matrix.project }}` to `npx nx run-many -t e2e -p "$PROJECT"`.
- KEEP the literal `run-many -t typecheck` and `run-many -t e2e` substrings intact (GUARD-01/01b/01c regex-match them; see interfaces).
- Update the two adjacent run-step comments (the typecheck pre-install fast-fail comment and the e2e run comment) to note the project is passed via the `$PROJECT` env var, not interpolated into the shell -- restoring the top-of-file threat-model invariant. Do NOT edit the top-of-file threat-model block itself; the fix makes it true again.

FIX 4 (shrink the unguarded static matrix fallback). The `matrix: project:` line uses `fromJSON(needs.discover.outputs.projects || '["angular-typechecker-cache-e2e","angular-typechecker-install-e2e","angular-typechecker-matrix-e2e","angular-typechecker-ng-cli-e2e"]')`. That static 4-project list exists ONLY so `act -n` (empty discover output) does not hit `fromJSON('')`; act only needs a non-empty `ci/e2e` plan token (tools/act/act-compat.sh:112), not an accurate list.
- Shrink the fallback to the single-element sentinel `'["angular-typechecker-cache-e2e"]'`. KEEP the `fromJSON(needs.discover.outputs.projects || '...')` shape (GUARD-01b matrix regex matches it).
- Update the adjacent comment to state the fallback is a non-authoritative act-plan placeholder (real CI always uses the discover-job output), and soften the nearby "no static list to drift" wording so it reads accurately (e.g. the fallback is a placeholder, not a coverage source).

Do NOT touch the "install-e2e + cache-e2e stay parallelism:false" enumeration comment or any other e2e-job comment beyond what FIX 1/FIX 4 require. ASCII only.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker</automated>
  </verify>
  <done>PROJECT env var present in the e2e job; both run lines use `-p "$PROJECT"`; fallback is `'["angular-typechecker-cache-e2e"]'`; GUARD-01/01b/01c still pass (nx test green). Optional: `./actionlint -color` clean on ci.yml if actionlint is locally available.</done>
</task>

<task type="auto">
  <name>Task 2: Fail loud on empty e2e discovery (FIX 2)</name>
  <files>tools/ci/list-e2e-projects.mjs</files>
  <action>
In listE2eProjects(), immediately BEFORE `return names.sort();`, add a guard: if `names.length === 0` throw an Error with the message `list-e2e-projects: no e2e projects discovered under e2e/ (expected at least one project.json with an \`e2e\` target)`. Use braces on the `if` body and keep the surrounding blank-line style. Add a short comment above the guard explaining WHY: an empty array would expand the CI `e2e` matrix to zero cells, GitHub reports the job `skipped`, and the `ci` aggregate excludes `skipped` from its fail set -- so the whole tarball tier would silently drop while `ci` stays green. ASCII only; no other logic changes.
  </action>
  <verify>
    <automated>node tools/ci/list-e2e-projects.mjs</automated>
  </verify>
  <done>With the 4 e2e projects present the CLI still writes the sorted 4-element JSON array to stdout and exits 0 (no throw). The GUARD-01b execSync discovery==enumeration check still passes under `nx test angular-typechecker`.</done>
</task>

<task type="auto">
  <name>Task 3: Serialize the 2nd Verdaccio publisher and encode the invariant in the guard (FIX 3)</name>
  <files>e2e/angular-typechecker-ng-cli-e2e/project.json, packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts</files>
  <action>
FIX 3(a) -- e2e/angular-typechecker-ng-cli-e2e/project.json: add `"parallelism": false` to the `e2e` target object (sibling of `executor`/`dependsOn`/`outputs`/`options`), mirroring install-e2e and cache-e2e. ng-cli-e2e's global-setup starts a 2nd Verdaccio registry on 127.0.0.1:4873, so it must run solo under the LOCAL `nx run-many -t e2e --parallel=2` command.

FIX 3(b) -- ci-e2e-coverage-guard.spec.ts, inside the existing `describe('GUARD-01b: ...')` block: add a new `it` encoding the REAL invariant "every e2e project whose global-setup starts a local registry sets parallelism:false on its e2e target". Implement using the existing helpers (do NOT reimplement): iterate `collectE2eFiles('global-setup.ts')`; for each file, read it and treat it as a registry-starter when any non-comment line (filter via `isTsComment`) `.includes('startLocalRegistry')`; derive the project name as the parent of the `src/` dir containing the global-setup (basename(dirname(dirname(path))) -- add `basename` to the existing `node:path` import); collect the registry-starting project names; assert the collected list is non-empty (a rename that hides all registry-starters must fail loud, matching this file's anti-vacuous-green pattern); then for each, assert `e2eTargetParallelism(project) === false` with a fail-loud, located message. This catches ng-cli-e2e now AND any future registry-starting e2e project. KEEP the existing dedicated install-e2e and cache-e2e `it` blocks (their rationale messages are worth keeping).

FIX 3(c) -- ci-e2e-coverage-guard.spec.ts: correct the now-stale install-e2e assertion message (~line 315) that calls install-e2e "the sole Verdaccio publisher" -- it is no longer sole; ng-cli-e2e is a second publisher and both are serialized (parallelism:false). Rewrite the message to say install-e2e is one of two Verdaccio publishers (with ng-cli-e2e) and running it alone keeps exactly one local registry live (no port/storage/htpasswd/authToken contention, no cross-registry yarn metadata-cache poisoning). Note: ci.yml carries no literal "sole Verdaccio publisher" claim, so there is no ci.yml comment to refresh for FIX 3(c) -- confirm by scanning and leave ci.yml untouched in this task.

Blank lines around control flow, braces on all bodies, ASCII only.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker</automated>
  </verify>
  <done>ng-cli-e2e e2e target has parallelism:false; the new registry-serialization `it` passes (asserts install-e2e + ng-cli-e2e serialize); install-e2e/cache-e2e its still present and green; all GUARD-01/01b/01c/01d/01e pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fork PR -> CI e2e job | e2e/*/project.json `name` (fork-PR-controlled) flows into the `discover` output and the e2e matrix value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q3l-01 | Tampering/Elevation | ci.yml e2e job run steps | mitigate | FIX 1: pass matrix value via `PROJECT` env var and reference as `"$PROJECT"`; never interpolate `${{ matrix.project }}` into a shell command -- removes the command-injection surface, restoring ci.yml:3-8 invariant |
| T-q3l-02 | Denial of Service (silent coverage loss) | tools/ci/list-e2e-projects.mjs | mitigate | FIX 2: throw on zero-project discovery so the e2e tier fails loud instead of expanding to zero matrix cells and passing the `ci` aggregate green as `skipped` |

No package-manager installs are added by this plan, so no supply-chain (T-*-SC) checkpoint applies.
</threat_model>

<verification>
Run from the workspace root after all three tasks:
- `npx nx test angular-typechecker` -- GUARD-01/01b/01c/01d/01e all pass, including the new registry-serialization `it` and the still-green execSync discovery==enumeration check.
- `node tools/ci/list-e2e-projects.mjs` -- still emits the sorted 4-project JSON array (no throw with 4 projects present).
- `npx nx format:check` on the changed files (run `npx nx format:write` first if needed).
- `npx nx run-many -t lint` -- clean at maxWarnings:0.
- Optional (if actionlint is locally installed): `./actionlint -color` on ci.yml; otherwise the CI `lint-workflows` job covers it.
</verification>

<success_criteria>
- ci.yml e2e job: `PROJECT: ${{ matrix.project }}` in `env:`, both run lines use `-p "$PROJECT"`, matrix fallback shrunk to `'["angular-typechecker-cache-e2e"]'`, adjacent comments accurate.
- list-e2e-projects.mjs throws on empty discovery; still returns the 4-element array otherwise.
- ng-cli-e2e e2e target has `parallelism: false`.
- New GUARD-01b `it` asserts every registry-starting e2e project serializes; install-e2e/cache-e2e its and their messages retained; stale "sole Verdaccio publisher" wording corrected.
- `nx test angular-typechecker`, `nx run-many -t lint`, and `nx format:check` on changed files all green. No product-code or version change (package.json stays 0.2.0).
</success_criteria>

<output>
Create `.planning/quick/260715-q3l-harden-ci-e2e-matrix-wiring-and-coverage/260715-q3l-SUMMARY.md` when done.
</output>
