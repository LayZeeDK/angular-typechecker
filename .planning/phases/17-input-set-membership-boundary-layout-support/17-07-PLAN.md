---
phase: 17-input-set-membership-boundary-layout-support
plan: 07
type: execute
wave: 4
depends_on: ["17-03", "17-04", "17-05"]
files_modified:
  - fixtures/external-template-tripwire/**
  - fixtures/clean-template-host/**
  - packages/angular-typechecker/src/core/dual-identity-tripwire.spec.ts
  - packages/angular-typechecker/src/core/external-template.integration.spec.ts
  - packages/angular-typechecker/src/core/fault-isolation.integration.spec.ts
autonomous: true
requirements: [SB-02, SB-03]

must_haves:
  truths:
    - "D-04: tripwires guard the branch-4a relatedInformation attribution invariant and the D-04a base-kept-non-rootName-is-in-graph clean-host invariant"
    - "External-template diagnostics DO carry a .ts relatedInformation (so a future Angular attribution flip is caught LOUD, not silently dropping diagnostics) -- D-09.2 tripwire"
    - "A declared-root's own failing diagnostic is KEPT on a case-INSENSITIVE FS, a case-SENSITIVE FS, AND through a symlink/junction; a genuine transitive dep is NOT kept -- D-09a(ii) dual-identity"
    - "A clean host using an external .html template AND an indirect-mapped inline template reports suppressedInGraph == 0 -- D-09a(i) base-clause invariant"
    - "A .ngtypecheck.ts shim diagnostic and an external-template diagnostic are never silently dropped -- D-09a(iii)"
    - "A whole-program TCB-generation Fatal (NG3004) now yields coverage-incomplete, and the fatal-code surface is version-pinned so drift beyond NG3004 trips LOUD -- D-09a(iv)/FM-9"
  artifacts:
    - path: "packages/angular-typechecker/src/core/external-template.integration.spec.ts"
      provides: "D-09.2 + D-09a(i)(iii) tripwires against real external/indirect templates"
    - path: "packages/angular-typechecker/src/core/dual-identity-tripwire.spec.ts"
      provides: "D-09a(ii) declared-root-kept across FS modes + transitive dep not kept"
    - path: "packages/angular-typechecker/src/core/fault-isolation.integration.spec.ts"
      provides: "D-09a(iv)/FM-9 drift probe: TCB-abort -> coverage-incomplete + pinned fatal-code surface"
  key_links:
    - from: "external-template NG diagnostic"
      to: "a .ts relatedInformation owner"
      via: "the D-09.2 tripwire assertion"
      pattern: "relatedInformation"
    - from: "templateCheckAborted (NG3004)"
      to: "coverage-incomplete verdict"
      via: "evaluateResult (17-04)"
      pattern: "coverage-incomplete|templateCheckAborted"
---

<objective>
Ship the D-09a MANDATORY tripwire fixtures + probes that guard the UNDOCUMENTED invariants
the hardened design rests on: (i) the external-template `relatedInformation` attribution
(D-09.2 -- a future Angular flip must trip LOUD), (ii) dual-identity declared-root recovery
across case-insensitive / case-sensitive / symlink filesystems (and a transitive dep NOT
kept), (iii) shim + external-template never silently dropped, (iv) the FM-9 drift probe (a
TCB-generation Fatal now yields coverage-incomplete; the fatal-code surface is version-pinned
to Angular 22.0.4 / TS 6.0.3).

Purpose: the input-set boundary + branch 4a rest on invariants TypeScript/Angular do not
document (a declared root's `SourceFile.fileName` is byte-equal to its rootName pre-realpath;
external templates attribute to the `.html` but carry a `.ts` `relatedInformation`; NG3004 is
the only TCB-generation Fatal at v22.0.4). These tripwires convert a silent future break into
a loud CI failure -- the charter's "never a silent false pass" applied to Angular drift.
Output: two focused fixtures + two new specs + the extended fault-isolation drift probe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

READ-FIRST source-of-truth:
@.planning/phases/17-input-set-membership-boundary-layout-support/17-DECISION-input-set-boundary.md
@.planning/phases/17-input-set-membership-boundary-layout-support/17-CONTEXT.md
@.planning/phases/17-input-set-membership-boundary-layout-support/17-RESEARCH.md

Conventions + the pipeline under test:
@packages/angular-typechecker/src/core/walk-references.integration.spec.ts
@packages/angular-typechecker/src/core/fault-isolation.integration.spec.ts
@packages/angular-typechecker/src/core/filter-diagnostics.spec.ts
@packages/angular-typechecker/src/core/diagnostic-codes.ts
@fixtures/ng-baseline/error.component.ts
@fixtures/fault-isolation/tcb-poison.component.ts

Prior-plan SUMMARYs (delivered pipeline: keep()/counter, verdict, executor):
@.planning/phases/17-input-set-membership-boundary-layout-support/17-01-SUMMARY.md
@.planning/phases/17-input-set-membership-boundary-layout-support/17-04-SUMMARY.md

Notes: D-09a fixtures are version-pinned to Angular 22.0.4 / TS 6.0.3. Dual-identity FS
variants are exercised at the UNIT level via injected `realpath` + `useCaseSensitiveFileNames`
(deterministic cross-platform; mirrors the existing filter-diagnostics.spec OUT-02/T8 idiom) --
do NOT depend on creating real symlinks/junctions in the test run.
</context>

<tasks>

<task type="auto">
  <name>Task 1: dual-identity declared-root tripwire (D-09a ii)</name>
  <files>packages/angular-typechecker/src/core/dual-identity-tripwire.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/filter-diagnostics.spec.ts (the injected-realpath + useCaseSensitiveFileNames idiom; the `diag(fileName)` helper; the T8/OUT-02 tests)
    - 17-DECISION-input-set-boundary.md ("Dual-identity is SOUND for declared .ts/.d.ts": SourceFile.fileName === rootName pre-realpath; a transient realpath throw is recovered via the raw form)
    - 17-CONTEXT.md D-02 (dual-identity) + D-09a(ii)
  </read_first>
  <action>
    Create `dual-identity-tripwire.spec.ts` calling `filterDiagnostics` (from filter-diagnostics.ts) with a declared `inputTs` rootName and a diagnostic on THAT rootName, asserting it is KEPT (never suppressedInGraph) across THREE filesystem modes: (1) case-INSENSITIVE (`useCaseSensitiveFileNames: false`, a mixed-case diagnostic path folds to the rootName); (2) case-SENSITIVE (`useCaseSensitiveFileNames: true`, exact-case match); (3) symlink/junction -- the diagnostic file's injected `realpath` THROWS (or resolves out-of-tree) yet the declared root is STILL KEPT via the raw-form dual-identity recovery (the load-bearing D-02 invariant). In the SAME spec, assert a genuine TRANSITIVE dependency `.ts` (not in `inputTs`, not under base) is SUPPRESSED into `suppressedInGraphErrorCount` (NOT kept, NOT third-party). Label each assertion with the D-09a(ii) invariant it guards so a future break reads as a loud, named regression.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- dual-identity</automated>
  </verify>
  <acceptance_criteria>
    - Declared-root diagnostic KEPT in all three FS modes (case-insensitive, case-sensitive, symlink-throw recovery).
    - Transitive dep `.ts` -> `suppressedInGraphErrorCount >= 1`, not kept, not third-party.
    - `npx nx test angular-typechecker -- dual-identity` green.
  </acceptance_criteria>
  <done>The dual-identity declared-root-kept invariant is guarded across FS modes with a transitive-dep negative, injected realpath, no real filesystem.</done>
</task>

<task type="auto">
  <name>Task 2: external-template relatedInformation tripwire + clean-host base-clause proof (D-09.2, D-09a i + iii)</name>
  <files>fixtures/external-template-tripwire/**, fixtures/clean-template-host/**, packages/angular-typechecker/src/core/external-template.integration.spec.ts</files>
  <read_first>
    - fixtures/ng-baseline/error.component.ts + error.component.html (external templateUrl + NG8xxx codeframe pattern)
    - packages/angular-typechecker/src/core/walk-references.integration.spec.ts (findWorkspaceRoot + fixtureTsConfig idiom)
    - packages/angular-typechecker/src/core/diagnostic-codes.ts (NG codes; ngCodeOf)
    - 17-CONTEXT.md D-04a (Angular attaches template diagnostics to NON-rootName files: external templateUrl -> .html; indirect inline -> synthetic "<ts> (X template)"; base-clause keeps them) + D-09.2/D-09a(i)(iii)
    - .planning/spikes/008-external-template-attribution/ (relatedInformation -> owning .ts, message "Error/Warning occurs in the template of component X")
  </read_first>
  <action>
    Create `fixtures/external-template-tripwire/` -- a leaf/solution tsconfig with a component using an EXTERNAL `templateUrl` `.html` that carries an NG template error (e.g. NG8002 unknown-element/binding). Create `fixtures/clean-template-host/` -- a CLEAN host (its `typecheck` points at the host solution tsconfig) whose declared surface includes a component with an external `.html` template AND another component with an INDIRECT inline template (a template that itself references something producing a synthetic `"<component.ts> (X template)"` diagnostic file name in the error path -- both CLEAN here). Create `external-template.integration.spec.ts`: (D-09.2 tripwire) `runTypecheck` against `external-template-tripwire` and assert the NG diagnostic's `relatedInformation` array contains an entry whose `file.fileName` ends in `.ts` (attribution invariant -- the LOUD guard: if Angular ever stops attaching the `.ts` relatedInformation, or flips attribution back to the `.ts`, this fails); also assert the diagnostic is KEPT (present in `result.diagnostics`), proving neither the shim nor the external template is silently dropped (D-09a iii). (D-09a i) `runTypecheck` against `clean-template-host` and assert `result.suppressedInGraphErrorCount === 0 && result.suppressedInGraphWarningCount === 0` -- the clean host's own external `.html` and indirect inline template are classified in-graph via the D-04a base clause, never counted as suppressed-out-of-graph. Inherit the config testTimeout.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- external-template</automated>
  </verify>
  <acceptance_criteria>
    - The external-template NG diagnostic carries a `.ts` `relatedInformation` (D-09.2) and is present in `result.diagnostics` (not dropped).
    - The clean host (external .html + indirect inline template) -> `suppressedInGraphErrorCount === 0 && suppressedInGraphWarningCount === 0`.
    - `npx nx test angular-typechecker -- external-template` green.
  </acceptance_criteria>
  <done>The attribution invariant + base-clause clean-host invariant + shim/external never-dropped are guarded by real-compiler tripwires.</done>
</task>

<task type="auto">
  <name>Task 3: FM-9 TCB-abort drift probe (D-09a iv)</name>
  <files>packages/angular-typechecker/src/core/fault-isolation.integration.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/fault-isolation.integration.spec.ts (the existing RES-02 TCB-poison assertions against fixtures/fault-isolation/tcb-poison.component.ts)
    - packages/angular-typechecker/src/core/diagnostic-codes.ts (TCB_GENERATION_FATAL_DIAGNOSTIC_CODE === NG(3004); the module header explaining NG3004 is the ONLY TCB-generation Fatal at v22.0.4)
    - packages/angular-typechecker/src/core/evaluate-result.ts (17-04: templateCheckAborted -> coverage-incomplete)
    - 17-CONTEXT.md D-06 (FM-9 fold) + D-09a(iv)
  </read_first>
  <action>
    Extend `fault-isolation.integration.spec.ts` with the FM-9 drift probe: (1) run the TCB-poison fixture through `runTypecheck` and assert `result.templateCheckAborted` is present AND `evaluateResult(result).success === false` with `outcome === 'coverage-incomplete'` -- the abort is now VERDICT-AFFECTING (17-04), no longer advisory-only. (2) Add a version-pinned drift assertion that the ONLY TCB-generation Fatal code the detector recognizes at Angular 22.0.4 is `TCB_GENERATION_FATAL_DIAGNOSTIC_CODE` (NG3004): assert `detectTemplateCheckAborted` fires on an NG3004 synthetic diagnostic and does NOT fire on a sibling structural code (NG3001/NG3003) -- with a LOUD comment that if a future Angular introduces a new TCB-generation Fatal, this pinned probe + the extended-catalog drift tests must be updated so coverage-incompleteness is never silently missed. Mirror the existing RES-02/HARD-01 drift-probe style.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- fault-isolation</automated>
  </verify>
  <acceptance_criteria>
    - The TCB-poison fixture now yields `success === false` / `outcome === 'coverage-incomplete'`.
    - The pinned probe asserts NG3004 is the recognized TCB-generation Fatal and NG3001/NG3003 are not treated as one.
    - `npx nx test angular-typechecker` full suite green.
  </acceptance_criteria>
  <done>The FM-9 fold is proven verdict-affecting and the fatal-code surface is version-pinned against silent Angular drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Angular/TS internal invariants -> the boundary filter | undocumented invariants (rootName===fileName pre-realpath; external-template relatedInformation; NG3004-only TCB Fatal) that, if they drift, silently break coverage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02 | Tampering (attribution flip drops diagnostics) | external-template relatedInformation | mitigate | D-09.2 tripwire fails LOUD if the external-template diagnostic stops carrying a `.ts` relatedInformation (or flips attribution) |
| T-17-01 | Tampering (symlink/case evades membership) | dual-identity declared-root recovery | mitigate | D-09a(ii) tripwire proves the declared root is kept across case-insensitive/sensitive/symlink modes + a transitive dep negative |
| T-17-16 | Tampering (base-clause misclassifies clean host template) | D-04a base clause on external/indirect templates | mitigate | D-09a(i) clean-host fixture asserts suppressedInGraph == 0 (no false coverage-incomplete on a clean host's own templates) |
| T-17-10 | Tampering (whole-program abort hidden) | FM-9 TCB-abort + fatal-code drift | mitigate | D-09a(iv) proves the abort is coverage-incomplete now + pins the NG3004-only fatal surface so a new Angular Fatal trips loud |
| T-17-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | N/A -- no package installs this phase |
</threat_model>

<verification>
- `npx nx test angular-typechecker -- dual-identity external-template fault-isolation` green.
- Per wave merge: full suite + build + lint + format:check (CI gates).
</verification>

<success_criteria>
- All D-09a MANDATORY tripwires (i-iv) + the D-09.2 attribution tripwire are committed and green.
- The dual-identity + attribution + base-clause + FM-9 invariants each fail LOUD on a future drift.
</success_criteria>

<output>
Create `.planning/phases/17-input-set-membership-boundary-layout-support/17-07-SUMMARY.md` when done.
</output>
