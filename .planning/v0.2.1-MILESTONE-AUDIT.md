---
milestone: v0.2.1
milestone_name: Angular CLI workspace support
audited: 2026-07-15
audited_head: c4ea7c8
status: passed
scores:
  requirements: 16/16
  phases: 4/4
  integration: 8-of-8-wired
  flows: 4/4 proven
signals_at_head:
  build: green
  unit: 386/386 (39 files)
  integration: 107/107 (20 files)
  lint: green (dependency-checks, maxWarnings:0)
nyquist:
  compliant_phases: [21, 22, 23, 24]
  partial_phases: []
  missing_phases: []
  overall: compliant
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt: []
bookkeeping:
  - artifact: "24-VERIFICATION.md frontmatter"
    state: "stale — literally still reads `status: human_needed`"
    reality: "the single human item (re-run the ACV-01 real-clone tarball gate post-24-06) is RESOLVED — quick 260715-ig5, commit 895ee43, both clone gates PASS"
    action: "cosmetic re-flip only; does NOT block /gsd-complete-milestone (STATE.md already records all 4 phases complete)"
resolved_debt:
  - phase: 22-configuration / 23-init
    items:
      - "RESOLVED (quick 260715-jho, 2026-07-15): the yarn-4 `ng generate angular-typechecker:configuration <project>` convertNx load path was EMPIRICALLY EXERCISED on a real yarn 4 (berry) workspace against the Verdaccio-published local dist and did NOT crash with `chalk.blue is not a function`. Locked by the committed CI-authoritative cell `e2e/angular-typechecker-ng-cli-e2e/src/ng-generate-configuration-yarn.e2e.spec.ts` (ran, 1 passed, ~84s). Per YAGNI the convertNx `configuration` schematic was NOT refactored. Affected REQ-IDs: ACS-01 (secondarily ACS-04, ACV-02)."
  - phase: 24-real-oss / release-mechanics
    items:
      - "RESOLVED (quick 260715-ig5, commit 895ee43, 2026-07-15): the milestone-FINAL ACV-01 real-clone tarball gate was RE-RUN against post-24-06 HEAD. Both gates PASS on the nx-free vanilla ng-add — bluehalo/ngx-leaflet @818e9ae (app+lib, npm) first-run auto-wire-all with 2-element tsConfig arrays + clean per-project scoping; realworld-angular @9e3528f (app-only, pnpm-workspace name-collision) wired the full [tsconfig.app.json, tsconfig.spec.json] array (build leaf NOT dropped), no stray nx.json, no ERR_REQUIRE_ESM. Affected REQ-IDs: ACV-01."
  - phase: 21-builder
    items:
      - "RESOLVED (Wave 2/3 boundary): pre-existing Prettier drift in the regenerated spike-findings SKILL.md (from 21-01, commit 57c391c) reformatted; `nx format:check` green. (Only entry in 21/deferred-items.md; already closed.)"
---

# Milestone v0.2.1 Audit: Angular CLI workspace support

**Verdict: PASSED (no open items).** All 16 requirements are code-level satisfied
(3-source cross-referenced), all 4 phases are verified + Nyquist-validated, cross-phase
integration is **8-of-8 seams WIRED re-verified at HEAD**, and all 4 E2E flows are proven.
Additive-only vs `angular-typechecker@0.2.0` HOLDS — the milestone stays on the 0.2.x line
(no v0.3.0 trigger). Package version is intentionally still `0.2.0`; the `0.2.1` cut is the
deferred human-gated Release-PR (AGENTS.md).

**This audit REFRESHES the earlier 2026-07-15 `passed` verdict against current HEAD
(`c4ea7c8`).** Six code changes landed AFTER that verdict was written and are now
independently re-verified for integration integrity (fresh build + 386 unit + 107
integration + lint, all green):

- `50edeb7` refactor(core): share the Angular CLI workspace predicate across BOTH generator forks
- `0aeb81f` refactor(core): share the leaf-gather accumulator + union-finalize tail across the walk and multi-tsconfig paths
- `aec9238` fix(core): reject a spec-only leaf set instead of silently dropping the build leaf
- `f167a8c` fix(configuration): print the no-caching notice on the Angular CLI write-fork
- `00cfd8f` fix(ng-add): skip-and-warn a non-resolvable project on the bulk path instead of aborting
- `2dffcc0` build(deps): declare `@angular-devkit/schematics` as an optional peer dependency (ACP-01 contract)

The fresh integration checker confirmed the two refactors (`50edeb7`, `0aeb81f`) introduced
**no drift** between the `configuration` / `init` / `ng-add` forks, and the deps + fix commits
did not regress any seam.

## Phases (4/4)

| Phase | Verify | Nyquist |
|-------|--------|---------|
| 21 angular-cli-builder + engine multi-tsConfig + GATE A' spike | passed 4/4 | compliant |
| 22 configuration schematic (angular.json write-fork) | passed 11/11 | compliant |
| 23 init parity + first-party ng-add | passed 4/4 | compliant |
| 24 real-OSS + scaffolded e2e + additive-only audit + docs | passed 6/6 (code) + ACV-01 human gate re-run PASS | compliant |

**Bookkeeping loose end (non-blocking):** `24-VERIFICATION.md`'s frontmatter still literally
reads `status: human_needed`. That verdict was conditioned on ONE human item — re-running the
external ACV-01 real-clone tarball gate against post-24-06 HEAD — which a human performed and
recorded (quick `260715-ig5`, commit `895ee43`, both clone gates PASS). The frontmatter was
simply never re-flipped. This is cosmetic: `STATE.md`/the milestone manifest already record all
4 phases complete (`all_phases_complete: true`), so it does not block
`/gsd-complete-milestone`. Recommend a one-line re-flip for tidiness, not a re-verification.

## Requirements (16/16 satisfied, 3-source cross-reference)

Sources: phase `VERIFICATION.md` status + `REQUIREMENTS.md` traceability (`Complete`) +
`SUMMARY` frontmatter `requirements_completed`. All three agree except **ENG-01** and
**ACB-02**, whose SUMMARY frontmatter is empty BY DESIGN (the 21-01/21-02 SUMMARYs record them
as "substantively delivered, formally closed by the phase verifier after GATE A' GO"); both are
marked SATISFIED with codebase evidence in `21-VERIFICATION.md` and re-confirmed WIRED by the
integration checker — treated as satisfied, not partial.

| REQ | Phase | Final | 3-source note |
|-----|-------|-------|---------------|
| ENG-01 | 21 | satisfied | passed + verifier-closed + WIRED (normalize-options → `handleMultiTsConfig` → single `finalizeUnion` over combined input set) |
| ACB-01 | 21 | satisfied | 21-03 frontmatter + WIRED (`builder.ts` = `convertNxExecutor(typecheckExecutor)`) |
| ACB-02 | 21 | satisfied | passed + verifier-closed + GATE A' spike 011 GO + `builder.js` has no static compiler-cli require; `compiler-loader.js` dynamic `import()` survives emit |
| ACB-03 | 21 | satisfied | 21-03 frontmatter + `executors ?? builders` regression guard (spec-proven) |
| ACS-01 | 22 | satisfied | 22-01 frontmatter + write-fork leaf-array guard + yarn-4 `ng generate :configuration` e2e-verified-safe (260715-jho) |
| ACS-02 | 22 | satisfied | 22-01 frontmatter + Nx else-branch byte-unchanged (separate path below the CLI `return`) |
| ACS-04 | 22 | satisfied | 22-02 frontmatter + `generators ?? schematics` guard |
| COV-01 | 22 | satisfied | 22-01 frontmatter + shared `resolveTsConfigLeaves` per-project no-bleed guard + ACV-01/ACV-02 |
| ACS-03 | 23 | satisfied | 23-01 frontmatter + init GENERATOR early-return fork seeds no stray nx.json (`nx g :init` / `nx add` → `<pkg>:init`, UNCHANGED); the Angular CLI init SCHEMATIC was dropped for surface symmetry (260715-mia) — collection.json now declares only `ng-add` + `configuration` |
| NGADD-01 | 23 (+24-06) | satisfied | 23-02/23-03 frontmatter + 24-06 vanilla nx-free schematic (dist `schematic.js` requires only the pure core; first-run yarn auto-wire) |
| ACP-01 | 23 | satisfied | 23-02 frontmatter + optional-peer manifest contract (`@angular-devkit/architect`, `rxjs`, and `@angular-devkit/schematics` via 2dffcc0 — all `optional: true`); dependency-checks green |
| ACV-01 | 24 | satisfied | 24-03 frontmatter + real-clone gate re-run PASS post-24-06 (`895ee43`) |
| ACV-02 | 24 | satisfied | 24-03/24-05/24-06 frontmatter + committed CI e2e (npm / yarn flat+workspace / pnpm collision) + coverage guard |
| ACV-03 | 24 | satisfied | 24-01 frontmatter + builder-over-BuilderContext parity spec; exercised by 386 unit + 107 integration green |
| ACP-02 | 24 | satisfied | 24-01/24-04/24-06 frontmatter + additive-only audit + `nx` direct `^23.0.0` dep + `src/index.ts` byte-unchanged vs 0.2.0 tag |
| ACD-01 | 24 | satisfied | 24-02/24-06 frontmatter + README `## Angular CLI` + docs tripwire (9 tests, yarn caveat retired) |

No unsatisfied requirements. No orphaned requirements (all 16 appear in a phase
VERIFICATION.md and were re-confirmed WIRED by the integration checker).

## Cross-Phase Integration (8/8 seams WIRED — re-verified at HEAD c4ea7c8)

Fresh signals this pass (`NX_DAEMON=false … --skip-nx-cache`): `nx build` SUCCESS · `nx test`
**386/386** (39 files) · `nx run :integration` **107/107** (20 files) · `nx lint` (dependency-checks
+ nx-plugin-checks, maxWarnings:0) **all pass**.

1. **Shared `src/core/angular-cli-wiring.ts` is a true single source of truth** feeding the Nx
   `configuration` generator, the `init` generator's CLI fork, AND the vanilla `ng-add`
   schematic. The `50edeb7` workspace-predicate extraction (`isAngularCliWorkspace`) and
   `0aeb81f` leaf-gather / union-finalize extraction introduced NO drift — both forks call the
   same helpers and produce identical per-project leaf arrays. WIRED. (ACS-01/ACS-03/NGADD-01/COV-01)
2. **All 4 manifest surfaces resolve to shipped dist impls** after a fresh build:
   `executors.json` / `builders.json` / `generators.json` / `collection.json`; the `files`
   whitelist ships all four; no `*.spec.js` leaked into dist. WIRED. (ACB-03/ACS-04/ACP-02)
3. **Nx surface provably unchanged additively:** `nx run :typecheck` (`executors ?? builders`),
   `nx g :configuration` (`generators ?? schematics`), `nx add` → `<pkg>:init` (`ng-add` absent
   from `generators.json`, present in `collection.json`; dropped init schematic asserted absent).
   WIRED. (ACB-03/ACS-04/NGADD-01)
4. **ENG-01 `tsConfig: string | string[]`** flows schema → `normalize-options` → core
   `handleMultiTsConfig` → single `finalizeUnion` over the combined input set. The `aec9238`
   spec-only-leaf rejection lives in the wiring-layer helper, NOT the core path, so it does not
   affect the single-string or array core flows. WIRED. (ENG-01)
5. **Builder `builder.js` has NO static `require('@angular/compiler-cli')`;** the dynamic
   `import()` CJS→ESM bridge survives (`compiler-loader.js` `yield import('@angular/compiler-cli')`
   not downleveled). WIRED. (ACB-01/ACB-02)
6. **Vanilla `ng-add` dist `schematic.js` requires ONLY `../../core/angular-cli-wiring`** (zero
   `@nx/devkit` / `convertNxGenerator` / `nx`) — grep-clean of the built artifact. WIRED. (NGADD-01)
7. **`nx` is a direct `^23.0.0` dependency** so `@nx/devkit`'s `require('nx/src/devkit-exports')`
   resolves under yarn; `@angular-devkit/architect` + `rxjs` + `@angular-devkit/schematics` are
   optional peers; dependency-checks green (via the documented `ignoredDependencies` lever — the
   intended ACP-01 mechanism, not masking). WIRED. (ACP-01/ACP-02)
8. **`f167a8c` no-caching notice** prints on the CLI `configuration` write-fork AFTER the target
   is written and before the `return`, so it does not alter the written target shape and the Nx
   path never prints it (uses the shared `NO_CACHING_NOTICE`, identical to `init` + `ng-add`).
   WIRED. (ACS-01/ACS-03)

## E2E Flows (4/4 proven)

1. `ng add` auto-wire-all → per-project `ng run :typecheck` scoping — CI-authoritative ACV-02
   (`angular-typechecker-ng-cli-e2e`: npm flat + yarn flat + yarn workspace collision + pnpm
   collision), backstopped by `ci-e2e-coverage-guard.spec.ts` (7 tests). PROVEN (specs read,
   substantive; CI per-project matrix runs them).
2. ACV-01 real-clone FINAL gate (ngx-leaflet npm app+lib; realworld-angular pnpm app): pack
   SHIPPED tarball → `ng add` → plant → `ng run` → assert. PROVEN (re-run `895ee43`, both PASS
   post-24-06).
3. Builder-over-`BuilderContext` == Nx executor `{ success }` parity (ACV-03). PROVEN (parity
   spec; 386+107 green).
4. `ng generate angular-typechecker:configuration <project>` → `ng run :typecheck` on a real
   yarn 4 workspace — CI-authoritative cell `ng-generate-configuration-yarn.e2e.spec.ts`
   (260715-jho): wired the app target with the `[tsconfig.app.json, tsconfig.spec.json]` array,
   clean `ng run` GREEN, planted TS2322 + TS2345 caught, no chalk.blue / ERR_REQUIRE_ESM. PROVEN.

## Nyquist Compliance

All 4 phases `nyquist_compliant: true`, `wave_0_complete: true`. Overall **compliant**. No phase
needs `/gsd-validate-phase`.

## Tech Debt

**None outstanding.** The two previously-reviewable items are both RESOLVED and locked by
committed CI cells (see `resolved_debt` frontmatter):

- yarn-4 `ng generate :configuration` convertNx load path — verified safe on a real yarn 4
  workspace (260715-jho); no speculative refactor (YAGNI).
- ACV-01 milestone-FINAL real-clone gate — re-run post-24-06, both clones PASS (260715-ig5 / `895ee43`).

**Out-of-scope deferrals** (tracked as Future Requirements, NOT debt from this milestone):
WALK-FUT-01 (`createNodesV2` Nx auto-provisioning), and the charter-excluded set (hand-written
architect builder/Rule, emitted per-project tsconfig, runtime angular.json parsing in the
builder, `.angular/cache` target caching, JSON/SARIF reporters, `NgtscProgram` incremental/watch,
standalone CLI, Jest, wider/off-stack Angular).

## Notable milestone findings

1. **The ACV-01 real-clone gate earned its keep (twice).** Its first run caught a charter-level
   silent-under-check defect (the CLI generator dropped the app build leaf on an Angular-CLI +
   pnpm-workspace name-collision workspace) that the clean scaffolded ACV-02 e2e structurally
   could not reproduce — fixed by reading `angular.json` directly. Its 2026-07-15 re-run
   confirmed the 24-06 nx-free rewrite still passes both clones.
2. **24-06 fixed yarn first-run auto-wire by removing `@nx/devkit` from the `ng-add` load path.**
   The follow-up (260715-jho) empirically settled that the SAME hardening was NOT needed for
   `configuration` — the yarn-4 `ng generate :configuration` path does not crash on a real yarn 4
   workspace — so the convertNx schematic was left in place (verified safe; the RESEARCH's
   HIGH-confidence crash prediction did not reproduce; the arbiter e2e, not the mechanism
   analysis, is the source of truth).
3. **Post-verdict refinement held up.** Six commits after the earlier `passed` verdict (two core
   refactors, three fixes, one optional-peer deps change) were re-verified at HEAD with no fork
   drift and no seam regression — 386 unit + 107 integration + lint all green.

---
*Refreshed 2026-07-15 against HEAD c4ea7c8 (gsd/v0.2.1-angular-cli-workspace-support).*
