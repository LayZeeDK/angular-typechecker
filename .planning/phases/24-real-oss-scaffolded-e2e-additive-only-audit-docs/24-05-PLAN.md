---
phase: 24-real-oss-scaffolded-e2e-additive-only-audit-docs
plan: 05
type: execute
wave: 2
depends_on: ["24-04"]
files_modified:
  - e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-yarn.e2e.spec.ts
  - e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-pnpm.e2e.spec.ts
autonomous: true
gap_closure: true
requirements: [ACV-02]

must_haves:
  truths:
    - "On a real yarn 4 Angular CLI workspace (BOTH flat and yarn-workspace layouts), the REAL `ng add angular-typechecker` auto-wires a typecheck target into every application + library project and `ng run <project>:typecheck` catches exactly each project's planted leaves (app TS2322+TS2345 vs library TS2554, no cross-bleed) -- with NO explicit `nx` install step in the spec (nx arrives via the direct dependency landed in 24-04)."
    - "The yarn spec carries no TEMP DIAGNOSTIC scaffolding (no `corepack yarn add -D angular-typechecker` probe, no `atc-probe.cjs`, no `NX_VERSION`/`corepack yarn add -D nx`), keeps the load-bearing `enableMirror: false`, and its docstring no longer claims the refuted `ng g`-vs-`ng add` distinction."
    - "A COMMITTED CLI x pnpm-workspace name-collision e2e proves that when an Angular CLI workspace is ALSO a pnpm workspace whose root package.json name collides with the app project name, `ng add` still auto-wires the app target with the FULL `[tsconfig.app.json, tsconfig.spec.json]` array -- the app BUILD leaf is never silently dropped (the committed form of the ACV-01 gate #2 real-clone scenario)."
    - "`ng run <project>:typecheck` under yarn and pnpm shows no `ERR_REQUIRE_ESM` and no stray `nx.json`; clean baselines exit 0."
  artifacts:
    - path: "e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-yarn.e2e.spec.ts"
      provides: "The finalized yarn CLI e2e -- real `ng add`, no temp scaffolding, enableMirror:false retained"
      contains: "ng add angular-typechecker"
    - path: "e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-pnpm.e2e.spec.ts"
      provides: "The committed CLI x pnpm-workspace x name-collision e2e (build leaf never dropped)"
      contains: "pnpm-workspace.yaml"
  key_links:
    - from: "e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-yarn.e2e.spec.ts"
      to: "corepack yarn ng add angular-typechecker --skip-confirmation"
      via: "real ng add (yarn installs the direct `nx` dep automatically -- no explicit install)"
      pattern: "ng add angular-typechecker"
    - from: "e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-pnpm.e2e.spec.ts"
      to: "angular.json app target options.tsConfig == [tsconfig.app.json, tsconfig.spec.json]"
      via: "pnpm-workspace.yaml packages:['.'] (root collision) + allowBuilds:{nx:true} -> ng add -> CLI write-fork reads angular.json"
      pattern: "tsconfig\\.app\\.json"
---

<objective>
Finalize the CI-authoritative Angular CLI e2e coverage on top of the 24-04 dependency-contract fix. Two committed specs in the existing `angular-typechecker-ng-cli-e2e` project (they auto-join `nx run-many -t e2e`, no ci.yml/project.json edit): (1) finalize the yarn CLI spec to the REAL `ng add` path now that `nx` is a hard dependency, stripping the debug-era temp scaffolding; (2) add a committed CLI x pnpm-workspace name-collision spec that locks the ACV-01 gate #2 defect (the CLI write-fork must never silently drop the app build leaf).

Purpose: closes the remaining CLI x yarn and CLI x pnpm committed-coverage gaps. The yarn spec's `corepack yarn add -D nx` workaround becomes unnecessary once 24-04 makes `nx` a direct dependency (yarn installs direct deps; it only skipped the `@nx/devkit` peer). The pnpm spec converts the manual/uncommitted ACV-01 realworld-angular gate (root-app name collision under a pnpm workspace) into a committed CI test -- the one genuine committed-coverage gap (CLI x pnpm SHADOWING dup, currently 0 committed).

Output: the finalized `ng-add-ng-run-yarn.e2e.spec.ts` + the new `ng-add-ng-run-pnpm.e2e.spec.ts`.

Wave/isolation rationale: sole Wave-2 gap-closure plan (single-plan wave -> run on the MAIN checkout with real node_modules, NOT a junctioned worktree). It packs + publishes the SHARED dist tarball via the shared globalSetup and shells the real `ng`/`yarn`/`pnpm` toolchain; running isolated avoids the shared-tarball race (AGENTS.md) and gives `ng` real dependency resolution. It runs AFTER 24-04 so the built+published dist carries `nx` in its `dependencies` -- the whole point of the yarn finalization.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/cli-yarn-e2e-wrong-version.md
@.planning/phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-PATTERNS.md

<interfaces>
<!-- Extracted so the executor needs no codebase exploration. -->

The ng-cli-e2e project already exists (Plan 24-03): project.json (e2e + typecheck targets, type:e2e tag),
vitest.config.mts (node env, singleFork, fileParallelism:false, 300000ms), src/global-setup.ts (127.0.0.1
Verdaccio publish-once), and the committed fixture fixtures/ng-cli-workspace/ (app `ng-cli-workspace` +
lib `my-lib`; root package.json name is `ng-cli-workspace` == the app project name). Any new
`src/**/*.e2e.spec.ts` auto-joins the project's vitest run -- NO project.json/tsconfig/ci.yml edit.

Sibling specs to mirror:
  e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run.e2e.spec.ts  -- the npm ACV-02 spec: the REAL
    `ng add` path proven green. Copy its shape: cpSync fixture -> tmp, writeVerdaccioNpmrc, env =
    buildCleanEnv({stripAllNpmConfig:true}) + npm_config_userconfig -> nonexistent, `npm install` (no
    --legacy-peer-deps), `npx ng add angular-typechecker --skip-confirmation`, non-vacuous baseline,
    distinct per-leaf codes (APP_COMPONENT_CODE TS2322 / APP_SPEC_CODE TS2345 / LIB_COMPONENT_CODE
    TS2554), the same plant()/ngRun()/typecheckTarget() helpers, removeTmpDir in finally.
  e2e/angular-typechecker-install-e2e/src/nx-add-pnpm.e2e.spec.ts -- the pnpm SETUP analog: pnpm-workspace.yaml
    with `allowBuilds: { nx: true }` (approves nx's postinstall so the pnpm 11 build-script gate passes),
    packageManager pin `pnpm@11.9.0`, `pnpmAvailable = commandSucceeds('pnpm --version')` skipIf guard,
    an effective-pnpm-major === 11 assertion, writeVerdaccioNpmrc, `pnpm install`.
  packages/angular-typechecker/src/generators/configuration/configuration-matrix.spec.ts -- the collision
    INVARIANT at unit level: CLI + pnpm-workspace-including-root + name collision must wire the FULL
    [build, spec] leaf array (pre-fix bug: root app -> spec-only). The pnpm e2e mirrors this at tarball level.

Current yarn spec (ng-add-ng-run-yarn.e2e.spec.ts) items to REMOVE / CHANGE:
  - const NX_VERSION = '4.17.0'? NO -- `const NX_VERSION = '23.0.1';` (~line 59) + its comment (~51-58) -> REMOVE.
  - install-sequence line `sh(\`corepack yarn add -D nx@${NX_VERSION}\`, ...)` (~272) + comment (~268-271) -> REMOVE.
  - TEMP DIAGNOSTIC (~273-289): `sh('corepack yarn add -D angular-typechecker', ...)`, the
    `writeFileSync(join(tmp,'atc-probe.cjs'), [...])`, and the `console.log('[ATC-PROBE]...` run -> REMOVE.
  - `sh('corepack yarn ng g angular-typechecker:ng-add', ...)` (~291) -> REPLACE with
    `sh('corepack yarn ng add angular-typechecker --skip-confirmation', {cwd:tmp, env:npmEnv})`.
  - KEEP: `enableMirror: false` in setupYarnWorkspace + its comment (separate, still-load-bearing mirror fix);
    writeVerdaccioNpmrc; `corepack yarn install`; the after-add auto-wire/scoping assertions; the availability
    skipIf; the `.yarnrc.yml` Verdaccio config.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize the yarn CLI e2e -- switch to real `ng add`, strip the temp scaffolding, keep enableMirror:false</name>
  <files>e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-yarn.e2e.spec.ts</files>
  <read_first>
    - e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-yarn.e2e.spec.ts (the spec being finalized -- the TEMP DIAGNOSTIC, NX_VERSION, `ng g`, and the docstring)
    - e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run.e2e.spec.ts (the npm ACV-02 analog -- the proven real `ng add --skip-confirmation` install path to mirror)
    - .planning/debug/cli-yarn-e2e-wrong-version.md (Resolution + the "blind_spots" note: `ng g` DID load the collection under yarn once the correct version installed -- the docstring's `ng g`-vs-`ng add` theory was a misdiagnosis of the mirror/version bug)
  </read_first>
  <action>
    Edit `ng-add-ng-run-yarn.e2e.spec.ts` in place (product source untouched -- this is a test-harness change):
    (1) Remove the `NX_VERSION` const and its comment; remove the `sh(\`corepack yarn add -D nx@${NX_VERSION}\`, ...)` step and its comment. Now that Plan 24-04 declares `nx` as a direct dependency of angular-typechecker, `ng add` (which runs `yarn add angular-typechecker`) installs `nx` automatically -- yarn installs direct deps, it only skips peers.
    (2) Remove the TEMP DIAGNOSTIC entirely: the standalone `sh('corepack yarn add -D angular-typechecker', ...)` probe, the `writeFileSync(...,'atc-probe.cjs', ...)` block, and the `console.log('[ATC-PROBE]...` run.
    (3) Replace the `sh('corepack yarn ng g angular-typechecker:ng-add', ...)` line with `sh('corepack yarn ng add angular-typechecker --skip-confirmation', { cwd: tmp, env: npmEnv })` -- the REAL `ng add` path, mirroring the npm ACV-02 spec (yarn is detected via yarn.lock + packageManager, so `ng add` runs `yarn add` then the ng-add schematic).
    (4) KEEP `enableMirror: false` in `setupYarnWorkspace` and its comment (it is the SEPARATE, still-load-bearing fix for the global-mirror locator collision -- independent of the nx peer). KEEP `writeVerdaccioNpmrc`, `corepack yarn install`, the `.yarnrc.yml` Verdaccio block, and every after-add assertion (auto-wire-all: both projects gain `angular-typechecker:typecheck` with their two-element `[build, spec]` tsConfig arrays; no stray `nx.json`; clean baseline exit 0; planted per-leaf scoping app=TS2322+TS2345 / lib=TS2554 with no cross-bleed; no `ERR_REQUIRE_ESM`; no `infrastructure error`) for BOTH `flat` and `workspace` layouts.
    (5) Correct the docstring: remove the refuted paragraph claiming `ng generate <collection>` resolves differently from `ng add` under yarn's node-modules linker (the debug session proved `ng g` DID load the collection once the correct version was installed -- that paragraph was a misdiagnosis of the version/mirror bug). State that the install uses the real `ng add angular-typechecker --skip-confirmation`, and that `nx` now arrives automatically because it is a direct dependency (yarn does not auto-install the `@nx/devkit` peer -- 24-04's product fix). Update the describe/it titles that say `ng g :ng-add` to `ng add`.
    Do NOT edit `project.json`, `vitest.config.mts`, `tsconfig*.json`, or the fixture.
  </action>
  <verify>
    <automated>NX_DAEMON=false npx nx e2e angular-typechecker-ng-cli-e2e --skip-nx-cache</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "atc-probe|NX_VERSION|yarn add -D nx|ng g angular-typechecker:ng-add" e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-yarn.e2e.spec.ts` returns 0 (all temp scaffolding + `ng g` removed).
    - The spec installs via `corepack yarn ng add angular-typechecker --skip-confirmation`.
    - `rg -c "enableMirror: false" e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-yarn.e2e.spec.ts` returns 1 (the mirror fix is retained).
    - `npx nx e2e angular-typechecker-ng-cli-e2e` passes: BOTH yarn layouts (flat + workspace) auto-wire both projects, clean baseline exits 0, per-leaf scoping proven (app TS2322+TS2345, lib TS2554, no cross-bleed), no `ERR_REQUIRE_ESM`, no stray `nx.json`; the npm ACV-02 spec stays green.
    - The docstring no longer contains the `ng generate <collection>`-differs-from-`ng add` claim.
  </acceptance_criteria>
  <done>The yarn CLI e2e is a clean, real-`ng add` proof against the shipped fixed artifact -- no debug scaffolding, mirror fix intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add the committed CLI x pnpm-workspace name-collision e2e (app build leaf never dropped)</name>
  <files>e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-pnpm.e2e.spec.ts</files>
  <read_first>
    - e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run.e2e.spec.ts (the npm ACV-02 analog: the fixture -> tmp -> ng add -> assert-auto-wire -> plant -> ng run -> scope flow, the plant()/ngRun()/typecheckTarget() helpers, the distinct per-leaf codes)
    - e2e/angular-typechecker-install-e2e/src/nx-add-pnpm.e2e.spec.ts (the pnpm SETUP analog: pnpm-workspace.yaml + `allowBuilds: { nx: true }`, packageManager `pnpm@11.9.0` pin, `pnpmAvailable` skipIf, effective-pnpm-major===11 assertion, writeVerdaccioNpmrc, stripAllNpmConfig)
    - packages/angular-typechecker/src/generators/configuration/configuration-matrix.spec.ts (the collision INVARIANT: CLI + pnpm-workspace-including-root + name collision must wire the FULL [build, spec] array; pre-fix bug = spec-only / throw)
    - .planning/phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-VERIFICATION.md (the ACV-01 gate #2 realworld-angular scenario this committed test locks: root-app name collision -> app build leaf silently dropped pre-fix 1837b25)
  </read_first>
  <action>
    Create `e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-pnpm.e2e.spec.ts` mirroring the npm ACV-02 spec's structure but with pnpm as the package manager and the name-collision scenario seeded. Steps in a tmp dir:
    (1) `cpSync` the committed `fixtures/ng-cli-workspace/` into tmp; drop the npm `package-lock.json` so pnpm's lockfile is authoritative.
    (2) Write `pnpm-workspace.yaml` with `packages:` including the ROOT (`- '.'`) AND `allowBuilds:` with `nx: true`. The `packages: ['.']` makes the ROOT package.json a pnpm workspace member; because the fixture's root package.json `name` is `ng-cli-workspace` (== the app project name), Nx infers a shadowing package stub -- exactly the ACV-01 gate #2 collision. `allowBuilds: { nx: true }` approves nx's postinstall so pnpm 11's build-script gate does not fail the install (mirrors nx-add-pnpm; `ng add`, like `nx add`, forwards no flags).
    (3) Pin `packageManager: "pnpm@11.9.0"` in the tmp package.json (a `PNPM_VERSION` const; keep in sync with ci.yml pnpm/action-setup); guard with `pnpmAvailable = commandSucceeds('pnpm --version', ...)` (`it.skipIf(!pnpmAvailable)`); `writeVerdaccioNpmrc(tmp, url, token)`; env = `buildCleanEnv({ stripAllNpmConfig: true })` + `npm_config_userconfig -> join(tmp,'.npmrc.nonexistent')`; re-assert the `http://127.0.0.1:` SAFETY invariant.
    (4) Non-vacuous baseline: assert neither project has a `typecheck` target BEFORE ng add. `sh('pnpm install', ...)`. Assert the EFFECTIVE pnpm major is 11 (`Number(sh('pnpm --version').trim().split('.')[0]) === 11`) so a <11 host fails loudly rather than passing the collision untested. Run `sh('npx ng add angular-typechecker --skip-confirmation', {cwd:tmp,env})` (the real Angular CLI flow; if the pnpm `ng add` install path misbehaves against Verdaccio, fall back to `sh('pnpm add -D angular-typechecker', ...)` then `sh('npx ng g angular-typechecker:ng-add', ...)` -- both hit the same allowBuilds-satisfied path).
    (5) REGRESSION assertion (the point of this spec): the app project's `typecheck` target `options.tsConfig` equals the FULL two-element array `['tsconfig.app.json', 'tsconfig.spec.json']` -- the app BUILD leaf is present, NOT silently dropped to `['tsconfig.spec.json']`. Assert the lib target's array too (`['projects/my-lib/tsconfig.lib.json', 'projects/my-lib/tsconfig.spec.json']`). Assert no stray `nx.json`.
    (6) Prove per-project scoping under real pnpm: clean baseline exits 0; then plant distinct per-leaf errors (app component TS2322, app spec TS2345, lib component TS2554) and assert `ng run <app>:typecheck` catches TS2322+TS2345 but not TS2554, and `ng run my-lib:typecheck` catches TS2554 but neither app code; no `ERR_REQUIRE_ESM`, no `infrastructure error`. Reuse the sibling spec's plant()/ngRun()/typecheckTarget() helper shapes. `removeTmpDir(tmp)` in `finally`. Timeout 300000-900000ms.
  </action>
  <verify>
    <automated>NX_DAEMON=false npx nx e2e angular-typechecker-ng-cli-e2e --skip-nx-cache</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-pnpm.e2e.spec.ts` exists, writes a `pnpm-workspace.yaml` including the root (`- '.'`) with `allowBuilds: { nx: true }`, and pins `packageManager: pnpm@11.9.0` with an effective-pnpm-major===11 assertion.
    - It asserts the app `typecheck` target's `tsConfig` is the FULL `['tsconfig.app.json', 'tsconfig.spec.json']` (build leaf never dropped) under the root name collision -- the committed form of the ACV-01 gate #2 defect.
    - It proves per-project scoping (app TS2322+TS2345 vs lib TS2554, no cross-bleed), a clean baseline exit 0, no stray `nx.json`, and no `ERR_REQUIRE_ESM`.
    - It re-asserts the `http://127.0.0.1:` Verdaccio SAFETY invariant and uses `buildCleanEnv({ stripAllNpmConfig: true })`; skips cleanly where pnpm is unavailable.
    - `npx nx e2e angular-typechecker-ng-cli-e2e` passes with all specs (npm + yarn + pnpm) green under `--parallel=1` serialization.
  </acceptance_criteria>
  <done>The CLI x pnpm-workspace name-collision (app build leaf never silently dropped) is a committed, CI-authoritative e2e -- the manual ACV-01 gate #2 scenario is now a standing test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e publish step -> npm registry | The shared global-setup publishes the built tarball to a registry; it MUST be local Verdaccio (127.0.0.1), never the public registry |
| tmp install (`ng add` under yarn/pnpm) -> package resolution | Inherited `npm_config_*` (a leaked `legacy-peer-deps`) or a global yarn mirror could mask a real result or install the wrong tarball |
| pnpm build-script gate -> install | pnpm 11 refuses unapproved postinstalls; `allowBuilds` approves ONLY `nx` (the one flagged dep), not a blanket allow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Tampering | e2e accidentally publishes to a real registry | mitigate | Reuse the existing global-setup 127.0.0.1-only publish SAFETY gate; each spec re-asserts `verdaccioUrl.startsWith('http://127.0.0.1:')` |
| T-24-07 | Tampering | inherited npm/yarn config masks a real result or installs a stale tarball | mitigate | `buildCleanEnv({ stripAllNpmConfig: true })` + `npm_config_userconfig` -> nonexistent path; yarn keeps `enableMirror: false` + per-fixture cache (defeats the global-mirror locator collision); pnpm reads the tmp Verdaccio `.npmrc` |
| T-24-10 | Elevation of privilege | pnpm postinstall build-script execution | accept | `allowBuilds: { nx: true }` approves ONLY nx's postinstall (a first-party Nrwl package already in the tree); no blanket build-script allow; the fixture ships no build scripts of its own |
| T-24-SC | Tampering | package installs from Verdaccio | accept | No NEW shipped dependency; the fixture declares canonical first-party Angular 22 devDeps only (RESEARCH.md Package Legitimacy Audit -- none `[ASSUMED]`/`[SUS]`); Verdaccio serves the local dist + proxies upstream at pinned versions on 127.0.0.1 |

No `high`+ severity threat exists in this plan's surface (test-infra only; the one network action is a loopback-gated publish; no new shipped dependency). Stated explicitly per the security context.
</threat_model>

<verification>
- `npx nx e2e angular-typechecker-ng-cli-e2e` -- npm (ACV-02) + yarn (flat + workspace) + pnpm (name-collision) specs all green.
- Post-merge full gate: `npx nx run-many -t e2e --parallel=1` -- all e2e projects serialized, no shared-tarball ENOENT.
- `npx nx test angular-typechecker` -- `ci-e2e-coverage-guard.spec.ts` (GUARD-01/01b/01c/01d) stays green (the new spec is another file in the existing project; set-equality unaffected).
- Post-execution gates (NOT plan tasks -- run by the orchestrator/agents after execution):
  - CODE-REVIEW both e2e specs via `gsd-code-reviewer` -> `gsd-code-fixer` (user rule: ALL added tests are code-reviewed).
  - Mark `.planning/debug/cli-yarn-e2e-wrong-version.md` RESOLVED and update its Resolution to reflect the FINAL product fix (nx as a direct dependency, 24-04) superseding the test-only `corepack yarn add -D nx` workaround; note the yarn UX caveat is now fixed in the product, not just documented.
</verification>

<success_criteria>
- ACV-02: the CI-authoritative Angular CLI e2e proves the REAL `ng add` -> `ng run <project>:typecheck` flow on real yarn 4 (flat + workspace) and real pnpm 11 (root name collision) workspaces, with per-project scoping and the app build leaf never dropped.
- The yarn spec carries no debug scaffolding and installs via the real `ng add`, exercising the shipped 24-04 fixed artifact (nx auto-installed as a direct dependency).
</success_criteria>

<output>
Create `.planning/phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-05-SUMMARY.md` when done.
</output>
