---
phase: 21-angular-cli-builder-engine-multi-tsconfig-gate-a-spike-go-no
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/builders/typecheck/builder.ts
  - packages/angular-typechecker/src/builders/typecheck/schema.json
  - packages/angular-typechecker/builders.json
  - packages/angular-typechecker/package.json
  - packages/angular-typechecker/project.json
  - packages/angular-typechecker/src/executors/typecheck/gate-a-static.spec.ts
  - packages/angular-typechecker/src/package-manifest.spec.ts
  - .planning/spikes/011-builder-ng-run-esm-bridge/harness.mjs
  - .planning/spikes/011-builder-ng-run-esm-bridge/README.md
  - .planning/spikes/011-builder-ng-run-esm-bridge/forensic-log.json
  - .planning/spikes/MANIFEST.md
  - .claude/skills/spike-findings-angular-typechecker/SKILL.md
autonomous: false
requirements: [ACB-01, ACB-02, ACB-03]
must_haves:
  truths:
    - "A real `ng run <project>:typecheck` (app ngx-leaflet-demo AND library ngx-leaflet) against the cloned Angular 22 ngx-leaflet workspace completes with NO ERR_REQUIRE_ESM -- including the eager retrieveProjectConfigurationsWithAngularProjects project-graph prelude"
    - "The builder surfaces a planted diagnostic RED via `ng run` and leaves a clean project GREEN, and the exit code maps to the verdict (proves the engine runs end-to-end through convertNxExecutor + Architect + the CJS->ESM compiler-cli bridge)"
    - "The GATE A' VERDICT recorded in .planning/spikes/011-builder-ng-run-esm-bridge/forensic-log.json is GO"
    - "The built builder.js reaches @angular/compiler-cli through core/compiler-loader.js and never require()s it (gate-a-static.spec.ts extended to the builder entry)"
    - "nx run <project>:typecheck still resolves after the builders field lands (executors ?? builders precedence)"
    - "Honors CONTEXT.md decisions D-01, D-02, D-03, D-04, D-05, D-07: real ngx-leaflet clone GATE A' proof; spike-011 record-only; the GO checklist; gate-first ordering with a NO-GO halting the phase (never a hand-written architect builder); the sanitized builder schema; and the three-tier substrate roles"
  artifacts:
    - path: "packages/angular-typechecker/src/builders/typecheck/builder.ts"
      provides: "convertNxExecutor(typecheckExecutor) re-export -- the whole builder"
      contains: "convertNxExecutor"
    - path: "packages/angular-typechecker/builders.json"
      provides: "Angular CLI builder manifest registering the typecheck builder"
      contains: "typecheck"
    - path: "packages/angular-typechecker/src/builders/typecheck/schema.json"
      provides: "Sanitized builder options schema (no cli/version/$id) over TypecheckExecutorOptions"
    - path: "packages/angular-typechecker/package.json"
      provides: "Additive builders field + builders.json files-allowlist entry"
      contains: "builders.json"
    - path: "packages/angular-typechecker/project.json"
      provides: "Build-asset glob copying builders.json into dist"
    - path: ".planning/spikes/011-builder-ng-run-esm-bridge/forensic-log.json"
      provides: "GATE A' evidence with VERDICT=GO, clone repo URL + SHA, per-project ng run output + ESM-signature scan"
      contains: "verdict"
  key_links:
    - from: "src/builders/typecheck/builder.ts"
      to: "src/executors/typecheck/executor (default export)"
      via: "import typecheckExecutor + convertNxExecutor"
      pattern: "executors/typecheck/executor"
    - from: "package.json builders"
      to: "builders.json -> src/builders/typecheck/builder implementation"
      via: "Angular CLI builder resolution"
      pattern: "builders\\.json"
    - from: "project.json build.options.assets"
      to: "dist/.../builders.json"
      via: "asset glob (tarball carries the manifest)"
      pattern: "builders\\.json"
---

<objective>
GATE A' gating plan (ACB-02, the milestone's headline GO/NO-GO). Land the MINIMAL real Angular CLI
builder (a 3-line `convertNxExecutor` re-export of the shipped `typecheck` executor) plus its
manifests and a sanitized schema, extend the built-artifact byte-assertion and the manifest spec to
cover the new builder entry, then run the GATE A' spike: pack the built dist, install it into the
real cloned Angular 22 `bluehalo/ngx-leaflet` workspace, hand-wire an `architect.typecheck` target on
an app AND a library, run a real `ng run <project>:typecheck`, and scan for `ERR_REQUIRE_ESM`. Record
a GO/NO-GO VERDICT in a committed spike record.

Purpose: empirically prove the CommonJS-executor-loads-ESM-`@angular/compiler-cli`-via-`await import()`
bridge SURVIVES `convertNxExecutor` + a real `ng run` (including the wrapper's eager project-graph
prelude, nrwl/nx#19475). This gate decides whether the entire milestone proceeds.

Output: a shippable minimal builder + manifests; the extended static guards; and spike `011` with a
recorded VERDICT. On GO, the builder code STAYS (it is the ACB-01 deliverable) and Waves 2-3 proceed.
On NO-GO, the phase HALTS with a documented re-scope and NEVER falls back to a hand-written
`@angular-devkit/architect` builder (D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-angular-cli-builder-engine-multi-tsconfig-gate-a-spike-go-no/21-RESEARCH.md
@.planning/phases/21-angular-cli-builder-engine-multi-tsconfig-gate-a-spike-go-no/21-PATTERNS.md
@.planning/spikes/CONVENTIONS.md

Project rules (follow exactly): @CLAUDE.md and @AGENTS.md. Salient: Windows arm64; use `git grep`/`rg`
never `grep`; blank lines around control-flow/returns + always-braces JS/TS; ASCII only; Prettier
`singleQuote:true`; dist/ is gitignored (read built `.js` with fs, never `git grep`); the TypeScript
LSP diagnostic feed is NOT authoritative -- trust `nx build`/`nx test`.

<interfaces>
<!-- Extracted from the codebase this session. Executor should use these directly -- no exploration needed. -->

The executor default export the builder wraps (src/executors/typecheck/executor.ts):
  export default async function typecheckExecutor(
    options: TypecheckExecutorOptions, context: ExecutorContext,
  ): Promise<{ success: boolean }>
Import ordering convention: external `@nx/devkit` import first, then a blank line, then the relative
`../../executors/typecheck/executor` import (mirror executor.ts lines 1-13).

Current executors.json (the manifest to mirror for builders.json -- top-level key becomes `builders`,
drop the Nx-only `outputCapture`, keep the extensionless `implementation` path):
  { "executors": { "typecheck": {
    "implementation": "./src/executors/typecheck/executor",
    "schema": "./src/executors/typecheck/schema.json",
    "outputCapture": "direct-nodejs",
    "description": "Type-checks an Angular project (TypeScript + template + extended NG8xxx) with no emit." } } }

Current executor schema.json Nx-only keys to STRIP for the sanitized builder schema: `$id`
("TypecheckExecutorOptions"), `cli` ("nx"), `version` (2). There are NO `x-*` and NO `$default` keys
to strip. Keep `$schema`, `title`, `description`, `type:"object"`, all five properties
(tsConfig/includeDeps/maxWarnings/failFast/strict) VERBATIM, `required:["tsConfig"]`,
`additionalProperties:false`. For THIS plan the builder `tsConfig` stays single-string `"type":"string"`
(the pure bridge gate uses a single-string tsConfig; ENG-01 widens both schemas to oneOf in Wave 2).

The builder's TS options type: REUSE the executor's `TypecheckExecutorOptions` (schema.d.ts). Do NOT
author a duplicate builder schema.d.ts (PATTERNS.md; ponytail: the executor interface is the single
source of truth for both option surfaces).

package.json current fields: `"executors": "./executors.json"`, `"generators": "./generators.json"`,
and files allowlist ["src","executors.json","generators.json","README.md","LICENSE"].

project.json build.options.assets currently globs `executors.json` and `generators.json` (input
"./packages/angular-typechecker", output ".") -- add an identical third block for `builders.json`.

gate-a-static.spec.ts derives distRoot from project.json build.options.outputPath
(= dist/packages/angular-typechecker), reads built `.js` with readFileSync + stripCommentLines, and
asserts the executor entry does NOT `require('@angular/compiler-cli')`. The builder .js reaches
compiler-cli via the SAME core/compiler-loader.js, so only the NEGATIVE require() assertion is needed
on the builder entry (the positive `import(` assertion on compiler-loader.js already covers the load
site).

Real GATE substrate (VERIFIED present this session): D:\projects\github\bluehalo\ngx-leaflet @
818e9ae55240b570397ede5a15cb4d466785abdc. Both projects use the `architect` target key (Assumption A1
CONFIRMED): app `ngx-leaflet-demo` (application, root; leaves tsconfig.app.json + tsconfig.spec.json);
lib `ngx-leaflet` (library, projects/ngx-leaflet; leaves projects/ngx-leaflet/tsconfig.lib.json +
tsconfig.spec.json). On-stack Angular 22, npm lockfile, non-Nx. Install with NO `--legacy-peer-deps`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land the minimal builder + manifests + sanitized schema</name>
  <files>packages/angular-typechecker/src/builders/typecheck/builder.ts, packages/angular-typechecker/src/builders/typecheck/schema.json, packages/angular-typechecker/builders.json, packages/angular-typechecker/package.json, packages/angular-typechecker/project.json</files>
  <read_first>
    - packages/angular-typechecker/src/executors/typecheck/executor.ts (the default export to wrap; import-ordering convention)
    - packages/angular-typechecker/executors.json (manifest analog)
    - packages/angular-typechecker/src/executors/typecheck/schema.json (schema to sanitize)
    - packages/angular-typechecker/src/executors/typecheck/schema.d.ts (TypecheckExecutorOptions -- reuse, do not duplicate)
    - packages/angular-typechecker/package.json (executors/generators/files fields)
    - packages/angular-typechecker/project.json (build.options.assets globs)
    - 21-PATTERNS.md sections "builder.ts", "builders.json", "builder schema.json", "package.json", "project.json (CRITICAL)"
  </read_first>
  <action>
    Create `src/builders/typecheck/builder.ts` as the whole builder: import `convertNxExecutor` from
    `@nx/devkit`, blank line, import the default `typecheckExecutor` from `../../executors/typecheck/executor`,
    blank line, `export default convertNxExecutor(typecheckExecutor);`. No other code (ACB-01 parity is
    structural -- it IS the same executor default export).

    Create `packages/angular-typechecker/builders.json` with top-level key `builders`, one entry
    `typecheck` whose `implementation` is `./src/builders/typecheck/builder` (extensionless), `schema` is
    `./src/builders/typecheck/schema.json`, and `description` matching the executor's. Do NOT include the
    Nx-only `outputCapture`.

    Create `src/builders/typecheck/schema.json` as a SANITIZED copy of the executor schema: strip `$id`,
    `cli`, and `version`; keep `$schema`, `title`, `description`, `type:"object"`, all five properties
    VERBATIM (tsConfig single-string for now), `required:["tsConfig"]`, `additionalProperties:false`.

    Edit `package.json`: add `"builders": "./builders.json"` alongside the existing `executors`/`generators`
    fields, and add `"builders.json"` to the `files` allowlist (place it after `generators.json`). ADDITIVE
    ONLY -- do not touch `executors`, `generators`, or any other field. Do NOT declare the optional
    `@angular-devkit/architect`/`rxjs` peers here -- that is Phase 23's ACP-01 (the ngx-leaflet clone
    provides both, so the gate is not blocked; deferring avoids an @nx/dependency-checks lint surprise
    mid-gate and avoids claiming a Phase-23 requirement).

    Edit `project.json`: add a third build-asset block mirroring the existing executors.json/generators.json
    blocks -- input `./packages/angular-typechecker`, glob `builders.json`, output `.` -- so the built dist
    (and the packed tarball) carries the builder manifest. Without this the tarball ships no builders.json
    and `ng run` cannot resolve the builder (0.0.1-0.1.0 packaging regression precedent).
  </action>
  <verify>
    <automated>nx build angular-typechecker && test -f dist/packages/angular-typechecker/builders.json && test -f dist/packages/angular-typechecker/src/builders/typecheck/builder.js && test -f dist/packages/angular-typechecker/src/builders/typecheck/schema.json</automated>
  </verify>
  <acceptance_criteria>
    - `nx build angular-typechecker` exits 0.
    - dist/packages/angular-typechecker/builders.json exists and its `builders.typecheck.implementation`
      is `./src/builders/typecheck/builder`.
    - dist/packages/angular-typechecker/src/builders/typecheck/builder.js exists (compiled CJS).
    - dist builder schema.json exists and has NO `cli`, `version`, or `$id` keys.
    - package.json declares `builders: "./builders.json"` and the files array contains `builders.json`;
      the `executors` and `generators` fields are byte-unchanged.
    - `nx lint angular-typechecker` exits 0 (no @nx/dependency-checks regression).
  </acceptance_criteria>
  <done>The minimal builder + manifests + sanitized schema build cleanly and the compiled artifacts + manifest land in dist.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the static byte-assertion + fix the manifest spec</name>
  <files>packages/angular-typechecker/src/executors/typecheck/gate-a-static.spec.ts, packages/angular-typechecker/src/package-manifest.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/executors/typecheck/gate-a-static.spec.ts (self -- extend the negative assertion)
    - packages/angular-typechecker/src/package-manifest.spec.ts (self -- files allowlist + generators assertions to update)
    - 21-PATTERNS.md sections "gate-a-static.spec.ts (MODIFIED)" and "package-manifest.spec.ts (MODIFIED)"
  </read_first>
  <action>
    In `gate-a-static.spec.ts`, add a `builderJsPath = join(distRoot, 'src', 'builders', 'typecheck',
    'builder.js')` and a new `it('negative: built builders/.../builder.js does NOT require() @angular/compiler-cli')`
    that reads the built builder.js with the existing `stripCommentLines` helper and asserts it does NOT
    match `/require\(["']@angular\/compiler-cli/`. Reuse the existing distRoot derivation and helper
    verbatim; the positive `import(` assertion stays on compiler-loader.js (the builder .js does no
    import() of its own).

    In `package-manifest.spec.ts`, add `'builders.json'` to the expected `files` array in the
    "declares the explicit files allowlist" assertion (after `generators.json`), add `builders?: string`
    to the `PluginManifest` interface, and add an assertion `expect(manifest.builders).toBe('./builders.json')`
    mirroring the existing `generators` assertion. Change nothing else.
  </action>
  <verify>
    <automated>nx build angular-typechecker && nx test angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `nx build angular-typechecker && nx test angular-typechecker` exits 0.
    - gate-a-static.spec.ts has a passing negative assertion on builder.js (no require() of compiler-cli).
    - package-manifest.spec.ts asserts `files` includes `builders.json` and `builders === './builders.json'`,
      and all pre-existing manifest assertions still pass.
  </acceptance_criteria>
  <done>The built-artifact byte guard covers the builder entry and the manifest spec locks the additive builders field + files entry; the full suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Run the GATE A' spike (011) against the real ngx-leaflet clone</name>
  <files>.planning/spikes/011-builder-ng-run-esm-bridge/harness.mjs, .planning/spikes/011-builder-ng-run-esm-bridge/README.md, .planning/spikes/011-builder-ng-run-esm-bridge/forensic-log.json, .planning/spikes/MANIFEST.md, .claude/skills/spike-findings-angular-typechecker/SKILL.md</files>
  <read_first>
    - 21-RESEARCH.md section "GATE A' spike mechanics" (steps 1-10) + the GO/NO-GO checklist
    - .planning/spikes/CONVENTIONS.md (assertion-bearing harness, forensic JSON, record-only, NG-code decoding)
    - .planning/spikes/007-forced-sb10-compile-ng8xxx/README.md + harness.mjs (frontmatter + assertion/forensic/exit pattern to mirror)
    - .planning/spikes/MANIFEST.md (verdict-row table format)
    - .claude/skills/spike-findings-angular-typechecker/SKILL.md (findings-index format)
  </read_first>
  <action>
    Author `.planning/spikes/011-builder-ng-run-esm-bridge/harness.mjs` as an ORCHESTRATOR (Git
    Bash-compatible Node ESM) that drives REAL tooling per RESEARCH steps 1-10: (1) `nx build
    angular-typechecker`; (2) `npm pack` the built dist at dist/packages/angular-typechecker and assert
    the tarball contains src/builders/typecheck/builder.js, the builder schema.json, and builders.json;
    (3) `npm install <abs-tarball> --save-dev` into the clone at D:\projects\github\bluehalo\ngx-leaflet
    with NO `--legacy-peer-deps` (if it needs the flag, record that as a finding); (4) hand-wire an
    `architect.typecheck` target (target key = `architect`, CONFIRMED for this clone) on app
    `ngx-leaflet-demo` (options.tsConfig = "tsconfig.app.json") AND lib `ngx-leaflet` (options.tsConfig =
    "projects/ngx-leaflet/tsconfig.lib.json") with builder `angular-typechecker:typecheck`; (5) plant ONE
    known diagnostic in one project (e.g. an unknown-property template error -> NG8002, or a `.ts` TS2322)
    and keep the other project clean; (6) run `npx ng run ngx-leaflet-demo:typecheck` and `npx ng run
    ngx-leaflet:typecheck`, capturing stdout + stderr + exit code each; (7) scan captured output for the
    NO-GO signals `ERR_REQUIRE_ESM`, `require() of ES Module`, `Cannot use import statement outside a
    module`, and any project-graph/daemon error thrown BEFORE any diagnostic; (8) assert GO evidence
    (planted diagnostic surfaces RED, clean project GREEN, exit code maps to verdict, none of the ESM
    signatures present); (9) write `forensic-log.json` (node/Angular/TS versions, clone repo URL +
    818e9ae SHA, per-project stdout excerpt + exit code, ESM-signature scan result, VERDICT); end with a
    labelled `[PASS]/[FAIL] id: detail` list via an `assert(id, cond, detail)` helper + a computed VERDICT
    string + `process.exit(allPass ? 0 : 1)`. Use the NG-code decoding helper (`code === -(990000 +
    ngNumber)`, NG8002 = -998002) to assert NG8xxx without hardcoding raw codes. NEVER commit the clone or
    its node_modules; clean up the `architect.typecheck` edits + the installed tarball + any `.nx/` dir
    from the clone after the run (tolerate the transitive `nx` install -- Pitfall 4).

    Write `README.md` with spike-007-style frontmatter (spike:011, name, type:standard, gate:[GATE-A'],
    validates, verdict, related, tags) + What/Research/How to Run sections documenting the exact repo URL +
    818e9ae SHA + the pack/install/wire/ng-run reproduction commands (commit the RECORD only).

    Add the verdict row to `.planning/spikes/MANIFEST.md` under a new "Idea 4 -- v0.2.1 Angular CLI builder
    GATE A'" heading (mirror the existing per-idea table format), and add a findings-index row to
    `.claude/skills/spike-findings-angular-typechecker/SKILL.md` surfacing the GATE A' verdict (the channel
    that carried the Phase-16 gate 006-008).
  </action>
  <verify>
    <automated>node .planning/spikes/011-builder-ng-run-esm-bridge/harness.mjs</automated>
  </verify>
  <acceptance_criteria>
    - forensic-log.json exists, records the clone repo URL + commit `818e9ae55240b570397ede5a15cb4d466785abdc`,
      per-project `ng run` exit codes + stdout excerpts, the ESM-signature scan result, and a `verdict` field.
    - The ESM-signature scan finds NONE of `ERR_REQUIRE_ESM` / `require() of ES Module` /
      `Cannot use import statement outside a module` in either project's `ng run` output.
    - The planted diagnostic surfaces RED (non-zero exit) on the project it was planted in, and the clean
      project is GREEN (exit 0).
    - The harness prints `VERDICT: GO` and `process.exit(0)` when all assertions pass.
    - .planning/spikes/MANIFEST.md and the spike-findings SKILL.md carry the recorded verdict row; the clone
      and its node_modules are NOT committed.
  </acceptance_criteria>
  <done>The GATE A' spike ran a real `ng run` on the app and library, produced GO evidence with no ESM failure signatures, and the record (harness + README + forensic-log + MANIFEST + skill) is committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    A minimal Angular CLI builder (`convertNxExecutor` re-export) with manifests + sanitized schema, the
    static byte-guard extended to the builder entry, and GATE A' spike 011 run against the real Angular 22
    `bluehalo/ngx-leaflet` clone with a recorded GO/NO-GO VERDICT in forensic-log.json.
  </what-built>
  <how-to-verify>
    1. Open .planning/spikes/011-builder-ng-run-esm-bridge/forensic-log.json and confirm `verdict` = GO.
    2. Confirm the ESM-signature scan result is empty for BOTH ngx-leaflet-demo and ngx-leaflet.
    3. Confirm the planted diagnostic surfaced RED and the clean project was GREEN (per-project exit codes).
    4. Confirm the on-stack install needed NO `--legacy-peer-deps` (or read the recorded finding if it did).
    5. Confirm `nx build angular-typechecker && nx test angular-typechecker` is green.
  </how-to-verify>
  <resume-signal>
    Type "GO" to authorize Waves 2-3 (ENG-01 + the ACB in-repo guard suite). If the VERDICT is NO-GO, type
    "NO-GO" -- the phase HALTS with a documented re-scope (never a hand-written @angular-devkit/architect
    builder, D-04); do NOT proceed to Plan 02/03.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Angular CLI (`ng run`) -> builder options | Consumer-supplied `tsConfig`/flags cross into the builder via Architect's schema validator |
| Local dev machine -> npm registry + external clone | The spike packs a dist tarball and `npm install`s it into an out-of-repo clone (drags `nx` transitively) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering (Input Validation, V5) | builder `schema.json` option surface | mitigate | Sanitized builder schema keeps `additionalProperties:false` + `required:["tsConfig"]`; the engine requires an absolute tsconfig path and never reads `process.cwd()`. Architect rejects unknown options (Pitfall 7). |
| T-21-02 | Tampering / Elevation (Supply chain, V14) | packed dist tarball + transitive `nx` install into the clone | mitigate | No NEW shipped dependency (convertNxExecutor is in the already-pinned @nx/devkit@23.0.1). Tarball-content audit asserts builders.json + builder.js + schema.json are the built artifacts; the `nx`-transitive + `.nx/` dir into a consumer workspace is a documented expected tradeoff (Pitfall 4), cleaned up by the harness. Never commit the clone or its node_modules. |
| T-21-03 | Repudiation (false PASS) | the bridge under `ng run` | mitigate | The GATE proves real diagnostics FLOW (planted error RED, clean GREEN) -- a vacuous pass is caught. The UNCHANGED core invariant (UNKNOWN_ERROR_CODE 500 re-thrown as TypecheckInfrastructureError) carries through the builder unchanged (convertNxExecutor wraps the same executor). |
| T-21-SC | Tampering | no npm/pip/cargo install of a NEW package in this plan | accept | RESEARCH Package Legitimacy Audit: no new shipped deps; the only install is our own built tarball into the clone. slopcheck clean. No blocking-human legitimacy checkpoint required (no [ASSUMED]/[SUS] packages). |
</threat_model>

<verification>
- `nx build angular-typechecker && nx test angular-typechecker` green (build-before-static-read for gate-a-static).
- `nx lint angular-typechecker` green (no @nx/dependency-checks regression from the builders field).
- The packed dist tarball contains src/builders/typecheck/builder.js, the builder schema.json, and builders.json.
- GATE A' spike 011 forensic-log.json records VERDICT=GO with no ESM failure signatures on the app or library.
</verification>

<success_criteria>
- The `typecheck` executor is re-exported as an Angular CLI builder (ACB-01 wiring landed; parity proven by the gate).
- `builders.json` + the package.json `builders` field are added additively and `nx run <project>:typecheck` still resolves (ACB-03).
- A real `ng run <project>:typecheck` on-stack Angular 22 completes with NO ERR_REQUIRE_ESM incl. the eager prelude, and the GO/NO-GO VERDICT is recorded (ACB-02 / GATE A').
- The human checkpoint confirms GO before Waves 2-3 proceed.
</success_criteria>

<output>
Create `.planning/phases/21-angular-cli-builder-engine-multi-tsconfig-gate-a-spike-go-no/21-01-SUMMARY.md` when done.
</output>
