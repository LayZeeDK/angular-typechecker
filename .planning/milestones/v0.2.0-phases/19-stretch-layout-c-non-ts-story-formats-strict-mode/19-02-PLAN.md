---
phase: 19-stretch-layout-c-non-ts-story-formats-strict-mode
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-composition/**
  - e2e/angular-typechecker-install-e2e/src/storybook-composition.int.spec.ts
autonomous: true
requirements: [SB-08]

must_haves:
  truths:
    - "A broken composed lib story FAILs via its OWN project's typecheck target (distinct full code token, e.g. TS2322)"
    - "Running the host's typecheck (dependsOn: ['^typecheck']) FAILs when a composed lib is broken -- the recipe fans out over the whole composed set"
    - "A mistyped host `refs` entry (e.g. url set to a number) FAILs the host's typecheck as an ordinary TypeScript error on .storybook/main.ts"
    - "A clean composed set passes (baseline exit 0) -- forced-SB10 node_modules .d.ts errors do not leak in-project"
    - "@storybook/angular@10.4.6 is force-installed --legacy-peer-deps in a SEPARATE step AFTER nx add angular-typechecker (install order + B-03, mirroring storybook-tarball.int.spec.ts)"
  artifacts:
    - path: "e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-composition"
      provides: "Synthetic-hybrid Composition workspace: 2 Layout-A libs with stories + a composing host with refs + implicitDependencies + dependsOn:['^typecheck']"
    - path: "e2e/angular-typechecker-install-e2e/src/storybook-composition.int.spec.ts"
      provides: "The Composition negative tests (broken composed story, fan-out, mistyped refs) + clean baseline -- a NEW spec FILE in the EXISTING serialized e2e project"
  key_links:
    - from: "consumer-storybook-composition/storybook-host/project.json"
      to: "the composed libs"
      via: "implicitDependencies: ['lib-buttons', 'lib-cards'] -- the graph edge is the source of truth (D-04), NOT the ref URL"
      pattern: "implicitDependencies"
    - from: "consumer-storybook-composition/storybook-host/project.json"
      to: "the composed libs' typecheck targets"
      via: "the host typecheck target's dependsOn: ['^typecheck'] fans out to upstream deps (D-05 recipe)"
      pattern: "\\^typecheck"
    - from: "storybook-composition.int.spec.ts"
      to: "@storybook/angular@10.4.6"
      via: "separate --legacy-peer-deps install step after nx add angular-typechecker"
      pattern: "legacy-peer-deps"
---

<objective>
Add Storybook Composition as a supported TOPOLOGY with ZERO engine code (D-04/D-05/D-06). Composition
is a multi-project topology, not a tsconfig layout: each composed project AND the host are ordinary
per-project Layout A projects, and the host's `.storybook/main.ts` `refs` object is type-checked as
plain TypeScript. Coverage = per-project `typecheck` + Nx graph fan-out; the graph edge
(`implicitDependencies` on the host), NEVER the ref URL, is the source of truth (URL resolution was
REJECTED in D-04). This plan ships the fixture + the negative tests that prove it -- no change to
run-typecheck.ts / filter-diagnostics.ts / walk-references.ts / evaluate-result.ts.

Purpose: prove the "one command checks the composed set" DX (`dependsOn: ["^typecheck"]`) works on the
supported stack, and that a broken composed story or a mistyped host `refs` entry FAILs -- so the
Composition claim in the README (19-03) rests on an exercised recipe, not an assertion.

Output: a committed synthetic-hybrid Composition fixture + one NEW `*.int.spec.ts` FILE inside the
EXISTING `angular-typechecker-install-e2e` project (NEVER a new e2e project -- the three e2e projects
race on the shared dist tarball and are serialized; a new project would ENOENT on the tarball).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-stretch-layout-c-non-ts-story-formats-strict-mode/19-CONTEXT.md
@.planning/phases/19-stretch-layout-c-non-ts-story-formats-strict-mode/19-RESEARCH.md

<!-- THE ANALOG to model exactly (install order + clean env + planted-error discipline + timeouts): -->
@e2e/angular-typechecker-install-e2e/src/storybook-tarball.int.spec.ts

<!-- The committed Layout-A fixture shape each composed lib mirrors: -->
@e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-a/tsconfig.json
@e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-a/tsconfig.app.json
@e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-a/.storybook/tsconfig.json
@e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-a/.storybook/main.ts
@e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-a/src/button.component.ts
@e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-a/src/button.stories.ts
@e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-a/package.json
@e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-b/nx.json

<interfaces>
<!-- @workspace/test-util barrel (used by the analog spec): findWorkspaceRoot, buildCleanEnv, run,
     sh, removeTmpDir, writeVerdaccioNpmrc; vitest `inject('verdaccioUrl')` / `inject('verdaccioToken')`.
     run(cwd, target, { env, skipNxCache }) returns { code, stdout }. sh(cmd, { cwd, env }) throws on
     non-zero. The e2e project's vitest config already serializes (singleFork / fileParallelism:false)
     and the global-setup builds+publishes the dist tarball ONCE to local Verdaccio. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the synthetic-hybrid Composition fixture</name>
  <files>e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-composition/** (nx.json, package.json; lib-buttons/ and lib-cards/ each: project.json, tsconfig.json, tsconfig.app.json, .storybook/tsconfig.json, .storybook/main.ts, src/*.component.ts, src/*.stories.ts; storybook-host/: project.json, tsconfig.json, .storybook/tsconfig.json, .storybook/main.ts)</files>
  <read_first>
    - e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-a/* (the full Layout-A committed tree -- copy its shape per composed lib: solution tsconfig.json with references to tsconfig.app.json + ./.storybook/tsconfig.json; a clean standalone component; a clean story with a plantable `count: 3` anchor on its own line; .storybook/main.ts as `Partial<StorybookConfig> = {}`; package.json with the Angular 22.0.4 / nx 23.0.1 / typescript 6.0.3 deps)
    - e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-b/nx.json (the namedInputs default/production/sharedGlobals block to reuse verbatim)
    - e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-b/storybook-host/project.json (projectType library + sourceRoot shape)
    - 19-RESEARCH.md Pattern 2 (host project.json: implicitDependencies + typecheck dependsOn:['^typecheck']) and Pattern 3 (host main.ts refs shape { title, url })
  </read_first>
  <action>
    Create `consumer-storybook-composition/` as a single npm/nx workspace containing THREE projects.
    Root: `nx.json` (reuse the namedInputs block from consumer-storybook-b) and a workspace `package.json`
    (copy the dependency set from consumer-storybook-a/package.json -- Angular 22.0.4, @angular/compiler-cli
    22.0.4, nx 23.0.1, @nx/devkit 23.0.1, typescript 6.0.3; add @nx/angular 23.0.1 only if needed for the
    graph; keep it minimal). Two composed libs `lib-buttons/` and `lib-cards/`, EACH shaped exactly like
    consumer-storybook-a (solution `tsconfig.json` with references to `tsconfig.app.json` and
    `./.storybook/tsconfig.json`; `tsconfig.app.json` files the component; `.storybook/tsconfig.json`
    include `main.ts` + `../src/**/*.stories.ts`; `.storybook/main.ts` typed `Partial<StorybookConfig> = {}`;
    a clean standalone component; a clean story with a plantable numeric anchor on its own line -- e.g.
    lib-buttons `count: 3`, lib-cards `count: 5`). Each lib's `project.json`: name (`lib-buttons` /
    `lib-cards`), projectType library, and a `typecheck` target using executor `angular-typechecker:typecheck`
    with options `{ "tsConfig": "<lib>/tsconfig.json" }` (the solution tsconfig, per README rule -- NOT a leaf).
    The `storybook-host/`: `project.json` name `storybook-host`, projectType library,
    `implicitDependencies: ["lib-buttons", "lib-cards"]` (the graph edge, D-04), and a `typecheck` target
    (executor `angular-typechecker:typecheck`, tsConfig `storybook-host/tsconfig.json`) carrying
    `dependsOn: ["^typecheck"]` (the D-05 recipe). Host `tsconfig.json` references `./.storybook/tsconfig.json`;
    host `.storybook/tsconfig.json` includes `main.ts`. Host `.storybook/main.ts` imports `StorybookConfig`
    from `@storybook/angular` and exports a config carrying a `refs` object with two entries keyed by lib
    (`{ title, url: 'http://localhost:7008' }` / `7009`), typed so a wrong `url` value (a number) is a
    TypeScript error -- prefer typing the refs via `StorybookConfig['refs']` (object form) so the config
    itself needs no other required fields; keep the committed baseline CLEAN. Keep each story a PLAIN `.ts`
    that does NOT import @storybook/angular (so a planted error yields ONE deliberate diagnostic, matching
    consumer-storybook-a). ASCII only; no emojis/box-drawing.
  </action>
  <acceptance_criteria>
    - The fixture tree exists with lib-buttons, lib-cards, and storybook-host projects, each with a `typecheck` target referencing executor `angular-typechecker:typecheck`.
    - `git grep -n "implicitDependencies" e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-composition/storybook-host/project.json` matches, listing both libs.
    - `git grep -n "\\^typecheck" e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-composition/storybook-host/project.json` matches (dependsOn recipe present).
    - Host `.storybook/main.ts` imports `StorybookConfig` from `@storybook/angular` and declares a `refs` object with a string `url` per entry.
    - Each composed lib has a clean story with a numeric anchor on its own line (a plantable clean baseline).
    - `git grep -c "@storybook/angular" e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-composition/lib-buttons/.storybook/main.ts` >= 1 (the composed libs are Storybook-shaped).
  </acceptance_criteria>
  <verify>
    <automated>test -f e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-composition/storybook-host/.storybook/main.ts && git grep -q "\\^typecheck" -- e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-composition/storybook-host/project.json && git grep -q "implicitDependencies" -- e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-composition/storybook-host/project.json && echo FIXTURE_OK</automated>
  </verify>
  <done>A committed synthetic-hybrid Composition workspace: two Layout-A composed libs (each with a typecheck target) + a host with implicitDependencies + a dependsOn:['^typecheck'] typecheck target + a typed refs object.</done>
</task>

<task type="auto">
  <name>Task 2: The Composition e2e spec (clean baseline + broken-story own-target + fan-out + mistyped-refs)</name>
  <files>e2e/angular-typechecker-install-e2e/src/storybook-composition.int.spec.ts</files>
  <read_first>
    - e2e/angular-typechecker-install-e2e/src/storybook-tarball.int.spec.ts (the whole file -- the install recipe `installStorybookAndTypechecker`, the clean env `buildCleanEnv({ stripAllNpmConfig: true })`, the Verdaccio localhost assertion, the plant-via-JSON.stringify discipline, the 300000ms per-test timeout, the removeTmpDir finally)
    - 19-RESEARCH.md Pitfall 3 (install order: npm install -> nx add angular-typechecker -> npm install @storybook/angular@10.4.6 --legacy-peer-deps -- SEPARATE steps) and Pitfall 4 (NX_DAEMON=false + skipNxCache for run-many/affected under a shared junction)
    - 19-RESEARCH.md ## Validation Architecture Code Examples (the Composition negative-test scaffolding)
  </read_first>
  <action>
    Add a NEW spec FILE `storybook-composition.int.spec.ts` inside the EXISTING
    `angular-typechecker-install-e2e` project (NEVER a new e2e project -- shared-tarball serialization).
    Reuse the @workspace/test-util barrel (findWorkspaceRoot, buildCleanEnv, run, sh, removeTmpDir,
    writeVerdaccioNpmrc) and vitest `inject('verdaccioUrl')` / `inject('verdaccioToken')`. Model the install
    on storybook-tarball.int.spec.ts EXACTLY: cpSync the consumer-storybook-composition fixture into a tmp
    workspace; `npm install`; `npx nx add angular-typechecker` (NO peer override -- B-03; installs the
    SHIPPED tarball from Verdaccio and resolves the `angular-typechecker:typecheck` executor referenced by
    the fixture targets); THEN `npm install @storybook/angular@10.4.6 --legacy-peer-deps` as a SEPARATE step
    (so the host + composed .storybook/main.ts type-check against real Storybook types). Assert
    `verdaccioUrl.startsWith('http://localhost:')`. Do the expensive install ONCE per describe (a shared tmp
    via a beforeAll, torn down in afterAll), then run the scenarios sequentially with plant-then-restore of
    the fixture files (each `run` passes `{ env, skipNxCache: true }`; if any run-many/affected is used, also
    set NX_DAEMON=false in env). Scenarios:
      (1) CLEAN BASELINE FIRST: `run(tmp, 'storybook-host:typecheck', ...)` exits 0 (fan-out over clean libs
          passes; forced-SB10 .d.ts errors do not leak). Optionally also `nx run-many -t typecheck` exits 0.
      (2) BROKEN COMPOSED STORY: plant a distinct full code token into lib-buttons' story clean anchor
          (string -> the number-typed field, yielding TS2322 via JSON.stringify, ASCII-safe); assert
          `lib-buttons:typecheck` exits non-zero AND stdout contains the FULL token `TS2322` (its OWN target
          catches it). Then assert `storybook-host:typecheck` ALSO exits non-zero (the dependsOn:['^typecheck']
          fan-out fails because the upstream lib failed -- the D-05 recipe covers the set). Restore the story.
      (3) MISTYPED HOST REFS: with clean libs, rewrite the host `.storybook/main.ts` so a `refs` entry's
          `url` becomes a number (replace `url: 'http://localhost:7008'` with `url: 123` via a string
          replace); assert `storybook-host:typecheck` exits non-zero and stdout shows an ordinary `TS` code
          on main.ts. Restore.
    Every planted run also asserts stdout does NOT match /ERR_REQUIRE_ESM/ and does NOT contain
    'infrastructure error' (the failure is a real diagnostic, not a crash), mirroring the analog. Use the
    300000ms per-test timeout. Blank lines around control flow/returns; always braces.
  </action>
  <acceptance_criteria>
    - The spec lives at e2e/angular-typechecker-install-e2e/src/storybook-composition.int.spec.ts (existing project, NOT a new e2e project).
    - It force-installs `@storybook/angular@10.4.6 --legacy-peer-deps` in a SEPARATE step AFTER `nx add angular-typechecker`.
    - It asserts a clean baseline exits 0 BEFORE any planted-error assertion.
    - It asserts a broken lib-buttons story makes `lib-buttons:typecheck` fail (full token TS2322) AND `storybook-host:typecheck` fail (fan-out via dependsOn:['^typecheck']).
    - It asserts a mistyped host `refs` url (number) makes `storybook-host:typecheck` fail on an ordinary TS diagnostic.
    - Each planted run asserts stdout has no /ERR_REQUIRE_ESM/ and no 'infrastructure error'.
    - `npx nx test angular-typechecker-install-e2e --skip-nx-cache` passes (serialized).
  </acceptance_criteria>
  <verify>
    <automated>npx nx test angular-typechecker-install-e2e --skip-nx-cache</automated>
  </verify>
  <done>The Composition topology is proven on the supported stack: per-project targets catch a broken composed story, the dependsOn:['^typecheck'] fan-out covers the set, and a mistyped host refs entry fails -- with a clean baseline passing first.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e tmp workspace -> npm registry | the fixture install resolves packages; must read ONLY local Verdaccio |
| transient @storybook/angular install | a throwaway force-install into a tmp workspace, never a tracked manifest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Spoofing | e2e npm install registry | mitigate | `buildCleanEnv({ stripAllNpmConfig: true })` + `writeVerdaccioNpmrc` + assert `verdaccioUrl.startsWith('http://localhost:')`; set `npm_config_userconfig` to a nonexistent path so the user ~/.npmrc cannot reintroduce a peer-resolution override (B-03), mirroring storybook-tarball.int.spec.ts. |
| T-19-04 | Tampering (supply chain) | transient @storybook/angular@10.4.6 install | accept | Pinned EXACT version, official Storybook framework package, already exercised in the shipped Phase-18 e2e/CI, installed into a throwaway tmp workspace only, never added to any tracked package.json. |
| T-19-SC | Tampering | npm installs (transient, e2e only) | accept | Per 19-RESEARCH.md ## Package Legitimacy Audit: NO new package is added to a tracked manifest; the transient force-install mirrors the shipped storybook-tarball.int.spec.ts. No [ASSUMED]/[SUS] packages -> no blocking human checkpoint required. |
</threat_model>

<verification>
- `npx nx test angular-typechecker-install-e2e --skip-nx-cache` green (the Composition spec + the existing serialized e2e set).
- The Composition spec is a new FILE in the existing project: `git grep -l "Composition" -- e2e/angular-typechecker-install-e2e/src` includes storybook-composition.int.spec.ts, and no new `e2e/angular-typechecker-*-e2e` project dir was added.
- Before the phase gate: `npx nx run-many -t lint` (maxWarnings:0) + `npx nx format:check` clean for the new files.
</verification>

<success_criteria>
- Storybook Composition ships as a supported topology with ZERO engine code (no change to run-typecheck.ts / filter-diagnostics.ts / walk-references.ts / evaluate-result.ts).
- Every shipped Composition behavior carries a negative test (phase-19 criterion 2): broken composed story (own target + fan-out) and mistyped host refs both FAIL; the clean baseline passes.
- The `dependsOn: ["^typecheck"]` recipe is EXERCISED by the fan-out assertion (its documentation lands in 19-03).
</success_criteria>

<output>
Create `.planning/phases/19-stretch-layout-c-non-ts-story-formats-strict-mode/19-02-SUMMARY.md` when done.
</output>
