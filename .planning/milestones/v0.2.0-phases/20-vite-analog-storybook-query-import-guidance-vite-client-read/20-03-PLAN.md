---
phase: 20-vite-analog-storybook-query-import-guidance-vite-client-read
plan: 03
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - packages/angular-typechecker/README.md
  - CHANGELOG.md
autonomous: true
requirements: [SB-09]

must_haves:
  truths:
    - "The README ## Storybook Vite caveat LEADS with the recommended fix -- add \"types\": [\"vite/client\"] to the CHECKED tsconfig (D-07)"
    - "The caveat names the hand `declare module '*?query'` .d.ts as the no-vite-dependency fallback and calls it INCOMPLETE by construction"
    - "The caveat documents the ONE wildcard blind spot (a ?query import of a MISSING base resolves through the wildcard and will NOT error -- Vite's own build-vs-typecheck split)"
    - "The caveat cross-references the new bundlerQueryImports advisory field and reaffirms the TS2307 are NEVER auto-suppressed"
    - "The Programmatic-API CoreResult comment lists bundlerQueryImports?: readonly string[]"
    - "The curated v0.1.2 CHANGELOG entry documents both signals (Signal 1 as guidance, Signal 2 as a feat advisory line) -- prose only, NO release cut"
  artifacts:
    - path: "packages/angular-typechecker/README.md"
      provides: "restructured Vite caveat (leads with the fix) + the CoreResult API comment gains the field"
      contains: "vite/client"
    - path: "CHANGELOG.md"
      provides: "both signals folded into the existing 0.1.2 entry"
      contains: "bundlerQueryImports"
  key_links:
    - from: "packages/angular-typechecker/README.md (## Storybook Vite caveat)"
      to: "the bundlerQueryImports field under ## Programmatic API"
      via: "cross-reference so a reader hitting TS2307 finds both the fix and the advisory"
      pattern: "bundlerQueryImports"
---

<objective>
Signal 1 (docs-only, ZERO engine change, D-07/D-08): restructure the README `## Storybook` Vite caveat
so it LEADS with the proven fix -- `"types": ["vite/client"]` on the checked tsconfig -- names the hand
ambient-shim fallback (incomplete by construction), documents the one wildcard blind spot, cross-refs
the new Signal 2 `bundlerQueryImports` field, and reaffirms the diagnostics are never auto-suppressed.
Add the field to the Programmatic-API `CoreResult` comment. Fold both signals into the existing curated
v0.1.2 `CHANGELOG.md` entry as prose. NO release cut (D-11).

Purpose: today's caveat BURIES the fix at the end of a long paragraph; a consumer hitting 227 TS2307
should read the one-line fix FIRST. Grounded in spike 009 (radix-ng 227 -> 0, no-false-pass preserved).

Output: the restructured caveat + the API comment field + the changelog additions. Depends on 20-01 for
the locked field name `bundlerQueryImports`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-vite-analog-storybook-query-import-guidance-vite-client-read/20-CONTEXT.md
@.planning/phases/20-vite-analog-storybook-query-import-guidance-vite-client-read/20-RESEARCH.md
@.claude/skills/spike-findings-angular-typechecker/references/vite-analog-query-imports.md

<interfaces>
<!-- Exact edit locations (verified this planning session). -->

README.md:
  // ## Storybook is at line 353. The Programmatic-API CoreResult comment is at 310-315 and currently
  //   ends: `notTypeCheckedDeclaredFiles?: readonly string[] }`.
  // The Vite caveat bullet is at ~432-443 -- currently a single paragraph that OPENS with
  //   "Vite/Analog Storybook import suffixes need ambient declarations" and BURIES the fix (the
  //   `declare module` / `"vite/client"` clause) near the end. Restructure IN PLACE (keep it a bullet
  //   in the same Caveats list; do NOT move the Storybook story into a docs/ dir -- consistent with
  //   Phase 18/19 D-05).

CHANGELOG.md (repo root, NOT under packages/):
  // The `## 0.1.2 (2026-07-06)` entry already exists (pre-written in Phase 18-05, NOT yet released --
  //   package.json is still 0.1.1). It has ### Features / ### Fixes / ### Internal / ### Compatibility.
  // Fold this phase's two signals INTO that same entry: a new ### Features bullet for the
  //   bundlerQueryImports advisory (Signal 2, a feat), and a guidance note (Signal 1) -- extend the
  //   existing Storybook prose or add a short guidance bullet. Do NOT create a new version heading; do
  //   NOT bump package.json (no release cut this phase -- the eventual bump is human-gated, D-11).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure the README Vite caveat + add the field to the CoreResult API comment</name>
  <files>packages/angular-typechecker/README.md</files>
  <read_first>
    - packages/angular-typechecker/README.md (the ## Storybook section 353-468, the Vite caveat bullet 432-443, and the Programmatic-API CoreResult comment 310-315)
    - .planning/spikes/009-vite-ambient-shim-resolves-query-imports/README.md (the proven recipe: `"types": ["vite/client"]` 227 -> 0; the hand-shim fallback is incomplete; the wildcard blind spot -- `./ghost.md?raw` resolves through `*?raw`)
    - .claude/skills/spike-findings-angular-typechecker/references/vite-analog-query-imports.md (the exact `"types": ["vite/client"]` recipe + the `declare module '*?query'` fallback + the blind-spot wording)
  </read_first>
  <action>
    Rewrite the Vite caveat bullet (~432-443) so it LEADS with the fix (D-07): first sentence recommends
    adding `"types": ["vite/client"]` to the tsconfig angular-typechecker is pointed at, and states this
    drove a real project's 227 `?query` TS2307 to 0 with no false pass. Then name the hand
    `declare module '*?raw' { const src: string; export default src; }` `.d.ts` as the
    no-`vite`-dependency FALLBACK, explicitly INCOMPLETE by construction (only covers the suffixes you
    enumerate -- prefer `vite/client`). Then document the ONE wildcard blind spot honestly: an ambient
    wildcard (`*?raw`) matches the SPECIFIER, not the file, so a `?query` import of a MISSING base file
    resolves through the wildcard and will NOT error -- the same build-vs-typecheck split Vite itself
    has; narrow (only `?query`-suffixed imports of a missing base). Cross-reference the new
    `bundlerQueryImports` advisory field (link/point to the Programmatic API section) so a reader who
    hits the TS2307 finds both the fix and the detection signal. Reaffirm the tool NEVER auto-suppresses
    these TS2307 (a missing module can be a genuine error). Keep it a bullet in the same Caveats list;
    keep the whole Storybook story in the README. In the Programmatic-API `CoreResult` comment (310-315),
    add `bundlerQueryImports?: readonly string[]` to the listed optional fields (after
    `notTypeCheckedDeclaredFiles?`). ASCII only; no em-dashes/curly-quotes/unicode.
  </action>
  <verify>
    <automated>npx nx run angular-typechecker:lint</automated>
  </verify>
  <acceptance_criteria>
    - The FIRST sentence of the restructured Vite caveat bullet leads with `"types": ["vite/client"]` (verify by reading 432-450 -- the fix is stated before the problem restatement).
    - `git grep -n "declare module" packages/angular-typechecker/README.md` matches (the hand-shim fallback is named), and the bullet calls it incomplete.
    - The bullet contains the wildcard blind-spot sentence (a ?query import of a missing base resolves through the wildcard) and the never-auto-suppressed reaffirmation.
    - `git grep -c "bundlerQueryImports" packages/angular-typechecker/README.md` is >= 2 (the caveat cross-ref + the CoreResult API comment).
    - No non-ASCII bytes introduced; `npx nx run angular-typechecker:lint` exits 0.
  </acceptance_criteria>
  <done>The README caveat leads with the fix, names the fallback + blind spot, cross-refs the field, and the API comment lists bundlerQueryImports.</done>
</task>

<task type="auto">
  <name>Task 2: Fold both signals into the curated v0.1.2 CHANGELOG entry (prose only, no release cut)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (the existing `## 0.1.2 (2026-07-06)` entry, lines 5-64 -- its ### Features / ### Fixes / ### Internal / ### Compatibility structure and prose voice)
    - .planning/phases/.../20-CONTEXT.md D-08 (fold Signal 1 as guidance, Signal 2 as a feat advisory line) and D-11 (prose only, NO release cut)
    - AGENTS.md `## Repo-specific gotchas` Pitfall 5 (changelog scope hygiene -- no internal plan-id scopes; hand-curated public prose)
  </read_first>
  <action>
    Extend the EXISTING `## 0.1.2` entry (do NOT add a new version heading, do NOT bump package.json).
    Add a new `### Features` bullet for Signal 2: a `bundlerQueryImports` field on the programmatic
    `CoreResult`, surfaced as a loud executor advisory, that flags unresolved `TS2307` whose module
    specifier contains a `?` bundler query (Vite/Analog `?raw`/`?url`/`?worker`/`?inline`) -- verdict-
    neutral, self-gating (silent once resolved), NEVER suppresses the diagnostic; recommend
    `"types": ["vite/client"]` as the fix. Add a short guidance note for Signal 1: the README Storybook
    caveat now leads with the `"types": ["vite/client"]` recipe (hand ambient-shim fallback) for Vite
    query imports. Match the entry's existing prose voice and ASCII conventions; use release-meaningful
    wording, never internal plan/phase ids. Prose only -- do NOT cut a release, tag, or run nx release.
  </action>
  <verify>
    <automated>git grep -c "bundlerQueryImports" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - The `## 0.1.2` entry now mentions `bundlerQueryImports` and the `"types": ["vite/client"]` fix (both signals folded in).
    - No new version heading was added; `git grep -c "^## 0.1.2" CHANGELOG.md` is `1`.
    - `packages/angular-typechecker/package.json` version is UNCHANGED at `0.1.1` (no release cut).
    - No internal plan-id/phase scopes leaked into the entry (no `20-01`, `[#N]`, etc.).
    - No non-ASCII bytes introduced.
  </acceptance_criteria>
  <done>Both signals are documented in the existing curated 0.1.2 changelog entry; no release cut, no version bump.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> consumer configuration | The README recipe tells consumers to add `vite/client` to their own tsconfig; no engine surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Information (accuracy of guidance) | README Vite caveat | mitigate | The recipe + blind spot are grounded in spike 009 (verified radix 227 -> 0; documented wildcard limitation); the caveat reaffirms never-auto-suppressed so no reader infers a silent false pass. |
| T-20-05 | Tampering (accidental release) | CHANGELOG / package.json | mitigate | Prose only -- no `nx release`, no tag, no version bump (D-11); acceptance asserts package.json stays 0.1.1 and no new version heading. |
| T-20-SC | Tampering | package installs | accept | N/A -- docs-only plan; no package installs. |
</threat_model>

<verification>
- `npx nx run angular-typechecker:lint` and Prettier `format:check` stay green (docs are linted/formatted).
- The Vite caveat leads with the fix; the CoreResult API comment and the caveat both reference `bundlerQueryImports`.
- No release artifacts changed (package.json 0.1.1; no new changelog version heading).
</verification>

<success_criteria>
- README caveat: fix-first, fallback named + called incomplete, blind spot documented, field cross-referenced, never-auto-suppressed reaffirmed.
- Programmatic-API CoreResult comment lists the new field.
- Both signals appear in the curated 0.1.2 changelog entry; no release cut.
</success_criteria>

<output>
Create `.planning/phases/20-vite-analog-storybook-query-import-guidance-vite-client-read/20-03-SUMMARY.md` when done.
</output>
