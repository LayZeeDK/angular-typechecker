---
phase: 20-vite-analog-storybook-query-import-guidance-vite-client-read
plan: 04
type: execute
wave: 3
depends_on: [20-01, 20-02, 20-03]
files_modified: []
autonomous: true
requirements: [SB-09]

must_haves:
  truths:
    - "On the merged tree, nx build + nx test + lint (maxWarnings 0) + Prettier format:check ALL pass locally BEFORE any push (they are the required CI gates)"
    - "The feature branch is pushed to origin and a PR into main is open, carrying BOTH the code and the .planning/ updates"
    - "The required CI checks -- `ci` + CodeQL `Analyze (actions)` + `Analyze (javascript-typescript)` -- are GREEN on the PR"
    - "The executor does NOT merge the PR, does NOT cut/publish any release, and does NOT approve the npm-publish environment (all human-gated, D-11)"
  artifacts: []
  key_links:
    - from: "the pushed feature branch"
      to: "a PR into main"
      via: "gh pr create --base main"
      pattern: "gh pr"
    - from: "the PR"
      to: "green required CI"
      via: "gh pr checks polled until ci + both CodeQL checks pass"
      pattern: "gh pr checks"
---

<objective>
Gate A (user-added HARD phase-end gate, D-09/D-11, AUTONOMOUS up to green CI): run the four required
CI gates locally on the merged tree, then push the feature branch and open a PR into `main`, and
confirm the required CI checks go green. The chain drives ONLY up to "PR open + green CI" -- merging to
`main` and any release cut/publish/environment approval are HUMAN-gated and MUST NOT be performed here.

Purpose: prove the SB-09 code + docs pass the same gates CI enforces, and get the PR to a mergeable,
green state for the human to merge.

Output: a pushed branch, an open PR into main, and a green required-CI report surfaced to the user.
Depends on 20-01, 20-02, 20-03 (all code + docs merged).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-vite-analog-storybook-query-import-guidance-vite-client-read/20-CONTEXT.md
@AGENTS.md

<interfaces>
<!-- Repo release + branch rules (from AGENTS.md). -->
// - `main` is PR-ONLY (empty-bypass ruleset); NEVER `git push origin main`.
// - Required PR checks: `ci` + CodeQL `Analyze (actions)` + `Analyze (javascript-typescript)`.
// - Current feature branch: `gsd/v0.1.2-storybook-story-type-checking`.
// - The PR carries BOTH the code AND `.planning/` (do NOT strip .planning/).
// - This is a FEATURE PR, NOT a release PR: no version bump, no tag, no `nx release`.
// - Required local gates that mirror CI: `npx nx build angular-typechecker`,
//     `npx nx test angular-typechecker`, `npx nx run angular-typechecker:lint` (maxWarnings 0),
//     Prettier `format:check` (the repo's format-check script/command).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the four required gates locally on the merged tree</name>
  <files></files>
  <read_first>
    - AGENTS.md `## The default-branch ruleset` + `## Quick checklist before cutting a release` (the required `ci` check; format:check + lint are required CI gates)
    - .planning/phases/.../20-VALIDATION.md `## Manual-Only Verifications` (Gate A row) and `## Test Infrastructure` (the also-required lint + format:check gates)
    - MEMORY [[verify-format-and-lint-before-release]] (phase verification often skips format:check + lint -- both are required, maxWarnings 0)
  </read_first>
  <action>
    On the merged main-checkout tree (single-plan wave -- no worktree; real node_modules present), run
    the four gates that CI enforces, in order, and confirm each is clean: `npx nx build
    angular-typechecker`; `npx nx test angular-typechecker`; `npx nx run angular-typechecker:lint`
    (maxWarnings 0 -- zero warnings tolerated); and the repo's Prettier format check (the `format:check`
    script / `npx prettier --check` over the changed files). If ANY gate fails, STOP and surface the
    failure -- do NOT push a red tree. Do NOT run `nx release` / tag / bump the version (this is not a
    release).
  </action>
  <verify>
    <automated>npx nx build angular-typechecker && npx nx test angular-typechecker && npx nx run angular-typechecker:lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx nx build angular-typechecker` exits 0.
    - `npx nx test angular-typechecker` exits 0 (all tiers green).
    - `npx nx run angular-typechecker:lint` exits 0 with zero warnings (maxWarnings 0).
    - Prettier format:check reports no formatting drift on the changed files.
    - No release/tag/version-bump command was run; package.json version is still `0.1.1`.
  </acceptance_criteria>
  <done>All four required CI gates pass locally on the merged tree.</done>
</task>

<task type="auto">
  <name>Task 2: Push the branch, open a PR into main, confirm required CI green (do NOT merge)</name>
  <files></files>
  <read_first>
    - AGENTS.md `## The default-branch ruleset` (main is PR-only; empty bypass -- NEVER push main directly; recover a red required check by editing the ruleset, not adding a standing bypass)
    - .planning/phases/.../20-CONTEXT.md D-11 (chain drives up to "PR open + green CI" ONLY; merge + release + environment approval are human-gated and MUST NOT be auto-approved)
    - MEMORY [[never-approve-github-deployments]] (never approve a deployment/environment gate -- it is human-only)
  </read_first>
  <action>
    Confirm the current branch is `gsd/v0.1.2-storybook-story-type-checking` (the feature branch). Stage
    ONLY the phase's changed files by name (NEVER `git add .`), commit any uncommitted SB-09 work with
    release-meaningful Conventional-Commit scopes (`feat(core): ...` / `feat(executor): ...` for the
    engine + render; `docs: ...` for README/CHANGELOG -- NEVER internal plan-id scopes; AGENTS.md
    Pitfall 5), and push the branch to origin. Open a PR into `main` via `gh pr create --base main` with
    a clean title (no internal scope) and a body summarizing SB-09 (both signals + charter guard). The
    PR carries BOTH the code and the `.planning/` updates -- do NOT strip `.planning/`. Poll
    `gh pr checks` until the required checks (`ci` + CodeQL `Analyze (actions)` + `Analyze
    (javascript-typescript)`) report success. STOP THERE: do NOT merge the PR, do NOT run `nx release`,
    do NOT tag, and do NOT approve the `npm-publish` (or any) environment/deployment gate -- all are
    human-gated (D-11). If a required check goes RED, surface the specific failing check and its log to
    the user; do NOT force it green by editing the ruleset unless the human directs it.
  </action>
  <verify>
    <automated>gh pr view --json state,url,statusCheckRollup</automated>
  </verify>
  <acceptance_criteria>
    - The feature branch is pushed to origin (`git status` shows up to date with the remote).
    - A PR into `main` is OPEN (`gh pr view --json state` is `OPEN`) and its URL is surfaced to the user.
    - The PR includes both the source changes and the `.planning/` updates.
    - `gh pr checks` shows `ci`, CodeQL `Analyze (actions)`, and `Analyze (javascript-typescript)` all passing.
    - The PR is NOT merged; no release/tag was created; no environment/deployment gate was approved.
  </acceptance_criteria>
  <done>The branch is pushed, a PR into main is open with green required CI, and nothing was merged, released, or approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local checkout -> origin (PR) | Code crosses into the shared remote and CI environment via a PR. |
| autonomous chain -> irreversible actions (merge/release/deploy) | The chain must NOT cross into merge/publish/environment-approval. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06 | Elevation of Privilege (unauthorized irreversible action) | merge / nx release / npm-publish environment | mitigate | The plan STOPS at "PR open + green CI"; merging, releasing, tagging, and approving the npm-publish environment are explicitly forbidden here (D-11 + never-approve-deployments) and asserted in acceptance criteria. |
| T-20-07 | Tampering (red-tree push) | pushed branch | mitigate | Task 1 runs all four required gates locally first; a failing gate STOPS the push. `main` is PR-only, so nothing lands there without the required checks + a human merge. |
| T-20-SC | Tampering | package installs | accept | N/A -- no package installs in this phase. |
</threat_model>

<verification>
- All four required CI gates pass locally before the push.
- The PR into main is open with green `ci` + both CodeQL checks.
- No merge, no release cut, no tag, no environment approval occurred.
</verification>

<success_criteria>
- Local build + test + lint + format:check green on the merged tree.
- Feature branch pushed; PR into main open (code + .planning/); required CI green.
- Merge/release/publish/environment-approval left for the human (D-11).
</success_criteria>

<output>
Create `.planning/phases/20-vite-analog-storybook-query-import-guidance-vite-client-read/20-04-SUMMARY.md` when done. Surface the PR URL and the green-CI status to the user.
</output>
