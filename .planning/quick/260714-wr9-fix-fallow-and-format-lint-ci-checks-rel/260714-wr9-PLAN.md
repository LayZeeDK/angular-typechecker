---
phase: 260714-wr9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .fallowrc.jsonc
  - .prettierignore
  - packages/angular-typechecker/src/core/angular-cli-wiring.spec.ts
autonomous: true
requirements:
  - REL-BLOCKER-fallow
  - REL-BLOCKER-format-lint

must_haves:
  truths:
    - "nx format:check --base origin/main reports zero unformatted files (exit 0)"
    - "npx fallow audit --base origin/main returns verdict PASS (exit 0)"
    - "nx test angular-typechecker stays green (all guards, incl. ci-e2e-coverage-guard)"
    - "nx lint angular-typechecker stays green (maxWarnings:0, @nx/dependency-checks unaffected)"
    - "The ng-cli-workspace fixture is NOT reformatted -- it is .prettierignore'd (preserves ng new fidelity)"
    - "No package.json version mutation (angular-typechecker stays 0.2.0)"
    - "@angular-devkit/architect remains an optional peerDependency (NOT moved to devDependencies)"
  artifacts:
    - path: ".fallowrc.jsonc"
      provides: "entry +5, ignoreDependencies +2, rules.test-only-dependencies off, health.ignore +4 globs, new duplicates.ignore block"
      contains: "test-only-dependencies"
    - path: ".prettierignore"
      provides: "ng-cli-workspace fixture dir ignored"
      contains: "ng-cli-workspace"
    - path: "packages/angular-typechecker/src/core/angular-cli-wiring.spec.ts"
      provides: "Prettier-conformant (whitespace only, no logic change)"
  key_links:
    - from: ".fallowrc.jsonc"
      to: "the 5 config-only-reachable files"
      via: "entry list clears unused-files false positives"
      pattern: "global-setup.ts|index.drift.ts|schematic.ts|aggregate-install-timings.mjs"
    - from: ".prettierignore"
      to: "e2e/angular-typechecker-ng-cli-e2e/fixtures/ng-cli-workspace/"
      via: "nx format:check skips the ng new fixture"
      pattern: "ng-cli-workspace"
    - from: ".fallowrc.jsonc rules.test-only-dependencies:off"
      to: "@angular-devkit/architect"
      via: "clears the test-only production dependency finding"
      pattern: "test-only-dependencies"
---

<objective>
Fix the two Release-PR-blocking CI checks (`fallow` audit + `format-lint`) surfaced by the
260714-sl6 dress-rehearsal PR #35. Both are pre-existing, accumulated across the v0.2.1
milestone, and WILL fail the v0.2.1 Release-PR's `ci` check.

This is a CONFIG + one format pass. NO product-logic change. NO package.json version
mutation. Every fallow fix follows the existing .fallowrc.jsonc precedent (FAL-xx / IN-xx
entries): `entry` for config-only-reachable files, `ignoreDependencies` for
Angular-CLI-provided type-only imports, `rules.*: off` for structural false positives,
`health.ignore` for reviewed essential-complexity files, `duplicates.ignore` for intentional
verbatim siblings. Per the CLAUDE.md fallow guidance: declare entry-points / ignores for the
false positives; do NOT refactor working, tested code to satisfy an estimated threshold.

Purpose: unblock the v0.2.1 Release-PR (the human-gated release cut runs AFTER this).
Output: a green `nx format:check` + a PASS `fallow audit` on the feature->main diff, locally
verifiable with the exact CI commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.fallowrc.jsonc
@.prettierignore
@.planning/quick/260714-sl6-fix-the-e2e-ci-regression-nx-run-many-t-/260714-sl6-SUMMARY.md

Exact fallow findings (reproduced locally, `npx fallow audit --format human --base origin/main`,
verdict = FAIL; 330 changed files vs origin/main):

Dead code (8 issues):
  Unused files (5) -- config-only-reachable FALSE POSITIVES, rule `unused-files`:
    - e2e/angular-typechecker-ng-cli-e2e/src/global-setup.ts   (vitest globalSetup string ref)
    - packages/angular-typechecker/src/index.drift.ts          (barrel drift tripwire, tsconfig.drift.json files)
    - packages/angular-typechecker/src/schematics/configuration/schematic.ts (convertNxGenerator re-export, collection.json)
    - packages/angular-typechecker/src/schematics/init/schematic.ts          (same, collection.json)
    - tools/e2e-timing/aggregate-install-timings.mjs           (CLI tool run via `node ...`, never imported)
  Unlisted dependencies (2) -- Angular-CLI-provided, type-only imports by the vanilla ng-add schematic:
    - @angular-devkit/core
    - @angular-devkit/schematics                               (@nx/dependency-checks already ignores this, 24-06)
  Test-only production dependency (1) -- rule `test-only-dependencies`:
    - @angular-devkit/architect  (INTENTIONAL optional peerDependency, 23-02; imported by builder.integration.spec)

Duplication (2 clone groups):
  - dup:85d857bc (198 lines) install-e2e/src/global-setup.ts:34-231 + ng-cli-e2e/src/global-setup.ts:45-242
    -> INTENTIONAL verbatim siblings (byte-identical Verdaccio bootstrap by design).
  - dup:c3bbefb1 (38 lines) core/run-typecheck.ts:641-658 + core/walk-references.ts:250-287
    -> the DELIBERATELY-MIRRORED "surviving entry" vs "surviving leaf" diagnostic-accumulation
       tails of the two walk implementations. Parity is an intentional design invariant
       (the comments enforce "mirror walk-references" / "MD-01 parity with the direct path")
       and is spec-covered. Extraction would thread 4 mutable accumulators + ng/ts/parsed
       across two hot core paths -- exactly the risky core refactor this config-only task forbids.

Complexity (4 above threshold; large functions in the same files):
  - packages/angular-typechecker/src/core/run-typecheck.ts (runTypecheck, handleMultiTsConfig, handleSolutionWalk, finalize)
  - packages/angular-typechecker/src/generators/configuration/generator.ts (configurationGenerator)
  - packages/angular-typechecker/src/schematics/ng-add/schematic.ts (ngAdd arrow)
  - tools/e2e-timing/aggregate-install-timings.mjs (deriveAction, derivePm)
  All are ESTABLISHED, TESTED, essential-complexity functions -- same class as
  core/walk-references.ts, already in health.ignore.

Exact fallow config keys (verified via `fallow config-schema`):
  - `entry`: string[] (file paths, forward-slash, relative to root)
  - `ignoreDependencies`: string[]
  - `rules.test-only-dependencies`: "off" | "warn" | "error"  (mirrors the existing `unused-dev-dependencies: off`)
  - `health.ignore`: string[] GLOB patterns (excludes a file from complexity analysis)
  - `duplicates.ignore`: string[] GLOB patterns (excludes a file from clone detection; `ignoreDefaults` stays true)

format:check flags 26 files: 25 under the ng new fixture
`e2e/angular-typechecker-ng-cli-e2e/fixtures/ng-cli-workspace/` (committed authentic `ng new`
output, regenerated via that dir's REGENERATE.md -- NOT Prettier-owned) + 1 real source
`packages/angular-typechecker/src/core/angular-cli-wiring.spec.ts` (Prettier-owns it).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .fallowrc.jsonc suppressions + .prettierignore the ng-cli-workspace fixture + format the one real source</name>
  <files>.fallowrc.jsonc, .prettierignore, packages/angular-typechecker/src/core/angular-cli-wiring.spec.ts</files>
  <action>
Make three config/format edits. Match the EXISTING .fallowrc.jsonc comment style (FAL-xx / IN-xx
headers, prose explaining WHY each entry is a false positive + why config not source). Tag the new
comments with this quick id (FAL-07 / etc.) for traceability. ASCII only, no emojis, no em dashes.

(1) .fallowrc.jsonc -- five additive edits, each with a documenting comment:

  a. `entry`: append the 5 config-only-reachable files (same class as the existing
     compiler-cli-types.drift.ts / extended-catalog.drift.ts / install-e2e global-setup entries):
     e2e/angular-typechecker-ng-cli-e2e/src/global-setup.ts,
     packages/angular-typechecker/src/index.drift.ts,
     packages/angular-typechecker/src/schematics/configuration/schematic.ts,
     packages/angular-typechecker/src/schematics/init/schematic.ts,
     tools/e2e-timing/aggregate-install-timings.mjs.
     Comment: these are reachable via vitest globalSetup string / tsconfig.drift.json files /
     collection.json / `node <path>` CLI invocation respectively -- never via the import graph, so
     fallow reports unused-files. Same false-positive class as the existing entry list.

  b. `ignoreDependencies`: append @angular-devkit/core and @angular-devkit/schematics.
     Comment: Angular-CLI-provided, type-only imports by the vanilla ng-add schematic (24-06 made
     the schematic use Rule/Tree/SchematicContext type-only imports, erased at compile). Same
     package-level unlisted-dependencies false positive as @angular/core (a per-glob override cannot
     reach a project-level finding -- see FAL-01). @nx/dependency-checks already ignores
     @angular-devkit/schematics (24-06) and independently gates the published manifest.

  c. `rules`: add "test-only-dependencies": "off" (mirrors the existing "unused-dev-dependencies": "off").
     Comment: @angular-devkit/architect is an INTENTIONAL OPTIONAL peerDependency (23-02, the runtime
     peer for the Architect `ng run` builder), imported by builder.integration.spec for testing.
     fallow suggests moving it to devDependencies -- WRONG: it is a runtime peer for consumers, not a
     dev tool. This repo's optional-peer pattern (architect + rxjs) structurally false-positives this
     rule; the published manifest's dep hygiene is owned by @nx/dependency-checks. Do NOT move architect
     out of optional peers.

  d. `health.ignore`: append four globs to the existing ignore array (which already carries
     core/walk-references.ts): packages/angular-typechecker/src/core/run-typecheck.ts,
     packages/angular-typechecker/src/generators/configuration/generator.ts,
     packages/angular-typechecker/src/schematics/ng-add/schematic.ts, tools/**.
     Comment: reviewed essential-complexity functions -- the whole-program type-check orchestration
     (run-typecheck.ts), the Angular CLI wiring generator/schematic, and a dev-only timing CLI tool.
     Same review-blessed class as walk-references.ts. Do NOT refactor tested core to satisfy an
     estimated-CRAP threshold (CLAUDE.md fallow guidance).

  e. Add a new top-level `duplicates` block with an `ignore` array (leave enabled/ignoreDefaults at
     their defaults -- ignore is ADDITIVE over the built-in generated-framework ignores). List:
     the two e2e global-setup paths (e2e/angular-typechecker-install-e2e/src/global-setup.ts,
     e2e/angular-typechecker-ng-cli-e2e/src/global-setup.ts) and the two core walk files
     (packages/angular-typechecker/src/core/run-typecheck.ts,
     packages/angular-typechecker/src/core/walk-references.ts).
     Comment: clone group 85d857bc = intentional verbatim-sibling Verdaccio bootstraps (test
     scaffolding, byte-identical by design); clone group c3bbefb1 = the DELIBERATELY-MIRRORED
     surviving-entry / surviving-leaf accumulation tails of the two walk implementations (parity is a
     design invariant enforced by comments + specs). Suppressed by config, not refactored -- extraction
     would thread 4 mutable accumulators + ng/ts/parsed across two hot core paths (a risky core refactor
     this config-only task forbids).

(2) .prettierignore -- append the ng-cli-workspace fixture directory
    `e2e/angular-typechecker-ng-cli-e2e/fixtures/ng-cli-workspace/` with a comment matching the
    existing whitespace-fixture / lockfile precedent: it is committed authentic `ng new` output
    (regenerated via its REGENERATE.md), not Prettier-owned; reformatting it would destroy CLI
    fidelity. Do NOT add the one real source spec here -- that gets formatted.

(3) `nx format:write` ONLY packages/angular-typechecker/src/core/angular-cli-wiring.spec.ts
    (real source, Prettier-owns it -- whitespace only, no logic change). Run:
    `NX_DAEMON=false npx nx format:write --files packages/angular-typechecker/src/core/angular-cli-wiring.spec.ts`
  </action>
  <verify>
    <automated>NX_DAEMON=false npx nx format:check --base origin/main</automated>
  </verify>
  <done>
    format:check exits 0 with zero files listed. .fallowrc.jsonc carries the 5 entry additions,
    2 ignoreDependencies, rules.test-only-dependencies:off, 4 health.ignore globs, and a duplicates.ignore
    block -- each with a documenting comment. .prettierignore carries the ng-cli-workspace fixture dir.
    angular-cli-wiring.spec.ts is Prettier-conformant (whitespace only). No package.json version change;
    @angular-devkit/architect still an optional peerDependency.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full local CI-parity verification (fallow PASS + tests + lint all green)</name>
  <files>(no edits -- verification only)</files>
  <action>
Run the exact CI-parity commands locally on the current feature branch (main checkout, no worktree)
and confirm every gate is green. If fallow still reports @angular-devkit/architect after the rule
toggle, the fallback is to ALSO add @angular-devkit/architect to `ignoreDependencies` (surgical to
the package) and re-run -- but the `test-only-dependencies: off` toggle is expected to clear it.
Do NOT change any product logic or package.json versions to make a check pass; the fixes are the
Task-1 config declarations only.
  </action>
  <verify>
    <automated>npx fallow audit --format human --base origin/main && NX_DAEMON=false npx nx test angular-typechecker --skip-nx-cache && npx nx lint angular-typechecker</automated>
  </verify>
  <done>
    `npx fallow audit --base origin/main` prints verdict PASS and exits 0 (zero dead-code / complexity /
    duplication findings introduced by the changeset). `nx test angular-typechecker` passes fully
    (all guards, incl. ci-e2e-coverage-guard). `nx lint angular-typechecker` passes at maxWarnings:0
    (@nx/dependency-checks unaffected by the .fallowrc / .prettierignore edits). `nx format:check
    --base origin/main` green (from Task 1).
  </done>
</task>

</tasks>

<threat_model>
No STRIDE register: this is a lint/config fix + one whitespace-only Prettier pass. No trust
boundary, no new dependency install, no input handling, no runtime surface change. The
@nx/dependency-checks lint gate and the published-manifest hygiene it enforces are unchanged
and independently re-verified in Task 2.
</threat_model>

<verification>
Local CI-parity battery (exact CI commands, run on the current feature branch / main checkout):
- `NX_DAEMON=false npx nx format:check --base origin/main` -> GREEN (0 files).
- `npx fallow audit --format human --base origin/main` -> verdict PASS, exit 0.
- `NX_DAEMON=false npx nx test angular-typechecker --skip-nx-cache` -> all specs + guards green.
- `npx nx lint angular-typechecker` -> maxWarnings:0, no @nx/dependency-checks regression.

Release/additive safety: only .fallowrc.jsonc + .prettierignore + one spec's whitespace change.
No product-logic change; no package.json version mutation; one hunk per file (revertible).
NOTE: a final full-suite dress-rehearsal PR (all CI jobs green together) is a SEPARATE follow-up;
the local format:check + fallow audit are the authoritative gates for THIS task (e2e already
passed in PR #35).
</verification>

<success_criteria>
- format-lint: `nx format:check --base origin/main` reports zero unformatted files.
- fallow: `npx fallow audit --base origin/main` returns verdict PASS (exit 0) -- all 8 dead-code
  issues, 4 complexity findings, and 2 duplication clone groups cleared via config declarations.
- No regression: `nx test angular-typechecker` and `nx lint angular-typechecker` stay green.
- ng-cli-workspace fixture preserved verbatim (.prettierignore'd, not reformatted).
- angular-typechecker package.json version unchanged (0.2.0); @angular-devkit/architect still an
  optional peerDependency.
</success_criteria>

<output>
Create `.planning/quick/260714-wr9-fix-fallow-and-format-lint-ci-checks-rel/260714-wr9-SUMMARY.md` when done.
</output>
