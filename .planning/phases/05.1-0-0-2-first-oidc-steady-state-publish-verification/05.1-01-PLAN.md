---
phase: 05.1-0-0-2-first-oidc-steady-state-publish-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/package.json
  - CHANGELOG.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [PKG-05]
user_setup:
  - service: github
    why: "Approve the npm-publish Environment deployment (the B-01 human gate on the irreversible publish)"
    dashboard_config:
      - task: "Approve the npm-publish environment deployment when release.yml runs"
        location: "GitHub -> Actions -> the release run -> Review deployments -> Approve"
  - service: npm
    why: "Trusted Publisher (OIDC) must remain registered + exact-match for tokenless auth"
    dashboard_config:
      - task: "Confirm the Trusted Publisher (provider GitHub Actions, repo LayZeeDK/angular-typechecker, workflow release.yml, env npm-publish) is still registered and NPM_SEED_TOKEN secret stays deleted"
        location: "npmjs.com -> angular-typechecker -> Settings -> Trusted Publisher; GitHub -> Settings -> Secrets"

must_haves:
  truths:
    - "angular-typechecker@0.0.2 is published live on npm via the registered Trusted Publisher with NO token in the CI run (tokenless OIDC steady-state proven)"
    - "The published 0.0.2 carries an SLSA v1 provenance attestation and dist-tags.latest points at 0.0.2"
    - "The version bump + tag are cut LOCALLY with an explicit literal version (no conventional-commits bump, no --first-release); the pushed tag fires release.yml"
    - "The public CHANGELOG.md 0.0.2 entry is hand-curated in the 0.0.1 style (ASCII-only, no GSD plan-id scopes, no work email)"
    - "The irreversible publish is performed only after a HUMAN approves the npm-publish environment; the autonomous chain stops at ready-to-approve and never publishes"
    - "release.yml stays OIDC-only (no active NODE_AUTH_TOKEN) and the release-hygiene regression spec stays green"
    - "PKG-05 is recorded in REQUIREMENTS.md and the ROADMAP phase 5.1 entry"
    - "AGENTS.md exists at the repo root and CLAUDE.md's first line is @AGENTS.md (verify only; already shipped)"
  artifacts:
    - path: "packages/angular-typechecker/package.json"
      provides: "version bumped 0.0.1 -> 0.0.2 by nx release"
      contains: "\"version\": \"0.0.2\""
    - path: "CHANGELOG.md"
      provides: "curated 0.0.2 entry in the 0.0.1 style"
      contains: "0.0.2"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PKG-05 requirement recorded"
      contains: "PKG-05"
  key_links:
    - from: "git tag angular-typechecker@0.0.2"
      to: ".github/workflows/release.yml"
      via: "push --follow-tags fires the on:push.tags trigger"
      pattern: "angular-typechecker@\\*"
    - from: "the human operator"
      to: "the npm-publish GitHub Environment"
      via: "required-reviewer approval gates the irreversible nx release publish"
      pattern: "environment: npm-publish"
    - from: "GitHub Actions OIDC id-token"
      to: "npm Trusted Publisher"
      via: "tokenless auth (no NODE_AUTH_TOKEN) + NPM_CONFIG_PROVENANCE"
      pattern: "id-token: write"
---

<objective>
Cut `angular-typechecker@0.0.2` and PROVE the tokenless OIDC steady-state npm publish works end-to-end. 0.0.1 was seed-published with a one-time granular token; the npm Trusted Publisher (OIDC) is REGISTERED but never EXERCISED. 0.0.2 is the first release that authenticates tokenlessly. This phase is operational verification, not new construction -- the release machinery (`release.yml`, `nx.json` release block) is already built and hardened.

Purpose: settle the single empirical question -- does `setup-node`'s `registry-url` write an empty `_authToken` into `.npmrc` that defeats OIDC and yields a misleading 404? With npm >= 11.5.1 + Node 24 (already in `release.yml`) the as-is config SHOULD succeed; the documented reactive remedy is to drop `registry-url` and re-fire (idempotent).

Output: a live, provenance-attested `angular-typechecker@0.0.2` on npm (dist-tags.latest), a GitHub Release, a curated CHANGELOG.md 0.0.2 entry, PKG-05 recorded, and a verified-OIDC CI run carrying no token. The local repo gains the nx-release version commit (package.json 0.0.1 -> 0.0.2), the curated changelog, and the tag.

GATE: The `--chain`/`auto_advance` MUST treat reaching publish-ready (pre-flight + dry-run + local cut + curate + push staged) as the phase-success boundary and STOP at the human approval gate. Do NOT auto-approve the npm-publish environment and do NOT perform any real publish (B-01, D-10). An npm version is immutable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-0-0-2-first-oidc-steady-state-publish-verification/05.1-CONTEXT.md
@.planning/phases/05.1-0-0-2-first-oidc-steady-state-publish-verification/05.1-RESEARCH.md

# The release machinery this phase exercises (read AS-IS; do not redesign):
@.github/workflows/release.yml
@nx.json
@packages/angular-typechecker/package.json
@CHANGELOG.md
@AGENTS.md

# The closest analog runbook (task structure + human-gated checkpoints):
@.planning/phases/05-packaging-publish-hardening-e2e-smoke-mvp/05-05-PLAN.md
@.planning/phases/05-packaging-publish-hardening-e2e-smoke-mvp/05-SECURITY.md

<interfaces>
<!-- Load-bearing facts the executor needs directly (no exploration required). -->

The 0.0.1 git tag already exists (verified): `git tag -l "angular-typechecker@*"` -> `angular-typechecker@0.0.1`.
=> Do NOT pass `--first-release` to nx release (D-01); tags + changelog already exist.

nx.json release block (verified):
- release.projects: ["angular-typechecker"]   (scope is the published package ONLY; fixtures/spike/e2e are private and excluded)
- release.releaseTag.pattern: "angular-typechecker@{version}"   (Nx 23 nested form; matches the release.yml on:push.tags glob 'angular-typechecker@*')
- release.version.conventionalCommits: true   (BUT zero releasable feat/fix commits since the tag -> NO auto bump; pin the literal)
- release.version.preVersionCommand: "npx nx run-many -t build"
- release.changelog.workspaceChangelog.createRelease: "github"   (the GitHub Release is cut from the changelog)

release.yml (verified, reuse AS-IS):
- on: push.tags ['angular-typechecker@*'] + workflow_dispatch
- top-level permissions: contents: read
- job publish: environment: npm-publish (required-reviewer gate = B-01), permissions id-token: write ONLY
- steps: checkout (persist-credentials:false, SHA-pinned v5.0.1) -> setup-node (node 24, registry-url https://registry.npmjs.org/, SHA-pinned v5.0.0) -> npm i -g npm@latest -> npm ci -> npx nx build angular-typechecker -> npx nx release publish (env: NPM_CONFIG_PROVENANCE: true; NO NODE_AUTH_TOKEN)
- the inline comment at the bottom documents the reactive registry-url drop if 0.0.2 404s on auth

package.json (verified): version 0.0.1; repository.url = "git+https://github.com/LayZeeDK/angular-typechecker.git" (LayZeeDK casing, byte-matches the TP); publishConfig { provenance: true, access: "public" } (access:public is load-bearing -- do NOT drop it).

CHANGELOG.md (verified): the curated 0.0.1 entry is the style template. This file is a repo/GitHub-Release artifact, NOT in the npm `files` allowlist.

release-hygiene regression spec (verified present):
  e2e/angular-typechecker-install-e2e/src/release-hygiene.int.spec.ts
  asserts: SHA-pinned actions, persist-credentials:false, contents:read, id-token:write, environment present, provenance on, NODE_AUTH_TOKEN unset (stripCommentLines first), nx.json release scoped to [angular-typechecker], no pull_request_target.
  It does NOT assert registry-url -> a reactive registry-url drop keeps it green.
  Run: `npx nx run angular-typechecker-install-e2e:test`

AGENTS.md + CLAUDE.md (verified, ALREADY DONE -- D-11): AGENTS.md exists at repo root; CLAUDE.md line 1 is exactly `@AGENTS.md` (line 3 begins `<!-- GSD:project-start ...`). VERIFY only; do NOT recreate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight re-confirmation + AGENTS.md/CLAUDE.md verify + dry-run preview + record PKG-05</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .github/workflows/release.yml (confirm OIDC-only: no active NODE_AUTH_TOKEN, node-version 24, npm i -g npm@latest, registry-url present, id-token: write, NPM_CONFIG_PROVENANCE)
    - nx.json (confirm release.projects ["angular-typechecker"], releaseTag.pattern "angular-typechecker@{version}", conventionalCommits true, createRelease github)
    - packages/angular-typechecker/package.json (confirm version 0.0.1, repository.url LayZeeDK casing byte-exact, publishConfig.access public)
    - AGENTS.md (confirm it documents Conventional Commits -> changelog/semver + the 0.x adjustSemverBumpsForZeroMajorVersion shift + the literal-version gotcha)
    - CLAUDE.md (confirm line 1 is exactly @AGENTS.md, outside the GSD HTML-comment block)
    - .planning/REQUIREMENTS.md (the PKG section + the Traceability table to extend)
    - .planning/ROADMAP.md (the Phase 05.1 entry that says Requirements: TBD)
    - .planning/phases/05.1-0-0-2-first-oidc-steady-state-publish-verification/05.1-RESEARCH.md (the D-05 pre-flight table + the dry-run code example)
  </read_first>
  <action>
    Autonomous pre-flight (D-05) + bookkeeping. NO publish, NO tag, NO version cut in this task.
    1. Re-confirm the D-05 invariants by reading the files above. Assert in the SUMMARY: release.yml is OIDC-only (no active NODE_AUTH_TOKEN assignment -- the only matches are explanatory comments), Node 24 + `npm i -g npm@latest` present (=> npm >= 11.5.1), registry-url present (keep it -- D-03), id-token: write present, NPM_CONFIG_PROVENANCE: true present; nx.json release scope = ["angular-typechecker"] and releaseTag.pattern = "angular-typechecker@{version}"; package.json repository.url byte-matches "git+https://github.com/LayZeeDK/angular-typechecker.git" (LayZeeDK casing) and publishConfig.access is "public". If any invariant has DRIFTED, STOP and surface it (do not proceed to the cut).
    2. Verify the seed secret is absent (best-effort): `gh secret list 2>/dev/null | rg -i "NPM_SEED_TOKEN" || echo "NPM_SEED_TOKEN absent (expected)"`. If `gh` is unavailable, note it as a human-confirm item for the approval gate (do not fail the task).
    3. D-11 VERIFY ONLY (do NOT recreate): assert AGENTS.md exists at the repo root and documents the Conventional-Commits -> changelog/semver mechanics incl. the 0.x bump shift; assert CLAUDE.md's first line is exactly `@AGENTS.md` and sits OUTSIDE the GSD HTML-comment block (line 3 begins `<!-- GSD:project-start`). If either is missing/inconsistent, surface it -- but per D-11 they are already shipped, so this is a guard, not a build step.
    4. Run the NON-DESTRUCTIVE dry-run: `npx nx release 0.0.2 --dry-run`. Confirm from the output that the proposed version is 0.0.2, the proposed tag is `angular-typechecker@0.0.2`, the scope is `angular-typechecker` ONLY (no fixture/spike/e2e project appears), and it writes nothing. Do NOT pass `--first-release` (the 0.0.1 tag already exists -- D-01). Record the dry-run summary in the plan SUMMARY.
    5. Record PKG-05 (per the research recommendation + OQ-2 resolution): add `PKG-05` to .planning/REQUIREMENTS.md under the PKG section as "Tokenless OIDC steady-state publish verified on 0.0.2: a real release authenticates via the registered npm Trusted Publisher with NO token, attaches SLSA v1 provenance, sets dist-tags.latest, and cuts a GitHub Release." Add a Traceability row `PKG-05 | Phase 5.1 | Pending` and update the coverage counts/per-phase counts to include it. In .planning/ROADMAP.md, replace the Phase 05.1 `**Requirements**: TBD` with `**Requirements**: PKG-05` (in both the Phase Details block and any placeholder that lists TBD). ASCII-only; do not touch unrelated requirement rows.
    6. Stage ONLY the two edited planning files by name (git rules: never `git add .`): `git add .planning/REQUIREMENTS.md .planning/ROADMAP.md`. Do NOT commit here -- the nx release version commit in Task 2 + the changelog curation will be committed together; record PKG-05 as a separate small commit `docs(release): record PKG-05 OIDC steady-state verification requirement` (stage by name, no AI attribution).
  </action>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && npx nx release 0.0.2 --dry-run 2>&1 | rg -i "0\.0\.2|angular-typechecker@0\.0\.2"</automated>
  </verify>
  <acceptance_criteria>
    - `npx nx release 0.0.2 --dry-run` output shows version 0.0.2 + tag angular-typechecker@0.0.2, scope angular-typechecker only, and writes nothing (no file changes, no git ops, no publish).
    - release.yml confirmed OIDC-only (no active NODE_AUTH_TOKEN assignment), Node 24 + npm@latest + registry-url + id-token:write + NPM_CONFIG_PROVENANCE all present.
    - package.json repository.url == "git+https://github.com/LayZeeDK/angular-typechecker.git" (LayZeeDK casing) AND publishConfig.access == "public".
    - AGENTS.md exists at repo root; CLAUDE.md first line === "@AGENTS.md" (outside the GSD comment block). Neither recreated.
    - .planning/REQUIREMENTS.md contains a PKG-05 entry + a Traceability row; .planning/ROADMAP.md Phase 05.1 entry shows `Requirements: PKG-05` (no longer TBD).
    - Edited planning files staged by explicit name only.
  </acceptance_criteria>
  <done>Pre-flight invariants re-confirmed (or drift surfaced and STOPPED); AGENTS.md/CLAUDE.md verified present + consistent; dry-run previews 0.0.2 + the correct tag scope writing nothing; PKG-05 recorded in REQUIREMENTS.md + ROADMAP and staged by name.</done>
</task>

<task type="auto">
  <name>Task 2: Cut 0.0.2 locally (--skip-publish) + hand-curate CHANGELOG.md + stage for push</name>
  <files>packages/angular-typechecker/package.json, CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (the curated 0.0.1 entry -- the exact style template: heading, intro sentence, sections, the trailing tag link)
    - AGENTS.md (gotcha #1 literal version; gotcha #2 curated changelog, no internal scopes; gotcha #3 do not hand-push tags outside nx release flow)
    - packages/angular-typechecker/package.json (the version field nx release will bump 0.0.1 -> 0.0.2)
    - nx.json (release block -- confirm createRelease github so the Release notes derive from the curated CHANGELOG)
    - .planning/phases/05.1-0-0-2-first-oidc-steady-state-publish-verification/05.1-RESEARCH.md (Pitfall 4 changelog drift; the "Cut + curate + push" code example; OQ-1 payload default = no functional change)
  </read_first>
  <action>
    Cut the version + tag LOCALLY and curate the public changelog. This task PREPARES the push; it does NOT push and does NOT publish.
    1. OQ-1 / D-07 payload: 0.0.2 is a pure verification release with NO functional source change (the recommended default). Do NOT add a README/docs touch unless the maintainer directs it at the Task 3 approval gate. (The payload is FLAGGED for human confirmation at the gate -- Task 3 -- not silently locked here. If the maintainer later wants a real change, that is decided at the gate before approving the publish.)
    2. Run `npx nx release 0.0.2 --skip-publish` (explicit LITERAL version per D-01; NOT a `patch` keyword, NOT `--first-release`). This creates the version commit (package.json 0.0.1 -> 0.0.2), the auto-generated changelog section, and the local tag `angular-typechecker@0.0.2`. Do NOT push (CI publishes -- D-02 the --skip-publish split).
    3. Hand-curate CHANGELOG.md (D-06, Pitfall 4): replace the auto-generated 0.0.2 section (it will be empty or leak `docs(...)`-style scopes / mis-parsed `[#N]` refs) with a clean 0.0.2 entry in the 0.0.1 style. Content for a verification release: a `## 0.0.2 (<release date>)` heading, a one/two-line note that 0.0.2 is a maintenance release verifying the tokenless OIDC steady-state publish path with no functional change to the executor, a Compatibility line identical to 0.0.1 (Nx 23 / Angular 22 ^22.0.0 / TS >=6.0.0 <6.1.0 / Node range), and a trailing `[0.0.2]: https://github.com/LayZeeDK/angular-typechecker/releases/tag/angular-typechecker@0.0.2` link. ASCII-only (no em-dashes -- use `--`; no curly quotes/ellipsis/emoji). NO GSD plan-id scopes, NO `[#N]` refs, NO work email (`consensus.dk`) -- public-only `larsbrinknielsen@gmail.com` if any contact is needed. Do NOT leak any private prior-art (Connect) specifics.
    4. Stage CHANGELOG.md by NAME and fold the curation onto the version commit so the tag stays on the right commit: `git add CHANGELOG.md` then `git commit --amend --no-edit` (the nx-release version commit is `HEAD`; amending keeps the `angular-typechecker@0.0.2` tag pointing at it). If the tag did not auto-attach to the amended commit, re-point it: `git tag -f angular-typechecker@0.0.2`. No AI attribution in the commit. Do NOT push in this task -- the push is the first action of Task 3 AFTER the human confirms payload + changelog at the gate.
    5. Re-run the regression gate to confirm nothing release-related regressed: `npx nx run angular-typechecker-install-e2e:test` (or the targeted `-- release-hygiene`). It must stay green (release.yml untouched in this task, so it will).
  </action>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && rg -n "\"version\": \"0\.0\.2\"" packages/angular-typechecker/package.json && rg -n "## 0\.0\.2" CHANGELOG.md && git tag -l "angular-typechecker@0.0.2"</automated>
  </verify>
  <acceptance_criteria>
    - packages/angular-typechecker/package.json version === "0.0.2".
    - git tag `angular-typechecker@0.0.2` exists locally and points at the HEAD commit that carries the curated CHANGELOG.md.
    - CHANGELOG.md has a `## 0.0.2` entry in the 0.0.1 style: ASCII-only, no GSD plan-id scopes (no `docs(...)`/`(05.1-...)`), no `[#N]` refs, no `consensus.dk`, includes the trailing `[0.0.2]:` release-tag link.
    - No `git add .`/`-A`/`-u` used; CHANGELOG.md staged by name; commit carries no AI attribution.
    - `npx nx run angular-typechecker-install-e2e:test` stays green (release-hygiene OIDC-only invariant intact).
    - NOTHING pushed and NOTHING published in this task.
  </acceptance_criteria>
  <done>0.0.2 is cut locally (version commit + curated changelog + tag on the right commit), the regression gate is green, and the repo is staged ready-to-push -- stopping before the push, which Task 3 performs only after the human gate.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Checkpoint: Confirm payload + changelog, push the tag, approve the npm-publish environment (B-01, irreversible)</name>
  <action>BLOCKING human-action checkpoint (B-01, D-10 -- the ONLY irreversible step). Under the chain this is the phase-success boundary: STOP here. The agent prepares and explains; the HUMAN confirms OQ-1, performs the push (or directs it), and approves the npm-publish environment. The agent NEVER approves the environment and NEVER performs the publish. An npm version is immutable and cannot be unpublished/republished.</action>
  <what-built>
    Everything is staged ready-to-publish: 0.0.2 is cut locally (version commit + curated CHANGELOG + tag `angular-typechecker@0.0.2`), pre-flight invariants are green, the regression gate is green, and PKG-05 is recorded. The remaining steps are the push (fires release.yml) and the irreversible human-approved OIDC publish.
  </what-built>
  <how-to-verify>
    Perform IN ORDER (the agent may run the push on explicit instruction, but MUST NOT approve the environment):
    1. CONFIRM OQ-1 (payload): the maintainer reviews the curated CHANGELOG.md 0.0.2 entry and confirms 0.0.2 ships with NO functional change (the verification-release default, D-07), OR directs a specific small change to fold in BEFORE the push. Do not assume -- this is the explicit confirmation point flagged in CONTEXT.md (OQ-1).
    2. RE-CONFIRM the Trusted Publisher is still registered + exact-match (A3, npm-account-side, not repo-verifiable): provider GitHub Actions, repo `LayZeeDK/angular-typechecker`, workflow `release.yml`, environment `npm-publish`, `npm publish` action ticked; and the `NPM_SEED_TOKEN` GitHub secret is still DELETED. If drifted, fix on npmjs.com/GitHub before pushing.
    3. PUSH (fires release.yml on the `angular-typechecker@0.0.2` tag): `git push --follow-tags`. Alternatively use `workflow_dispatch` (also wired in release.yml) -- maintainer's discretion.
    4. APPROVE THE GATE (HUMAN ONLY): when release.yml reaches the `npm-publish` environment, the maintainer opens GitHub -> Actions -> the release run -> Review deployments -> Approve. The job then runs checkout -> setup-node (node 24 + registry-url) -> npm i -g npm@latest -> npm ci -> npx nx build angular-typechecker -> npx nx release publish (OIDC, NPM_CONFIG_PROVENANCE: true, NO token). The agent does NOT click approve.
    5. REACTIVE FALLBACK ONLY (D-04) -- if the publish 404s on auth (OIDC never engaged; the empty-`_authToken` trap): edit `.github/workflows/release.yml` to DROP the `registry-url:` line from the `actions/setup-node` step (keep everything else, incl. SHA pins, persist-credentials:false, no token). Commit by name `git add .github/workflows/release.yml` + `git commit -m "ci: drop registry-url to fix OIDC empty-_authToken 404"` + `git push`. Then re-fire the SAME existing tag's workflow (`gh run rerun <id>` or `workflow_dispatch`) -- do NOT re-run `nx release` locally (the tag already exists; nx release publish is idempotent -- it `npm view`s the version first). Re-run `npx nx run angular-typechecker-install-e2e:test` to confirm release-hygiene stays green (it does not assert registry-url). Do this ONLY on a 404-on-auth; do NOT proactively drop registry-url (verifying the as-is config is the point of this phase).
  </how-to-verify>
  <read_first>
    - .github/workflows/release.yml (the publish job + the inline registry-url drop note at the bottom)
    - .planning/phases/05.1-0-0-2-first-oidc-steady-state-publish-verification/05.1-RESEARCH.md (the OIDC/registry-url mechanics; the system-architecture diagram; Pitfall 1 + Pitfall 3; the reactive remediation code example)
    - .planning/phases/05.1-0-0-2-first-oidc-steady-state-publish-verification/05.1-CONTEXT.md (OQ-1; D-03/D-04/D-08/D-10)
  </read_first>
  <acceptance_criteria>
    - The maintainer has explicitly confirmed the OQ-1 payload (no functional change, or a directed change folded in) BEFORE the push.
    - The tag `angular-typechecker@0.0.2` is pushed and release.yml is triggered.
    - The npm-publish environment is approved by a HUMAN (never the agent); the publish job ran via OIDC with no NODE_AUTH_TOKEN.
    - If a 404-on-auth occurred: registry-url was dropped reactively, committed by name, pushed, the workflow re-fired, and release-hygiene stayed green. Otherwise release.yml is UNCHANGED.
  </acceptance_criteria>
  <resume-signal>Type "published" once 0.0.2 is live on npm (the human approved the environment and the publish job succeeded), or "404 -- applied registry-url drop, re-fired" if the reactive fallback was used, or describe what stopped it (the phase STOPS here under the chain -- this is the success boundary).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Post-publish OIDC + provenance evidence (D-08 / D-09)</name>
  <action>BLOCKING human-verify checkpoint. After the human-approved publish, verify the D-08 success evidence and the D-09 regression guard, then record it in the plan SUMMARY. The agent may RUN the read-only verification commands; the human confirms the live registry/GitHub state.</action>
  <what-built>
    Post-publish evidence that the tokenless OIDC steady-state publish (PKG-05) actually happened: version + SLSA provenance + dist-tags.latest + GitHub Release, published via OIDC with no token; plus the regression guard that release.yml stayed OIDC-only.
  </what-built>
  <how-to-verify>
    1. D-08 version + provenance: `npm view angular-typechecker@0.0.2 --json` and confirm `.version == "0.0.2"` AND `.dist.attestations.provenance.predicateType == "https://slsa.dev/provenance/v1"` (SLSA v1 provenance present).
    2. D-08 dist-tag: `npm view angular-typechecker dist-tags --json` and confirm `.latest == "0.0.2"`.
    3. D-08 published-via-OIDC + GitHub Release: `gh run list --workflow release.yml --limit 3` (confirm the publishing run carried NO seed token / NODE_AUTH_TOKEN -- the run log shows OIDC auth, not a token), and `gh release view "angular-typechecker@0.0.2"` (confirm the Release exists with the curated notes derived from the curated CHANGELOG). If `gh` is unavailable, confirm via the npmjs.com package page (provenance badge) + the GitHub Releases UI.
    4. D-09 regression guard: confirm `.github/workflows/release.yml` is still OIDC-only (no active NODE_AUTH_TOKEN; if the D-04 drop was applied, registry-url is gone but no token was added) and `NPM_SEED_TOKEN` remains a deleted GitHub secret; run `npx nx run angular-typechecker-install-e2e:test` and confirm the release-hygiene spec is green.
    5. Record all evidence (the npm view JSON fields, the dist-tag, the run id, the Release URL, the green regression gate, and whether the D-04 fallback was needed) in 05.1-01-SUMMARY.md. Mark PKG-05 acceptance satisfied.
  </how-to-verify>
  <read_first>
    - .planning/phases/05.1-0-0-2-first-oidc-steady-state-publish-verification/05.1-CONTEXT.md (D-08 / D-09 success + regression criteria)
    - .planning/phases/05-packaging-publish-hardening-e2e-smoke-mvp/05-05-SUMMARY.md (the 0.0.1 npm view verification pattern -- the same provenance field shape)
    - e2e/angular-typechecker-install-e2e/src/release-hygiene.int.spec.ts (the OIDC-only assertions that must stay green)
  </read_first>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && npx nx run angular-typechecker-install-e2e:test 2>&1 | rg -i "pass|release-hygiene|fail"</automated>
  </verify>
  <acceptance_criteria>
    - `npm view angular-typechecker@0.0.2 --json` -> .version == "0.0.2" AND .dist.attestations.provenance.predicateType == "https://slsa.dev/provenance/v1".
    - `npm view angular-typechecker dist-tags --json` -> .latest == "0.0.2".
    - The publishing CI run used OIDC (no NODE_AUTH_TOKEN) and a GitHub Release `angular-typechecker@0.0.2` exists with curated notes.
    - release.yml stays OIDC-only; NPM_SEED_TOKEN secret still deleted; `npx nx run angular-typechecker-install-e2e:test` (release-hygiene) green.
    - Evidence recorded in 05.1-01-SUMMARY.md; PKG-05 acceptance (D-08) satisfied.
  </acceptance_criteria>
  <resume-signal>Type "verified" once 0.0.2 shows version + SLSA provenance + dist-tags.latest, the publishing run used OIDC (no token), the GitHub Release exists, and release-hygiene is green; or describe a discrepancy (e.g. no provenance attestation, dist-tag not advanced).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Reused from 05-SECURITY.md (25/25 closed). The supply-chain surface (release.yml, the npm publish) is UNCHANGED; the only delta is OIDC auth replacing the one-time seed token -- which REMOVES the leaked-token surface. No new boundary is introduced (a possible reactive registry-url drop edits one non-asserted line in release.yml).

| Boundary | Description |
|----------|-------------|
| GitHub OIDC id-token -> npm Trusted Publisher | The TP must trust the EXACT workflow filename (release.yml) + environment (npm-publish), not the whole repo (TanStack vector). Tokenless; no standing secret. |
| git tag push -> privileged release run | Trigger is tag-push (+ workflow_dispatch) only; NO pull_request_target (s1ngularity vector). |
| published version immutability | An npm version is irreversible (immutable, name claimed) -- hence human-gated (B-01). |
| curated public artifact -> npm/GitHub | CHANGELOG/Release notes must not leak internal GSD scopes, the work email, or private prior-art. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-01 | Information Disclosure | re-introduced standing npm token | mitigate | release.yml stays OIDC-only (no NODE_AUTH_TOKEN; an empty value breaks OIDC and a real one is unnecessary); NPM_SEED_TOKEN secret stays deleted; release-hygiene.int.spec.ts asserts NODE_AUTH_TOKEN unset (stripCommentLines first). Verified Task 1 (pre-flight) + Task 4 (post-publish D-09). |
| T-05.1-02 | Spoofing / Tampering | Trusted Publisher trust scope | mitigate | TP bound to exact repo + workflow filename (release.yml) + environment (npm-publish), not whole-repo. Re-confirmed at the human gate (Task 3 step 2; A3 -- npm-account-side, operator-attested). |
| T-05.1-03 | Elevation of Privilege | unauthorized / accidental publish | mitigate | The npm-publish required-reviewer environment gates the only irreversible action (B-01, D-10); the agent never approves. Asserted by release-hygiene (environment present) + human approval (Task 3). |
| T-05.1-04 | Tampering | untrusted-PR code in a privileged run | mitigate | Tag-push + workflow_dispatch only; no pull_request_target. Asserted by release-hygiene; unchanged this phase. |
| T-05.1-05 | Tampering | mutable action tag repointed to malware | mitigate | Every `uses:` SHA-pinned (40-char); Dependabot (github-actions) freshens. Asserted by release-hygiene; unchanged this phase. |
| T-05.1-06 | Repudiation | unverifiable build origin | mitigate | NPM_CONFIG_PROVENANCE: true + id-token: write -> SLSA v1 attestation, independent of auth mode. Verified live via `npm view ... .dist.attestations.provenance.predicateType` (Task 4, D-08). |
| T-05.1-07 | Information Disclosure | secrets/PII leak in the public changelog/Release | mitigate | Hand-curated CHANGELOG/Release notes: ASCII-only, no GSD plan-id scopes, no `consensus.dk` work email, no private prior-art (Connect). Enforced in Task 2 + reviewed at the human gate (Task 3 step 1). |
| T-05.1-08 | Tampering | reactive registry-url drop weakening the workflow | mitigate | If applied (D-04), the drop removes ONLY the registry-url line (not asserted by release-hygiene); SHA pins, persist-credentials:false, contents:read, id-token:write, no-token all stay. Re-run release-hygiene after the edit to confirm no invariant regressed (Task 3 step 5 + Task 4 D-09). |
| T-05.1-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable; the published dependency set is unchanged from 0.0.1 -- no commit since the tag touches the package). The only `npm i -g npm@latest` is in-CI workflow text, not a plan install. No [ASSUMED]/[SUS] packages -> no blocking legitimacy checkpoint required. |
</threat_model>

<verification>
- Automated (per-task + per-wave): `npx nx run angular-typechecker-install-e2e:test` (release-hygiene OIDC-only invariant) stays green after Task 1 (pre-flight, no edits to release.yml), Task 2 (local cut, no release.yml edit), and Task 4 (post-publish; also after any D-04 reactive drop). The non-destructive `npx nx release 0.0.2 --dry-run` (Task 1) previews 0.0.2 + the tag scope and writes nothing.
- Manual-only (B-01, D-08 -- inherently one-shot, NOT unit-testable): the live tokenless OIDC publish of 0.0.2 and its `npm view` evidence (version + SLSA v1 provenance + dist-tags.latest), the OIDC-no-token CI run, and the GitHub Release. The human approves the npm-publish environment; the agent never publishes.
- There is NO automated chain gate that performs the publish -- reaching publish-ready (Task 1 + Task 2 green + staged for push) is the phase-success boundary; the publish is a human-triggered release event.
</verification>

<success_criteria>
- `angular-typechecker@0.0.2` is live on npm, published via the registered Trusted Publisher with NO token in the CI run (tokenless OIDC steady-state PROVEN), carrying an SLSA v1 provenance attestation, with `dist-tags.latest == 0.0.2` and a GitHub Release `angular-typechecker@0.0.2` cut from the curated CHANGELOG (PKG-05 / D-08).
- The version + tag were cut LOCALLY with an explicit literal version (no conventional-commits bump, no `--first-release`); the pushed tag fired release.yml; the curated public CHANGELOG.md 0.0.2 entry is ASCII-only with no GSD scopes / no work email / no private prior-art (D-01/D-02/D-06).
- The irreversible publish was performed only after a HUMAN approved the npm-publish environment; the autonomous chain stopped at ready-to-approve (B-01/D-10).
- release.yml stayed OIDC-only (no active NODE_AUTH_TOKEN), NPM_SEED_TOKEN stayed deleted, and the release-hygiene regression spec stayed green (D-09); if a 404-on-auth occurred, the registry-url drop (D-04) resolved it and kept the gate green.
- PKG-05 is recorded in REQUIREMENTS.md + ROADMAP; AGENTS.md + CLAUDE.md (@AGENTS.md first line) verified present and consistent (D-11, verify-only).
</success_criteria>

<output>
Create `.planning/phases/05.1-0-0-2-first-oidc-steady-state-publish-verification/05.1-01-SUMMARY.md` after the human-gated publish. Record: the dry-run preview summary; the local cut (version commit + curated changelog + tag); the live publish evidence (`npm view` version + SLSA provenance predicateType + dist-tags.latest), the publishing run id (OIDC, no token), the GitHub Release URL; whether the D-04 registry-url reactive drop was needed (and that release-hygiene stayed green if so); the PKG-05 recording; and the AGENTS.md/CLAUDE.md verify result. Note that the Nx community-registry-listing PR is a separate follow-up, not part of this phase.
</output>
