// A plain .ts file physically inside .storybook/, matched by the leaf's "*.ts"
// include glob. Present to prove local .storybook TS files also materialize as
// declared rootNames alongside the cross-project (../../mylib) stories.
export const framework = '@storybook/angular';
