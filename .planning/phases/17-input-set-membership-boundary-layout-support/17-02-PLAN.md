---
phase: 17-input-set-membership-boundary-layout-support
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/core/walk-references.ts
  - packages/angular-typechecker/src/core/walk-references.spec.ts
autonomous: true
requirements: [SB-02]

must_haves:
  truths:
    - "The walk surfaces the DECLARED readConfiguration().rootNames PATHS of every SURVIVING leaf as a union (not just the count)"
    - "A skipped / out-of-project / zero-root-names / not-found leaf contributes ZERO paths to the union"
    - "The surfaced paths are the DECLARED set (readConfiguration().rootNames) -- never program.getRootFileNames() (no .ngtypecheck.ts shims)"
  artifacts:
    - path: "packages/angular-typechecker/src/core/walk-references.ts"
      provides: "WalkResult.rootNamePaths accumulated from each surviving leaf's parsed.rootNames"
      contains: "rootNamePaths"
    - path: "packages/angular-typechecker/src/core/walk-references.spec.ts"
      provides: "unit proof that rootNamePaths accumulates surviving leaves only"
  key_links:
    - from: "surviving-leaf branch (parsed.rootNames.length > 0)"
      to: "WalkResult.rootNamePaths"
      via: "rootNamePaths.push(...parsed.rootNames)"
      pattern: "rootNamePaths"
---

<objective>
Surface each walked leaf's DECLARED rootName paths on `WalkResult` so `run-typecheck.ts`
(plan 17-03) can build the `inputTs` union that the input-set-membership boundary filter
needs (D-02). Today `walkReferences` accumulates only `rootNamesCount` and discards the
paths.

Purpose: the Layout-B centralized host aggregates cross-project `*.stories.ts` /
`*.component.ts` OUTSIDE the host dir; those files materialize as the storybook leaf's
`readConfiguration().rootNames`. Surfacing the PATHS (not the count) is the single piece of
data that lets the boundary keep aggregated files while still isolating dependencies.
Output: a one-field additive change to `WalkResult` + accumulation for surviving leaves + unit proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

READ-FIRST source-of-truth:
@.planning/phases/17-input-set-membership-boundary-layout-support/17-CONTEXT.md
@.planning/phases/17-input-set-membership-boundary-layout-support/17-RESEARCH.md
@.claude/skills/spike-findings-angular-typechecker/references/storybook-input-set-boundary.md

The file being modified + its unit spec:
@packages/angular-typechecker/src/core/walk-references.ts
@packages/angular-typechecker/src/core/walk-references.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: accumulate declared rootName paths on WalkResult</name>
  <files>packages/angular-typechecker/src/core/walk-references.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/walk-references.ts (the surviving-leaf tail already has `rootNamesCount += parsed.rootNames.length;` -- add the path accumulation there; the skip/not-found/zero-root-names branches `continue` BEFORE the tail so they contribute nothing)
    - 17-RESEARCH.md "Pattern 3: Surface declared rootName paths from the walk"
    - 17-CONTEXT.md D-02 (DECLARED readConfiguration().rootNames, NOT program.getRootFileNames() -- the .ngtypecheck.ts shim landmine)
  </read_first>
  <action>
    Add `rootNamePaths: readonly string[]` to the `WalkResult` interface (documented as the union of surviving leaves' DECLARED `readConfiguration().rootNames`, D-02). Declare `const rootNamePaths: string[] = [];` alongside the existing `rawDiagnostics`/`skippedReferences` accumulators, and in the SURVIVING-leaf tail (immediately alongside `rootNamesCount += parsed.rootNames.length;`) push `rootNamePaths.push(...parsed.rootNames);`. Include `rootNamePaths` in the returned `WalkResult`. Do NOT read rootNames off any Program (the `.ngtypecheck.ts` shim landmine, D-02); use the leaf's `parsed.rootNames` which the loop already holds. The skip / out-of-project / duplicate / self-reference / not-found / zero-root-names branches all `continue` before the tail, so they contribute nothing -- do not add accumulation to them.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- walk-references</automated>
  </verify>
  <acceptance_criteria>
    - `WalkResult` has `rootNamePaths: readonly string[]`.
    - The push occurs ONLY in the surviving-leaf tail (after all `continue` guards).
    - No Program-derived rootNames are used (grep shows no `getRootFileNames`/`getTsProgram().getRootFileNames` added here).
    - `npx nx test angular-typechecker -- walk-references` passes.
  </acceptance_criteria>
  <done>WalkResult carries rootNamePaths = the union of surviving leaves' declared rootNames; skipped leaves contribute none.</done>
</task>

<task type="auto">
  <name>Task 2: unit proof of rootNamePaths accumulation</name>
  <files>packages/angular-typechecker/src/core/walk-references.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/walk-references.spec.ts (the hand-built `ng` stub: `readConfiguration` returns `{ rootNames, options, errors, projectReferences }`; the `leaf()` helper resolves ref paths cross-platform; `ts.sys` forced case-sensitive identity-realpath)
  </read_first>
  <action>
    Extend `walk-references.spec.ts` with assertions on `result.rootNamePaths`: a two-surviving-leaf solution unions BOTH leaves' declared `rootNames` (drive the `ng` stub `readConfiguration` to return distinct `rootNames` arrays per leaf) and `rootNamePaths` contains exactly those paths; a solution mixing a surviving leaf with a zero-root-names leaf and an out-of-project reference surfaces ONLY the surviving leaf's rootNames (the skipped ones contribute none). Reuse the existing stub/`leaf()` idiom -- no cold compiler.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- walk-references</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts the union of two surviving leaves' rootNames appears in `rootNamePaths`.
    - A test asserts a skipped/zero-root-names/out-of-project leaf contributes NO paths.
    - `npx nx test angular-typechecker -- walk-references` passes.
  </acceptance_criteria>
  <done>The rootNamePaths accumulation is proven surviving-leaves-only against the existing stub, no compiler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solution tsconfig references[] -> resolved leaf configs | consumer-declared reference paths resolve to leaf tsconfigs whose declared rootNames enter the input set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Tampering (of coverage) | reading rootNames off a Program instead of the declared config | mitigate | surface `parsed.rootNames` (the DECLARED set) ONLY; never `program.getRootFileNames()` -- avoids the synthetic `.ngtypecheck.ts` shim corrupting the input set (D-02 landmine); acceptance grep guards it |
| T-17-06 | Spoofing (out-of-project leaf inflating the input set) | a skipped/out-of-project leaf contributing paths | mitigate | accumulation is in the surviving-leaf tail AFTER all boundary/skip `continue` guards, so an out-of-project or zero-root-names leaf contributes nothing; unit test proves it |
| T-17-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | N/A -- no package installs this phase |
</threat_model>

<verification>
- `npx nx test angular-typechecker -- walk-references` green.
- Per wave-1 merge: full suite + build + lint + format:check (CI gates).
</verification>

<success_criteria>
- WalkResult.rootNamePaths surfaces the declared surviving-leaf rootName union.
- Skipped/zero-root-names/out-of-project leaves contribute zero paths (proven by unit test).
</success_criteria>

<output>
Create `.planning/phases/17-input-set-membership-boundary-layout-support/17-02-SUMMARY.md` when done.
</output>
