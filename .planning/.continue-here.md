---
context: default
phase: none (milestone boundary)
task: 0
total_tasks: 2
status: paused
last_updated: 2026-07-20T23:16:01.276Z
---

# Resume: complete v0.2.3, then start v0.2.4 (Enhanced SARIF reporting for GitHub Code Scanning)

Paused at a **milestone boundary**. v0.2.3 shipped; v0.2.4 is fully scoped (empirically) but not started.

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured state for `/gsd:resume-work`.
2. Memory `code-scanning-sarif-empirical-behavior.md` — **the spike findings** (authoritative on what SARIF changes are worth building).
3. Memory `ci-sarif-code-scanning-dogfood.md` — the shipped PR #49 CI wiring.
4. Memory `no-github-issues-on-my-behalf.md` — never `gh issue create`.
5. `AGENTS.md` — release mechanics + `main` is PR-only.

## Next actions (in order)

1. **`/gsd:complete-milestone v0.2.3`** — archive v0.2.3: `git mv` phases `30`/`31`/`32` → `.planning/milestones/v0.2.3-phases/`, add a `MILESTONES.md` entry, produce the milestone audit. **Must run before `new-milestone`'s `phases.clear`, or those phase dirs get wiped.**
2. **`/gsd:new-milestone v0.2.4`** ("Enhanced SARIF reporting for GitHub Code Scanning") — write PROJECT.md + REQUIREMENTS.md + ROADMAP.md on a **kickoff branch off `main`** (PR-only) → PR. **Skip the 4-researcher round** — the spike + earlier SARIF research already answered the unknowns.

<current_state>
On `main`, clean tree, `head ac760aa`, 0 unpushed. PR #49 (CI dogfood of angular-typechecker + fallow SARIF → Code Scanning) is merged and real-CI-verified. The v0.2.4 scoping spike (throwaway PR #53) is closed and its branch/diagnostics deleted. No `.planning/` milestone artifacts for v0.2.4 written yet.
</current_state>

<completed_work>
- **PR #49 merged** (`be2a730`): CI `code-scanning` job uploads angular-typechecker (on `ng-spike-app`) + fallow SARIF to GitHub Code Scanning. Verified in real CI — both analyses land on the PR ref AND the `main` baseline. `upload-sarif` pinned `v4.37.1`. fallow's multi-run SARIF fixed via per-run `automationDetails.id` (GitHub rejects multi-run-same-category). Job kept OUT of the required `ci` aggregate.
- **Empirical spike (PR #53, closed + deleted)**: deliberate diagnostics (TS2322 in .ts, TS2551 + NG8107 in template) → real Code Scanning alerts. Settled every open v0.2.4 question (see Decisions).
</completed_work>

<remaining_work>
### v0.2.4 scope (5 areas — evidence-locked by the spike)

| # | Area | Type |
|---|------|------|
| 1 | **Per-project SARIF categories** — one run per workspace project using the executor (`ng-spike-app`, `typecheck-consumer`, `typecheck-consumer-dep`, `typecheck-walk-consumer`; auto-discovered), merged into one file, `automationDetails.id = angular-typecheck/<project>`, single upload, no `category` input. Mechanism proven by fallow's multi-run fix. | CI-only |
| 2 | **Rule cataloging + family tags** — catalog ALL diagnostic families (TS / template-type-check / NG8xxx / ATC900x), each rule with `properties.tags` (family), `defaultConfiguration.level`, and `help.text`/`help.markdown` (the alert "rule help" panel needs `help`, not `helpUri`). Today only the 18 NG8xxx are cataloged with no tags. | **SARIF change → v0.2.4 release** |
| 3 | **"Scanned files" panel** — DOCUMENT in-repo as a known GitHub limitation (CodeQL-only telemetry). Do NOT emit `run.artifacts` (proven inert). Do NOT file a GitHub Issue. | Docs only |
| 4 | **Gating** — add `code-scanning` to `ci.needs` + enable GitHub "Require code scanning results" ruleset for angular-typechecker + fallow, WITH planning-only-PR deadlock mitigation. Findings already gate via `test`/`fallow`. | CI + ruleset |
| 5 | **Automated proof** — fixture-driven Actions + `gh` CLI check: deliberate per-family diagnostics in an ISOLATED fixture (outside the normal typecheck gate) → assert via `gh api` that alerts land with the expected category, tags, and severity. Relies on `upload-sarif` `wait-for-processing` + `security-events: read`. | CI-only |

**Release rule:** only area 2 (SARIF change) makes v0.2.4 a release; 1/3/4/5 are CI/ruleset/docs.
</remaining_work>

<decisions_made>
- **v0.2.4 = "Enhanced SARIF reporting for GitHub Code Scanning."**
- Release only if the SARIF report changes → area 2 is the release trigger; the rest ships without a release.
- **"Scanned files" is a GitHub product gap** — proven: emitting `run.artifacts` (7 analysisTarget files) did NOT populate the panel; CodeQL's works because it's CodeQL-internal per-language telemetry. → document, don't chase, don't emit run.artifacts.
- **Rule tags/catalog/help WORK** in Code Scanning (spike-verified: `tag:extended-diagnostics`/`tag:typescript` filters work; TS codes gained descriptions; NG8107 shows its tag).
- **No GitHub Issues on the user's behalf** — document limitations in-repo instead.
- Per-project = category axis (`automationDetails.id`); diagnostic-family = rule-tag axis (ecosystem convention, ecosystem research + spike confirmed).
</decisions_made>

<blockers>
- **`main` is PR-only** (empty-bypass ruleset): kickoff artifacts must land on a branch → PR, never a direct push.
- **v0.2.3 phases 30/31/32 are un-archived** in `.planning/phases/`: `new-milestone`'s `phases.clear` would wipe them — run `complete-milestone v0.2.3` first (or manually `git mv` to `.planning/milestones/v0.2.3-phases/`).
- `init.new-milestone` reports `current_milestone: v0.2.3` (correct); create the v0.2.4 branch with the right name yourself (don't trust GSD's auto branch-name derivation).
</blockers>

## Critical Anti-Patterns (do NOT repeat)

- [ANTI-PATTERN] Emitting `run.artifacts` to populate "Scanned files" → it's inert for third-party tools (GitHub gap). Don't add it.
- [ANTI-PATTERN] Uploading a multi-run SARIF under one `category` → GitHub (2025-07-21+) REJECTS it. Use a distinct `automationDetails.id` per run and no `category` input.
- [ANTI-PATTERN] Filing a GitHub Issue for the Scanned-files limitation → the user does not want issues filed on their behalf; document in-repo.
- [ANTI-PATTERN] Running `new-milestone` before archiving v0.2.3 phases → `phases.clear` wipes them.

## Infrastructure State
- Code Scanning: CodeQL default setup enabled; angular-typechecker + fallow registered as third-party tools ("All tools working as expected"). PR #49 wiring live on `main`.
- Local repo: clean `main` at `ac760aa`; a WIP handoff commit is added on top locally (not pushed — `main` is PR-only).

<context>
This session dogfooded angular-typechecker's SARIF into GitHub Code Scanning (PR #49, verified in real CI incl. the fallow multi-run fix + upload-sarif v4), then ran an empirical spike to scope v0.2.4. The spike is the authority: it proved rule tags/catalog/help work in Code Scanning (→ the one release-bearing SARIF change) and proved the "Scanned files" panel is an unfixable GitHub gap. v0.2.4 is fully scoped; the only remaining work is the GSD milestone ceremony (complete v0.2.3, then define v0.2.4) plus the implementation phases the roadmap will produce.
</context>

<next_action>
Start with: `/gsd:complete-milestone v0.2.3` (archive phases 30-32 + MILESTONES.md + audit), then `/gsd:new-milestone v0.2.4` with the 5-area scope above and the spike findings. Do both on branches → PRs (main is PR-only).
</next_action>
