---
phase: 15-generator-e2e-ci-self-audit-guard
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/package.json
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/nx.json
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/project.json
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/tsconfig.json
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/tsconfig.lib.json
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/tsconfig.spec.json
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/src/consumer-generator.component.ts
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/src/consumer-generator.component.spec.ts
  - e2e/angular-typechecker-install-e2e/src/generator-e2e.int.spec.ts
  - e2e/angular-typechecker-install-e2e/src/nx-add-e2e.int.spec.ts
  - e2e/angular-typechecker-install-e2e/src/tarball-audit.int.spec.ts
autonomous: true
requirements: [GE2E-01, GE2E-02, GE2E-03]
tags: [e2e, tarball, generator, nx-add, main-tree]

must_haves:
  truths:
    - "Installing the packed tarball + `nx g angular-typechecker:configuration <proj>` on an un-wired project yields exactly ONE `typecheck` target (executor `angular-typechecker:typecheck`) pointed at the solution `tsconfig.json`"
    - "The init-seeded `nx.json` `targetDefaults[angular-typechecker:typecheck]` has `default` (never `production`) as its first input, `cache:true`, `outputs:[]`"
    - "`nx run <proj>:typecheck --skip-nx-cache` is GREEN (exit 0) on the committed clean sources"
    - "Errors injected into BOTH the lib leaf AND the spec leaf yield a non-zero verdict with BOTH distinct diagnostic codes visible in stdout (proving both leaves were walked)"
    - "`nx add`'s init path (`npm install <tarball>` + `nx g angular-typechecker:init`) seeds `targetDefaults` from ABSENT"
  artifacts:
    - path: "e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/tsconfig.json"
      provides: "solution tsconfig with a non-empty references[] -> lib + spec leaves (D-01)"
      contains: "references"
    - path: "e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/nx.json"
      provides: "namedInputs with NO targetDefaults[angular-typechecker:typecheck] key (D-02)"
      contains: "namedInputs"
    - path: "e2e/angular-typechecker-install-e2e/src/generator-e2e.int.spec.ts"
      provides: "GE2E-01 + GE2E-02 (generate -> assert config -> clean run -> two-leaf injection verdict)"
      contains: "consumer-generator:typecheck"
      min_lines: 120
    - path: "e2e/angular-typechecker-install-e2e/src/nx-add-e2e.int.spec.ts"
      provides: "GE2E-03 (npm install tarball + nx g init -> seeded-from-absent)"
      contains: "angular-typechecker:init"
      min_lines: 60
  key_links:
    - from: "e2e/angular-typechecker-install-e2e/src/generator-e2e.int.spec.ts"
      to: "e2e/angular-typechecker-install-e2e/fixtures/consumer-generator"
      via: "cpSync + npm install <tarball> + nx g angular-typechecker:configuration + nx run"
      pattern: "nx g angular-typechecker:configuration"
    - from: "e2e/angular-typechecker-install-e2e/src/nx-add-e2e.int.spec.ts"
      to: "nx.json targetDefaults"
      via: "nx g angular-typechecker:init (the exact command nx add runs internally)"
      pattern: "targetDefaults"
---

<objective>
Prove the Phase 14 generator suite end-to-end against the freshly-packed tarball. A new un-wired multi-leaf consumer fixture is installed from the tarball; `nx g angular-typechecker:configuration` wires ONE `typecheck` target at the solution `tsconfig.json` and (via `init`) seeds the `nx.json` `targetDefaults`; the target then runs to a real multi-leaf WALK verdict (clean -> success; lib+spec errors -> failure with BOTH codes visible). A second scenario proves `nx add`'s install-time init path seeds `targetDefaults` from absent. Optionally hardens the packaging audit for the shipped generator files (D-13).

Purpose: This is the empirical, real-consumer proof that the shipped generators + reference-walking executor work from a clean install — the last coverage gate before the v0.1.0 milestone Release PR. It exercises GEN-01/02/03/04/07/08/09 through their published surface.

Output: A new `consumer-generator` fixture workspace (fixture DATA, not a new nx project) + `generator-e2e.int.spec.ts` (GE2E-01+02) + `nx-add-e2e.int.spec.ts` (GE2E-03) + a small `tarball-audit.int.spec.ts` extension (D-13). No plugin/engine/generator source changes.

EXECUTION NOTE (LOAD-BEARING — main tree, worktree-hostile): The heavy specs run a real `nx build` + `npm pack` + `npm install <tarball>` + nested `nx run`. Per AGENTS.md D-22 these are WORKTREE-HOSTILE and MUST run SEQUENTIALLY on the MAIN checkout (real `node_modules`, not a junction). This plan is the SOLE plan in Wave 2 precisely so the orchestrator runs it on the main tree (single-plan wave = no worktree isolation). Do NOT execute this plan in an isolated worktree. `depends_on: ["15-01"]` is an EXECUTION-ORDERING constraint (keep this heavy plan alone in its wave, on the main tree) — NOT a code dependency on the guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-generator-e2e-ci-self-audit-guard/15-RESEARCH.md
@.planning/phases/15-generator-e2e-ci-self-audit-guard/15-PATTERNS.md

<interfaces>
<!-- The shipped generator behavior the specs assert against. Extracted from source; no exploration needed. -->

configuration generator (packages/angular-typechecker/src/generators/configuration/generator.ts):
- awaits initGenerator({ skipFormat: true }) FIRST, then resolves tsConfig (D-07 order: override -> solution tsconfig.json with non-empty references[] -> flat leaf -> throw), then writes ONE target:
    targets[targetName] = { ...existing, executor: 'angular-typechecker:typecheck', options: { ...existing?.options, tsConfig } }
  targetName defaults to 'typecheck'. For a ROOT-level project (root '.') the solution branch returns joinPathFragments('.', 'tsconfig.json') === 'tsconfig.json'.
- schema.json accepts `skipFormat` (boolean). Passing `--skipFormat` makes formatFiles() a no-op (fixture has no Prettier).

init generator (packages/angular-typechecker/src/generators/init/generator.ts):
- whole-entry `??=`: seeds nx.json targetDefaults['angular-typechecker:typecheck'] ONLY when absent (don't-clobber). The seeded block (TYPECHECK_TARGET_DEFAULTS):
    { cache: true, outputs: [], inputs: ['default', '{projectRoot}/tsconfig*.json', '{projectRoot}/package.json', '{workspaceRoot}/tsconfig.base.json', '^default', {dependentTasksOutputFiles...}, {externalDependencies:['typescript','@angular/compiler-cli']}] }
  LOAD-BEARING invariant: inputs[0] === 'default' (never 'production' — WALK-02 spec-hashing landmine).
- The UNSCOPED published id is `angular-typechecker:typecheck` (assert this in the tmp consumer; the dev workspace-scoped `@angular-typechecker/...` key does NOT bind in a consumer install).

nx add contract (nx 23.0.1, Finding 1): `nx add <bare-name>` resolves `pkg@latest` from the REGISTRY (wrong artifact, needs network). Internally `runPluginInitGenerator` runs the byte-identical `nx g angular-typechecker:init`. The deterministic OFFLINE proof of GEN-09 is `npm install <tarball>` + `nx g angular-typechecker:init`. NO `ng-add` alias is needed.
</interfaces>

<!-- Harness + fixture analogs to COPY verbatim (change only the operation). Read them in the tasks. -->
@e2e/angular-typechecker-matrix-e2e/src/matrix-5types.int.spec.ts
@e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts
@e2e/angular-typechecker-install-e2e/src/tarball-audit.int.spec.ts
@e2e/angular-typechecker-install-e2e/fixtures/consumer-app/package.json
@e2e/angular-typechecker-install-e2e/fixtures/consumer-app/nx.json
@e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/local-lib/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the un-wired multi-leaf `consumer-generator` fixture workspace</name>
  <files>
    e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/package.json,
    e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/nx.json,
    e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/project.json,
    e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/tsconfig.json,
    e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/tsconfig.lib.json,
    e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/tsconfig.spec.json,
    e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/src/consumer-generator.component.ts,
    e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/src/consumer-generator.component.spec.ts
  </files>
  <read_first>
    - e2e/angular-typechecker-install-e2e/fixtures/consumer-app/{package.json,nx.json,project.json,tsconfig.json,tsconfig.lib.json,src/app.component.ts} (the installable flat-workspace shape to mirror)
    - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/local-lib/{tsconfig.json,tsconfig.lib.json,tsconfig.spec.json,src/local-lib.component.ts,src/local-lib.component.spec.ts} (the multi-leaf solution + component + inline-declared-globals spec shape to mirror)
    - .planning/phases/15-generator-e2e-ci-self-audit-guard/15-PATTERNS.md (the `consumer-generator/` fixture section — each file's exact shape + the D-02 ANTI-PATTERN warning)
    - .planning/phases/15-generator-e2e-ci-self-audit-guard/15-RESEARCH.md Finding 3 (a-e)
  </read_first>
  <action>
    Create a NEW fixture workspace at `e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/` (fixture DATA under `install-e2e/fixtures/`, NOT a new nx project under `e2e/` — do not add an `e2e/consumer-generator/` dir). It is a ROOT-LEVEL `projectType: "library"`, un-wired, with a solution `tsconfig.json` carrying a non-empty `references[]` -> BOTH leaves. Compose it from two analogs:
    - `package.json`: COPY `consumer-app/package.json` verbatim (same dep set so `npm install <tarball>` resolves — `@angular/common|compiler|core@22.0.4`, `rxjs@7.8.2`, `zone.js@0.16.0`; devDeps `@angular/compiler-cli@22.0.4`, `@nx/devkit@23.0.1`, `nx@23.0.1`, `typescript@6.0.3`), change `name` to `consumer-generator`, keep `private: true` + `license: "MIT"`.
    - `nx.json`: COPY `consumer-app/nx.json` `namedInputs` (default/production/sharedGlobals) but DELETE the entire `targetDefaults` block (D-02, LOAD-BEARING — the `angular-typechecker:typecheck` key MUST be ABSENT so `init` genuinely seeds it and the GE2E-01(b)/GE2E-03 assertions are non-vacuous). ANTI-PATTERN: do NOT copy `consumer-app`'s `targetDefaults` block (it pre-declares the key with `production`-first inputs).
    - `project.json`: `{ name: "consumer-generator", $schema: "node_modules/nx/schemas/project-schema.json", sourceRoot: "src", projectType: "library" }` with NO `targets` block (un-wired — the generator adds the target).
    - `tsconfig.json` (SOLUTION): `{ files: [], include: [], references: [ { path: "./tsconfig.lib.json" }, { path: "./tsconfig.spec.json" } ] }` — TWO references (this is the load-bearing difference from `consumer-app`, which references only the lib leaf). This is what makes `resolveTsConfig` take case (2) and the engine walk BOTH leaves.
    - `tsconfig.lib.json`: COPY `local-lib/tsconfig.lib.json` verbatim (noEmit, strict, moduleResolution bundler, `angularCompilerOptions.strictTemplates: true`, `include: ["src/**/*.ts"]`, `exclude: ["src/**/*.spec.ts", "src/**/*.test.ts"]`). The `exclude` is load-bearing so the spec file is checked ONLY via the spec leaf.
    - `tsconfig.spec.json`: COPY `local-lib/tsconfig.spec.json` verbatim (`include: ["src/**/*.spec.ts", "src/**/*.test.ts"]`).
    - `src/consumer-generator.component.ts`: a committed-CLEAN standalone template-bearing component mirroring `local-lib.component.ts` — `@Component({ selector: 'consumer-generator-root', standalone: true, template: '<p>{{ label }}</p>' })` exporting `class ConsumerGeneratorComponent` with `readonly label: string = 'angular-typechecker generator e2e';`.
    - `src/consumer-generator.component.spec.ts`: a committed-CLEAN spec mirroring `local-lib.component.spec.ts` — inline `declare function describe/it/expect` (so the fixture needs NO test-runner package under `types: ["node"]`), importing `ConsumerGeneratorComponent`, one `it()` that constructs it and asserts the label.
    NO lockfile (a stray `pnpm-lock.yaml` hard-fails Nx's lockfile plugin under `npm install`).
    Use `singleQuote` for the TS files; JSON files as shown. Keep the component + spec type-clean so the later clean `nx run` exits 0.
  </action>
  <acceptance_criteria>
    - All 8 files exist under `fixtures/consumer-generator/`; no `e2e/consumer-generator/` project dir was created; NO lockfile present.
    - `tsconfig.json` has `references` of length 2 pointing at `./tsconfig.lib.json` AND `./tsconfig.spec.json`.
    - `nx.json` has `namedInputs` but `targetDefaults?.['angular-typechecker:typecheck']` is `undefined` (D-02).
    - `project.json` is `projectType: "library"`, `name: "consumer-generator"`, with NO `targets` block.
    - `tsconfig.lib.json` excludes `*.spec.ts`; `tsconfig.spec.json` includes only `*.spec.ts`; both set `strictTemplates: true`.
    - Component + spec are committed-clean (the Task-2 clean `nx run` will exit 0); the spec declares its test globals inline.
    - Every JSON file parses; `package.json` deps match `consumer-app`, `private: true`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const b='e2e/angular-typechecker-install-e2e/fixtures/consumer-generator';const t=JSON.parse(fs.readFileSync(b+'/tsconfig.json','utf8'));const n=JSON.parse(fs.readFileSync(b+'/nx.json','utf8'));const p=JSON.parse(fs.readFileSync(b+'/project.json','utf8'));if(!Array.isArray(t.references)||t.references.length!==2)throw new Error('solution references must be length 2');if(n.targetDefaults&&n.targetDefaults['angular-typechecker:typecheck'])throw new Error('nx.json must NOT pre-declare the targetDefaults key (D-02)');if(p.projectType!=='library'||p.targets)throw new Error('project.json must be an un-wired library');if(fs.existsSync(b+'/pnpm-lock.yaml'))throw new Error('no lockfile allowed');console.log('fixture OK')"</automated>
  </verify>
  <done>The `consumer-generator` fixture exists as an un-wired root-level library with a two-reference solution tsconfig, a targetDefaults-free `nx.json`, a clean template-bearing component + inline-globals spec, and no lockfile.</done>
</task>

<task type="auto">
  <name>Task 2: generator-e2e.int.spec.ts — GE2E-01 (generate + assert config) + GE2E-02 (two-leaf walk verdict)</name>
  <files>e2e/angular-typechecker-install-e2e/src/generator-e2e.int.spec.ts</files>
  <read_first>
    - e2e/angular-typechecker-matrix-e2e/src/matrix-5types.int.spec.ts (PRIMARY harness — COPY `buildCleanEnv`/`NX_RUNNER_ENV_KEYS` verbatim, the beforeAll build+pack, the tmp cpSync+.npmrc+install honesty flow, the `run(cwd,target)` execSync-catch verdict helper with `--skip-nx-cache`, the BROKEN_FIELD/BROKEN_STATEMENT JSON.stringify injection idiom, the afterEach source-restore)
    - e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts (the single-scenario tmp-install shape + the TS2322/`ERR_REQUIRE_ESM`/`infrastructure error` assertion discipline)
    - e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/** (the fixture from Task 1 — the operand)
    - packages/angular-typechecker/src/generators/configuration/generator.ts (the written target shape lines 156-160; the resolveTsConfig solution branch lines 78-87)
    - packages/angular-typechecker/src/generators/init/generator.ts (the seeded TYPECHECK_TARGET_DEFAULTS block; inputs[0]==='default')
    - .planning/phases/15-generator-e2e-ci-self-audit-guard/15-PATTERNS.md (the `generator-e2e.int.spec.ts` section) + 15-RESEARCH.md Finding 3e + Finding 4
  </read_first>
  <action>
    Create `e2e/angular-typechecker-install-e2e/src/generator-e2e.int.spec.ts` (the `include: ['src/**/*.int.spec.ts']` glob auto-picks it up; the serialized `vitest.config.mts` — forks/singleFork/node/300000ms — applies). COPY the `matrix-5types` harness verbatim, changing ONLY the operation:
    - Imports + `NX_RUNNER_ENV_KEYS` + `buildCleanEnv()` (strip NX_* runner vars + `npm_config_legacy_peer_deps`/`NPM_CONFIG_LEGACY_PEER_DEPS`; set `NX_DAEMON:'false'`, `FORCE_COLOR:'0'`) — byte-identical to matrix/install-smoke.
    - `workspaceRoot` = 3 dirs up via `fileURLToPath(import.meta.url)`; `distDir = dist/packages/angular-typechecker`; `fixtureDir = e2e/angular-typechecker-install-e2e/fixtures/consumer-generator`.
    - `beforeAll` (300000ms): `npx nx build angular-typechecker --skip-nx-cache` then `npm pack --json` from `distDir` -> `tarballPath`. `afterAll`: `rmSync(tarballPath, { force: true })`. (Per-file build+pack — the D-08 acceptable-fallback for isolation parity with the existing specs.)
    - `run(cwd, target)` verdict helper: COPY matrix's — `execSync('npx nx run <target> --output-style=static --skip-nx-cache', {cwd,env,encoding:'utf8'})` in try/catch; keep `--skip-nx-cache` (LOAD-BEARING — the injected re-run must really execute). NEVER pipe `nx` through `head`/`rg`.
    In ONE `it()` (a per-scenario tmp workspace, cleaned in `finally`):
    1. `mkdtempSync(join(tmpdir(), 'atc-gen-'))` -> `cpSync(fixtureDir, tmp, {recursive:true})` -> `writeFileSync(join(tmp,'.npmrc'),'')` -> `npm install <tarballPath>` with `env: { ...env, npm_config_userconfig: join(tmp,'.npmrc.nonexistent') }` (B-03 honesty; NO peer-override).
    2. Generate: `execSync('npx nx g angular-typechecker:configuration consumer-generator --skipFormat', {cwd:tmp, env, encoding:'utf8'})`. Pass `--skipFormat` (fixture has no Prettier). Do NOT pass `--output-style=static` to `nx g` (that is a run flag — Finding 4 / A2; corrects CONTEXT D-04's copy-paste).
    3. GE2E-01(a): read tmp `project.json`; assert `Object.keys(targets)` is exactly `['typecheck']`; assert `targets.typecheck.executor === 'angular-typechecker:typecheck'`; assert `targets.typecheck.options.tsConfig === 'tsconfig.json'` (the solution tsconfig for a root-level project). If Nx normalizes the root differently, assert it endsWith `'tsconfig.json'` and is NOT a leaf (`tsconfig.lib.json`/`tsconfig.spec.json`).
    4. GE2E-01(b): read tmp `nx.json`; `const seeded = nxJson.targetDefaults['angular-typechecker:typecheck']`; assert `seeded.cache === true`, `seeded.outputs` toEqual `[]`, `seeded.inputs[0] === 'default'` (never 'production' — WALK-02 landmine). Assert against the `init` SEEDED shape (inputs[0]==='default'), NOT the fixture nx.json blocks.
    5. GE2E-02 clean: `const green = run(tmp, 'consumer-generator:typecheck')`; `expect(green.code).toBe(0)`.
    6. GE2E-02 two-leaf injection (DISTINCT codes per Finding 3e / Pitfall 4): mutate `src/consumer-generator.component.ts` — insert a class FIELD `readonly broken: number = ${JSON.stringify('str')};` ahead of the `label` line -> TS2322 (lib leaf). Mutate `src/consumer-generator.component.spec.ts` — insert a STATEMENT inside the `it()` body that produces a DIFFERENT code; recommended `('x').padStart(${JSON.stringify('str')});` -> TS2345 (Argument of type 'string' not assignable to parameter of type 'number'; a valid statement in a function body). Both built via `JSON.stringify` (ASCII-only, no escaping hazard). D-14 discretion: any two DISTINCT individually-assertable codes (one per leaf) are acceptable, provided the spec-leaf line is a valid statement (a `declare`/`readonly` field would be a syntax error in a function body and mask the intended code).
    7. `const bad = run(tmp, 'consumer-generator:typecheck')`; assert `bad.code` NOT 0; `bad.stdout` toContain `'TS2322'` AND toContain `'TS2345'` (full tokens, not bare `2322`); `not.toMatch(/ERR_REQUIRE_ESM/)`; `not.toContain('infrastructure error')`. Both distinct codes present is the two-leaves-walked proof.
    Follow repo TS style + `singleQuote`.
  </action>
  <acceptance_criteria>
    - `beforeAll` builds `--skip-nx-cache` + `npm pack --json`; the tmp install uses an empty `.npmrc` + non-existent `npm_config_userconfig` + NO peer-override.
    - Generate uses `nx g angular-typechecker:configuration consumer-generator --skipFormat` with NO `--output-style=static`.
    - GE2E-01: tmp `project.json` has exactly ONE target `typecheck`, executor `angular-typechecker:typecheck`, `options.tsConfig` resolving to the solution `tsconfig.json`; tmp `nx.json` `targetDefaults['angular-typechecker:typecheck']` has `cache:true`, `outputs:[]`, `inputs[0]==='default'`.
    - GE2E-02: clean `run` exits 0; after injecting TS2322 (lib field) + TS2345 (spec statement), `run` exits non-zero AND stdout contains BOTH `TS2322` and `TS2345` AND no `ERR_REQUIRE_ESM` AND no `infrastructure error`.
    - Every `nx run` carries `--skip-nx-cache`; `nx` is never piped through `head`/`rg`; injected lines built via `JSON.stringify`.
    - `npx nx test angular-typechecker-install-e2e` passes with the new spec GREEN.
  </acceptance_criteria>
  <verify>
    <automated>npx nx test angular-typechecker-install-e2e</automated>
  </verify>
  <done>`generator-e2e.int.spec.ts` proves, from a clean tarball install, that `configuration` wires the single walk target + `init` seeds the `default`-input cache block, and the target walks BOTH leaves (green clean; TS2322+TS2345 on injected lib+spec errors).</done>
</task>

<task type="auto">
  <name>Task 3: nx-add-e2e.int.spec.ts — GE2E-03 (install-time init seed) + D-13 tarball-audit hardening</name>
  <files>
    e2e/angular-typechecker-install-e2e/src/nx-add-e2e.int.spec.ts,
    e2e/angular-typechecker-install-e2e/src/tarball-audit.int.spec.ts
  </files>
  <read_first>
    - e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts (the harness to reuse for GE2E-03 — buildCleanEnv, beforeAll build+pack, tmp install honesty)
    - e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/nx.json (the un-wired fixture — its targetDefaults key is ABSENT, the seeded-from-absent baseline)
    - packages/angular-typechecker/src/generators/init/generator.ts (the seeded TYPECHECK_TARGET_DEFAULTS shape; inputs[0]==='default')
    - e2e/angular-typechecker-install-e2e/src/tarball-audit.int.spec.ts (the file being modified for D-13 — the REQUIRED_FILES array + the leak guards)
    - .planning/phases/15-generator-e2e-ci-self-audit-guard/15-RESEARCH.md Finding 1 (the copy-pasteable GE2E-03 recipe + the nx add -> nx g init equivalence rationale) + Finding 5 (the five shipped generator paths) + 15-PATTERNS.md (`nx-add-e2e.int.spec.ts` + `tarball-audit` D-13 sections)
  </read_first>
  <action>
    Part A (GE2E-03): Create `e2e/angular-typechecker-install-e2e/src/nx-add-e2e.int.spec.ts` reusing the `install-smoke` harness (buildCleanEnv, `beforeAll` build `--skip-nx-cache` + `npm pack --json` -> `tarballPath`, `afterAll` rmSync tgz). Put a HEADER comment documenting the equivalence (Finding 1): `nx add <bare-name>` resolves `pkg@latest` from the REGISTRY (wrong artifact, needs network), so the deterministic OFFLINE proof of GEN-09 is `npm install <tarball>` + the byte-identical internal command `nx add`'s `runPluginInitGenerator` runs — `nx g angular-typechecker:init`; no `ng-add` alias needed; do NOT ship an Angular-CLI `ng add` surface. In ONE `it()` (tmp workspace, cleaned in `finally`):
    1. `mkdtempSync(join(tmpdir(),'atc-add-'))` -> `cpSync(fixtureDir, tmp, {recursive:true})` (fixtureDir = the consumer-generator fixture) -> `writeFileSync(join(tmp,'.npmrc'),'')`.
    2. Seeded-from-absent baseline: read the tmp `nx.json` BEFORE init; assert `targetDefaults?.['angular-typechecker:typecheck']` is `undefined` (proves the seed is non-vacuous).
    3. `execSync('npm install ' + JSON.stringify(tarballPath), {cwd:tmp, env:{...env, npm_config_userconfig: join(tmp,'.npmrc.nonexistent')}, encoding:'utf8'})`.
    4. `execSync('npx nx g angular-typechecker:init --skipFormat', {cwd:tmp, env, encoding:'utf8'})` — the exact command `nx add` runs internally.
    5. Read tmp `nx.json`; `const seeded = nxJson.targetDefaults['angular-typechecker:typecheck']`; assert `seeded` toBeDefined, `seeded.cache === true`, `seeded.outputs` toEqual `[]`, `seeded.inputs[0] === 'default'`.
    Part B (D-13, discretionary hardening — include it; ~5 lines, low-cost belt-and-suspenders): extend `tarball-audit.int.spec.ts` `REQUIRED_FILES` by APPENDING the five shipped generator paths (Finding 5, verified shipped via the `project.json` asset globs + `package.json` files allowlist): `'generators.json'`, `'src/generators/configuration/generator.js'`, `'src/generators/configuration/schema.json'`, `'src/generators/init/generator.js'`, `'src/generators/init/schema.json'`. Do NOT touch the existing gates/leak guards — they do not false-positive on `src/generators/*/generator.js` or `schema.json` (the `.spec.ts` generator tests are excluded by `tsconfig.lib.json`; the `**/!(*.ts)` asset glob excludes `.ts`). Add a short comment marking these as Phase 15 D-13 additions.
    Follow repo TS style + `singleQuote`.
  </action>
  <acceptance_criteria>
    - `nx-add-e2e.int.spec.ts` header documents the `nx add` -> `nx g angular-typechecker:init` equivalence (why it is the faithful offline proof, not a shortcut).
    - The spec asserts the fixture `nx.json` key is ABSENT before init, then present after `nx g angular-typechecker:init` with `cache:true`, `outputs:[]`, `inputs[0]==='default'`.
    - The install uses an empty `.npmrc` + non-existent `npm_config_userconfig` + NO peer-override; `nx` is never piped through `head`/`rg`.
    - `tarball-audit.int.spec.ts` `REQUIRED_FILES` now includes the five generator paths; the existing leak guards and the no-install-scripts guard are unchanged and still pass.
    - `npx nx test angular-typechecker-install-e2e` passes with `nx-add-e2e.int.spec.ts` GREEN and `tarball-audit.int.spec.ts` GREEN (the shipped tarball contains all five generator files).
  </acceptance_criteria>
  <verify>
    <automated>npx nx test angular-typechecker-install-e2e</automated>
  </verify>
  <done>GE2E-03 proves `nx add`'s init path seeds `targetDefaults` from absent against the installed tarball; the packaging audit now asserts the five generator files ship (D-13).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev workspace -> nested nx/npm subprocess (tmp consumer) | The specs shell out (`execSync`) to `nx`/`npm` inside per-scenario tmp workspaces. Only fixed literal ids/flags cross; no untrusted external input. |
| registry -> tmp install | The tmp `npm install` resolves the fixture's Angular/Nx/TS deps from the public registry; the PACKAGE UNDER TEST comes from the local tarball. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `execSync` in `generator-e2e`/`nx-add-e2e` specs | mitigate | Fixed target ids (`consumer-generator:typecheck`, `angular-typechecker:configuration`/`:init`) + fixed flags only; injected source lines built via `JSON.stringify`; NO PR-metadata/untrusted string reaches the shell (verified pattern in all existing specs). |
| T-15-02 | Tampering (V14 supply-chain honesty) | tmp install of the tarball | mitigate | Keep the `install-smoke` honesty controls: empty `.npmrc` + non-existent `npm_config_userconfig` + NO peer-override (B-03) so a real consumer ERESOLVE surfaces instead of being masked. |
| T-15-03 | Elevation | a `postinstall`/lifecycle script in the shipped tarball (s1ngularity vector) | mitigate | D-13 keeps `tarball-audit`'s no-install-scripts guard intact while adding the generator files to `REQUIRED_FILES`; the shipped `package.json` declares no install scripts. |

No new secrets, permissions, or workflow-permission changes. Package Legitimacy Gate: NOT applicable — this phase installs the LOCAL freshly-packed tarball and resolves already-vetted fixture deps from the registry; it adds NO new dependency to the workspace (RESEARCH "Package Legitimacy Audit"). The single required `ci` gate stays byte-unchanged (the new `*.int.spec.ts` files auto-route into the existing `e2e` job's project list — no `ci.yml` change).
</threat_model>

<verification>
- `npx nx test angular-typechecker-install-e2e` GREEN with `generator-e2e.int.spec.ts`, `nx-add-e2e.int.spec.ts`, and the extended `tarball-audit.int.spec.ts` (heavy tarball tier; runs SEQUENTIALLY on the MAIN tree — worktree-hostile).
- Fixture structural check (Task 1 verify) passes: 2-reference solution tsconfig, targetDefaults-free `nx.json`, un-wired library `project.json`, no lockfile.
- Phase-gate: after this plan lands on main, run the full e2e run-many (`npx nx run-many -t test -p angular-typechecker-install-e2e angular-typechecker-cache-e2e angular-typechecker-matrix-e2e`) on the merged main tree.
</verification>

<success_criteria>
- ROADMAP SC1: a clean tarball install + `nx g angular-typechecker:configuration` on the un-wired `consumer-generator` yields ONE `typecheck` target (executor `angular-typechecker:typecheck`) at the solution `tsconfig.json` AND the `init`-seeded `default`-input cache block. (GE2E-01)
- ROADMAP SC2: `nx run consumer-generator:typecheck --skip-nx-cache` is green clean and fails with BOTH the lib (TS2322) and spec (TS2345) codes visible on injected two-leaf errors. (GE2E-02)
- ROADMAP SC3: `nx add`'s init path (`npm install <tarball>` + `nx g angular-typechecker:init`) seeds `targetDefaults` from absent. (GE2E-03)
- D-13 (discretionary): the packaging audit asserts the five shipped generator files ship in the tarball.
</success_criteria>

<output>
Create `.planning/phases/15-generator-e2e-ci-self-audit-guard/15-02-SUMMARY.md` when done.
</output>
