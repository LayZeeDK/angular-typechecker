---
context: phase
phase: 35-automated-code-scanning-proof
task: 4
total_tasks: 4
status: in_progress
last_updated: 2026-07-21T22:14:00.172Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These were discovered through real failures. Acknowledge each explicitly before executing
> 35-04 or declaring the phase done.

- [ ] CONSTRAINT: **GitHub Code Scanning rejects no-location SARIF results** — a result with no `locations` key makes GitHub reject the ENTIRE SARIF (`locationFromSarifResult: expected at least one location`). This is exactly what 35-04 fixes: file-less diagnostics get a region-less whole-file fallback location on the relativized `tsConfigPath` (reverses locked D-01 in the SARIF emission only). Do NOT weaken or skip that fix.
- [ ] CONSTRAINT: **The proof is real-CI-only (Nyquist point)** — no local gate (SARIF schema validation, the drift-lock integration spec, the `ASSERT_ALERTS_FILE` unit seam, `act`) can prove GitHub ingestion. Only the `code-scanning-proof` job on a real PR run against `main` exercises it. Every local gate can be green while ingestion is red. The phase closes on the `code-scanning-proof` job GREEN, NOT on local green.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Declaring done on local green | Local schema/drift-lock/unit/act all passed while GitHub rejected the SARIF in the prior session. | blocking | After 35-04 lands, PUSH to PR #55 and read the `code-scanning-proof` job result; only that proves G-35-01 fixed. |
| Executor in a bare worktree | 35-04 needs real `node_modules` for the 8 gates; a fresh worktree has none. `worktree.base-check shouldDegrade` returned `false` this session, so execute-phase will NOT auto-degrade. | advisory | Run 35-04 on the MAIN working tree (single-plan wave -> AGENTS.md "single-plan wave: skip worktrees"). If a worktree is used, provision via the AGENTS.md `node_modules` junction (Pattern A valid — no dep change). |
| Trusting the LSP feed on sarif-report.ts | The passive `new-diagnostics` feed went stale mid-edit on this file; disproven against `nx test` + the compiler. | advisory | Trust `nx test` / `nx integration` / `tsc` / `fallow audit`, never the ambient LSP feed. |

<current_state>
Gap plan **35-04 is planned + fully vetted but NOT executed.** HEAD = `298b1b8` on
`gsd/v0.2.4-enhanced-sarif-reporting-for-github-code-scanning` (PR #55, draft). Two RED
checks on PR #55, BOTH cleared by this session's remaining work:
- `cve-lite` + required `ci` — RED only because the svgo fix (`4c38ab7`) is committed but UNPUSHED (local cve-lite already exits 0).
- `code-scanning-proof` — RED because G-35-01 is not yet fixed (35-04 fixes it).
Working tree clean except `.planning/config.json` (ephemeral `_auto_chain_active` flip — do NOT commit).
</current_state>

<completed_work>
- Resumed from the prior handoff (consumed its HANDOFF.json).
- Quick 260721-wda: svgo HIGH cve fixed via `npm update svgo` 4.0.1 -> 4.0.2 (lockfile-only, no override; `4c38ab7` + `ca59856`). Local cve-lite/fallow green.
- Locked the G-35-01 D-01 reversal contract (D1-D7) in `35-G-35-01-CONTEXT.md` (region-less whole-file location, user-confirmed). Commit `68a5137`.
- Planned gap 35-04: gsd-planner (opus) -> gsd-plan-checker (sonnet, 0 blockers / 1 warning incorporated as Task 4 README fix) -> decision-coverage 14/14. Commit `298b1b8`.
</completed_work>

<remaining_work>
1. **Execute 35-04** (`/gsd-core:execute-phase 35 --gaps-only`) on the MAIN tree. 4 tasks:
   (1) `sarif-report.ts` PASS-2 file-less branch -> `{ fileUri: relativizePath(result.tsConfigPath, pathBase) }` (region-less) + `relativizePath` import + D-01 comment updates;
   (2) `sarif-report.spec.ts` flip the file-less no-location assertion + regenerate `sarif-report.spec.ts.snap`;
   (3) `machine-reporters-sarif.integration.spec.ts` flip the two "NO locations" assertions (TS2318 + ATC90002) + regenerate its snap;
   (4) `README.md:710-713` correct the stale "no-location / ignore the SARIF alert" paragraph.
2. Run the 8 local gates: `nx test`, `nx integration`, `nx typecheck` (3 tsc — `nx test` does NOT type-check specs), `nx run-many -t lint` (maxWarnings:0), `nx format:check`, `npm run fallow`, `npm run cve-lite`, `nx build`. Additive-only spot check: only the 6 `files_modified` changed.
3. **Push** the branch (user-authorized this session) — one CI run proves BOTH red checks green.
4. Verify PR #55 in real CI (`rtk gh pr checks 55`): cve-lite green, ci green, **code-scanning-proof green** (authoritative). If code-scanning-proof is still red with an ingestion/locationFromSarifResult error, GitHub rejected the region-less location -> fall back to a synthetic region line 1, col 1 (CONTEXT D6) and re-push.
5. `/gsd-core:verify-work 35` to close the 3 UAT items (all were blocked on ingestion).
6. Post-completion (only once green): `/gsd-core:secure-phase 35` -> `/gsd-core:validate-phase 35` -> `/gsd-core:extract-learnings 35`.
</remaining_work>

<decisions_made>
- Region-less whole-file fallback location (honest project/config-level, smallest diff, GitHub supports path-level alerts) over a synthetic line-1 region. Line-1 is the documented fallback IF GitHub rejects region-less.
- SARIF-only reversal: JSON reporter / DiagnosticRecord / fingerprint / barrel / version all byte-unchanged; stays 0.2.4 patch (additive-only).
- svgo lean-inline (trivial in-range patch bump; no override -> no npm 10/11 trap).
- Run 35-04 on the MAIN tree (single-plan wave; needs real node_modules; no dep change).
</decisions_made>

<blockers>
- code-scanning-proof RED until 35-04 executed + pushed. Single anticipated failure mode: GitHub rejects the region-less location -> synthetic line-1 fallback.
- cve-lite/ci RED until the (already-committed) svgo fix is pushed.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — structured state + ordered remaining tasks with resume commands.
2. `.planning/phases/35-automated-code-scanning-proof/35-G-35-01-CONTEXT.md` — the LOCKED D1-D7 contract (source of truth; wins on any discrepancy).
3. `.planning/phases/35-automated-code-scanning-proof/35-04-PLAN.md` — the executable plan (4 tasks, must_haves, gates).
4. `.planning/phases/35-automated-code-scanning-proof/35-UAT.md` (## Gaps) — G-35-01 root cause + the 3 UAT items to close.
5. `AGENTS.md` — PR/release mechanics (main is PR-only; PR #55 stays draft until milestone complete; NO self-merge without explicit user OK) + the node_modules junction rules.

## Infrastructure State
- Branch `gsd/v0.2.4-...` = PR #55 (draft, base main), HEAD `298b1b8`. The svgo fix + gap plan are committed locally but the branch is NOT yet pushed with them (last pushed HEAD was `f89bcb4`).
- No running servers/watchers; no async external jobs. `.planning/config.json` dirty (ephemeral — leave it).

<context>
Everything is staged for a clean execution: the plan is checked, the contract is locked, and
the two red PR checks both clear via execute-then-push. The only real unknown is GitHub's
acceptance of the region-less location — which the code-scanning-proof job itself resolves in
real CI (that is the phase's whole point). Run 35-04 on main, watch the 8 gates, push once,
read the CI result, then verify-work.
</context>

<next_action>
Start with: `/gsd-core:resume-work` (loads this handoff), then `/gsd-core:execute-phase 35 --gaps-only`
to execute 35-04 on the MAIN working tree. Then run the 8 gates, push the branch, and confirm
cve-lite/ci + code-scanning-proof go green on PR #55.
</next_action>
