# Throwaway ruleset probe (planning-only)

Temporary file for a planning-only probe PR that verifies which Code Scanning
analyses a `.planning/`-only change produces on its merge ref. This PR is not
merged; the branch is deleted after observation.
