---
phase: quick-260719-m76
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/executors/typecheck/executor.ts
  - packages/angular-typechecker/src/core/render-report.ts
  - packages/angular-typechecker/src/cli/main.integration.spec.ts
  - packages/angular-typechecker/src/core/json-report.spec.ts
  - packages/angular-typechecker/src/core/sarif-report.spec.ts
  - packages/angular-typechecker/src/executors/typecheck/normalize-options.spec.ts
autonomous: true
requirements: [VER-01, VER-02, REP-01, REP-02, FMT-01]

must_haves:
  truths:
    - "A real cold-compiler coverage-incomplete run (errorCount 0, success false) through --format json returns exit 1 AND a payload with summary.success false + summary.outcome 'coverage-incomplete' (FIX 3 / VER-01 / VER-02)."
    - "The JSON reporter emits a file-set-but-position-absent diagnostic with file non-null and line/column/endLine/endColumn null, never dropped (FIX 4 / REP-01)."
    - "The SARIF reporter emits a file-set-but-position-absent diagnostic as a location with artifactLocation and NO region, still one result per diagnostic, still schema-valid (FIX 4 / REP-02)."
    - "normalizeOptions forwards format:'json' and defaults an absent format to 'human' (FIX 5 / FMT-01)."
    - "A clean CoreResult renders an empty-results SARIF payload carrying the full 18-rule catalog that schema-validates (FIX 6 / REP-02)."
    - "The executor advisory-gate comment scopes the machine-payload-redundancy claim to JSON and notes SARIF omits advisory meta; the render-report JSDoc drops the landed-threading clause (FIX 1 / FIX 2)."
  artifacts:
    - packages/angular-typechecker/src/cli/main.integration.spec.ts
    - packages/angular-typechecker/src/core/json-report.spec.ts
    - packages/angular-typechecker/src/core/sarif-report.spec.ts
    - packages/angular-typechecker/src/executors/typecheck/normalize-options.spec.ts
    - packages/angular-typechecker/src/executors/typecheck/executor.ts
    - packages/angular-typechecker/src/core/render-report.ts
  key_links:
    - "renderReport format switch -> formatJsonReport / formatSarifReport: FIX 3 proves the json path end-to-end over a real compiler; FIX 4/6 lock the reporter record + empty shapes at the unit tier."
    - "normalizeOptions.format default 'human' -> executor forwards format into renderReport: FIX 5 locks the mapping FIX 1's corrected comment describes."
---

<objective>
Address the six surviving PR #47 (v0.2.3 machine-readable reporters) code-review findings: two comment-accuracy corrections (FIX 1, FIX 2) and four test-coverage additions (FIX 3, FIX 4, FIX 5, FIX 6). All changes are ADDITIVE. No production behavior changes. Version stays 0.2.2 (the Release PR owns the bump).

Purpose: Close accuracy + coverage gaps a multi-agent review found. The one end-to-end assertion the milestone hinges on -- a REAL-compiler coverage-incomplete run proven THROUGH a machine format (FIX 3) -- is currently only mocked; land it.

Output: 6 edited files across 3 clean conventional-commit commits (one comment fix, two test additions). Full test + typecheck + lint + format gates green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
Read the target files and mirror the EXISTING helper/style in each spec (do not introduce a new fixture-builder idiom):

@packages/angular-typechecker/src/executors/typecheck/executor.ts
@packages/angular-typechecker/src/core/render-report.ts
@packages/angular-typechecker/src/executors/typecheck/normalize-options.ts

Spec conventions to reuse verbatim:
- json-report.spec.ts / sarif-report.spec.ts already define `positionedDiag()` (file + start) and `filelessDiag()` (file undefined + start undefined) via `{ ... } as ts.Diagnostic` casts, plus a `coreResult(overrides)` factory. The new FIX-4 case adds a THIRD helper shaped like `filelessDiag()` but with a `file` object set (fileName + a `getLineAndCharacterOfPosition` that is never called) and `start`/`length` undefined.
- sarif-report.spec.ts already has `sarifOf(result)` (JSON.parse of `formatSarifReport`) and the `SarifResult`/`SarifPhysicalLocation` interfaces. Schema validation uses `validateSarif(sarifJson: string): { valid, errors }` from `@workspace/test-util` (add the import; `redactVolatile` shows the import path).
- main.integration.spec.ts already declares `cleanTsConfig` (not-type-checked-clean), `emptyLeafTsConfig` (solution-style-empty), and `NO_COLOR_ENV`; reuse them (do NOT redeclare fixtures).

Constraints (from CONTEXT + AGENTS.md):
- Comment fixes are comment-ONLY; do NOT touch the `if (format === 'human')` gate or any runtime logic.
- Do NOT edit .planning/ source files, package.json version, or any production code path.
- Commit scopes are clean conventional-commit scopes (docs/test with a release-meaningful scope) -- NEVER a GSD plan-id scope like (30-01), which would leak into the public changelog.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the two stale reporter comments (FIX 1 + FIX 2)</name>
  <files>packages/angular-typechecker/src/executors/typecheck/executor.ts, packages/angular-typechecker/src/core/render-report.ts</files>
  <action>
FIX 1 -- executor.ts, the advisory-gate comment (~lines 55-60). The clause claiming the machine payload "already carries every advisory field (summary.suppressed* / advisories.*), so the notices are redundant there" is TRUE for JSON but FALSE for SARIF: formatSarifReport emits only tool.driver + rules[] + results[] -- no summary.suppressed*, no advisories.*. Rewrite so the redundancy claim is SCOPED to the JSON format, and add that SARIF INTENTIONALLY omits advisory meta (there is no SARIF slot for it) while the verdict / exit code still reflects coverage (evaluateResult owns the verdict, unchanged). KEEP the primary, load-bearing justification intact: @nx/devkit logger.info routes to stdout in a task process and would prepend text that corrupts the raw payload / breaks JSON.parse and jq. Comment-only. Do NOT change the `if (format === 'human')` gate or any code.

FIX 2 -- render-report.ts, the RenderOptions JSDoc (~lines 19-21). Drop the now-stale clause stating the shipped callers (main.ts, executor.ts) "compile unchanged until 30-03 threads a real value" -- that threading LANDED in this PR (both main.ts and executor.ts already pass `format`). Keep the rest of that bullet: `format` is optional, defaults to 'human', and human output stays byte-identical / back-compatible.

Do not reflow unrelated comment lines; keep the diff minimal.
  </action>
  <verify>
    <automated>nx lint angular-typechecker && nx format:check</automated>
  </verify>
  <done>executor.ts advisory comment scopes the payload-redundancy claim to JSON and states SARIF omits advisory meta by design (verdict unaffected); the Nx-stdout-corruption justification and the human-format gate are unchanged. render-report.ts RenderOptions JSDoc no longer references a future threading step. No runtime code changed. lint (maxWarnings:0) + format:check green. Commit as `docs(core): scope SARIF advisory-meta omission and drop landed-threading note`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unit-tier reporter + normalize coverage (FIX 4 + FIX 5 + FIX 6)</name>
  <files>packages/angular-typechecker/src/core/json-report.spec.ts, packages/angular-typechecker/src/core/sarif-report.spec.ts, packages/angular-typechecker/src/executors/typecheck/normalize-options.spec.ts</files>
  <behavior>
    - FIX 4 (json-report.spec.ts): a diagnostic whose `file` is SET but `start`/`length` are undefined projects to a record with `file` NON-null (relativized forward-slash) AND `line`/`column`/`endLine`/`endColumn` all null; it is never dropped (payload.diagnostics length is one-to-one).
    - FIX 4 (sarif-report.spec.ts): the same file-set-position-absent diagnostic maps to a SARIF result WITH a location (artifactLocation.uri set, forward-slash relativized) and NO region; results length stays one-to-one with diagnostics; the payload still schema-validates.
    - FIX 6 (sarif-report.spec.ts): a clean CoreResult (zero diagnostics) renders results empty, driver.rules length 18, and the payload schema-validates.
    - FIX 5 (normalize-options.spec.ts): `format: 'json'` is forwarded to NormalizedOptions.format; an absent `format` defaults to 'human'.
  </behavior>
  <action>
FIX 4 -- add ONE new fixture helper in BOTH json-report.spec.ts and sarif-report.spec.ts, shaped like the existing `filelessDiag()` but with `file` set to an object carrying a `fileName` (e.g. 'D:/ws/proj/src/z.component.ts') plus a `getLineAndCharacterOfPosition` that is never invoked, and `start`/`length` undefined (build it with the same `{ ... } as unknown as ts.SourceFile` / `as ts.Diagnostic` cast idiom the file already uses). In json-report.spec.ts add an `it` inside the `formatJsonReport` describe (sibling to "never drops a file-less diagnostic") asserting: `payload.diagnostics` length equals the input diagnostics length, the record's `file` is the relativized 'src/z.component.ts' (NON-null), and `line`/`column`/`endLine`/`endColumn` are each null. In sarif-report.spec.ts add an `it` in the `formatSarifReport` describe asserting: `log.runs[0].results` length equals diagnostics length; the file-set result HAS `locations` with `physicalLocation.artifactLocation.uri === 'src/z.component.ts'`; its `region` carries NO position (region.startLine is undefined); and `validateSarif(await formatSarifReport(result, ts, 'D:/ws/proj')).valid === true`.

FIX 6 -- add an `it` in the sarif-report.spec.ts `formatSarifReport` describe: over `coreResult()` (default empty diagnostics) assert `log.runs[0].results` has length 0, `log.runs[0].tool.driver.rules` has length 18 (mirror the existing 18-rule assertion), and `validateSarif(await formatSarifReport(coreResult(), ts, 'D:/ws/proj')).valid === true`. Import `validateSarif` from '@workspace/test-util'.

FIX 5 -- add two asserts in normalize-options.spec.ts (siblings to the existing strict/failFast cases): given `format: 'json'`, `normalized.format` is 'json'; given no `format` key, `normalized.format` is 'human'. Use the existing `contextWithRoot('/ws')` + `TypecheckExecutorOptions` literal idiom.

TDD: write the four new assertions first, confirm they exercise real behavior (they should pass immediately since the production code already implements it -- these close COVERAGE gaps, not bugs), then confirm the whole unit tier stays green.
  </action>
  <verify>
    <automated>nx test angular-typechecker && nx typecheck angular-typechecker</automated>
  </verify>
  <done>json-report.spec.ts + sarif-report.spec.ts carry the file-set-position-absent case (JSON: file non-null, positions null; SARIF: location present, region absent, schema-valid, one-to-one length); sarif-report.spec.ts carries the empty-diagnostics case (results empty, 18 rules, schema-valid); normalize-options.spec.ts asserts format forwarded + defaulted. `nx test angular-typechecker` green with the new cases; `nx typecheck angular-typechecker` green (specs TYPE-CHECK -- the tsc gate vitest skips). Commit as `test(core): cover file-set position-absent diagnostics, empty SARIF, and format normalize`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove coverage-incomplete through --format json end-to-end (FIX 3)</name>
  <files>packages/angular-typechecker/src/cli/main.integration.spec.ts</files>
  <behavior>
    - Driving the EXISTING two-entry array [cleanTsConfig, emptyLeafTsConfig] through run() with `--format json` yields exit 1 AND a JSON payload whose `summary.success` is false AND `summary.outcome` is 'coverage-incomplete' -- the verdict a unit stub cannot fake, now proven over the real cold compiler through a machine format.
  </behavior>
  <action>
Add ONE new `describe`/`it` in main.integration.spec.ts immediately after the existing "REAL coverage-incomplete ... exit 1" scenario (~line 129). Reuse the already-declared `cleanTsConfig`, `emptyLeafTsConfig`, and `NO_COLOR_ENV` -- do NOT redeclare fixtures. Call `run(['-c', cleanTsConfig, '-c', emptyLeafTsConfig, '--format', 'json'], NO_COLOR_ENV)`. Assert `result.exitCode === 1`. Then `JSON.parse(result.stdout)` (assert it does not throw) and read the summary: `payload.summary.success === false` and `payload.summary.outcome === 'coverage-incomplete'`. Type the parsed payload with a minimal local interface (e.g. `{ summary: { success: boolean; outcome: string } }`) or an inline cast, matching the JsonPayload-interface style used in machine-reporters-json.integration.spec.ts -- keep it small. Add a short header note that this extends the sibling human-format coverage-incomplete case with its machine-format proof (the file's existing rendered-string discipline gains one JSON-parsing case by design).

This is the cold-compiler integration tier -- the run is slower than the unit specs; allow time.
  </action>
  <verify>
    <automated>nx integration angular-typechecker && nx typecheck angular-typechecker</automated>
  </verify>
  <done>main.integration.spec.ts asserts a real coverage-incomplete run through `--format json` returns exit 1 with summary.success false + summary.outcome 'coverage-incomplete', reusing the existing fixtures. `nx integration angular-typechecker` green (cold compiler); `nx typecheck angular-typechecker` green. Commit as `test(cli): prove coverage-incomplete through --format json end-to-end`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. All six changes are additive test assertions + comment corrections in an existing package; no new input path, no network, no runtime code path, no dependency install.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-m76-01 | Tampering | reporter/normalize source under test | low | accept | Tasks 2-3 are test-only; comment fixes (Task 1) touch no logic. Regression is caught by the existing `nx test` / `nx integration` / `nx typecheck` gates run per task. |
| T-m76-SC | Tampering | npm/pip/cargo installs | low | accept | NO package installs in this task (`validateSarif`/`redactVolatile` are existing @workspace/test-util helpers; ajv/ajv-formats are already ROOT devDeps). No supply-chain surface added. |
</threat_model>

<verification>
Fix-to-task coverage (all six review findings planned, zero unplanned):

| Fix | What | Task |
|-----|------|------|
| FIX 1 | executor.ts comment: scope JSON redundancy, note SARIF omits advisory meta | Task 1 |
| FIX 2 | render-report.ts JSDoc: drop landed-threading clause | Task 1 |
| FIX 3 | main.integration.spec.ts: coverage-incomplete through --format json | Task 3 |
| FIX 4 | json-report.spec.ts + sarif-report.spec.ts: file-set position-absent | Task 2 |
| FIX 5 | normalize-options.spec.ts: format forwarded + defaulted | Task 2 |
| FIX 6 | sarif-report.spec.ts: empty-diagnostics, 18 rules, schema-valid | Task 2 |

Final full-suite gate (repo "verify format + lint before release" rule -- run after all three tasks):
- `nx test angular-typechecker` (unit tier, FIX 4/5/6)
- `nx integration angular-typechecker` (cold-compiler tier, FIX 3)
- `nx typecheck angular-typechecker` (tsc --noEmit over tsconfig.spec/drift/tools -- specs TYPE-CHECK; vitest does NOT)
- `nx lint angular-typechecker` (maxWarnings:0)
- `nx format:check`
</verification>

<success_criteria>
- All six PR #47 findings addressed: 2 comment corrections + 4 test additions.
- Additive only: no production behavior changed, package.json version stays 0.2.2, no .planning/ source edits.
- 3 clean conventional-commit commits (docs(core), test(core), test(cli)) -- no GSD plan-id scopes.
- Full battery green: nx test + nx integration + nx typecheck + nx lint + nx format:check.
</success_criteria>

<output>
Create `.planning/quick/260719-m76-address-pr-47-review-findings-json-sarif/260719-m76-SUMMARY.md` when done.
</output>
