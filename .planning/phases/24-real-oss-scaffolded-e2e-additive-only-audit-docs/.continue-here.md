---
context: phase
phase: 24-real-oss-scaffolded-e2e-additive-only-audit-docs
task: 8
total_tasks: 9
status: in_progress
last_updated: 2026-07-11T10:38:15.751Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure this session.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Never self-certify a GSD post-execution gate inline** — verify/secure/validate/code-review MUST be reached by their dedicated agent (gsd-verifier / gsd-security-auditor / gsd-nyquist-auditor / gsd-code-reviewer), never authored by the orchestrator's own cross-referencing, EVEN when a workflow short-circuit (secure `threats_open:0`, validate no-gaps) would let the orchestrator write the artifact. `/gsd-extract-learnings` is the ONE inline exception (no agent exists). Mitigation: spawn the named agent, let it reach the verdict, orchestrator only routes + commits.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Inline self-certification of post-execution gates | This session the orchestrator wrote SECURITY.md and VALIDATION.md itself (from its own reading of the code review + verification) instead of spawning gsd-security-auditor / gsd-nyquist-auditor. The user rejected it as "marking its own homework" and required the dedicated agents. | blocking | Spawn the dedicated agent for verify/secure/validate/code-review; never derive the verdict inline. Codified in global CLAUDE.md (GSD region) + memory `use-gsd-agents-for-post-execution`. extract-learnings stays inline (no agent). |
| Auto-approving a human-only gate | ACV-01 is a real-clone gate; the npm publish / tag / GitHub environment approval are deployment gates. | advisory | Run ACV-01 autonomously (user authorized THAT), but STOP at the npm-publish/tag/deployment approval — always human-only ([[never-approve-github-deployments]]). |
| Using `grep` on Windows arm64 | `grep` is denied in this env; produces silent wrong results. | advisory | Use `git grep` (tracked) or `rg` (untracked / node_modules). MSYS tar/pack needs `/d/...` paths, not `D:/...`. |

<current_state>
Phase 24 is the FINAL phase of milestone v0.2.1 (Angular CLI workspace support). All 3 plans are executed and every automatable gate is DONE and independently agent-audited: code review (0 blockers, fixes applied), verify_phase_goal (`human_needed`, 0 gaps), secure-phase (SECURED, 0 open threats), validate-phase (NO GAPS, nyquist_compliant). Working tree is CLEAN. The ONLY thing left before phase completion is the ACV-01 real-clone gate — manual by design, but the user has authorized a fresh session to run it AUTONOMOUSLY.
</current_state>

<completed_work>

Completed (all committed):
- 24-01 (ACV-03 builder-over-BuilderContext gap-fill via TestingArchitectHost + ACP-02 additive-only audit + index.drift.ts barrel tripwire) — `7a17b46`
- 24-02 (ACD-01 README `## Angular CLI` + docs tripwire + prose CHANGELOG 0.2.1, no cut) — `d4b47c6`
- 24-03 (ACV-02 scaffolded ng-cli-e2e project + committed pinned Ng22 fixture + ACV-01 UAT doc) — `d391541`
- code review (gsd-code-reviewer) + WR-01/IN-01/IN-02 fixes re-verified — `57c6b7e`
- verify_phase_goal (gsd-verifier): human_needed, 0 gaps; 24-HUMAN-UAT.md — `ba23215`
- secure-phase (gsd-security-auditor): SECURED, threats_open:0 — `dc302c7`
- validate-phase (gsd-nyquist-auditor): NO GAPS, nyquist_compliant:true — `f065060`
- (meta, outside repo) global CLAUDE.md GSD rule added (agent-reviewed) + 2 memories saved.
</completed_work>

<remaining_work>

- Task 8 (NOW): Run ACV-01 both clones AUTONOMOUSLY per `24-ACV-01-UAT.md`, IN ORDER:
  1. `bluehalo/ngx-leaflet` @ `818e9ae55240b570397ede5a15cb4d466785abdc` (app `ngx-leaflet-demo` + lib `ngx-leaflet`)
  2. `realworld-angular/realworld-angular` @ `9e3528ff27bad5fedaefb879ccc4aaf4717b137b` (single app, app-only)
  For each: clone -> pack the SHIPPED dist tarball (`nx build` then `npm pack` the dist) -> `ng add angular-typechecker` from the tarball -> plant per-leaf errors (app TS2322, spec TS2345, lib TS2554) -> `ng run <project>:typecheck` -> assert planted codes + per-project scoping (no cross-project bleed) + a clean baseline exits 0 + no ERR_REQUIRE_ESM. Record pass/fail in `24-HUMAN-UAT.md`. On PASS both, update `24-VERIFICATION.md` status to `passed` (or re-run gsd-verifier).
- Task 9 (AFTER ACV-01 passes): `gsd-sdk query phase.complete 24` -> `/gsd-extract-learnings 24` (INLINE) + the global-learnings bridge (features.global_learnings=true) -> `/gsd-audit-milestone` -> `/gsd-complete-milestone v0.2.1`. Then STOP — the Release-PR + npm publish is human-gated.
</remaining_work>

<decisions_made>

- ACV-01 substrate = TWO on-stack Ng22 clones ORDERED (ngx-leaflet app+lib, then realworld-angular app-only); off-stack Ng21 dropped everywhere. User directive 2026-07-11.
- Post-execution gates run via dedicated agents (not inline) — see the blocking constraint above.
- extract-learnings + phase.complete deferred until ACV-01 closes the phase (CLAUDE.md: skip while unresolved human_needed).
- Executed sequentially on the MAIN tree (no worktree isolation); continue that if re-executing anything.
</decisions_made>

<blockers>
- ACV-01 needs two UNCOMMITTED OSS clones + the real `ng`/`npm` toolchain + network; each gate ~2-5 min. Reuse the GREEN committed e2e (`e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run.e2e.spec.ts`) as the reference for the exact planted-error anchors + assertions — it proves the identical `ng add`->`ng run` flow on a committed fixture. On-stack Angular 22 install needs NO `--legacy-peer-deps`.
</blockers>

## Required Reading (in order)
1. `.planning/phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-ACV-01-UAT.md` — the reproducible ACV-01 procedure for both clones (the task to run).
2. `.planning/phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-HUMAN-UAT.md` — where to record results (2 tests, pending).
3. `e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run.e2e.spec.ts` + `.../src/global-setup.ts` — the GREEN reference implementation of the same flow (planted-error anchors, per-project scoping asserts, Verdaccio publish-once, 127.0.0.1 SAFETY gate, `buildCleanEnv({stripAllNpmConfig:true})`).
4. `~/.claude/CLAUDE.md` `## GSD Workflow` region — the new "post-execution gates via dedicated agents" rule + the extract-learnings / auto_copy_learnings bridge rules for Task 9.
5. `AGENTS.md` — the Release-PR flow (Task 9's milestone close ends BEFORE the human-gated publish).

## Infrastructure State
- Working tree CLEAN on branch `gsd/v0.2.1-angular-cli-workspace-support`. All Phase 24 work committed.
- `packages/angular-typechecker/package.json` version = `0.2.0` (unchanged; ACP-02 additive-only holds; v0.3.0 NOT triggered). Do not bump it here — the version cut is the Release-PR.
- No running background services from this session.
- OSS clones are NOT present yet (uncommitted convention) — clone them fresh at the pinned SHAs.

<context>
The `--auto --chain` pipeline (discuss->plan->execute->gates) ran end-to-end and produced a clean, independently-audited phase. The user then paused specifically to hand the ACV-01 real-clone gate to a fresh-context session to run autonomously, before phase post-execution completion (extract-learnings) and milestone close. Treat ACV-01 as the gate to pass; the committed ACV-02 e2e is your working template for HOW. Do not touch production code — this phase adds none.
</context>

<next_action>
Start with: read `24-ACV-01-UAT.md` + `e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run.e2e.spec.ts`, then `git clone` `bluehalo/ngx-leaflet` and check out `818e9ae55240b570397ede5a15cb4d466785abdc`, and run ACV-01 gate #1 (pack the shipped dist tarball, `ng add`, plant app TS2322 + spec TS2345 + lib TS2554, `ng run <project>:typecheck`, assert per-project scoping + clean baseline). Then gate #2 (realworld-angular @9e3528f). Record both in `24-HUMAN-UAT.md`.
</next_action>
