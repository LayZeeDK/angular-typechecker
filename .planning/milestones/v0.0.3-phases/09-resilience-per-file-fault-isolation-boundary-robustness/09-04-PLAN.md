---
phase: 09-resilience-per-file-fault-isolation-boundary-robustness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/core/run-typecheck.ts
  - packages/angular-typechecker/src/core/infra-failure.spec.ts
  - packages/angular-typechecker/src/core/suppress-output-path.integration.spec.ts
autonomous: true
requirements: [RES-04]
tags: [angular, compiler-cli, readConfiguration, suppressOutputPathCheck, resilience]

must_haves:
  truths:
    - "runTypecheck passes { suppressOutputPathCheck: true } as the SECOND ARG to ng.readConfiguration(options.tsConfigPath, ...) (RES-04 / SC4 / D-09)."
    - "No output-path nuisance diagnostic (TS5055 / overwrite-class) surfaces in the type-only flow; verified safe under noEmit:true (the engine override already sets noEmit:true)."
    - "The placement is the readConfiguration second-arg seam (matches @angular/build angular-compilation.ts:51 @ v22.0.4); no shim change (the existingOptions overload is already declared)."
  artifacts:
    - path: "packages/angular-typechecker/src/core/run-typecheck.ts"
      provides: "readConfiguration called with { suppressOutputPathCheck: true } as the second arg (line 105)"
      contains: "suppressOutputPathCheck"
    - path: "packages/angular-typechecker/src/core/infra-failure.spec.ts"
      provides: "The readConfiguration-spy unit asserting the second-arg is { suppressOutputPathCheck: true } (deterministic Option a)"
      contains: "suppressOutputPathCheck"
    - path: "packages/angular-typechecker/src/core/suppress-output-path.integration.spec.ts"
      provides: "Real-compiler no-nuisance assertion: no TS5055/overwrite-class code surfaces under the no-emit flow"
      contains: "5055"
  key_links:
    - from: "run-typecheck.ts"
      to: "ng.readConfiguration"
      via: "second-arg existingOptions { suppressOutputPathCheck: true }"
      pattern: "readConfiguration\\(.*suppressOutputPathCheck"
    - from: "infra-failure.spec.ts"
      to: "the readConfiguration stub"
      via: "toHaveBeenCalledWith(path, { suppressOutputPathCheck: true })"
      pattern: "toHaveBeenCalledWith"
---

<objective>
RES-04: Pass `suppressOutputPathCheck: true` as the `existingOptions` second arg to `ng.readConfiguration(options.tsConfigPath, { suppressOutputPathCheck: true })` (`run-typecheck.ts:105`), matching `@angular/build` exactly (`angular-compilation.ts:51` @ v22.0.4), so output-path configuration nuisance errors never surface in the type-only flow.

Purpose: Defensive belt-and-suspenders parity with the build. RESEARCH RESOLVED the placement caveat: the output-path overwrite check fires in TypeScript's `verifyCompilerOptions()` at the END of `createProgram`, gated by `!options.noEmit && !options.suppressOutputPathCheck` (typescript.js:129892) — NOT in `readConfiguration`. So `noEmit:true` (already in the engine's emit-neutralizing override) ALONE suppresses it, and the second-arg placement is correct and early enough. The "too late in parsed.errors" hazard does NOT exist (Pitfall 3).
Output: The one-line `run-typecheck.ts` change + a deterministic `readConfiguration`-spy unit + a real-compiler no-nuisance integration assertion (RESEARCH Open Q1 Option a — deterministic).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-CONTEXT.md
@.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-RESEARCH.md
@.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-PATTERNS.md
@.planning/phases/08-correctness-completeness-fixes/08-CONTEXT.md

<interfaces>
<!-- The readConfiguration overload RES-04 uses — ALREADY declared on the shim. No shim change. -->
From packages/angular-typechecker/src/core/compiler-cli-types.ts (lines 154-158):
```ts
readConfiguration(
  project: string,
  existingOptions?: ts.CompilerOptions,
): ParsedConfiguration;
```
`ts.CompilerOptions` carries an index signature, so the extra `suppressOutputPathCheck` key type-checks. The current call at run-typecheck.ts:105 is `const parsed = ng.readConfiguration(options.tsConfigPath);` — RES-04 adds ONLY the second arg. The COR-01 500-scan (run-typecheck.ts:107-132) and the parsed.errors fold (run-typecheck.ts:134-137) immediately after MUST be left intact.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pass suppressOutputPathCheck to readConfiguration and add the deterministic spy unit</name>
  <files>packages/angular-typechecker/src/core/run-typecheck.ts, packages/angular-typechecker/src/core/infra-failure.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/run-typecheck.ts (lines 95-137 — the readConfiguration call at :105, the COR-01 500-scan and parsed.errors fold that follow and MUST stay intact)
    - packages/angular-typechecker/src/core/compiler-cli-types.ts (lines 154-158 — the readConfiguration(project, existingOptions?) overload; confirm no shim change needed)
    - packages/angular-typechecker/src/core/infra-failure.spec.ts (lines 1-80 — the vi.hoisted compilerCliStub.readConfiguration + vi.mock('./compiler-loader') idiom; the fakeProgram() helper at :67-73 for the non-infra path)
    - .planning/phases/08-correctness-completeness-fixes/08-CONTEXT.md (D-05/D-06 — the infra-vs-type / TypecheckInfrastructureError policy; RES-04 must NOT blur it: the COR-01 scan and the parsed.errors handling are untouched)
    - .planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-RESEARCH.md (Pitfall 3 — the placement caveat is RESOLVED; Open Q1 — pick deterministic Option a; "Code Examples — RES-04")
    - .planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-PATTERNS.md ("run-typecheck.ts (MODIFY, RES-04, line 105)" + "run-typecheck.spec.ts (EXTEND) + infra-failure.spec.ts (analog, RES-04)" — the spy assertion shape)
  </read_first>
  <behavior>
    - Test 1 (RES-04 spy, deterministic): when `runTypecheck` runs against a stubbed compiler-loader whose `performCompilation` returns a `fakeProgram()` (non-infra path completes), `compilerCliStub.readConfiguration` was called with `('/virtual/tsconfig.json', { suppressOutputPathCheck: true })` (`toHaveBeenCalledWith`).
    - Test 2 (regression): the existing infra-failure cases (code-500 re-throw; non-500 TS2322 NOT re-thrown) still pass — the second-arg addition does not change the COR-01 500-scan or the parsed.errors handling.
  </behavior>
  <action>
    In `run-typecheck.ts`, change the call at line 105 from `const parsed = ng.readConfiguration(options.tsConfigPath);` to pass the second `existingOptions` arg `{ suppressOutputPathCheck: true }`. Add a brief comment: RES-04 / D-09 — matches `@angular/build` (`angular-compilation.ts:51` @ v22.0.4); `noEmit:true` already suppresses the output-path check (Pitfall 3 — the check is in `verifyCompilerOptions` gated by `!noEmit && !suppressOutputPathCheck`), this is belt-and-suspenders parity. Do NOT touch the COR-01 500-scan or the `parsed.errors` fold that follow. Follow CLAUDE.md JS/TS style.

    Extend `infra-failure.spec.ts` (it already mocks `./compiler-loader` with a `compilerCliStub.readConfiguration` spy and has the `fakeProgram()` helper): add an `it('RES-04: passes { suppressOutputPathCheck: true } to readConfiguration', ...)` that sets `compilerCliStub.performCompilation` to return `{ diagnostics: [], program: fakeProgram() }`, calls `await runTypecheck({ tsConfigPath: '/virtual/tsconfig.json' })`, and asserts `expect(compilerCliStub.readConfiguration).toHaveBeenCalledWith('/virtual/tsconfig.json', { suppressOutputPathCheck: true })`. Reset the spy in `beforeEach` consistent with the existing block.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- infra-failure.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `run-typecheck.ts` line 105 calls `ng.readConfiguration(options.tsConfigPath, { suppressOutputPathCheck: true })`.
    - The COR-01 500-scan (`parsed.errors.find(d => d.code === ng.UNKNOWN_ERROR_CODE)`) and the `configDiagnostics = [...parsed.errors]` fold immediately after are unchanged.
    - `infra-failure.spec.ts` contains a RES-04 test asserting `readConfiguration` was called with `('/virtual/tsconfig.json', { suppressOutputPathCheck: true })`.
    - `npx nx test angular-typechecker -- infra-failure.spec.ts` exits 0 (RES-04 case + the pre-existing infra-failure cases).
    - No `compiler-cli-types.ts` change (the overload already exists); `npx nx build angular-typechecker` exits 0 (the build is the drift guard — the extra key type-checks via the index signature).
  </acceptance_criteria>
  <done>runTypecheck passes suppressOutputPathCheck:true to readConfiguration, proven deterministically by a readConfiguration spy, with the Phase 8 infra-vs-type handling untouched and the build green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the real-compiler no-nuisance integration assertion</name>
  <files>packages/angular-typechecker/src/core/suppress-output-path.integration.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/no-emit-override.integration.spec.ts (lines 1-55 — the packageRoot/workspaceRoot scaffolding, the `result.diagnostics.map(d => d.code)` + `.not.toContain(<raw TS code>)` shape, the RAW-TS-code convention)
    - fixtures/composite-triangle/tsconfig.json (the existing emit-option-collision fixture: composite:true + declarationMap:true + emitDeclarationOnly:true — the closest in-repo config that would, without neutralization, drive output/emit option diagnostics)
    - packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts (lines 1-65 — the real-compiler runTypecheck-against-fixture idiom; TS codes raw, NG() only for Angular extended)
    - .planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-RESEARCH.md (Open Q1 Option a — "an integration assertion that a config with a colliding outDir/rootDir shape reports NO TS5055/overwrite-class diagnostic"; Pitfall 4 — TS codes RAW; Pitfall 5 — the cold-compiler timeout is inherited, do NOT add a per-file timeout)
    - .planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-PATTERNS.md ("RES-04 integration (no-nuisance ...)" — the `.not.toContain(TS5055)` shape)
  </read_first>
  <action>
    Create `packages/angular-typechecker/src/core/suppress-output-path.integration.spec.ts` using the real-compiler integration scaffolding (`packageRoot`/`workspaceRoot` + a fixture `tsconfig` path; reuse the existing `fixtures/composite-triangle/tsconfig.json` which already carries the emit-option-collision shape — do NOT create a new fixture unless composite-triangle cannot exercise the assertion). Define `const TS5055 = 5055;` (RAW TS code; the "Cannot write file ... would overwrite input file" / output-path-overwrite class — assert the overwrite-class codes the research names: TS5055 and, if surfaced in this flow, TS5042/TS5056-class are NOT present). Call `await runTypecheck({ tsConfigPath: compositeTriangleTsConfig })`, map `result.diagnostics` to `.code`, and assert `expect(codes).not.toContain(TS5055)` — proving no output-path nuisance surfaces in the type-only (`noEmit:true` + `suppressOutputPathCheck:true`) flow. Do NOT add a per-file `testTimeout` (inherited 30000ms from `vitest.config.mts`). ASCII-only.

    If, while authoring, the executor finds `composite-triangle` cannot exercise any output-path/overwrite-class code path at all (the flow never produces TS5055-class regardless), document that in the SUMMARY and keep the assertion as the safe-under-noEmit proof (the absence holds and is the SC4 evidence per RESEARCH A3 — `noEmit:true` is the primary suppressor; `suppressOutputPathCheck` is the parity belt). Do NOT temporarily unset `noEmit` in the shipped spec (the deterministic Option a is preferred over the probe Option b).
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- suppress-output-path.integration.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `suppress-output-path.integration.spec.ts` exists, calls `runTypecheck` against a real fixture, and asserts `result.diagnostics` codes do NOT contain the output-path overwrite-class code (TS5055, raw positive).
    - No new fixture is created unless `composite-triangle` provably cannot exercise the assertion (documented in SUMMARY if so).
    - No per-file `testTimeout` is added (inherited from `vitest.config.mts`).
    - `npx nx test angular-typechecker -- suppress-output-path.integration.spec.ts` exits 0.
  </acceptance_criteria>
  <done>A real-compiler integration spec proves no output-path nuisance (TS5055/overwrite-class) diagnostic surfaces in the no-emit type-only flow — the safe-under-noEmit evidence for SC4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer tsconfig -> config resolution / program creation | The consumer's tsconfig (outDir/rootDir/composite shape) is untrusted input; an output-path config nuisance could be mis-counted as a type error (false fail) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Repudiation (mis-classification) | run-typecheck.ts readConfiguration call (line 105) | mitigate | RES-04 / D-09: pass `suppressOutputPathCheck: true` to `readConfiguration` (+ the existing `noEmit:true` override which already gates the TS `verifyCompilerOptions` output-path check) so an output-path config nuisance never surfaces as a type error in the type-only verdict. Proven by the deterministic readConfiguration spy + the no-nuisance integration assertion. |
| T-09-04 | Tampering with the verdict's meaning | run-typecheck.ts infra-vs-type policy (COR-01 500-scan, lines 107-137) | mitigate | D-05 / Phase 8 policy preserved: RES-04 adds ONLY the second arg; the COR-01 500-scan and the `parsed.errors` fold are untouched, so a config-resolution infra crash still re-throws `TypecheckInfrastructureError` and is never reclassified. Regression-covered by the retained infra-failure cases. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH "Package Legitimacy Audit: Not applicable"). |
</threat_model>

<verification>
- `npx nx test angular-typechecker -- infra-failure.spec.ts` exits 0 (RES-04 spy + retained infra-failure cases).
- `npx nx test angular-typechecker -- suppress-output-path.integration.spec.ts` exits 0 (no TS5055/overwrite-class code surfaces).
- `npx nx build angular-typechecker` exits 0 (drift guard — the extra option key type-checks via the index signature; no shim change).
- Full suite green: `npx nx test angular-typechecker`.
</verification>

<success_criteria>
- SC4 satisfied: the no-emit override path passes `suppressOutputPathCheck: true` to `readConfiguration`; output-path nuisance errors never surface (verified safe under `noEmit:true`).
- The Phase 8 infra-vs-type policy is preserved (no change to the 500-scan / parsed.errors handling).
- No package added; no shim change.
</success_criteria>

<output>
Create `.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-04-SUMMARY.md` when done.
</output>
