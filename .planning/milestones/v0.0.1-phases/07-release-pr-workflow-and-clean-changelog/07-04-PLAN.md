---
phase: 07-release-pr-workflow-and-clean-changelog
plan: 04
type: execute
wave: 2
depends_on: [07-02]
files_modified: []
autonomous: false
requirements: [REL-02]
must_haves:
  truths:
    - 'The Default-branch ruleset (18229122) is enforcement:active with strict:false, empty bypass, merge:[merge], and the 3 required checks (ci + 2 CodeQL)'
    - 'The temporary v0.0.1 ruleset (18229088) is DELETED (returns 404)'
    - 'The Release-tag ruleset (18229053) is RETAINED (still active, target tag)'
    - 'No unprotected window opens during the switch (PUT-enable FIRST, THEN delete)'
  artifacts: []
  key_links:
    - from: 'Default-branch ruleset 18229122 required_status_checks'
      to: '.github/workflows/ci.yml `ci` aggregate gate (Plan 02)'
      via: "required check context 'ci' (integration_id 15368)"
      pattern: 'ci'
    - from: 'Release-tag ruleset 18229053'
      to: '.github/workflows/release.yml tag-triggered OIDC publish'
      via: 'governs angular-typechecker@* tag pushes (owner+DeployKey bypass)'
      pattern: 'angular-typechecker@'
---

<objective>
Switch `main` to PR-mode branch protection (REL-02, D-06/D-09): ENABLE the staged "Default branch" ruleset (require PR + the `ci` status check + 2 CodeQL checks, force-push/deletion blocked, empty bypass, merge-commit-only, strict:false), DELETE the temporary "v0.0.1" ruleset, and RETAIN the "Release tag" ruleset.

This is a LIVE, outward-facing GitHub configuration change on the real repo, performed via `gh api` PUT/DELETE (Runbook 2). It is IRREVERSIBLE in the sense that it changes how `main` accepts changes for everyone (including the owner). Therefore this plan is `autonomous: false` -- the executor PAUSES at a blocking human checkpoint so the maintainer reviews and runs (or approves) the live ops, mirroring Phase 5.1's human-gated publish.

SAFE ORDER (load-bearing, D-09): PUT-enable 18229122 FIRST (atomic disabled->active -- never an unprotected window; deletion + non_fast_forward stay continuously asserted by 18229122 during the swap), VERIFY the enable landed, THEN DELETE 18229088. No deadlock: `ci` is already in the required list, is green on `main`, and runs on PRs (Plan 02 keeps the `ci` name + makes planning-only PRs reportable).

Purpose: full PR-mode protection is the REL-02 deliverable that makes the Release-PR flow enforced rather than conventional. Depends on Plan 02 (the ci.yml path-aware skip must be correct first, so the required `ci` check stays valid and planning-only PRs do not deadlock once `main` is PR-only).
Output: live ruleset state changed; post-switch `gh api` verification reads captured (the operational verification recorded in VERIFICATION.md, like Phase 6 SC3).

This plan does NOT cut a release or push a tag. The ruleset switch is the only live op.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-release-pr-workflow-and-clean-changelog/07-CONTEXT.md
@.planning/phases/07-release-pr-workflow-and-clean-changelog/07-RESEARCH.md
@.planning/phases/07-release-pr-workflow-and-clean-changelog/07-02-PLAN.md

<interfaces>
<!-- LIVE GitHub config (NOT files). IDs verified live this session (RESEARCH Sources). -->
Default-branch ruleset id 18229122 (target branch, ~DEFAULT_BRANCH) -- PUT to set enforcement:active + strict:false
v0.0.1 ruleset id 18229088 (target branch) -- DELETE
Release-tag ruleset id 18229053 (target tag) -- RETAIN (governs angular-typechecker@* tag pushes)

<!-- The full-replacement PUT body for 18229122 (RESEARCH Runbook 2): -->

name "Default branch", target "branch", enforcement "active", bypass_actors []
conditions.ref_name.include ["~DEFAULT_BRANCH"]
rules:

- deletion
- non_fast_forward
- pull_request { required_approving_review_count:0, required_review_thread_resolution:true, allowed_merge_methods:["merge"] }
- required_status_checks { strict_required_status_checks_policy:false, required_status_checks:[
  {context:"Analyze (actions)", integration_id:15368},
  {context:"Analyze (javascript-typescript)", integration_id:15368},
  {context:"ci", integration_id:15368} ] }

<!-- Repo: LayZeeDK/angular-typechecker (exact casing). gh api verified working this session. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify the live ruleset pre-state and build the PUT body</name>
  <files>(none -- read-only gh api + a temp JSON body in the scratchpad)</files>
  <read_first>
    - .planning/phases/07-release-pr-workflow-and-clean-changelog/07-RESEARCH.md ("### Runbook 2: gh-api ruleset switch (REL-02)" -- the exact PUT body, the verify --jq, the DELETE, the final verify, and the D-12 recovery; "Valid until" note -- re-verify the live ruleset state at plan time)
    - .planning/phases/07-release-pr-workflow-and-clean-changelog/07-CONTEXT.md (<decisions> D-06/D-09/D-10/D-11 -- empty bypass, full-replacement PUT, strict:false, approvals 0)
    - .github/workflows/ci.yml (confirm the `ci` job name is intact after Plan 02 -- the required-check context must match)
  </read_first>
  <action>
    Re-read the three live rulesets to confirm they are still in the expected pre-state (the SHA/IDs may drift per the RESEARCH "Valid until" note): `gh api repos/LayZeeDK/angular-typechecker/rulesets --jq '.[] | {id, name, enforcement, target}'` -- expect 18229122 (Default branch, disabled, branch), 18229088 (v0.0.1, branch), 18229053 (Release tag, active, tag). Confirm 18229122 already lists all three required checks (the maintainer added `ci` during discussion) and read its current enforcement + strict value: `gh api repos/LayZeeDK/angular-typechecker/rulesets/18229122 --jq '{enforcement, strict: (.rules[]|select(.type=="required_status_checks").parameters.strict_required_status_checks_policy), checks: [(.rules[]|select(.type=="required_status_checks").parameters.required_status_checks[].context)], bypass: .bypass_actors}'`. Write the FULL-REPLACEMENT PUT body for 18229122 to the scratchpad as JSON exactly per Runbook 2 (enforcement:"active", bypass_actors:[], strict_required_status_checks_policy:false, the 3 checks each with integration_id 15368, pull_request approvals 0 + required_review_thread_resolution:true + allowed_merge_methods:["merge"], deletion + non_fast_forward rules). Confirm `ci` is the exact context name (matches the Plan 02 `ci` job name). Do NOT run the PUT/DELETE in this task -- only verify + stage the body. If any ID returns 404 or a different pre-state, STOP and surface the drift to the maintainer before proceeding (do not improvise an ID).
  </action>
  <verify>
    <automated>gh api repos/LayZeeDK/angular-typechecker/rulesets --jq '.[] | {id, name, enforcement, target}'</automated>
  </verify>
  <acceptance_criteria>
    - `gh api .../rulesets/18229122 --jq '.target'` returns `branch`; `.enforcement` is `disabled` (pre-state)
    - `gh api .../rulesets/18229122` lists all three required checks (`ci`, `Analyze (actions)`, `Analyze (javascript-typescript)`)
    - `gh api .../rulesets/18229088 --jq '.name'` returns `v0.0.1` (still present, pre-delete)
    - `gh api .../rulesets/18229053 --jq '{name, target, enforcement}'` returns the active tag ruleset
    - A full-replacement PUT body JSON is staged in the scratchpad with enforcement:active, bypass:[], strict:false, the 3 checks, merge:["merge"]
    - The `ci` context in the body exactly matches the ci.yml `ci` job name (Plan 02)
  </acceptance_criteria>
  <done>The three rulesets are confirmed in the expected pre-state; the full-replacement PUT body for 18229122 is staged; any ID drift is surfaced (not improvised).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: HUMAN-GATED live ruleset switch (PUT-enable 18229122, then DELETE 18229088)</name>
  <files>(none -- live GitHub config via gh api; no repo file changes)</files>
  <read_first>
    - .planning/phases/07-release-pr-workflow-and-clean-changelog/07-RESEARCH.md ("### Runbook 2" steps 1-4 + the D-12 lockout recovery)
    - the scratchpad PUT body staged in Task 1
  </read_first>
  <action>
    PAUSE for the maintainer. This is a blocking human checkpoint -- the executor presents the staged PUT body and the ordered `gh api` commands and waits for the maintainer to review and run (or explicitly approve running) them in the SAFE order, then captures the verification outputs. Do NOT run the PUT/DELETE autonomously. Execute (or have the maintainer execute) exactly: (1) PUT-enable 18229122 with the staged body; (2) VERIFY the enable landed (enforcement active, strict false, 3 checks, empty bypass, merge ["merge"]); (3) only after the verify passes, DELETE 18229088; (4) VERIFY final state (18229122 active branch, 18229053 active tag, 18229088 -> 404). On a `gh api` auth error, authenticate and retry (dynamic auth gate, not a plan change). Capture the step-2 and step-4 outputs verbatim into the SUMMARY (they feed VERIFICATION.md like Phase 6 SC3).
  </action>
  <what-built>
    Plan 02 reworked ci.yml so the required `ci` check stays valid and planning-only PRs do not deadlock. Task 1 re-verified the live pre-state and staged the full-replacement PUT body for the Default-branch ruleset (18229122). The actual live switch is an IRREVERSIBLE, repo-wide configuration change (even the owner loses direct-push to `main`), so it pauses here for the maintainer to review and run (or approve running) the `gh api` ops in the SAFE order.
  </what-built>
  <how-to-verify>
    Run these in order (RESEARCH Runbook 2). The maintainer should review the staged PUT body first.

    1. PUT-ENABLE FIRST (atomic disabled->active; never an unprotected window):
       `gh api --method PUT repos/LayZeeDK/angular-typechecker/rulesets/18229122 --input <staged-body.json>`

    2. VERIFY the enable landed BEFORE deleting v0.0.1:
       `gh api repos/LayZeeDK/angular-typechecker/rulesets/18229122 --jq '{enforcement, strict: (.rules[]|select(.type=="required_status_checks").parameters.strict_required_status_checks_policy), checks: [(.rules[]|select(.type=="required_status_checks").parameters.required_status_checks[].context)], bypass: .bypass_actors, merge: (.rules[]|select(.type=="pull_request").parameters.allowed_merge_methods)}'`
       Expect: enforcement "active", strict false, checks [Analyze (actions), Analyze (javascript-typescript), ci], bypass [], merge ["merge"].

    3. DELETE the now-redundant v0.0.1 ruleset (only carried deletion + non_fast_forward, both re-asserted by 18229122):
       `gh api --method DELETE repos/LayZeeDK/angular-typechecker/rulesets/18229088`

    4. VERIFY final state:
       `gh api repos/LayZeeDK/angular-typechecker/rulesets --jq '.[] | {id, name, enforcement, target}'`
       Expect: 18229122 Default branch active branch ; 18229053 Release tag active tag ; (18229088 GONE).
       `gh api repos/LayZeeDK/angular-typechecker/rulesets/18229088` -> 404.

    If `gh api` returns an auth error, authenticate (`gh auth login` / `gh auth status`) and retry -- this is a dynamic auth gate, not a plan change.

    LOCKOUT RECOVERY (D-12) if `ci` ever blocks the merge button: admins can EDIT (not bypass) the ruleset -- `gh api --method PUT .../rulesets/18229122 --input <(jq '.enforcement="disabled"' <staged-body.json>)`, push the fix, then re-enable. Prefer the enforcement toggle over a standing bypass actor.

    Capture the step-2 and step-4 outputs verbatim into the SUMMARY (and they feed VERIFICATION.md, like Phase 6 SC3).

  </how-to-verify>
  <verify>
    <automated>gh api repos/LayZeeDK/angular-typechecker/rulesets/18229122 --jq .enforcement</automated>
  </verify>
  <acceptance_criteria>
    - `gh api .../rulesets/18229122 --jq .enforcement` returns `active`
    - `gh api .../rulesets/18229122` shows strict_required_status_checks_policy false, bypass_actors [], allowed_merge_methods ["merge"], and the 3 required checks (ci + 2 CodeQL)
    - `gh api .../rulesets/18229088` returns 404 (deleted)
    - `gh api .../rulesets/18229053 --jq '{name,target,enforcement}'` still returns the active tag ruleset (retained)
    - The PUT (enable) was run and verified BEFORE the DELETE (safe order, no unprotected window)
  </acceptance_criteria>
  <done>18229122 is active (strict:false, empty bypass, merge:[merge], 3 checks), 18229088 is deleted (404), 18229053 is retained; the enable preceded the delete; step-2/step-4 outputs captured in the SUMMARY.</done>
  <resume-signal>Type "approved" after the ruleset is active + verified and v0.0.1 is deleted (paste the step-2 and step-4 gh api outputs), or describe issues (e.g. an ID drifted, an auth error, or a check-name mismatch).</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                                                                                                                                            |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| maintainer -> live GitHub config | The `gh api` PUT/DELETE changes repo-wide branch protection. A wrong order or a wrong body could open an unprotected window or block all merges.                                                       |
| branch ruleset vs tag ruleset    | The branch ruleset (empty bypass) gates `main`; the SEPARATE Release-tag ruleset gates `angular-typechecker@*` tag pushes. The empty branch bypass must NOT touch tag pushes (so releases still work). |

## STRIDE Threat Register

| Threat ID | Category                 | Component                            | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                |
| --------- | ------------------------ | ------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-07-11   | Tampering / Elevation    | ruleset switch order                 | mitigate    | PUT-enable 18229122 FIRST (atomic disabled->active), verify, THEN delete 18229088 (D-09 safe order); deletion + non_fast_forward stay asserted by 18229122 throughout -- never an unprotected window.                                                                                                          |
| T-07-12   | Elevation of Privilege   | manual-tag publish path              | mitigate    | Tag pushes are governed by the RETAINED Release-tag ruleset (18229053, owner+DeployKey bypass), NOT the branch ruleset; the empty branch bypass does not touch tag pushes; release.yml's `if: startsWith(github.ref,'refs/tags/angular-typechecker@')` job gate is unchanged. No publish bypass re-introduced. |
| T-07-13   | Denial of Service (self) | empty-bypass + non-reporting ci      | mitigate    | D-12 recovery: admins EDIT the ruleset (enforcement:disabled), push the fix, re-enable -- documented in the checkpoint + (Plan 03) AGENTS.md; no standing bypass actor needed. Plan 02 keeps `ci` reportable on planning-only PRs so the common case does not deadlock.                                        |
| T-07-14   | Spoofing / Elevation     | new long-lived secret for the switch | accept      | No secret introduced -- the switch uses the maintainer's interactive `gh` auth (human-gated), not a stored PAT/App.                                                                                                                                                                                            |
| T-07-SC   | Tampering                | npm/pip/cargo installs               | accept      | No package-manager install (live GitHub config op only). No legitimacy checkpoint required.                                                                                                                                                                                                                    |

</threat_model>

<verification>
- Post-switch `gh api` reads (checkpoint step 2 + step 4) confirm: 18229122 active + strict:false + empty bypass + merge:["merge"] + 3 checks; 18229088 -> 404; 18229053 retained active tag ruleset. These are ONE-TIME operational verifications recorded in VERIFICATION.md (like Phase 6 SC3), NOT repeatable CI assertions.
- The live skip-gate proof (planning-only PR skips matrix yet `ci` green; code PR runs matrix) is a separate draft-PR operational verification (REL-02 DX) captured at phase verification, not in this plan's tasks.
</verification>

<success_criteria>

- The Default-branch ruleset (18229122) is enforcement:active with strict:false, empty bypass, merge-commit-only, and the 3 required checks (ci + 2 CodeQL) -- full PR-mode protection on `main` (REL-02, D-06/D-09/D-10).
- The temporary v0.0.1 ruleset (18229088) is deleted; the Release-tag ruleset (18229053) is retained.
- The switch was performed in the safe order (enable-then-delete) behind a blocking human gate; the post-switch verification reads are captured.
  </success_criteria>

<output>
Create `.planning/phases/07-release-pr-workflow-and-clean-changelog/07-04-SUMMARY.md` when done.
</output>
