---
phase: 09-resilience-per-file-fault-isolation-boundary-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fixtures/fault-isolation/tcb-poison.component.ts
  - fixtures/fault-isolation/tcb-poison.component.html
  - fixtures/fault-isolation/survivor.component.ts
  - fixtures/fault-isolation/survivor.component.html
  - fixtures/fault-isolation/tsconfig.app.json
  - packages/angular-typechecker/src/core/res-01-spike.probe.spec.ts
  - .planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-RES-01-SPIKE.md
autonomous: true
requirements: [RES-01]
tags: [angular, compiler-cli, diagnostics, spike]

must_haves:
  truths:
    - 'A recorded GO decision (SIMPLE | HYBRID) on the per-file isolation shape exists as a durable artifact the verifier can read (RES-01 / SC1 / D-01).'
    - 'The decision states whether any Angular non-template diagnostic (traitCompiler / checkForPrivateExports) is file-less in the no-emit path, with the empirical evidence that justified it (D-02).'
    - 'SIMPLE is recorded ONLY with positive proof that the per-file union equals the whole-program non-template set; otherwise HYBRID (D-03).'
    - 'The multi-file fault-isolation fixture exists and produces (a) a TCB-phase IMPORT_GENERATION_FAILURE Fatal from the poison component and (b) a plain template error on the survivor component (Pitfall 2).'
  artifacts:
    - path: '.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-RES-01-SPIKE.md'
      provides: 'The recorded GO decision artifact (SIMPLE|HYBRID + fixture + method + empirical result + v22.0.4 citations)'
      contains: 'GO DECISION'
    - path: 'fixtures/fault-isolation/tcb-poison.component.ts'
      provides: 'Component A: template references a non-exported/local-only symbol -> IMPORT_GENERATION_FAILURE Fatal during TCB generation'
    - path: 'fixtures/fault-isolation/survivor.component.ts'
      provides: 'Component B: a plain template error (NG8109 / TS2322) that vanishes pre-RES-02 and survives post-RES-02'
    - path: 'fixtures/fault-isolation/tsconfig.app.json'
      provides: 'strictTemplates:true + noEmit:true config listing both components in files[]'
    - path: 'packages/angular-typechecker/src/core/res-01-spike.probe.spec.ts'
      provides: 'Throwaway probe that inspects d.file on the whole-program non-template set vs the per-file union'
  key_links:
    - from: 'res-01-spike.probe.spec.ts'
      to: 'fixtures/fault-isolation/tsconfig.app.json'
      via: 'runTypecheck + live api.Program against the fixture'
      pattern: 'fault-isolation'
    - from: '09-RES-01-SPIKE.md'
      to: 'fixtures/fault-isolation/'
      via: 'the recorded decision cites the fixture it exercised'
      pattern: 'fault-isolation'
---

<objective>
RES-01 GATE spike: empirically settle the per-file isolation SHAPE (SIMPLE vs HYBRID) before any RES-02 isolation code is written, and record a durable GO artifact the phase verifier checks.

The one load-bearing open question (PRIOR-ART #3 / ENGINE-REF Open Q3): are any Angular non-template diagnostics (`traitCompiler.diagnostics` / `checkForPrivateExports`) file-less (`d.file === undefined`) in the no-emit path? `NgCompiler.getDiagnosticsForFile` filters the non-template set by `d.file === file` (`compiler.ts:618` @ v22.0.4), so a naive per-file loop would silently DROP any file-less non-template diagnostic. SIMPLE is valid ONLY if the spike POSITIVELY proves none exist; inconclusive defaults to HYBRID (D-02/D-03).

Purpose: Gate RES-02. RES-02 (plan 09-02) does NOT start until this returns a recorded GO. The probe is THROWAWAY code; the deliverable is the recorded decision + the reusable fault-isolation fixture (which RES-02 also consumes).
Output: `fixtures/fault-isolation/` (multi-file fixture), a throwaway `res-01-spike.probe.spec.ts`, and the committed `09-RES-01-SPIKE.md` GO artifact.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-CONTEXT.md
@.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-RESEARCH.md
@.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-PATTERNS.md
@.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the fault-isolation multi-file fixture</name>
  <files>fixtures/fault-isolation/tcb-poison.component.ts, fixtures/fault-isolation/tcb-poison.component.html, fixtures/fault-isolation/survivor.component.ts, fixtures/fault-isolation/survivor.component.html, fixtures/fault-isolation/tsconfig.app.json</files>
  <read_first>
    - fixtures/gate-b-error/error.component.ts (the NG8109+TS2322-in-one-component model + the MANDATORY "OUT OF the project graph / errors ARE the input / do NOT add @ts-nocheck" comment block — copy verbatim shape from lines 3-7)
    - fixtures/gate-b-error/error.component.html (the external-template convention)
    - fixtures/gate-b-error/tsconfig.app.json (extends ../../tsconfig.base.json; noEmit:true; strictTemplates:true; files[])
    - fixtures/sibling-import/ (multi-component fixture + paths-alias convention, for the multi-file layout)
    - .planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-RESEARCH.md (Pitfall 2 — the TCB-vs-analysis Fatal distinction; the IMPORT_GENERATION_FAILURE trigger; Assumptions Log A1)
    - .planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-PATTERNS.md ("fixtures/fault-isolation/ (NEW dir)" section — the exact tsconfig + component conventions)
  </read_first>
  <action>
    Create `fixtures/fault-isolation/` at the WORKSPACE ROOT (not under the package — fixtures live at `<workspaceRoot>/fixtures/<name>/`, kept out of the plugin build by `tsconfig.lib.json`'s `include: ["src/**/*.ts"]` scope).

    Component A `tcb-poison.component.ts` (+ `tcb-poison.component.html`): a standalone `@Component` whose TEMPLATE references a non-exported / local-only symbol so the compiler raises `IMPORT_GENERATION_FAILURE` (a `FatalDiagnosticError`) during TCB GENERATION — NOT during analysis (Pitfall 2: an analysis-phase Fatal lands in `traitCompiler.diagnostics`/the non-template set and would NOT exercise `getDiagnosticsForFile`'s per-file template try/catch, so it would not prove isolation). Construct it so the poison diagnostic is produced by the TEMPLATE path. The executor MUST VERIFY this during Task 2 (the poison code is ABSENT from the whole-program non-template set — see A1).

    Component B `survivor.component.ts` (+ `survivor.component.html`): a PLAIN template error using the established `gate-b-error` model — an interpolated un-invoked `signal` (`status = signal('ready')` + `<p>{{ status }}</p>` -> NG8109) and/or a template-bound TS2322. This is the diagnostic that vanishes today (whole-program early-return) and survives post-RES-02.

    Every component file carries the MANDATORY deliberate-error comment block (verbatim shape from `gate-b-error/error.component.ts:3-7`): OUT OF the project graph, kept out of the plugin build by the `include` scope, do NOT add `@ts-nocheck` — the errors ARE the input.

    `tsconfig.app.json`: copy `fixtures/gate-b-error/tsconfig.app.json` — `extends: "../../tsconfig.base.json"`, `compilerOptions: { noEmit:true, strict:true, ... }`, `angularCompilerOptions: { strictTemplates: true }`, `files: ["tcb-poison.component.ts", "survivor.component.ts"]`. ASCII-only; `"singleQuote"` is for code, JSON uses double quotes.

  </action>
  <verify>
    <automated>test -f fixtures/fault-isolation/tcb-poison.component.ts && test -f fixtures/fault-isolation/survivor.component.ts && test -f fixtures/fault-isolation/tsconfig.app.json && echo FIXTURE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `fixtures/fault-isolation/` contains both components (+ their `.html` if external-template), and `tsconfig.app.json` lists BOTH in `files[]`.
    - `tsconfig.app.json` extends `../../tsconfig.base.json`, sets `noEmit: true`, and sets `angularCompilerOptions.strictTemplates: true`.
    - Each `*.component.ts` carries the deliberate-error comment block; NONE contains `@ts-nocheck`.
    - The poison component's offending reference is in the TEMPLATE (drives `IMPORT_GENERATION_FAILURE` during TCB generation), not an analysis-phase decorator/initializer error.
  </acceptance_criteria>
  <done>The fault-isolation fixture exists at the workspace root with a TCB-poison component A and a plain-template-error survivor component B, both listed in a strictTemplates+noEmit tsconfig.</done>
</task>

<task type="auto">
  <name>Task 2: Probe the live api.Program for file-less non-template diagnostics and record the GO decision</name>
  <files>packages/angular-typechecker/src/core/res-01-spike.probe.spec.ts, .planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-RES-01-SPIKE.md</files>
  <read_first>
    - packages/angular-typechecker/src/core/run-typecheck.ts (lines 95-233 — how the engine loads the compiler-cli ESM, calls readConfiguration + performCompilation with gatherAllDiagnostics, and reads result.program / getTsProgram())
    - packages/angular-typechecker/src/core/global-diagnostics.integration.spec.ts (the single-fixture real-compiler spec scaffolding: packageRoot/workspaceRoot/fixtureTsConfig + describe/it)
    - packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts (the NG() helper at :17, the diagnosticsOnFile helper at :54-65, the real-compiler shape)
    - packages/angular-typechecker/src/core/compiler-cli-types.ts (lines 60-80 — the getNgSemanticDiagnostics(fileName?) overload; lines 154-165 — the CompilerCli surface the probe reaches via loadCompilerCli)
    - .planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-RESEARCH.md (Pattern 3 — getNonTemplateDiagnostics = traitCompiler.diagnostics + checkForPrivateExports filtered by d.file === file; Pitfall 1 — absence-of-evidence; Open Q2 — the GO artifact shape; Assumptions A1/A2)
    - .planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-PATTERNS.md ("RES-01 spike probe" section — the throwaway probe scaffolding)
  </read_first>
  <action>
    Create THROWAWAY probe `packages/angular-typechecker/src/core/res-01-spike.probe.spec.ts` (NOT shipped engine code; it is removed/ignored after the decision is recorded — `tsconfig.lib.json` already excludes spec files from the build). Reuse the integration-spec path scaffolding (`packageRoot`/`workspaceRoot` + `fixtures/fault-isolation/tsconfig.app.json`) and the `NG()` negative-code helper.

    The probe must, against the live `api.Program` produced for the fault-isolation fixture: (1) gather the WHOLE-PROGRAM Angular set via `program.getNgSemanticDiagnostics()` (no fileName) and inspect each diagnostic's `.file` — enumerate which are file-less (`d.file === undefined` or attached to a `ts.SourceFile` not strictly `===` an iterated source file); (2) build the per-file UNION via `program.getNgSemanticDiagnostics(sf.fileName)` over `getTsProgram().getSourceFiles()` skipping `isDeclarationFile`; (3) COMPARE: does the per-file union contain every diagnostic the whole-program call produced? Reach the live `program` through the same `loadCompilerCli()` + `performCompilation` path the engine uses (run-typecheck.ts:102-193) so the probe sees exactly the no-emit program the engine builds.

    VERIFY A1 as part of the probe: confirm the poison component's diagnostic is produced by the TEMPLATE path (absent from the whole-program non-template set) — if it appears in the non-template set, the fixture poison is analysis-phase and Task 1 must be revised (Pitfall 2 warning sign).

    Decide per D-02/D-03: SIMPLE ONLY if the probe POSITIVELY proves the per-file union equals the whole-program set (no file-less non-template diagnostic dropped); otherwise (any file-less non-template diagnostic found, OR inability to positively enumerate every non-template diagnostic as file-bearing-and-matched) -> HYBRID. Absence of evidence is NOT proof of absence.

    Write `.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-RES-01-SPIKE.md` containing a `## GO DECISION` heading with: (1) the decision (SIMPLE | HYBRID); (2) the fixture used; (3) the method (whole-program `d.file` inspection vs per-file union); (4) the empirical result (e.g. "N non-template diagnostics, all file-bearing and matched -> SIMPLE" OR "found M file-less non-template diagnostics / could not positively enumerate -> HYBRID per D-03"); (5) the v22.0.4 citations (`compiler.ts:618`, `compiler.ts:1243-1258`, `program.ts:224-243`). ASCII-only.

  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- res-01-spike.probe.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx nx test angular-typechecker -- res-01-spike.probe.spec.ts` runs the probe against the live compiler and exits 0 (the probe completes; it is diagnostic, not a permanent assertion).
    - The poison diagnostic is confirmed ABSENT from the whole-program non-template set (proves it is a template/TCB Fatal, A1) — recorded in the artifact.
    - `09-RES-01-SPIKE.md` exists and contains a `## GO DECISION` block stating exactly `SIMPLE` or `HYBRID`, the fixture, the method, the empirical result, and the v22.0.4 citations.
    - If the artifact records SIMPLE, it cites POSITIVE proof (per-file union == whole-program set); if it cannot, it records HYBRID (D-03).
  </acceptance_criteria>
  <done>The probe has empirically inspected the file-less-non-template question against the live api.Program, and the GO decision (SIMPLE|HYBRID) is recorded with evidence in 09-RES-01-SPIKE.md for the verifier and for plan 09-02 to implement against.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                                                                                      |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| consumer source/template -> compiler | The fixture components are untrusted "input" the compiler processes; a poisoned component must not be able to hide other components' diagnostics |

## STRIDE Threat Register

| Threat ID | Category                        | Component                                      | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                     |
| --------- | ------------------------------- | ---------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-09-01   | Denial of Service / Repudiation | gather-diagnostics.ts per-file gathering shape | mitigate    | This spike SETTLES the isolation shape (SIMPLE vs HYBRID) so the RES-02 implementation cannot silently under-gather file-less non-template diagnostics; HYBRID is the strict-superset default on inconclusive (D-03), guaranteeing no diagnostic class is dropped before isolation code is written. |
| T-09-SC   | Tampering                       | npm/pip/cargo installs                         | accept      | No package installs in this phase (RESEARCH "Package Legitimacy Audit: Not applicable — installs NO external packages"); the slopcheck/legitimacy gate is moot. Adding a package to satisfy RES-01 is a scope violation.                                                                            |

</threat_model>

<verification>
- `npx nx test angular-typechecker -- res-01-spike.probe.spec.ts` exits 0.
- `09-RES-01-SPIKE.md` records a definitive GO decision (SIMPLE | HYBRID) with positive proof or the HYBRID default.
- The fault-isolation fixture produces a TCB-phase Fatal on component A (absent from the non-template set) and a plain template error on component B.
- Full suite still green: `npx nx test angular-typechecker` (no regression; the probe is excluded from the build, the fixture is out of the project graph).
</verification>

<success_criteria>

- SC1 (GATE) satisfied: a recorded GO decision on the per-file isolation shape exists, settling whether file-less non-template diagnostics occur in the no-emit path, with SIMPLE gated on positive proof and HYBRID the inconclusive default.
- The reusable fault-isolation fixture is committed for plan 09-02 to consume.
- No production engine code changed; no package added.
  </success_criteria>

<output>
Create `.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-01-SUMMARY.md` when done. The SUMMARY MUST restate the GO decision (SIMPLE | HYBRID) prominently so plan 09-02's executor reads it.
</output>
