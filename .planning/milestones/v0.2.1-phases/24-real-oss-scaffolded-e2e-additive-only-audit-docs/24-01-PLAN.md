---
phase: 24-real-oss-scaffolded-e2e-additive-only-audit-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/builders/typecheck/builder.integration.spec.ts
  - fixtures/builder-context/angular.json
  - fixtures/builder-context/tsconfig.app.json
  - fixtures/builder-context/tsconfig.spec.json
  - fixtures/builder-context/app.component.ts
  - fixtures/builder-context/app.component.spec.ts
  - packages/angular-typechecker/src/index.drift.ts
  - packages/angular-typechecker/tsconfig.drift.json
  - .planning/phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-ADDITIVE-AUDIT.md
autonomous: true
requirements: [ACV-03, ACP-02]

must_haves:
  truths:
    - "The Angular CLI builder, driven over a real BuilderContext, returns success:true on a clean fixture and success:false on a planted-error fixture, matching the Nx executor verdict."
    - "The src/index.ts public barrel export SET (runTypecheck, TypecheckInfrastructureError, CoreOptions, CoreResult, SkippedReference) is locked by a standing tsc --noEmit tripwire that fails loudly if any export is removed or renamed."
    - "A recorded audit confirms the executor id, the public barrel, and the pre-existing schemas are widened-only versus the angular-typechecker@0.2.0 tag."
    - "D-05: the single genuine ACV-03 gap (the builder over a BuilderContext) is filled; the already-shipped Angular-CLI-vs-Nx coverage from Phases 21-23 is audited and cited, not re-authored (no duplicate coverage)."
    - "D-07: this phase adds no production engine/generator/schematic surface -- only the integration test, the barrel tripwire, and the recorded audit -- so ACP-02 additive-only holds by construction for the phase's own changes, and the audit confirms the Phase 21-23 additions stayed additive."
  artifacts:
    - path: "packages/angular-typechecker/src/builders/typecheck/builder.integration.spec.ts"
      provides: "Builder-over-BuilderContext execution + parity proof (the ACV-03 gap)"
      min_lines: 40
    - path: "fixtures/builder-context/angular.json"
      provides: "A resolvable Angular workspace root for the builder's eager project-graph prelude"
      contains: "angular-typechecker:typecheck"
    - path: "packages/angular-typechecker/src/index.drift.ts"
      provides: "Additive-only barrel tripwire referencing all five barrel exports"
      contains: "from './index'"
    - path: ".planning/phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-ADDITIVE-AUDIT.md"
      provides: "The ACP-02 additive-only git-diff verdict + guard cross-check"
      contains: "angular-typechecker@0.2.0"
  key_links:
    - from: "packages/angular-typechecker/tsconfig.drift.json"
      to: "packages/angular-typechecker/src/index.drift.ts"
      via: "files array entry (drift target compiles it)"
      pattern: "index\\.drift\\.ts"
    - from: "packages/angular-typechecker/src/builders/typecheck/builder.integration.spec.ts"
      to: "fixtures/builder-context"
      via: "TestingArchitectHost workspaceRoot resolution"
      pattern: "builder-context"
---

<objective>
Fill the ONE genuine ACV-03 coverage gap and stand up the ACP-02 additive-only enforcement + audit. No production surface is added -- this plan ships a builder-over-`BuilderContext` integration test, a barrel-export compile-time tripwire, and a recorded git-diff audit against the `angular-typechecker@0.2.0` tag.

Purpose: ACV-03 names five Angular-CLI-vs-Nx difference areas; four are already covered by the Phase 21-23 spec suite (RESEARCH.md ACV-03 audit table). The single gap is that `builder.spec.ts` asserts the Architect brand + `handler` but NEVER RUNS the builder. ACP-02 is trivially true by construction for Phase 24's own changes; this plan proves Phases 21-23 stayed additive and adds the one missing standing guard (the public barrel).

Output: `builder.integration.spec.ts` + a `fixtures/builder-context/` workspace; `src/index.drift.ts` wired into `tsconfig.drift.json`; and `24-ADDITIVE-AUDIT.md` recording the widen-only verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-RESEARCH.md
@.planning/phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-PATTERNS.md

<interfaces>
<!-- Extracted so the executor needs no codebase exploration. -->

The Angular CLI builder (packages/angular-typechecker/src/builders/typecheck/builder.ts):
  export default convertNxExecutor(typecheckExecutor);
  // Resolves at runtime to an Architect builder OBJECT branded
  // Symbol.for('@angular-devkit/architect:builder') === true, carrying a `handler` fn.
  // Its eager prelude calls retrieveProjectConfigurationsWithAngularProjects(workspaceRoot),
  // which READS workspaceRoot FROM DISK -- so the test needs a resolvable Angular workspace root.

The public barrel (packages/angular-typechecker/src/index.ts) exports EXACTLY:
  value: runTypecheck, TypecheckInfrastructureError
  type:  CoreOptions, CoreResult (from ./core/run-typecheck), SkippedReference (from ./core/walk-references)

The installed harness (@angular-devkit/architect@0.2200.6, an optional peer):
  import { Architect } from '@angular-devkit/architect';
  import { TestingArchitectHost } from '@angular-devkit/architect/testing';
  import { schema } from '@angular-devkit/core';
  // host = new TestingArchitectHost(workspaceRoot, workspaceRoot);
  // host.addBuilder('angular-typechecker:typecheck', builderDefault);
  // architect.scheduleBuilder(...) -> run.result is a BuilderOutput { success: boolean }

Integration-tier config (packages/angular-typechecker/vitest.integration.config.mts):
  include: ['src/**/*.integration.spec.ts']; environment jsdom; testTimeout 30000.
  Run via: npx nx integration angular-typechecker  (dependsOn build).

The drift target (packages/angular-typechecker/project.json `typecheck` target) already runs
  `tsc --noEmit -p packages/angular-typechecker/tsconfig.drift.json`
so a new file only needs adding to that tsconfig's `files` array.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Builder-over-BuilderContext integration test + builder-context fixture (ACV-03 gap-fill)</name>
  <files>fixtures/builder-context/angular.json, fixtures/builder-context/tsconfig.app.json, fixtures/builder-context/tsconfig.spec.json, fixtures/builder-context/app.component.ts, fixtures/builder-context/app.component.spec.ts, packages/angular-typechecker/src/builders/typecheck/builder.integration.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/builders/typecheck/builder.spec.ts (the EXISTING structural/brand guard that never runs the builder -- the partial coverage this task extends)
    - packages/angular-typechecker/src/core/multi-tsconfig.integration.spec.ts (analog: integration-tier harness idiom -- findWorkspaceRoot + join(workspaceRoot,'fixtures',...), planted-code assertions, the `const TS2322 = 2322; const TS2345 = 2345;` convention)
    - fixtures/multi-tsconfig-array/ (analog fixture layout: tsconfig.app.json + tsconfig.spec.json + a component with a planted diagnostic + a spec) -- copy its shape, ADD an angular.json
    - 24-PATTERNS.md sections "src/builders/typecheck/builder.integration.spec.ts" and "fixtures/builder-context/" (the recommended TestingArchitectHost harness + Pitfall F)
    - 24-RESEARCH.md "ACV-03: Filling the gap" (harness code, implementation notes, Assumption A1, the handler-direct fallback)
  </read_first>
  <action>
    Create `fixtures/builder-context/` as a minimal but RESOLVABLE Angular CLI workspace root: an `angular.json` declaring ONE application project whose `architect.typecheck` uses `builder: "angular-typechecker:typecheck"` with `options.tsConfig: ["tsconfig.app.json", "tsconfig.spec.json"]`; `tsconfig.app.json` + `tsconfig.spec.json` leaves; an `app.component.ts` and `app.component.spec.ts`. Plant a distinct raw TS diagnostic in each source file (TS2322 type-not-assignable in the component; TS2345 arg-not-assignable in the spec) so a run over the array surfaces both. Add the `angular.json` per Pitfall F -- the builder's eager `retrieveProjectConfigurationsWithAngularProjects(workspaceRoot)` reads the root from disk; a bare tsconfig tree is not enough.

    Create `packages/angular-typechecker/src/builders/typecheck/builder.integration.spec.ts` (integration tier -- the `*.integration.spec.ts` suffix routes it to `vitest.integration.config.mts`). Resolve the fixture with `findWorkspaceRoot(dirname(fileURLToPath(import.meta.url)))` + `join(workspaceRoot, 'fixtures', 'builder-context')`. Drive the builder over a real `BuilderContext` via `@angular-devkit/architect/testing` `TestingArchitectHost` (host = new TestingArchitectHost(fixtureRoot, fixtureRoot); host.addBuilder('angular-typechecker:typecheck', builderDefault); new Architect(host, registry).scheduleBuilder(...); await run.result; await run.stop()). Assert TWO cases: a CLEAN run (no planted errors -- either a clean sibling fixture or point at leaves with no error) yields `output.success === true`; the PLANTED-error run yields `output.success === false`. Assert parity: the SAME `{ success }` the Nx executor default (`../../executors/typecheck/executor`) returns for the identical fixture + `tsConfig`, since both route through the same `runTypecheck` core. Confirm Assumption A1 during implementation -- `TestingArchitectHost(fixtureRoot)` must scope the eager prelude to the fixture, not the dev-repo nx context. If `TestingArchitectHost` scoping is fiddly, use the documented fallback: call `builderDefault.handler(options, { workspaceRoot: fixtureRoot, logger, target })` directly (same eager prelude, fewer abstractions) -- do NOT shortcut to driving the executor directly, which skips the builder wrapper. Do NOT modify `builder.ts`, `builder.spec.ts`, or any production file.
  </action>
  <verify>
    <automated>npx nx integration angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `npx nx integration angular-typechecker` passes with `builder.integration.spec.ts` included and green.
    - The spec asserts `output.success === true` on a clean run AND `output.success === false` on the planted-error run (two distinct assertions, non-vacuous -- a real BuilderOutput flows).
    - The spec asserts the builder's `{ success }` equals the Nx executor's `{ success }` for the same fixture + `tsConfig` array.
    - `fixtures/builder-context/angular.json` declares a project with `builder: "angular-typechecker:typecheck"` and `tsConfig` as a two-element array.
    - No production file (builder.ts, executor, core, index.ts, schemas) is modified -- confirm `git diff --name-only` lists only the new fixture files + the new spec.
  </acceptance_criteria>
  <done>The ACV-03 builder-over-BuilderContext gap is closed by a green integration test proving success + Nx-parity on clean and planted-error fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Barrel additive-only tripwire (src/index.drift.ts) wired into the drift target (ACP-02, RF-02)</name>
  <files>packages/angular-typechecker/src/index.drift.ts, packages/angular-typechecker/tsconfig.drift.json</files>
  <read_first>
    - packages/angular-typechecker/src/index.ts (the LOCKED barrel -- the exact five exports the tripwire must reference)
    - packages/angular-typechecker/src/core/compiler-cli-types.drift.ts (analog: the repo's `*.drift.ts` `void`/tuple idiom for value + type-only references)
    - packages/angular-typechecker/tsconfig.drift.json (the file being edited -- its `files` array and `moduleResolution: node` classic setting)
    - 24-PATTERNS.md sections "src/index.drift.ts" and "packages/angular-typechecker/tsconfig.drift.json"
    - 24-RESEARCH.md "RF-02 -- add a standing barrel guard? RECOMMEND: LIGHT YES"
  </read_first>
  <action>
    Create `packages/angular-typechecker/src/index.drift.ts` as a type-only compile-time tripwire mirroring `compiler-cli-types.drift.ts`: `import { runTypecheck, TypecheckInfrastructureError } from './index';` (value exports), `import type { CoreOptions, CoreResult, SkippedReference } from './index';` (erased type exports), then reference each so a removal/rename fails `tsc --noEmit` loudly -- `void runTypecheck; void TypecheckInfrastructureError;` for the value exports and a `type _Guard = [CoreOptions, CoreResult, SkippedReference];` tuple for the type exports. This covers ALL FIVE exports including the type-only ones a runtime `.spec.ts` cannot see (per D-04 / RF-02). Do NOT ship it: it is never imported by production code, uses the drift target's classic `moduleResolution: node`, and never lands in lib/spec tsconfigs.

    Edit `packages/angular-typechecker/tsconfig.drift.json`: add `"src/index.drift.ts"` to the existing `files` array (alongside `compiler-cli-types.drift.ts` and `extended-catalog.drift.ts`). No target wiring is needed -- the plugin `typecheck` target already runs `tsc --noEmit -p packages/angular-typechecker/tsconfig.drift.json`.
  </action>
  <verify>
    <automated>npx nx typecheck angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `npx nx typecheck angular-typechecker` passes with `index.drift.ts` compiled (the drift `tsc --noEmit` leg is green).
    - `packages/angular-typechecker/tsconfig.drift.json` `files` array contains `"src/index.drift.ts"`.
    - `src/index.drift.ts` references all five exports: `runTypecheck`, `TypecheckInfrastructureError` (value) and `CoreOptions`, `CoreResult`, `SkippedReference` (type).
    - The tripwire is fail-loud: renaming any export in `src/index.ts` would break `tsc --noEmit` (verify by reasoning -- do not commit a break).
  </acceptance_criteria>
  <done>The public barrel export set is locked by a standing drift tripwire that rides the existing `typecheck` target; a removed/renamed export fails CI loudly.</done>
</task>

<task type="auto">
  <name>Task 3: Additive-only git-diff audit versus angular-typechecker@0.2.0 (ACP-02, D-04)</name>
  <files>.planning/phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-ADDITIVE-AUDIT.md</files>
  <read_first>
    - 24-RESEARCH.md "ACP-02: Additive-only audit" (the four `git diff angular-typechecker@0.2.0..HEAD` commands, the guard cross-check map, the expected "barrel unchanged" verdict)
    - packages/angular-typechecker/src/index.ts (the barrel under audit)
    - packages/angular-typechecker/src/builders/typecheck/nx-surface-regression.spec.ts (the `executors ?? builders` guard the audit cross-checks)
    - packages/angular-typechecker/src/schematics/configuration/nx-generators-surface-regression.spec.ts (the `generators ?? schematics` guard)
    - packages/angular-typechecker/src/package-manifest.spec.ts (the static manifest contract guard)
    - 24-PATTERNS.md section "ACP-02 git-diff audit (verification output)"
  </read_first>
  <action>
    Run the four ACP-02 diff commands against the `angular-typechecker@0.2.0` tag (which exists) over: `src/index.ts`; `src/executors/typecheck/schema.json`; `src/generators/configuration/schema.json` + `src/generators/init/schema.json`; and `executors.json` + `generators.json`. Confirm the executor id (`angular-typechecker:typecheck`), the barrel export names, and the pre-existing schemas are WIDENED-ONLY -- never narrowed, removed, or renamed. Note the expected result (RESEARCH-confirmed): the barrel is byte-unchanged since 0.2.0; the only executor-schema delta is the ENG-01 `tsConfig` `oneOf` string|array widening (additive); `builders.json` / `collection.json` / the builder + ng-add schemas are NEW-file additions (no 0.2.0 baseline -> additive by construction). Windows shell note: use `git grep`/`rg` (never `grep`); pipe filters through `rg`.

    Write `.planning/phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-ADDITIVE-AUDIT.md` recording: (1) the guard cross-check map (each additive-only surface -> its standing guard spec, all present + green, now including the Task-2 barrel drift tripwire); (2) the git-diff verdict per audited path (widen-only / unchanged / new-file); (3) an explicit ACP-02 disposition -- additive-only HOLDS, no breaking change, so the milestone stays 0.2.x (v0.3.0 NOT triggered). Reference requirement ID ACP-02. Use end-user-neutral factual language; this is an internal audit artifact (not the public CHANGELOG), so plan-id references are acceptable here.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `24-ADDITIVE-AUDIT.md` exists and records a per-path git-diff verdict against `angular-typechecker@0.2.0` for `src/index.ts`, the executor schema, both generator schemas, and `executors.json`/`generators.json`.
    - The audit states the ACP-02 verdict explicitly: additive-only holds; no executor-id / barrel / schema narrowing; milestone remains 0.2.x.
    - The audit cross-checks the existing guards (`nx-surface-regression`, `nx-generators-surface-regression`, `schema-parity`, `package-manifest`) plus the new barrel drift tripwire, all listed as present + green.
    - `npx nx test angular-typechecker` (the guard suite) passes -- the audit's guard cross-check is factually current.
  </acceptance_criteria>
  <done>ACP-02 is enforced (standing guards + barrel tripwire) and audited (recorded widen-only verdict vs the 0.2.0 tag); the milestone stays 0.2.x.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public API barrel (`src/index.ts`) -> downstream consumers | A narrowed/renamed export is a silent breaking change; this plan adds the tripwire that guards it |
| test fixture on disk -> builder eager prelude | `fixtures/builder-context/angular.json` is read from disk by the builder's project-graph prelude; it is a first-party test fixture, not untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | public barrel / schemas (additive-only regression) | mitigate | `src/index.drift.ts` `tsc --noEmit` tripwire (Task 2) + the recorded git-diff audit vs `angular-typechecker@0.2.0` (Task 3) + existing surface-regression/schema-parity guards |
| T-24-02 | Spoofing | builder-context fixture resolves the wrong workspace root | mitigate | Point `TestingArchitectHost` at the dedicated `fixtures/builder-context/` root (Pitfall F); confirm the eager prelude scopes to the fixture, not the dev-repo nx context (Assumption A1) |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO new package into the shipped plugin; `@angular-devkit/architect/testing` is already an installed optional peer (RESEARCH.md Package Legitimacy Audit: all first-party Angular, none [ASSUMED]/[SUS]) |

No `high`+ severity threat exists in this plan's surface (compile-time tripwire + integration test + audit doc; no runtime code, no network, no auth, no new dependency). Stated explicitly per the security context.
</threat_model>

<verification>
- `npx nx integration angular-typechecker` -- builder-over-BuilderContext test green (ACV-03).
- `npx nx typecheck angular-typechecker` -- the drift `tsc --noEmit` including `index.drift.ts` green (ACP-02 tripwire).
- `npx nx test angular-typechecker` -- the additive-only guard suite green (ACP-02 cross-check).
- `24-ADDITIVE-AUDIT.md` records the widen-only verdict vs the 0.2.0 tag.
- `npx nx lint angular-typechecker` clean (maxWarnings:0).
</verification>

<success_criteria>
- ACV-03: the one genuine gap (builder over `BuilderContext`) is filled; the other four sub-items remain covered by the existing suite (no duplicate coverage added).
- ACP-02: additive-only is enforced by a standing barrel tripwire + existing guards AND audited by a recorded git-diff verdict; no breaking change; milestone stays 0.2.x.
</success_criteria>

<output>
Create `.planning/phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-01-SUMMARY.md` when done.
</output>
