---
phase: 13-engine-solution-tsconfig-reference-walking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/core/filter-diagnostics.ts
autonomous: true
requirements: [WALK-01]
must_haves:
  truths:
    - 'createCanonicalizer and isUnderDir are importable from filter-diagnostics.ts by another core module'
    - 'The canonicalizer/containment implementation is reused verbatim (single implementation, no duplicate)'
  artifacts:
    - path: 'packages/angular-typechecker/src/core/filter-diagnostics.ts'
      provides: 'Exported createCanonicalizer + isUnderDir for the D-01 boundary guard + D-04 self-ref dedupe'
      contains: 'export function createCanonicalizer'
  key_links:
    - from: 'packages/angular-typechecker/src/core/walk-references.ts (Plan 13-03)'
      to: 'packages/angular-typechecker/src/core/filter-diagnostics.ts'
      via: 'import { createCanonicalizer, isUnderDir }'
      pattern: 'createCanonicalizer|isUnderDir'
---

<objective>
Export the module-private `createCanonicalizer` and `isUnderDir` helpers from
`filter-diagnostics.ts` so the Phase 13 walk (`walk-references.ts`, Plan 13-03) can reuse the
SAME realpath/case-fold path-containment machinery VERBATIM for the D-01 module-boundary guard
and the D-04 self-reference/leaf dedupe -- no duplicate canonicalizer (Pitfall 6 / RESEARCH Open
Question 1: export is the smallest delta).

Purpose: D-01 mandates "reuse tested machinery verbatim"; the two helpers are currently private,
which would tempt a duplicate implementation. This is a pure visibility change -- ADD the `export`
keyword to two existing declarations; do NOT alter their bodies or doc-comments.

Output: `filter-diagnostics.ts` with `createCanonicalizer` and `isUnderDir` exported; no behavior
change; the full existing suite stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-engine-solution-tsconfig-reference-walking/13-RESEARCH.md
@.planning/phases/13-engine-solution-tsconfig-reference-walking/13-PATTERNS.md

<interfaces>
<!-- Helpers to export. Extracted from filter-diagnostics.ts (RESEARCH/PATTERNS cites). -->
<!-- filter-diagnostics.ts:128  function createCanonicalizer(...)  -- module-private today -->
<!-- filter-diagnostics.ts:184  function isUnderDir(...)          -- module-private today -->
<!-- filter-diagnostics.ts:173  function isNodeModulesPath(...)   -- STAYS private (walk does not need it) -->
<!-- filter-diagnostics.ts:64   export function filterDiagnostics(...)  -- the existing export-style analog -->
<!-- filter-diagnostics.ts:39   export interface FilterOptions          -- existing export analog -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export createCanonicalizer and isUnderDir from filter-diagnostics.ts</name>
  <read_first>
    - packages/angular-typechecker/src/core/filter-diagnostics.ts (the file being modified; the
      two declarations at :128 `createCanonicalizer` and :184 `isUnderDir`, plus the existing
      `export function filterDiagnostics` at :64 as the export-style analog)
    - .planning/phases/13-engine-solution-tsconfig-reference-walking/13-PATTERNS.md (section
      "CHANGED src/core/filter-diagnostics.ts": ADD the `export` keyword to those two ONLY; keep
      doc-comments verbatim; do NOT alter bodies; `isNodeModulesPath` stays private)
    - .planning/phases/13-engine-solution-tsconfig-reference-walking/13-RESEARCH.md (Pitfall 6;
      Open Question 1 recommendation: export from filter-diagnostics.ts, smallest delta)
  </read_first>
  <action>
    In `packages/angular-typechecker/src/core/filter-diagnostics.ts`, add the `export` keyword to
    exactly two declarations: the `function createCanonicalizer` declaration (~:128) and the
    `function isUnderDir` declaration (~:184). Change each `function <name>` to
    `export function <name>`. Leave every doc-comment and function body BYTE-UNCHANGED. Do NOT
    export `isNodeModulesPath` (~:173) -- the walk does not need it and keeping it private minimizes
    the public surface. Do NOT touch the existing `filterDiagnostics` export or `FilterOptions`
    interface. This is a pure visibility change: no new imports, no signature change, no logic
    change. Core purity is preserved (no `console`/`process` added).
  </action>
  <verify>
    <automated>npx nx test angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "export function createCanonicalizer" -- packages/angular-typechecker/src/core/filter-diagnostics.ts` returns exactly one match.
    - `git grep -n "export function isUnderDir" -- packages/angular-typechecker/src/core/filter-diagnostics.ts` returns exactly one match.
    - `git grep -c "export function isNodeModulesPath" -- packages/angular-typechecker/src/core/filter-diagnostics.ts` returns 0 (stays private).
    - `npx nx test angular-typechecker` passes (no behavior change; existing filter-diagnostics coverage stays green).
    - No `console.` or `process.` token added under `packages/angular-typechecker/src/core/**` (core purity).
  </acceptance_criteria>
  <done>
    `createCanonicalizer` and `isUnderDir` are exported from filter-diagnostics.ts, their bodies
    and doc-comments unchanged, `isNodeModulesPath` still private, and `npx nx test angular-typechecker`
    is green.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                                |
| ---------------------------- | ---------------------------------------------------------------------------------------------------------- |
| developer tsconfig -> engine | Reference paths are read from the developer's own solution `tsconfig.json`; not networked/multi-user input |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                | Disposition        | Mitigation Plan                                                                                                                                                                                                                                         |
| --------- | ---------------------- | -------------------------------------------------------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-13-01   | Information Disclosure | out-of-project reference traversal (D-01 boundary guard) | mitigate (ENABLER) | This plan exports the SAME `createCanonicalizer` + `isUnderDir` the guard reuses verbatim, so the Plan 13-03 boundary guard cannot silently diverge from the tested containment logic. Guard behavior + `solution-style-oop` proof land in 13-03/13-05. |
| T-13-SC   | Tampering              | npm/pip/cargo installs                                   | accept             | No package installs in this phase (RESEARCH "Package Legitimacy Audit is N/A -- all machinery reuses shipped typescript + @angular/compiler-cli peers"). No install task exists.                                                                        |

</threat_model>

<verification>
- `npx nx test angular-typechecker` green (full existing plugin suite; no behavior change).
- Repo-wide grep confirms exactly the two intended exports were added and `isNodeModulesPath` stays private.
</verification>

<success_criteria>
`createCanonicalizer` and `isUnderDir` are importable from `filter-diagnostics.ts`; no duplicate
canonicalizer will be needed by the walk; the existing suite is green.
</success_criteria>

<output>
Create `.planning/phases/13-engine-solution-tsconfig-reference-walking/13-01-SUMMARY.md` when done.
</output>
