---
context: phase
phase: 36-code-scanning-gating-scanned-files-documentation
milestone: v0.2.4
task: rework-red-sarif-proof
status: paused
last_updated: 2026-07-22T09:20:09.701Z
---

# BLOCKING CONSTRAINTS -- Read Before Anything Else

> Each was discovered through actual failure this session. Acknowledge before proceeding.

- [ ] CONSTRAINT: **The proof-fixture RED alerts HARD-BLOCK the milestone PR merge.** GitHub's code scanning merge protection prohibits merging PR #55 because the PR-only `code-scanning-proof` job uploads a SARIF whose `driver.name` is `angular-typechecker` (the real CLI's tool name) containing 4 deliberate error alerts from `tools/sarif-proof-fixture/`. GitHub groups Code Scanning alerts by **tool (driver.name)**, NOT by category -- the job's distinct `category: angular-typecheck-proof` is insufficient. This was PROVEN empirically (`gh pr merge 55 --merge` -> "the base branch policy prohibits the merge"). Do NOT assume this red check is "advisory / non-required" (an earlier claim that was wrong). Structural mitigation: give the proof SARIF a **separate `driver.name`** so it reports under its own Code Scanning tool, unconflated from the clean dogfood tool.

**Do not proceed until the box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Assuming a red Code Scanning check is non-blocking | The `angular-typechecker` Code Scanning result check was assumed non-required (ruleset `code_scanning` rule lists only CodeQL), but the base branch policy still hard-blocked the merge on it. | blocking | Before claiming a check is non-blocking, PROVE it: attempt `gh pr merge` (non-destructive) or read the repo code scanning PR-check-failure protection. Treat any red Code Scanning check as merge-blocking until proven otherwise. |
| Crediting a stale CI run | The green run 29881837667 was on the PRE-change head `42295c7` (where code-scanning was NOT yet a ci member); it did not evidence GATE-01. | advisory | Always confirm a CI run's `headSha` matches the commit you're evaluating before crediting its result. |
| Using `grep` / `head` in Bash | Both are denied in this environment (project rule). | advisory | Use `git grep` (tracked) or `rg` (untracked/node_modules); never pipe to `grep`/`head`. |
| Bypassing main protection | `gh` suggested `--admin` to force the blocked merge. | blocking | NEVER use `--admin` or direct push to `main` -- the empty-bypass PR-only ruleset forbids it; any `main`-protection change is human-only (escalate). |

<current_state>
Phase 36 (the LAST phase of milestone v0.2.4) is fully built, reviewed, secured, validated. UAT item 1 (real-CI RED/GREEN of the required `ci` aggregate) is PASSED. Milestone PR #55 is marked ready (no longer draft) and OPEN, but its merge is `BLOCKED` by GitHub code scanning protection on the deliberate proof-fixture RED alerts (see the blocking constraint). Branch `gsd/v0.2.4-...` is at `4a8ac92`, 0 behind `main`, in sync with its origin remote. Chaos PR #56 (used to prove RED) is CLOSED and its branch deleted. UAT item 2 (the `main` "Require code scanning results" ruleset toggle) remains a POST-MERGE, human-only action.
</current_state>

<completed_work>
- Plans 36-01 + 36-02 executed and committed (ci.yml gating + drift guard; AGENTS.md runbook + README DOC-01 + docs tripwire).
- Code review (deep): WR-01 stale comment fixed (`5aff3a7`); 0 blockers.
- Verification: `36-VERIFICATION.md` (human_needed -- 2 real-CI/human items).
- Security: `36-SECURITY.md` (SECURED, 0 open; HIGH command-injection T-36-02 verified clean).
- Nyquist: `36-VALIDATION.md` (compliant, wave-0 complete).
- UAT item 1 PASSED in real CI (GREEN run 29898624245 + RED chaos run 29901279596); recorded in `36-UAT.md`.
- Pushed phase-36 to PR #55; marked PR ready.
</completed_work>

<remaining_work>
1. **REWORK (the paused task)** -- make the intended-RED SARIF proof a standalone, descriptively-named job reporting under a SEPARATE Code Scanning tool; prove it passes only when the RED report lands with EXACTLY the expected diagnostics in the expected files. (See `<next_action>`.)
2. Confirm the rework unblocks PR #55 (dogfood `angular-typechecker` tool stays green; the separate proof tool's red must not hard-block). Verify on a throwaway/probe PR.
3. Merge PR #55 (merge commit; never `--admin`).
4. POST-MERGE HUMAN: UAT item 2 -- AGENTS.md ruleset runbook on `main`.
5. Wrap-up: `phase.complete 36`; `extract-learnings 36` (LAST) + global-learnings bridge; flip REQUIREMENTS.md GATE-01/02; milestone v0.2.4 close (`audit-milestone` -> `complete-milestone`); human-gated release cut (`release/0.2.4`, curate CHANGELOG, tag `angular-typechecker@0.2.4`, OIDC publish).
</remaining_work>

<decisions_made>
- Rework the proof (separate Code Scanning tool) rather than dismiss the proof alerts (user declined) or `--admin` bypass (forbidden). The separate tool is the durable fix -- EVERY code PR triggers the PR-only proof job, so without it every code PR would be merge-blocked the same way.
- Verified UAT item 1 via real CI (push + chaos PR), not just by construction.
</decisions_made>

<blockers>
- PR #55 merge is `BLOCKED` by GitHub code scanning protection on the 4 deliberate proof-fixture error alerts (TS2322, NG8002, NG8101, ATC90002) reported under the `angular-typechecker` tool. All other gates pass.
- The EXACT block mechanism is unconfirmed: the ruleset `code_scanning` rule gates only CodeQL (satisfied), so a REPO-LEVEL code scanning PR-check-failure protection is the likely culprit. Determine this FIRST -- if unblocking needs a `main`-protection/ruleset/settings change, that is human-only (escalate).
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` -- full structured state (blockers, decisions, context_notes with the exact fix direction).
2. `.github/workflows/ci.yml` -- the `code-scanning-proof` job (~line 690) is what changes; the `code-scanning` dogfood job (~line 559) and the fallow-leg `automationDetails` rewrite (~line 600, the pattern to mirror for a distinct tool identity) are the references.
3. `tools/sarif-proof-fixture/` -- the fixture with the 4 deliberate one-per-family diagnostics (unchanged by the fix).
4. `.planning/phases/36-code-scanning-gating-scanned-files-documentation/36-UAT.md` -- UAT item 1 evidence + item 2 (human, post-merge).
5. AGENTS.md -- release mechanics + the human-only `main`-protection posture + the "Require code scanning results" runbook (item 2).
6. Auto-memory `code-scanning-sarif-empirical-behavior` and `ci-sarif-code-scanning-dogfood` -- what SARIF changes actually do in GitHub Code Scanning.

## Infrastructure State
- Branch `gsd/v0.2.4-enhanced-sarif-reporting-for-github-code-scanning` @ `4a8ac92`, 0 behind `main`, synced with origin. (This WIP handoff commit will sit on top, local until pushed.)
- PR #55: OPEN, ready (not draft), `BLOCKED`. PR #56 (chaos): CLOSED, branch deleted.
- `security_enforcement: true` was set in `.planning/config.json` (`5ba7f82`) during the session.
- `on: pull_request: {}` in ci.yml has NO branch filter -- a probe PR can target the milestone branch for a minimal diff.

## Pre-Execution Critique Required
- Design question to resolve BEFORE editing: will a SEPARATE proof tool's red ALSO hard-block the merge? Determine the block mechanism (repo code scanning protection vs ruleset). If yes, the design must additionally auto-dismiss the proof tool's alerts AFTER Code Scanning API confirmation (proof stays valid because confirmed first), OR escalate a human-only protection change. Do not start editing until this is answered.

<context>
The proof harness works (it surfaced the alerts = pipeline proven). The problem is purely identity: the intended RED and the real dogfood share the tool name `angular-typechecker`, so GitHub can't tell "intended proof red" from "real regression red," and the intended red blocks the merge. The fix separates them by tool. Keep the change additive/CI-only (no runtime `src/**`, no manifest, no version bump), ASCII-only, SHA-pinned actions preserved, no PR-metadata in any `run:` shell. Verify with a throwaway probe PR, then merge.
</context>

<next_action>
Start with (fresh session):

`/gsd-core:quick --full --auto Change our CI so that the job intended to report a RED SARIF report for GitHub Code Scanning is a standalone job with a descriptive name, separating it from unintended RED SARIF reports. The job that proves it explicitly passes when the RED SARIF report job successfully submits a RED report, confirmed via GitHub Code Scanning but ONLY RED for the expected diagnostics in the expected files. Maybe a separate GitHub Code Scanning *tool* for the expected RED SARIF report.`

FIRST determine the merge-block mechanism (query repo code scanning protection). THEN post-process the proof SARIF to a distinct `driver.name` (e.g. `angular-typechecker-red-proof`), rename the job descriptively, and make the assert confirm EXACTLY {TS2322, NG8002, NG8101, ATC90002} in `tools/sarif-proof-fixture/*` via the Code Scanning alerts API. Verify on a throwaway probe PR before relying on it.
</next_action>
