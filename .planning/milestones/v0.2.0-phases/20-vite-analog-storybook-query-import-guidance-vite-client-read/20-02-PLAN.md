---
phase: 20-vite-analog-storybook-query-import-guidance-vite-client-read
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - packages/angular-typechecker/src/executors/typecheck/executor.ts
  - packages/angular-typechecker/src/executors/typecheck/executor.spec.ts
autonomous: true
requirements: [SB-09]

must_haves:
  truths:
    - "When bundlerQueryImports is non-empty, the executor emits ONE logger.warn naming the count, the recommended \"types\": [\"vite/client\"] fix, an explicit ADVISORY-not-suppressed statement, and the specifier(s) (D-04)"
    - "When bundlerQueryImports is undefined, warnBundlerQueryImports is silent -- self-gating; always-on, no new public option (D-03)"
    - "The advisory renders ONLY in the executor tier (core stays pure); the verdict is untouched (evaluateResult owns success)"
    - "The executor doc-comment count is bumped from four to five advisory warn* notices, and the list includes bundlerQueryImports"
  artifacts:
    - path: "packages/angular-typechecker/src/executors/typecheck/executor.ts"
      provides: "warnBundlerQueryImports(result) + its call site after warnNotTypeChecked + the five-notice doc-comment update"
      contains: "warnBundlerQueryImports"
    - path: "packages/angular-typechecker/src/executors/typecheck/executor.spec.ts"
      provides: "render test (fires once with vite/client + ADVISORY + specifier) + silent-when-undefined test"
      contains: "bundlerQueryImports"
  key_links:
    - from: "packages/angular-typechecker/src/executors/typecheck/executor.ts"
      to: "logger.warn"
      via: "warnBundlerQueryImports called after warnNotTypeChecked(result)"
      pattern: "warnBundlerQueryImports\\(result\\)"
    - from: "warnBundlerQueryImports"
      to: "result.bundlerQueryImports"
      via: "optional-chained length gate mirroring warnNotTypeChecked"
      pattern: "result\\.bundlerQueryImports\\?\\.length"
---

<objective>
Signal 2, the executor half (D-04): render the core's pure `bundlerQueryImports` field as ONE loud
`@nx/devkit` `logger.warn`, mirroring `warnNotTypeChecked`. Core stays framework-agnostic; the executor
adapter is the only tier that logs. The notice names the count, the recommended
`"types": ["vite/client"]` fix, states the diagnostics are NOT suppressed (charter), and lists the
specifiers -- so a human sees the fix above the codeframe dump. This is the fifth advisory warn* notice.
It fires only on presence of the field (self-gating; no new public option -- D-03).

Purpose: turn the machine-readable field (20-01) into an actionable human/CI notice without touching
the verdict (evaluateResult never reads the field).

Output: `warnBundlerQueryImports` + its call site + the bumped doc-comment counts + the render/silent
unit tests. Depends on 20-01 for the `CoreResult.bundlerQueryImports` field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-vite-analog-storybook-query-import-guidance-vite-client-read/20-CONTEXT.md
@.planning/phases/20-vite-analog-storybook-query-import-guidance-vite-client-read/20-RESEARCH.md
@.planning/phases/20-vite-analog-storybook-query-import-guidance-vite-client-read/20-01-SUMMARY.md

<interfaces>
<!-- Current shape of the touched contracts (verified this planning session). -->

executor.ts:
  // line 18-21 doc comment: "... -> the four advisory warn*(result) notices -> renderReport ..."
  // line 32-38 doc comment: "The four warn*(result) helpers surface the core's PURE structured
  //   advisory fields (templateCheckAborted / skippedReferences / the split suppressed counts /
  //   notTypeCheckedDeclaredFiles) as @nx/devkit `logger` notices ..."
  // inside typecheckExecutor (53-56), the notices fire in order:
  //   warnTemplateCheckAborted(result); warnSkippedReferences(result); warnSuppressed(result);
  //   warnNotTypeChecked(result);
  // warnNotTypeChecked is defined at 219-231:
  //   function warnNotTypeChecked(result: CoreResult): void {
  //     if (!result.notTypeCheckedDeclaredFiles?.length) { return; }
  //     logger.warn(`angular-typechecker: ${...length} declared file(s) ... File(s): ${...join(', ')}.`);
  //   }

executor.spec.ts:
  // vi.mock hoisted `mocks` with loggerWarn/loggerError/renderReport/normalizeOptions/evaluateResult.
  // `coreResult(errorCount)` factory (spread base); `notTypeCheckedCoreResult(files)` = { ...coreResult(0), notTypeCheckedDeclaredFiles: files } at 131-138.
  // Render test asserts `mocks.loggerWarn` toHaveBeenCalledOnce() + toHaveBeenCalledWith(expect.stringContaining(...)).
  // Silent test: runTypecheck.mockResolvedValue(coreResult(0)) -> loggerWarn not called.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add warnBundlerQueryImports + call site + bump the advisory-notice count</name>
  <files>packages/angular-typechecker/src/executors/typecheck/executor.ts</files>
  <read_first>
    - packages/angular-typechecker/src/executors/typecheck/executor.ts (the whole file -- the two doc comments at 15-38, the notice call block at 53-56, and warnNotTypeChecked at 219-231 as the exact render template)
    - 20-RESEARCH.md `## Architecture Patterns` (the executor render example + Pitfall 4: bump "four" -> "five" in BOTH doc comments)
  </read_first>
  <action>
    Add `warnBundlerQueryImports(result)` to the notice block IMMEDIATELY AFTER
    `warnNotTypeChecked(result);` (line 56). Define `function warnBundlerQueryImports(result: CoreResult): void`
    after `warnNotTypeChecked` (after 231), mirroring it exactly: early-return on
    `if (!result.bundlerQueryImports?.length) { return; }` (this is the self-gate -- D-03), then ONE
    `logger.warn` whose message names the count (`result.bundlerQueryImports.length`), says the imports
    use a bundler query suffix (e.g. ?raw/?url/?worker/?inline) that looks like a Vite/Analog import,
    recommends adding `"types": ["vite/client"]` to the checked tsconfig (naming the ambient
    `declare module` shim as the fallback), states plainly this is ADVISORY -- the TS2307 are NOT
    suppressed (a missing module can be a real bug), and lists the specifiers via
    `result.bundlerQueryImports.join(', ')`. Add a doc comment mirroring warnNotTypeChecked (D-04:
    ADVISORY only, verdict unchanged, names the consumer's OWN specifiers). Then bump BOTH doc comments
    (Pitfall 4): line ~18-21 "the four advisory warn*(result) notices" -> "five", and line ~32-34 "The
    four warn*(result) helpers ..." -> "five" AND append `/ bundlerQueryImports` to the parenthesized
    field list. ASCII only; braces on all bodies; a blank line before the `return` and around the guard
    per the repo JS/TS style.
  </action>
  <verify>
    <automated>npx nx build angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "warnBundlerQueryImports(result)" packages/angular-typechecker/src/executors/typecheck/executor.ts` shows the call site (once) AND it appears AFTER the `warnNotTypeChecked(result);` line.
    - `warnBundlerQueryImports` guards on `result.bundlerQueryImports?.length` and calls `logger.warn` exactly once (no loop).
    - The logger.warn message literal contains `vite/client`, `ADVISORY`, and `join(`.
    - `git grep -c "the four advisory" packages/angular-typechecker/src/executors/typecheck/executor.ts` is `0` (both "four" doc mentions bumped to "five").
    - `npx nx build angular-typechecker` exits 0 (compiles under module: nodenext).
  </acceptance_criteria>
  <done>The executor renders the fifth advisory notice after warnNotTypeChecked; doc comments say five and list the new field.</done>
</task>

<task type="auto">
  <name>Task 2: Executor render + silent-when-undefined unit tests</name>
  <files>packages/angular-typechecker/src/executors/typecheck/executor.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/executors/typecheck/executor.spec.ts (the `coreResult` factory + `notTypeCheckedCoreResult` at 131-138; the render test at 467-493 and the silent test at 495-503 -- clone both)
    - 20-RESEARCH.md `## Code Examples` (the bundlerQueryCoreResult helper: `{ ...coreResult(2), bundlerQueryImports: specs }` -- the ?query TS2307 are counted errors)
  </read_first>
  <action>
    Add a `bundlerQueryCoreResult(specs: readonly string[]): CoreResult` helper returning
    `{ ...coreResult(2 /* the ?query TS2307 are counted errors */), bundlerQueryImports: specs }`. Add a
    render test: mock `runTypecheck` to resolve that CoreResult with a `['./x?raw']` (only
    bundlerQueryImports set, all other advisory fields empty so exactly ONE warn fires), stub
    `evaluateResult` to `{ success: false }` (errors present), import the executor fresh, and assert
    `mocks.loggerWarn` was called once with a message `stringContaining` each of `vite/client`,
    `ADVISORY`, and the specifier `./x?raw`; assert `mocks.loggerError` was NOT called. Add a
    silent test: mock `runTypecheck` to resolve `coreResult(0)` (field undefined) and assert
    `mocks.loggerWarn` was NOT called (self-gating -- D-03). Mirror the existing notTypeChecked
    render/silent tests exactly.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- executor</automated>
  </verify>
  <acceptance_criteria>
    - The render test asserts a single `logger.warn` containing `vite/client`, `ADVISORY`, and the specifier.
    - The silent test asserts no `logger.warn` when `bundlerQueryImports` is undefined.
    - `npx nx test angular-typechecker -- executor` exits 0.
  </acceptance_criteria>
  <done>The executor render is proven to fire once (with the fix + ADVISORY + specifier) and to stay silent when the field is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core CoreResult -> executor logger | The executor adapter (only logging tier) renders the pure field to a `@nx/devkit` notice. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02 | Information Disclosure | warnBundlerQueryImports message | mitigate | The notice names ONLY `result.bundlerQueryImports` (the consumer's own specifiers from the POST-filter set); no dependency error text -- mirrors the warnNotTypeChecked / warnSuppressed isolation rule. |
| T-20-03 | Tampering (verdict integrity) | executor notice vs verdict | mitigate | The notice is additive signalling BEFORE the report; `evaluateResult` alone owns `{ success }` and never reads the field. The silent-when-undefined test guards against a false positive. |
| T-20-SC | Tampering | package installs | accept | N/A -- no package installs in this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `npx nx build angular-typechecker` compiles (executor emits under module: nodenext).
- `npx nx test angular-typechecker -- executor` green: one warn on presence, silent on absence.
- The verdict path is untouched: `evaluateResult` is still called with `{ maxWarnings, strict }` only.
</verification>

<success_criteria>
- A non-empty `bundlerQueryImports` yields exactly one `logger.warn` (count + vite/client fix + ADVISORY-not-suppressed + specifiers).
- An undefined field is silent (self-gating, D-03).
- Both executor doc comments say five and list `bundlerQueryImports`.
</success_criteria>

<output>
Create `.planning/phases/20-vite-analog-storybook-query-import-guidance-vite-client-read/20-02-SUMMARY.md` when done.
</output>
