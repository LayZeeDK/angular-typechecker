---
status: complete
run_mode: autonomous
authorized_by: user (2026-07-22, "Run UATs autonomously; approve pushing temporary branches except to main")
result: PASS (all UAT items + all 3 deferred open questions settled)
---

# Quick Task 260722-g6y -- UAT Results (real CI)

**Probe vehicle:** throwaway PR #57 (`probe/red-proof-uat` -> milestone branch), CI run `29946473573`.
**Baseline:** `260722-g6y-UAT-BASELINE.md` (captured before the rework).

## UAT-1 -- Tool separation works: PASS

| Check | Baseline (PR #55, before) | Probe (PR #57, after) |
|-------|---------------------------|------------------------|
| `code-scanning-red-proof` job | did not exist | **SUCCESS** |
| Code Scanning check `angular-typechecker` (dogfood) | **FAILURE** | **SUCCESS** |
| Code Scanning check `angular-typechecker-red-proof` | did not exist | **SUCCESS** |
| Code Scanning check `fallow` | success | SUCCESS |

The four deliberate proof diagnostics now report under tool `angular-typechecker-red-proof`. The
dogfood `angular-typechecker` tool check went from FAILURE to SUCCESS -- the conflation is gone,
which was the entire point of the rework.

## UAT-2 -- Exact-set assert passes against live Code Scanning: PASS

The `code-scanning-red-proof` job completed SUCCESS, meaning `tupleDiff` found neither a missing nor
an extra tuple against the D-03 expected set:

| Diagnostic | Path confirmed live |
|------------|---------------------|
| TS2322     | `tools/sarif-proof-fixture/type-error.ts` |
| NG8002     | `tools/sarif-proof-fixture/proof.component.html` |
| NG8101     | `tools/sarif-proof-fixture/proof.component.html` |
| ATC90002   | `tools/sarif-proof-fixture/tsconfig.json` |

**The D-03 correction is vindicated.** ATC90002 landed at `tsconfig.json`, exactly as the research
predicted and the baseline showed. The ORIGINAL D-03 wording ("file-less") would have made this
assert fail permanently in CI.

## UAT-3 -- Assert-then-dismiss (D-01) works: PASS

Post-run alert state on the probe ref -- all four dismissed with the correct reason, and all four
re-homed to the new tool:

```
#8 TS2322   tool=angular-typechecker-red-proof state=dismissed reason="used in tests"
#7 NG8101   tool=angular-typechecker-red-proof state=dismissed reason="used in tests"
#6 NG8002   tool=angular-typechecker-red-proof state=dismissed reason="used in tests"
#5 ATC90002 tool=angular-typechecker-red-proof state=dismissed reason="used in tests"
```

Dismissal ran only after a passing assert (structural data dependency), and `used in tests` is
confirmed as a valid `dismissed_reason` enum value against the live API.

## Open question A1 -- ANSWERED: dismissal DOES resolve the review threads

Research could not determine this from documentation. Empirically settled:

- Before: PR #55 had **3 unresolved** `github-advanced-security` threads and `mergeStateStatus: BLOCKED`.
- After the probe's dismissals (dismissal is GLOBAL across branches): PR #55 has **0 unresolved**
  threads and `mergeStateStatus: CLEAN`, `mergeable: MERGEABLE`.

**Consequence: the separately-planned "resolve the 3 threads" step is no longer needed.** The rework
subsumed it. This is why the assert-then-dismiss design (D-01) was the right call over manual
thread resolution -- it fixes the recurrence structurally rather than per-PR.

## Open question A3 -- ANSWERED: the PATCH succeeds on a PR-only alert

All four alerts were PR-ref-only (top-level `state: null`) and all four dismissed successfully. The
`most_recent_instance.state` read (not top-level `state`) is confirmed correct. The non-fatal
`::warning::` guard was not exercised because nothing failed -- it remains as designed insurance.

## Open question A2 -- ANSWERED: the assert PASSES on a run against already-dismissed alerts

Settled by re-running the `code-scanning-red-proof` job (id 89013031350) on probe run 29946473573
AFTER the first run had already dismissed all four alerts globally. Result: job SUCCESS. The assert
log proves it MATCHED rather than silently skipped:

```
code-scanning red proof: all expected (rule id, file path) tuples present on refs/pull/57/merge
code-scanning red proof: dismissed alert 8
code-scanning red proof: dismissed alert 7
code-scanning red proof: dismissed alert 6
code-scanning red proof: dismissed alert 5
```

This is the failure mode the state-agnostic query was built for: had the assert kept a `state=open`
filter, this second run would have found nothing and reported all four tuples MISSING -> permanent
red. Instead the state-agnostic query returns the dismissed alerts, the exact-set assert passes, and
the dismissal re-applies idempotently. The repo-wide P2 fallback was available as a second layer but
was not needed -- the ref-scoped query already returned the dismissed instances.

**Consequence:** every future code-touching PR that re-fires the proof stays green. The design is
self-healing, not a one-shot that breaks on the next PR.

## Superseded prediction (recorded for accuracy)

Research finding P4 predicted that renaming the job changes `analysis_key`, orphaning legacy alerts
#5-#8 as `open` under `angular-typechecker` forever, so "the rename alone does not green PR #55".
**This did not happen.** GitHub re-associated the same alert numbers (#5-#8) to the new tool, and the
dismissal then cleared them globally. The prediction was reasonable but empirically wrong -- which is
precisely why this was carried as a real-CI-only UAT item rather than trusted as fact.
