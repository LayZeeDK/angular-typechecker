---
phase: 32-verification-docs-additive-audit
plan: 4
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/README.md
  - CHANGELOG.md
  - packages/angular-typechecker/src/machine-readable-docs.spec.ts
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "D-06: the README gains a ## Machine-readable output section documenting the --format flag (human|json|sarif, default human, + the matching Nx executor / Angular CLI builder format option), the JSON payload schema (flat diagnostics[] with 1-based positions + code strings + file-less null, and the summary/outcome fields), the SARIF upload-sarif GitHub Code Scanning recipe, and the run-from-the-repo-root caveat so artifactLocation.uri stays repo-relative."
    - "D-06: a curated UNDATED public CHANGELOG 0.2.3 entry lands in END-USER language with no internal requirement ids / plan-id scopes / board jargon; the package version STAYS 0.2.2 (no release cut, no bump, no tag, no publish this phase)."
    - "D-06: a docs content tripwire spec (mirroring standalone-cli-docs.spec.ts) drift-locks the documented flag + claims against the live parseCliArgs(['--help']) HELP_TEXT and the CHANGELOG hygiene guard, AND asserts the three now-false pre-existing reporter claims stay removed from the README (no reporter non-goal claim, no 'lands in a later release' clause)."
  artifacts:
    - packages/angular-typechecker/README.md (## Machine-readable output section)
    - CHANGELOG.md (curated undated ## 0.2.3 entry)
    - packages/angular-typechecker/src/machine-readable-docs.spec.ts
  key_links:
    - "--format already appears in HELP_TEXT (Phase 30), so the shipped 'documents every long-form flag' derivation forces --format into the README; the new spec locks the section + claims."
    - "CHANGELOG.md is the REPO ROOT file (../../../CHANGELOG.md from the plugin src); descending version sections with ## 0.2.2 already at the top."
---

<objective>
DOC-01: document the machine-readable output surface for end users -- a README
`## Machine-readable output` section and a curated, undated public CHANGELOG `0.2.3`
entry -- and drift-lock the documented claims with a content tripwire spec.

Purpose: an Angular dev / CI author needs to know how to select `--format json|sarif`,
what the JSON payload looks like, and how to wire the SARIF into GitHub Code Scanning
`upload-sarif` (including the run-from-repo-root caveat). Docs land undated; the
version stays 0.2.2 (the release cut is a later human-gated Release-PR).

Output: the README section, the curated CHANGELOG entry, and a docs tripwire spec.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-verification-docs-additive-audit/32-CONTEXT.md
@.planning/phases/32-verification-docs-additive-audit/32-RESEARCH.md

# The docs patterns to mirror + the exact payload shape to document:
@packages/angular-typechecker/src/standalone-cli-docs.spec.ts
@packages/angular-typechecker/src/core/json-report.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: README ## Machine-readable output section + ToC anchor</name>
  <files>packages/angular-typechecker/README.md</files>
  <read_first>
    - packages/angular-typechecker/README.md (the ## Contents ToC lines 28-46 + the ## Standalone CLI section with its ### Options table carrying --format/--quiet/--color)
    - packages/angular-typechecker/README.md the THREE stale reporter passages to reconcile, all now false since JSON shipped Phase 30 + SARIF shipped Phase 31 (STATE.md's Phase-30 entry deliberately left them for DOC-01): the ### Options `--format <fmt>` row (~line 529, ends with the "lands in a later release" clause), the paragraph right after the human-report notes (~line 260, "deliberate non-goal ... only output"), and the ## Limitations bullet (~line 694, reporters "non-goals in v0.x")
    - packages/angular-typechecker/src/core/json-report.ts (the exact shipped JSON payload keys to document: formatVersion, tool, version, tsConfigPath, summary, diagnostics[])
    - .planning/phases/32-verification-docs-additive-audit/32-RESEARCH.md ("DOC-01 deep-dive -> README structure")
  </read_first>
  <action>
    Add a `## Machine-readable output` section to README.md (place it after
    `## Standalone CLI` / before `## Storybook`, or another coherent spot) and add its
    ToC entry `[Machine-readable output](#machine-readable-output)` to the `## Contents`
    list. In END-USER language, document:
    1. `--format <human|json|sarif>` (default human) on the standalone CLI, and the
       matching `format` option on the Nx executor + Angular CLI builder. Note that
       machine formats go to stdout only; advisories/notices go to stderr; no ANSI
       ever appears in a machine payload.
    2. The JSON payload schema (the shipped shape): top-level formatVersion, tool,
       version, tsConfigPath, summary, diagnostics[]; summary carries the discriminated
       outcome (clean / type-error / coverage-incomplete / warnings-exceeded), success,
       the category counts, optional totalFilesCount, and the suppression fields; each
       diagnostics[] entry carries file (repo-relative path OR null for file-less),
       1-based line/column/endLine/endColumn, a humanized code string (TS####/NG8xxx/ATC9000x)
       plus the raw rawCode int, severity, and message.
    3. The SARIF upload-sarif recipe: a GitHub Actions snippet piping
       `atc -c tsconfig.json --format sarif > results.sarif` into
       `github/codeql-action/upload-sarif` with `sarif_file: results.sarif`; mention
       the 18 NG8xxx rules[] catalog + partialFingerprints for alert grouping.
    4. The run-from-the-repo-root caveat: artifactLocation.uri is relativized against
       the process root, so run from the repo root to keep URIs repo-relative and let
       GitHub Code Scanning match files.
    5. File-less diagnostics: file:null (JSON) / no-location result (SARIF); the exit
       code / success -- not the SARIF alert -- is the authoritative fail signal for them.
    6. RECONCILE the three now-FALSE pre-existing passages that would contradict this
       new section (JSON shipped Phase 30, SARIF shipped Phase 31; STATE.md's Phase-30
       entry deliberately left them for DOC-01 to fix). Locate and correct each:
       - the `### Options` table `--format <fmt>` row (~line 529) currently reads
         `Output format: human (default), json, or sarif (SARIF lands in a later release).`
         -- DROP the trailing ` (SARIF lands in a later release)` clause so it reads
         `Output format: human (default), json, or sarif.`
       - the paragraph right after the human-report notes (~line 260) currently reads
         `Machine-readable reporters (JSON, SARIF) are a deliberate non-goal in v0.x; the
         human-readable format above is the only output.` -- REPLACE it with a one-line
         pointer to the new `## Machine-readable output` section (or delete it outright).
       - the `## Limitations` bullet (~line 694) currently reads
         `Machine-readable reporters (JSON, SARIF) are non-goals in v0.x.` -- DELETE that
         bullet (machine-readable output now ships).
       After this edit the README must carry NO reporter non-goal claim and NO
       `lands in a later release` clause anywhere (grep-confirmed both strings live only
       in these three passages); Task 3's tripwire drift-locks their absence.
    ASCII only (no em/en dashes, curly quotes). Fenced code blocks are fine in the
    README itself (this is a doc). Do NOT leak internal ids / board jargon into the
    prose (end-user language only).
  </action>
  <verify>
    <automated>rg -q '## Machine-readable output' packages/angular-typechecker/README.md &amp;&amp; rg -q '\[Machine-readable output\]\(#machine-readable-output\)' packages/angular-typechecker/README.md &amp;&amp; rg -q 'upload-sarif' packages/angular-typechecker/README.md</automated>
  </verify>
  <acceptance_criteria>
    - README has a `## Machine-readable output` heading AND the matching `## Contents` ToC anchor.
    - The section documents `--format` with all three values (human, json, sarif), the JSON payload keys, the `upload-sarif` recipe, the repo-root/artifactLocation.uri caveat, and the file-less file:null behavior.
    - Prose is ASCII-only and end-user-facing (no internal ids / board jargon).
    - The three stale reporter passages are reconciled: the `--format` Options row no
      longer contains `(SARIF lands in a later release)`; the post-notes paragraph no
      longer claims reporters are `a deliberate non-goal in v0.x` / `the only output`;
      and the `## Limitations` bullet claiming reporters are `non-goals in v0.x` is gone.
      The README contains no reporter non-goal claim and no `lands in a later release`
      clause anywhere.
  </acceptance_criteria>
  <done>The README documents the --format flag, the JSON payload schema, the SARIF upload-sarif recipe, and the run-from-repo-root caveat in end-user language, with a ToC anchor; and the three now-false pre-existing reporter passages (the "lands in a later release" clause + the two non-goal claims) are removed/rewritten so nothing contradicts the new section.</done>
</task>

<task type="auto">
  <name>Task 2: Curated undated CHANGELOG 0.2.3 entry (end-user language)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (the repo-root changelog; the existing ## 0.2.2 entry's end-user prose style to match; ## 0.2.3 goes above it)
    - .planning/phases/32-verification-docs-additive-audit/32-RESEARCH.md ("Curated CHANGELOG (D-06)")
    - memory changelog-readme-end-user-facing (the rule: consumer language, no Layout B / input-set / SB-0x / phase / plan-id jargon)
  </read_first>
  <action>
    Add an UNDATED `## 0.2.3` entry at the TOP of CHANGELOG.md (above `## 0.2.2`),
    matching the existing entry's end-user prose style. In consumer language, describe:
    machine-readable output via `--format json` and `--format sarif` across the Nx
    executor, the Angular CLI builder, and the standalone CLI; JSON for AI agents /
    scripts; SARIF 2.1.0 for GitHub Code Scanning `upload-sarif`; a one-line note that
    nothing changes for existing users when `--format` is omitted (additive). NO date,
    NO link-refs, NO internal requirement ids / plan-id scopes / board jargon (this
    becomes the public GitHub Release notes). Keep the package `version` at 0.2.2 --
    do NOT bump package.json, do NOT tag, do NOT publish (the 0.2.2 -> 0.2.3 cut is the
    later human-gated Release-PR flow, AGENTS.md). ASCII only.
  </action>
  <verify>
    <automated>rg -q '## 0.2.3' CHANGELOG.md &amp;&amp; node -e "const p=require('./packages/angular-typechecker/package.json'); if(p.version!=='0.2.2')process.exit(1); console.log('version held at',p.version)"</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md has an undated `## 0.2.3` entry above `## 0.2.2`, in end-user language, mentioning JSON + SARIF machine-readable output and `upload-sarif`.
    - The entry contains no date, no internal requirement ids, no plan-id scopes, and no board jargon.
    - `packages/angular-typechecker/package.json` version is still `0.2.2` (no release cut this phase).
  </acceptance_criteria>
  <done>An undated, curated, end-user-language CHANGELOG 0.2.3 entry documents the machine-readable output feature with no internal ids, and the package version stays 0.2.2.</done>
</task>

<task type="auto">
  <name>Task 3: Docs content tripwire spec</name>
  <files>packages/angular-typechecker/src/machine-readable-docs.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/standalone-cli-docs.spec.ts (the exact pattern: readFileSync README + CHANGELOG, whitespace-normalize, derive flags from parseCliArgs(['--help']), the CHANGELOG hygiene regex at line 122 to reuse)
    - packages/angular-typechecker/src/cli/parse-args.ts (parseCliArgs(['--help']) returns { kind:'help', text } -- --format already appears in HELP_TEXT from Phase 30)
  </read_first>
  <action>
    Create `machine-readable-docs.spec.ts` mirroring standalone-cli-docs.spec.ts (a
    pure readFileSync of ../README.md + ../../../CHANGELOG.md, whitespace-normalized
    `\s+ -> ' '`, no compiler load -- runs in the fast nx test loop). Assert:
    - README contains `## Machine-readable output` AND the ToC anchor
      `[Machine-readable output](#machine-readable-output)`.
    - README documents `--format` and each of the three values `human`, `json`, `sarif`.
    - README mentions `upload-sarif` AND the repo-root / `artifactLocation.uri` caveat
      (assert both the token `artifactLocation.uri` and a repo-root phrase appear).
    - ABSENCE of the reconciled stale reporter claims (BLOCKER-1 drift-lock): assert the
      whitespace-normalized README `not.toMatch(/non-goal/i)` (both removed reporter
      passages used non-goal / non-goals; grep-confirmed the word lives ONLY in those
      passages) AND `not.toContain('lands in a later release')` (the removed `--format`
      Options clause). Either stale claim re-appearing in the README fails the spec.
    - Drift-lock `--format` against the live help: derive the long-form flag tokens
      from `parseCliArgs(['--help'])` (same idiom as standalone-cli-docs.spec.ts) and
      assert `--format` is present in BOTH the help text and the README.
    - CHANGELOG contains `## 0.2.3`, and the 0.2.3 entry (sliced from `## 0.2.3` to the
      next `## `) carries no internal ids: REUSE the shipped CHANGELOG-hygiene regex
      from standalone-cli-docs.spec.ts:122, extended for board jargon per the
      changelog-readme-end-user-facing memory rule. Do not reproduce the regex literal
      in this plan -- copy it from the shipped spec.
    ASCII only.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `machine-readable-docs.spec.ts` asserts the README section + ToC anchor, the three --format values, the upload-sarif recipe + repo-root caveat, the --format help/README drift-lock, and the CHANGELOG 0.2.3 presence + hygiene.
    - The spec also drift-locks the ABSENCE of the reconciled stale claims: it fails if the README re-gains a non-goal / non-goals reporter claim or the `lands in a later release` clause.
    - `nx test angular-typechecker` is green with the new spec included; removing the README section or leaking an internal id into the CHANGELOG 0.2.3 entry would fail it.
  </acceptance_criteria>
  <done>A docs tripwire spec drift-locks the machine-readable README claims against the live --help, asserts the three stale reporter claims stay removed (no non-goal claim, no "lands in a later release" clause), and enforces CHANGELOG hygiene on the 0.2.3 entry, green under nx test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated CHANGELOG -> public GitHub Release notes | internal ids / jargon must not leak into consumer-facing release notes |
| documented claims -> shipped behavior | docs must not drift from the real --format flag / payload shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-05 | Information disclosure | CHANGELOG / README prose | low | mitigate | The docs tripwire's hygiene regex (reused from standalone-cli-docs.spec.ts) blocks internal ids / plan-id scopes in the 0.2.3 entry; the changelog-readme-end-user-facing rule keeps prose consumer-facing. |
| T-32-06 | Tampering (doc drift) | documented --format claims | low | mitigate | The tripwire drift-locks --format against the live parseCliArgs(['--help']) and the README section/anchor, so a removed flag or missing section fails nx test. |
| T-32-SC | Tampering | supply chain (this plan) | low | accept | Docs-only: no code, no dependency, no network surface. |

No high-severity threats: DOC-01 is docs + a filesystem-read tripwire spec; no runtime
surface, no dependency, and the version is explicitly held at 0.2.2.
</threat_model>

<verification>
Per the validation contract (landmine #4):
- `npx nx test angular-typechecker` (the new docs tripwire + all unit/docs specs)
- `npx nx typecheck angular-typechecker` (the new spec is type-checked -- nx test does NOT type-check specs)
- `npx nx lint angular-typechecker` at `maxWarnings:0`
- `npx nx format:check` (the README / CHANGELOG / spec formatting)
</verification>

<success_criteria>
- README has a ## Machine-readable output section (+ ToC anchor) documenting --format, the JSON schema, the upload-sarif recipe, and the repo-root caveat, in end-user language.
- An undated, curated, end-user-language CHANGELOG 0.2.3 entry lands with no internal ids; package.json stays 0.2.2.
- The docs tripwire spec drift-locks the claims and is green under nx test; typecheck/lint/format:check pass.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `packages/angular-typechecker/README.md` -- the `## Machine-readable output` section (+ ToC anchor).
- `CHANGELOG.md` -- the curated undated `## 0.2.3` entry (end-user language).
- `packages/angular-typechecker/src/machine-readable-docs.spec.ts` -- the docs content tripwire.
</artifacts>

<output>
Create `.planning/phases/32-verification-docs-additive-audit/32-04-SUMMARY.md` when done.
</output>
