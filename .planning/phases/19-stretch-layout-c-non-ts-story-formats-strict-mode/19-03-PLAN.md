---
phase: 19-stretch-layout-c-non-ts-story-formats-strict-mode
plan: 03
type: execute
wave: 2
depends_on: ["19-01", "19-02"]
files_modified:
  - packages/angular-typechecker/README.md
  - packages/angular-typechecker/src/storybook-docs.spec.ts
  - .planning/phases/19-stretch-layout-c-non-ts-story-formats-strict-mode/19-DECISIONS.md
autonomous: true
requirements: [SB-08]

must_haves:
  truths:
    - "README ## Storybook documents Composition: the per-project model + Nx graph fan-out (run-many / affected / dependsOn:['^typecheck'] + implicitDependencies)"
    - "README states the Composition coverage claim in board trust-lens style: a MUST (each composed project's declared TS surface is checked when a typecheck target points at its solution tsconfig; run-many/affected covers the set) and a MUST-NOT (we do NOT verify composed refs resolve or deploy -- runtime URLs)"
    - "README carries a Layout C verification note and an Angular-CLI PLANNED/DEFERRED caveat worded 'not yet covered, planned' (NOT 'unsupported')"
    - "A phase decision note records Layout C-beyond-guard and .mdx/.tsx-beyond-advisory as 'not warranted' with cited rationale (CONSENSUS D7/D2, OSS-CANDIDATES no-exact-stack-Layout-C, the run-typecheck.ts DIRECT-path fact) -- phase-19 success criterion 1"
    - "A content-assertion tripwire spec fails if the Composition MUST/MUST-NOT claim or the caveats are removed"
  artifacts:
    - path: "packages/angular-typechecker/README.md"
      provides: "The Storybook Composition section + coverage MUST/MUST-NOT + Layout C verification note + Angular-CLI planned/deferred caveat + a brief opt-in strict mention"
      contains: "Composition"
    - path: ".planning/phases/19-stretch-layout-c-non-ts-story-formats-strict-mode/19-DECISIONS.md"
      provides: "The recorded 'not warranted' decisions for Layout C-beyond-guard and .mdx/.tsx-beyond-advisory with citations (criterion 1)"
      contains: "not warranted"
    - path: "packages/angular-typechecker/src/storybook-docs.spec.ts"
      provides: "A deterministic README content-assertion tripwire (no compiler load)"
  key_links:
    - from: "packages/angular-typechecker/README.md"
      to: "the dependsOn:['^typecheck'] recipe exercised by 19-02"
      via: "the documented recipe matches the fan-out assertion in storybook-composition.int.spec.ts"
      pattern: "\\^typecheck"
    - from: "packages/angular-typechecker/src/storybook-docs.spec.ts"
      to: "packages/angular-typechecker/README.md"
      via: "readFileSync of ../README.md + string assertions on the Composition claim + caveats"
      pattern: "README"
---

<objective>
Extend the shipped SB-07 docs (D-10) and record the deferred SB-08 dispositions (D-02/D-03), closing
phase-19 success criterion 1 (a recorded decision for Layout C support beyond the no-silent-pass guard).
Docs land in `packages/angular-typechecker/README.md ## Storybook` -- the shipped consumer doc in the
`files` whitelist where the SB-07 coverage claim already lives. There is NO `docs/` directory.

OPEN QUESTION FLAGGED FOR HUMAN REVIEW (A2): 19-CONTEXT.md D-05/D-10 literally say `docs/*.md`, but no
`docs/` dir exists and the README is the shipped consumer doc. This plan targets README ## Storybook.
Confirm this placement before execution; if a separate file is preferred, create
`packages/angular-typechecker/docs/storybook-composition.md` and link it from the README instead
(cosmetic placement only -- the content is unchanged).

Purpose: document Composition (per-project model + graph fan-out) with a precise trust-lens coverage
claim, note the Layout C direct-path reality, word the Angular-CLI gap as PLANNED (not unsupported),
and record why Layout C-beyond-guard and .mdx/.tsx-beyond-advisory are NOT warranted -- all prose +
one content tripwire; no engine or executor code.

Output: the README ## Storybook additions + a `19-DECISIONS.md` note + a content-assertion tripwire spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-stretch-layout-c-non-ts-story-formats-strict-mode/19-CONTEXT.md
@.planning/phases/19-stretch-layout-c-non-ts-story-formats-strict-mode/19-RESEARCH.md
@.planning/research/v0.1.2-storybook/board/CONSENSUS.md
@.planning/research/v0.1.2-storybook/OSS-CANDIDATES.md

<!-- The existing ## Storybook section this plan extends (README lines 352-415): coverage claim +
     Layout A/B + the existing caveats incl. the Layout C "not a supported layout" and .mdx/.tsx bullets. -->
@packages/angular-typechecker/README.md

<!-- The model for a deterministic README-reading tripwire (no compiler load, fast nx test loop): -->
@packages/angular-typechecker/src/executors/typecheck/schema-parity.spec.ts

<interfaces>
<!-- schema-parity.spec.ts pattern: const p = join(dirname(fileURLToPath(import.meta.url)), '<file>');
     readFileSync(p, 'utf8'). From src/storybook-docs.spec.ts the README is at '../README.md'. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README ## Storybook additions + a content-assertion tripwire spec</name>
  <files>packages/angular-typechecker/README.md, packages/angular-typechecker/src/storybook-docs.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/README.md ## Storybook (lines 352-415) -- the existing coverage claim, Layout A/B, and the caveats block (incl. the current Layout C bullet at 403-405 and the .mdx/.tsx bullet at 392-395)
    - 19-CONTEXT.md D-10 (the exact docs additions + the MUST/MUST-NOT coverage claim wording) and D-04/D-05 (implicitDependencies is the source of truth; the dependsOn:['^typecheck'] recipe)
    - 19-RESEARCH.md Pattern 2 (run-many / affected / dependsOn:['^typecheck']) and Open Question 2 (README already carries Layout C + .mdx/.tsx caveats)
    - packages/angular-typechecker/src/executors/typecheck/schema-parity.spec.ts (the readFileSync-from-import.meta.url tripwire pattern)
  </read_first>
  <action>
    Extend README ## Storybook with a "Storybook Composition" subsection: state Composition is a
    multi-project TOPOLOGY, not a tsconfig layout -- each composed project AND the host are per-project
    Layout A projects, and the host's `.storybook/main.ts` `refs` object is checked as ordinary TypeScript.
    Explain coverage = per-project `typecheck` + Nx graph fan-out: `nx run-many -t typecheck`,
    `nx affected -t typecheck`, or a `dependsOn: ["^typecheck"]` on the host's typecheck target (so
    `nx typecheck <host>` first checks its upstream deps); the graph edge (`implicitDependencies` on the
    host), NEVER the ref URL, is the source of truth. Add the trust-lens coverage claim: a MUST -- "each
    composed project's declared TypeScript surface is type-checked when a `typecheck` target points at that
    project's solution `tsconfig.json`; `nx run-many`/`affected` covers the set"; a MUST-NOT -- "we do NOT
    verify that composed `refs` resolve, are reachable, or deploy -- those are runtime URLs." Add a Layout C
    verification note: Layout C (a flat root tsconfig, no `references[]`) is still NOT a committed-supported
    layout, but a flat tsconfig's declared stories ARE type-checked via the direct single-leaf path, and an
    empty/story-less config is guarded -- it never silently passes (reflecting the run-typecheck.ts DIRECT-path
    fact). Add an Angular-CLI caveat worded as PLANNED/DEFERRED, NOT "unsupported": the Angular CLI
    `angular.json` Storybook shape (a base tsconfig + per-target leaf tsconfigs, no solution tsconfig / no
    `references[]`) is "not yet covered, planned for a future milestone" (GEN-FUT-01/02). Add a brief
    mention of the opt-in `strict` executor option (default false) that escalates a coverage-incomplete
    outcome to a hard failure (shipped in 19-01). Keep the existing Layout C and .mdx/.tsx caveats; refine
    them only for consistency with the new subsection. ASCII only (no em/en dashes, curly quotes, ellipsis,
    or bullets -- use `--`, `'`, `...`, `*`/`-`). Then create `packages/angular-typechecker/src/storybook-docs.spec.ts`:
    a deterministic vitest spec that readFileSync's `../README.md` and asserts the section contains the
    Composition subsection, the MUST and MUST-NOT phrases, the `dependsOn: ["^typecheck"]` recipe token,
    the Layout C verification note, and the Angular-CLI "planned"/"not yet covered" wording (model the
    readFileSync-from-import.meta.url pattern on schema-parity.spec.ts). No compiler load.
  </action>
  <acceptance_criteria>
    - README ## Storybook contains a Composition subsection mentioning `implicitDependencies`, `run-many`/`affected`, and `dependsOn: ["^typecheck"]`.
    - README contains the Composition MUST claim ("each composed project's declared ... surface is type-checked ...") and the MUST-NOT ("we do NOT verify ... refs resolve ... runtime URLs").
    - README contains a Layout C verification note and an Angular-CLI caveat worded "not yet covered, planned" (NOT "unsupported").
    - `git grep -q "Composition" -- packages/angular-typechecker/README.md` succeeds.
    - storybook-docs.spec.ts reads ../README.md and asserts the Composition claim + MUST-NOT + Layout C note + Angular-CLI planned wording.
    - `npx nx test angular-typechecker` passes (the tripwire spec green).
    - No non-ASCII characters introduced (em/en dashes, curly quotes, ellipsis, bullets).
  </acceptance_criteria>
  <verify>
    <automated>npx nx test angular-typechecker --skip-nx-cache</automated>
  </verify>
  <done>The README ## Storybook section documents Composition with a precise trust-lens claim, a Layout C direct-path note, and an Angular-CLI planned caveat; a content tripwire locks the claim against drift.</done>
</task>

<task type="auto">
  <name>Task 2: Record the deferred SB-08 "not warranted" decisions (criterion 1)</name>
  <files>.planning/phases/19-stretch-layout-c-non-ts-story-formats-strict-mode/19-DECISIONS.md</files>
  <read_first>
    - 19-CONTEXT.md D-02 (Layout C beyond guard -- rationale + the run-typecheck.ts dispatch-keys-on-rootNames.length fact) and D-03 (.mdx/.tsx beyond advisory -- .stories.mdx is SB6-legacy-removed; .stories.tsx unused in Angular)
    - .planning/research/v0.1.2-storybook/board/CONSENSUS.md (D7 "exclude Layout C from committed support, build nothing else"; D2 residual "reserve fail-on-suppressedInGraph for a future opt-in strict mode")
    - .planning/research/v0.1.2-storybook/OSS-CANDIDATES.md (no exact-stack Ng22/Nx23 flat Layout C repo; bitwarden is off-stack informational)
    - packages/angular-typechecker/README.md (the caveats block -- confirm it already reflects both dispositions after Task 1)
  </read_first>
  <action>
    Write `19-DECISIONS.md` recording two "not warranted" dispositions with cited rationale. (1) Layout C
    committed support BEYOND the no-silent-pass guard = NOT WARRANTED: cite CONSENSUS D7 (exclude from
    committed support, build nothing else); OSS-CANDIDATES.md (no exact-stack Ng22/Nx23 flat Layout C repo
    exists; it is a rare deliberate pattern); and the run-typecheck.ts DIRECT-path fact (dispatch keys on
    `parsed.rootNames.length === 0`, NOT on references -- a flat tsconfig with a populated `include` takes
    the direct single-leaf path and ALREADY type-checks its declared rootNames, including stories); note the
    guard was verified on a real Layout C repo (bitwarden) informationally only. (2) `.mdx`/`.tsx`
    type-check BEYOND the shipped advisory = NOT WARRANTED: `.stories.mdx` is Storybook-6 legacy, REMOVED in
    Storybook 7+ (nonexistent in current repos); `.stories.tsx` is used by no Angular + @storybook/angular
    repo (jsx unset everywhere); the `.mdx` "not type-checked" advisory (detect-unchecked-declared.ts,
    Phase 18) ALREADY ships and was verified on radix-ng informationally. State that this note satisfies
    phase-19 success criterion 1, and confirm the README caveats (after Task 1) already reflect both
    dispositions. These are tracked Future Requirements (SB-08), not abandoned. ASCII only.
  </action>
  <acceptance_criteria>
    - 19-DECISIONS.md exists and records BOTH dispositions as "not warranted".
    - It cites CONSENSUS D7, OSS-CANDIDATES (no exact-stack Layout C), and the run-typecheck.ts direct-path fact for Layout C.
    - It cites the SB6-legacy-removed .stories.mdx + unused .stories.tsx evidence and the shipped .mdx advisory for the .mdx/.tsx disposition.
    - It states it satisfies phase-19 success criterion 1 and confirms the README caveats reflect both.
    - `git grep -q "not warranted" -- .planning/phases/19-stretch-layout-c-non-ts-story-formats-strict-mode/19-DECISIONS.md` succeeds.
    - No non-ASCII characters.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/19-stretch-layout-c-non-ts-story-formats-strict-mode/19-DECISIONS.md && git grep -q "not warranted" -- .planning/phases/19-stretch-layout-c-non-ts-story-formats-strict-mode/19-DECISIONS.md && git grep -q "run-typecheck" -- .planning/phases/19-stretch-layout-c-non-ts-story-formats-strict-mode/19-DECISIONS.md && echo DECISIONS_OK</automated>
  </verify>
  <done>The Layout C-beyond-guard and .mdx/.tsx-beyond-advisory dispositions are recorded with cited rationale; phase-19 criterion 1 is closed and the README caveats are confirmed consistent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README (public consumer doc) -> consumer trust | a coverage claim shapes what consumers rely on; an over-claim is a false assurance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Tampering (false assurance) | README Composition coverage claim | mitigate | The claim is written in board trust-lens MUST/MUST-NOT form (never over-claim "all Storybook files" / that refs resolve); the storybook-docs.spec.ts content tripwire fails CI if the MUST-NOT caveat or the Composition claim is removed, so drift toward an over-claim is caught. |
| T-19-06 | Information disclosure | README / 19-DECISIONS.md | accept | Docs are public consumer-facing; no secrets. Internal plan-id scopes leaking is a release-changelog concern (AGENTS.md), not a README/decision-note concern -- this plan writes no changelog and cuts no release. |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (docs + one deterministic spec on the locked stack). Supply-chain checkpoint N/A. |
</threat_model>

<verification>
- `npx nx test angular-typechecker --skip-nx-cache` green (the storybook-docs.spec.ts tripwire).
- `git grep -q "Composition" -- packages/angular-typechecker/README.md` and `git grep -q "not warranted" -- .planning/phases/19-stretch-layout-c-non-ts-story-formats-strict-mode/19-DECISIONS.md` both succeed.
- Before the phase gate: `npx nx format:check` + `npx nx run-many -t lint` (maxWarnings:0) clean; ASCII-only scan of the touched files.
</verification>

<success_criteria>
- Phase-19 success criterion 1 is closed: a decision is recorded for Layout C support beyond the no-silent-pass guard (plus the parallel .mdx/.tsx disposition).
- README ## Storybook documents Composition with a precise MUST/MUST-NOT coverage claim, the Layout C verification note, the Angular-CLI planned/deferred caveat, and a brief opt-in strict mention.
- The docs claim is self-checking (content tripwire) and the documented dependsOn:['^typecheck'] recipe matches the one 19-02 exercised.
</success_criteria>

<output>
Create `.planning/phases/19-stretch-layout-c-non-ts-story-formats-strict-mode/19-03-SUMMARY.md` when done.
</output>
