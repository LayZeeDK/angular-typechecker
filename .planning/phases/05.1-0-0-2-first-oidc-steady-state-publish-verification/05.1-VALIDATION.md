---
phase: 05.1
slug: 0-0-2-first-oidc-steady-state-publish-verification
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-28
---

# Phase 05.1 -- Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> NOTE: this phase's core outcome (a live, irreversible, human-gated OIDC npm publish of
> 0.0.2) is inherently NOT unit-testable. The automatable surface is the release CONFIG
> invariants + the existing release-hygiene regression spec; the live publish is proven by
> post-publish `npm view` evidence (manual verification).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest via `@nx/vitest:test` |
| **Config file** | `packages/angular-typechecker/vite.config.ts` (existing) |
| **Quick run command** | `npx nx test angular-typechecker` |
| **Full suite command** | `npx nx run-many -t test` |
| **Estimated runtime** | ~10-30 seconds (unit/integration tiers) |

---

## Sampling Rate

- **After every task commit:** Run `npx nx test angular-typechecker`
- **After every plan wave:** Run `npx nx run-many -t test`
- **Before the human publish gate:** the release-hygiene integration spec must be green
  (asserts `release.yml` is OIDC-only -- no active `NODE_AUTH_TOKEN`).
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| (filled at planning) | 01 | 1 | PKG-05 | T-05-* (reused) | release.yml stays OIDC-only (no token) | integration | `npx nx test angular-typechecker` | check | pending |

*Status: pending / green / red / flaky. The planner fills concrete task rows.*

---

## Wave 0 Requirements

- Existing infrastructure covers the automatable surface: the `release-hygiene` integration
  spec already asserts `release.yml` carries no active auth token (OIDC-only). Extend it (or
  add a sibling assertion) for any NEW invariant this phase introduces (e.g. `package.json`
  version moved to 0.0.2; `registry-url` still present unless the reactive fallback was
  applied). No new test framework is needed.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Tokenless OIDC publish of 0.0.2 succeeds | PKG-05 | Irreversible, human-gated (B-01), one-shot live action; an npm version cannot be unpublished/republished | After pushing tag `angular-typechecker@0.0.2`, a human approves the `npm-publish` environment; then run `npm view angular-typechecker@0.0.2 --json` and confirm `version: 0.0.2`, `dist.attestations.provenance.predicateType: https://slsa.dev/provenance/v1`, and `dist-tags.latest: 0.0.2` |
| CI authenticated via OIDC (no token) | PKG-05 | Verified by inspecting the live CI run + that no seed token/secret exists | Confirm the publishing workflow run used no `NODE_AUTH_TOKEN`; confirm `NPM_SEED_TOKEN` secret remains deleted |
| Registry-url contingency (only if 404) | PKG-05 | Depends on live registry behavior | If the OIDC publish 404s on auth, drop `registry-url` from the setup-node step and re-fire the tag; re-run is idempotent |

---

## Validation Sign-Off

- [ ] All automatable tasks have `<automated>` verify or Wave 0 dependencies
- [ ] The live publish + `npm view` evidence is recorded under Manual-Only (cannot be automated)
- [ ] Sampling continuity: no 3 consecutive automatable tasks without automated verify
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter (by /gsd-validate-phase post-execution)

**Approval:** pending
