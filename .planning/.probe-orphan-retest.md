# Throwaway probe: re-test gate after orphan cleanup

Planning-only PR to test whether the Code Scanning merge gate passes with the
CURRENT unmodified setup after deleting the orphaned angular-typechecker config
from main. Not merged.
