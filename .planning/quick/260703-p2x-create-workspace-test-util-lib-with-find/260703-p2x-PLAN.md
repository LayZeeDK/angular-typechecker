---
quick_id: 260703-p2x
slug: create-workspace-test-util-lib-with-find
description: Create @workspace/test-util lib with findWorkspaceRoot and address Finding J
date: 2026-07-03
status: complete
---

# Quick Task 260703-p2x: `@workspace/test-util` lib + Finding J

## Goal

Address review Finding J (26 specs hand-roll a depth-coupled workspace-root
computation) by creating a real internal Nx library `@workspace/test-util` that
exports a tested `findWorkspaceRoot`, then refactoring the specs to use it.

Stacked on PR #19 (branch `chore/workspace-test-util` forks from
`fix/scoped-executor-id-refs` HEAD, because Finding J refactors specs that PR #19
also edits). Merge #19 first.

## Tasks

1. **Create the lib** (Nx generator)
   - `npx nx g @nx/js:library test-util --directory=libs/test-util --importPath=@workspace/test-util --unitTestRunner=vitest --bundler=none`
   - Revert the generator's inferred-`plugins` block in `nx.json` (workspace uses
     explicit executor-keyed targets); give the lib explicit `build`/`lint`/`test`
     targets matching convention. `build` (`@nx/js:tsc`) makes it a buildable lib so
     the buildable plugin may import it (`@nx/enforce-module-boundaries`).
   - `tsconfig.json`: `ignoreDeprecations: "6.0"` (TS 6 node10-resolution deprecation).
   - `findWorkspaceRoot(startDir)`: walk up to the nearest `nx.json`.
   - Tests: resolves the nx.json ancestor, depth-independence, self-dir, throws at
     the filesystem root.

2. **Finding J refactor**
   - Replace `const workspaceRoot = join(<arg>, '..', ...)` with
     `findWorkspaceRoot(<arg>)` in all 26 specs (plugin + 3 e2e projects).
   - Import `findWorkspaceRoot` from `@workspace/test-util`; eslint `--fix` sorts.
   - Correct the now-stale "N dirs up" comments.

## Verification

- `nx run-many -t test lint -p test-util` (4 findWorkspaceRoot tests)
- plugin: lint + typecheck-drift + 250 tests + build
- e2e (serialized): cache 9, install 26, matrix 7 -- proves the alias resolves in
  every e2e vitest config
- tarball isolation: no test-util in the plugin's shipped src or `dist`
- `nx format:check` clean; repo-wide scoped-name scan clean
