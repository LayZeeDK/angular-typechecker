---
milestone: v0.2.2
milestone_name: Standalone CLI
audited: 2026-07-17
audited_head: 2eaeefd
status: passed
human_signoff:
  - item: "VER-05 literal human sign-off"
    resolution: "ACCEPTED by the user during this audit (2026-07-17). The authorized autonomous real-clone UAT run (5/5 PASS, both workspace kinds), independently backstopped by CI-authoritative VER-04, is accepted as the VER-05 pass. No re-run required. 28-VERIFICATION.md re-flipped human_needed -> passed."
scores:
  requirements: 21/21 satisfied
  phases: 5/5 verified
  integration: 8-of-8-wired
  flows: 1/1 primary CLI flow proven (VER-04 CI-authoritative; VER-05 real-clone assertions passed + accepted)
signals_at_head:
  build: green
  unit: 448/448 (44 files)
  integration: 119/119 (21 files)
  lint: green (dependency-checks + nx-plugin-checks, maxWarnings:0)
  typecheck: green (spec + drift + tools tsc)
  format_check: green
nyquist:
  compliant_phases: [25, 26, 27, 28, 29]
  partial_phases: []
  missing_phases: []
  overall: compliant
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt: []   # none outstanding — the one integration-checker WARNING was fixed during this audit (see resolved_debt)
resolved_debt:
  - phase: 26-pure-cli-core / core
    items:
      - "RESOLVED during this audit: STALE COMMENT (integration-checker WARNING) in `src/core/exit-codes.ts` header + JSDoc that called the standalone CLI 'deferred', claimed `toExitCode` 'has no live consumer', and said the CLI owns `process.exit(toExitCode(...))`. Rewritten to name `src/cli/main.ts` as the live consumer and note `src/cli/bin.ts` applies the code via `process.exitCode =` for flush-safety. Comment-only change; behavior unchanged; all gates re-green."
  - phase: 29-docs
    items:
      - "RESOLVED post-verification: WR-01 (--fail-fast HELP_TEXT/README wording vs format-report truncation behavior) -> commit 73591e5 `fix(29): align --fail-fast description with actual behavior`."
      - "RESOLVED post-verification: IN-01 (doc drift-lock only caught removed/renamed flags, not ADDED flags) -> commit f568629 `test(29): make flag drift-lock catch added flags`."
      - "RESOLVED post-verification: IN-02 (over-broad `\\bphase\\b` changelog hygiene regex) -> commit 2eaeefd `test(29): narrow changelog hygiene regex to id-shaped phase leaks`."
bookkeeping:
  - artifact: "REQUIREMENTS.md traceability table + VER checkbox list"
    state: "RESOLVED during this audit — VER-01..VER-05 checkboxes flipped to `[x]`; VER-04 + VER-05 traceability flipped `Pending` -> `Complete`; footer date updated."
    action: "done."
  - artifact: "28-04-UAT.md frontmatter"
    state: "already clean — a single `status: executed-autonomous-pass` key (the duplicate the Phase-28 verifier flagged was already collapsed). No edit needed."
    action: "none."
  - artifact: "28-VERIFICATION.md frontmatter"
    state: "RESOLVED during this audit — re-flipped `human_needed` -> `passed` now that the user accepted the VER-05 sign-off."
    action: "done."
  - artifact: "package.json version"
    state: "intentionally still `0.2.1`."
    reality: "the `0.2.2` bump + tag + publish is the separate human-gated Release-PR flow (AGENTS.md). `--version` reads the manifest at runtime and is drift-locked, so the bump flows automatically at release."
    action: "none — expected."
---

# Milestone v0.2.2 Audit: Standalone CLI

**Verdict: PASSED (no open items).**

All 21 requirements are code-level satisfied (3-source cross-referenced), all 5 phases are
verified + Nyquist-validated, cross-phase integration is **8-of-8 seams WIRED** (fresh
integration-checker pass, traced through source not docs), and the primary CLI E2E flow is proven.
The milestone stays additive-only vs `angular-typechecker@0.2.1` (no v0.3.0 trigger). All
authoritative gates are green at HEAD `2eaeefd`: build · unit **448/448** (44 files) · integration
**119/119** (21 files) · lint (maxWarnings:0) · typecheck (spec + drift + tools) · format:check.

The one governance item — **VER-05** (real-clone UAT), whose 5/5 assertions ran and PASSED across
both workspace kinds but were executed by an authorized **autonomous** agent rather than a literal
human sign-off — was **ACCEPTED by the user during this audit** (2026-07-17). Results are real and
independently backstopped by the CI-authoritative VER-04 (identical shipped-bin 0/1/2 contract,
Linux + Windows, PR #41 green). Phase 28's verifier was re-flipped `human_needed` -> `passed`. The
one integration-checker WARNING (a stale `exit-codes.ts` comment) was **fixed during this audit**.
Package version is intentionally still `0.2.1`; the `0.2.2` cut is the deferred human-gated
Release-PR (AGENTS.md).

## Phases (5/5 verified)

| Phase | Verify | Nyquist |
|-------|--------|---------|
| 25 Extract the advisory-notice seam | passed 5/5 | compliant |
| 26 Pure CLI core + exit-code wiring | passed 15/15 (2 accepted fixture-attribution deviations) | compliant |
| 27 Bin shell + cross-platform packaging | passed 4/4 (5/5 REQ IDs) | compliant |
| 28 Shipped-tarball e2e + real-clone UAT | passed 4/4 (VER-05 human sign-off accepted by the user) | compliant |
| 29 Docs | passed 7/7 | compliant |

## Requirements (21/21 satisfied, 3-source cross-reference)

Sources: phase `VERIFICATION.md` status + `REQUIREMENTS.md` traceability + `SUMMARY` frontmatter
`requirements_completed`. Phase-26/28 SUMMARYs deliberately record `[]` (requirements span
multiple plans, closed at phase verification) — the phase VERIFICATION is authoritative there.

| REQ | Phase | Final | 3-source note |
|-----|-------|-------|---------------|
| CLI-04 | 25 | satisfied | 25 VERIFICATION passed + 25-01 SUMMARY + traceability Complete; seam consumed by executor.ts:53 |
| CLI-02 | 26 | satisfied | 26 VERIFICATION passed (composes same `runTypecheck` core); WIRED |
| CLI-03 | 26 | satisfied | nx-free boundary (lint + static + runtime probe); report->stdout, notices->stderr |
| ARGS-01 | 26 | satisfied | `node:util.parseArgs`, zero new deps (package.json untouched since 0.2.1 cut) |
| ARGS-02 | 26 | satisfied | `-c`/`--tsConfig` repeatable+required; `-p`/`--project` rejected as usage error |
| ARGS-03 | 26 | satisfied | single->string, 2+->string[] (unit + integration collapse proofs) |
| ARGS-04 | 26 | satisfied | help/version->0; unknown/missing/non-integer->usage 2 |
| ARGS-05 | 26 | satisfied | NO_COLOR>FORCE_COLOR>isTTY precedence |
| EXIT-01 | 26 | satisfied | two-step compose; 0/1 from evaluateResult().success (errorCount===0/success===false->1 proven); infra->2 via toExitCode |
| EXIT-02 | 26 | satisfied | run() purity spies (no process.exit/stream write); bin.ts sole exit/write site |
| PKG-03 | 26 | satisfied | nx-free `node:path` + `realpathSync.native`; relative-cwd integration case |
| VER-01 | 26 | satisfied | unit *.spec.ts vs stubbed core; 448-test tier green |
| VER-02 | 26 | satisfied | integration *.integration.spec.ts vs real cold compiler; 119-test tier green |
| CLI-01 | 27 | satisfied | two bin names -> one bin.js; 27-01 SUMMARY + traceability Complete |
| PKG-01 | 27 | satisfied | LF shebang survives build (dist byte-checked) + publint bin audit |
| PKG-02 | 27 | satisfied | `module:nodenext` keeps `await import()` bridge un-downleveled (built compiler-loader.js) |
| VER-03 | 27 | satisfied | bin-static.spec.ts (built shebang + nx-free require walk); import-ban provably fires |
| ADD-01 | 27 | satisfied | additive-only vs 0.2.1 (git-diff 0 lines on 9 public paths + barrel-drift green + 27-ADDITIVE-AUDIT.md) |
| DOC-01 | 29 | satisfied | README `## Standalone CLI` + curated `## 0.2.2` CHANGELOG + doc tripwire (8 tests) |
| VER-04 | 28 | satisfied | shipped-tarball e2e 0/1/2 through `.bin` shim (npm+yarn flat/workspace+pnpm+npx) on Linux+Windows; PR #41 green. (traceability flipped `Pending` -> `Complete` this audit) |
| VER-05 | 28 | satisfied | real-clone UAT 5/5 PASS (both workspace kinds, both bin names + `npx angular-typechecker`, no ERR_REQUIRE_ESM). Executed by an authorized AUTONOMOUS run; the literal human sign-off gate was **accepted by the user during this audit** (2026-07-17), backstopped by CI-authoritative VER-04. |

No orphaned requirements (all 21 appear in a phase VERIFICATION.md). No `unsatisfied` requirement.

## Cross-Phase Integration (8/8 seams WIRED — traced at HEAD 2eaeefd)

Fresh integration-checker pass read the actual source (not phase docs) and confirmed the E2E flow
`npx angular-typechecker -c <tsconfig>` (or installed `atc`) -> `bin.ts` -> `run()` -> `parseCliArgs`
-> `runTypecheck` (SAME core) -> `emitAdvisoryNotices` + `renderReport` -> `evaluateResult().success` /
`toExitCode` -> literal exit 0/1/2 completes end-to-end with **0 blockers**:

1. `bin` field (both names) -> `./src/cli/bin.js` — WIRED (package.json:33-36; e2e asserts both `.bin` shims). (CLI-01)
2. `bin.ts` -> `run()` (`import { run } from './main'`) — WIRED. (EXIT-02)
3. `run()` -> `runTypecheck` — WIRED; main.ts imports the IDENTICAL `../core/run-typecheck` the executor imports. True third adapter, no re-implementation. (CLI-02)
4. `run()` + executor -> `emitAdvisoryNotices(result, logger)` — WIRED; the Phase-25 seam consumed by BOTH adapters, notices fire before the report, land in stderr not stdout. (CLI-04/CLI-03)
5. `run()` -> `evaluateResult().success` (0/1) — WIRED; NOT raw counts. (EXIT-01)
6. `run()` -> `toExitCode` (infra=2) — WIRED; sole call site is the `TypecheckInfrastructureError` catch. (EXIT-01)
7. nx-free at three tiers (lint import-ban fires · bin-static require walk · installed-bin require.cache probe, no ERR_REQUIRE_ESM) — WIRED. (CLI-03/PKG-02)
8. Additive-only: Nx executor id, `runTypecheck`/`CoreResult`/`CoreOptions` API, Angular CLI builder (`convertNxExecutor`), generator schemas all unchanged; `bin` field + `src/cli/**` net-new. (ADD-01)

**1 WARNING (non-blocking, tech debt):** stale explanatory comment in `src/core/exit-codes.ts`
(see `tech_debt`). Behavior is correct; only the "deferred / no live consumer" prose is inaccurate.

## E2E Flows (proven)

1. **Standalone CLI exit-code contract** (`npx angular-typechecker` / `angular-typechecker` / `atc`
   -> literal 0/1/2 through the real PM `.bin` shim): CI-authoritative VER-04 — dedicated
   `angular-typechecker-cli-e2e` project across npm + yarn (flat + workspace) + pnpm + npx, on
   Linux AND Windows (Node 24), PR #41 green (23/23 checks). PROVEN.
2. **Real-clone UAT** (shipped bins at real project tsconfigs, both workspace kinds): 28-04-UAT.md
   5/5 PASS — ngx-leaflet @818e9ae + realworld-angular @9e3528f (Angular CLI) and radix-ng/primitives
   @4a7390a2 + analogjs/analog @5b0b8b66 (Nx); RED/GREEN/bad-path->2, no ERR_REQUIRE_ESM. Executed
   autonomously; human sign-off accepted by the user this audit. PROVEN.

## Nyquist Compliance

All 5 phases `nyquist_compliant: true`, `wave_0_complete: true`. Overall **compliant**. No phase
needs `/gsd-validate-phase`.

## VER-05 human sign-off — RESOLVED

VER-05 (real-clone UAT) was the only item not cleanly closed at the start of this audit. What is true:

- **The assertions RAN and PASSED**, 5/5, across both workspace kinds and both bin names + `npx
  angular-typechecker`, with no ERR_REQUIRE_ESM / infra error on any run (28-04-UAT.md). Results
  are real, not fabricated. The only non-green cell was analog's exit-0 GREEN, blocked by analog's
  OWN unbuilt-monorepo TS2882 — an external condition, not an angular-typechecker defect.
- **They were executed by an authorized autonomous agent** (paused-session HANDOFF), NOT a literal
  human sign-off. The 28-04 PLAN and the UAT artifact itself explicitly designed VER-05 as a
  "must NOT mark done from automation" human-run gate — so Phase 28's verifier correctly returned
  `human_needed` rather than silently passing it.
- **It is independently backstopped.** VER-04 proves the IDENTICAL shipped-bin 0/1/2 exit-code
  contract deterministically on CI (Linux + Windows, PR #41 green). The tool's behavior is gated
  regardless of this decision.

**Resolution (2026-07-17):** the user ACCEPTED the authorized autonomous run as the VER-05 pass.
28-VERIFICATION.md was re-flipped `human_needed` -> `passed`; REQUIREMENTS.md VER-05 -> `Complete`.

## Tech Debt

**None outstanding.** The one integration-checker WARNING was fixed during this audit:

- **`src/core/exit-codes.ts` stale comment** — the "deferred / no live consumer / CLI owns
  `process.exit`" prose was rewritten to name `src/cli/main.ts` as the live consumer and note
  `bin.ts` applies the code via `process.exitCode =` for flush-safety. Comment-only; behavior
  unchanged; gate battery re-green.

Three Phase-29 advisory items (WR-01 --fail-fast wording, IN-01 added-flag drift-lock, IN-02
hygiene regex) were already RESOLVED post-verification (commits 73591e5 / f568629 / 2eaeefd) — see
`resolved_debt`.

**Out-of-scope deferrals** (Future Requirements, NOT debt from this milestone): REP-01/02 (JSON /
SARIF reporters), CLIX-01 (`--watch`, needs `NgtscProgram`), CLIX-02 (`--quiet` / explicit color
overrides).

## Notable milestone findings

1. **A true third adapter, proven structurally.** The standalone CLI composes the SAME
   `runTypecheck` core (byte-identical `evaluateResult().success` verdict) as the Nx executor and
   the `convertNxExecutor`-derived Angular CLI builder — confirmed by identical module imports and
   step-for-step pipeline parity, not by phase docs. The only differences are the three
   adapter-specific seams: sink (BufferingLogger vs `@nx/devkit.logger`), path resolution
   (`realpathSync.native` vs `joinPathFragments`), and return shape (`{exitCode,stdout,stderr}` vs
   `{success}`).
2. **The Phase-25 seam extraction paid off exactly as designed.** Pulling the five advisory
   `warn*` helpers into a pure, Logger-injected `core/emit-advisory-notices.ts` let the CLI reuse
   them without ever importing `executor.ts` (which would drag `@nx/devkit`/`chalk` — the 24-06
   crash class). Both adapters now drive one `emitAdvisoryNotices(result, logger)` call.
3. **The exit-code owner finally has its consumer.** `toExitCode` — reserved since v0.0.3 COR-04
   as the deliberate scaffold for "the deferred CLI" — is live for the first time in the infra
   catch of `main.ts`. The two-step compose (0/1 from `evaluateResult().success`, literal 2 from
   `toExitCode`) is the anti-false-pass design: a coverage-incomplete / warnings-exceeded run has
   `errorCount === 0` but still exits 1.

---
*Audited 2026-07-17 against HEAD 2eaeefd (gsd/v0.2.2-standalone-cli).*
