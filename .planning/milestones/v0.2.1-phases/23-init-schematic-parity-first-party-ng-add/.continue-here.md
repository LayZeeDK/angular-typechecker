---
context: phase
phase: 23-init-schematic-parity-first-party-ng-add
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-07-10T22:51:07.541Z
---

# Resume: Phase 23 execution paused after Wave 1

Auto-chain `discuss --analyze --auto --chain -> plan --auto -> execute --auto --no-transition`.
Paused mid-`execute-phase` with Wave 1 (23-01, 23-02) complete and spot-checked. Only Wave 2
(23-03) plus the post-execution gates remain.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Trusting the LSP `new-diagnostics` feed | This session it flagged `logger` / `collectionManifest` as unused in freshly-edited files; both were STALE false positives (the symbols ARE used). | advisory | Treat `nx test` / `nx lint` / `nx build` as authoritative (per CLAUDE.md "TypeScript LSP diagnostics are not authoritative"); verify a flagged symbol with `git grep` before acting. |
| Worktree node_modules gap | Worktree executors start with no `node_modules` and cannot run `nx`/`tsc`/`vitest` to verify — this repo's AGENTS.md documents a junction dance with Windows teardown hazards. | advisory | This session runs executors SEQUENTIALLY on the main tree (real `node_modules`). CONTINUE that for 23-03 — it is a single-plan wave, which AGENTS.md says to run without worktrees anyway. |
| RF-01 install placement | The naive assumption ("`ng add` forces `dependencies`; use `addDependenciesToPackageJson`") is WRONG and would fire a redundant `npm install` via `convertNxGenerator`'s RunCallbackTask. | advisory | 23-03 MUST use Approach C: the `ng-add.save: devDependencies` field (already in package.json) + a DEFENSIVE in-schematic devDep move, and `ngAddGenerator` returns VOID. Grounded in 23-RESEARCH.md. |

_No `blocking`-severity anti-patterns: nothing failed methodologically this session; the two interruptions were external (org spend limit)._

<current_state>
Phase 23 (`init` schematic parity + first-party `ng-add`), branch
`gsd/v0.2.1-angular-cli-workspace-support`. Working tree CLEAN. Wave 1 done + verified:
- **23-01** (ACS-03, init schematic parity) — commits `df2d804`, `88bbf1d`, `5addd4a`; SUMMARY present.
- **23-02** (ACP-01 optional-peer classification + RF-01 `ng-add.save` lever) — commits `4552a34`,
  `3144fa2`, `b8ed004`, `90eaeb4`; SUMMARY present.

Post-plan spot-check PASSED (key files exist, commits present, no `Self-Check: FAILED`).
Independent gate: `nx lint` green; executor-reported `nx test` = 297 green, `nx build` green.
</current_state>

<completed_work>

- **discuss-phase 23** — `23-CONTEXT.md` (D-01..D-09 locked; RF-01/RF-02 flagged). Commit `c67f2b2`.
- **plan-phase 23** — `23-RESEARCH.md` (RF-01=Approach C, RF-02=guard, init no-op + ACP-01 lever
  all verified vs installed source), `23-VALIDATION.md`, `23-PATTERNS.md`, 3 plans (`5f28370`),
  plan-checker (0 blockers, 2 warnings fixed `3ea90ba`), STATE (`8811ae2`).
- **execute-phase 23 Wave 1** — 23-01 done, 23-02 done (see current_state).
</completed_work>

<remaining_work>

- **Plan 23-03 (Wave 2)** — NOT started. First-party `ng-add` schematic (NGADD-01):
  composed `ngAddGenerator` = RF-02 `tree.exists('angular.json')` guard + defensive devDep move +
  `getProjects` filter to `application`+`library` + compose the shipped
  `configurationGenerator(tree, { project, skipFormat: true })` per project + print
  `NO_CACHING_NOTICE` once + **return VOID** (no `addDependenciesToPackageJson`/install callback);
  `schema.json`/`schema.d.ts`; `convertNxGenerator(ngAddGenerator)` re-export at
  `src/schematics/ng-add/schematic.ts`; `collection.json` `ng-add` entry (NEVER `generators.json`);
  `ng-add.spec.ts`; extend `nx-generators-surface-regression.spec.ts`.
- **Post-execution gates** (execute-phase.md): code_review_gate (`gsd-code-review 23`, advisory),
  regression_gate (`nx test`), schema_drift_gate (N/A), codebase_drift_gate,
  **verify_phase_goal** (spawn `gsd-verifier` opus -> `23-VERIFICATION.md`; goal-backward vs SC1-SC4;
  every phase_req_id ACS-03/NGADD-01/ACP-01 accounted for), then `update_roadmap`
  (`gsd-sdk query phase.complete 23`) only if verification `passed`.
- **MANDATORY CLAUDE.md post-completion sequence** (after verify passes, in this order):
  `/gsd-secure-phase 23` -> `/gsd-validate-phase 23` -> `/gsd-extract-learnings 23` (last).
  Then the global-learnings bridge ONLY if `features.global_learnings: true`.
</remaining_work>

<decisions_made>

- **Sequential-on-main execution** (no worktree isolation) — deliberate; continue for 23-03.
- **RF-01 = Approach C** (`ng-add.save` field + defensive move, return VOID) — supersedes the
  CONTEXT.md starting hypothesis; verified in 23-RESEARCH.md.
- **ACP-01 lever = `ignoredDependencies`** in `eslint.config.mjs` (landed in 23-02; `peerDependenciesMeta.optional`
  alone does NOT exempt from the obsolete-dependency check).
- **Two plan-checker warnings fixed inline** (`3ea90ba`) rather than a full revision cycle (0 blockers).
</decisions_made>

<blockers>
- Org monthly Claude spend limit hit twice this session (reset both times). External, not a code
  blocker. `/gsd:execute-phase 23` is idempotent (skips plans with a SUMMARY.md), so resume is safe.
</blockers>

## Required Reading (in order)
1. `.planning/phases/23-init-schematic-parity-first-party-ng-add/23-03-PLAN.md` — the exact plan to execute.
2. `.planning/phases/23-init-schematic-parity-first-party-ng-add/23-RESEARCH.md` — RF-01 Approach C,
   RF-02 guard, the `ng-add` authoring/compose pattern (AUTHORITATIVE).
3. `.planning/phases/23-init-schematic-parity-first-party-ng-add/23-CONTEXT.md` — D-01..D-09 locked decisions.
4. `.planning/phases/23-init-schematic-parity-first-party-ng-add/23-PATTERNS.md` — analog line ranges.
5. `AGENTS.md` — release/worktree/execution rules; `CLAUDE.md` — content-search (`git grep`/`rg -uu`,
   never `grep`), no `git add .`, JS/TS style, Windows shell, public gmail identity, and the
   mandatory post-completion sequence (secure -> validate -> extract-learnings).

## Infrastructure State
- Branch `gsd/v0.2.1-angular-cli-workspace-support`, working tree clean, `node_modules` present on main tree.
- No running servers/watchers. No worktrees created.

<context>
Small, heavily-locked additive thin-adapter phase. 23-03 composes the already-shipped Phase-22
`configuration` write-fork and imports `NO_CACHING_NOTICE` from `init/generator.ts`. Additive-only
charter: no breaking changes to the executor id / `runTypecheck` / `CoreResult` / `CoreOptions` /
existing schemas (widening only).
</context>

<next_action>
Start with: `/gsd:execute-phase 23 --auto --no-transition`. It will skip 23-01 + 23-02 (they have
SUMMARYs) and resume at Wave 2 (23-03). Run 23-03 as ONE `gsd-executor` (opus) sequentially on the
main tree. Then the post-execution gates + `verify_phase_goal`, then secure-phase -> validate-phase
-> extract-learnings. After PHASE COMPLETE, the milestone's next step is `/gsd:discuss-phase 24 --auto`.
</next_action>
