---
phase: 13-engine-solution-tsconfig-reference-walking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - fixtures/solution-style/tsconfig.json
  - fixtures/solution-style/tsconfig.spec.json
  - fixtures/solution-style/error.component.ts
  - fixtures/solution-style/error.component.spec.ts
  - fixtures/solution-style-overlap/tsconfig.json
  - fixtures/solution-style-overlap/tsconfig.lib.json
  - fixtures/solution-style-overlap/tsconfig.spec.json
  - fixtures/solution-style-overlap/shared.component.ts
  - fixtures/solution-style-oop/tsconfig.json
  - fixtures/solution-style-empty/tsconfig.json
  - fixtures/solution-style-broken-ref/tsconfig.json
  - fixtures/solution-style-broken-ref/tsconfig.app.json
  - fixtures/solution-style-broken-ref/error.component.ts
  - fixtures/solution-style-selfref/tsconfig.json
  - fixtures/solution-style-selfref/tsconfig.app.json
  - fixtures/solution-style-selfref/error.component.ts
autonomous: true
requirements: [WALK-01]
must_haves:
  truths:
    - 'solution-style references both an app leaf and a tsconfig.spec.json leaf, each carrying a DISTINCT planted TS2322 in its own file'
    - 'solution-style-overlap has a lib leaf and a spec leaf that both include ONE shared source carrying a single planted error'
    - 'solution-style-oop references ONLY a resolvable leaf outside the fixture directory'
    - 'solution-style-empty has files:[] and NO references'
    - 'solution-style-broken-ref references a real leaf plus a nonexistent path'
    - 'solution-style-selfref references itself and a leaf twice'
  artifacts:
    - path: 'fixtures/solution-style/tsconfig.spec.json'
      provides: 'The spec leaf so the walk union proves the spec was type-checked'
      contains: 'error.component.spec.ts'
    - path: 'fixtures/solution-style/error.component.spec.ts'
      provides: 'DISTINCT planted TS2322 reachable only through the spec leaf'
      contains: 'also-not-a-number'
    - path: 'fixtures/solution-style-overlap/shared.component.ts'
      provides: 'One shared source compiled in both leaves -> cross-Program dedupe proof'
    - path: 'fixtures/solution-style-broken-ref/tsconfig.json'
      provides: 'Real leaf + nonexistent path for the D-05 fold-and-count proof'
  key_links:
    - from: 'walk-references.integration.spec.ts (Plan 13-05)'
      to: 'fixtures/solution-style*'
      via: 'runTypecheck({ tsConfigPath: <fixture>/tsconfig.json })'
      pattern: 'fixtures/solution-style'
---

<objective>
Upgrade `fixtures/solution-style` and add five sibling fixtures that supply the exact substrate the
walk assertions need (SC1-SC4, D-04, D-05). Pure test-data only -- NO engine code. Every planted
error is a PLAIN TS2322 (string-to-number) with a LITERAL template so nothing co-fires (Pitfall 3:
avoid spike-001's NG8117+NG8109 interpolated-signal surprise). ASCII-only.

Purpose: the walk assertions in Plan 13-05 (and the walk-references integration spec) prove
UNION completeness (both leaves ran -> two distinct-file TS2322), cross-`Program` dedupe (shared
source -> one), boundary skip (out-of-project ref skipped), the three-way D-03a split (empty / none-
in-project), D-05 fold-and-count (nonexistent path -> 90002 + survivor walked), and D-04 self-ref
dedupe. Those proofs need committed fixtures BEFORE the specs run.

Output: the upgraded `solution-style` fixture group plus `solution-style-overlap`,
`solution-style-oop`, `solution-style-empty`, `solution-style-broken-ref`, `solution-style-selfref`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-engine-solution-tsconfig-reference-walking/13-RESEARCH.md
@.planning/phases/13-engine-solution-tsconfig-reference-walking/13-PATTERNS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade fixtures/solution-style (planted TS2322 in app leaf + new tsconfig.spec.json leaf)</name>
  <read_first>
    - fixtures/solution-style/tsconfig.json (current: extends ../../tsconfig.base.json, files:[], references:[{ path: ./tsconfig.app.json }])
    - fixtures/solution-style/tsconfig.app.json (structure template for the new tsconfig.spec.json: extends/compilerOptions/files shape)
    - fixtures/solution-style/error.component.ts (current CLEAN signal component; header-comment style noting the fixture is out of the plugin build graph, no @ts-nocheck)
    - .planning/phases/13-engine-solution-tsconfig-reference-walking/13-RESEARCH.md ("Directive 4" -- exact fixture file shapes for tsconfig.json, error.component.ts, tsconfig.spec.json, error.component.spec.ts; Pitfall 3)
    - .planning/phases/13-engine-solution-tsconfig-reference-walking/13-PATTERNS.md ("UPGRADED fixtures/solution-style/*")
  </read_first>
  <action>
    (1) fixtures/solution-style/tsconfig.json: ADD a second reference { "path": "./tsconfig.spec.json" } to the existing references array (keep { "path": "./tsconfig.app.json" }, extends, compileOnSave:false, files:[]).
    (2) fixtures/solution-style/error.component.ts: REPLACE the clean signal component with a standalone component SolutionStyleLeafComponent (selector solution-style-leaf, standalone:true, template the plain literal '<p>ready</p>') that plants ONE TS2322: a field count: number = 'not-a-number';. Keep the existing fixture-header comment style noting the fixture is out of the plugin build graph; NO @ts-nocheck; NO interpolated signal.
    (3) NEW fixtures/solution-style/tsconfig.spec.json: mirror tsconfig.app.json's extends/compilerOptions shape (extends "../../tsconfig.base.json", noEmit:true, strict:true, module "preserve", moduleResolution "bundler", target "es2022", emitDecoratorMetadata:false, experimentalDecorators:false), add "types": ["vitest/globals", "node"], and "files": ["error.component.spec.ts"].
    (4) NEW fixtures/solution-style/error.component.spec.ts: import SolutionStyleLeafComponent from './error.component', plant a DISTINCT TS2322 in this file (const specOnly: number = 'also-not-a-number';), then void specOnly; void SolutionStyleLeafComponent;. Add a comment noting a build never compiles specs but the walk's spec leaf does -- the named build differentiator. ASCII-only throughout.
  </action>
  <verify>
    <automated>node -e "const p=require('./fixtures/solution-style/tsconfig.json'); const r=(p.references||[]).map(x=>x.path); if(!r.includes('./tsconfig.app.json')||!r.includes('./tsconfig.spec.json')){process.exit(1)} require('./fixtures/solution-style/tsconfig.spec.json'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - fixtures/solution-style/tsconfig.json references array contains BOTH ./tsconfig.app.json and ./tsconfig.spec.json (parseable JSON).
    - fixtures/solution-style/error.component.ts contains the token 'not-a-number', does NOT contain `{{` (no interpolation), and does NOT contain @ts-nocheck.
    - fixtures/solution-style/error.component.spec.ts exists, imports from './error.component', and contains the token 'also-not-a-number'.
    - fixtures/solution-style/tsconfig.spec.json exists, is parseable JSON, and its files array contains error.component.spec.ts.
    - All four files are ASCII-only.
  </acceptance_criteria>
  <done>
    solution-style references app+spec leaves; each leaf carries a distinct-file TS2322; templates are literal; files are ASCII-only and JSON-parseable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the five sibling fixtures (overlap, oop, empty, broken-ref, selfref)</name>
  <read_first>
    - fixtures/solution-style/tsconfig.app.json (the leaf-tsconfig shape to mirror for every new leaf)
    - fixtures/solution-style/error.component.ts (component shape to mirror for planted errors)
    - .planning/phases/13-engine-solution-tsconfig-reference-walking/13-RESEARCH.md ("Directive 4" references-less + out-of-project-refs shapes; "Validation Architecture -> Minimum fixture set" -- the six-fixture roster and what each proves)
    - .planning/phases/13-engine-solution-tsconfig-reference-walking/13-PATTERNS.md ("5 NEW sibling fixtures" list)
  </read_first>
  <action>
    Create each fixture under fixtures/, every leaf extending "../../tsconfig.base.json", every planted error a plain TS2322 with a literal template. ASCII-only.

    (a) solution-style-overlap/: tsconfig.json (solution) with files:[] and references:[{ path: "./tsconfig.lib.json" }, { path: "./tsconfig.spec.json" }]. BOTH tsconfig.lib.json and tsconfig.spec.json have files:["shared.component.ts"] (the SAME source in both leaves). shared.component.ts is a standalone component planting exactly ONE TS2322 (count: number = 'shared-not-a-number';). This is the cross-Program dedupe proof: the shared diagnostic must collapse to ONE. Give tsconfig.spec.json "types": ["vitest/globals", "node"] like the solution-style spec leaf. Mirror tsconfig.app.json compilerOptions for both leaves.

    (b) solution-style-oop/: tsconfig.json (solution) with files:[] and a single reference that points OUTSIDE the fixture directory to an EXISTING in-repo leaf. FIRST confirm which sibling fixture tsconfig actually exists (list fixtures/ and pick a resolvable leaf tsconfig OUTSIDE solution-style-oop/ that contains at least one diagnostic in its own sources so a no-guard baseline WOULD leak it). Set references:[{ "path": "<relative path to that existing outside leaf>" }] (e.g. "../solution-style/tsconfig.app.json" if it resolves relative to this fixture dir, or another confirmed-existing sibling). The referenced leaf MUST be resolvable so the guard is what skips it, not a not-found. Add NO local source files.

    (c) solution-style-empty/: a single tsconfig.json = { "extends": "../../tsconfig.base.json", "files": [] } with NO references. Proves the references-less empty-project branch (90001 empty-project).

    (d) solution-style-broken-ref/: tsconfig.json (solution) with files:[] and references:[{ path: "./tsconfig.app.json" }, { path: "./tsconfig.missing.json" }] where tsconfig.missing.json does NOT exist (nonexistent PATH -> ENOENT -> code-500). tsconfig.app.json is a real leaf files:["error.component.ts"] mirroring tsconfig.app.json compilerOptions; error.component.ts plants ONE TS2322 (count: number = 'broken-ref-not-a-number';). Proves D-05 fold-and-count: one 90002 for the missing path PLUS the survivor leaf's TS2322 still reported.

    (e) solution-style-selfref/: tsconfig.json (solution) with files:[] and references that include a self-reference AND a duplicate leaf: references:[{ path: "./tsconfig.json" }, { path: "./tsconfig.app.json" }, { path: "./tsconfig.app.json" }]. tsconfig.app.json is a real leaf files:["error.component.ts"] mirroring tsconfig.app.json compilerOptions; error.component.ts plants ONE TS2322 (count: number = 'selfref-not-a-number';). Proves D-04 output-neutral dedupe + self-reference skip (the leaf's single TS2322 appears exactly once despite the duplicate + self references).

  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for(const f of ['solution-style-overlap','solution-style-oop','solution-style-empty','solution-style-broken-ref','solution-style-selfref']){require('./fixtures/'+f+'/tsconfig.json')} if(fs.existsSync('./fixtures/solution-style-broken-ref/tsconfig.missing.json')){process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All five sibling tsconfig.json files are parseable JSON.
    - solution-style-overlap/tsconfig.lib.json and tsconfig.spec.json BOTH list shared.component.ts in files; shared.component.ts contains the token 'shared-not-a-number'.
    - solution-style-oop/tsconfig.json has exactly one reference whose path resolves to an EXISTING tsconfig OUTSIDE the solution-style-oop directory (verify the target file exists).
    - solution-style-empty/tsconfig.json has files:[] and NO references key.
    - solution-style-broken-ref/tsconfig.json references both ./tsconfig.app.json and ./tsconfig.missing.json; fixtures/solution-style-broken-ref/tsconfig.missing.json does NOT exist; error.component.ts contains 'broken-ref-not-a-number'.
    - solution-style-selfref/tsconfig.json references ./tsconfig.json (self) and lists ./tsconfig.app.json twice; error.component.ts contains 'selfref-not-a-number'.
    - Every planted component uses a literal template (no `{{`) and no @ts-nocheck; all files ASCII-only.
  </acceptance_criteria>
  <done>
    All five sibling fixtures exist with the shapes above; the missing-path fixture's referenced file is genuinely absent; the oop reference resolves to a real outside leaf; JSON is parseable and files are ASCII-only.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                     | Description                                                                                 |
| ---------------------------- | ------------------------------------------------------------------------------------------- |
| developer tsconfig -> engine | Fixture tsconfigs stand in for developer-authored solution/leaf tsconfigs the walk resolves |

## STRIDE Threat Register

| Threat ID | Category               | Component                          | Disposition               | Mitigation Plan                                                                                                                                                                             |
| --------- | ---------------------- | ---------------------------------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-13-01   | Information Disclosure | out-of-project reference traversal | mitigate (test substrate) | solution-style-oop supplies a resolvable outside leaf with its own diagnostic so Plan 13-05 can prove the D-01 guard SKIPS it (no leak) vs the no-guard baseline.                           |
| T-13-02   | Tampering (false PASS) | broken-reference false PASS        | mitigate (test substrate) | solution-style-broken-ref supplies a real leaf + a nonexistent path so Plan 13-05 can prove D-05 synthesizes a counted 90002 and still walks the survivor (deterministic non-zero verdict). |
| T-13-SC   | Tampering              | npm/pip/cargo installs             | accept                    | No package installs (fixtures are static test data; RESEARCH: Package Legitimacy Audit N/A).                                                                                                |

</threat_model>

<verification>
- Both `node -e` gates print `OK` (all fixture JSON parses; the missing-path file is genuinely absent).
- Manual: confirm the oop reference target file exists on disk (an existing tsconfig OUTSIDE the oop dir).
- Full behavioral proof is deferred to Plan 13-05 (walk-references.integration.spec.ts asserts against these fixtures once the engine exists).
</verification>

<success_criteria>
The upgraded solution-style group and all five sibling fixtures exist with the exact reference/leaf/
planted-error shapes; JSON parses; planted errors are plain TS2322 with literal templates; ASCII-only.
</success_criteria>

<output>
Create `.planning/phases/13-engine-solution-tsconfig-reference-walking/13-02-SUMMARY.md` when done.
</output>
