---
phase: 12-extended-diagnostic-catalog-completeness-tripwire
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/DIAGNOSTIC-CATALOG.md
autonomous: true
requirements: [CAT-05]
must_haves:
  truths:
    - "DIAGNOSTIC-CATALOG.md's extended-diagnostics section lists the authoritative 18-member ExtendedTemplateDiagnosticName set (name + NG code + category + introduction-version)"
    - "NG8112 (unusedLetDeclaration) and NG8011 (controlFlowPreventingContentProjection) are INCLUDED as enum members (the old catalog wrongly excluded NG8112)"
    - "NG8110 (UNSUPPORTED_INITIALIZER_API_USAGE) and NG8118 (FORBIDDEN_REQUIRED_INITIALIZER_INVOCATION) are noted as ErrorCodes that are NOT configurable extended diagnostics (not in the enum)"
    - "The stale per-introduction-version file-split test-org guidance and the jscodeshift injection guidance are removed and replaced with the single enum-keyed it.each + completeness-tripwire decision"
    - "The unverified alias parentheticals (NG8004 (NG1019), NG2005 (NG1005), NG3003 (NG8003)) are dropped"
    - "NG8011 is framed as emitted out-of-band (no extended/checks factory) but STILL promotable -- never 'not promotable'"
    - "Traceability -- implements CONTEXT.md decisions D-10 (full rewrite to the 18-member enum), D-11 (NG8112 included), D-12 (NG8110/NG8118 noted non-enum; stale test-org guidance replaced), D-13 (CAT-02/CONSENSUS flagged superseded for the milestone audit)"
  artifacts:
    - path: ".planning/research/DIAGNOSTIC-CATALOG.md"
      provides: "The corrected 18-member catalog of record documentation (CAT-05)"
      contains: "controlFlowPreventingContentProjection"
  key_links: []
---

<objective>
Rewrite `.planning/research/DIAGNOSTIC-CATALOG.md` to the source-verified authoritative 18-member
`ExtendedTemplateDiagnosticName` set, correcting the errors the DRIFT-01 tripwire exists to catch.

Purpose: CAT-05 requires the catalog doc match the enum (the build/test source of truth), which the
current doc does NOT -- it is docs-derived (16 entries), wrongly excludes NG8112, carries stale
per-version-split + jscodeshift test-org guidance, unverified alias numbers, and (if it framed it) a
"NG8011 not promotable" claim. The tripwire (Plan 01) keeps the SET honest; this doc is the
human-readable companion that must not re-introduce the docs-vs-enum gap.

Output: the rewritten `DIAGNOSTIC-CATALOG.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-CONTEXT.md
@.planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-RESEARCH.md
@.planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-PATTERNS.md
@.planning/research/DIAGNOSTIC-CATALOG.md

<interfaces>
<!-- The authoritative 18-member table to write into the doc (RESEARCH.md Standard Stack, lines 89-108). -->
<!-- VERIFIED vs @angular/compiler-cli@22.0.4 extended_template_diagnostic_name.d.ts + error_code.d.ts. -->
<!-- ALL 18 default to Warning under strictTemplates. Introduction-version is a coverage taxonomy. -->

18 members = {NG8011, NG8021} UNION {NG8101..NG8117 MINUS NG8110}. A numeric "NG81xx" filter is
provably WRONG (it misses NG8011/NG8021 and wrongly includes NG8110/NG8118).

| Enum member (string value) | NG code | Default category |
|----------------------------|---------|------------------|
| invalidBananaInBox | NG8101 | Warning |
| nullishCoalescingNotNullable | NG8102 | Warning |
| optionalChainNotNullable | NG8107 | Warning |
| missingControlFlowDirective | NG8103 | Warning |
| missingStructuralDirective | NG8116 | Warning |
| textAttributeNotBinding | NG8104 | Warning |
| uninvokedFunctionInEventBinding | NG8111 | Warning |
| missingNgForOfLet | NG8105 | Warning |
| suffixNotSupported | NG8106 | Warning |
| skipHydrationNotStatic | NG8108 | Warning |
| interpolatedSignalNotInvoked | NG8109 | Warning |
| controlFlowPreventingContentProjection | NG8011 | Warning (out-of-band, no factory; STILL promotable) |
| unusedLetDeclaration | NG8112 | Warning (enum-only; angular.dev docs OMIT it) |
| uninvokedTrackFunction | NG8115 | Warning |
| unusedStandaloneImports | NG8113 | Warning (out-of-band, no factory; STILL promotable) |
| unparenthesizedNullishCoalescing | NG8114 | Warning |
| uninvokedFunctionInTextInterpolation | NG8117 | Warning |
| deferTriggerMisconfiguration | NG8021 | Warning |

## Corrections to encode (D-10, D-11, D-12, D-13)
1. The angular.dev docs list only 16; the enum has 18. The 2 enum-only members are NG8011
   (controlFlowPreventingContentProjection) and NG8112 (unusedLetDeclaration). Include BOTH.
2. NG8110 (UNSUPPORTED_INITIALIZER_API_USAGE) and NG8118 (FORBIDDEN_REQUIRED_INITIALIZER_INVOCATION)
   are `ErrorCode`s but NOT in the enum -- note them as NOT configurable extended diagnostics.
3. Promotability: ALL 18 are promotable via `extendedDiagnostics.defaultCategory: "error"`. The real
   distinction is emission MECHANISM: 16 have an `extended/checks/<name>/` factory; 2 (NG8011, NG8113)
   are emitted out-of-band but BOTH honor `defaultCategory`. NEVER write "NG8011 not promotable."
4. REMOVE the stale "Test organization" section (per-introduction-version file split + jscodeshift
   injection). REPLACE with the D-01/D-05 decision: a SINGLE enum-keyed `it.each` catalog (intro-version
   a row field) + a type-level enum-vs-table completeness tripwire in `typecheck-drift`, over committed
   static fixtures (no jscodeshift).
5. DROP the unverified alias parentheticals: NG8004 (NG1019), NG2005 (NG1005), NG3003 (NG8003) --
   those alias numbers do not appear in v22.0.4 `error_code.d.ts`. Keep the primary codes only.
6. Update the introductory prose and the "VERIFY ON IMPLEMENTATION" banner so it reflects that the enum
   (build/test source of truth) -- not the angular.dev docs -- drives membership, and that DRIFT-01's
   tripwire consumes the enum precisely because the docs lag/omit members.

## Baseline section
Keep the baseline TS/NG code table (TS2322, TS2339, NG2003, NG2005, NG2007, NG2009, NG1001, NG3003,
NG6100, NG8001, NG8002, NG8004) but drop the alias parentheticals and note NG6100 is a Warning.
Do NOT change the runtime-error scope-boundary paragraph.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite DIAGNOSTIC-CATALOG.md to the authoritative 18-member enum set</name>
  <read_first>
    - .planning/research/DIAGNOSTIC-CATALOG.md (the BEFORE state -- the 16-entry docs-derived table, the stale Test-organization section, the alias parentheticals to remove)
    - .planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-RESEARCH.md (Standard Stack table lines 89-108 -- the authoritative 18-member table; State of the Art lines 405-414 -- the old-vs-new deltas; Promotability lines 239-250)
    - .planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-CONTEXT.md (D-10, D-11, D-12, D-13 -- the correction scope)
    - .planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-PATTERNS.md (DIAGNOSTIC-CATALOG.md section lines 352-359)
  </read_first>
  <files>.planning/research/DIAGNOSTIC-CATALOG.md</files>
  <action>
    Rewrite the extended-diagnostics section to the 18-member table in `<interfaces>` (name + NG code +
    default category + introduction-version), driven from the enum, NOT the angular.dev docs. Add NG8011
    and NG8112 as enum members. Add a note that NG8110 (UNSUPPORTED_INITIALIZER_API_USAGE) and NG8118
    (FORBIDDEN_REQUIRED_INITIALIZER_INVOCATION) are `ErrorCode`s but NOT configurable extended diagnostics
    (not in the enum). Frame NG8011 (and NG8113) as emitted out-of-band (no `extended/checks/` factory) but
    STILL promotable via `defaultCategory` -- never "not promotable." State that all 18 default to Warning
    under `strictTemplates` and are promotable. REMOVE the "Test organization" section wholesale and REPLACE
    it with the D-01/D-05 decision: a single enum-keyed `it.each` catalog (intro-version a row field) plus a
    type-level enum-vs-table completeness tripwire run by `typecheck-drift`, over committed static fixtures
    (drop the jscodeshift injection guidance). In the baseline table, drop the alias parentheticals
    (NG8004 (NG1019), NG2005 (NG1005), NG3003 (NG8003)) and note NG6100 is a Warning. Update the intro prose
    + the "VERIFY ON IMPLEMENTATION" banner to state that the enum is the source of truth (docs lag/omit)
    and that DRIFT-01 consumes the enum for exactly that reason. Follow the repo's ASCII-only rule (no
    em/en dashes, no curly quotes, no Unicode symbols). Keep the runtime-error scope-boundary paragraph.
  </action>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && git grep -c "controlFlowPreventingContentProjection\|unusedLetDeclaration\|deferTriggerMisconfiguration" -- .planning/research/DIAGNOSTIC-CATALOG.md</automated>
  </verify>
  <acceptance_criteria>
    - The extended table lists all 18 members incl. `controlFlowPreventingContentProjection` (NG8011), `unusedLetDeclaration` (NG8112), and `deferTriggerMisconfiguration` (NG8021) -- `git grep -c` for all three returns >= 1 each.
    - The doc notes NG8110 AND NG8118 are `ErrorCode`s NOT in the enum (`git grep -c "8110\|8118" -- .planning/research/DIAGNOSTIC-CATALOG.md` returns >= 1, with prose marking them non-enum).
    - The doc contains NO "not promotable" framing for NG8011 (`git grep -ci "not promotable" -- .planning/research/DIAGNOSTIC-CATALOG.md` returns 0).
    - The stale per-version-split + jscodeshift guidance is gone (`git grep -ci "jscodeshift\|executor.angularNN" -- .planning/research/DIAGNOSTIC-CATALOG.md` returns 0).
    - The alias parentheticals are gone (`git grep -c "NG1019\|NG1005\|NG8003" -- .planning/research/DIAGNOSTIC-CATALOG.md` returns 0).
    - No non-ASCII characters were introduced (spot-check the diff for em/en dashes, curly quotes, Unicode symbols).
  </acceptance_criteria>
  <done>DIAGNOSTIC-CATALOG.md documents the authoritative 18-member enum set; NG8112/NG8011 included; NG8110/NG8118 noted as non-enum; NG8011 framed as out-of-band-but-promotable; stale test-org/jscodeshift/alias content removed; ASCII-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only change to an in-repo planning doc. No runtime code path, no network, no untrusted input, no new dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information Disclosure | `.planning/research/DIAGNOSTIC-CATALOG.md` | accept | No new threat surface: a planning-doc rewrite of public Angular diagnostic facts; no secrets, no PII, no prior-art leakage (this is generic Angular compiler information). RESEARCH section Security Domain: no ASVS category applies. |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | N/A -- this plan installs NO packages. If an executor unexpectedly adds a dependency, STOP and escalate to a `checkpoint:human-verify` per the security threat-model directive. |

No `high`-severity threats; nothing to block on (RESEARCH section Security Domain: no new threat surface introduced).
</threat_model>

<verification>
- The rewritten doc lists all 18 enum members (NG8112 + NG8011 included).
- NG8110/NG8118 noted as non-enum ErrorCodes; NG8011 framed as out-of-band-but-promotable.
- No jscodeshift/per-version-split/alias/"not promotable" content remains; ASCII-only.
</verification>

<success_criteria>
- CAT-05: `DIAGNOSTIC-CATALOG.md` corrected to the authoritative 18-member set, aligned with the enum the DRIFT-01 tripwire consumes.
</success_criteria>

<output>
Create `.planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-04-SUMMARY.md` when done.
</output>
