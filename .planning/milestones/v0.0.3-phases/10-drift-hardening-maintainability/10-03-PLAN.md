---
phase: 10-drift-hardening-maintainability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/core/compiler-cli-types.runtime.spec.ts
autonomous: true
requirements: [HARD-01]
must_haves:
  truths:
    - "A runtime Vitest spec against the REAL `await import('@angular/compiler-cli')` asserts every gathered getter is present as a function on the live NgtscProgram (renamed/removed -> loud failure)"
    - 'The spec flags any NEW diagnostic getter for review (the additions blind-spot the build-time type gate structurally cannot catch)'
    - 'The spec pins the NG encoding round-trip: NG(n) === cli.ngErrorCode(n) and cli.UNKNOWN_ERROR_CODE === 500 (runtime-semantic drift no type check catches)'
  artifacts:
    - path: 'packages/angular-typechecker/src/core/compiler-cli-types.runtime.spec.ts'
      provides: 'Runtime getter-set SUBSET-containment + additions-review diff + encoding round-trip'
      contains: "await import('@angular/compiler-cli')"
  key_links:
    - from: 'compiler-cli-types.runtime.spec.ts'
      to: '@angular/compiler-cli real NgtscProgram + ./diagnostic-codes NG/ngCodeOf'
      via: 'await import + subset-containment getter check + encoding round-trip'
      pattern: 'ngErrorCode'
---

<objective>
Author `compiler-cli-types.runtime.spec.ts`: the RUNTIME half of the HARD-01
two-pronged drift guard (D-04). It runs against the REAL
`await import('@angular/compiler-cli')` surface (the executor's actual load path)
and asserts (a) the 7 frozen gathered-getter names are present as functions on the
live `NgtscProgram` via SUBSET containment (a renamed/removed getter fails loudly);
(b) any NEW diagnostic getter is flagged for review (the additions blind-spot the
build-time type gate structurally cannot see); (c) the NG encoding round-trip
(`NG(n) === cli.ngErrorCode(n)`, `cli.UNKNOWN_ERROR_CODE === 500`) -- runtime-semantic
drift no type check can catch.

Purpose: Closes the two blind spots of the build-time type gate (Plan 02): newly
ADDED upstream getters (TS width subtyping makes a probe blind to them) and
arithmetic/encoding drift (`ngErrorCode` is a runtime function). This plan is
independent of Plans 01/02 (it is a new test file; it introspects the real package,
not the shim) and runs in Wave 1 in parallel.
Output: 1 new integration-tier spec file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-drift-hardening-maintainability/10-CONTEXT.md
@.planning/phases/10-drift-hardening-maintainability/10-RESEARCH.md
@.planning/phases/10-drift-hardening-maintainability/10-PATTERNS.md
@packages/angular-typechecker/src/core/gather-diagnostics.ts
@packages/angular-typechecker/src/core/diagnostic-codes.ts

<interfaces>
<!-- The FROZEN gathered-getter set the spec asserts (matches gather-diagnostics.ts:62-80): -->
<!--   getTsProgram, getTsOptionDiagnostics, getNgOptionDiagnostics, getTsSyntacticDiagnostics, -->
<!--   getTsSemanticDiagnostics, getNgStructuralDiagnostics, getNgSemanticDiagnostics -->
<!-- PLUS getTsProgram().getGlobalDiagnostics (COR-02 reach-through, lives on ts.Program). -->

<!-- Encoding helpers to import (diagnostic-codes.ts, dependency-free, production-importable): -->
<!--   NG(code) = -990000 - code  (:39)    ngCodeOf(code) = Math.abs(code) - 990000  (:50) -->
<!-- Verified live: cli.ngErrorCode(8001/8101/8109/3004) === -998001/-998101/-998109/-993004; -->
<!--   cli.UNKNOWN_ERROR_CODE === 500. -->

<!-- SUBSET containment, NOT prototype EQUALITY (RESEARCH anti-pattern): the live NgtscProgram -->
<!-- prototype has runtime-only EXTRAS (emitXi18n, getApiDocumentation, getEmittedSourceFiles, -->
<!-- getIndexedComponents, getReuseTsProgram) -- an equality check would false-positive. -->

<!-- Build the program with gatherDiagnostics: () => [] so the spec only introspects the SHAPE -->
<!-- (fast, deterministic). Reuse an existing fixture (ng-baseline or extended-v13). -->

<!-- Fixture-path resolution idiom (extended.promotion.integration.spec.ts:27-35): -->
<!--   const packageRoot = join(dirname(fileURLToPath(import.meta.url)), '..', '..'); -->
<!--   const workspaceRoot = join(packageRoot, '..', '..'); -->
<!--   join(workspaceRoot, 'fixtures', '<name>', 'tsconfig.app.json') -->
<!-- Available fixtures (verified): ng-baseline, extended-v13, extended-promoted, global-diagnostics, ... -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the runtime getter-set + additions-review + encoding spec (HARD-01 D-04)</name>
  <files>packages/angular-typechecker/src/core/compiler-cli-types.runtime.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/gather-diagnostics.ts:62-80 (the EXACT getter SET the frozen `GATHERED_GETTERS` must mirror, including the per-file `getNgSemanticDiagnostics(sf.fileName)` and `getTsProgram().getGlobalDiagnostics()`)
    - packages/angular-typechecker/src/core/diagnostic-codes.ts:39,50 (the `NG`/`ngCodeOf` helpers to import; the encoding the round-trip pins against the real `ngErrorCode`)
    - 10-RESEARCH.md "Pattern 4" (the VERIFIED runtime probe shape: subset containment, additions-review filtered diff `/^get.*Diagnostics$/`, encoding round-trip), Open Question 2 (reuse `ng-baseline`/`extended-v13` with `gatherDiagnostics: () => []`), the anti-pattern "do NOT assert prototype EQUALITY", and the Primary sources line listing the runtime-only extras
    - 10-PATTERNS.md "compiler-cli-types.runtime.spec.ts" section + "Shared Patterns > NG-code encoding helpers" + "Integration-spec fixture-path resolution"
    - packages/angular-typechecker/src/core/extended.promotion.integration.spec.ts:1-35 (the fixture-path + describe/it + `await runTypecheck` conventions; the `import { NG } from './diagnostic-codes'` style)
  </read_first>
  <action>
    Create `packages/angular-typechecker/src/core/compiler-cli-types.runtime.spec.ts` (integration tier; it MAY `await import('@angular/compiler-cli')` like the existing integration specs -- this is the executor's real load path, allowed in the test tier). Resolve a fixture tsconfig via the `fileURLToPath`/`packageRoot`/`workspaceRoot` idiom and reuse an existing fixture (`ng-baseline` or `extended-v13`) `tsconfig.app.json`. Declare the FROZEN `GATHERED_GETTERS` `as const` tuple matching `gather-diagnostics.ts:62-80` exactly: `getTsProgram`, `getTsOptionDiagnostics`, `getNgOptionDiagnostics`, `getTsSyntacticDiagnostics`, `getTsSemanticDiagnostics`, `getNgStructuralDiagnostics`, `getNgSemanticDiagnostics`. In each test, build a real program: `cli.readConfiguration(tsconfig, { suppressOutputPathCheck: true })` then `cli.performCompilation({ rootNames, options: { ...parsed.options, noEmit: true }, emitFlags: 0, gatherDiagnostics: () => [] })` (the empty gatherer keeps it fast + shape-only).
    Test (a) SUBSET containment -- for each name in `GATHERED_GETTERS`, `expect(typeof program[name]).toBe('function')`, plus `expect(typeof program.getTsProgram().getGlobalDiagnostics).toBe('function')` (COR-02 reach-through). Do NOT assert prototype EQUALITY (the runtime `NgtscProgram` has runtime-only extras `emitXi18n`/`getApiDocumentation`/etc.).
    Test (b) ADDITIONS review -- read `Object.getPrototypeOf(program)` own names, filter to `/^get.*Diagnostics$/.test(n) || n === 'getTsProgram'`, compute `added = runtimeDiagGetters.filter((n) => !GATHERED_GETTERS.includes(n))`, `expect(added).toEqual([])` (verified empty at 22.0.4; a non-empty `added` is the "do we now miss diagnostics?" review signal -- the additions blind-spot the build-time gate cannot see).
    Test (c) ENCODING round-trip -- `expect(NG(8001)).toBe(cli.ngErrorCode(8001))`, `expect(NG(8109)).toBe(cli.ngErrorCode(8109))`, `expect(NG(3004)).toBe(cli.ngErrorCode(3004))`, `expect(ngCodeOf(cli.ngErrorCode(8109))).toBe(8109)`, `expect(cli.UNKNOWN_ERROR_CODE).toBe(500)`.
    Name the top-level describe so the filter `-t "compiler-cli-types runtime"` selects it. ASCII only; if asserting on any control char use `String.fromCharCode(0x1b)` (not expected here).
  </action>
  <verify>
    <automated>npx nx run angular-typechecker:test -t "compiler-cli-types runtime"</automated>
  </verify>
  <acceptance_criteria>
    - `npx nx run angular-typechecker:test -t "compiler-cli-types runtime"` passes (all three tests green against the live Angular 22.0.4)
    - The spec `await import('@angular/compiler-cli')` and builds a real program with `gatherDiagnostics: () => []` (confirm by reading -- no fake/stub program)
    - The getter-set check is SUBSET containment (`typeof program[name] === 'function'` per name), NOT a prototype-equality assertion
    - The additions-review test uses a `/^get.*Diagnostics$/`-style filtered diff and `expect(added).toEqual([])`
    - The encoding test pins `NG(n) === cli.ngErrorCode(n)` and `cli.UNKNOWN_ERROR_CODE === 500` using the imported `NG`/`ngCodeOf` (NOT a re-derived `parseInt('-99'+code)`)
    - `GATHERED_GETTERS` matches the getter set in `gather-diagnostics.ts` exactly (7 names; `getNgStructuralDiagnostics` included for HARD-04 coverage)
  </acceptance_criteria>
  <done>The runtime spec proves the gathered getters exist on the real NgtscProgram, flags any new diagnostic getter for review, and pins the NG encoding round-trip; green against Angular 22.0.4.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                | Description                                                                                                                                                              |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| runtime spec -> @angular/compiler-cli (real ESM import) | The spec `await import`s the already-installed, `@nx/dependency-checks`-policed package -- the same module the executor loads in production; no external/untrusted input |
| spec -> fixture tsconfig                                | Reads a committed in-repo fixture tsconfig; no network, no secrets                                                                                                       |

## STRIDE Threat Register

| Threat ID  | Category          | Component                                                     | Disposition | Mitigation Plan                                                                                                                                                                                                            |
| ---------- | ----------------- | ------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-10-03-01 | Tampering         | Runtime spec executes arbitrary code via the imported package | accept      | It imports ONLY the already-installed `@angular/compiler-cli` (the production load path); no new package, no dynamic/untrusted module specifier                                                                            |
| T-10-03-02 | Denial of service | The spec builds a real program (could be slow/flaky)          | mitigate    | `gatherDiagnostics: () => []` keeps it shape-only (no diagnostic gathering); reuses an existing small fixture (`ng-baseline`/`extended-v13`); the existing 30000ms testTimeout (STATE [08-02]) covers cold-compiler warmup |
| T-10-03-SC | Tampering         | npm/pip/cargo installs                                        | accept      | No package installs (D-03: zero new dependency); no supply-chain surface added                                                                                                                                             |

</threat_model>

<verification>
- `npx nx run angular-typechecker:test -t "compiler-cli-types runtime"` passes (subset containment + additions diff + encoding round-trip green).
- The spec uses the real `await import('@angular/compiler-cli')`, subset containment (not equality), and imports `NG`/`ngCodeOf` from `diagnostic-codes.ts`.
</verification>

<success_criteria>

- HARD-01 runtime half (D-04): the real NgtscProgram carries every gathered getter (renamed/removed fails loudly); a new diagnostic getter is flagged for review (additions blind-spot closed); the NG encoding round-trip + `UNKNOWN_ERROR_CODE === 500` are pinned against the real `ngErrorCode`.
  </success_criteria>

<output>
Create `.planning/phases/10-drift-hardening-maintainability/10-03-SUMMARY.md` when done.
</output>
