---
context: phase
phase: 28-shipped-tarball-e2e-real-clone-uat
task: 4
total_tasks: 4
status: in_progress
last_updated: 2026-07-16T21:01:33.504Z
---

# Resume: Phase 28 -- autonomous VER-05 UAT + draft-PR Windows CI verification

The `--auto --chain` pipeline (discuss -> plan -> execute -> code-review) is DONE for all
autonomous work. Two things remain, both set up for you here:

1. **Run the VER-05 real-clone UAT autonomously** (it was authored as a human-run procedure;
   this session's task is to run it as an agent and record real results).
2. **Watch draft PR #41's CI**, especially the new `e2e-windows` job (the one surface that
   can only be proven on a real `windows-latest` runner).

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Worktree staleness on an unmerged milestone | v0.2.2 (phases 25-28) is NOT on `origin/main`, and `worktree.baseRef` is unset -> Claude Code worktrees branch from `origin/HEAD` and would start MISSING all of `src/cli/` (the code the e2e installs). | advisory | Run any executor/UAT on the MAIN checkout (sequential), NOT in worktrees, for this milestone. `node_modules` is present on main; Verdaccio uses one 127.0.0.1 port (parallel worktrees would race it). |
| `npx atc` supply-chain hazard | `atc@0.0.6` is an UNRELATED published npm package; `npx atc` would fetch it. | advisory | Invoke the installed `.bin/atc` by path, or `npx angular-typechecker` (the package name matches a bin, so npx resolves the local install). Never `npx atc`. Enforced by source-grep in the specs + UAT. |
| Stale Nx-kind UAT SHAs | `radix-ng/primitives` + `analogjs/analog` have NO pinned SHA (repos move); RESEARCH lists candidates to re-verify. | advisory | Pin a FRESH on-stack Angular 22 SHA at UAT time and RECORD it in `28-04-UAT.md`. Angular-CLI kind SHAs are already pinned (`ngx-leaflet@818e9ae`, `realworld-angular@9e3528f`). |
| MSYS drive-letter mangling | Git Bash reads `D:/...` as a remote host for `tar`/`npm pack`. | advisory | Use MSYS `/d/...` paths for local pack/tar on this Windows arm64 host. |
| Merging the draft PR prematurely | PR #41 is a DRAFT for CI verification; the milestone is NOT done (Phase 29 docs pending) and VER-05 is unverified. | advisory | Do NOT merge PR #41. The real merge is the human-gated Release-PR flow (AGENTS.md) after Phase 29 + a clean milestone. Keep it draft. |

<current_state>
Phase 28 (v0.2.2 "Standalone CLI", the FINAL verification phase). All source + CI work is
committed on branch `gsd/v0.2.2-standalone-cli` (pushed to origin; 87 commits ahead of main)
and locally GREEN. Draft PR #41 is open for CI verification.

- 28-01 (VER-04): new `angular-typechecker-cli-e2e` project + `runShim` helper + ECONNREFUSED
  retry + npm exit-code spec. Full e2e GREEN locally.
- 28-02 (VER-04): yarn (flat+workspace) + pnpm exit-code specs + runtime nx-free/ESM probe. GREEN.
- 28-03 (VER-04 SC-2): dedicated `e2e-windows` CI job (option b; Linux jobs byte-unchanged,
  SHA-pinned) + GUARD-01f drift guard. `nx test` GREEN (439).
- 28-04 (VER-05): `28-04-UAT.md` procedure AUTHORED (status: pending-human-run) -- NOT yet run.
- Code review: 0 blockers; WR-01 (Windows shim quoting) + WR-02 (multi-tsConfig union cell)
  FIXED + re-verified GREEN (commit 3d6c4a3). IN-01/IN-02 info-level, deferred.
</current_state>

<completed_work>
- Discuss (28-CONTEXT.md, 9 decisions), Research (28-RESEARCH.md), Patterns, Validation.
- Plans 28-01..04 (plan-checker: 0 blockers; coverage 2/2 reqs, 9/9 decisions).
- Executed 28-01/02/03 (autonomous, sequential-on-main). 28-04 UAT procedure authored.
- Code review + both warning fixes committed and re-verified.
- Pushed branch + opened draft PR #41.
</completed_work>

<remaining_work>
1. **Autonomous VER-05 UAT** -- follow `28-04-UAT.md` end to end:
   - `nx build angular-typechecker --skip-nx-cache`, then `npm pack` from
     `dist/packages/angular-typechecker` (MSYS `/d/...` path) -> `$ABS_TGZ`. (The published
     0.2.1 has NO CLI; the local tarball is required.)
   - For each clone (Angular-CLI kind: `ngx-leaflet@818e9ae`, `realworld-angular@9e3528f`;
     Nx kind: `radix-ng/primitives` + `analogjs/analog` -- pin FRESH Angular-22 SHAs + record):
     `git clone` + `git checkout <sha>`, install `$ABS_TGZ` with the clone's own PM (clones
     stay UNCOMMITTED), then run `atc -c <real tsconfig>`: clean -> exit 0; planted `TSxxxx`
     -> exit 1 (code in stdout, no `ERR_REQUIRE_ESM`/"infrastructure error"); bad path
     (`atc -c does-not-exist.json`) / no `-c` / `--nonsense` -> exit 2. Revert each plant.
   - Fill the `result:`/`evidence:` fields + `## Summary` + sign-off table in `28-04-UAT.md`;
     flip frontmatter `status`/`outcome` from `pending-human-run` to the executed verdict.
   - Record honestly. If any clone fails, treat as a gap (`/gsd-plan-phase 28 --gaps`), not a pass.
2. **Monitor draft PR #41 CI** -- `gh pr checks 41`. The gating checks: `e2e (angular-typechecker-cli-e2e)`
   (Linux tarball e2e) and `e2e-windows` (Windows leg, Node 24). If `e2e-windows` fails on
   Verdaccio robustness (127.0.0.1 bind / ECONNREFUSED), the `mintCiToken` retry (28-01) should
   have handled it -- if not, that is a real gap to fix, not to wave through.
3. **Close the phase (only after 1 + 2 are GREEN):** mark VER-05 done, then run the deferred
   GSD gates in order: `verify_phase_goal` (gsd-verifier) -> `/gsd-secure-phase 28` ->
   `/gsd-validate-phase 28` -> `/gsd-extract-learnings 28`. Then **Phase 29 (Docs)**.
</remaining_work>

<decisions_made>
- Executors ran SEQUENTIAL on the MAIN checkout, not worktrees (worktree.baseRef unset +
  v0.2.2 not on origin/main + heavy single-port Verdaccio e2e). Continue this for phase 28.
- Windows CI = a SEPARATE `e2e-windows` job (research option b), NOT `matrix.include` (which
  would silently merge the Windows cell into the Linux combination). Locked by GUARD-01f.
- VER-05 was authored as human-run and NOT self-approved by automation (CONTEXT D-08). This
  session's mandate ("autonomous UAT run") overrides that: the next agent RUNS it and records
  real results -- still no fabrication.
- Draft PR #41 opened purely to verify CI (esp. the Windows leg); it is NOT the release cut.
</decisions_made>

<blockers>
- VER-05 UAT not yet executed (task 1 above) -- the phase cannot close until it + PR CI pass.
- The Windows e2e leg proves ONLY on GitHub `windows-latest` (PR #41), never locally.
</blockers>

## Required Reading (in order)
1. `.planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-04-UAT.md` -- the procedure to run.
2. `.planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-CONTEXT.md` -- D-08/D-09 (manual UAT, substrate + SHAs).
3. `.planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-RESEARCH.md` -- "Tertiary" candidate Nx-kind SHAs to re-verify; PM install/shim mechanics.
4. `.planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-REVIEW.md` -- IN-01/IN-02 deferred info findings (optional follow-up).
5. `./AGENTS.md` -- the Release-PR flow (for the eventual real merge; NOT this draft) + e2e CI matrix invariants.

## Infrastructure State
- Branch `gsd/v0.2.2-standalone-cli` pushed to origin (tracking set). 87 commits ahead of main.
- Draft PR #41: https://github.com/LayZeeDK/angular-typechecker/pull/41 (base main, draft, MERGEABLE). CI running at push time: `discover` green + `cli-e2e` in the Linux matrix + `e2e-windows` queued.
- No running local servers/watchers. `node_modules` present on main checkout.

<context>
The whole milestone (phases 25-28) is on this one unpushed-until-now branch. This is the last
verification phase before docs (Phase 29). Everything auto-completable is done and green
locally; what's left is inherently human/CI-gated and is now teed up: run the UAT autonomously,
watch PR #41's Windows leg, then close out. Do not merge the draft.
</context>

<next_action>
Start with: read `28-04-UAT.md`, then `gh pr checks 41` to see current CI state. Kick off the
autonomous UAT (Test 1: `nx build angular-typechecker --skip-nx-cache` + `npm pack`) while the
PR CI runs in parallel. Record UAT results honestly in `28-04-UAT.md`. Close the phase only once
BOTH the UAT and PR #41's `e2e (angular-typechecker-cli-e2e)` + `e2e-windows` are green.
</next_action>
