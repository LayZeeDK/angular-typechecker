---
phase: 18-packaged-tarball-e2e-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/core/detect-unchecked-declared.ts
  - packages/angular-typechecker/src/core/detect-unchecked-declared.spec.ts
  - packages/angular-typechecker/src/core/run-typecheck.ts
  - packages/angular-typechecker/src/core/walk-references.ts
  - packages/angular-typechecker/src/executors/typecheck/executor.ts
  - packages/angular-typechecker/src/executors/typecheck/executor.spec.ts
  - packages/angular-typechecker/src/core/evaluate-result.spec.ts
autonomous: true
requirements: [SB-06]

must_haves:
  truths:
    - "A declared .mdx file (or a .tsx whose resolved compilerOptions.jsx is unset/None) produces a notTypeCheckedDeclaredFiles entry on CoreResult"
    - "The executor prints ONE loud logger.warn naming those files, distinct from the suppressedInGraph notice"
    - "The advisory field NEVER flips the verdict: a result with a non-empty notTypeCheckedDeclaredFiles and errorCount===0 stays { success: true, outcome: 'clean' }"
    - "detection names only the consumer's OWN declared files, never dependency error text"
  artifacts:
    - path: "packages/angular-typechecker/src/core/detect-unchecked-declared.ts"
      provides: "Pure detector: .tsx-without-jsx filter (unit-testable) + .mdx enumeration via ts.parseJsonConfigFileContent extraFileExtensions"
      exports: ["detectTsxWithoutJsx", "detectUncheckedDeclaredFiles"]
    - path: "packages/angular-typechecker/src/core/run-typecheck.ts"
      provides: "notTypeCheckedDeclaredFiles?: readonly string[] on CoreResult, attached [] -> undefined on walk + direct paths"
      contains: "notTypeCheckedDeclaredFiles"
    - path: "packages/angular-typechecker/src/core/walk-references.ts"
      provides: "WalkResult aggregation of surviving-leaf declared-uncheckable paths in the surviving-leaf tail"
      contains: "notTypeCheckedDeclaredFiles"
    - path: "packages/angular-typechecker/src/executors/typecheck/executor.ts"
      provides: "logger.warn render block gated on result.notTypeCheckedDeclaredFiles?.length"
    - path: "packages/angular-typechecker/src/core/evaluate-result.spec.ts"
      provides: "Negative test locking the field out of the verdict"
  key_links:
    - from: "packages/angular-typechecker/src/core/run-typecheck.ts"
      to: "detect-unchecked-declared.ts"
      via: "direct single-leaf path computes from parsed + options.tsConfigPath"
      pattern: "detectUncheckedDeclaredFiles"
    - from: "packages/angular-typechecker/src/core/walk-references.ts"
      to: "detect-unchecked-declared.ts"
      via: "surviving-leaf tail aggregates per leafPath + parsed"
      pattern: "detectUncheckedDeclaredFiles"
    - from: "packages/angular-typechecker/src/executors/typecheck/executor.ts"
      to: "result.notTypeCheckedDeclaredFiles"
      via: "optional-chained length gate before renderReport"
      pattern: "notTypeCheckedDeclaredFiles\\?\\.length"
---

<objective>
Add the D-01 net-new "not type-checked" advisory for declared-but-uncheckable files
(`.mdx` always; `.tsx` only when the resolved `compilerOptions.jsx` is unset/`None`),
following the shipped `skippedReferences` / `templateCheckAborted` detection-in-core +
render-in-executor split EXACTLY. This is criterion 3 / T11's engine half.

Purpose: criterion 3 requires a loud notice for declared files the type-check cannot cover.
Verified absent today (18-RESEARCH RQ2 / D-01). It must be ADVISORY: the verdict stays green.

Output: a pure detector + a new `readonly string[]` CoreResult field surfaced on both the
walk and direct paths, one executor `logger.warn`, and a negative unit test that locks the
field out of the verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-packaged-tarball-e2e-docs/18-CONTEXT.md
@.planning/phases/18-packaged-tarball-e2e-docs/18-RESEARCH.md
@.planning/phases/18-packaged-tarball-e2e-docs/18-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live tree; use directly. -->

CoreResult advisory-field family to extend (run-typecheck.ts lines 40-100). The new
field joins templateCheckAborted? / skippedReferences? with the same additive/advisory
doc discipline and the [] -> undefined conditional-spread idiom (lines 296-299, 590):
  templateCheckAborted?: TemplateCheckAborted;
  skippedReferences?: readonly SkippedReference[];
Recommended new field (18-RESEARCH RQ2):
  notTypeCheckedDeclaredFiles?: readonly string[];   // undefined when empty; ADVISORY, verdict-unchanged

WalkResult to extend (walk-references.ts lines 40-63). Aggregate a parallel readonly
string[] alongside rootNamePaths, populated in the surviving-leaf tail at line 268 AFTER
every skip/not-found/zero-root-names `continue` (Pitfall 7):
  rootNamePaths: readonly string[];   // the aggregation pattern to mirror

ParsedConfiguration the detector reads (compiler-cli-types.ts lines 144-151):
  options: ts.CompilerOptions & { basePath?: string };   // options.jsx is a ts.JsxEmit (None = 0)
  rootNames: readonly string[];

TS 6.0.3 detection primitives (18-RESEARCH RQ2, verified): ts.JsxEmit.None === 0;
"jsx unset/none" == parsed.options.jsx === undefined || parsed.options.jsx === 0.
.tsx is always a TS-supported extension so it is already in parsed.rootNames -> filter
rootNames for .endsWith('.tsx'). .mdx never appears in rootNames -> re-parse the leaf
tsconfig with ts.readConfigFile + ts.parseJsonConfigFileContent(..., extraFileExtensions:
[{ extension: 'mdx', isMixedContent: false, scriptKind: ts.ScriptKind.Unknown }]) and
filter fileNames for .endsWith('.mdx'). ts.sys is permitted in core (walk-references.ts
uses it). Do NOT hand-roll a glob (Don't-Hand-Roll table).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure detect-unchecked-declared helper + unit spec</name>
  <files>packages/angular-typechecker/src/core/detect-unchecked-declared.ts, packages/angular-typechecker/src/core/detect-unchecked-declared.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/run-typecheck.ts (lines 610-654: the exported pure detector `detectTemplateCheckAborted` shape + the `.tsx`/`.ngtypecheck` LIMITATION doc that is a SEPARATE concern -- do not conflate)
    - packages/angular-typechecker/src/core/compiler-cli-types.ts (lines 144-151: ParsedConfiguration)
    - packages/angular-typechecker/src/core/walk-references.ts (lines 105-113: the sanctioned `ts.sys` usage in core)
    - packages/angular-typechecker/src/core/evaluate-result.spec.ts (minimal-literal pure-fn spec idiom)
    - .planning/phases/18-packaged-tarball-e2e-docs/18-RESEARCH.md (RQ2: the exact TS 6.0.3 detection mechanism + Assumptions A1/A2)
  </read_first>
  <behavior>
    - detectTsxWithoutJsx(['/a/x.tsx','/a/y.ts'], undefined) -> ['/a/x.tsx'] (jsx unset)
    - detectTsxWithoutJsx(['/a/x.tsx'], 0 /* JsxEmit.None */) -> ['/a/x.tsx']
    - detectTsxWithoutJsx(['/a/x.tsx'], 4 /* JsxEmit.ReactJSX */) -> [] (jsx enabled: file IS checked)
    - detectTsxWithoutJsx(['/a/y.ts','/a/z.ts'], undefined) -> [] (no .tsx: no false positives)
  </behavior>
  <action>
    Create a pure core module exporting two functions (NO console/process; `ts.sys` and
    `ts.parseJsonConfigFileContent` ARE allowed). `detectTsxWithoutJsx(rootNames: readonly
    string[], jsx: ts.JsxEmit | undefined): readonly string[]` filters `.tsx` rootNames when
    `jsx === undefined || jsx === 0` (ts.JsxEmit.None), else returns []. `detectUncheckedDeclaredFiles(ts:
    typeof import('typescript'), parsed: ParsedConfiguration, leafTsConfigPath: string): readonly
    string[]` returns the union of `detectTsxWithoutJsx(parsed.rootNames, parsed.options.jsx)` and the
    declared `.mdx` set (re-parse leafTsConfigPath via ts.readConfigFile + ts.parseJsonConfigFileContent
    with extraFileExtensions `[{ extension: 'mdx', isMixedContent: false, scriptKind: ts.ScriptKind.Unknown }]`,
    then `fileNames.filter(n => n.endsWith('.mdx'))`) per 18-RESEARCH RQ2 / 18-PATTERNS lines 55-67.
    Write the .spec.ts covering the `.tsx` half only with synthetic input (the behavior block);
    the `.mdx` half is proven at the integration tier in plan 18-03 (needs a real parse). Do NOT
    inline the `.mdx` re-parse into the `.tsx` unit spec.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- --run detect-unchecked-declared</automated>
  </verify>
  <acceptance_criteria>
    - detect-unchecked-declared.spec.ts asserts detectTsxWithoutJsx returns the .tsx path when jsx is undefined AND when jsx===0, [] when jsx===4, and [] on a .ts-only set
    - `git grep -n -e 'console' -e 'process' -- packages/angular-typechecker/src/core/detect-unchecked-declared.ts` returns nothing (core purity)
    - `npx nx test angular-typechecker` green
  </acceptance_criteria>
  <done>The pure detector exists, is exported, and its `.tsx`/jsx branches are unit-proven with synthetic input; no console/process in the new core file.</done>
</task>

<task type="auto">
  <name>Task 2: Surface notTypeCheckedDeclaredFiles on CoreResult via walk + direct paths</name>
  <files>packages/angular-typechecker/src/core/run-typecheck.ts, packages/angular-typechecker/src/core/walk-references.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/run-typecheck.ts (lines 40-100 CoreResult; 276-369 walk branch incl. the `skipped` conditional-spread 296-299 and the rootNamesCount>0 return 310-325; 408-428 direct-path finalize return; 519-592 finalize)
    - packages/angular-typechecker/src/core/walk-references.ts (lines 40-63 WalkResult; 244-271 surviving-leaf tail + return)
    - packages/angular-typechecker/src/core/detect-unchecked-declared.ts (the Task 1 detector)
    - .planning/phases/18-packaged-tarball-e2e-docs/18-PATTERNS.md (lines 80-119 run-typecheck attach points; 123-149 walk aggregation; Pitfall 7)
  </read_first>
  <action>
    Add `notTypeCheckedDeclaredFiles?: readonly string[]` to CoreResult (run-typecheck.ts), documented
    ADDITIVE/advisory in the `suppressedInGraphFiles`/`skippedReferences` family, `undefined` when empty.
    In walk-references.ts add a `notTypeCheckedDeclaredFiles: readonly string[]` to WalkResult, aggregate
    each surviving leaf's `detectUncheckedDeclaredFiles(ts, parsed, leafPath)` in the surviving-leaf tail
    at line 268 (immediately beside `rootNamePaths.push(...)`, AFTER every skip `continue` so an
    out-of-project/zero-root-names/not-found leaf contributes nothing -- Pitfall 7), and return it. In
    run-typecheck.ts attach the field via the `[] -> undefined` conditional-spread idiom on BOTH the walk
    branch (add to the `skipped`-style spread on the `walk.rootNamesCount > 0` return at line 325, sourced
    from `walk.notTypeCheckedDeclaredFiles`) AND the direct single-leaf return (compute
    `detectUncheckedDeclaredFiles(ts, parsed, options.tsConfigPath)` and spread onto the `finalize(...)`
    return at lines 414-427). Do NOT touch the zero-rootNames guard paths (no surviving leaf -> empty).
    Leave the `normalizeShimFileName` LIMITATION doc (lines 637-644) intact -- it is a separate concern.
  </action>
  <verify>
    <automated>npx nx build angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -c 'notTypeCheckedDeclaredFiles' packages/angular-typechecker/src/core/run-typecheck.ts` >= 3 (field decl + walk spread + direct spread)
    - `git grep -c 'notTypeCheckedDeclaredFiles' packages/angular-typechecker/src/core/walk-references.ts` >= 2 (WalkResult field + return)
    - The walk aggregation push sits in the surviving-leaf tail after the last `continue` (adjacent to `rootNamePaths.push`)
    - `npx nx build angular-typechecker` succeeds (the build is the drift guard on the vendored types)
  </acceptance_criteria>
  <done>CoreResult carries the new field on both engine paths, empty-mapped to undefined; the build is green.</done>
</task>

<task type="auto">
  <name>Task 3: Executor render block + green-verdict negative test</name>
  <files>packages/angular-typechecker/src/executors/typecheck/executor.ts, packages/angular-typechecker/src/executors/typecheck/executor.spec.ts, packages/angular-typechecker/src/core/evaluate-result.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/executors/typecheck/executor.ts (lines 108-155: the suppressedThirdParty/suppressedInGraph render blocks + the renderReport call boundary)
    - packages/angular-typechecker/src/executors/typecheck/executor.spec.ts (the SB-04 suppressedInGraph render tests at lines 356-408 -- mirror this behavioral render-test shape; read it first)
    - packages/angular-typechecker/src/core/evaluate-result.ts (lines 62-71 EvaluateInput -- the new field is deliberately ABSENT; 87-132 the ordered verdict)
    - packages/angular-typechecker/src/core/evaluate-result.spec.ts (the "stays clean" advisory cases)
    - .planning/phases/18-packaged-tarball-e2e-docs/18-RESEARCH.md (RQ2 executor render copy; Pitfall 1 + 2)
  </read_first>
  <action>
    In executor.ts add ONE `logger.warn` block gated on `result.notTypeCheckedDeclaredFiles?.length`,
    placed AFTER the `suppressedInGraph` block (ends line 145) and BEFORE `renderReport` (line 147) so it
    is not buried under a codeframe dump. Copy tone from 18-RESEARCH RQ2's render point: state `.mdx` is
    NEVER type-checked and `.tsx` is only checked when `compilerOptions.jsx` is set, that the notice is
    ADVISORY (verdict unchanged), and name the files via `result.notTypeCheckedDeclaredFiles.join(', ')`
    (the consumer's OWN declared files only -- the isolation rule). Do NOT touch evaluate-result.ts:
    the field must NOT enter EvaluateInput (Pitfall 2). In evaluate-result.spec.ts ADD a negative case:
    a result literal carrying a non-empty `notTypeCheckedDeclaredFiles` (via a cast, since EvaluateInput
    does not declare it) with `errorCount: 0`, `warningCount: 0`, and no other coverage trigger asserts
    `{ success: true, outcome: 'clean' }`.
    ALSO add ONE behavioral render test to executor.spec.ts, mirroring the existing SB-04 suppressedInGraph
    render tests (lines 356-408): mock `runTypecheck` to resolve a CoreResult carrying a non-empty
    `notTypeCheckedDeclaredFiles` (e.g. `['/ws/libs/x/docs.mdx']`), and assert `logger.warn` is called with a
    message containing the "not type-checked" advisory text AND naming the file -- this proves the render gate
    actually emits (the structural `git grep` alone cannot). One added test case; do NOT rewrite the suite.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- --run evaluate-result</automated>
  </verify>
  <acceptance_criteria>
    - evaluate-result.spec.ts asserts { success: true, outcome: 'clean' } when notTypeCheckedDeclaredFiles is non-empty and errorCount===0
    - `git grep -c 'notTypeCheckedDeclaredFiles' packages/angular-typechecker/src/core/evaluate-result.ts` == 0 (field never enters the verdict)
    - executor.ts contains exactly one new `logger.warn` gated on `result.notTypeCheckedDeclaredFiles?.length`
    - executor.spec.ts asserts logger.warn is called with a message containing 'not type-checked' (and naming the file) when notTypeCheckedDeclaredFiles is non-empty
    - `npx nx test angular-typechecker` green
  </acceptance_criteria>
  <done>The executor renders the advisory loudly and the negative test proves the field cannot flip the verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer tsconfig -> core | The detector reads paths from the consumer's own tsconfig, already parsed by `readConfiguration` / `ts.parseJsonConfigFileContent`. No new external input crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-I | Information disclosure | executor.ts D-01 notice | mitigate | Notice names ONLY `result.notTypeCheckedDeclaredFiles` (the consumer's own declared paths), never dependency error text -- same isolation rule as `suppressedInGraphFiles`. |
| T-18-01-T | Tampering (V5 input) | detect-unchecked-declared.ts | accept | Reads already-parsed tsconfig paths/globs via `ts.sys`; no new untrusted input, no filesystem writes. Low value target. |
</threat_model>

<verification>
- `npx nx test angular-typechecker` green (unit + integration).
- `npx nx build angular-typechecker` green (vendored-type drift guard).
- `git grep -n -e 'console' -e 'process' -- packages/angular-typechecker/src/core/detect-unchecked-declared.ts` returns nothing (core purity).
- `git grep -c 'notTypeCheckedDeclaredFiles' packages/angular-typechecker/src/core/evaluate-result.ts` == 0.
</verification>

<success_criteria>
- Declared `.mdx` / `.tsx`-without-`jsx` files land in `CoreResult.notTypeCheckedDeclaredFiles` on both engine paths.
- The executor renders one loud advisory naming those files.
- The verdict stays green for a result carrying the field with no errors (negative test passes).
- Core purity preserved; build + tests green.
</success_criteria>

<output>
Create `.planning/phases/18-packaged-tarball-e2e-docs/18-01-SUMMARY.md` when done.
</output>
