---
phase: 08-correctness-completeness-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/core/run-typecheck.ts
  - packages/angular-typechecker/src/core/infra-failure.spec.ts
  - packages/angular-typechecker/src/core/config-resolution.integration.spec.ts
autonomous: true
requirements: [COR-01]
must_haves:
  truths:
    - 'A config-resolution crash (UNKNOWN_ERROR_CODE 500 in readConfiguration().errors) re-throws TypecheckInfrastructureError before the zero-rootNames guard runs (D-01)'
    - 'A nonexistent tsconfig path is classified as infrastructure (rejects), never counted as a type error or folded into reported diagnostics'
    - 'A genuine config diagnostic (code 5012 from a nonexistent extends target) stays folded and is RETURNED with errorCount >= 1, never thrown (D-03 boundary preserved)'
    - 'The existing post-performCompilation 500 scan is retained unchanged (D-02 defense-in-depth, two distinct stages)'
  artifacts:
    - path: 'packages/angular-typechecker/src/core/run-typecheck.ts'
      provides: 'Early parsed.errors 500 scan inserted after readConfiguration, before the zero-rootNames guard'
      contains: 'configInfrastructureFailure'
    - path: 'packages/angular-typechecker/src/core/infra-failure.spec.ts'
      provides: 'COR-01 unit twin: stubbed readConfiguration returning code-500 + rootNames: [] rejects; a code-5012 entry does not throw'
    - path: 'packages/angular-typechecker/src/core/config-resolution.integration.spec.ts'
      provides: 'COR-01 real-compiler integration: nonexistent tsconfig path rejects with TypecheckInfrastructureError; existing malformed (5012) case unchanged'
  key_links:
    - from: 'run-typecheck.ts readConfiguration'
      to: 'TypecheckInfrastructureError throw'
      via: 'parsed.errors.find(d => d.code === ng.UNKNOWN_ERROR_CODE) before the rootNames guard'
      pattern: "parsed\\.errors\\.find"
---

<objective>
COR-01: A config-resolution infrastructure crash -- an `UNKNOWN_ERROR_CODE` (500)
diagnostic returned in `readConfiguration().errors` -- is detected immediately after
the config parse and re-thrown as `TypecheckInfrastructureError`, never folded into the
reported diagnostics or counted as a type error.

Purpose: Today only `result.diagnostics` (post-`performCompilation`) is scanned for the
500 sentinel; `parsed.errors` is folded verbatim into `configDiagnostics`, so a broken
`extends`/host throw is mis-counted as a type error AND can be swallowed by the
zero-rootNames guard (the 500 case has `rootNames: []`). A type-checker that reports
"clean" (or "type error") for an infrastructure crash is a false negative -- the core
correctness guarantee CI/agents depend on.

Output: An early `parsed.errors` 500 scan in `run-typecheck.ts` (structural twin of the
existing post-`performCompilation` scan), plus a failing-then-passing unit twin and a
real-compiler integration case. Both 500 scans coexist (D-02 defense-in-depth).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-correctness-completeness-fixes/08-CONTEXT.md
@.planning/phases/08-correctness-completeness-fixes/08-RESEARCH.md
@.planning/phases/08-correctness-completeness-fixes/08-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase. Use directly; no exploration needed. -->

From packages/angular-typechecker/src/core/run-typecheck.ts:

- `export class TypecheckInfrastructureError extends Error` (line 70) -- the typed infra-vs-type signal. REUSE; do not create a new error class.
- `const parsed = ng.readConfiguration(options.tsConfigPath);` (line 105) -- the insertion point is immediately AFTER this line.
- `const configDiagnostics = [...parsed.errors];` (line 110) -- the new scan goes BEFORE this.
- `if (parsed.rootNames.length === 0) { ... }` (line 117) -- the zero-rootNames guard; the new scan MUST precede it (the 500 case has rootNames: []).
- The EXISTING post-performCompilation 500 scan to mirror (lines 168-179): `result.diagnostics.find((diagnostic) => diagnostic.code === ng.UNKNOWN_ERROR_CODE)` then `throw new TypecheckInfrastructureError(ts.flattenDiagnosticMessageText(infrastructureFailure.messageText, '\n'))`. KEEP this unchanged.
- `ng.UNKNOWN_ERROR_CODE` is the loaded-namespace 500 sentinel (do NOT hardcode 500 in source). `ts` is already loaded at line 103, so `ts.flattenDiagnosticMessageText` is in scope before the guard.

From packages/angular-typechecker/src/core/infra-failure.spec.ts:

- `const UNKNOWN_ERROR_CODE = 500;` (line 15) and `const TS2322 = 2322;` (line 16).
- `vi.hoisted` handle + `vi.mock('./compiler-loader', ...)` (lines 20-47) returning a `CompilerCli` stub whose `readConfiguration` currently returns non-empty `rootNames` (line 33-39).
- `errorDiagnostic(code, message)` helper (lines 49-58) builds file-less `ts.Diagnostic` literals.
- Existing 500-on-result.diagnostics case (lines 77-92) and the contrasting TS2322 case (lines 94-114) -- mirror their `rejects.toBeInstanceOf` / `errorCount` assertion shapes.

From packages/angular-typechecker/src/core/config-resolution.integration.spec.ts:

- `workspaceRoot` join idiom (lines 32-33): `const packageRoot = join(dirname(fileURLToPath(import.meta.url)), '..', '..'); const workspaceRoot = join(packageRoot, '..', '..');`
- Existing malformed-config (5012, nonexistent extends target) "does NOT throw / resolves" assertions (lines 73-98) -- DO NOT regress these.
- `messageTextOf(diagnostic)` helper (lines 54-56).
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the early parsed.errors 500 scan to run-typecheck.ts</name>
  <files>packages/angular-typechecker/src/core/run-typecheck.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/run-typecheck.ts (the file being modified; the existing post-performCompilation 500 scan at lines 168-179 is the exact in-file twin; insertion point is after line 105, before line 110/117)
    - 08-PATTERNS.md section "src/core/run-typecheck.ts (core/orchestrator, transform) -- COR-01" (the verbatim twin + insertion point + critical ordering)
    - 08-RESEARCH.md "Pattern 1: COR-01 ..." + "Pitfall 2: The 500-in-parsed.errors case also has rootNames: []"
    - 08-CONTEXT.md D-01, D-02, D-03
  </read_first>
  <action>
    Insert a new code-500 scan in `run-typecheck.ts` immediately after `const parsed = ng.readConfiguration(options.tsConfigPath);` (line 105) and BEFORE `const configDiagnostics = [...parsed.errors];` (line 110) and the `if (parsed.rootNames.length === 0)` zero-rootNames guard (line 117). The scan is a structural twin of the existing post-`performCompilation` scan (lines 168-179): `parsed.errors.find((diagnostic) => diagnostic.code === ng.UNKNOWN_ERROR_CODE)` into a `const configInfrastructureFailure`, then if it is not `undefined`, `throw new TypecheckInfrastructureError(ts.flattenDiagnosticMessageText(configInfrastructureFailure.messageText, '\n'))`. Detect by CODE only (D-02) -- never `source` or message text. Use `ng.UNKNOWN_ERROR_CODE` (the loaded namespace), never a hardcoded literal 500. Add a comment matching the PATTERNS wording explaining the ordering rationale (the 500 case has `rootNames: []`, so a late scan would be swallowed by the guard and mis-counted). Do NOT touch the existing post-`performCompilation` scan -- both must coexist per D-02. Do NOT broaden infra classification beyond code 500: every other `parsed.errors` entry stays folded into `configDiagnostics` exactly as today (D-03). Follow CLAUDE.md JS/TS style: blank line before/after the new `if` and `throw`, braces on the body.
  </action>
  <acceptance_criteria>
    - `run-typecheck.ts` contains a `configInfrastructureFailure` scan on `parsed.errors` keyed on `=== ng.UNKNOWN_ERROR_CODE`, positioned textually before the `const configDiagnostics = [...parsed.errors];` line and before the `if (parsed.rootNames.length === 0)` guard.
    - The scan re-throws `TypecheckInfrastructureError` with a flattened `messageText`.
    - The existing post-`performCompilation` scan (lines ~168-179) remains present and unchanged.
    - No hardcoded literal `500` appears in the new source (uses `ng.UNKNOWN_ERROR_CODE`).
    - `npx nx build angular-typechecker` succeeds (the module: nodenext GATE A compile -- type-level regression guard; the build is also `dependsOn` of `test`).
  </acceptance_criteria>
  <verify>
    <automated>cd packages/angular-typechecker && npx nx build angular-typechecker</automated>
  </verify>
  <done>The early 500 scan is in place before the guard; both scans coexist; build is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Failing-then-passing COR-01 unit twin + real-compiler integration case</name>
  <files>packages/angular-typechecker/src/core/infra-failure.spec.ts, packages/angular-typechecker/src/core/config-resolution.integration.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/infra-failure.spec.ts (the file being modified; the existing mock scaffolding at lines 20-47, the `errorDiagnostic` helper at 49-58, the 500 case at 77-92, the contrast TS2322 case at 94-114)
    - packages/angular-typechecker/src/core/config-resolution.integration.spec.ts (the file being modified; the workspaceRoot join at 32-33, the malformed-5012 "does NOT throw" cases at 73-98 which must NOT regress)
    - 08-PATTERNS.md sections "src/core/infra-failure.spec.ts ... COR-01 unit (EXTEND)" and "src/core/config-resolution.integration.spec.ts ... COR-01 integration (EXTEND)"
    - 08-RESEARCH.md "COR-01 unit test", "COR-01 integration test (real compiler; nonexistent path)", and "Pitfall 1: Broken extends does NOT reliably produce a 500"
  </read_first>
  <action>
    Extend `infra-failure.spec.ts` with the COR-01 unit twin: add a test that drives `readConfiguration` to return the real 500 shape -- `rootNames: []` AND `errors: [{ category: 1, code: UNKNOWN_ERROR_CODE, source: 'angular', file: undefined, start: undefined, length: undefined, messageText: "Error: ENOENT: no such file or directory, lstat '...'" }]` -- proving `runTypecheck` rejects with `TypecheckInfrastructureError` (mirror the existing `rejects.toBeInstanceOf` at lines 89-91). Because the file's `vi.mock` returns a single `readConfiguration` stub, drive the variant either by making `readConfiguration` a hoisted `vi.fn` you can `mockReturnValueOnce`/`mockReturnValue` per test (mirroring how `performCompilation` is a hoisted handle), or by adding the new case inside its own `vi.mock` arrangement consistent with the file's existing structure -- keep the existing 500-on-result.diagnostics case working. Add a CONTRAST case: a `parsed.errors` entry with `code: 5012` (use the `errorDiagnostic` helper) plus non-empty `rootNames` must NOT throw and must surface in the returned result (assert it resolves; D-03 boundary). Then extend `config-resolution.integration.spec.ts` with the real-compiler case: build a path to a NONEXISTENT tsconfig (e.g. `join(workspaceRoot, 'fixtures', 'config-broken', 'tsconfig.does-not-exist.json')`) and assert `await expect(runTypecheck({ tsConfigPath: missing })).rejects.toBeInstanceOf(TypecheckInfrastructureError)` (dynamic-import `TypecheckInfrastructureError` from `./run-typecheck` as the file's idiom allows, or import at top). Do NOT add a fixture file -- the nonexistent path triggers ENOENT -> a code-500 deterministically cross-OS. Do NOT modify or weaken the existing malformed-5012 "resolves / does NOT throw" assertions. RED-then-GREEN: confirm these new assertions FAIL against the pre-Task-1 source and PASS after.
  </action>
  <behavior>
    - Unit: stubbed `readConfiguration` returning `{ rootNames: [], errors: [code-500 ...] }` -> `runTypecheck(...)` rejects with `TypecheckInfrastructureError` (proves the scan fires BEFORE the zero-rootNames guard).
    - Unit contrast: a `parsed.errors` entry with `code: 5012` + non-empty `rootNames` -> `runTypecheck(...)` resolves (does NOT throw); the 5012 stays folded (D-03).
    - Integration: a nonexistent tsconfig path -> `runTypecheck(...)` rejects with `TypecheckInfrastructureError`.
    - Integration regression: the existing malformed fixture (nonexistent extends target, code 5012) still resolves with `errorCount >= 1` and does NOT throw.
  </behavior>
  <acceptance_criteria>
    - New `infra-failure.spec.ts` case asserts `rejects.toBeInstanceOf(TypecheckInfrastructureError)` for the `{ rootNames: [], errors: [code-500] }` stub.
    - New contrast case asserts the code-5012 + non-empty-rootNames path resolves (does NOT throw).
    - New `config-resolution.integration.spec.ts` case asserts `rejects.toBeInstanceOf(TypecheckInfrastructureError)` for a nonexistent tsconfig path; NO new fixture file is committed.
    - The pre-existing malformed-5012 cases (lines 73-98) are unchanged and still pass.
    - `npx nx test angular-typechecker -- infra-failure config-resolution` passes (both spec files green).
  </acceptance_criteria>
  <verify>
    <automated>cd packages/angular-typechecker && npx nx test angular-typechecker -- infra-failure config-resolution</automated>
  </verify>
  <done>Unit twin + integration case prove the early scan; D-03 contrast and the existing 5012 cases still pass; targeted suite green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                         | Description                                                                                                                             |
| -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| consumer tsconfig path -> engine | The only "input" is the tsconfig path the consumer controls via Nx target wiring; no network, no PII, no auth, no untrusted user input. |

## STRIDE Threat Register

| Threat ID | Category  | Component                           | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                           |
| --------- | --------- | ----------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-08-01   | Spoofing  | run-typecheck.ts config parse       | mitigate    | A config-resolution crash must NEVER be reported as "clean" or as a plain type error (the tool lying about safety). COR-01 re-throws `TypecheckInfrastructureError` on a code-500 in `parsed.errors`; the existing post-`performCompilation` 500 scan is RETAINED (D-02 two-stage defense-in-depth).                                                                      |
| T-08-02   | Tampering | COR-01 re-throw vs real type errors | mitigate    | The re-throw is gated strictly on `code === ng.UNKNOWN_ERROR_CODE` (D-02), so it cannot mask a real type error: every non-500 `parsed.errors` entry (e.g. 5012/18003/1005) stays folded and counted exactly as today (D-03). The CONTRAST unit case + the retained malformed-5012 integration cases prove a genuine config diagnostic is not swallowed by the infra path. |
| T-08-SC   | Tampering | npm/pip/cargo installs              | N/A         | No package installs in this phase (08-RESEARCH Package Legitimacy Audit: not applicable; zero new dependencies).                                                                                                                                                                                                                                                          |

Note: no network, auth, crypto, or PII surface. The realistic threat is correctness (a crash masquerading as a clean/typed verdict) and the inverse (the infra re-throw masking a real type error) -- both mitigated above and test-gated.
</threat_model>

<verification>
- `npx nx build angular-typechecker` green (GATE A nodenext compile).
- `npx nx test angular-typechecker -- infra-failure config-resolution` green (the two COR-01 spec files).
- Full `npx nx test angular-typechecker` green at wave merge.
- `npx nx lint angular-typechecker` green (core/** boundary unchanged; no new `@nx/*` / `process.exit` in core).
</verification>

<success_criteria>

- The early `parsed.errors` 500 scan exists in `run-typecheck.ts` before the zero-rootNames guard, keyed on `ng.UNKNOWN_ERROR_CODE`.
- Both 500 scans (parse + post-compilation) coexist (D-02).
- A nonexistent tsconfig path rejects with `TypecheckInfrastructureError` (integration); a code-500 stub rejects (unit).
- A code-5012 config diagnostic stays folded and is RETURNED, never thrown (D-03 contrast + existing malformed cases unchanged).
  </success_criteria>

<output>
Create `.planning/phases/08-correctness-completeness-fixes/08-01-SUMMARY.md` when done.
</output>
