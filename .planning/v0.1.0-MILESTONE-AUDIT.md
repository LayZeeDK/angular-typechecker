---
milestone: v0.1.0
milestone_name: configuration + init generators, nx add support, and the typecheck executor rename
audited: 2026-07-02T19:15:00Z
status: passed
re_audit:
  previous_status: tech_debt
  previous_audited: 2026-07-02T09:20:57Z
scores:
  requirements: 22/22
  phases: 5/5
  integration: 8/8
  flows: 1/1
nyquist:
  compliant_phases: 5
  partial_phases: 0
  missing_phases: 0
  overall: COMPLIANT
gaps:
  requirements: []   # no unsatisfied requirements
  integration: []    # no broken cross-phase seams (0 BLOCKERs, 0 WARNINGs)
  flows: []           # install/add -> generate -> run -> walk-both-leaves -> verdict completes end-to-end (both entry points: configuration generator AND nx add)
tech_debt: []          # all WARNING/INFO items from the 2026-07-02T09:20:57Z audit are resolved (see Re-Audit Delta)
future_deferred:  # tracked as Future Requirements, NOT debt
  - "FSTREE-01 (bespoke real-disk createFsTree helper) -- only if a future generator emits files."
  - "GEN-FUT-01 (Angular CLI angular.json generator support), GEN-FUT-02 (ng add Angular-CLI schematic). Nx's nx add IS shipped (GEN-09)."
  - "WALK-FUT-01 (createNodesV2 granular per-leaf typecheck targets), WALK-FUT-02 (NgtscProgram incremental declaration-reuse to collapse the walk double-compile tax)."
next_step: "PR #15 is OPEN, MERGEABLE, all 16 checks green. Review + merge as a merge commit (main is PR-only), then /gsd-complete-milestone v0.1.0 -> tag the merge commit angular-typechecker@0.1.0 per AGENTS.md release mechanics (956e657 feat(executor)! + 96e9c83 refactor(core)! both drive the 0.x minor bump to 0.1.0; package.json is still 0.0.3, no tag yet)."
---

# Milestone v0.1.0 -- Audit Report (Re-Audit)

**Milestone:** v0.1.0 -- reference-walking engine, the `typecheck` executor rename, and the configuration + init generator suite (Phases 12-15, incl. inserted 13.1)
**Audited:** 2026-07-02 (re-audit; supersedes the 2026-07-02T09:20:57Z `tech_debt` audit)
**Status:** `passed` -- all 22 requirements satisfied, 0 critical blockers, 0 broken cross-phase seams, full E2E flow green, and every WARNING/INFO item from the prior audit is now resolved.

## Verdict

This is a re-audit of the same milestone, run after ~35 additional commits landed on the branch since the last audit (PR #15 review-triage rounds, a fallow CI-gate fix, an e2e shared-tarball race fix, and two follow-on quick tasks that closed the prior audit's tech debt). All findings below were independently re-verified against the current tree -- phase VERIFICATION.md re-read, the 3-source requirements cross-reference re-run, and a fresh `gsd-integration-checker` pass (opus) re-traced all 6 requested cross-phase seams plus 3 additional ones through actual source files (not summaries).

Every requirement in the milestone is delivered and independently verified, every cross-phase seam is wired, and the headline user flow (`nx add` / `nx g :configuration` -> target wired + caching seeded -> `nx run :typecheck` walks the lib + spec leaves -> correct verdict) is proven end-to-end against the real packed tarball, via BOTH entry points (`configuration` generator and `nx add`). Nyquist coverage is complete across all five phases, and all five carry a SECURITY.md. PR #15 (the milestone PR) is `OPEN`, `MERGEABLE`, with all 16 CI checks green (ci, both CodeQL analyzers, e2e, fallow, format-lint, 7 test-matrix cells across ubuntu/windows/macos x Node 22/24/26).

**Upgrade from `tech_debt` to `passed`:** the prior audit's two WARNINGs (stale README denying the generators exist; README Option B cache recipe using `production` instead of `default`) were both fixed in commit `f947013`. The prior audit's INFO item (root workspace README still the Nx scaffold default) was fixed in `eb5821b`. The prior audit's cosmetic SUMMARY-frontmatter bookkeeping gap (CAT-05/WALK-02/GEN-06 absent from per-plan `requirements-completed`) was closed in `642d08d`. No new debt was introduced by the intervening work.

## Requirements Coverage (3-source cross-reference)

22/22 satisfied. Sources: each phase `VERIFICATION.md` status, each plan `SUMMARY.md` `requirements-completed` frontmatter, and the `REQUIREMENTS.md` traceability table (`[x]`).

| REQ-ID | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md | Final |
|--------|-------|--------------|---------------------|-----------------|-------|
| CAT-01 | 12 | passed | 12-02 | [x] | **satisfied** |
| CAT-02 | 12 | passed | 12-02 | [x] | **satisfied** (D-13 doc reconciled at the prior audit; NG8101 proof) |
| CAT-03 | 12 | passed | 12-03 | [x] | **satisfied** |
| CAT-04 | 12 | passed | 12-02 | [x] | **satisfied** |
| CAT-05 | 12 | passed (Truth 5) | 12-04 | [x] | **satisfied** (frontmatter backfilled 642d08d) |
| DRIFT-01 | 12 | passed | 12-01 | [x] | **satisfied** |
| WALK-01 | 13 | passed | 13-03/13-04 | [x] | **satisfied** |
| WALK-02 | 13 | passed (Truth 26) | 13-06 | [x] | **satisfied** (frontmatter backfilled 642d08d) |
| EXEC-01 | 13.1 | passed | 13.1-01 | [x] | **satisfied** (dual-grep 0 stale ids, re-confirmed this audit) |
| GEN-01 | 14 | passed | 14-02 | [x] | **satisfied** |
| GEN-02 | 14 | passed | 14-02 | [x] | **satisfied** |
| GEN-03 | 14 | passed | 14-02 | [x] | **satisfied** |
| GEN-04 | 14 | passed | 14-02 | [x] | **satisfied** |
| GEN-05 | 14 | passed | 14-03 | [x] | **satisfied** |
| GEN-06 | 14 | passed | 14-01/14-02 | [x] | **satisfied** (frontmatter backfilled 642d08d, dual-listed per WALK-01 precedent) |
| GEN-07 | 14 | passed | 14-01 | [x] | **satisfied** |
| GEN-08 | 14 | passed | 14-02 | [x] | **satisfied** |
| GEN-09 | 14 | passed | 14-03 | [x] | **satisfied** |
| GE2E-01 | 15 | passed | 15-02 | [x] | **satisfied** |
| GE2E-02 | 15 | passed | 15-02 | [x] | **satisfied** |
| GE2E-03 | 15 | passed | 15-02 | [x] | **satisfied** |
| GUARD-01 | 15 | passed | 15-01 | [x] | **satisfied** |

**Orphan detection:** none. Every REQ-ID in the REQUIREMENTS.md traceability table appears in exactly one phase's VERIFICATION.md requirements-coverage table, and now also in at least one plan's SUMMARY frontmatter (the 3 rows that were `*(absent)*` at the prior audit are backfilled).

## Phase Verification Summary

| Phase | Goal | Status | Score |
|-------|------|--------|-------|
| 12 -- Extended-diagnostic catalog + completeness tripwire | Observe the COMPLETE Angular 22 diagnostic surface (18 extended + baseline) by exact identity; drift fails CI loudly | passed | 6/6 |
| 13 -- Engine: solution-tsconfig reference-walking | One `runTypecheck` walks a solution tsconfig's in-project leaves; union+dedupe; boundary guard; three-way split | passed | 26/26 |
| 13.1 -- Rename executor to `typecheck` (INSERTED) | Breaking rename `angular-typecheck` -> `typecheck` everywhere; behavior unchanged; drives 0.1.0 | passed | 5/5 |
| 14 -- configuration + init generators, nx add | `nx g :configuration` wires ONE target; `init` seeds targetDefaults; `nx add` runs `init` | passed | 5/5 |
| 15 -- Generator e2e + CI self-audit guard | Prove generators e2e against the tarball; `-p` set-equality guard | passed | 4/4 |

All five VALIDATION.md report `nyquist_compliant: true` + `wave_0_complete: true`. All five phases carry SECURITY.md. No phase VERIFICATION.md reports any critical anti-pattern; the few Info-level cosmetics logged at verification time (README boilerplate, an advisory-label precision note, notice grammar) are all either fixed (README, grammar -- `216c935`, `f947013`, `eb5821b`) or explicitly accepted-by-design (the self-reference/duplicate advisory-label distinction, now further clarified in `walk-references.ts` comments).

## Cross-Phase Integration (gsd-integration-checker, opus, re-run this audit)

**8/8 seams WIRED. 0 BLOCKER. 0 WARNING. 0 broken links.**

| Seam | Verdict |
|------|---------|
| 1. Phase 13 (walk) -> Phase 13.1 (rename): renamed executor still invokes the walk engine; no stale `angular-typecheck` path | WIRED -- `executor.ts:6-9,47` imports+calls `runTypecheck`; `run-typecheck.ts:15,264` invokes `walkReferences`; `executors.json` implementation resolves; old executor directory does not exist |
| 2. Phase 13.1 (renamed id) -> Phase 14 (generators): both generators key off the new id | WIRED -- single source of truth `init/generator.ts:15 TYPECHECK_EXECUTOR_ID`, imported and reused by `configuration/generator.ts:12,168,185` -- no drift possible |
| 3. Phase 14 (generators) -> Phase 15 (e2e): real tarball, real generators, real walk verdict | WIRED -- `generator-e2e.int.spec.ts` builds+packs a fresh dist, installs the tarball, runs the real generator and a real `nx run :typecheck`; two DISTINCT injected codes (TS2322 lib-only util, TS2345 spec-only) prove both leaves were genuinely walked, not stubbed |
| 4. Phase 12 (catalog) undisturbed by the rename/walk changes | WIRED -- `extended-catalog.integration.spec.ts:9` still imports `runTypecheck` from the current path with the unchanged `CoreOptions` signature; catalog membership/drift modules untouched |
| 5. End-to-end user flow traced link-by-link through source | WIRED -- `configuration` generator -> `executors.json` -> `executor.ts` -> `normalize-options.ts` -> `run-typecheck.ts` -> `walkReferences` -> `finalize` -> `CoreResult` -> `evaluateResult`; fixture `consumer-generator` has no pre-wired target and a solution tsconfig referencing both `tsconfig.lib.json` and `tsconfig.spec.json` |
| 6. No stale `angular-typecheck` (old id) anywhere shipped | WIRED -- `git grep -P 'angular-typecheck\b'` (word-boundary, excludes `angular-typechecker`) = 0 hits across `src/`, `executors.json`, `generators.json`, `package.json`, `e2e/`, repo-wide excl. `.planning/`/CHANGELOG |
| 7. CI self-audit guard (GUARD-01) | WIRED, non-vacuous -- `ci-e2e-coverage-guard.spec.ts` bidirectionally set-equals the real `ci.yml` `e2e` job `-p` list against the real `e2e/*/project.json` names (3==3); `--parallel=1` present, guarded by IN-01 |
| 8. Manual-sync seam (init generator's hand-copied WALK-02 block <-> workspace `nx.json`) | WIRED with guard -- `target-defaults-drift.spec.ts` deep-equals the generator-seeded block against BOTH committed `nx.json` keys (unscoped + scoped); `nx-target-defaults.spec.ts` pins the `default`-not-`production` invariant on both |

Two additional seams (barrel-export/e2e programmatic parity; the scoped-vs-unscoped dev-alias distinction) were checked and confirmed intentional/correct, not counted separately as they are sub-checks of seams 5 and 8.

## Nyquist Compliance

| Phase | VALIDATION.md | Compliant | Wave-0 | Overall |
|-------|---------------|-----------|--------|---------|
| 12 | exists | true | true | COMPLIANT |
| 13 | exists | true | true | COMPLIANT |
| 13.1 | exists | true | true | COMPLIANT |
| 14 | exists | true | true | COMPLIANT |
| 15 | exists | true | true | COMPLIANT |

Overall: **COMPLIANT** (5/5). No `/gsd-validate-phase` re-run needed.

## Re-Audit Delta (what changed since 2026-07-02T09:20:57Z)

Both WARNINGs and all INFO items from the prior audit are now resolved by intervening commits:

1. **[RESOLVED] Shipped README denied the generators exist** (`README.md:59`) -- fixed in `f947013`: now leads with `nx add angular-typechecker` / `nx g angular-typechecker:configuration`, documents `--tsConfig`/`--targetName` + idempotency.
2. **[RESOLVED] Shipped README Option B used `production`** (`README.md:105`) -- fixed in `f947013`: changed to `default` per the WALK-02/GEN-07 invariant, with an explicit note on why.
3. **[RESOLVED] Root workspace README was the Nx scaffold default** -- fixed in `eb5821b`: replaced with a real monorepo README (not shipped in the tarball regardless, so zero consumer impact even before the fix).
4. **[RESOLVED] SUMMARY-frontmatter bookkeeping gap** (CAT-05/WALK-02/GEN-06 absent from `requirements-completed`) -- backfilled in `642d08d`.
5. **[RESOLVED, non-blocking at the time] Executor skipped-reference notice grammar** -- fixed in `216c935`.

No new tech debt was introduced by the ~35 intervening commits, which also included: a public-barrel trim to a minimal API surface (`96e9c83`, a deliberate, user-approved breaking change -- Option A from a PR-review finding, documented in quick task `20260702-trim-public-barrel`), a `/simplify` complexity-reduction pass on the walk/resolver code (`c5e9131`), a fallow CI-gate fix, and an e2e shared-tarball ENOENT race fix (serialized e2e to `--parallel=1`). All were captured as quick tasks with `status: complete` and are reflected in the phase artifacts and CI state audited above.

Deferred future requirements (FSTREE-01, GEN-FUT-01/02, WALK-FUT-01/02, and carried-forward v0.0.1/v0.0.3 families) remain tracked as Future Requirements, not debt.

## Recommended Next Step

The milestone is functionally complete, verified, and has zero outstanding tech debt. PR #15 is green and mergeable.

```
gh pr view 15 --web          # review
# merge as a merge commit (main is PR-only)
/gsd-complete-milestone v0.1.0
```

Note: `package.json` is still `0.0.3` and no `angular-typechecker@0.1.0` tag exists yet. Two breaking commits now drive the 0.0.3 -> 0.1.0 minor bump: `956e657` (`feat(executor)!`, EXEC-01) and `96e9c83` (`refactor(core)!`, public-barrel trim). Tag the merge commit per AGENTS.md release mechanics once the PR lands.

---
_Audited: 2026-07-02T19:15:00Z_
_Auditor: Claude (gsd-audit-milestone orchestrator) + gsd-integration-checker (opus)_
