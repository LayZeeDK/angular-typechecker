---
phase: 260714-1gr-apply-lever-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/test-util/src/lib/e2e-fixture.ts
  - libs/test-util/src/lib/e2e-fixture.spec.ts
  - libs/test-util/src/index.ts
  - e2e/angular-typechecker-install-e2e/src/global-setup.ts
  - e2e/angular-typechecker-ng-cli-e2e/src/global-setup.ts
  - .verdaccio/config.yml
  - .planning/quick/260714-1gr-apply-lever-1-persist-verdaccio-uplink-c/260714-1gr-MEASUREMENTS.md
autonomous: true
requirements:
  - 260714-1gr

must_haves:
  truths:
    - "clearStorage:false is set at BOTH registry global-setups (install-e2e line 113, ng-cli-e2e line 124), so the @nx/js verdaccio executor never wipes storage"
    - "A single shared resetVerdaccioPublishState(root) helper (ONE implementation in test-util, imported by both global-setups) deletes ONLY storage/angular-typechecker + storage/.htpasswd, preserving the rest of storage (the npmjs proxy cache)"
    - "resetVerdaccioPublishState is called BEFORE startLocalRegistry in both global-setups (after the NX_INVOCATION_ROOT_PID delete)"
    - "A full instrumented e2e run is GREEN after the flip -- all invariants survive: the real ci-token mint works (fresh .htpasswd, no 409), our dist republishes clean (fresh storage/angular-typechecker, no EPUBLISHCONFLICT), SAFETY gate + 127.0.0.1 loopback + parallelism:false + --parallel=2 all intact (the COLD run)"
    - "The report shows a cold > warm install-time delta on the Verdaccio-routed subset (install-e2e + ng-cli-e2e), with matrix-e2e roughly FLAT across cold/warm as a control (proves the scope is bounded)"
    - "The 4 now-false comments (.verdaccio/config.yml x2 + both global-setups) are updated to describe the new selective-reset (clearStorage:false + resetVerdaccioPublishState deletes only angular-typechecker + .htpasswd; the npmjs proxy cache persists across runs)"
    - "No package.json version mutation; no product/source change (test-harness + config-comment only)"
  artifacts:
    - path: "libs/test-util/src/lib/e2e-fixture.ts"
      provides: "resetVerdaccioPublishState(root) -- selective storage reset before startLocalRegistry"
      contains: "resetVerdaccioPublishState"
    - path: "libs/test-util/src/lib/e2e-fixture.spec.ts"
      provides: "Unit proof the helper deletes ONLY angular-typechecker + .htpasswd and preserves siblings; first-run (no storage) is a no-op"
    - path: "libs/test-util/src/index.ts"
      provides: "Barrel re-export of resetVerdaccioPublishState from @workspace/test-util"
      contains: "resetVerdaccioPublishState"
    - path: "e2e/angular-typechecker-install-e2e/src/global-setup.ts"
      provides: "clearStorage:false + resetVerdaccioPublishState(root) before startLocalRegistry + updated comment"
      contains: "clearStorage: false"
    - path: "e2e/angular-typechecker-ng-cli-e2e/src/global-setup.ts"
      provides: "clearStorage:false + resetVerdaccioPublishState(root) before startLocalRegistry + updated comment"
      contains: "clearStorage: false"
    - path: ".verdaccio/config.yml"
      provides: "Comments updated (no longer claim clear:true wipes storage each run)"
    - path: ".planning/quick/260714-1gr-apply-lever-1-persist-verdaccio-uplink-c/260714-1gr-MEASUREMENTS.md"
      provides: "cold-vs-warm per-PM measurement + report, matrix flat control, vs w87 baseline, Windows caveat, actions/cache follow-up"
  key_links:
    - from: "e2e/angular-typechecker-{install,ng-cli}-e2e/src/global-setup.ts"
      to: "resetVerdaccioPublishState"
      via: "import from @workspace/test-util; called after `delete process.env.NX_INVOCATION_ROOT_PID` and before `await startLocalRegistry`"
      pattern: "resetVerdaccioPublishState\\(root\\)"
    - from: "resetVerdaccioPublishState"
      to: "<root>/tmp/local-registry/storage"
      via: "rmSync of angular-typechecker (recursive) + .htpasswd only"
      pattern: "rmSync"
    - from: "260714-1gr-MEASUREMENTS.md"
      to: ".planning/quick/260713-w87-.../260713-w87-MEASUREMENTS.md"
      via: "cold-vs-warm delta compared against the w87 cold baseline"
---

<objective>
Apply Lever 1 from the w87 measurement report: persist the Verdaccio npmjs uplink
proxy cache across e2e runs by flipping `clearStorage: true -> false` in BOTH
registry global-setups and, before each `startLocalRegistry`, selectively deleting
ONLY the two run-scoped essentials (`storage/angular-typechecker` so our freshly
built dist republishes clean, and `storage/.htpasswd` so the fresh ci-token mint
still works). Then re-measure the install-time delta (cold vs warm) vs the w87 cold
baseline.

APPLY (local flip) + MEASURE. The `actions/cache` CI extension is DEFERRED to a
documented turnkey follow-up in the report -- it is unmeasurable locally and would
muddy the PR. Do NOT add it in this task.

The RESEARCH (`260714-1gr-RESEARCH.md`) is validated by the orchestrator against
first-party @nx/js source; FOLLOW it exactly.

Purpose: faster e2e re-runs (bounded to the Verdaccio-routed subset) with a
concrete cold-vs-warm number, and a lower-bound estimate of the larger CI win.
Output:
- A shared `resetVerdaccioPublishState(root)` helper (test-util, not shipped) + a
  unit proof it deletes ONLY the right two things.
- Both global-setups flipped to `clearStorage:false` and calling the helper.
- The 4 now-false comments corrected.
- `260714-1gr-MEASUREMENTS.md`: cold-vs-warm per-PM on the Verdaccio-routed subset,
  matrix flat control, vs the w87 baseline, the Windows caveat, and the actions/cache
  follow-up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./AGENTS.md
@./CLAUDE.md
@.planning/quick/260714-1gr-apply-lever-1-persist-verdaccio-uplink-c/260714-1gr-RESEARCH.md
@.planning/quick/260713-w87-measure-e2e-install-time-across-the-matr/260713-w87-MEASUREMENTS.md
@libs/test-util/src/lib/e2e-fixture.ts
@libs/test-util/src/index.ts
@e2e/angular-typechecker-install-e2e/src/global-setup.ts
@e2e/angular-typechecker-ng-cli-e2e/src/global-setup.ts
@.verdaccio/config.yml
@tools/e2e-timing/aggregate-install-timings.mjs

<interfaces>
<!-- VERIFIED contracts the executor needs -- do NOT re-explore. -->

The exact helper to add (from RESEARCH, verified against @nx/js source):

  export function resetVerdaccioPublishState(root: string): void {
    const storageDir = join(root, 'tmp', 'local-registry', 'storage');
    rmSync(join(storageDir, 'angular-typechecker'), { recursive: true, force: true });
    rmSync(join(storageDir, '.htpasswd'), { force: true });
  }

- `force: true` makes the FIRST run (storage absent) a no-op -- never throws.
- Storage path resolution is byte-identical to the executor's:
  global-setup does `root = findWorkspaceRoot(__dirname)`; executor does
  `resolve(context.root, './tmp/local-registry/storage')`; both = <root>/tmp/local-registry/storage.
- `.verdaccio-db.json` (JWT secret + package registry) lives at storage/.verdaccio-db.json
  and is PRESERVED -- a fresh token is minted+used within the same run, so a persistent
  secret is harmless.
- clearStorage contract (VERIFIED start-local-registry.js:22 + verdaccio.impl.js:31):
  startLocalRegistry forks `--clear ${clearStorage ?? true}`; the executor wipes ONLY
  `if (options.clear && existsSync(storage)) rmSync(...)`. With `--clear false` the branch
  is skipped -- our two rmSync calls are the COMPLETE and ONLY reset.

Insertion point in BOTH global-setups (identical edit):
- install-e2e: after `delete process.env.NX_INVOCATION_ROOT_PID;` (line 107) and before
  `const stop = await startLocalRegistry({` (line 109); flip clearStorage (line 113).
- ng-cli-e2e: after the same delete (line 118) and before startLocalRegistry (line 120);
  flip clearStorage (line 124).

Import: both global-setups already `import { buildCleanEnv, findWorkspaceRoot, sh } from '@workspace/test-util';`
-- add `resetVerdaccioPublishState` to that import.

Barrel: libs/test-util/src/index.ts re-exports e2e-fixture's public members
(writeVerdaccioNpmrc, readTypecheckTargetDefault, ...) -- add resetVerdaccioPublishState.

Measurement seam (unchanged; reuse w87): sh() appends one JSONL line per install when
ATC_TIME_INSTALLS=1, to ATC_TIMING_OUT (absolute wins). Aggregate with
`node tools/e2e-timing/aggregate-install-timings.mjs <jsonl>` -- reads ONE JSONL, emits
per-(PM x scenario x action) + per-PM totals + grand total. Run it once per JSONL.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply Lever 1 (shared helper + flip both global-setups + fix 4 comments), prove invariants survive with a folded COLD e2e run</name>
  <files>libs/test-util/src/lib/e2e-fixture.ts, libs/test-util/src/lib/e2e-fixture.spec.ts, libs/test-util/src/index.ts, e2e/angular-typechecker-install-e2e/src/global-setup.ts, e2e/angular-typechecker-ng-cli-e2e/src/global-setup.ts, .verdaccio/config.yml</files>
  <behavior>
    - resetVerdaccioPublishState(root) deletes EXACTLY storage/angular-typechecker (the
      whole dir, recursive) and storage/.htpasswd, and NOTHING else under
      <root>/tmp/local-registry/storage. Siblings (.verdaccio-db.json, any other package
      dir, the npmjs proxy cache) are PRESERVED.
    - When the storage dir does not yet exist (first run), the helper is a no-op and does
      NOT throw (force:true on both rmSync).
  </behavior>
  <action>
    Follow RESEARCH section (a) verbatim.

    1) In `libs/test-util/src/lib/e2e-fixture.ts`: add and export
       `resetVerdaccioPublishState(root: string): void` exactly as the <interfaces>
       block shows (import `rmSync` from node:fs -- `join` is already imported). Keep the
       RESEARCH doc-comment explaining WHY (the two run-scoped essentials + the preserved
       proxy cache + first-run no-op). Do NOT touch the existing helpers.

    2) In `libs/test-util/src/index.ts`: add `resetVerdaccioPublishState` to the existing
       `export { ... } from './lib/e2e-fixture';` block.

    3) Create `libs/test-util/src/lib/e2e-fixture.spec.ts` (vitest, match
       find-workspace-root.spec.ts style): in an OS-temp root, mkdir
       `tmp/local-registry/storage/angular-typechecker` + a sibling
       `tmp/local-registry/storage/other-pkg`, write `storage/.htpasswd`,
       `storage/.verdaccio-db.json`, and `storage/other-pkg/index.json`. Call
       resetVerdaccioPublishState(root). Assert: angular-typechecker dir GONE, .htpasswd
       GONE; .verdaccio-db.json PRESERVED, other-pkg/ + its file PRESERVED. Second case:
       call the helper on a fresh temp root with NO storage dir -> it does not throw.
       Clean up temp dirs in afterEach.

    4) Edit BOTH global-setups identically:
       - Add `resetVerdaccioPublishState` to the `@workspace/test-util` import.
       - Insert the call at the RESEARCH insertion point (after
         `delete process.env.NX_INVOCATION_ROOT_PID;`, before `await startLocalRegistry`):
         ```
         // Lever 1: persist the npmjs uplink proxy cache across runs (clearStorage:false)
         // but reset the two run-scoped essentials so the publish gate + token mint stay
         // correct. MUST run before startLocalRegistry (Verdaccio reads htpasswd + package
         // metadata at boot).
         resetVerdaccioPublishState(root);
         ```
       - Flip `clearStorage: true` -> `clearStorage: false` (install line 113 / ng-cli line 124).

    5) Fix the 4 now-FALSE comments (AGENTS.md comment-accuracy rule) to describe the new
       selective-reset -- clearStorage:false so the @nx/js executor no longer wipes storage;
       resetVerdaccioPublishState deletes ONLY angular-typechecker + .htpasswd each run; the
       rest of storage (the npmjs proxy cache) PERSISTS across runs:
       - `.verdaccio/config.yml` comment @13-17 (the `clear:true ... wipes ... INCLUDING .htpasswd`).
       - `.verdaccio/config.yml` comment @20-22 (`placing it under the cleared storage dir means clear:true resets it each run`).
       - install-e2e global-setup comment @85-88 (`clearStorage wipes the storage dir -- including the htpasswd`).
       - ng-cli-e2e global-setup comment @96-99 (same).
       Keep every OTHER load-bearing comment (127.0.0.1 loopback, NX_INVOCATION_ROOT_PID,
       SAFETY gate) byte-unchanged.

    6) FAST gate first (fail fast before the 23-min run): run the <verify> command. It must
       be GREEN.

    7) Then the folded COLD e2e run = the invariant proof AND run C of the measurement.
       Run on the CURRENT branch (gsd/v0.2.1-angular-cli-workspace-support), MAIN checkout,
       NO worktree (the e2e tier needs real node_modules + Verdaccio). Delete the WHOLE
       storage to force a from-scratch fill, then run instrumented:
         rm -rf tmp/local-registry/storage
         ATC_TIME_INSTALLS=1 ATC_TIMING_OUT="$PWD/tmp/1gr-run1-cold.jsonl" NX_DAEMON=false npx nx run-many -t e2e --parallel=2 --skip-nx-cache
       This is SLOW (~23 min, exceeds the Bash 10-min ceiling) -- run it DETACHED / in the
       background and poll for completion; a long run is NOT a failure. STANDBY RISK: if
       the dev box sleeps mid-run, detached runs are RESUMED not failed -- re-run/resume,
       do not record as failed. `--skip-nx-cache` is REQUIRED (a cache hit runs no installs
       -> empty JSONL). The run exiting 0 proves the flip's invariants survive: real token
       mint works (no 409), our dist republishes clean (no EPUBLISHCONFLICT) -- both failure
       modes are LOUD. KNOWN INFRA (STATE 24-06): ng-cli-e2e can flake under run-many with
       the Nx local-registry "already invoked by a parent Nx process" error; if it flakes,
       re-run ng-cli-e2e standalone with the SAME env
       (`ATC_TIME_INSTALLS=1 ATC_TIMING_OUT="$PWD/tmp/1gr-run1-cold.jsonl" NX_DAEMON=false npx nx e2e angular-typechecker-ng-cli-e2e --skip-nx-cache`)
       so its lines land in the same JSONL; note the re-run in the report.

    FALLBACK (release/additive safety): if the COLD run surfaces an EPUBLISHCONFLICT, a
    token-mint 409, or a stale-dist spec failure, revert `clearStorage:false -> true` in
    both files (one line each) and remove the two helper calls -- a one-commit revert.

    STYLE: Prettier singleQuote, ASCII only, braces on every control-flow body, blank lines
    around control flow + returns. `.verdaccio/config.yml` is Prettier-owned (NOT in
    .prettierignore) -- keep it clean. Do NOT commit tmp/*.jsonl (gitignored raw data).
  </action>
  <verify>
    <automated>npx nx test test-util && npx nx lint test-util && npx nx typecheck test-util && npx nx format:check --files libs/test-util/src/lib/e2e-fixture.ts libs/test-util/src/lib/e2e-fixture.spec.ts libs/test-util/src/index.ts e2e/angular-typechecker-install-e2e/src/global-setup.ts e2e/angular-typechecker-ng-cli-e2e/src/global-setup.ts .verdaccio/config.yml</automated>
  </verify>
  <done>
    The shared resetVerdaccioPublishState helper exists in test-util (ONE implementation,
    exported from the barrel), its unit spec proves it deletes ONLY angular-typechecker +
    .htpasswd and is a first-run no-op, both global-setups import + call it before
    startLocalRegistry with clearStorage:false, and the 4 now-false comments are corrected.
    nx test/lint/typecheck/format:check are GREEN. The folded COLD e2e run
    (`nx run-many -t e2e --parallel=2 --skip-nx-cache`, storage deleted first, timing on)
    EXITED 0 -- proving the token-mint and no-EPUBLISHCONFLICT and dist-republish invariants
    survive the flip -- and produced a non-empty tmp/1gr-run1-cold.jsonl covering npm/pnpm/yarn.
    No package.json version mutated; no product/source change beyond test-util + the two
    global-setups + the config comment.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the WARM re-measure, aggregate cold vs warm, write 260714-1gr-MEASUREMENTS.md</name>
  <files>.planning/quick/260714-1gr-apply-lever-1-persist-verdaccio-uplink-c/260714-1gr-MEASUREMENTS.md</files>
  <action>
    Follow RESEARCH section (c). Run on the CURRENT branch, MAIN checkout, NO worktree.

    1) WARM run -- do NOT delete storage (the T1 COLD run already populated the npmjs proxy
       cache; the helper removes only angular-typechecker + .htpasswd, preserving that cache):
         ATC_TIME_INSTALLS=1 ATC_TIMING_OUT="$PWD/tmp/1gr-run2-warm.jsonl" NX_DAEMON=false npx nx run-many -t e2e --parallel=2 --skip-nx-cache
       SLOW (~23 min) -- run DETACHED / background and poll; a long run is NOT a failure.
       STANDBY RISK: a detached run is RESUMED not failed if the box sleeps; re-run/resume,
       do not record as failed. Same ng-cli-e2e run-many flake fallback as T1 (re-run
       standalone into the SAME warm JSONL if it hits the "already invoked" Nx error).
       Do NOT perturb any isolation invariant; NO package.json version mutation.

    2) Aggregate BOTH JSONLs (reuse the w87 aggregator unchanged, once per file):
         node tools/e2e-timing/aggregate-install-timings.mjs tmp/1gr-run1-cold.jsonl
         node tools/e2e-timing/aggregate-install-timings.mjs tmp/1gr-run2-warm.jsonl
       Capture both markdown outputs.

    3) Write `260714-1gr-MEASUREMENTS.md` in this quick dir with these sections:
       - What + how: Lever 1 applied (clearStorage:false + resetVerdaccioPublishState),
         the 2-run protocol (COLD = run C, storage wiped first; WARM = run W, cache
         preserved), the exact commands, --skip-nx-cache rationale.
       - Environment + CAVEAT: Windows arm64 dev box (Defender inflates node_modules write;
         corepack/cmd.exe spawn tax); dev-box absolutes inflated vs the Linux-only CI e2e
         gate; single run per condition (no median-of-3) -> treat absolutes as directional,
         the RELATIVE cold-vs-warm delta is the valid signal; JSONL append-atomic under
         --parallel=2. The Windows local win is a LOWER BOUND on the proportional CI win.
       - Measured tables: paste the COLD and WARM aggregator tables (per-PM x scenario x
         action + per-PM totals + grand totals).
       - Cold-vs-warm delta: per-PM on the Verdaccio-routed subset (install-e2e + ng-cli
         scenarios). Show matrix-e2e (atc-matrix npm <tgz>, atc-pnpm pnpm add <tgz>) roughly
         FLAT across cold/warm as the CONTROL that proves the scope is bounded (npmjs-direct,
         no registry, no storage touch). Also compare COLD (run C) vs the w87 cold baseline
         (npm ~594s/26, yarn ~209s/11, pnpm ~85s/9, grand ~888s/46) -- run C may already be
         < 888s because the 2nd registry-project warm-hits the 1st's cache within the run;
         do NOT expect an exact w87 reproduction (w87 wiped storage before EACH project).
         Report the actual numbers; do NOT pre-commit to a figure.
       - Honest ceiling: warm collapses the network-fetch portion toward ~0 but the
         irreducible local cost remains (tarball extract, node_modules link, corepack shim,
         Defender). Concrete floor = the w87 in-run 93.4s -> 43.8s (~2.1x) identical-fixture
         drop; a persistent cache generalizes it.
       - actions/cache follow-up: DEFERRED (unmeasurable locally; entangling it muddies the
         PR). Paste the RESEARCH section (d) turnkey recipe (SHA-pin actions/cache; path
         includes tmp/local-registry/storage but EXCLUDES angular-typechecker + .htpasswd;
         key on the Verdaccio-routed fixture lockfiles + plugin manifest + ci.yml; verify
         by 2 consecutive CI e2e runs). State the expected CI win is LARGER than the local
         win (no Defender, faster disk on Linux).
       - GO/NO-GO outcome: state whether the flip held (all e2e green, no EPUBLISHCONFLICT/
         409) and the recommendation (ship the local flip; leave actions/cache as follow-up).

    4) Do NOT commit tmp/*.jsonl (gitignored raw data). Only the report + the T1 code.
  </action>
  <verify>
    <automated>test -s tmp/1gr-run2-warm.jsonl && node -e "const fs=require('fs'); const lines=fs.readFileSync('tmp/1gr-run2-warm.jsonl','utf8').split('\n').filter(Boolean); const rows=[]; for(const l of lines){ try{ rows.push(JSON.parse(l)); }catch{} } if(rows.length===0) throw new Error('no parseable warm timing lines'); const c=rows.map(o=>o.cmd).join(' '); if(!/npm/.test(c)||!/pnpm/.test(c)||!/yarn/.test(c)) throw new Error('missing a PM'); console.log('warm parseable lines',rows.length)" && rg -q "Windows" .planning/quick/260714-1gr-apply-lever-1-persist-verdaccio-uplink-c/260714-1gr-MEASUREMENTS.md && rg -q -i "warm" .planning/quick/260714-1gr-apply-lever-1-persist-verdaccio-uplink-c/260714-1gr-MEASUREMENTS.md && rg -q -i "actions/cache" .planning/quick/260714-1gr-apply-lever-1-persist-verdaccio-uplink-c/260714-1gr-MEASUREMENTS.md && rg -q "matrix" .planning/quick/260714-1gr-apply-lever-1-persist-verdaccio-uplink-c/260714-1gr-MEASUREMENTS.md</automated>
  </verify>
  <done>
    A WARM e2e run produced a non-empty tmp/1gr-run2-warm.jsonl covering npm/pnpm/yarn.
    Both JSONLs are aggregated. `260714-1gr-MEASUREMENTS.md` exists with: the 2-run protocol,
    the Windows caveat, both cold + warm tables, the cold-vs-warm per-PM delta on the
    Verdaccio-routed subset with matrix as a flat control, the comparison vs the w87 cold
    baseline, the honest ceiling, the deferred actions/cache turnkey follow-up (with the
    expected larger CI win), and the GO/NO-GO outcome.
  </done>
</task>

</tasks>

<threat_model>
resetVerdaccioPublishState is new load-bearing logic (a selective filesystem delete), but
it introduces NO new external trust boundary: the registry stays 127.0.0.1-loopback +
SAFETY-gated, single 4873 registry, and the two registry-starting projects never co-run
(parallelism:false), so no concurrent storage touch. The only new risk is the delete
removing the WRONG thing -- but both real failure modes are LOUD and hard-fail the run:
a missed storage/angular-typechecker delete -> `nx release publish` EPUBLISHCONFLICT; a
missed .htpasswd delete -> a duplicate ci-user sign-up 409 in mintCiToken. Neither can
silently ship a stale dist. The T1 unit spec locks that the helper deletes ONLY the two
intended paths (never a sibling / the proxy cache), and the COLD e2e run exercises both
LOUD invariants end-to-end. No product/runtime code, no published-artifact change, no
network-facing surface, no package.json version mutation.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1gr-01 | Tampering | resetVerdaccioPublishState selective delete | mitigate | Unit spec asserts ONLY angular-typechecker + .htpasswd removed, siblings preserved; COLD e2e run exercises the token-mint + republish invariants (both LOUD) |
| T-1gr-02 | Denial of Service | shared storage under --parallel=2 | accept | install-e2e + cache-e2e parallelism:false; install + ng-cli never co-run -> no concurrent storage touch (VERIFIED, unchanged) |
</threat_model>

<verification>
- FAST gate (T1): nx test test-util (new e2e-fixture.spec locks the selective-delete +
  first-run no-op), nx lint test-util, nx typecheck test-util, and format:check on all six
  changed files are GREEN.
- COLD e2e run (T1): `nx run-many -t e2e --parallel=2 --skip-nx-cache` (storage deleted
  first, ATC_TIME_INSTALLS=1) exits 0 -- proving the flip's invariants survive (no
  EPUBLISHCONFLICT, no token 409) -- and produces a non-empty cold JSONL across all 3 PMs.
- WARM e2e run (T2): a second run (no storage delete) exits 0 and produces a non-empty warm
  JSONL across all 3 PMs.
- Report (T2): carries both cold+warm tables, the per-PM cold-vs-warm delta on the
  Verdaccio-routed subset, matrix as a flat control, the Windows caveat, the honest ceiling,
  and the deferred actions/cache follow-up.
</verification>

<success_criteria>
- clearStorage:false at both global-setups; a shared resetVerdaccioPublishState (ONE impl,
  barrel-exported) deletes ONLY angular-typechecker + .htpasswd, called before
  startLocalRegistry in both.
- A full e2e run is GREEN after the flip (all 8 invariants survive; both failure modes stayed
  quiet).
- The 4 now-false comments are corrected; every other load-bearing comment is byte-unchanged.
- The report shows a cold > warm install-time delta on the Verdaccio-routed subset with
  matrix flat as a control, compared vs the w87 baseline, with the Windows caveat and the
  actions/cache follow-up documented.
- No package.json version mutation; no product/source change beyond test-util + the two
  global-setups + the config comment; the flip is a one-line-per-file revert if it breaks.
</success_criteria>

<output>
Create `.planning/quick/260714-1gr-apply-lever-1-persist-verdaccio-uplink-c/260714-1gr-SUMMARY.md` when done.
</output>
