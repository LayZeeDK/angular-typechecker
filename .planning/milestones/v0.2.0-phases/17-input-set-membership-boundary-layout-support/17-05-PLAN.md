---
phase: 17-input-set-membership-boundary-layout-support
plan: 05
type: execute
wave: 3
depends_on: ["17-03"]
files_modified:
  - packages/angular-typechecker/src/executors/typecheck/executor.ts
  - packages/angular-typechecker/src/executors/typecheck/executor.spec.ts
autonomous: true
requirements: [SB-04]

must_haves:
  truths:
    - "The executor renders suppressedThirdParty as an INFO notice (expected node_modules suppressions)"
    - "The executor renders a LOUD coverage-incomplete WARN when suppressedInGraphErrorCount > 0 or suppressedInGraphWarningCount > 0, naming the dropped files (suppressedInGraphFiles)"
    - "Both counts are surfaced in executor stdout AND remain on the structured CoreResult"
    - "The zero-root-names skippedReferences advisory and the templateCheckAborted notice state the verdict is now coverage-incomplete (non-clean), not advisory-only"
    - "The executor's final { success } comes from evaluateResult; a clean host emits no coverage-incomplete WARN"
  artifacts:
    - path: "packages/angular-typechecker/src/executors/typecheck/executor.ts"
      provides: "loud rendering of the two suppressed counts + coverage-incomplete notice from pure CoreResult fields"
      contains: "suppressedInGraph"
    - path: "packages/angular-typechecker/src/executors/typecheck/executor.spec.ts"
      provides: "coreResult() helper updated to the split-counter fields + count-rendering assertions"
  key_links:
    - from: "result.suppressedInGraphErrorCount / suppressedInGraphFiles"
      to: "logger.warn coverage-incomplete notice"
      via: "the adapter render step (mirrors templateCheckAborted/skippedReferences)"
      pattern: "logger.warn"
---

<objective>
Render the two suppressed counts LOUDLY from the pure structured `CoreResult` fields
(SB-04 charter floor): an INFO line for expected `suppressedThirdParty` (node_modules)
suppressions, and a LOUD WARN naming the dropped first-party files when
`suppressedInGraph*` > 0 (coverage is incomplete). Update the existing
`templateCheckAborted` and zero-root-names `skippedReferences` notices so their wording
reflects that these are now coverage-incomplete (non-clean), not advisory-only.

Purpose: CI gates on exit code, agents on the structured verdict -- but a human needs the
loud notice too. This mirrors the existing detection(core)-vs-rendering(adapter) split
(`templateCheckAborted` / `skippedReferences`): the core stays PURE, the adapter is the only
tier allowed to touch `@nx/devkit` `logger`.
Output: the executor rendering + its updated unit tier.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

READ-FIRST source-of-truth:
@.planning/phases/17-input-set-membership-boundary-layout-support/17-CONTEXT.md
@.planning/phases/17-input-set-membership-boundary-layout-support/17-RESEARCH.md

The files being modified + the CoreResult contract (17-03 output):
@packages/angular-typechecker/src/executors/typecheck/executor.ts
@packages/angular-typechecker/src/executors/typecheck/executor.spec.ts
@packages/angular-typechecker/src/core/run-typecheck.ts

Prior-plan SUMMARY (delivered CoreResult fields):
@.planning/phases/17-input-set-membership-boundary-layout-support/17-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: render both suppressed counts + coverage-incomplete notice</name>
  <files>packages/angular-typechecker/src/executors/typecheck/executor.ts</files>
  <read_first>
    - packages/angular-typechecker/src/executors/typecheck/executor.ts (the existing render order: templateCheckAborted WARN, then the skippedReferences per-entry WARN, then renderReport to raw stdout, then evaluateResult; logger is @nx/devkit, adapter-only)
    - 17-RESEARCH.md "Executor rendering (mirror the skippedReferences pattern)" + "Pitfall 5" (surfacing counts silently)
    - 17-CONTEXT.md code_context ("Core is PURE"; the templateCheckAborted / skippedReferences detection-vs-rendering split; D-07 suppressedInGraphFiles is adapter-rendered, names files NOT the dependency's error text)
  </read_first>
  <action>
    After `runTypecheck` and BEFORE `renderReport` (alongside the existing notices), add: (1) when `result.suppressedThirdParty > 0`, `logger.info` an expected-suppression line ("N node_modules diagnostic(s) suppressed (expected; pass includeDeps to include them)"); (2) when `result.suppressedInGraphErrorCount > 0 || result.suppressedInGraphWarningCount > 0`, `logger.warn` a LOUD coverage-incomplete notice that states this run's coverage is INCOMPLETE and the verdict is NOT clean, reports the two counts, and lists the dropped first-party files from `result.suppressedInGraphFiles` (names files ONLY -- never the dependency's error text, preserving content isolation). Update the existing `templateCheckAborted` WARN wording to add that this makes the run coverage-incomplete (a non-clean verdict), and update the `skippedReferences` per-entry wording so the `zero-root-names` reason no longer claims "advisory only -- verdict unchanged" (it now contributes to a coverage-incomplete verdict when a sibling leaf was checked); leave the other reasons (out-of-project / self-reference / duplicate) advisory-wording intact, and keep the `not-found` counted-90002 wording. The final return stays `evaluateResult(result, { maxWarnings })` mapped to Nx `{ success }` (destructure `.success`; the authoritative verdict lives in evaluateResult -- plan 17-04). Keep the raw-stdout `renderReport` path unchanged. Adapter-only logging; do NOT move logging into core.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- executor</automated>
  </verify>
  <acceptance_criteria>
    - `suppressedThirdParty > 0` triggers a `logger.info`; `suppressedInGraph*` > 0 triggers a `logger.warn` naming the files.
    - A clean result (all suppressed fields 0) triggers NEITHER notice.
    - The zero-root-names skippedReferences wording no longer claims "verdict unchanged"; the templateCheckAborted notice states coverage-incomplete.
    - `npx nx test angular-typechecker -- executor` passes (Task 2 specs).
  </acceptance_criteria>
  <done>The adapter loudly surfaces both counts + the coverage-incomplete notice from pure CoreResult fields, isolating dependency error text.</done>
</task>

<task type="auto">
  <name>Task 2: update executor.spec coreResult() helper + count-rendering assertions</name>
  <files>packages/angular-typechecker/src/executors/typecheck/executor.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/executors/typecheck/executor.spec.ts (the `coreResult(errorCount)` helper hardcodes `suppressedCount: 0` ~73; `abortedCoreResult`; the hoisted `loggerInfo`/`loggerWarn` mocks; the evaluateResult mock)
  </read_first>
  <action>
    Update the `coreResult()` helper: REMOVE `suppressedCount`; ADD `suppressedThirdParty: 0`, `suppressedInGraphErrorCount: 0`, `suppressedInGraphWarningCount: 0`, `suppressedInGraphFiles: []` (so every existing test compiles against the new CoreResult). Add tests driving `runTypecheck` (mocked) to return a result with `suppressedThirdParty > 0` -> asserts `loggerInfo` called with the expected-suppression message; and a result with `suppressedInGraphErrorCount > 0` + a `suppressedInGraphFiles` entry -> asserts `loggerWarn` called with a coverage-incomplete message that CONTAINS the dropped file path but NOT any dependency error text. Add a clean-result test asserting NEITHER `loggerInfo` nor `loggerWarn` fired for the counts. Keep the evaluateResult mock returning `{ success, outcome }` shape where the test needs it (or just `{ success }` -- the executor reads only `.success`).
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- executor</automated>
  </verify>
  <acceptance_criteria>
    - `coreResult()` returns the four new fields and no `suppressedCount`.
    - Tests assert INFO for third-party, WARN (with file path) for in-graph, and silence on a clean result.
    - `npx nx test angular-typechecker` full suite passes.
  </acceptance_criteria>
  <done>The executor unit tier proves the loud count rendering and updates the shared coreResult() helper to the split-counter shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CoreResult (in-process) -> executor stdout | the loud notice is the human-facing surface of coverage loss; it must name files but never leak a dependency's error text (content isolation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-12 | Repudiation (silent coverage loss) | a suppressed count present but never rendered | mitigate | the adapter renders BOTH counts (INFO third-party, WARN in-graph naming files); a clean run stays silent; mirrors the audited templateCheckAborted/skippedReferences split |
| T-17-13 | Information disclosure (dependency error text leaking) | rendering suppressed diagnostics' content | mitigate | render ONLY file paths from suppressedInGraphFiles (pure detection), never the dependency's message text -- preserves content isolation (criterion 3) |
| T-17-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | N/A -- no package installs this phase |
</threat_model>

<verification>
- `npx nx test angular-typechecker -- executor` green.
- Per wave merge: full suite + build + lint + format:check (CI gates).
</verification>

<success_criteria>
- Both suppressed counts render loudly (INFO third-party, WARN in-graph naming files); a clean run is silent.
- The coreResult() helper matches the split-counter CoreResult shape.
- Dependency error text is never surfaced by the counts rendering.
</success_criteria>

<output>
Create `.planning/phases/17-input-set-membership-boundary-layout-support/17-05-SUMMARY.md` when done.
</output>
