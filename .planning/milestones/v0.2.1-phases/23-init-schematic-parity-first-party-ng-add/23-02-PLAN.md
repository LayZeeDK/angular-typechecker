---
phase: 23-init-schematic-parity-first-party-ng-add
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/package.json
  - packages/angular-typechecker/eslint.config.mjs
  - packages/angular-typechecker/src/package-manifest.spec.ts
autonomous: true
requirements: [ACP-01, NGADD-01]

must_haves:
  truths:
    - "ACP-01 (D-07): package.json declares `@angular-devkit/architect` (`^0.2200.0`) and `rxjs` (`^7.8.0`) as peerDependencies with `peerDependenciesMeta.<dep>.optional: true`; `nx` is NOT declared (it flows in transitively via @nx/devkit's own peer)."
    - "ACP-01 (D-08): both optional peers are added to the `@nx/dependency-checks` rule's `ignoredDependencies` in eslint.config.mjs BY HAND; `nx lint angular-typechecker` is green (maxWarnings:0); `checkVersionMismatches:false` is preserved (never `eslint --fix` the manifest)."
    - "ACP-01: the `ng add` pulls `nx` transitively via @nx/devkit + may materialize a `.nx/` dir consequence is documented as a code comment (the README `## Angular CLI` section is deferred to Phase 24 / ACD-01, so prose is minimal here)."
    - "NGADD-01 (RF-01): package.json declares the top-level `\"ng-add\": { \"save\": \"devDependencies\" }` field so `@angular/cli`'s `ng add` installs angular-typechecker into devDependencies -- the idiomatic Angular-native install-placement lever (precedent: @angular-eslint/schematics)."
    - "The existing package-manifest.spec.ts contract assertions (files allowlist, engines, exports, existing peer ranges, `nx` absent) stay green -- every addition here is purely additive."
  artifacts:
    - path: "packages/angular-typechecker/package.json"
      provides: "The two optional peerDependencies + peerDependenciesMeta + the ng-add.save field."
      contains: "peerDependenciesMeta"
    - path: "packages/angular-typechecker/eslint.config.mjs"
      provides: "The @nx/dependency-checks ignoredDependencies lever + the nx-transitive/.nx doc comment."
      contains: "ignoredDependencies"
    - path: "packages/angular-typechecker/src/package-manifest.spec.ts"
      provides: "Static contract assertions for ng-add.save, the two optional peers, and peerDependenciesMeta.optional."
      min_lines: 100
  key_links:
    - from: "package.json#peerDependenciesMeta"
      to: "package.json#peerDependencies (@angular-devkit/architect, rxjs)"
      via: "optional:true marking the two runtime peers of the converted builder"
      pattern: "\"optional\": true"
    - from: "eslint.config.mjs @nx/dependency-checks ignoredDependencies"
      to: "the two optional peers (not imported by src/, require()d inside @nx/devkit)"
      via: "ignoredDependencies short-circuits the obsolete-dependency check"
      pattern: "@angular-devkit/architect"
---

<objective>
Classify the converted builder's runtime peers and set the `ng add` install placement (ACP-01 + the RF-01
lever of NGADD-01): declare `@angular-devkit/architect` + `rxjs` as OPTIONAL peerDependencies, keep the
`@nx/dependency-checks` gate green by hand-adding both to `ignoredDependencies`, document the
`nx`-transitive/`.nx/` consequence as a code comment, and declare the `ng-add.save: devDependencies`
manifest field so `ng add angular-typechecker` installs into devDependencies.

Purpose: the converted builder `require()`s `@angular-devkit/architect` + `rxjs` at runtime (the
`require()`s live inside `@nx/devkit`, invisible to this plugin's dep linter), and a type-checker is dev
tooling that must land in devDependencies. This plan owns ALL package.json manifest edits + the static
manifest contract test, so Plan 03 (the ng-add generator) touches no manifest. This plan is INDEPENDENT of
the init/ng-add generator code and runs in Wave 1 alongside Plan 01.

Output: the extended `package.json`, `eslint.config.mjs`, and `package-manifest.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-init-schematic-parity-first-party-ng-add/23-CONTEXT.md
@.planning/phases/23-init-schematic-parity-first-party-ng-add/23-RESEARCH.md
@.planning/phases/23-init-schematic-parity-first-party-ng-add/23-PATTERNS.md

<interfaces>
<!-- Exact analog shapes + verified versions. Use directly; no exploration needed. -->

Current package.json peerDependencies block to EXTEND (L50-53):
  "peerDependencies": { "@angular/compiler-cli": "^22.0.0", "typescript": ">=6.0.0 <6.1.0" }

Verified versions (RESEARCH L94-95, registry 2026-07-10): @angular-devkit/architect latest 0.2200.6
(peer range `^0.2200.0` -- the 0.22xx.x scheme, NOT 22.x); rxjs latest 7.8.2 (peer range `^7.8.0`).

Current eslint.config.mjs @nx/dependency-checks rule-options object to EXTEND (L66-88):
  '@nx/dependency-checks': ['error', { checkVersionMismatches: false, ignoredFiles: [ ... ] }]
  // add `ignoredDependencies: ['@angular-devkit/architect', 'rxjs']` to this SAME options object.

Current package-manifest.spec.ts PluginManifest interface (L32-56) lacks `peerDependenciesMeta` and the
`['ng-add']` field -- extend the interface, then add a new describe block. Its existing assertions use
per-key property access (not a whole-object toEqual on peerDependencies), so extra peers do NOT break them;
`files` IS a strict toEqual but is NOT modified by this plan.

Precedent for ng-add.save (RESEARCH L440): node_modules/@angular-eslint/schematics/package.json ships
`"ng-add": { "save": "devDependencies" }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare optional peers + peerDependenciesMeta + ng-add.save; add the @nx/dependency-checks ignoredDependencies lever + doc comment</name>
  <files>packages/angular-typechecker/package.json, packages/angular-typechecker/eslint.config.mjs</files>
  <read_first>
    - packages/angular-typechecker/package.json (whole file -- the peerDependencies block, the field ordering, the files whitelist which must NOT change)
    - packages/angular-typechecker/eslint.config.mjs L66-88 (the @nx/dependency-checks block with checkVersionMismatches:false + ignoredFiles)
    - 23-PATTERNS.md L257-311 (the three package.json edits + the eslint ignoredDependencies edit, incl. the "by hand / never eslint --fix" warning)
    - 23-RESEARCH.md L82-95 (the exact manifest additions) + L254-258 (Pitfall 4: the ignoredDependencies short-circuit) + L146 (the .nx/ consequence doc scope) + L419 (never eslint --fix; checkVersionMismatches guards the public ranges)
  </read_first>
  <action>
    (1) Edit `package.json` with three ADDITIVE changes (never touching `executors`/`generators`/`builders`/
    `schematics`/`files`/`dependencies`, and never declaring `nx`):
      (a) ADD to the existing `peerDependencies` block: `"@angular-devkit/architect": "^0.2200.0"` (the
          0.22xx.x scheme, NOT 22.x) and `"rxjs": "^7.8.0"`.
      (b) ADD a NEW top-level `"peerDependenciesMeta"` object marking BOTH as optional:
          `{ "@angular-devkit/architect": { "optional": true }, "rxjs": { "optional": true } }`.
      (c) ADD a NEW top-level `"ng-add": { "save": "devDependencies" }` field (RF-01 Approach C -- the
          idiomatic Angular-native install-placement lever; precedent @angular-eslint/schematics).
    Keep JSON Prettier-clean; ASCII only.

    (2) Edit `eslint.config.mjs`: add `ignoredDependencies: ['@angular-devkit/architect', 'rxjs']` to the
    SAME `@nx/dependency-checks` rule-options object that already sets `checkVersionMismatches: false`
    (do NOT change `checkVersionMismatches: false` or `ignoredFiles`). Add a concise code comment on that
    array explaining (i) WHY these are ignored -- the `require()`s live inside `@nx/devkit`, so the plugin's
    own `src/` never imports them and the obsolete-dependency check would otherwise fire; and (ii) the ACP-01
    consequence: `ng add` into a non-Nx workspace pulls `nx` transitively via `@nx/devkit`'s peer and may
    materialize a `.nx/` dir (accepted; README `## Angular CLI` prose is Phase 24 / ACD-01). Add
    `ignoredDependencies` BY HAND -- NEVER run `eslint --fix` on the manifest (it would rewrite the public
    peer ranges to installed exacts despite `checkVersionMismatches:false` guarding version-mismatch autofix).
  </action>
  <verify>
    <automated>nx lint angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `nx lint angular-typechecker` exits 0 (maxWarnings:0): `@nx/dependency-checks` does NOT report the two new peers as obsolete (ignoredDependencies short-circuit), and `@nx/nx-plugin-checks` accepts the new peer/meta/ng-add fields.
    - `git grep -n "@angular-devkit/architect" -- packages/angular-typechecker/package.json` shows it under BOTH peerDependencies and peerDependenciesMeta; likewise `git grep -n "rxjs" -- packages/angular-typechecker/package.json`.
    - `git grep -n "\"ng-add\"" -- packages/angular-typechecker/package.json` matches the top-level save field.
    - `git grep -n "ignoredDependencies" -- packages/angular-typechecker/eslint.config.mjs` shows the array (the entry is on a code line, so no comment-exclusion is needed); confirm both peers are listed in it.
    - package.json still does NOT declare `nx` in dependencies or peerDependencies; `checkVersionMismatches: false` is still present in eslint.config.mjs.
  </acceptance_criteria>
  <done>The two optional peers + peerDependenciesMeta + ng-add.save are declared, the @nx/dependency-checks gate is green with the hand-added ignoredDependencies + doc comment, and no public peer range was rewritten.</done>
</task>

<task type="auto">
  <name>Task 2: Static manifest contract spec for ng-add.save + optional peers + peerDependenciesMeta.optional</name>
  <files>packages/angular-typechecker/src/package-manifest.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/package-manifest.spec.ts (whole file, 151 lines -- the PluginManifest interface + the two existing describe blocks; extend, do not rewrite)
    - 23-PATTERNS.md L359-362 (the static peer-dep spec) + 23-RESEARCH.md L382 (ACP-01 static assertion) + L375 (the ng-add.save static assertion)
  </read_first>
  <action>
    Extend `package-manifest.spec.ts` (the natural home for the manifest contract -- fewest files):
      (1) Extend the `PluginManifest` interface with `peerDependenciesMeta?: Record<string, { optional?: boolean }>`
          and `'ng-add'?: { save?: string }`.
      (2) Add a NEW `describe` block (title it for ACP-01 + RF-01) asserting:
          - `manifest['ng-add']?.save === 'devDependencies'` (RF-01);
          - `manifest.peerDependencies?.['@angular-devkit/architect'] === '^0.2200.0'` and
            `manifest.peerDependencies?.['rxjs'] === '^7.8.0'` (D-07 ranges);
          - `manifest.peerDependenciesMeta?.['@angular-devkit/architect']?.optional === true` and
            `manifest.peerDependenciesMeta?.['rxjs']?.optional === true` (optional classification);
          - (reassert) `manifest.peerDependencies` still does NOT have `nx` (guard against a regression).
    Leave the existing CMP-01/CMP-02 and PKG-01 blocks untouched. Pure filesystem read (no compiler-cli load,
    no build) so it stays in the fast tier. JS/TS style: blank lines around control-flow; braces on all bodies.
  </action>
  <verify>
    <automated>nx test angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `nx test angular-typechecker` exits 0, including the new ACP-01/RF-01 describe block in package-manifest.spec.ts and all existing manifest assertions.
    - `git grep -n "peerDependenciesMeta\|ng-add" -- packages/angular-typechecker/src/package-manifest.spec.ts` shows the new assertions.
    - The pre-existing `files` toEqual allowlist assertion still passes (this plan did not modify `files`).
  </acceptance_criteria>
  <done>The static manifest contract proves ng-add.save === devDependencies, both optional peers with their ranges, and peerDependenciesMeta.optional === true, with the existing manifest contract untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published package.json / lint config -> consumer install + CI gate | The manifest peers + ng-add.save drive how a consumer installs the package and which runtime peers are declared; the eslint config drives the CI dependency gate. No network, no runtime input. Threats are ASVS L1 dependency-classification + packaging-hygiene. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02 | Tampering | angular-typechecker classified as a production `dependency` (wrong for a dev-only type-checker) | mitigate | `ng-add.save: devDependencies` makes `@angular/cli` install with `--save-dev`; the Plan-03 defensive tree edit backstops the install-skipped edge; static spec asserts the field. |
| T-23-07 | Tampering | `@nx/dependency-checks` autofix rewriting the PUBLIC peer ranges to installed exacts | mitigate | `checkVersionMismatches: false` preserved; `ignoredDependencies` added BY HAND; never `eslint --fix` the manifest (project rule). |
| T-23-08 | Information Disclosure / Tampering | declaring `nx` explicitly double-constrains the consumer's Nx | mitigate | Do NOT declare `nx` (accept the transitive pull via @nx/devkit's peer; documented as a code comment). |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | N/A -- Plan 23-02 installs no external packages; the two optional peers are canonical first-party Angular/RxJS packages already in the workspace (RESEARCH Package Legitimacy Audit: Approved, no new runtime packages). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `nx lint angular-typechecker` green (the @nx/dependency-checks gate accepts the optional peers via ignoredDependencies; no public range rewritten).
- `nx test angular-typechecker` green (the new ACP-01/RF-01 static assertions + all existing manifest assertions).
</verification>

<success_criteria>
- ACP-01: `@angular-devkit/architect` + `rxjs` are declared as OPTIONAL peerDependencies, `@nx/dependency-checks` stays green, and the `nx`-transitive + `.nx/` consequence is documented (code comment).
- NGADD-01 (RF-01 portion): `ng add angular-typechecker` installs into devDependencies via the `ng-add.save` manifest field.
</success_criteria>

<output>
Create `.planning/phases/23-init-schematic-parity-first-party-ng-add/23-02-SUMMARY.md` when done.
</output>
