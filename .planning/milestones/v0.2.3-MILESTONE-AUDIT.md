---
milestone: v0.2.3
audited: 2026-07-19
status: passed
scores:
  requirements: 13/13
  phases: 3/3
  integration: 13/13
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 32-verification-docs-additive-audit
    items:
      - "Human verification (pending CI): cross-OS/Node byte-stability of the committed redacted integration snapshots (machine-reporters-{json,sarif}.integration.spec.ts.snap) is confirmed only on the local Windows dev machine. The branch is 95 commits ahead of origin (unpushed by design -- main is PR-only). The 6-cell OS x Node matrix confirmation runs when the v0.2.3 Release-PR opens. NOT a code gap -- the redaction + forward-slash-URI mechanism is wired and locally green; only cross-OS equality remains CI-observable."
  - phase: 30-reporter-seam-json-reporter-format-threading-observability
    items:
      - "Coverage-tier (WARNING, non-blocking): executor normalize-options.spec.ts was not extended with a direct unit test for the new `format` field default/forwarding. Covered indirectly at the schema-parity, executor.spec.ts (hoisted mock), and shipped-tarball e2e tiers -- not a wiring break."
  - phase: 31-sarif-reporter
    items:
      - "Coverage-tier (WARNING, non-blocking): builder.integration.spec.ts (the lighter TestingArchitectHost tier) was not extended to drive `--format json/sarif` through a real BuilderContext. The builder machine-format seam is proven end-to-end at the heavier shipped-tarball `ng run` e2e tier (ng-add-ng-run.e2e.spec.ts) instead."
  - phase: docs
    items:
      - "Doc drift (corrected by this audit): REQUIREMENTS.md marked the 7 Phase-30 requirements (FMT-01/02/03, REP-01, OBS-01, CLIX-02) and VER-01/02/03/04, DOC-01 as `[ ]` / `Pending` in the traceability table despite all being VERIFIED in their phase VERIFICATION.md files. Checkboxes + statuses updated to reflect verified state."
      - "Cosmetic: VER-04 was omitted from 31-02-SUMMARY.md `requirements_completed` frontmatter (it is tagged in `tags:` and VERIFIED as SATISFIED in 31-VERIFICATION.md)."
---

# Milestone v0.2.3 -- Machine-readable reporters -- Audit

**Audited:** 2026-07-19
**Status:** passed
**Phases:** 30, 31, 32 (all complete)
**Version:** 0.2.2 (the `0.2.3` bump is a release-time action; this milestone is additive-only patch)

## Summary

Milestone v0.2.3 adds machine-readable output -- JSON (agent-parseable) and SARIF 2.1.0
(GitHub Code Scanning `upload-sarif`) -- across all three adapters (Nx executor, Angular CLI
builder, standalone `atc` CLI) over the one shared `runTypecheck` core. The verdict and exit
code stay owned by the engine (`evaluateResult`/`toExitCode`); the reporters are pure
functions over `CoreResult`. The whole milestone is proven additive-only vs
`angular-typechecker@0.2.2`.

All 13 milestone requirements are **satisfied** by the 3-source cross-reference
(VERIFICATION.md + SUMMARY frontmatter + REQUIREMENTS.md traceability). Cross-phase
integration is **integrated** -- every REQ-ID resolves to a traced multi-phase or
adapter-to-adapter seam, verified against actual source (not SUMMARY prose). No critical
blockers. The only open items are two coverage-tier test WARNINGs (both covered at other
tiers) and one pending CI cross-OS byte-stability confirmation that is inherent to the
release process (the branch is unpushed by design; main is PR-only).

## Scores

| Dimension | Score | Notes |
|-----------|-------|-------|
| Requirements | 13/13 | All satisfied; 0 unsatisfied; 0 orphaned |
| Phases | 3/3 | 30 passed (8/8), 31 passed (9/9), 32 human_needed (4/4 code truths VERIFIED; 1 CI human-verify item) |
| Integration | 13/13 | Every REQ-ID WIRED; verdict integrated |
| Flows (adapters) | 3/3 | CLI, Nx executor, Angular CLI builder all emit JSON + SARIF end-to-end via shipped tarball |

## Phase Verification Roll-up

| Phase | Status | Must-haves | Security | Nyquist |
|-------|--------|-----------|----------|---------|
| 30 -- Reporter seam + JSON + `--format` threading + observability | passed | 8/8 verified | SECURED (0 open) | COMPLIANT |
| 31 -- SARIF reporter | passed | 9/9 verified | SECURED (0 open) | COMPLIANT |
| 32 -- Verification + docs + additive audit | human_needed | 4/4 verified | SECURED (7/7 closed) | COMPLIANT |

Phase 32's `human_needed` is **not** a code gap: all four VER-02/VER-03/ADD-01/DOC-01 truths
are VERIFIED locally (139 integration tests + all three e2e projects green, additive-only
git-diff reproduced independently, README/CHANGELOG confirmed). The single human-verification
item is the cross-OS/Node matrix confirmation of the committed redacted snapshots, which is
CI-observable only and runs when the Release-PR opens.

## Requirements Coverage (3-Source Cross-Reference)

| REQ-ID | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS table | Final |
|--------|-------|--------------|---------------------|--------------------|-------|
| FMT-01 | 30 | passed / SATISFIED | listed (30-02, 30-03) | Pending* | **satisfied** |
| FMT-02 | 30 | passed / SATISFIED | listed (30-02) | Pending* | **satisfied** |
| FMT-03 | 30 | passed / SATISFIED | listed (30-02, 30-03) | Pending* | **satisfied** |
| REP-01 | 30 | passed / SATISFIED | listed (30-02) | Pending* | **satisfied** |
| OBS-01 | 30 | passed / SATISFIED | listed (30-01) | Pending* | **satisfied** |
| CLIX-02 | 30 | passed / SATISFIED | listed (30-03) | Pending* | **satisfied** |
| VER-01 | 30 | passed / SATISFIED | listed (all 30/31) | Complete | **satisfied** |
| REP-02 | 31 | passed / SATISFIED | listed (31-01) | Complete | **satisfied** |
| VER-04 | 31 | passed / SATISFIED | missing (cosmetic)** | Complete | **satisfied** |
| VER-02 | 32 | passed / SATISFIED | listed (32-01) | Complete | **satisfied** |
| VER-03 | 32 | passed / SATISFIED | listed (32-02) | Complete | **satisfied** |
| ADD-01 | 32 | passed / SATISFIED | listed (32-03) | Complete | **satisfied** |
| DOC-01 | 32 | passed / SATISFIED | listed (32-04) | Complete | **satisfied** |

\* REQUIREMENTS.md traceability drift: these 7 Phase-30 rows read `Pending` / `[ ]` despite
being VERIFIED in 30-VERIFICATION.md. Per the audit status matrix (passed + listed -> update
checkbox), corrected by this audit.

\** VER-04 is absent from 31-02-SUMMARY.md `requirements_completed` frontmatter but is tagged
in `tags:` and VERIFIED as SATISFIED in 31-VERIFICATION.md (require-graph guard 3/3, real-import
interop 1/1, `nx lint` dependency-checks green). Cosmetic frontmatter omission only.

**Orphan detection:** none. Every REQ-ID in the REQUIREMENTS.md traceability table appears in
at least one phase VERIFICATION.md requirements table.

**FAIL gate:** not triggered. Zero `unsatisfied` requirements.

## Cross-Phase Integration (gsd-integration-checker)

**Verdict: integrated.** No BLOCKERs. Seams verified against actual source under
`packages/angular-typechecker/src/`, `libs/test-util/`, and `e2e/`, plus
`git diff angular-typechecker@0.2.2`:

1. `--format` enum (`human|json|sarif`, default `human`) threaded identically across CLI
   `parse-args`, executor `schema.json`/`schema.d.ts`, builder `schema.json`; both
   `schema-parity.spec.ts` files pin the same `EXPECTED_KEYS` incl. `format`. -- WIRED
2. `render-report.ts` dispatches on `format`: human loads `@angular/compiler-cli`; json ->
   `formatJsonReport`; sarif -> `await import('./sarif-report.js')`. -- WIRED
3. SARIF reuses Phase 30's `toDiagnosticRecord` projection (no re-implemented `path.relative`
   / position math). JSON + SARIF share one normalized record. -- WIRED
4. Exit-code parity across formats incl. the coverage-incomplete (`errorCount===0`,
   `success===false`) case, proven at unit AND shipped-tarball e2e tiers through all three
   real adapters. -- WIRED
5. Machine payloads stdout-only, zero ANSI under `FORCE_COLOR=1`; advisories to stderr;
   `--quiet` gates stderr chatter only (executor CR-01 gate + main.ts quiet gate). -- WIRED
6. Additive-only: `index.ts`, `index.drift.ts`, `builder.ts` byte-unchanged vs 0.2.2; only
   new dep is `node-sarif-builder@^4.1.0` (classified as `dependency`, policed by
   `@nx/dependency-checks`). -- WIRED

### Non-blocking WARNINGs (test-coverage tier, not functional breaks)

1. `normalize-options.spec.ts` (executor) not extended with a direct `format` default/forward
   unit test -- covered at schema-parity + executor.spec + tarball e2e tiers.
2. `builder.integration.spec.ts` (lighter `TestingArchitectHost` tier) not extended to drive
   `--format json/sarif` -- covered by the heavier shipped-tarball `ng run` e2e.

## Nyquist Compliance

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Status |
|-------|---------------|-------------------|-----------------|--------|
| 30 | exists | true | true | COMPLIANT |
| 31 | exists | true | true | COMPLIANT |
| 32 | exists | true | true | COMPLIANT |

**Overall: COMPLIANT** (3/3 phases validated).

## Security

| Phase | SECURITY.md | threats_open | Verdict |
|-------|-------------|--------------|---------|
| 30 | exists | 0 | secure |
| 31 | exists | 0 | secure |
| 32 | exists | 0 | SECURED (7/7 closed) |

## Definition of Done (ROADMAP milestone goal)

> Add machine-readable output -- JSON (agent-parseable) and SARIF 2.1.0 (GitHub Code Scanning
> `upload-sarif`) -- across all three adapters over the one shared `runTypecheck` core, so AI
> coding agents and CI can consume the complete diagnostic set as data. Additive-only patch
> bump (`0.2.2 -> 0.2.3`).

**Met.** JSON + SARIF ship across CLI, Nx executor, and Angular CLI builder over the shared
core; verdict/exit code stay engine-owned; additive-only proven via git-diff + barrel drift
tripwire; README + CHANGELOG documented in end-user language.

## Verdict

**PASSED.** All 13 requirements satisfied, cross-phase integration integrated, security
secured, Nyquist compliant. No critical blockers. Two minor coverage-tier WARNINGs and one
pending CI cross-OS confirmation are the only open items -- none block milestone completion;
the CI confirmation is the normal next step when the Release-PR opens.

---

*Audited: 2026-07-19*
*Auditor: Claude (gsd-audit-milestone orchestrator + gsd-integration-checker)*
