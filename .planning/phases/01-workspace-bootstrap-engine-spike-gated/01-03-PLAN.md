---
phase: 01-workspace-bootstrap-engine-spike-gated
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - packages/angular-typechecker/src/core/compiler-loader.ts
  - packages/angular-typechecker/src/core/gather-diagnostics.ts
  - packages/angular-typechecker/src/core/run-typecheck.ts
  - packages/angular-typechecker/src/index.ts
  - packages/angular-typechecker/src/executors/angular-typecheck/executor.ts
  - packages/angular-typechecker/src/executors/angular-typecheck/schema.json
  - packages/angular-typechecker/src/executors/angular-typecheck/schema.d.ts
  - packages/angular-typechecker/executors.json
  - fixtures/gate-b-error/error.component.ts
  - fixtures/gate-b-error/error.component.html
  - fixtures/gate-b-error/tsconfig.app.json
  - fixtures/gate-b-error/tsconfig.lib.json
autonomous: true
requirements: [ENG-03, WS-02]
must_haves:
  truths:
    - "The core loads ESM @angular/compiler-cli via a memoized await import() (the only runtime value-import of compiler-cli)"
    - "The all-getter gathers ALL six diagnostic getters unconditionally including getNgSemanticDiagnostics()"
    - "runTypecheck(CoreOptions) returns a structured CoreResult with codes[], errorCount, warningCount, and durationMs"
    - "A minimal Nx executor stub default-exports an adapter that delegates to runTypecheck"
    - "The deliberate-error fixture compiles a TS2322 + NG8109 component out of the project graph with its own app + lib tsconfigs"
    - "nx build angular-typechecker produces a built executor.js"
    - "Honors locked decisions D-08 (tracer-bullet core, promoted not throwaway), D-13 (deliberate-error fixture out of the project graph with its own app + lib tsconfigs, nothing imports it), D-16 (Approach A: performCompilation + unconditional all-getter incl. getNgSemanticDiagnostics()), and D-18 (pin the spike to stable Angular 22.0.4)"
  artifacts:
    - path: "packages/angular-typechecker/src/core/compiler-loader.ts"
      provides: "Memoized await import('@angular/compiler-cli') ESM bridge (ENG-03, GATE A runtime path)"
      contains: "await import"
    - path: "packages/angular-typechecker/src/core/gather-diagnostics.ts"
      provides: "Unconditional 6-getter gatherer including getNgSemanticDiagnostics()"
      contains: "getNgSemanticDiagnostics"
    - path: "packages/angular-typechecker/src/core/run-typecheck.ts"
      provides: "performCompilation orchestration returning structured CoreResult"
      exports: ["runTypecheck", "CoreOptions", "CoreResult"]
    - path: "packages/angular-typechecker/src/executors/angular-typecheck/executor.ts"
      provides: "Thin CJS executor stub (the GATE A artifact); only tier importing @nx/devkit"
    - path: "fixtures/gate-b-error/error.component.ts"
      provides: "Co-located TS2322 + NG8109 fixture (out of graph, nothing imports it)"
      contains: "count: number = 'not a number'"
  key_links:
    - from: "packages/angular-typechecker/src/core/run-typecheck.ts"
      to: "@angular/compiler-cli (ESM)"
      via: "loadCompilerCli() memoized await import()"
      pattern: "await import\\(['\"]@angular/compiler-cli"
    - from: "packages/angular-typechecker/src/executors/angular-typecheck/executor.ts"
      to: "runTypecheck"
      via: "default export delegates to core"
      pattern: "runTypecheck"
---

<objective>
Build the tracer-bullet core engine (`compiler-loader`, `gather-diagnostics`, `run-typecheck`), the
minimal Nx executor stub, the executor manifest (`executors.json` + `schema.json`/`schema.d.ts`), and
the out-of-graph deliberate-error fixture (TS2322 + NG8109), then build the plugin with `@nx/js:tsc`
so the `import(`-bearing `executor.js` exists for Plan 04's GATE A static check.

Purpose: this is the promoted tracer-bullet (D-08) -- lean production-quality code that Phase 2 grows,
not a throwaway. The core/adapter split is honored: `src/core/*` has ZERO `@nx/devkit` imports and the
only runtime value-import of `@angular/compiler-cli` lives in `compiler-loader.ts` (every other core
file uses `import type`). The executor stub is the artifact GATE A inspects. The fixture is the
compiler input GATE B asserts on -- it lives out of the project graph (D-13) so `apps/ng-spike-app`
stays green.

Output: the built `dist/.../executor.js` (still containing literal `import(`) plus the committed
fixture + core + executor source. Plan 04 drives the gate assertions against these.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-workspace-bootstrap-engine-spike-gated/01-CONTEXT.md
@.planning/phases/01-workspace-bootstrap-engine-spike-gated/01-RESEARCH.md
@.planning/phases/01-workspace-bootstrap-engine-spike-gated/01-PATTERNS.md
@.planning/phases/01-workspace-bootstrap-engine-spike-gated/01-02-SUMMARY.md

<interfaces>
<!-- Contracts this plan creates that Plan 04's gate specs consume. Author these signatures exactly. -->

src/core/compiler-loader.ts:
  export async function loadCompilerCli(): Promise<typeof import('@angular/compiler-cli')>

src/core/gather-diagnostics.ts:
  export function gatherAllDiagnostics(program: Program): readonly ts.Diagnostic[]
  // Program imported `import type { Program } from '@angular/compiler-cli'` (type-only)

src/core/run-typecheck.ts:
  export interface CoreOptions { tsConfigPath: string }
  export interface CoreResult {
    diagnostics: readonly ts.Diagnostic[];
    codes: number[];            // for code-based assertions: 2322, 8109
    errorCount: number;
    warningCount: number;
    durationMs: number;         // cold-run timing (gate item 6)
  }
  export async function runTypecheck(options: CoreOptions): Promise<CoreResult>

src/executors/angular-typecheck/schema.d.ts:
  export interface AngularTypecheckExecutorOptions { tsConfig: string }

src/executors/angular-typecheck/executor.ts:
  export default async function angularTypecheckExecutor(
    options: AngularTypecheckExecutorOptions, _context: ExecutorContext,
  ): Promise<{ success: boolean }>
</interfaces>

<notes>
- The verbatim shapes to follow are in 01-PATTERNS.md "Pattern Assignments" (one section per file,
  with the verified analog source + line numbers) and 01-RESEARCH.md "Code Examples" + Patterns 1-4.
  Copy the verified shapes; do not re-derive.
- GATE A static-token discipline (Shared Pattern, 01-PATTERNS.md): keep the literal substring
  `import(` OUT of comments in `compiler-loader.ts`/`executor.ts` so the GATE A regex matches the
  real `await import(` call, not a comment line.
- DROP the prototype's node_modules/out-of-project diagnostic filtering -- out-of-project filtering
  is DEFERRED to Phase 3 (OUT-02, D-10). This core does NO filtering.
- Build `parsed` ONCE and spread into a FRESH `options` object per `performCompilation` call so the
  differential's two calls (Plan 04) do not share mutated `noEmit` state (research Open Question 1).
- `emitFlags: 0 as ng.EmitFlags` = no emit (prototype idiom, correct).
</notes>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the tracer-bullet core (compiler-loader, gather-diagnostics, run-typecheck, index)</name>
  <files>packages/angular-typechecker/src/core/compiler-loader.ts, packages/angular-typechecker/src/core/gather-diagnostics.ts, packages/angular-typechecker/src/core/run-typecheck.ts, packages/angular-typechecker/src/index.ts</files>
  <read_first>
    - .planning/phases/01-workspace-bootstrap-engine-spike-gated/01-PATTERNS.md ("compiler-loader.ts", "gather-diagnostics.ts", "run-typecheck.ts" sections -- verified analog shapes)
    - .planning/phases/01-workspace-bootstrap-engine-spike-gated/01-RESEARCH.md (Patterns 1-3; Don't Hand-Roll table; Pitfall 6 UNKNOWN_ERROR_CODE)
    - .planning/phases/01-workspace-bootstrap-engine-spike-gated/01-CONTEXT.md (D-08 tracer bullet, D-16 Approach A 6 getters, ENG-03)
    - .planning/PROJECT.md (core/adapter split: zero @nx/devkit imports in core)
  </read_first>
  <behavior>
    - `loadCompilerCli()` returns the compiler-cli namespace via a memoized `await import('@angular/compiler-cli')`; the second call returns the same cached module (no second import).
    - `gatherAllDiagnostics(program)` calls all six getters unconditionally in order (`getTsOptionDiagnostics`, `getNgOptionDiagnostics`, `getTsSyntacticDiagnostics`, `getTsSemanticDiagnostics`, `getNgStructuralDiagnostics`, `getNgSemanticDiagnostics`) and returns the concatenation -- never short-circuiting after a TS error.
    - `runTypecheck({ tsConfigPath })` resolves to a `CoreResult` with `codes` = `diagnostics.map(d => d.code)`, `errorCount` counted via `DiagnosticCategory.Error`, `warningCount` = total - errors, and `durationMs` measured around `performCompilation`.
  </behavior>
  <action>
    Create `src/core/compiler-loader.ts` (Pattern 1): a module-level `let cached`, `export async
    function loadCompilerCli()` doing `cached ??= await import('@angular/compiler-cli'); return
    cached;`, with `import type * as ng from '@angular/compiler-cli'` (type-only). This is the SINGLE
    runtime value-import of compiler-cli (GATE A runtime path). Create `src/core/gather-diagnostics.ts`
    (Pattern 2): the ported 6-getter `gatherAllDiagnostics(program: Program)` calling every getter
    unconditionally, with `import type { Program } from '@angular/compiler-cli'` and `import type ts
    from 'typescript'` (type-only). Create `src/core/run-typecheck.ts` (Pattern 3): `CoreOptions`,
    `CoreResult`, and `runTypecheck` that `await loadCompilerCli()`, `ng.readConfiguration(tsConfigPath)`,
    times a `ng.performCompilation({ rootNames, options: { ...parsed.options, noEmit: true },
    emitFlags: 0 as ng.EmitFlags, gatherDiagnostics: gatherAllDiagnostics })`, and returns the
    structured result (`codes`, `errorCount` via `DiagnosticCategory.Error`, `warningCount`,
    `durationMs`). Create `src/index.ts` re-exporting the core surface (`runTypecheck`, `CoreOptions`,
    `CoreResult`). Re-validate all six getter names against `@angular/compiler-cli@22.0.4`. Do NOT add
    out-of-project/node_modules filtering (deferred, D-10). Follow CLAUDE.md JS/TS style (blank lines
    around control flow + returns; always braces).
  </action>
  <verify>
    <automated>npx nx test angular-typechecker 2>&1 | rg -q "import\(" ; node -e "const fs=require('fs');const c=fs.readFileSync('packages/angular-typechecker/src/core/compiler-loader.ts','utf8').split('\n').filter(l=>!l.trimStart().startsWith('//')).join('\n');const g=fs.readFileSync('packages/angular-typechecker/src/core/gather-diagnostics.ts','utf8');process.exit(/await import\(['\"]@angular\/compiler-cli/.test(c)&&/getNgSemanticDiagnostics/.test(g)?0:1)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `compiler-loader.ts` source contains `await import('@angular/compiler-cli')` on a non-comment line and uses `import type * as ng`.
    - `gather-diagnostics.ts` calls all six getters including `getNgSemanticDiagnostics()`, and imports `Program` + `ts` type-only.
    - `run-typecheck.ts` exports `runTypecheck`, `CoreOptions`, `CoreResult` matching the `<interfaces>` block; spreads a FRESH `options` object with `noEmit: true`; uses `emitFlags: 0 as ng.EmitFlags`.
    - No `@nx/devkit` import in any `src/core/*` file (core/adapter split).
    - `npx nx test angular-typechecker` exits 0 (generated sample spec + any new unit assertions pass).
  </acceptance_criteria>
  <done>The promoted core engine loads compiler-cli via await import(), gathers all six getters unconditionally, and returns a structured CoreResult.</done>
</task>

<task type="auto">
  <name>Task 2: Build the executor stub + manifest, and the out-of-graph error fixture</name>
  <files>packages/angular-typechecker/src/executors/angular-typecheck/executor.ts, packages/angular-typechecker/src/executors/angular-typecheck/schema.json, packages/angular-typechecker/src/executors/angular-typecheck/schema.d.ts, packages/angular-typechecker/executors.json, fixtures/gate-b-error/error.component.ts, fixtures/gate-b-error/error.component.html, fixtures/gate-b-error/tsconfig.app.json, fixtures/gate-b-error/tsconfig.lib.json</files>
  <read_first>
    - .planning/phases/01-workspace-bootstrap-engine-spike-gated/01-PATTERNS.md ("executor.ts", "schema.json + schema.d.ts", "executors.json", "fixtures/gate-b-error/*" sections)
    - .planning/phases/01-workspace-bootstrap-engine-spike-gated/01-RESEARCH.md (Pattern 4 executor stub; "GATE B fixture component"; Pitfall 4 strictTemplates direct; Code Examples fixture tsconfig)
    - .planning/phases/01-workspace-bootstrap-engine-spike-gated/01-CONTEXT.md (D-11 executor stub, D-13 fixture placement, D-17 fixture shape codes 2322+8109, D-18 stable 22.0.4)
  </read_first>
  <action>
    Create `src/executors/angular-typecheck/executor.ts` (Pattern 4): default-export
    `angularTypecheckExecutor(options, _context)` that `await runTypecheck({ tsConfigPath:
    options.tsConfig })` and returns `{ success: result.errorCount === 0 }`, with `import type
    { ExecutorContext } from '@nx/devkit'` (type-only). This is the ONLY tier importing `@nx/devkit`;
    keep it thin. Create `schema.json` (minimal v2): `$schema`, `$id: "AngularTypecheckExecutorOptions"`,
    `type: object`, one `tsConfig` string property, `required: ["tsConfig"]`, `additionalProperties:
    false` (strict). Create `schema.d.ts`: `export interface AngularTypecheckExecutorOptions
    { tsConfig: string }`. Ensure `executors.json` maps `angular-typecheck` to
    `./src/executors/angular-typecheck/executor` (extensionless) + the `schema.json` (trim the
    generated manifest to ONE entry). Create the out-of-graph fixture `fixtures/gate-b-error/`:
    `error.component.ts` (standalone component, `count: number = 'not a number';` for TS2322 +
    `status = signal('ready');` referenced un-invoked for NG8109, `templateUrl:
    './error.component.html'`); `error.component.html` containing `{{ status }}` (NOT `status()`);
    `tsconfig.app.json` and `tsconfig.lib.json` BOTH extending `../../tsconfig.base.json` with
    `compilerOptions: { noEmit: true, moduleResolution: "bundler" }` and
    `angularCompilerOptions: { strictTemplates: true }` set DIRECTLY (Pitfall 4) -- the two variants
    differ only in `files`/`include`/lib-style `rootDir` (gate breadth item 5). The signal property
    name MUST match between the `.ts` and the `.html`. Do NOT use `@ts-nocheck`. Confirm the fixture
    dir is EXCLUDED from the project graph (no `project.json`, listed in the plugin
    `tsconfig.lib.json` exclude from Plan 02) and that NO workspace file imports it (TS #36017).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const e=fs.readFileSync('packages/angular-typechecker/src/executors/angular-typecheck/executor.ts','utf8');const comp=fs.readFileSync('fixtures/gate-b-error/error.component.ts','utf8');const html=fs.readFileSync('fixtures/gate-b-error/error.component.html','utf8');const app=require('./fixtures/gate-b-error/tsconfig.app.json');process.exit(/runTypecheck/.test(e)&&/count: number = 'not a number'/.test(comp)&&/signal\(/.test(comp)&&/\{\{\s*status\s*\}\}/.test(html)&&app.angularCompilerOptions.strictTemplates===true?0:1)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `executor.ts` default-exports a function delegating to `runTypecheck`; imports `ExecutorContext` type-only; no other `@nx/devkit` runtime import.
    - `schema.json` has `additionalProperties: false`, `required: ["tsConfig"]`, one `tsConfig` property; `schema.d.ts` matches `AngularTypecheckExecutorOptions`.
    - `executors.json` has exactly one entry (`angular-typecheck` -> extensionless executor path + schema.json).
    - `fixtures/gate-b-error/error.component.ts` has `count: number = 'not a number'` (TS2322) and a `signal('ready')` property whose name matches the `{{ ... }}` interpolation in `error.component.html` (NG8109).
    - `tsconfig.app.json` AND `tsconfig.lib.json` both set `angularCompilerOptions.strictTemplates: true` directly and `compilerOptions.noEmit: true`.
    - `npx nx show projects` does NOT list any fixture project (out of graph, D-13); `apps/ng-spike-app` is still green.
  </acceptance_criteria>
  <done>The executor stub, manifest, and the out-of-graph TS2322+NG8109 fixture (app + lib tsconfig variants) exist.</done>
</task>

<task type="auto">
  <name>Task 3: Build the plugin and confirm the import(-bearing executor.js exists</name>
  <files>(reads built dist/.../executor.js; no new source files) packages/angular-typechecker build output</files>
  <read_first>
    - .planning/phases/01-workspace-bootstrap-engine-spike-gated/01-02-SUMMARY.md (the recorded generated build outputPath)
    - .planning/phases/01-workspace-bootstrap-engine-spike-gated/01-RESEARCH.md (Pitfall 2 @nx/js:tsc reads-not-reassigns module; System Architecture Diagram build step)
    - .planning/phases/01-workspace-bootstrap-engine-spike-gated/01-PATTERNS.md ("project.json" build asset-copy; derive dist path from outputPath -- Open Q 2)
  </read_first>
  <action>
    Run `npx nx build angular-typechecker`. Using the `build.options.outputPath` recorded in the
    01-02-SUMMARY, locate the built `dist/.../src/executors/angular-typecheck/executor.js` and
    confirm (via `node -e` + `fs.readFileSync`, NOT `git grep` -- `dist/` is gitignored) that the
    built artifact contains a literal `import(` on a non-comment line and does NOT contain
    `require("@angular/compiler-cli")` / `require('@angular/compiler-cli')`. Record the EXACT
    resolved built `executor.js` path in the SUMMARY so Plan 04's GATE A static spec derives it
    (do not hard-code). If the built file shows `__importStar(require("@angular/compiler-cli"))`, the
    module patch from Plan 02 did not take -- STOP and report (do not proceed to Plan 04).
  </action>
  <verify>
    <automated>npx nx build angular-typechecker 2>&1 | rg -q -e "Successfully ran" -e "successfully" ; BUILT=$(ls dist/**/src/executors/angular-typecheck/executor.js 2>/dev/null | head -1); test -n "$BUILT" && node -e "const fs=require('fs');const c=fs.readFileSync(process.argv[1],'utf8').split('\n').filter(l=>!l.trimStart().startsWith('//')).join('\n');process.exit(/import\(/.test(c)&&!/require\(['\"]@angular\/compiler-cli/.test(c)?0:1)" "$BUILT" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx nx build angular-typechecker` succeeds (CJS `.js` + `.d.ts` emitted via `@nx/js:tsc`).
    - The built `executor.js` contains a literal `import(` on a non-comment line.
    - The built `executor.js` does NOT contain `require("@angular/compiler-cli")` or `require('@angular/compiler-cli')`.
    - `executors.json` + `schema.json` are present in the build output (asset-copy globs).
    - The exact resolved built `executor.js` path is recorded in the SUMMARY for Plan 04.
  </acceptance_criteria>
  <done>The plugin builds to CommonJS and the emitted executor.js retains a literal import( (module: nodenext did not downlevel it) -- the GATE A artifact is ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor (untrusted tsConfig path) -> core | the executor's only input is a `tsConfig` path string crossing into `runTypecheck` |
| core -> ESM compiler-cli | `await import()` loads and executes the compiler at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | Built executor downlevels `import()` to `require()` (ERR_REQUIRE_ESM at runtime) | mitigate | Task 3 reads the built `.js` and asserts literal `import(` present + `require('@angular/compiler-cli')` absent; halts before Plan 04 if the patch did not take |
| T-01-07 | Information Disclosure | ESM-load failure masquerades as a diagnostic (`UNKNOWN_ERROR_CODE` 500 from `performCompilation`'s catch) | mitigate | `runTypecheck` surfaces config errors as the result; Plan 04 GATE B asserts `not.toContain(500)` so a hidden load failure cannot be mistaken for a passing run (Pitfall 6) |
| T-01-08 | Spoofing | Stray workspace import of the error fixture re-introduces TS2322/NG8109 into `ng-spike-app` (TS #36017) | mitigate | Fixture is out of graph (no `project.json`), excluded in `tsconfig.lib.json`, and nothing imports it; `apps/ng-spike-app` green-smoke assertion is the backstop |
| T-01-09 | Elevation of Privilege | Executor input validation absent (V5) | accept | Phase-1 input is a single `tsConfig` path normalized by `readConfiguration`; full schema validation/normalize-options DEFERRED to Phase 4 (EXE-01); low risk for an in-repo spike |
</threat_model>

<verification>
- `src/core/*` has zero `@nx/devkit` imports; `compiler-loader.ts` is the only runtime value-import of compiler-cli.
- `gather-diagnostics.ts` calls all six getters including `getNgSemanticDiagnostics()`.
- `runTypecheck` returns `codes`, `errorCount`, `warningCount`, `durationMs`.
- Executor stub delegates to `runTypecheck`; `@nx/devkit` imported type-only.
- Fixture: TS2322 + NG8109 component, out of graph, app + lib tsconfig variants, `strictTemplates: true` direct.
- `nx build angular-typechecker` succeeds; built `executor.js` retains literal `import(`, no `require('@angular/compiler-cli')`.
</verification>

<success_criteria>
The tracer-bullet core loads ESM compiler-cli via `await import()` and gathers all diagnostics
unconditionally (ENG-03); the executor stub + manifest exist; the out-of-graph TS2322+NG8109 fixture
(app + lib variants) is committed; and `nx build angular-typechecker` produces an `executor.js` that
still contains a literal `import(` (WS-02).
</success_criteria>

<output>
Create `.planning/phases/01-workspace-bootstrap-engine-spike-gated/01-03-SUMMARY.md` when done.
CRITICAL: record the EXACT resolved built `executor.js` path (Plan 04 derives the GATE A path from
it) and the absolute paths of `fixtures/gate-b-error/tsconfig.app.json` + `tsconfig.lib.json`.
</output>
