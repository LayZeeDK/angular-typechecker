---
phase: 13-engine-solution-tsconfig-reference-walking
plan: 04
type: execute
wave: 3
depends_on: ["13-03"]
files_modified:
  - packages/angular-typechecker/src/core/run-typecheck.ts
  - packages/angular-typechecker/src/index.ts
  - packages/angular-typechecker/src/executors/angular-typecheck/executor.ts
autonomous: true
requirements: [WALK-01]
must_haves:
  truths:
    - "runTypecheck on a solution tsconfig with references + >=1 in-project leaf walks the leaves and feeds the union into the SINGLE existing finalize"
    - "references present + 0 in-project leaves -> synthesized 90001 with a distinct none-in-project message"
    - "no references -> unchanged empty-project 90001 error"
    - "the rootNames > 0 direct-leaf path and the COR-01 direct 500 scan/rethrow (:149-178) stay BYTE-UNCHANGED"
    - "CoreResult carries an optional skippedReferences field (D-02); the executor adapter renders it via logger.warn, advisory-only"
  artifacts:
    - path: "packages/angular-typechecker/src/core/run-typecheck.ts"
      provides: "Three-way D-03a split invoking walkReferences; union -> single finalize; skippedReferences threaded onto CoreResult"
      contains: "walkReferences"
    - path: "packages/angular-typechecker/src/index.ts"
      provides: "Public export of SkippedReference type"
      contains: "SkippedReference"
    - path: "packages/angular-typechecker/src/executors/angular-typecheck/executor.ts"
      provides: "logger.warn render of skippedReferences, gated on non-empty"
      contains: "skippedReferences"
  key_links:
    - from: "packages/angular-typechecker/src/core/run-typecheck.ts"
      to: "packages/angular-typechecker/src/core/walk-references.ts"
      via: "await walkReferences(ng, ts, parsed, options.tsConfigPath) at the D-03a split"
      pattern: "walkReferences"
    - from: "packages/angular-typechecker/src/executors/angular-typecheck/executor.ts"
      to: "result.skippedReferences"
      via: "logger.warn gated on non-empty array"
      pattern: "skippedReferences"
---

<objective>
Wire the walk into the engine: replace the single-branch D-03a guard in `run-typecheck.ts` with the
THREE-WAY split (L-3 / Spike 004), invoke `walkReferences` on the references-present-and->=1-in-project
branch, feed the walk's raw union into the EXISTING single `finalize` (same filter args, basePath =
solution dir, `includeDeps` once), thread `skippedReferences` onto `CoreResult`, add the optional
`skippedReferences?` field + re-export `SkippedReference`, and render the advisory `logger.warn`
notice in the executor adapter (mirroring the `templateCheckAborted` seam). The `rootNames > 0`
direct-leaf path and the COR-01 direct 500 scan/rethrow (`:149-178`) + its pinning test stay
BYTE-UNCHANGED.

Purpose: this is the engine-integration half of WALK-01. It turns the pure walk module (Plan 13-03)
into an observable `runTypecheck` behavior a single target pointed at `tsconfig.json` can rely on.

Output: extended `run-typecheck.ts`, `index.ts` re-export, executor `logger.warn` block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-engine-solution-tsconfig-reference-walking/13-RESEARCH.md
@.planning/phases/13-engine-solution-tsconfig-reference-walking/13-PATTERNS.md

<interfaces>
<!-- From Plan 13-03 walk-references.ts:
       export async function walkReferences(ng, ts, solutionParsed, solutionTsConfigPath): Promise<WalkResult>
       WalkResult { rawDiagnostics, rootNamesCount, skippedReferences }
       SkippedReference { referencePath, reason: 'out-of-project'|'zero-root-names'|'self-reference'|'not-found' } -->
<!-- run-typecheck.ts anchors (RESEARCH verified CURRENT):
       :33-71  CoreResult interface (existing fields incl. templateCheckAborted?)
       :80-87  TemplateCheckAborted interface
       :89-93  ZERO_ROOT_NAMES_DIAGNOSTIC_CODE = 90001
       :149-178 COR-01 direct 500 scan/rethrow  -- BYTE-UNCHANGED
       :185-203 D-03a guard block; :190 the `if (parsed.rootNames.length === 0)` split point
       :212-239 direct-path emit-neutralizing performCompilation override (UNCHANGED)
       :287-298 the direct path's finalize filter args block (basePath/includeDeps/useCaseSensitiveFileNames/realpath)
       :288-291 resolveFilterBasePath(parsed.options.basePath, options.tsConfigPath)
       :292     includeDeps: options.includeDeps ?? false
       :329-356 synthesizeZeroRootNamesDiagnostic (references-present branch predicate at :333-335)
       :394-456 finalize (filter -> ts.sortAndDeduplicateDiagnostics -> explicit category counts)
       :444     const templateCheckAborted = detectTemplateCheckAborted(diagnostics)  (pre-filter arg = the union)
       :454     ...(templateCheckAborted !== undefined ? { templateCheckAborted } : {})  -- the spread idiom to mirror -->
<!-- executor.ts:2  logger imported from @nx/devkit; :49-63 the templateCheckAborted logger.warn seam to mirror -->
<!-- index.ts:15  export type { CoreOptions, CoreResult } from './core/run-typecheck' -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-03a three-way split in run-typecheck.ts + skippedReferences on CoreResult</name>
  <read_first>
    - packages/angular-typechecker/src/core/run-typecheck.ts (the file being modified: D-03a guard :185-203 and split point :190; the direct-path finalize filter args :287-298; resolveFilterBasePath :288-291; synthesizeZeroRootNamesDiagnostic :329-356 and its references-present predicate :333-335; finalize :394-456; the templateCheckAborted spread idiom :454; CoreResult :33-71; TemplateCheckAborted :80-87; COR-01 500 scan :149-178 which must NOT change)
    - packages/angular-typechecker/src/core/walk-references.ts (walkReferences signature + WalkResult + SkippedReference from Plan 13-03)
    - .planning/phases/13-engine-solution-tsconfig-reference-walking/13-RESEARCH.md ("Directive 3" the runTypecheck invocation point / three-way split; "Directive 5" includeDeps/pathBase once; "Directive 6" the union feeds the pre-filter Fatal scan; Open Question 2 recommendation: attach skippedReferences after finalize returns in the walk branch, do NOT add a finalize param; Pitfall 4 COR-01 containment)
    - .planning/phases/13-engine-solution-tsconfig-reference-walking/13-PATTERNS.md ("CHANGED src/core/run-typecheck.ts" items 1-4 + COR-01 containment)
  </read_first>
  <behavior>
    - runTypecheck({ tsConfigPath: solution-style/tsconfig.json }) (references + in-project leaves) resolves with rootNamesCount > 0, errorCount === 2, two distinct-file TS2322, skippedReferences undefined.
    - runTypecheck({ tsConfigPath: solution-style-oop/tsconfig.json }) (references, all out-of-project) resolves with errorCount === 1, a code-90001 (references-present none-in-project message), skippedReferences a non-empty array with reason:'out-of-project'.
    - runTypecheck({ tsConfigPath: solution-style-empty/tsconfig.json }) (no references) resolves with errorCount === 1, code 90001 (empty-project message), skippedReferences undefined.
    - runTypecheck({ tsConfigPath: solution-style-broken-ref/tsconfig.json }) RESOLVES (does NOT throw TypecheckInfrastructureError); codes contain 90002 exactly once AND the survivor leaf's TS2322; skippedReferences has reason:'not-found'.
    - A solution whose ONE leaf carries a TCB Fatal sets result.templateCheckAborted (union pre-filter scan).
    - The existing direct-leaf specs (rootNames > 0) and the COR-01 direct 500 pinning test stay green unchanged.
  </behavior>
  <action>
    In packages/angular-typechecker/src/core/run-typecheck.ts:

    (1) Add to the CoreResult interface (:33-71) an optional field `skippedReferences?: readonly SkippedReference[];` with a doc-comment mirroring the templateCheckAborted? field style (present-and-non-empty only during a walk; undefined on the direct path and on a clean all-walked walk; pure detection, adapter renders; additive/non-breaking 0.x). Import (or re-declare-by-import) SkippedReference from './walk-references' at the top of the file, and import walkReferences from './walk-references'.

    (2) Replace the single-branch guard at :190 (`if (parsed.rootNames.length === 0)`) with the three-way split. Use the SAME references-present predicate the synthesizer uses at :333-335 (`parsed.projectReferences !== undefined && parsed.projectReferences.length > 0`):
      - rootNames > 0 -> the existing compile-direct path (:212-299) UNCHANGED.
      - rootNames === 0 && references present -> `const walk = await walkReferences(ng, ts, parsed, options.tsConfigPath);`
          if walk.rootNamesCount > 0 -> call the EXISTING finalize over [...configDiagnostics, ...walk.rawDiagnostics] with the SAME filter args block as the direct path (:287-298): basePath = resolveFilterBasePath(parsed.options.basePath, options.tsConfigPath) (SOLUTION dir), includeDeps: options.includeDeps ?? false, useCaseSensitiveFileNames from ONE walked leaf's program (the first walked leaf; obtain it by having walkReferences expose it OR by re-deriving from ts.sys -- prefer the direct path's existing realpath/useCaseSensitiveFileNames derivation to avoid a new dependency; if the first-leaf program is not available, reuse `ts.sys.useCaseSensitiveFileNames` and `ts.sys.realpath` as the host, matching the direct path's `(filePath) => ts.sys.realpath?.(filePath) ?? filePath`). Pass rootNamesCount = walk.rootNamesCount. After finalize returns, thread skippedReferences onto the result: attach `skippedReferences` ONLY when walk.skippedReferences.length > 0 (map [] -> undefined), using the same conditional-spread idiom as templateCheckAborted at :454.
          else (walk.rootNamesCount === 0, all references skipped) -> synthesizeZeroRootNamesDiagnostic with the references-present ("none-in-project") message branch (code 90001), and attach walk.skippedReferences (non-empty here) onto that result via the same conditional spread.
      - rootNames === 0 && no references -> synthesizeZeroRootNamesDiagnostic with the empty-project message branch (code 90001), UNCHANGED (no skippedReferences).

    (3) Confirm the walk branch's finalize receives the RAW union as its pre-filter `diagnostics` arg so detectTemplateCheckAborted (:444, :474-489) scans the union (Directive 6) -- no change to the detection function.

    (4) DO NOT touch the COR-01 direct 500 scan/rethrow (:149-178) or the direct-path override block (:212-239). Fold-and-count lives ONLY inside walkReferences (Pitfall 4). Keep includeDeps applied ONCE at the single finalize (Directive 5); pathBase remains untouched (engine ignores it).

    Follow blank-line-around-control-flow + braces-on-all-bodies; ASCII-only; count by DiagnosticCategory (never length - errorCount).
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- run-typecheck</automated>
  </verify>
  <acceptance_criteria>
    - run-typecheck.ts contains `await walkReferences(` invoked at the D-03a split, and imports walkReferences + SkippedReference from './walk-references'.
    - CoreResult declares `skippedReferences?: readonly SkippedReference[]`.
    - The COR-01 direct 500 scan block (originally :149-178) is byte-unchanged: `git diff` on run-typecheck.ts shows NO change within that scan/rethrow region (the direct nonexistent-config path still throws TypecheckInfrastructureError).
    - The references-present none-in-project branch and the no-references empty-project branch BOTH synthesize code 90001 (two distinct messages); the walk branch feeds walk.rawDiagnostics into the existing finalize (no second dedupe layer -- `git grep -c "sortAndDeduplicateDiagnostics" -- packages/angular-typechecker/src/core/run-typecheck.ts` is unchanged from before, i.e. exactly one call).
    - No `console.`/`process.` added under packages/angular-typechecker/src/core/** (`git grep -c "console\." -- packages/angular-typechecker/src/core/run-typecheck.ts` returns 0).
    - `npx nx test angular-typechecker` passes (the direct-leaf and COR-01 pinning tests remain green; the walk-branch behavior is exercised by the specs in Plan 13-05, but existing coverage must not regress here).
  </acceptance_criteria>
  <done>
    run-typecheck.ts has the three-way split invoking walkReferences, feeds the raw union into the single existing finalize with solution-dir basePath and once-applied includeDeps, threads skippedReferences (non-empty only) onto CoreResult, and leaves the COR-01 direct path + direct-leaf path byte-unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-export SkippedReference from index.ts + render logger.warn in the executor adapter</name>
  <read_first>
    - packages/angular-typechecker/src/index.ts (the existing `export type { CoreOptions, CoreResult } from './core/run-typecheck';` at :15 -- the export-style analog)
    - packages/angular-typechecker/src/executors/angular-typecheck/executor.ts (logger imported from @nx/devkit at :2; the templateCheckAborted render seam at :49-63 -- copy its structure exactly)
    - packages/angular-typechecker/src/core/walk-references.ts (SkippedReference shape from Plan 13-03; the location the type is exported from)
    - .planning/phases/13-engine-solution-tsconfig-reference-walking/13-RESEARCH.md ("Directive 2" adapter emission gate: presence AND non-empty, iterate array, logger.warn per reference, advisory-only; index.ts export line)
    - .planning/phases/13-engine-solution-tsconfig-reference-walking/13-PATTERNS.md ("CHANGED src/index.ts" and "CHANGED src/executors/.../executor.ts")
  </read_first>
  <action>
    (1) packages/angular-typechecker/src/index.ts: add `SkippedReference` to the existing type re-export. If SkippedReference lives in walk-references.ts, add `export type { SkippedReference } from './core/walk-references';` alongside the existing CoreResult export (:15); if run-typecheck re-exports it, keep the single line. Additive, non-breaking.
    (2) packages/angular-typechecker/src/executors/angular-typecheck/executor.ts: directly AFTER the existing `if (result.templateCheckAborted !== undefined) { ... }` block (:52-63), ADD a new block gated on `result.skippedReferences !== undefined && result.skippedReferences.length > 0`. Inside, iterate `for (const skipped of result.skippedReferences) { logger.warn(...); }` emitting an ASCII-only advisory message naming skipped.referencePath and skipped.reason and stating the verdict is unchanged (advisory only, L-4). Reuse the already-imported `logger` from @nx/devkit (executor.ts:2) -- NO new import. This is the ONLY place logging happens for skippedReferences (core sets the field purely).
    Blank lines around control flow; braces on all bodies; ASCII-only.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -c "SkippedReference" -- packages/angular-typechecker/src/index.ts` returns >= 1 (the type is publicly re-exported).
    - executor.ts contains a block gated on `result.skippedReferences !== undefined && result.skippedReferences.length > 0` that calls `logger.warn` per entry.
    - executor.ts adds NO new import (logger already imported at :2); the skippedReferences block appears AFTER the templateCheckAborted block.
    - The logger.warn message is advisory (states the verdict is unchanged) and is ASCII-only.
    - `npx nx test angular-typechecker` passes (existing executor unit coverage stays green; the new-block behavior is asserted by the executor unit spec in Plan 13-05).
  </acceptance_criteria>
  <done>
    SkippedReference is publicly re-exported; the executor adapter renders a per-reference advisory logger.warn gated on a non-empty skippedReferences array, with no verdict change and no new import.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runTypecheck D-03a split -> walk vs synth | The zero-rootNames outcome is classified three ways; only references-present-and-in-project reaches the walk |
| core CoreResult -> executor adapter | The pure skippedReferences field crosses the core/adapter seam; only the adapter logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | out-of-project reference traversal (surfaced at the executor seam) | mitigate | The walk skips the out-of-project leaf (Plan 13-03); this plan surfaces it loudly via logger.warn (reason:'out-of-project') without changing the verdict. Verified: solution-style-oop integration + executor unit spec in Plan 13-05. |
| T-13-02 | Tampering (false PASS) | broken-reference false PASS (runTypecheck does not rethrow the per-leaf 500) | mitigate | The walk branch RESOLVES with a counted 90002 in the union (fold-and-count) rather than a stale PASS; the COR-01 direct 500 rethrow stays byte-unchanged so direct-path infra failures still throw. Verified: solution-style-broken-ref RESOLVES-with-90002 test + COR-01 pinning test unchanged in Plan 13-05. |
| T-13-02c | Tampering (false PASS) | references-present but 0 in-project -> silent PASS | mitigate | The three-way split synthesizes a counted 90001 (none-in-project) rather than an empty zero-diagnostic PASS, and records skippedReferences. Verified: solution-style-oop 90001 assertion in Plan 13-05. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs (reuses shipped peers; RESEARCH: audit N/A). |
</threat_model>

<verification>
- `npx nx test angular-typechecker` green (direct-leaf + COR-01 pinning tests unchanged; no regression).
- `git diff` confirms the COR-01 500 scan/rethrow region and the direct-path override block are byte-unchanged.
- Core-purity grep: no `console.`/`process.` added to run-typecheck.ts; logging only in executor.ts.
</verification>

<success_criteria>
runTypecheck implements the three-way D-03a split, walks references into the single existing finalize
with solution-dir basePath and once-applied includeDeps, threads a non-empty-only skippedReferences
onto CoreResult, re-exports SkippedReference, and the executor renders an advisory per-reference
logger.warn -- all while the direct-leaf path and COR-01 direct 500 handling stay byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/13-engine-solution-tsconfig-reference-walking/13-04-SUMMARY.md` when done.
</output>
