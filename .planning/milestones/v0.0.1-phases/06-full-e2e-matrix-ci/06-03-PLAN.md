---
phase: 06-full-e2e-matrix-ci
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/core/filter-diagnostics.spec.ts
  - packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts
autonomous: true
requirements: [OUT-02]
user_setup: []

must_haves:
  truths:
    - "The filter-diagnostics unit suite (runs on every CI cell) asserts mixed-case fold under useCaseSensitiveFileNames:false for in-project, out-of-project, AND node_modules-segment paths, and proves the fold is GATED on the flag (the same mixed-case inputs are NOT folded under useCaseSensitiveFileNames:true)"
    - "The unit suite proves the node_modules-segment exclusion generalizes across node_modules/.pnpm/..., node_modules/.bun/..., and plain node_modules/<pkg>/... (synthetic realpaths, NO install) -- the segment match is not hardcoded to .pnpm (RD-04 store-dir generality)"
    - "An integration assertion (runs the real compiler on every CI cell) proves the host derives useCaseSensitiveFileNames from the real ts.sys/program host -- live case-insensitive on the macOS/Windows CI legs, case-sensitive on Linux"
  artifacts:
    - path: "packages/angular-typechecker/src/core/filter-diagnostics.spec.ts"
      provides: "extended D-10 mixed-case + RD-04 store-dir-generality unit coverage"
      contains: "useCaseSensitiveFileNames"
    - path: "packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts"
      provides: "host-derived useCaseSensitiveFileNames integration assertion (D-10)"
      contains: "useCaseSensitiveFileNames"
  key_links:
    - from: "packages/angular-typechecker/src/core/filter-diagnostics.spec.ts"
      to: "filterDiagnostics canonicalizer (realpath-first, case-fold gated on the flag, node_modules-segment)"
      via: "diag() literals + injected realpath + both case modes"
      pattern: "useCaseSensitiveFileNames"
    - from: "packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts"
      to: "the real program host"
      via: "result.program.getTsProgram().useCaseSensitiveFileNames() + ts.sys.realpath"
      pattern: "getTsProgram"
---

<objective>
Extend the cross-OS unit + integration tier so the OUT-02 case-insensitive-FS fold and the
realpath/segment logic are exercised on the macOS + Windows CI legs (where the bug actually bites)
rather than the Linux-only e2e gate (which is dead code for a case-insensitive assertion). Two edits:
(1) more mixed-case cases in `filter-diagnostics.spec.ts` under both case modes PLUS the RD-04
store-dir-generality case (`.pnpm`/`.bun`/plain `node_modules`), and (2) one host-derived
`useCaseSensitiveFileNames` assertion in the real-compiler integration spec.

Purpose: these are the OUT-02 cross-platform regression BACKSTOP. The fold only manifests on
macOS/Windows; these specs run on ALL CI matrix cells (the lean 6-cell matrix from 06-05), so the
macOS + Windows legs become the live case-insensitive samples. On the Windows arm64 dev box this tier
is ALSO a live case-insensitive sample, so it verifies locally too.

Output: extended `filter-diagnostics.spec.ts` + extended `run-typecheck.integration.spec.ts`. No
production code changes (the seams already exist).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-full-e2e-matrix-ci/06-PATTERNS.md
@.planning/phases/06-full-e2e-matrix-ci/06-RESEARCH.md

<interfaces>
<!-- The production seams these specs exercise (already wired; NO production change). Verified this
     session from packages/angular-typechecker/src/core/{filter-diagnostics.ts,run-typecheck.ts}. -->

filterDiagnostics(diagnostics, options): FilterResult
  options.basePath: string                       // leaf tsconfig basePath
  options.includeDeps: boolean                    // true => keep all, suppressedCount 0
  options.useCaseSensitiveFileNames: boolean      // false => lower-case fold AFTER realpath
  options.realpath: (filePath: string) => string  // tests inject identity / a mapping
  -> { kept: ts.Diagnostic[], suppressedCount: number }
  // canonicalizer: realpath FIRST, then \\ -> /, then toLowerCase() ONLY when !useCaseSensitiveFileNames
  // node_modules excluded by path SEGMENT: canonicalFile.split('/').includes('node_modules')
  // containment is segment-bounded: equal OR startsWith(dir + '/')

run-typecheck.ts host seam (already wired -- the integration spec asserts it is HOST-derived):
  useCaseSensitiveFileNames: result.program.getTsProgram().useCaseSensitiveFileNames()
  realpath: (filePath) => ts.sys.realpath?.(filePath) ?? filePath
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-10 mixed-case + RD-04 store-dir generality unit cases (filter-diagnostics.spec.ts)</name>
  <files>packages/angular-typechecker/src/core/filter-diagnostics.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/filter-diagnostics.spec.ts (the EXISTING idiom to extend: the `diag(fileName, code)` literal builder, the `base` object with `useCaseSensitiveFileNames: true` + injectable `realpath`, the seed realpath-before-case-fold case, and the seed `useCaseSensitiveFileNames:false` mixed-case in-project case)
    - packages/angular-typechecker/src/core/filter-diagnostics.ts (the canonicalizer: realpath FIRST, then `\\`->`/`, then case-fold gated on the flag; `isNodeModulesPath` SEGMENT test; `isUnderDir` segment-bounded containment) -- so each new case targets a real code path
    - 06-PATTERNS.md section "EXTEND filter-diagnostics.spec.ts" (the D-10 case list + style rules) and 06-RESEARCH.md D-10 + RD-04 (store-dir generality across .pnpm/.bun/plain node_modules)
  </read_first>
  <action>
    Add unit cases to the existing `describe('filterDiagnostics', ...)` block, matching the file's
    exact idiom (`diag(...)` literals, the `base` object spread, blank lines around the `expect` after
    `filterDiagnostics`, braces always, single quotes, `it('... (OUT-02)', ...)` naming):

    D-10 mixed-case parity set (the seed file already has the in-project `:false` case -- add the
    siblings that prove the fold is GATED on the flag):
    - mixed-case OUT-of-project under `useCaseSensitiveFileNames:false` is SUPPRESSED (e.g.
      `/WS/SIBLING/src/x.ts` against basePath `/ws/proj`).
    - mixed-case `node_modules` SEGMENT under `:false` is SUPPRESSED (e.g.
      `/WS/PROJ/NODE_MODULES/x/y.d.ts` -- folds to a `node_modules` segment).
    - the SAME mixed-case in-project input under `useCaseSensitiveFileNames:true` is NOT folded ->
      SUPPRESSED (proves the fold is gated on the flag, not unconditional).

    RD-04 store-dir generality set (synthetic realpaths, NO install -- the segment match must not be
    hardcoded to `.pnpm`): assert that an in-project diagnostic whose realpath crosses through a
    `node_modules/.pnpm/<pkg>@<ver>/node_modules/<pkg>/...` segment is SUPPRESSED, and the same for
    `node_modules/.bun/...` and a plain `node_modules/<pkg>/...` -- all three excluded by the single
    `node_modules`-SEGMENT test. Use the injected `realpath` to map a friendly input path to the
    store realpath so the case exercises the realpath-FIRST-then-segment path exactly as production
    does. Keep each as its own `it('... (RD-04)' / '... (OUT-02)', ...)`.

    Do NOT modify production code; do NOT touch the existing seed cases beyond adding new `it(...)`
    blocks alongside them.
  </action>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && npx nx run angular-typechecker:test 2>&1 | rg -q "filter-diagnostics" && node -e "const s=require('fs').readFileSync('packages/angular-typechecker/src/core/filter-diagnostics.spec.ts','utf8'); const ok=/RD-04/.test(s)&&/\.pnpm/.test(s)&&/\.bun/.test(s)&&/useCaseSensitiveFileNames:\s*true/.test(s)&&/useCaseSensitiveFileNames:\s*false/.test(s); process.exit(ok?0:1)" && echo FILTER_SPEC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `filter-diagnostics.spec.ts` adds: mixed-case OUT-of-project suppressed under `:false`, mixed-case `node_modules`-segment suppressed under `:false`, and the SAME mixed-case in-project input NOT folded (suppressed) under `:true`
    - adds RD-04 store-dir-generality cases proving the `node_modules`-segment exclusion fires for `.pnpm`, `.bun`, AND plain `node_modules/<pkg>` realpaths (synthetic, no install)
    - the file contains the literal tokens `RD-04`, `.pnpm`, `.bun`, and both `useCaseSensitiveFileNames: true` and `useCaseSensitiveFileNames: false`
    - `npx nx run angular-typechecker:test` exits 0 on the dev box (a live case-insensitive sample) with the extended spec green; no production code changed
  </acceptance_criteria>
  <done>The extended `filter-diagnostics.spec.ts` is green; the structural verify passes.</done>
</task>

<task type="auto">
  <name>Task 2: host-derived useCaseSensitiveFileNames integration assertion (run-typecheck.integration.spec.ts)</name>
  <files>packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts (the EXISTING idiom: `runTypecheck` against committed fixtures `gate-b-error` + `sibling-import`, the `diagnosticsOnFile` forward-slash comparator, the `describe.each([['app tsconfig', appTsConfig], ['local-library tsconfig', libTsConfig]])` parameterization)
    - packages/angular-typechecker/src/core/run-typecheck.ts (lines ~181-205 -- the host seam: `useCaseSensitiveFileNames: result.program.getTsProgram().useCaseSensitiveFileNames()` + `realpath: ts.sys.realpath`)
    - 06-PATTERNS.md section "EXTEND run-typecheck.integration.spec.ts" (the D-10 host-derived case shape) + 06-RESEARCH.md "D-10 host-derived useCaseSensitiveFileNames (integration tier)"
  </read_first>
  <action>
    Add ONE integration assertion proving the host DERIVES case-sensitivity from the real program
    host (not a hard-coded constant). Reuse the existing `gate-b-error` (or `sibling-import`) fixture
    + the `diagnosticsOnFile` normalized comparator. The assertion must be true on EVERY OS leg:
    classify an in-project diagnostic correctly when the queried path differs only in CASE from the
    on-disk path. On the macOS/Windows legs the host yields `useCaseSensitiveFileNames === false`, so
    a mixed-case in-project `fileName` still classifies in-project (KEPT); on Linux the host yields
    `true` and the on-disk-exact path is KEPT. Phrase the assertion so it holds on all three OS:
    assert that the in-project diagnostic (from the committed clean-path fixture) is KEPT and reported
    on its real path on every leg -- and, to make the host-derivation explicit, run the same fixture
    under `includeDeps:true` vs default and assert the default suppresses an out-of-project sibling
    while keeping the in-project one (the classification is host-derived, not a literal). If a
    direct case-mismatch query is cleanly expressible against the existing fixture, prefer it; do NOT
    add a new committed fixture for this -- reuse what is there. Match TS style.

    Do NOT modify production code (the seam is already wired). The goal is an assertion that BECOMES a
    live case-insensitive exercise on the mac/win matrix legs, not a Linux-only constant check.
  </action>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && npx nx run angular-typechecker:test 2>&1 | rg -q "run-typecheck.integration" && node -e "const s=require('fs').readFileSync('packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts','utf8'); const ok=/useCaseSensitiveFileNames|getTsProgram|case-insensit/i.test(s); process.exit(ok?0:1)" && echo RT_INT_SPEC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `run-typecheck.integration.spec.ts` gains one new assertion that runs the real compiler and proves in-project classification is HOST-derived (live case-insensitive on mac/win, case-sensitive on Linux) -- holds on all three OS legs
    - the new assertion reuses an existing committed fixture (no new fixture committed) and the `diagnosticsOnFile` comparator
    - the file references the host-derived case-sensitivity intent (token `useCaseSensitiveFileNames` / `getTsProgram` / "case-insensit*")
    - `npx nx run angular-typechecker:test` exits 0 on the dev box; no production code changed
  </acceptance_criteria>
  <done>The integration spec asserts host-derived case-sensitivity and is green; the structural verify passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure-function unit cases over hand-built `ts.Diagnostic` literals + real-compiler integration over committed local fixtures. No secrets, no network, no install, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | a mixed-case assertion placed where it is dead code (Linux-only e2e) | mitigate | the assertion lives in the cross-OS unit + integration tier that runs on every matrix cell, so the macOS/Windows legs are the live case-insensitive samples (D-10) |

> Test-only changes; no production code modified.
</threat_model>

<verification>
- `npx nx run angular-typechecker:test` exits 0 on the dev box (a live case-insensitive sample) with both extended specs green.
- The macOS + Windows matrix legs (06-05 ci.yml) are the authoritative case-insensitive samples; the draft-PR run confirms green on all legs.
</verification>

<success_criteria>
- OUT-02 backstop: the case-fold is exercised under both case modes (in/out-of-project + node_modules-segment) and proven gated on the flag; the node_modules-segment exclusion generalizes across `.pnpm`/`.bun`/plain `node_modules` (RD-04).
- The host-derived `useCaseSensitiveFileNames` is asserted via the real program host, so the mac/win matrix legs are live case-insensitive exercises.
- No production code change (the seams already exist).
</success_criteria>

<output>
Create `.planning/phases/06-full-e2e-matrix-ci/06-03-SUMMARY.md` when done.
</output>
