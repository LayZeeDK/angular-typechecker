---
phase: 29-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/README.md
  - CHANGELOG.md
  - packages/angular-typechecker/src/standalone-cli-docs.spec.ts
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "A reader finds a `## Standalone CLI` section in the README with install steps, the full flag set, and the exit-code contract"
    - "A reader is steered to `npx angular-typechecker` and is NEVER told to run `npx atc`"
    - "The documented flag set matches the CLI's actual `--help` output (drift-locked)"
    - "The exit-code table states `0` clean / `1` verdict-fail / `2` infrastructure-or-usage without contradicting the existing `## Exit codes` section"
    - "The repo-root CHANGELOG carries a `## 0.2.2` entry in end-user language with no internal ids/scopes"
    - "The doc-tripwire spec is green under `nx test angular-typechecker`"
    - "Locked CONTEXT.md decisions are honored: D-01, D-02 (section + ToC placement), D-03, D-04 (installation forms), D-05 (never `npx atc`; `atc@0.0.6` supply-chain warning), D-06 (mirror the 7-flag HELP_TEXT), D-07, D-08 (own `0`/`1`/`2` table, reconciled with `## Exit codes`), D-09, D-10 (curated undated `## 0.2.2` entry) -- each mapped to a task action below"
  artifacts:
    - path: "packages/angular-typechecker/README.md"
      provides: "`## Standalone CLI` section + `[Standalone CLI](#standalone-cli)` ToC anchor"
      contains: "## Standalone CLI"
    - path: "CHANGELOG.md"
      provides: "Curated public `## 0.2.2` entry"
      contains: "## 0.2.2"
    - path: "packages/angular-typechecker/src/standalone-cli-docs.spec.ts"
      provides: "Doc-drift tripwire for the new section (README + CHANGELOG hygiene)"
      contains: "not.toContain('npx atc')"
  key_links:
    - from: "packages/angular-typechecker/README.md"
      to: "packages/angular-typechecker/src/cli/parse-args.ts"
      via: "parseCliArgs(['--help']).text flag tokens drift-locked against README prose"
      pattern: "parseCliArgs"
    - from: "packages/angular-typechecker/src/standalone-cli-docs.spec.ts"
      to: "packages/angular-typechecker/README.md"
      via: "fs read of ../README.md"
      pattern: "\\.\\./README\\.md"
    - from: "packages/angular-typechecker/src/standalone-cli-docs.spec.ts"
      to: "CHANGELOG.md"
      via: "fs read of ../../../CHANGELOG.md"
      pattern: "\\.\\./\\.\\./\\.\\./CHANGELOG\\.md"
---

<objective>
Document the standalone CLI that shipped in Phases 25-28. Two doc deliverables +
one drift-lock test, no code/behavior changes:

1. A new README `## Standalone CLI` section (install, the full flag set, the
   `0`/`1`/`2` exit-code contract table) plus its `## Contents` ToC anchor.
2. A curated public `## 0.2.2` CHANGELOG entry in end-user language.
3. A new `standalone-cli-docs.spec.ts` doc tripwire that locks the load-bearing
   claims (supply-chain guard + HELP_TEXT/README drift-lock).

Purpose: close DOC-01 -- steer users to `npx angular-typechecker` (never
`npx atc`, which fetches the unrelated `atc@0.0.6`) and give them an accurate
flag + exit-code reference that can never silently drift from the shipped
`--help`.
Output: an edited README.md, an edited repo-root CHANGELOG.md, and a new spec,
with `nx test angular-typechecker` + format/lint/typecheck green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-docs/29-CONTEXT.md
@.planning/phases/29-docs/29-RESEARCH.md
@.planning/phases/29-docs/29-PATTERNS.md

# Source-of-truth for CLI facts (read exactly; do NOT re-invent wording):
@packages/angular-typechecker/src/cli/parse-args.ts
@packages/angular-typechecker/README.md
@CHANGELOG.md
</context>

<constraints>
- This is a docs-only phase. Do NOT change any CLI flag, exit code, or packaging
  behavior -- all locked in Phases 25-28. Do NOT cut a release (that is a
  separate Release PR per AGENTS.md); the `## 0.2.2` entry is written now, the
  version is bumped/tagged later. Leave the entry UNDATED (matches `## 0.2.1`).
- ASCII only (project rule): `--` not em-dash, `->` not arrows, straight quotes.
- Fence conventions: ```sh for shell, ```json / ```jsonc for config, bare ```
  for captured tool output. Match the existing README exactly.
- No `--watch` docs (deferred, no engine). No new top-of-README "three ways to
  run it" comparison (deferred; only add if it falls out naturally -- it does
  not here).
- CHANGELOG hygiene (hard rule, AGENTS.md + memory changelog-readme-end-user-facing):
  end-user language ONLY. NO internal ids/scopes -- no `DOC-01`, `CLI-0x`,
  `SC#2`, `Phase 29`, plan numbers, or board/layout jargon.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Add the README `## Standalone CLI` section + ToC anchor</name>
  <files>packages/angular-typechecker/README.md</files>
  <read_first>
    - packages/angular-typechecker/README.md (the file being edited: ToC at line 28-45, `## Installation` 77-98, `## Exit codes` 262-277, `## Angular CLI` 381-471 = the closest structural analog, `## Storybook` at 502)
    - packages/angular-typechecker/src/cli/parse-args.ts (HELP_TEXT lines 64-82 = the authoritative flag names + descriptions the section MUST mirror)
    - packages/angular-typechecker/src/cli/main.ts (lines 111-186 = the two-step exit-code compose that the `0`/`1`/`2` table describes)
    - packages/angular-typechecker/src/core/exit-codes.ts (toExitCode = 2 for infrastructure errors)
    - packages/angular-typechecker/package.json (the `bin` map: both `angular-typechecker` + `atc` resolve to one `./src/cli/bin.js`)
  </read_first>
  <action>
    Insert a `- [Standalone CLI](#standalone-cli)` line into the `## Contents` ToC
    between the existing `- [Angular CLI](#angular-cli)` and `- [Storybook](#storybook)`
    lines (per D-02). Then add a new `## Standalone CLI` section positioned
    immediately AFTER the `## Angular CLI` section (which ends at line 471) and
    BEFORE `## Storybook` (per D-01), mirroring the `## Angular CLI` section's
    shape (intro paragraph + `###` subsections + cross-links).

    Section content:
    - Intro paragraph: the standalone CLI is the third thin adapter over the same
      `runTypecheck` core (Nx executor -> Angular CLI builder -> standalone CLI);
      it runs the complete Angular type-check in ANY repo with no Nx and no
      Angular CLI present.
    - Install + run: LEAD with the zero-install canonical invocation
      `npx angular-typechecker -c <tsconfig>` (D-03; matches the HELP_TEXT synopsis
      at parse-args.ts:64). Then show the installed forms using the SAME ```sh
      fences and the `-Dw` idiom already in `## Installation`:
      `npm install --save-dev angular-typechecker` and `pnpm add -Dw angular-typechecker`.
      Document `atc` ONLY as a post-install PATH shorthand (after a local install,
      `atc -c <tsconfig>` resolves to the installed bin), with an explicit inline
      warning that `npx atc` would fetch the UNRELATED published package `atc@0.0.6`
      (a 2013 "Manage fleet spawns" package -- a supply-chain hazard), so the
      uninstalled path is always `npx angular-typechecker` (D-04/D-05, LOAD-BEARING).
      NEVER write the string `npx atc` anywhere.
    - Flag reference: mirror HELP_TEXT (parse-args.ts:64-82) so `--help` and the
      README stay one source (D-06). Document all 7 flags with descriptions matching
      HELP_TEXT: `-c, --tsConfig <path>` (repeatable; required; single = solution
      reference-walk, two+ = union), `--max-warnings <n>` (non-negative integer; 0
      fails on any warning), `--fail-fast`, `--include-deps`, `--strict`, `-h, --help`,
      `--version`. Table-vs-definition-list is your discretion, but the wording must
      match HELP_TEXT. Note that `-p`/`--project` is deliberately NOT a flag (it
      would collide with Nx/ng project selection; it surfaces as an unknown-flag
      usage error -> exit `2`).
    - Exit-code table (D-07): a three-row table -- `0` clean / `1` verdict-fail /
      `2` infrastructure-or-usage -- using the recommended table in 29-RESEARCH.md
      ("Recommended docs exit-code table"). `1` = type/template/NG8xxx errors OR
      warning count exceeded `--max-warnings` OR coverage-incomplete; `2` = compiler
      failed to run (missing/unreadable tsconfig, config-resolution failure) OR a
      usage error (unknown flag, missing required `--tsConfig`, non-integer
      `--max-warnings`).
    - Reconcile with the existing `## Exit codes` section (D-08): state that the
      standalone CLI is the FIRST adapter that owns the literal OS `2`, whereas the
      Nx executor and Angular CLI builder surface a `{success}` boolean and let the
      host collapse `1` and `2` into non-zero. Do NOT restate or contradict the
      pass/fail-only text; cross-link back with `[Exit codes](#exit-codes)` as the
      Nx/ng `{success}` view (and `[Output](#output)` for the report format).
    - Optional (your discretion): one short worked example -- an
      `npx angular-typechecker -c tsconfig.json` invocation with a planted error
      returning exit `1` -- consistent with the README's existing tone.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('packages/angular-typechecker/README.md','utf8'); const n=s.replace(/\s+/g,' '); if(!s.includes('## Standalone CLI')) throw new Error('missing heading'); if(!s.includes('[Standalone CLI](#standalone-cli)')) throw new Error('missing ToC anchor'); if(!n.includes('npx angular-typechecker')) throw new Error('missing canonical invocation'); if(n.includes('npx atc')) throw new Error('LEAK: npx atc present'); for(const f of ['-c, --tsConfig','--max-warnings','--fail-fast','--include-deps','--strict','-h, --help','--version']){ if(!n.includes(f)) throw new Error('missing flag '+f);} if(!n.includes('infrastructure-or-usage')) throw new Error('missing exit-code meaning'); console.log('README section OK');"</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains the exact heading `## Standalone CLI`, placed after `## Angular CLI` and before `## Storybook`.
    - The `## Contents` ToC contains `[Standalone CLI](#standalone-cli)` between the `Angular CLI` and `Storybook` entries.
    - The section contains `npx angular-typechecker` and does NOT contain the string `npx atc` anywhere in the file.
    - `atc` is shown only as a post-install PATH alias, accompanied by an explicit warning naming `atc@0.0.6`.
    - All 7 HELP_TEXT flag tokens (`-c, --tsConfig`, `--max-warnings`, `--fail-fast`, `--include-deps`, `--strict`, `-h, --help`, `--version`) appear with descriptions matching HELP_TEXT.
    - An exit-code table states `0` clean / `1` verdict-fail / `2` infrastructure-or-usage, and the prose explicitly frames the CLI as the first adapter owning literal `2` while Nx/ng collapse `1`+`2` into non-zero (no contradiction of the existing `## Exit codes` section).
  </acceptance_criteria>
  <done>The README carries an accurate, drift-safe `## Standalone CLI` section and ToC anchor; the inline verify passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add the curated `## 0.2.2` CHANGELOG entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (the file being edited: preamble lines 1-3, the `## 0.2.1` entry at lines 5-45 = the shape to clone, `## 0.2.0` dated entry at line 47 for ordering)
    - AGENTS.md ("The auto-generated changelog renders the commit SCOPE" + the changelog-hygiene rule: no internal scopes/ids in a public CHANGELOG)
  </read_first>
  <action>
    Add a new `## 0.2.2` entry ABOVE `## 0.2.1` (immediately after the preamble at
    line 3), newest-first. Leave it UNDATED -- `## 0.2.1` is undated and the date is
    stamped at the Release-PR cut (per AGENTS.md; per D-09/Open-Q1). Clone the
    `## 0.2.1` shape exactly: a bold lead sentence + one framing paragraph, then
    `### Features`, `### Notes`, `### Compatibility`.

    Content (D-10, end-user language):
    - Lead + framing: a standalone `angular-typechecker` command you can run with
      `npx angular-typechecker` in ANY repo -- no Nx and no Angular CLI needed --
      that runs the same complete Angular type-check (TypeScript + template + NG8xxx,
      no emit). Frame as purely additive: nothing changes for existing Nx or Angular
      CLI users.
    - `### Features`: the standalone command and its two bin names
      (`angular-typechecker`, and `atc` as the short installed alias); the flag set
      described in end-user terms (`-c/--tsConfig` repeatable + required,
      `--max-warnings`, `--fail-fast`, `--include-deps`, `--strict`, `--help`,
      `--version`); and the `0`/`1`/`2` exit codes as the new capability -- the CLI
      is the first entry point to return a literal OS exit `2` for an
      infrastructure/usage error (Nx/ng only surfaced pass/fail).
    - `### Notes`: the canonical uninstalled invocation is `npx angular-typechecker`;
      note that `atc` is a post-install shorthand and that running `npx atc` would
      fetch the unrelated published package `atc@0.0.6`. Keep this consumer-facing.
    - `### Compatibility`: repeat the `## 0.2.1` line VERBATIM --
      `Nx 23, Angular 22 (\`@angular/compiler-cli\` \`^22.0.0\`), TypeScript \`>=6.0.0 <6.1.0\`, Node \`^22.22.3 || ^24.15.0 || ^26.0.0\`. No new runtime dependency.`
      (accurate: the CLI adds no npm dependency -- Node built-ins only, notably
      `node:util`).

    HARD hygiene rule: NO internal ids/scopes anywhere in the entry -- no `DOC-01`,
    `CLI-0x`, `SC#`, `Phase 29`, plan numbers, or board/layout jargon.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('CHANGELOG.md','utf8'); if(!s.includes('## 0.2.2')) throw new Error('missing 0.2.2 heading'); const start=s.indexOf('## 0.2.2'); const next=s.indexOf('## 0.2.1'); if(next<0||next<start) throw new Error('0.2.2 must sit above 0.2.1'); const entry=s.slice(start,next); if(!/### Compatibility/.test(entry)) throw new Error('missing Compatibility'); if(/DOC-01|CLI-0\d|SC#|\bphase\b/i.test(entry)) throw new Error('LEAK: internal id/scope in 0.2.2 entry'); console.log('CHANGELOG 0.2.2 OK');"</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md contains `## 0.2.2`, positioned above `## 0.2.1`, undated.
    - The entry has `### Features`, `### Notes`, and `### Compatibility` subsections cloned from the `## 0.2.1` shape, with a bold lead sentence.
    - The `### Compatibility` line repeats the 0.2.1 stack line verbatim and states "No new runtime dependency."
    - The entry contains NO internal ids/scopes: `not.toMatch(/DOC-01|CLI-0\d|SC#|\bphase\b/i)` holds.
  </acceptance_criteria>
  <done>A curated, hygienic, end-user-facing `## 0.2.2` entry exists; the inline verify passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the `standalone-cli-docs.spec.ts` doc tripwire (drift-lock + hygiene)</name>
  <files>packages/angular-typechecker/src/standalone-cli-docs.spec.ts</files>
  <behavior>
    - `## Standalone CLI` heading present in README.
    - ToC contains `[Standalone CLI](#standalone-cli)`.
    - README contains `npx angular-typechecker` AND `not.toContain('npx atc')`.
    - Drift-lock: for each of the 7 flag tokens, the token appears in BOTH the
      normalized README AND the normalized `parseCliArgs(['--help'])` help text
      (so a HELP_TEXT flag change forces a README + spec update).
    - Exit-code triad: README (normalized) contains `infrastructure-or-usage` and
      the three codes `0`/`1`/`2` with their meanings.
    - CHANGELOG hygiene: `## 0.2.2` present in repo-root CHANGELOG AND no
      internal-id leak (`not.toMatch(/DOC-01|CLI-0\d|SC#|\bphase\b/i)` over the
      0.2.2 entry slice).
  </behavior>
  <read_first>
    - packages/angular-typechecker/src/angular-cli-docs.spec.ts (the structural analog: imports, README load via `../README.md`, `\s+` whitespace-normalize, describe/it shape -- clone this)
    - packages/angular-typechecker/src/cli/parse-args.spec.ts (the `parseCliArgs(['--help'])` narrowing + the `not.toContain('npx atc')` precedent at lines 179/186, and the exit-code-triad assertions)
    - packages/angular-typechecker/src/cli/parse-args.ts (confirms `parseCliArgs` is exported at line 96 and `HELP_TEXT` is module-private -- read it via the `help` result)
    - packages/angular-typechecker/README.md (assertion target -- must already contain the Task 1 section)
    - CHANGELOG.md (assertion target -- must already contain the Task 2 entry)
  </read_first>
  <action>
    Create the new spec at `packages/angular-typechecker/src/standalone-cli-docs.spec.ts`
    (in `src/`, NOT `src/cli/`). Clone `angular-cli-docs.spec.ts` verbatim for the
    imports + README load: `readFileSync` from `node:fs`, `dirname`/`join` from
    `node:path`, `fileURLToPath` from `node:url`, `describe`/`expect`/`it` from
    `vitest`; `readmePath = join(dirname(fileURLToPath(import.meta.url)), '../README.md')`;
    `const readme = readFileSync(readmePath,'utf8'); const normalized = readme.replace(/\s+/g,' ');`.

    Import the exported seam with `import { parseCliArgs } from './cli/parse-args';`
    (note the `./cli/` prefix -- this spec sits one dir above `src/cli/`, unlike
    `parse-args.spec.ts` which uses `./parse-args`). Obtain the live help text:
    `const help = parseCliArgs(['--help']); const helpText = help.kind === 'help' ? help.text.replace(/\s+/g,' ') : '';`.

    Assertions (assert exact headings against raw `readme`; prose/flags against
    `normalized`):
    - heading: `expect(readme).toContain('## Standalone CLI')`.
    - ToC anchor: `expect(readme).toContain('[Standalone CLI](#standalone-cli)')`.
    - supply-chain: `expect(normalized).toContain('npx angular-typechecker')` AND
      `expect(normalized).not.toContain('npx atc')` (the load-bearing D-05 guard;
      mirrors parse-args.spec.ts:179).
    - drift-lock: iterate the 7 tokens `['-c, --tsConfig','--max-warnings','--fail-fast','--include-deps','--strict','-h, --help','--version']`
      and for each assert BOTH `expect(normalized).toContain(flag)` AND
      `expect(helpText).toContain(flag)` (genuine README/`--help` single-source guard).
    - exit codes: `expect(normalized).toContain('infrastructure-or-usage')` plus
      assert the three codes `0`/`1`/`2` and their meanings appear.
    - CHANGELOG hygiene (a second describe block): read the repo-root CHANGELOG via
      `join(dirname(fileURLToPath(import.meta.url)), '../../../CHANGELOG.md')`
      (src -> pkg root -> packages -> repo root), assert it contains `## 0.2.2`, then
      slice from `## 0.2.2` to the next `## 0.2.1` and assert
      `expect(entry).not.toMatch(/DOC-01|CLI-0\d|SC#|\bphase\b/i)`.

    Keep it a pure deterministic fs read -- no compiler load, no build artifact --
    so it runs in the fast `nx test` loop. Order this task LAST so both README
    (Task 1) and CHANGELOG (Task 2) already contain the asserted content; the spec
    must end GREEN.
  </action>
  <verify>
    <automated>nx test angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `packages/angular-typechecker/src/standalone-cli-docs.spec.ts` exists and imports `parseCliArgs` from `./cli/parse-args`.
    - The spec asserts the `## Standalone CLI` heading, the ToC anchor, `npx angular-typechecker` present + `not.toContain('npx atc')`, all 7 flag tokens in BOTH README and live help text, the `0`/`1`/`2` triad + `infrastructure-or-usage`, and a `## 0.2.2` CHANGELOG entry with no internal-id leak.
    - `nx test angular-typechecker` passes (all new assertions green) with no regression to existing specs.
  </acceptance_criteria>
  <done>The tripwire is green and will fail CI if any load-bearing claim (supply-chain guard, flag drift, exit-code triad, changelog hygiene) regresses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> reader's shell | A reader copies a command from the README/CHANGELOG and executes it. A wrong or ambiguous invocation string crosses into arbitrary code execution on the reader's machine. |

This is a pure documentation phase: no code paths, no runtime inputs, no
packages installed (npm/pip/cargo), and no trust-boundary change in the product.
ASVS L1 categories (auth, session, access control, crypto, input validation) are
therefore N/A. No package-install tasks exist, so no `T-{phase}-SC` legitimacy
gate applies.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Spoofing / Tampering | README `## Standalone CLI` install docs | mitigate | The docs present `npx angular-typechecker` as the ONLY uninstalled invocation; `atc` appears solely as a post-install PATH alias with an explicit inline warning naming the unrelated published package `atc@0.0.6` ("Manage fleet spawns", 2013 -- verified unrelated on the npm registry). Task 3's tripwire asserts `not.toContain('npx atc')`, mirroring `parse-args.spec.ts:179`, so the hazard cannot reappear without failing CI. |
| T-29-02 | Tampering | CHANGELOG `## 0.2.2` public entry | mitigate | Changelog-hygiene: end-user language only, no internal ids/scopes (AGENTS.md + memory changelog-readme-end-user-facing). Task 3's tripwire regex-asserts `not.toMatch(/DOC-01|CLI-0\d|SC#|\bphase\b/i)` over the entry so an internal-id leak into the public changelog / GitHub Release notes fails CI. |
</threat_model>

<verification>
Run all of the following green before phase verification (memory
verify-format-and-lint-before-release -- docs edits still trip Prettier/lint,
and the new spec must type-check):

- `nx test angular-typechecker` (the new tripwire + all existing specs)
- `nx format:check`
- `nx lint angular-typechecker`
- `nx typecheck angular-typechecker`
</verification>

<success_criteria>
- README has a `## Standalone CLI` section (install + full flag set mirroring
  HELP_TEXT + `0`/`1`/`2` exit-code table) and a `[Standalone CLI](#standalone-cli)`
  ToC anchor between Angular CLI and Storybook.
- The canonical uninstalled invocation documented is `npx angular-typechecker`;
  `npx atc` appears NOWHERE; `atc@0.0.6` supply-chain hazard is called out.
- The new exit-code table owns literal `2` and reconciles with (does not
  contradict) the existing `## Exit codes` section.
- CHANGELOG has a curated, undated, end-user-language `## 0.2.2` entry with no
  internal ids/scopes.
- `standalone-cli-docs.spec.ts` is green and drift-locks the flag set against the
  shipped `--help`.
- format:check, lint, and typecheck all pass.
</success_criteria>

<output>
Create `.planning/phases/29-docs/29-01-SUMMARY.md` when done.
</output>
