---
phase: 13-engine-solution-tsconfig-reference-walking
plan: 05
type: execute
wave: 4
depends_on: ["13-02", "13-04"]
files_modified:
  - packages/angular-typechecker/src/core/walk-references.integration.spec.ts
  - packages/angular-typechecker/src/core/config-resolution.integration.spec.ts
  - packages/angular-typechecker/src/core/run-typecheck.spec.ts
  - packages/angular-typechecker/src/executors/angular-typecheck/executor.spec.ts
autonomous: true
requirements: [WALK-01]
must_haves:
  truths:
    - "A real-compiler walk of solution-style reports two distinct-file TS2322 (both leaves ran)"
    - "The overlap fixture's shared-source diagnostic collapses to ONE across Programs"
    - "The out-of-project ref is skipped (its error never reported) and recorded skippedReferences reason:'out-of-project'"
    - "The broken-ref solution RESOLVES with one counted 90002 plus the survivor's TS2322 (fold-and-count)"
    - "config-resolution solution-style block asserts the walk; the COR-01 :100-121 block is byte-unchanged"
    - "detectTemplateCheckAborted fires on a TCB Fatal in a synthesized cross-leaf union"
    - "the executor logs skippedReferences via logger.warn only when non-empty"
  artifacts:
    - path: "packages/angular-typechecker/src/core/walk-references.integration.spec.ts"
      provides: "Real-compiler SC1/SC2/SC3/D-04/D-05 walk proofs against the new fixtures"
    - path: "packages/angular-typechecker/src/core/config-resolution.integration.spec.ts"
      provides: "Rewritten solution-style block asserting the walk; COR-01 block byte-unchanged"
  key_links:
    - from: "packages/angular-typechecker/src/core/walk-references.integration.spec.ts"
      to: "fixtures/solution-style*"
      via: "runTypecheck({ tsConfigPath })"
      pattern: "fixtures/solution-style"
---

<objective>
Prove the walk end-to-end against the committed fixtures (Plan 13-02) and the wired engine
(Plan 13-04): a real-compiler integration spec for SC1/SC2/SC3/D-04/D-05, the
`config-resolution.integration.spec.ts:124-152` solution-style rewrite (with the COR-01 `:100-121`
block byte-unchanged), a pure `detectTemplateCheckAborted` cross-leaf union unit, and an executor
unit for the `skippedReferences` `logger.warn`.

Purpose: this closes the WALK-01 validation contract's behavioral half -- every locked decision
(union completeness, cross-`Program` dedupe, boundary skip, three-way split, D-05 fold-and-count,
D-04 self-ref, cross-leaf TCB, adapter notice) gets an automated proof off `CoreResult`.

Output: the new integration spec, the rewritten config-resolution block, the union-scan unit, and
the executor unit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-engine-solution-tsconfig-reference-walking/13-RESEARCH.md
@.planning/phases/13-engine-solution-tsconfig-reference-walking/13-PATTERNS.md
@.planning/phases/13-engine-solution-tsconfig-reference-walking/13-VALIDATION.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: walk-references.integration.spec.ts (real-compiler SC1/SC2/SC3/D-04/D-05)</name>
  <read_first>
    - packages/angular-typechecker/src/core/config-resolution.integration.spec.ts (the FULL harness to copy: packageRoot/workspaceRoot via dirname(fileURLToPath(import.meta.url)) then join(workspaceRoot, 'fixtures', '<name>', 'tsconfig.json') at :32-52; `const TS2322 = 2322;` and `const NG = (code) => -990000 - code;` at :29-30; messageTextOf via ts.flattenDiagnosticMessageText at :54-56; assertions off `await runTypecheck({ tsConfigPath })` at :59-70)
    - packages/angular-typechecker/src/core/run-typecheck.ts (CoreResult fields consumed: rootNamesCount, errorCount, diagnostics, skippedReferences; TypecheckInfrastructureError for the D-05 negative assertion)
    - fixtures/solution-style, fixtures/solution-style-overlap, fixtures/solution-style-oop, fixtures/solution-style-empty, fixtures/solution-style-broken-ref, fixtures/solution-style-selfref (Plan 13-02 substrate)
    - .planning/phases/13-engine-solution-tsconfig-reference-walking/13-RESEARCH.md ("Validation Architecture" -> "Exact assertions proving each SC" + "Behaviors to sample")
    - .planning/phases/13-engine-solution-tsconfig-reference-walking/13-VALIDATION.md (Per-Task Verification Map rows)
    - .planning/codebase/TESTING.md (*.integration.spec.ts naming, 30000 timeout, it.each, NG() negative-encoding)
  </read_first>
  <action>
    Create packages/angular-typechecker/src/core/walk-references.integration.spec.ts mirroring the config-resolution harness (path helpers, TS2322/NG consts, messageTextOf, 30000 testTimeout). Assert bare positive codes for TS (2322) and the synthesized 90002 (`codes).toContain(90002)`); NG()-encode Angular codes only. Cover:
      - SC1/SC2 union completeness + both leaves ran (solution-style): rootNamesCount > 0; errorCount === 2; `codes.filter(c => c === 2322).length === 2`; the two reported TS2322 have DISTINCT file.fileName (error.component.ts vs error.component.spec.ts); result.skippedReferences is undefined.
      - SC2 dedupe collapse (solution-style-overlap): the shared-source diagnostic appears EXACTLY ONCE though gathered in both leaves -- assert the count of that specific (fileName, code) pair === 1; errorCount reflects the deduped multiset (no double-count).
      - SC2 boundary skip (solution-style-oop): the out-of-project leaf's planted error code is NOT in `codes`; errorCount === 1 with code 90001 (references-present none-in-project); skippedReferences is a non-empty array containing { reason: 'out-of-project', referencePath: <the oop path> }.
      - SC3 three-way split (it.each over [solution-style -> walk rootNamesCount>0 err 2], [solution-style-oop -> 90001 none-in-project], [solution-style-empty -> 90001 empty-project]).
      - SC3/D-05 fold-and-count (solution-style-broken-ref): `codes).toContain(90002)`; `codes.filter(c => c === 90002).length === 1`; the survivor leaf's TS2322 ALSO in codes; errorCount >= 2; skippedReferences has reason:'not-found'. NEGATIVE assertion: `await expect(runTypecheck({ tsConfigPath: brokenRefSolution })).resolves.toBeDefined()` -- it does NOT reject with TypecheckInfrastructureError (the per-leaf 500 was reclassified, not rethrown).
      - D-04 self/dup ref (solution-style-selfref): the single leaf TS2322 appears EXACTLY once despite the self + duplicate references (output-neutral); skippedReferences records reason:'self-reference'.
    Blank lines around control flow; braces on all bodies; ASCII-only.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- walk-references.integration.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The spec asserts, for solution-style: rootNamesCount > 0, errorCount === 2, exactly two code===2322 with distinct file.fileName, skippedReferences === undefined.
    - The overlap spec asserts the shared (fileName, code) pair count === 1 (dedupe collapse).
    - The oop spec asserts the out-of-project error code is absent from codes AND skippedReferences has a reason:'out-of-project' entry AND code 90001 present.
    - The broken-ref spec asserts exactly one code===90002, the survivor TS2322 present, and that runTypecheck RESOLVES (does NOT reject with TypecheckInfrastructureError).
    - The selfref spec asserts the leaf TS2322 count === 1 and skippedReferences reason:'self-reference'.
    - `npx nx test angular-typechecker -- walk-references.integration.spec.ts` passes; the file is named *.integration.spec.ts and uses a 30000 timeout.
  </acceptance_criteria>
  <done>
    All SC1/SC2/SC3/D-04/D-05 real-compiler proofs are green against the Plan 13-02 fixtures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the config-resolution solution-style block (COR-01 block byte-unchanged)</name>
  <read_first>
    - packages/angular-typechecker/src/core/config-resolution.integration.spec.ts (the FULL file: the COR-01 direct nonexistent-path pinning test at :100-121 which MUST stay byte-unchanged -- `rejects.toBeInstanceOf(TypecheckInfrastructureError)`; the solution-style guard block at :124-152 to REWRITE; the TS18003-independence `it` at :142-151 to KEEP)
    - fixtures/solution-style (the upgraded substrate: error.component.ts + error.component.spec.ts each carrying a distinct TS2322; both leaves referenced)
    - .planning/phases/13-engine-solution-tsconfig-reference-walking/13-RESEARCH.md ("Directive 4" expected codes/identities the rewritten spec asserts; "Exact assertions proving each SC" SC4; Pitfall 4 COR-01 containment)
    - .planning/phases/13-engine-solution-tsconfig-reference-walking/13-PATTERNS.md ("The block to REWRITE" description)
  </read_first>
  <action>
    In packages/angular-typechecker/src/core/config-resolution.integration.spec.ts, REWRITE ONLY the solution-style guard block (originally :124-152). It currently asserts rootNamesCount === 0, errorCount === 1, and the guard message. Replace with walk assertions: rootNamesCount > 0; errorCount === 2; `codes.filter(c => c === 2322).length === 2`; the two TS2322 have distinct file.fileName (error.component.ts vs error.component.spec.ts); codes still does NOT contain 18003 (KEEP the existing TS18003-independence `it` at :142-151 unchanged, per Spike 004 note 4); result.skippedReferences is undefined (both refs are in-project and walk cleanly). Do NOT alter the COR-01 direct nonexistent-path pinning test (:100-121) -- it stays BYTE-UNCHANGED (still `rejects.toBeInstanceOf(TypecheckInfrastructureError)`). ASCII-only.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- config-resolution.integration.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The solution-style block asserts rootNamesCount > 0, errorCount === 2, exactly two code===2322 with distinct file.fileName, and skippedReferences === undefined.
    - The codes-does-NOT-contain-18003 assertion is retained.
    - The COR-01 pinning test region is byte-unchanged: it still asserts `rejects.toBeInstanceOf(TypecheckInfrastructureError)` for the direct nonexistent-config path (`git diff` shows no change inside that `it`/`describe`).
    - `npx nx test angular-typechecker -- config-resolution.integration.spec.ts` passes.
  </acceptance_criteria>
  <done>
    The solution-style block asserts the walk; the COR-01 direct 500 pinning test and the TS18003-independence assertion are unchanged; the spec is green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cross-leaf detectTemplateCheckAborted unit + executor skippedReferences unit</name>
  <read_first>
    - packages/angular-typechecker/src/core/run-typecheck.ts (exported detectTemplateCheckAborted :474-489 and TCB_GENERATION_FATAL_DIAGNOSTIC_CODE; the "exported for the RES-02 unit tier" note :470-473)
    - packages/angular-typechecker/src/core/run-typecheck.spec.ts (existing detectTemplateCheckAborted unit coverage -- the pattern to extend for a synthesized cross-leaf union)
    - packages/angular-typechecker/src/executors/angular-typecheck/executor.ts (the skippedReferences logger.warn block added in Plan 13-04; the templateCheckAborted block :49-63)
    - packages/angular-typechecker/src/executors/angular-typecheck/executor.spec.ts (existing executor unit; how logger is mocked and how a CoreResult is returned by a mocked core -- the pattern to extend)
    - .planning/phases/13-engine-solution-tsconfig-reference-walking/13-RESEARCH.md ("Exact assertions" cross-leaf TCB: unit over a synthesized union; the executor unit with a mocked core returning skippedReferences)
    - .planning/phases/13-engine-solution-tsconfig-reference-walking/13-VALIDATION.md (cross-leaf TCB abort row; the D-02 adapter render)
  </read_first>
  <action>
    (1) In packages/angular-typechecker/src/core/run-typecheck.spec.ts, ADD a unit test: build a synthesized cross-leaf union `[...leafAClean, ...leafBWithTcbFatal]` where leafB contains a diagnostic with code === the TCB fatal code (NG3004 negative-encoded / TCB_GENERATION_FATAL_DIAGNOSTIC_CODE). Assert detectTemplateCheckAborted(union) returns the abort details (fires on a TCB abort in ANY leaf via the pre-filter union scan) and returns undefined for a union with no such code. Detect BY CODE only.
    (2) In packages/angular-typechecker/src/executors/angular-typecheck/executor.spec.ts, ADD unit tests for the D-02 render: mock the core to return a CoreResult whose skippedReferences is a non-empty array (one { referencePath, reason: 'out-of-project' }) -> assert logger.warn was called once per entry with an advisory message; then a CoreResult with skippedReferences undefined (and with an empty array if the core could produce one) -> assert logger.warn is NOT called for skippedReferences. Assert the notice does NOT change the returned success verdict (advisory-only).
    Blank lines around control flow; braces on all bodies; ASCII-only.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- run-typecheck.spec.ts executor.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - run-typecheck.spec.ts has a test proving detectTemplateCheckAborted fires on a TCB fatal present in the SECOND leaf of a synthesized union, and returns undefined when absent; detection is by code only.
    - executor.spec.ts has a test where a non-empty skippedReferences triggers logger.warn once per entry, and a test where undefined skippedReferences triggers NO skippedReferences logger.warn.
    - The executor test asserts the success verdict is unchanged by the notice (advisory-only).
    - `npx nx test angular-typechecker -- run-typecheck.spec.ts executor.spec.ts` passes.
  </acceptance_criteria>
  <done>
    The cross-leaf TCB union scan and the executor skippedReferences render are both unit-proven and green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture tsconfig -> runTypecheck | Integration specs drive the real engine over developer-authored-style tsconfigs |
| mocked core -> executor adapter | Executor unit drives the adapter render seam with a controlled CoreResult |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Information Disclosure | out-of-project reference traversal | mitigate (VERIFIER) | Task 1 solution-style-oop spec proves the outsider's error is NOT in codes and skippedReferences records reason:'out-of-project'; Task 3 proves the adapter surfaces it via logger.warn. This is the test that would catch a no-guard-baseline leak. |
| T-13-02 | Tampering (false PASS) | broken-reference false PASS | mitigate (VERIFIER) | Task 1 solution-style-broken-ref spec proves one counted 90002 + survivor TS2322 AND that runTypecheck RESOLVES (no rethrow); Task 2 proves the COR-01 direct 500 rethrow is byte-unchanged (direct-path infra still throws). Together they pin the fold-and-count semantics. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs (test-only additions; RESEARCH: audit N/A). |
</threat_model>

<verification>
- `npx nx test angular-typechecker` green (all new integration + unit specs, no regression).
- `git diff` confirms the COR-01 pinning test (:100-121) and the TS18003-independence `it` are byte-unchanged.
</verification>

<success_criteria>
The real-compiler integration spec proves SC1/SC2/SC3/D-04/D-05; the config-resolution solution-style
block asserts the walk while COR-01 stays byte-unchanged; the cross-leaf TCB union scan and the
executor skippedReferences render are unit-proven -- all green.
</success_criteria>

<output>
Create `.planning/phases/13-engine-solution-tsconfig-reference-walking/13-05-SUMMARY.md` when done.
</output>
