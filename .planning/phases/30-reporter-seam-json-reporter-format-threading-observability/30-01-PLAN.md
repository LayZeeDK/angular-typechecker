---
phase: 30-reporter-seam-json-reporter-format-threading-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/core/run-typecheck.ts
  - packages/angular-typechecker/src/core/walk-references.ts
  - packages/angular-typechecker/src/core/run-typecheck.spec.ts
  - packages/angular-typechecker/src/core/total-files-count.integration.spec.ts
autonomous: true
requirements: [OBS-01, VER-01]
must_haves:
  truths:
    - "D-11 / OBS-01: CoreResult carries an OPTIONAL totalFilesCount (non-declaration source-file count) captured off the live Program on the direct path AND via a name-deduped Set across walked leaves on the walk / multi-tsconfig path"
    - "D-11: evaluateResult NEVER reads totalFilesCount -- the verdict is byte-identical with and without the field present (EvaluateInput Pick keeps omitting it)"
    - "VER-01: a real-compiler integration spec over the solution-tsconfig walk fixture `solution-style-overlap` (whose `shared.component.ts` is compiled by BOTH leaves) asserts the EXACT deduped totalFilesCount literal -- proving the doubly-compiled file is counted exactly ONCE (a broken dedupe / naive per-leaf sum yields a strictly larger number)"
  artifacts:
    - "packages/angular-typechecker/src/core/run-typecheck.ts (totalFilesCount? on CoreResult + direct-path capture + finalizeUnion threading)"
    - "packages/angular-typechecker/src/core/walk-references.ts (LeafAccumulator source-file Set + gatherLeafInto population)"
    - "packages/angular-typechecker/src/core/total-files-count.integration.spec.ts (real count)"
  key_links:
    - "run-typecheck.finalizeUnion threads the walked Set size onto the returned CoreResult"
    - "gatherLeafInto (the shared walk + array-fan-out helper) populates the Set once, covering both multi-tsconfig paths"
  prohibitions:
    - "totalFilesCount MUST be OPTIONAL (a required field is a breaking CoreResult change under a patch bump -- Pitfall 14)"
    - "evaluate-result.ts EvaluateInput Pick MUST keep omitting totalFilesCount (verdict-neutral, D-11)"
    - "The count MUST exclude declaration files (!sf.isDeclarationFile) so lib.d.ts is never counted (D-11)"
---

<objective>
Capture the OPTIONAL observability field `CoreResult.totalFilesCount` (OBS-01, D-11):
the count of NON-declaration source files the type-check actually processed. This
is the foundational, verdict-neutral engine change the JSON reporter (30-02) reads
into `summary.totalFilesCount`.

Purpose: give agents/CI a meaningful "files checked" number without ever coupling it
to the verdict. `evaluateResult` / `toExitCode` stay the sole verdict owners (D-11).
Output: `totalFilesCount?` on `CoreResult`, captured on both engine paths, plus a
real-compiler integration proof and a verdict-neutrality negative test.

This plan has NO cross-plan dependency: it only widens the engine's result shape.
30-02 (json-report + render-report widen) depends on the `totalFilesCount` field
this plan adds; 30-03 (adapter threading) depends on 30-02.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/30-reporter-seam-json-reporter-format-threading-observability/30-CONTEXT.md
@.planning/phases/30-reporter-seam-json-reporter-format-threading-observability/30-RESEARCH.md
@.planning/phases/30-reporter-seam-json-reporter-format-threading-observability/30-PATTERNS.md

Project rules (load-bearing): Windows arm64 -- use `git grep`/`rg`, NEVER `grep`;
stage files by name (never `git add .`/`-A`/`-u`). JS/TS style: blank lines around
control flow + `return`; ALWAYS braces on control-flow bodies; `singleQuote: true`.
`nx test` (Vitest/esbuild) does NOT type-check specs -- `nx typecheck`, `nx lint`
(maxWarnings:0), and `nx format:check` are SEPARATE gates.
</context>

<artifacts_this_phase_produces>
This plan produces:
- `CoreResult.totalFilesCount?: number` (optional, non-declaration source-file count)
- `LeafAccumulator.sourceFileNames: Set<string>` (walk-path accumulator field)
- `packages/angular-typechecker/src/core/total-files-count.integration.spec.ts` (new spec)

(Phase-wide, for the source-grounding pass -- these symbols are NEW this phase and are
excluded from drift verification: `formatJsonReport`, `core/diagnostic-record.ts`
projection helper, the `--format`/`--quiet`/`--color` CLI flags + `ReportFormat` enum,
the `format` schema option on executor + builder, `CoreResult.totalFilesCount`,
`formatVersion`, and every new `*.spec.ts` this milestone adds.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture totalFilesCount on the direct single-leaf path (D-11 / OBS-01)</name>
  <files>packages/angular-typechecker/src/core/run-typecheck.ts, packages/angular-typechecker/src/core/run-typecheck.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/run-typecheck.ts (CoreResult interface :54-140; direct path + program-undefined guard :458-486; direct return :497-503; the VALUE-presence conditional-spread idiom at `finalize` :905; `presentIfNonEmpty` :256-263 -- ARRAY-only, do NOT use for a scalar)
    - packages/angular-typechecker/src/core/gather-diagnostics.ts (the non-declaration source-file iteration template :152-153 -- `if (sourceFile.isDeclarationFile) { continue; }`)
    - packages/angular-typechecker/src/core/evaluate-result.ts (EvaluateInput Pick :84-93 -- it MUST keep omitting totalFilesCount)
    - packages/angular-typechecker/src/core/run-typecheck.spec.ts (existing unit harness to extend)
  </read_first>
  <behavior>
    - A direct single-leaf run returns a CoreResult whose `totalFilesCount` equals the number of `!isDeclarationFile` source files on the live `Program` (lib.d.ts excluded).
    - `totalFilesCount` is present only when captured (value-presence spread), matching the shipped optional-field idiom.
    - Negative test: `evaluateResult` returns the IDENTICAL `{ success, outcome }` for a CoreResult with `totalFilesCount` present vs absent (locks EvaluateInput's omission -- verdict-neutral, D-11).
  </behavior>
  <action>
    Add `totalFilesCount?: number;` to the `CoreResult` interface (alongside the other
    optional fields, run-typecheck.ts:54-140). On the direct path, after `directResult`
    is built (:473-486) and before the return (:497-503), compute the count from the live
    program exactly as gather-diagnostics.ts:152-153 iterates: filter
    `result.program.getTsProgram().getSourceFiles()` by `!sf.isDeclarationFile` and take
    `.length`. Spread it onto the returned object with the VALUE-presence idiom copied from
    finalize (:905): `...(totalFilesCount !== undefined ? { totalFilesCount } : {})`.
    Do NOT use `presentIfNonEmpty` (array-only). Keep the field OPTIONAL (Pitfall 14 --
    a required field breaks CoreResult under the 0.2.2 -> 0.2.3 patch bump). Do NOT touch
    `evaluate-result.ts` -- its EvaluateInput Pick (:84-93) MUST keep omitting the field
    so the verdict stays neutral (D-11). In run-typecheck.spec.ts add: (a) a unit assertion
    that a stubbed/direct-path result carries `totalFilesCount`, and (b) the verdict-neutrality
    negative test -- `evaluateResult` yields the same verdict with and without the field.
  </action>
  <acceptance_criteria>
    - `run-typecheck.ts` `CoreResult` declares `totalFilesCount?: number`
    - The direct-path capture filters on `!sf.isDeclarationFile` (git grep confirms the `isDeclarationFile` guard on the new count)
    - `run-typecheck.spec.ts` contains a verdict-neutrality assertion that `evaluateResult` returns the same result with/without `totalFilesCount`
    - `nx test angular-typechecker` passes; `nx typecheck angular-typechecker` passes (spec type-check is a separate gate)
  </acceptance_criteria>
  <verify>
    <automated>nx test angular-typechecker && nx typecheck angular-typechecker</automated>
  </verify>
  <done>Direct-path `totalFilesCount` is captured as an optional non-declaration count; a negative test locks that the verdict never reads it; unit + spec type-check green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture totalFilesCount on the walk / multi-tsconfig path + real-count integration proof (D-11 / OBS-01 / VER-01)</name>
  <files>packages/angular-typechecker/src/core/walk-references.ts, packages/angular-typechecker/src/core/run-typecheck.ts, packages/angular-typechecker/src/core/total-files-count.integration.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/walk-references.ts (LeafAccumulator :111-116; gatherLeafInto :145-161 -- the shared walk + array-fan-out helper where each leaf's program is live; WalkResult :41-74; walkReferences acc literal :194-199)
    - packages/angular-typechecker/src/core/run-typecheck.ts (finalizeUnion :278-313; handleMultiTsConfig acc literal :650-655)
    - packages/angular-typechecker/src/core/walk-references.integration.spec.ts (:53 fixtureTsConfig helper; the "SC2 cross-Program dedupe collapse (solution-style-overlap)" describe :110-136 -- the real-compiler walk harness analog whose fixture is the one to reuse)
    - fixtures/solution-style-overlap/ (root tsconfig.json references tsconfig.lib.json + tsconfig.spec.json -> solution/walk path -> finalizeUnion; `shared.component.ts` is the ONE non-declaration source file compiled in BOTH leaves -- the genuine cross-leaf shared import that exercises the Set-dedupe)
  </read_first>
  <behavior>
    - A walk / multi-tsconfig run accumulates a name-deduped `Set<string>` of non-declaration source-file names across all leaves; `set.size` is threaded onto CoreResult.totalFilesCount via finalizeUnion.
    - A non-declaration source file compiled in two leaves is counted ONCE (name-dedupe, RESEARCH A3).
    - The integration spec runs the `solution-style-overlap` walk fixture (whose `shared.component.ts` is in BOTH the lib and spec leaves) and asserts the EXACT deduped `totalFilesCount` literal -- a broken dedupe (naive per-leaf sum) would double-count `shared.component.ts` and yield a strictly larger number, so the exact literal IS the dedupe proof.
  </behavior>
  <action>
    Add a fifth field `sourceFileNames: Set<string>` to `LeafAccumulator`
    (walk-references.ts:111-116). Populate it in `gatherLeafInto` (:145-161) where the
    leaf program is live -- for each `result.program` source file, if `!isDeclarationFile`,
    `acc.sourceFileNames.add(sf.fileName)` (dedupe by NAME string, not object -- RESEARCH A3).
    Both acc-literal constructors add the empty `Set`: `walkReferences` (:194-199) and
    `handleMultiTsConfig` (run-typecheck.ts:650-655). Carry `set.size` on `WalkResult`
    (:41-74) and thread it into `finalizeUnion` (:278-313) as a new param, spread onto the
    returned CoreResult with the same value-presence idiom as Task 1. Then create
    `core/total-files-count.integration.spec.ts` by cloning the walk-references.integration.spec.ts
    harness (findWorkspaceRoot + its `fixtureTsConfig` helper, :53). It MUST run the GENUINE
    solution-tsconfig walk fixture `solution-style-overlap` (do NOT use a single-leaf / direct-path
    tsconfig such as `gate-b-error/tsconfig.app.json` -- that takes the DIRECT path and never
    exercises the walk `Set`-dedupe): `await runTypecheck({ tsConfigPath:
    fixtureTsConfig('solution-style-overlap') })`. Determine the real deduped count once (run the
    fixture) and PIN it as an EXACT regression literal: `expect(result.totalFilesCount).toBe(<N>)`,
    where `<N>` is the count of DISTINCT non-declaration source files and `shared.component.ts`
    (compiled in BOTH the lib and spec leaves) contributes EXACTLY 1. Add a spec comment stating that
    a dedupe regression (naive per-leaf sum) would double-count `shared.component.ts` and yield a
    strictly larger number, so the exact-literal assertion IS the dedupe proof. Do NOT settle for
    `>= rootNamesCount` (it passes whether or not dedupe works). This is the ONE place the actual
    deduped value is exercised.
  </action>
  <acceptance_criteria>
    - `walk-references.ts` `LeafAccumulator` declares `sourceFileNames: Set<string>` and `gatherLeafInto` adds `!isDeclarationFile` file names to it
    - `finalizeUnion` receives and spreads the walked count onto CoreResult
    - `core/total-files-count.integration.spec.ts` runs the `solution-style-overlap` walk fixture (NOT a direct-path leaf) and asserts an EXACT deduped literal `result.totalFilesCount === <N>` (not `>= rootNamesCount`), proving `shared.component.ts` -- compiled in both leaves -- is counted exactly once
    - `nx test angular-typechecker` + `nx typecheck angular-typechecker` pass; `nx integration angular-typechecker` passes the new integration spec
  </acceptance_criteria>
  <verify>
    <automated>nx test angular-typechecker && nx typecheck angular-typechecker && nx integration angular-typechecker</automated>
  </verify>
  <done>The walk / multi-tsconfig path accumulates a name-deduped non-declaration Set threaded through finalizeUnion; a real-compiler integration spec over `solution-style-overlap` asserts the exact deduped literal, proving the doubly-compiled `shared.component.ts` is counted once; all tiers green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiler Program -> CoreResult | The engine reads the live `ts.Program`'s source files. `totalFilesCount` is a COUNT only -- no path text, no message text crosses into it. |

No package installs this phase (pure engine change) -- no supply-chain (T-*-SC) threat.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Repudiation / false-pass | `totalFilesCount` on CoreResult | high | mitigate | `evaluateResult`'s EvaluateInput Pick (evaluate-result.ts:84-93) keeps omitting the field; a negative test locks that the verdict is identical with/without it (D-11). The field is verdict-neutral by construction. |
| T-30-02 | Information disclosure | `totalFilesCount` value | low | accept | It is an integer count of non-declaration files -- no path or source text. Residual risk low. |
</threat_model>

<verification>
- `nx test angular-typechecker` green (unit incl. verdict-neutrality negative test).
- `nx typecheck angular-typechecker` green (spec type-check -- separate gate; `nx test` does NOT type-check specs).
- `nx integration angular-typechecker` green (real `totalFilesCount` proof).
- `nx lint angular-typechecker` (maxWarnings:0) + `nx format:check` green before plan completion.
- `index.drift.ts` still compiles (additive-only proxy) -- `totalFilesCount` is optional; the barrel is untouched.
</verification>

<success_criteria>
`CoreResult.totalFilesCount` is an optional, non-declaration source-file count captured
on both engine paths (direct live Program; walk Set-dedupe via finalizeUnion), proven by a
real-compiler integration spec, and the verdict never reads it (negative test). No breaking
change to CoreResult (field is optional).
</success_criteria>

<output>
Create `.planning/phases/30-reporter-seam-json-reporter-format-threading-observability/30-01-SUMMARY.md` when done.
</output>
