---
phase: 17-input-set-membership-boundary-layout-support
plan: 03
type: execute
wave: 2
depends_on: ["17-01", "17-02"]
files_modified:
  - packages/angular-typechecker/src/core/run-typecheck.ts
  - packages/angular-typechecker/src/core/run-typecheck.spec.ts
  - packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts
  - packages/angular-typechecker/src/core/infra-failure.spec.ts
autonomous: true
requirements: [SB-02, SB-04]

must_haves:
  truths:
    - "D-02: thread the inputTs union of DECLARED rootNames (walk rootNamePaths / direct parsed.rootNames) into the one shared finalize filter"
    - "Both entry paths (walk union + direct single-leaf) thread an inputTs union into the ONE shared finalize()->filterDiagnostics chokepoint"
    - "The walk path's inputTs = walk.rootNamePaths; the direct path's inputTs = parsed.rootNames"
    - "CoreResult carries suppressedThirdParty + suppressedInGraphErrorCount + suppressedInGraphWarningCount + suppressedInGraphFiles (no suppressedCount)"
    - "Guard paths (zero-rootNames, no Program) return the new suppressed fields as 0 / empty"
    - "An out-of-project first-party diagnostic suppressed by the boundary now increments suppressedInGraph (not a silent drop)"
  artifacts:
    - path: "packages/angular-typechecker/src/core/run-typecheck.ts"
      provides: "inputTs threaded through FinalizeFilter/buildFinalizeFilter/finalize; CoreResult split-counter fields"
      contains: "inputTs"
    - path: "packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts"
      provides: "real-compiler proof of the new count fields (sibling-import suppression -> suppressedInGraph)"
  key_links:
    - from: "walk.rootNamePaths"
      to: "buildFinalizeFilter(..., inputTs)"
      via: "the >=1 in-project leaf finalize call"
      pattern: "rootNamePaths"
    - from: "FinalizeFilter.inputTs"
      to: "filterDiagnostics FilterOptions.inputTs"
      via: "finalize()"
      pattern: "inputTs"
---

<objective>
Thread the `inputTs` union through the single `finalize()` -> `buildFinalizeFilter()` ->
`filterDiagnostics()` chokepoint that BOTH callers (solution walk-union and direct
single-leaf) already share, and replace the shipped scalar `CoreResult.suppressedCount`
with the split-counter fields from plan 17-01 (`suppressedThirdParty`,
`suppressedInGraphErrorCount`, `suppressedInGraphWarningCount`, `suppressedInGraphFiles`).

Purpose: this is the integration seam that gives "one boundary semantics" for free -- no new
plumbing, just an added field on `FinalizeFilter`/`FilterOptions` and the union build. After
this plan, an out-of-project first-party diagnostic the directory rule used to drop SILENTLY
is now counted as in-graph -- the milestone's core correctness fix, visible on `CoreResult`.
Output: threaded core + updated core specs proving the new count semantics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

READ-FIRST source-of-truth:
@.planning/phases/17-input-set-membership-boundary-layout-support/17-DECISION-input-set-boundary.md
@.planning/phases/17-input-set-membership-boundary-layout-support/17-CONTEXT.md
@.planning/phases/17-input-set-membership-boundary-layout-support/17-RESEARCH.md

The file being modified + the upstream contracts (17-01 + 17-02 outputs):
@packages/angular-typechecker/src/core/run-typecheck.ts
@packages/angular-typechecker/src/core/filter-diagnostics.ts
@packages/angular-typechecker/src/core/walk-references.ts

Prior-plan SUMMARYs (dependency outputs -- the delivered FilterResult / WalkResult shapes):
@.planning/phases/17-input-set-membership-boundary-layout-support/17-01-SUMMARY.md
@.planning/phases/17-input-set-membership-boundary-layout-support/17-02-SUMMARY.md

<interfaces>
<!-- Consumes: FilterOptions.inputTs + the split FilterResult (17-01); WalkResult.rootNamePaths (17-02). -->
<!-- Delivers: the CoreResult shape below (fields consumed by 17-04 verdict + 17-05 executor). -->

```typescript
// run-typecheck.ts -- CoreResult (delta from current):
//   REMOVE:  suppressedCount: number
//   ADD:
export interface CoreResult {
  // ...existing tsConfigPath / rootNamesCount / diagnostics / errorCount /
  //    warningCount / durationMs / templateCheckAborted? / skippedReferences? ...
  suppressedThirdParty: number;
  suppressedInGraphErrorCount: number;
  suppressedInGraphWarningCount: number;
  suppressedInGraphFiles: readonly string[];
}

// FinalizeFilter + buildFinalizeFilter gain `inputTs: readonly string[]`.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: thread inputTs + split CoreResult counters through finalize</name>
  <files>packages/angular-typechecker/src/core/run-typecheck.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/run-typecheck.ts (the file being modified: `FinalizeFilter` interface ~467, `buildFinalizeFilter` ~166, `finalize` ~498, the walk `>=1 in-project leaf` finalize call ~295, the direct-path finalize call ~398, the guard-path finalize calls ~329/344)
    - packages/angular-typechecker/src/core/filter-diagnostics.ts (17-01: FilterOptions.inputTs + the four FilterResult fields)
    - packages/angular-typechecker/src/core/walk-references.ts (17-02: WalkResult.rootNamePaths)
    - 17-RESEARCH.md "System Architecture Diagram" (both entry paths) + "Pattern 4"
  </read_first>
  <action>
    Add `inputTs: readonly string[]` to the `FinalizeFilter` interface and to `buildFinalizeFilter` (thread it into the returned filter object). At the WALK `>=1 in-project leaf` finalize call, pass `inputTs: walk.rootNamePaths` into `buildFinalizeFilter`. At the DIRECT single-leaf finalize call, pass `inputTs: parsed.rootNames`. In `finalize`, pass `filter.inputTs` into `filterDiagnostics` via `FilterOptions.inputTs`, and read the four new `FilterResult` fields (`suppressedThirdParty`, `suppressedInGraphErrorCount`, `suppressedInGraphWarningCount`, `suppressedInGraphFiles`) into locals. REPLACE `CoreResult.suppressedCount` (interface + all return sites) with the four new fields; on the guard paths where `finalize` is called WITHOUT a filter (zero-rootNames / no Program), default all four to `0` / `[]`. Keep `resolveFilterBasePath` and the base-path (D-05) logic unchanged (base stays the SOLUTION/host tsconfig dir -- the narrowed base clause of D-04a). Do NOT alter the `templateCheckAborted` / `skippedReferences` detection (they stay as-is; the verdict wiring is plan 17-04). Core stays PURE.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- run-typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `CoreResult` has the four new fields and NO `suppressedCount`; `FinalizeFilter`/`buildFinalizeFilter` carry `inputTs`.
    - The walk finalize call passes `walk.rootNamePaths`; the direct call passes `parsed.rootNames`.
    - `npx nx build angular-typechecker` type-checks (the field rename cascades cleanly).
    - `npx nx test angular-typechecker -- run-typecheck` passes (Task 2 specs).
  </acceptance_criteria>
  <done>Both paths thread inputTs into the one shared filter; CoreResult exposes the split counter; guard paths return zeros.</done>
</task>

<task type="auto">
  <name>Task 2: update core specs for the field rename + new count semantics</name>
  <files>packages/angular-typechecker/src/core/run-typecheck.spec.ts, packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts, packages/angular-typechecker/src/core/infra-failure.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts (the `sibling-import` out-of-project case asserts `suppressedCount >= 1` ~128; the `includeDeps: true` case asserts `suppressedCount 0` ~146; ~209)
    - packages/angular-typechecker/src/core/infra-failure.spec.ts (~241 `suppressedCount >= 1`)
    - packages/angular-typechecker/src/core/run-typecheck.spec.ts (any suppressedCount assertions)
    - 17-RESEARCH.md "Pitfall 3" (the split-counter classifies a correctly-isolated out-of-project first-party diagnostic as suppressedInGraph -- this is the intended R1 semantics)
  </read_first>
  <action>
    Replace every `result.suppressedCount` assertion with the correct new field: an out-of-project first-party `.ts` sibling (the `sibling-import` fixture) that was suppressed now asserts `result.suppressedInGraphErrorCount >= 1` AND its file appears in `result.suppressedInGraphFiles` (per R1: a suppressed non-node_modules first-party diagnostic is in-graph, NOT third-party). A `node_modules` suppression asserts `result.suppressedThirdParty >= 1`. The `includeDeps: true` fold-back asserts ALL four suppressed fields are `0`/empty. Do NOT assert a clean/`success` verdict on any run where the boundary suppressed a first-party diagnostic -- these specs assert on CoreResult COUNTS only (the coverage-incomplete VERDICT flip is proven in plan 17-04 / 17-05). If any existing assertion implied a clean run coexisting with a suppression, update the comment to reflect the new in-graph semantics.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- run-typecheck infra-failure</automated>
  </verify>
  <acceptance_criteria>
    - No `suppressedCount` reference remains in the three specs.
    - The sibling-import out-of-project case asserts `suppressedInGraphErrorCount >= 1` + a file in `suppressedInGraphFiles`.
    - The includeDeps fold-back asserts all four suppressed fields zero/empty.
    - `npx nx test angular-typechecker` full suite passes.
  </acceptance_criteria>
  <done>All core specs consume the split-counter fields; the sibling-import case documents the R1 in-graph classification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| walk/direct entry -> shared finalize filter | the ONE chokepoint both paths route through; an inputTs threaded to only one path would reintroduce walk/direct drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Tampering (walk/direct drift) | threading inputTs to one path only | mitigate | inputTs is threaded through the SINGLE shared `finalize`->`buildFinalizeFilter`->`filterDiagnostics` chokepoint both callers already use; a missed path fails the 17-06 Layout-A/B integration proof |
| T-17-01 | Tampering (of the verdict) | boundary classification on the solution base + inputTs | mitigate | inherits 17-01's dual-identity canonicalizer + KEEP-on-throw; base stays the narrowed solution/host dir (D-04a), never the workspace root |
| T-17-08 | Repudiation (a dropped first-party diagnostic reads clean) | guard paths returning stale/absent counts | mitigate | guard paths (no Program) return the four suppressed fields as 0/[]; a real suppression on the filter path increments suppressedInGraph, feeding the 17-04 coverage-incomplete gate |
| T-17-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | N/A -- no package installs this phase |
</threat_model>

<verification>
- `npx nx test angular-typechecker` full suite green (unit + integration).
- `npx nx build angular-typechecker` type-checks (field-rename cascade).
- Per wave merge: build + lint + format:check (CI gates).
</verification>

<success_criteria>
- inputTs threaded through both entry paths via the one shared filter.
- CoreResult exposes the split counter; guard paths return zeros.
- Existing core specs green against the new fields, documenting the R1 in-graph classification.
</success_criteria>

<output>
Create `.planning/phases/17-input-set-membership-boundary-layout-support/17-03-SUMMARY.md` when done.
</output>
