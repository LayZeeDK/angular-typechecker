---
phase: 12-extended-diagnostic-catalog-completeness-tripwire
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - packages/angular-typechecker/src/core/extended-catalog.integration.spec.ts
  - packages/angular-typechecker/src/core/baseline.angular13.integration.spec.ts
  - fixtures/ng-baseline-extra/error.component.ts
  - fixtures/ng-baseline-extra/error.component.html
  - fixtures/ng-baseline-extra/tsconfig.app.json
  - fixtures/ng-baseline-import-cycle/first.component.ts
  - fixtures/ng-baseline-import-cycle/second.component.ts
  - fixtures/ng-baseline-import-cycle/tsconfig.app.json
  - .planning/codebase/TESTING.md
autonomous: true
requirements: [CAT-03]
must_haves:
  truths:
    - "The 12 baseline TS/NG codes (TS2322, TS2339, NG2003, NG2005, NG2007, NG2009, NG1001, NG3003, NG6100, NG8001, NG8002, NG8004) are each asserted by exact code in a sibling it.each table"
    - "NG6100 is asserted as a Warning (WARN_ prefix) and counted in warningCount"
    - "TS codes assert as bare numbers; NG codes route through NG()"
    - "The folded baseline.angular13 spec is deleted -- one catalog of record"
    - "TESTING.md's integration-spec-count line reflects the net fold (3 specs removed, 1 catalog added)"
  artifacts:
    - path: "packages/angular-typechecker/src/core/extended-catalog.integration.spec.ts"
      provides: "The sibling baseline it.each table (added alongside the extended table from Plan 02)"
      contains: "NG(8002)"
  key_links:
    - from: "packages/angular-typechecker/src/core/extended-catalog.integration.spec.ts"
      to: "./run-typecheck"
      via: "each baseline row runs runTypecheck against a fixtures/* tsconfig and asserts the code is present"
      pattern: "runTypecheck"
    - from: "packages/angular-typechecker/src/core/extended-catalog.integration.spec.ts"
      to: "./diagnostic-codes"
      via: "NG() encodes every NG baseline code; TS codes assert bare"
      pattern: "NG\\("
---

<objective>
Add the baseline TS/NG code coverage (CAT-03) as a sibling `it.each` table inside the catalog of record,
back it with the ~2 new baseline fixtures the existing ones do not cover, fold and delete
`baseline.angular13.integration.spec.ts`, and correct TESTING.md's spec-count tally for the net fold.

Purpose: CAT-03 requires the 12 baseline codes each asserted by exact code. Placing them as a sibling
table in the same catalog spec keeps one catalog of record (D-06/D-07) rather than a scattered set of
per-version files.

Output: the baseline `it.each` block appended to `extended-catalog.integration.spec.ts`, ~2 new baseline
fixtures (a general NG-baseline-extra program + an import-cycle program for the trickiest NG3003), the
deletion of `baseline.angular13.integration.spec.ts`, and an updated TESTING.md count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-CONTEXT.md
@.planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-RESEARCH.md
@.planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-PATTERNS.md

<interfaces>
<!-- Exact contracts the executor needs. Use directly; no codebase exploration needed. -->

From packages/angular-typechecker/src/core/diagnostic-codes.ts:
- `export const NG = (code: number): number => -990000 - code;` (NG codes route through NG(); TS codes assert bare).

From packages/angular-typechecker/src/core/baseline.angular13.integration.spec.ts (the analog to extend/fold):
- Idiom (lines 38-56): `const codes = result.diagnostics.map(d => d.code); expect(codes).toContain(TS2339);`
  for a raw TS code, and `expect(codes).toContain(NG(8001));` for an NG code. Asserts by PRESENCE of the
  exact code (CAT-03 asks "asserted by exact code", not exact count -- keep the `toContain` idiom for baseline).
- The cwd-independent path resolver (lines 22-23): same `packageRoot`/`workspaceRoot` two-step as Plan 02.

## The 12 baseline codes (VERIFIED vs @angular/compiler-cli@22.0.4 error_code.d.ts; RESEARCH Baseline Codes lines 252-276)
| Code | Category | Minimal trigger | Covered today? |
|------|----------|-----------------|----------------|
| TS2322 | Error (raw) | `count: number = 'x'` class-level type error | YES `fixtures/gate-b-error/` |
| TS2339 | Error (raw) | template references a missing class member | YES `fixtures/ts-baseline/` |
| NG2003 | Error | constructor with a primitive/untyped DI param lacking a token | NO |
| NG2005 | Error | undecorated class passed as a provider | NO |
| NG2007 | Error | undecorated base class uses Angular features | NO |
| NG2009 | Error | `ViewEncapsulation.ShadowDom` + selector without a hyphen | NO |
| NG1001 | Error | `@Component(someVar)` (metadata not a literal) | NO |
| NG3003 | Error | a directive/pipe relationship forcing an un-handleable cyclic import | NO (trickiest -- own 2-file fixture) |
| NG6100 | WARNING (WARN_ prefix) | `@NgModule({ id: module.id })` | NO -- assert `DiagnosticCategory.Warning`, count in warningCount |
| NG8001 | Error | unknown custom element in template | YES `fixtures/ng-baseline/` |
| NG8002 | Error | unknown attribute on a known element | NO |
| NG8004 | Error | template uses a pipe that isn't declared/imported | NO |

## Baseline-code notes (RESEARCH lines 273-276 + Open Question 2)
- NG6100 is a WARNING (`WARN_` prefix): its row must expect `DiagnosticCategory.Warning`, not Error.
- Baseline codes have NO `it.skip` carve-out (the `it.skip` rule is scoped to the 18 EXTENDED members).
  NG3003 (import-cycle) is the trickiest to stage statically -- attempt a 2-file directive/pipe cross-
  reference cycle first. If NG3003 genuinely will not fire deterministically from a static fixture, that
  is a REQUIREMENTS question, NOT an auto-skip: flag it in the SUMMARY as `human_needed` rather than
  silently skipping it.
- Do NOT re-introduce the stale "aliases" (NG8004 (NG1019), NG2005 (NG1005), NG3003 (NG8003)) -- those
  alias numbers do not appear in v22.0.4 `error_code.d.ts`; assert the primary codes only.

## Fixtures
- REUSE existing: `fixtures/ts-baseline/` (TS2339), `fixtures/ng-baseline/` (NG8001), `fixtures/gate-b-error/` (TS2322).
- NEW `fixtures/ng-baseline-extra/`: a program (may be a small multi-file program) that fires the 8 uncovered
  NG codes that do NOT need a cross-file import cycle: NG2003, NG2005, NG2007, NG2009, NG1001, NG6100,
  NG8002, NG8004. Each trigger from the table above. If a single component cannot host all 8 cleanly
  (e.g. NG1001's non-literal metadata vs the others), use multiple `.ts` files listed in the tsconfig
  `files` array within this one fixture directory, or split into a second sibling fixture -- update
  `files_modified` accordingly.
- NEW `fixtures/ng-baseline-import-cycle/`: the 2-file directive/pipe cross-reference cycle that forces NG3003.

## TESTING.md count line (`.planning/codebase/TESTING.md:46`)
Current: `(10 .integration.spec.ts files).` Net fold across Plans 02+03: DELETE extended.angular13,
extended.promotion, baseline.angular13 (-3) and ADD extended-catalog.integration.spec.ts (+1) => net -2 =>
`(8 .integration.spec.ts files).` Recompute against the actual file count at execution time
(`git ls-files 'packages/angular-typechecker/src/**/*.integration.spec.ts' | wc -l`) rather than trusting
the arithmetic blindly, and set the line to the real number.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the ~2 new baseline fixtures (general NG codes + the NG3003 import cycle)</name>
  <read_first>
    - fixtures/ng-baseline/error.component.ts + tsconfig.app.json (the single-diagnostic NG fixture shape to mirror)
    - fixtures/ts-baseline/error.component.ts (a second example)
    - .planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-RESEARCH.md (Baseline Codes lines 252-276 -- the verified trigger for each of the 9 uncovered baseline codes + the NG3003/Open-Question-2 note)
    - node_modules/@angular/compiler-cli/src/ngtsc/diagnostics/src/error_code.d.ts (use `rg -uu` -- confirm NG2003=57, NG2005=60, NG2007=70, NG2009=80, NG1001=12, NG3003=166, NG6100=230, NG8002=242, NG8004=250 before authoring)
  </read_first>
  <files>fixtures/ng-baseline-extra/, fixtures/ng-baseline-import-cycle/</files>
  <behavior>
    - `fixtures/ng-baseline-extra/` (via runTypecheck) surfaces NG2003, NG2005, NG2007, NG2009, NG1001,
      NG6100, NG8002, NG8004. NG6100 is a Warning; the others are Errors.
    - `fixtures/ng-baseline-import-cycle/` surfaces NG3003 (import-cycle detected).
  </behavior>
  <action>
    Create `fixtures/ng-baseline-extra/` mirroring `fixtures/ng-baseline/`: a `tsconfig.app.json`
    (extends `../../tsconfig.base.json`, `strictTemplates: true`, `noEmit: true`) and the component
    file(s) using the verified triggers from RESEARCH Baseline Codes (e.g. `@NgModule({ id: module.id })`
    for NG6100; `ViewEncapsulation.ShadowDom` + hyphen-less selector for NG2009; `@Component(someVar)`
    non-literal metadata for NG1001; an unknown attribute on a known element for NG8002; an undeclared
    pipe for NG8004). If one component cannot host all 8 codes cleanly, use multiple `.ts` files in the
    fixture's `files` array (this is a baseline PRESENCE check, not an exact-count check, so co-firing is
    tolerable as long as each target code appears). Create `fixtures/ng-baseline-import-cycle/` as a 2-file
    directive/pipe cross-reference cycle that forces NG3003. Give each component a load-bearing header
    naming the NG codes it triggers. Do NOT add `@ts-nocheck`. CONFIRM each code actually appears in a real
    run (Task 2 asserts them). If NG3003 will not fire deterministically, flag it `human_needed` in the
    SUMMARY (baseline codes have no `it.skip` carve-out -- see `<interfaces>`); do NOT silently drop it.
  </action>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && ls fixtures/ng-baseline-extra fixtures/ng-baseline-import-cycle</automated>
  </verify>
  <acceptance_criteria>
    - Both new fixture directories exist, each with a `tsconfig.app.json` extending `../../tsconfig.base.json` with `strictTemplates: true`.
    - `fixtures/ng-baseline-import-cycle/` has two component `.ts` files forming a cross-reference cycle.
    - No fixture contains `@ts-nocheck`; each component .ts has a header naming its target NG codes.
  </acceptance_criteria>
  <done>The 2 new baseline fixtures exist, triggering the 9 uncovered baseline codes (NG3003 via the 2-file cycle), mirroring the existing baseline-fixture discipline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the sibling baseline it.each table to the catalog spec</name>
  <read_first>
    - packages/angular-typechecker/src/core/baseline.angular13.integration.spec.ts (the `codes.toContain(...)` idiom to extend/absorb -- folded/deleted in Task 3)
    - packages/angular-typechecker/src/core/extended-catalog.integration.spec.ts (the catalog of record from Plan 02 -- append the baseline block; reuse its `packageRoot`/`workspaceRoot` resolver + NG import)
    - .planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-RESEARCH.md (Baseline Codes lines 252-276)
    - .planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-PATTERNS.md (Sibling baseline table note, lines 150-155)
  </read_first>
  <files>packages/angular-typechecker/src/core/extended-catalog.integration.spec.ts</files>
  <behavior>
    - For each of the 12 baseline codes, `runTypecheck` against its fixture yields a `result.diagnostics`
      whose `.code` set contains the exact code: bare `2322`/`2339` for TS; `NG(2003)`, `NG(2005)`,
      `NG(2007)`, `NG(2009)`, `NG(1001)`, `NG(3003)`, `NG(6100)`, `NG(8001)`, `NG(8002)`, `NG(8004)` for NG.
    - The NG6100 assertion additionally confirms the matched diagnostic's `.category === ts.DiagnosticCategory.Warning`.
  </behavior>
  <action>
    Append a sibling baseline `it.each` block to `extended-catalog.integration.spec.ts` (do NOT create a
    new file -- one catalog of record, D-06/D-07). Model rows as
    `{ label, code, fixtureTsConfig, isNg, expectWarning? }` where `code` is the value to look for
    (bare TS number, or the 4-digit NG number that will be wrapped with `NG()` when `isNg`). Drive with
    `describe.each(BASELINE_CATALOG)('baseline diagnostic %s', (row) => { ... })`. In each `it`,
    `runTypecheck({ tsConfigPath: row.fixtureTsConfig })`, build `const codes = result.diagnostics.map(d => d.code)`,
    and assert `codes.toContain(row.isNg ? NG(row.code) : row.code)` (mirroring
    `baseline.angular13.integration.spec.ts`). For NG6100, additionally `find` the diagnostic by `NG(6100)`
    and assert `.category === ts.DiagnosticCategory.Warning` and `result.warningCount >= 1`. Map: TS2339 ->
    ts-baseline, NG8001 -> ng-baseline, TS2322 -> gate-b-error, NG3003 -> ng-baseline-import-cycle, the
    remaining 8 NG codes -> ng-baseline-extra (or its split siblings from Task 1). Reuse the file's existing
    `packageRoot`/`workspaceRoot` resolver and `NG` import; add no duplicate imports. Do NOT re-introduce
    the stale alias parentheticals. Confirm the block is GREEN in a real run.
  </action>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && npx nx test angular-typechecker --skip-nx-cache -- extended-catalog.integration</automated>
  </verify>
  <acceptance_criteria>
    - `npx nx test angular-typechecker --skip-nx-cache -- extended-catalog.integration` passes including the baseline block.
    - All 12 baseline codes are asserted: `git grep -c "NG(2003)\|NG(2005)\|NG(2007)\|NG(2009)\|NG(1001)\|NG(3003)\|NG(6100)\|NG(8001)\|NG(8002)\|NG(8004)" packages/angular-typechecker/src/core/extended-catalog.integration.spec.ts` covers all 10 NG codes, and both `2322` and `2339` appear as bare TS numbers.
    - The NG6100 row asserts `ts.DiagnosticCategory.Warning` (not Error).
    - No stale alias numbers (NG1019/NG1005/NG8003) are asserted.
  </acceptance_criteria>
  <done>The sibling baseline table asserts all 12 baseline codes by exact code (NG6100 as a Warning), appended to the single catalog of record.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the folded baseline spec and correct the TESTING.md spec count</name>
  <read_first>
    - packages/angular-typechecker/src/core/baseline.angular13.integration.spec.ts (its TS2339 + NG8001 assertions must now live in the catalog baseline block before deletion)
    - packages/angular-typechecker/src/core/extended-catalog.integration.spec.ts (confirm TS2339 + NG8001 are covered by the baseline block)
    - .planning/codebase/TESTING.md (the spec-count line at ~line 46: `(10 .integration.spec.ts files).`)
  </read_first>
  <files>packages/angular-typechecker/src/core/baseline.angular13.integration.spec.ts, .planning/codebase/TESTING.md</files>
  <action>
    AFTER confirming the catalog baseline block covers TS2339 (ts-baseline) and NG8001 (ng-baseline) --
    the two assertions `baseline.angular13.integration.spec.ts` held -- DELETE it via `git rm` (per
    AGENTS.md, prefer `git rm`; never `git add -A`). Then update `.planning/codebase/TESTING.md`: recompute
    the true integration-spec count with `git ls-files 'packages/angular-typechecker/src/**/*.integration.spec.ts' | wc -l`
    (the net fold across Plans 02+03 removes 3 specs and adds 1 catalog => expect 8, but use the measured
    number) and set the `(N .integration.spec.ts files).` line to that value. Change only that count; do not
    rewrite the surrounding TESTING.md prose. Re-run the full plugin suite to confirm no dangling reference
    to the deleted spec.
  </action>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && test ! -f packages/angular-typechecker/src/core/baseline.angular13.integration.spec.ts && npx nx test angular-typechecker --skip-nx-cache</automated>
  </verify>
  <acceptance_criteria>
    - `baseline.angular13.integration.spec.ts` no longer exists; the deletion is staged via `git rm` (noted in the SUMMARY).
    - `.planning/codebase/TESTING.md`'s `(N .integration.spec.ts files).` line equals the measured count from `git ls-files 'packages/angular-typechecker/src/**/*.integration.spec.ts' | wc -l`.
    - `npx nx test angular-typechecker --skip-nx-cache` passes with no reference to the deleted spec.
  </acceptance_criteria>
  <done>The baseline spec is folded and removed via `git rm`; TESTING.md's spec count matches the real post-fold file count; the suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test/doc-only phase: committed in-repo fixtures fed to the locked, lockfile-pinned `@angular/compiler-cli@22.0.4`; a planning-doc count edit. No runtime code path, no network, no untrusted input, no new dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | new baseline fixtures + the baseline spec block + TESTING.md count | accept | No new threat surface: authored-in-repo fixtures fed to the locked compiler; a documentation count edit. RESEARCH section Security Domain: no ASVS category applies. |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | N/A -- this plan installs NO packages. If an executor unexpectedly adds a dependency, STOP and escalate to a `checkpoint:human-verify` per the security threat-model directive. |

No `high`-severity threats; nothing to block on (RESEARCH section Security Domain: no new threat surface introduced).
</threat_model>

<verification>
- `npx nx test angular-typechecker --skip-nx-cache -- extended-catalog.integration` passes with the baseline block.
- `baseline.angular13.integration.spec.ts` deleted; TESTING.md count corrected to the measured value.
- NG6100 asserted as a Warning; all 12 baseline codes asserted by exact code; no stale aliases.
</verification>

<success_criteria>
- CAT-03: the 12 baseline TS/NG codes each asserted by exact code in a sibling table within the catalog of record.
- D-06/D-07: baseline spec folded and deleted; one catalog of record.
- TESTING.md's spec-count tally is honest after the net fold.
</success_criteria>

<output>
Create `.planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-03-SUMMARY.md` when done.
</output>
