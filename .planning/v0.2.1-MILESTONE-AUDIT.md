---
milestone: v0.2.1
milestone_name: Angular CLI workspace support
audited: 2026-07-11
status: passed
scores:
  requirements: 16/16
  phases: 4/4
  integration: integrated
  flows: 3/3
nyquist:
  compliant_phases: [21, 22, 23, 24]
  partial_phases: []
  missing_phases: []
  overall: compliant
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt: []
---

# Milestone v0.2.1 Audit: Angular CLI workspace support

**Verdict: PASSED.** All 16 requirements satisfied (3-source cross-referenced), all 4 phases
verified + secured + Nyquist-validated, cross-phase integration INTEGRATED with 0 gaps, all
3 E2E flows complete. Additive-only vs `angular-typechecker@0.2.0` HOLDS -- the milestone stays
on the 0.2.x line (no v0.3.0 trigger). Package version intentionally still `0.2.0`; the 0.2.1
cut is the deferred human-gated Release-PR (AGENTS.md).

## Phases (4/4)

| Phase | Verify | Secure | Nyquist |
|-------|--------|--------|---------|
| 21 angular-cli-builder + engine multi-tsConfig + GATE A' spike | passed | verified | compliant |
| 22 configuration schematic (angular.json write-fork) | passed | threats_open 0 | compliant |
| 23 init parity + first-party ng-add | passed | verified | compliant |
| 24 real-OSS + scaffolded e2e + additive-only audit + docs | passed | verified | compliant |

## Requirements (16/16 satisfied, 3-source cross-reference)

VERIFICATION.md status + REQUIREMENTS.md traceability (`Complete`) + SUMMARY frontmatter
`requirements-completed`. All three agree except ENG-01 and ACB-02, whose SUMMARY frontmatter is
empty by design (the 21-01/21-02 SUMMARYs record them as "substantively delivered + closed by the
phase verifier after GATE A' GO"); both were manually re-verified as satisfied via the phase
verifier + the integration checker (WIRED) + the standing test guards.

| REQ | Phase | Final | 3-source note |
|-----|-------|-------|---------------|
| ENG-01 | 21 | satisfied | passed + verifier-closed + WIRED (normalize-options + multi-tsconfig integration) |
| ACB-01 | 21 | satisfied | 21-03 frontmatter + WIRED |
| ACB-02 | 21 | satisfied | passed + verifier-closed + GATE A' spike 011 GO + ACV-01 real `ng run` |
| ACB-03 | 21 | satisfied | 21-03 frontmatter + `executors ?? builders` regression guard |
| ACS-01 | 22 | satisfied | 22-01 frontmatter + write-fork leaf-array guard |
| ACS-02 | 22 | satisfied | 22-01 frontmatter + Nx else-branch byte-unchanged |
| ACS-04 | 22 | satisfied | 22-02 frontmatter + `generators ?? schematics` guard |
| COV-01 | 22 | satisfied | 22-01 frontmatter + per-project no-bleed guard + ACV-01 real clone |
| ACS-03 | 23 | satisfied | 23-01 frontmatter + init CLI early-return fork |
| NGADD-01 | 23 | satisfied | 23-02/23-03 frontmatter + ng-add composes configuration |
| ACP-01 | 23 | satisfied | 23-02 frontmatter + optional-peer manifest contract |
| ACV-01 | 24 | satisfied | 24-03 frontmatter + real-clone gate PASS (post-fix, re-verified) |
| ACV-02 | 24 | satisfied | 24-03 frontmatter + committed scaffolded CI e2e |
| ACV-03 | 24 | satisfied | 24-01 frontmatter + builder-over-BuilderContext parity spec |
| ACP-02 | 24 | satisfied | 24-01 frontmatter + additive-only audit + barrel drift tripwire |
| ACD-01 | 24 | satisfied | 24-02 frontmatter + README `## Angular CLI` + docs tripwire |

No unsatisfied requirements. No orphaned requirements (all 16 appear in a phase VERIFICATION.md).

## Cross-Phase Integration (INTEGRATED, 0 gaps)

gsd-integration-checker traced every link end-to-end: executor -> `convertNxExecutor` builder ->
`ng run :typecheck`; generators -> collection.json; `ng-add` composes `configuration`;
`executors ?? builders` + `generators ?? schematics` keep the Nx surface byte-unchanged; the ENG-01
`tsConfig: string | string[]` widening flows schema -> normalize -> core `handleMultiTsConfig` ->
single `finalize`; the shipped dist tarball carries all four manifests + 25 compiled `.js` (0 raw
`.ts`). The ACV-01 gap-fix is contained to the write-fork internals and leaves all wiring surfaces
green (330/330 unit guards).

## E2E Flows (3/3 complete)

1. `ng add` auto-wire-all -> per-project `ng run :typecheck` scoping -- committed CI-authoritative
   ACV-02 (`angular-typechecker-ng-cli-e2e`). COMPLETE.
2. ACV-01 real-clone final gate (ngx-leaflet npm app+lib; realworld-angular pnpm app-only): pack
   SHIPPED tarball -> `ng add` -> plant -> `ng run` -> assert. COMPLETE (2/2 pass; the
   pnpm-workspace name-collision silent-under-check defect caught, fixed, re-verified).
3. Builder-over-`BuilderContext` == Nx executor `{ success }` parity (ACV-03). COMPLETE.

## Nyquist Compliance

All 4 phases `nyquist_compliant: true`. Phase 24's VALIDATION.md was re-audited after the ACV-01
fix and gained an `ng add`-composition coverage guard (previously only `configurationGenerator` was
tested directly).

## Tech Debt / Deferred

None blocking. Deferred items remain tracked as Future Requirements in STATE.md (Storybook SB-08,
FsTree helpers, WALK-FUT, REP-RES-02b, OBS-01, standalone CLI, JSON/SARIF reporters) -- out of
scope for v0.2.1 by charter, not debt introduced by this milestone.

## Notable milestone finding

The ACV-01 real-clone gate did its job: it caught a charter-level silent-under-check defect (the
Angular CLI `configuration`/`ng-add` generator dropped the app build leaf on an Angular-CLI +
pnpm-workspace + name-collision workspace) that the clean scaffolded e2e (ACV-02) structurally
could not reproduce. Fixed within the unreleased Angular CLI surface (additive-only preserved),
regression-locked (CLI matrix + Nx-collision lock + ng-add composition), and independently re-audited
by all four dedicated post-execution agents. See phase-24 24-HUMAN-UAT.md / 24-REVIEW-ACV01FIX.md.
