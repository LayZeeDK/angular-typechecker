---
phase: 35-automated-code-scanning-proof
plan: 04
type: execute
gap_closure: true
gap_id: G-35-01
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/core/sarif-report.ts
  - packages/angular-typechecker/src/core/sarif-report.spec.ts
  - packages/angular-typechecker/src/core/machine-reporters-sarif.integration.spec.ts
  - packages/angular-typechecker/src/core/__snapshots__/sarif-report.spec.ts.snap
  - packages/angular-typechecker/src/core/__snapshots__/machine-reporters-sarif.integration.spec.ts.snap
  - packages/angular-typechecker/README.md
autonomous: true
requirements: [PROOF-01, PROOF-02]

must_haves:
  truths:
    - "Every file-less SARIF result (record.file === null) carries exactly one location whose physicalLocation.artifactLocation.uri === relativizePath(result.tsConfigPath, pathBase), with NO region (D1, D5, D6)."
    - "results.length === result.diagnostics.length still holds: the file-less case is now LOCATED, never dropped and never omitted (D1 never-drop preserved)."
    - "The change is confined to sarif-report.ts PASS-2; json-report.ts, DiagnosticRecord, and the JSON snapshot are byte-unchanged -- a JSON consumer still sees file: null (D2)."
    - "synthesizeFilelessError still emits file: undefined diagnostics (the never-suppress boundary shape is untouched); the fallback lives only in the reporter, out-of-band (D3)."
    - "fingerprintOf still hashes the TRUE file-less record (empty-string sentinels); atcFingerprint/v1 alert identity does not churn -- the synthetic location never feeds the fingerprint (D4)."
    - "No version bump and no release cut: this is the same unreleased 0.2.4 SARIF work; packages/angular-typechecker/package.json version is unchanged (D7)."
    - "src/index.ts barrel + src/index.drift.ts are byte-unchanged; NO new public symbol is added (additive-only holds)."
    - "All local gates green: nx test, nx integration (incl. the phase-35 sarif-proof-fixture drift-lock), nx typecheck (3 tsc incl. tsconfig.spec.json), nx run-many -t lint (maxWarnings:0), nx format:check, npm run fallow, npm run cve-lite, nx build."
    - "PROOF-01 / PROOF-02 close in REAL CI (the Nyquist point): the code-scanning-proof job on the milestone PR ingests the SARIF with no locationFromSarifResult rejection, and the assert step exits 0 with all four (family tag, severity) tuples under the angular-typecheck-proof category. Verified in CI after push, NOT a local code task."
  artifacts:
    - "packages/angular-typechecker/src/core/sarif-report.ts -- PASS-2 file-less branch supplies the fileUri fallback; relativizePath imported; both D-01 comments updated."
    - "packages/angular-typechecker/src/core/sarif-report.spec.ts -- the file-less test asserts a located, region-less result on the relativized tsconfig."
    - "packages/angular-typechecker/src/core/machine-reporters-sarif.integration.spec.ts -- the global-diagnostics and solution-style-all-missing 'NO locations' assertions are flipped."
    - "packages/angular-typechecker/src/core/__snapshots__/sarif-report.spec.ts.snap -- regenerated."
    - "packages/angular-typechecker/src/core/__snapshots__/machine-reporters-sarif.integration.spec.ts.snap -- regenerated."
    - "packages/angular-typechecker/README.md -- the SARIF file-less-diagnostic paragraph (~lines 710-713) is corrected to describe the new whole-file fallback-location behavior (ships with the npm package; end-user-facing)."
  key_links:
    - "sarif-report.ts PASS-2 file-less else-branch -> relativizePath(result.tsConfigPath, pathBase) -> node-sarif-builder initSimple({ fileUri }) -> a region-less physicalLocation.artifactLocation.uri. This is the ingestion-critical wiring."
    - "SARIF payload -> GitHub Code Scanning upload-sarif ingestion: every result now carries >= 1 location, so locationFromSarifResult no longer rejects the whole file (real-CI proof)."
---

<objective>
Fix gap G-35-01: the SARIF reporter emits file-less (tool / project-level) diagnostics
with NO `locations`, and GitHub Code Scanning rejects the WHOLE SARIF
(`locationFromSarifResult: expected at least one location`), so the `code-scanning-proof`
job never ingests and its assert step is skipped. This REVERSES the locked D-01
"file-less record becomes a no-location result" in the SARIF EMISSION only: attach a
region-less whole-file fallback location -- `artifactLocation.uri` = the relativized,
always-present `result.tsConfigPath` -- to every file-less result, so the SARIF is
ingestible while every diagnostic is still NEVER dropped.

Purpose: unblock PROOF-01 / PROOF-02. Phase 35's fixture, assert script, and CI job are
correct; they did their job by surfacing this on the first real CI run (PR #55). This is
the one production change (`sarif-report.ts`) needed for the proof to actually run.

Output: the SARIF file-less branch supplies a fallback location; the two specs assert the
new located shape; both SARIF snapshots regenerate. No version bump, no release cut,
no new public symbol.

Decision & requirement coverage: D1 + D5 + D6 (fallback URI = relativized tsConfigPath,
all file-less results, region-less) -> Task 1; D2 + D3 + D4 + D7 (SARIF-only, record /
fingerprint / version untouched) -> held across Task 1 and asserted in Tasks 2-3;
PROOF-01 / PROOF-02 -> the local specs (Tasks 2-3) plus the real-CI code-scanning-proof
Nyquist must_have. The full locked contract is in 35-G-35-01-CONTEXT.md (it wins on any
discrepancy).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-automated-code-scanning-proof/35-G-35-01-CONTEXT.md
@.planning/phases/35-automated-code-scanning-proof/35-UAT.md
@packages/angular-typechecker/src/core/sarif-report.ts
@packages/angular-typechecker/src/core/diagnostic-record.ts

Verified behavior of the SARIF builder (node_modules/node-sarif-builder/dist/lib/sarif-result-builder.js):
`initSimple` calls `setLocationArtifactUri` when `options.fileUri` is truthy, and adds a
`region` ONLY when `options.startLine` is neither null nor undefined. So passing
`{ fileUri }` ALONE yields a location with `physicalLocation.artifactLocation.uri` and NO
region -- exactly the region-less whole-file shape D6 requires.
</context>

<artifacts_this_phase_produces>
This gap adds NO new public symbol. The barrel (`src/index.ts`) and its drift tripwire
(`src/index.drift.ts`) stay byte-unchanged. The only new thing is an INTERNAL behavior in
`sarif-report.ts`: a file-less result now carries a whole-file fallback location on the
relativized tsconfig, instead of emitting no `locations` key. The JSON reporter,
`DiagnosticRecord`, the fingerprint, the assert script, and the proof fixture are all
unchanged, and the package version stays at the already-derived unreleased 0.2.4.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Attach a region-less tsconfig fallback location to file-less SARIF results (D1/D5/D6)</name>
  <files>packages/angular-typechecker/src/core/sarif-report.ts</files>
  <read_first>
    - .planning/phases/35-automated-code-scanning-proof/35-G-35-01-CONTEXT.md (the locked D1-D7 contract)
    - packages/angular-typechecker/src/core/sarif-report.ts (PASS-2 loop ~lines 196-219; the file-less else-branch `: {}` at ~line 211; D-01 comments at ~line 28 header + ~line 202 inline)
    - packages/angular-typechecker/src/core/diagnostic-record.ts (relativizePath is already exported ~line 149; repo-relative forward-slash, cross-OS-stable)
  </read_first>
  <action>
    In packages/angular-typechecker/src/core/sarif-report.ts:

    1. Add `relativizePath` to the existing named import from `./diagnostic-record`
       (currently `{ toDiagnosticRecord, toolVersion, type DiagnosticRecord }`). Import it
       as a value (it is a runtime function).

    2. In PASS 2 (the `for (const diagnostic of result.diagnostics)` loop that builds one
       `SarifResultBuilder` per diagnostic), change ONLY the file-less arm of the
       conditional spread. Today the `record.file !== null` ternary supplies
       `fileUri`/`startLine`/`startColumn`/`endLine`/`endColumn` in the true arm and `{}`
       in the false arm. Replace the false arm `{}` with
       `{ fileUri: relativizePath(result.tsConfigPath, pathBase) }` -- fileUri ALONE, NO
       startLine/startColumn/endLine/endColumn. `result.tsConfigPath` is
       `CoreResult.tsConfigPath` (always present) and `pathBase` is this function's existing
       parameter. Because no startLine is passed, node-sarif-builder emits a location with
       `artifactLocation.uri` and no region (D6 region-less). This applies to ALL file-less
       results (record.file === null), not just the ATC codes (D5).

    3. Leave the `record.file !== null` located arm byte-unchanged. Do NOT touch PASS 1
       (the on-demand rule catalog), `fingerprintOf` (D4 -- the record is unchanged, so the
       synthetic location never feeds the fingerprint), or `toDiagnosticRecord` usage. Do NOT
       touch diagnostic-record.ts / json-report.ts / diagnostic-codes.ts (D2/D3). Do NOT bump
       the package version (D7).

    4. Update the two D-01 doc-comments to describe the new behavior: the module-header
       comment (~line 28, the "file-less record becomes a no-location result" clause) and the
       inline `// D-01:` comment above the ternary (~line 202). Both should now state that a
       file-less record carries a whole-file fallback location on the relativized
       `tsConfigPath` (its always-present owner) and is STILL never dropped, so GitHub Code
       Scanning ingests it. Reference D1/D5/D6 and note this reverses the old no-location
       emission for ingestion.

    Follow the repo TS style (braces on all control-flow bodies, blank lines around control
    flow / return). This is a value-change to a spread arm plus an import and comments; no
    new control flow is expected.
  </action>
  <verify>
    <automated>npx nx build angular-typechecker</automated>
    <automated>npx nx typecheck angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - The `./diagnostic-record` import line includes `relativizePath` (value import).
    - PASS 2's file-less arm supplies `fileUri: relativizePath(result.tsConfigPath, pathBase)`
      and NOTHING else (no startLine / region).
    - The `record.file !== null` located arm, PASS 1, and `fingerprintOf` are byte-unchanged.
    - Both D-01 comments describe the fallback location (not a no-location result).
    - `nx build angular-typechecker` and `nx typecheck angular-typechecker` (all 3 tsc:
      tsconfig.spec.json + tsconfig.drift.json + tsconfig.tools.json) are green.
    - No other file changed; packages/angular-typechecker/package.json version unchanged.
    - EXPECTED at this point: `nx test` and `nx integration` are RED (they still assert the
      old no-location behavior). Tasks 2 and 3 flip them -- this is normal intra-plan
      sequencing, not a regression.
  </acceptance_criteria>
  <done>File-less SARIF results carry a region-less whole-file location on the relativized tsConfigPath; prod compiles and type-checks; the record, fingerprint, JSON reporter, barrel, and version are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the unit spec file-less assertion to the located shape + regenerate its snapshot</name>
  <files>packages/angular-typechecker/src/core/sarif-report.spec.ts, packages/angular-typechecker/src/core/__snapshots__/sarif-report.spec.ts.snap</files>
  <read_first>
    - .planning/phases/35-automated-code-scanning-proof/35-G-35-01-CONTEXT.md (D1/D5/D6)
    - packages/angular-typechecker/src/core/sarif-report.spec.ts (the `filelessDiag` fixture ~line 76; its comment ~line 75; the fixture `coreResult()` tsConfigPath 'D:/ws/proj/libs/x/tsconfig.lib.json' ~line 162; `sarifOf` pathBase 'D:/ws/proj' ~line 216; the target test ~lines 361-374; the `fileSetPositionAbsentDiag` located-no-region test ~lines 376-399 as the access-pattern template; the snapshot test ~lines 508-531)
  </read_first>
  <action>
    In packages/angular-typechecker/src/core/sarif-report.spec.ts:

    1. In the test currently titled
       "never drops a file-less diagnostic -- emits it as a no-location result, length
       one-to-one (D-01)" (~lines 361-374): rename the title to describe the new behavior
       (e.g. "never drops a file-less diagnostic -- emits it as a whole-file located result on
       the tsconfig with no region, length one-to-one (D1/D5/D6)"). Keep
       `expect(log.runs[0].results).toHaveLength(result.diagnostics.length)` and
       `expect(fileless.ruleId).toBe('ATC90001')`. REPLACE `expect('locations' in fileless).toBe(false)`
       with assertions that the file-less result is now LOCATED and region-less: read
       `const physical = fileless.locations?.[0].physicalLocation;` (mirror the existing
       `fileSetPositionAbsentDiag` test's access pattern), then
       `expect(physical?.artifactLocation.uri).toBe('libs/x/tsconfig.lib.json')` (the fixture
       tsConfigPath 'D:/ws/proj/libs/x/tsconfig.lib.json' relativized against the sarifOf
       pathBase 'D:/ws/proj') and `expect(physical?.region).toBeUndefined()`.

    2. Update the `filelessDiag` fixture comment (~line 75) so it no longer says the shape
       yields a no-location result -- it now yields a whole-file located result on the
       tsconfig fallback (the diagnostic is still file: undefined by construction; the
       fallback is added by the reporter).

    3. Do NOT change any other test (the `fileSetPositionAbsentDiag` located-no-region test,
       the fingerprint / severity / clean-result tests, the D-04 any-.html-wins test). Do NOT
       change the `SarifPhysicalLocation`/`SarifResult` local interfaces.

    4. Regenerate the unit snapshot: run `npx nx test angular-typechecker -u` (the full-shape
       snapshot test uses `[positionedDiag(), filelessDiag()]`, so the file-less result's new
       location appears in `__snapshots__/sarif-report.spec.ts.snap`). Then run
       `npx nx test angular-typechecker` WITHOUT `-u` to confirm the committed snapshot matches.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -u</automated>
    <automated>npx nx test angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - The flipped test asserts `physical?.artifactLocation.uri === 'libs/x/tsconfig.lib.json'`
      AND `physical?.region` is undefined.
    - The `results.length === result.diagnostics.length` and `ruleId === 'ATC90001'`
      assertions are retained.
    - `__snapshots__/sarif-report.spec.ts.snap` is regenerated and committed.
    - A clean (`-u`-free) `nx test angular-typechecker` run passes.
    - No production file is touched in this task.
  </acceptance_criteria>
  <done>The unit spec proves the file-less SARIF result is located (repo-relative tsconfig uri, no region) and one-to-one; the unit snapshot matches on a clean run.</done>
</task>

<task type="auto">
  <name>Task 3: Flip the two integration "NO locations" assertions + regenerate its snapshot</name>
  <files>packages/angular-typechecker/src/core/machine-reporters-sarif.integration.spec.ts, packages/angular-typechecker/src/core/__snapshots__/machine-reporters-sarif.integration.spec.ts.snap</files>
  <read_first>
    - .planning/phases/35-automated-code-scanning-proof/35-G-35-01-CONTEXT.md (D1/D5/D6)
    - packages/angular-typechecker/src/core/machine-reporters-sarif.integration.spec.ts (module header ~lines 22-30; the `artifactUris` helper ~lines 173-190; global-diagnostics "NO locations" test ~lines 327-333; solution-style-all-missing test ~lines 431-441 with the `locations?.length ?? 0).toBe(0)` at ~line 439; the sarif-proof-fixture drift-lock describe ~lines 463-517, which must stay green and untouched)
  </read_first>
  <action>
    In packages/angular-typechecker/src/core/machine-reporters-sarif.integration.spec.ts.
    Both fixtures run with cwd = workspaceRoot (the beforeAll `process.chdir`), so a file-less
    result's fallback uri is its tsconfig relativized to the repo root:
    `fixtures/global-diagnostics/tsconfig.json` and
    `fixtures/solution-style-all-missing/tsconfig.json`. Assert the uri by
    `endsWith(...)` + no-backslash/colon (robust to the exact pathBase prefix) rather than
    pinning a full absolute-derived prefix.

    1. global-diagnostics describe, the test "keeps at least one file-less result with NO
       locations (never dropped)" (~lines 327-333): rename it (e.g. "gives every file-less
       result exactly one whole-file location on the tsconfig, never dropped"). Replace the
       `.some((result) => (result.locations?.length ?? 0) === 0)).toBe(true)` with: every one
       of the 10 results now carries exactly one location (results still length 10, one-to-one),
       and each `physicalLocation.artifactLocation.uri` ends with
       `'global-diagnostics/tsconfig.json'` and matches `not.toMatch(/[\\:]/)` (repo-relative,
       forward-slash). The existing `artifactUris(payload)` helper is a convenient way to
       collect every uri.

    2. solution-style-all-missing describe, the test "resolves both file-less results to the
       single tool rule by ruleIndex 0 (RULE-01)" (~lines 431-441): change
       `expect(result.locations?.length ?? 0).toBe(0)` to `.toBe(1)` and add that each
       location's `physicalLocation.artifactLocation.uri` ends with
       `'solution-style-all-missing/tsconfig.json'` and `not.toMatch(/[\\:]/)`. KEEP the
       `result.ruleId === 'ATC90002'` and `result.ruleIndex === 0` assertions and the
       `results.toHaveLength(2)`.

    3. Update the module-header comment (~lines 22-30) where it describes global-diagnostics as
       "the SARIF no-`locations` result that is NEVER dropped" and solution-style-all-missing as
       "two file-less ATC90002 not-found errors": both now carry a whole-file fallback location
       on the relativized tsconfig (still never dropped).

    4. Do NOT touch the sarif-proof-fixture drift-lock describe (it asserts rule (family, level)
       tuples, not locations -- it must stay green), the proof fixture, or the assert script.

    5. Regenerate the integration snapshot: run `npx nx integration angular-typechecker -u`
       (the global-diagnostics redacted snapshot now shows each result's location;
       layout-b-host is file-attributed and should be unchanged). Then run
       `npx nx integration angular-typechecker` WITHOUT `-u` to confirm the committed snapshot
       matches AND the sarif-proof-fixture drift-lock is still green.
  </action>
  <verify>
    <automated>npx nx integration angular-typechecker -u</automated>
    <automated>npx nx integration angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - global-diagnostics: every result has exactly one location, uri ends with
      'global-diagnostics/tsconfig.json', no `[\\:]`; results length still 10.
    - solution-style-all-missing: both results have `locations.length === 1` with uri ending
      'solution-style-all-missing/tsconfig.json'; ruleId/ruleIndex/length(2) assertions retained.
    - `__snapshots__/machine-reporters-sarif.integration.spec.ts.snap` regenerated and committed.
    - A clean (`-u`-free) `nx integration angular-typechecker` run passes, INCLUDING the
      sarif-proof-fixture drift-lock describe.
    - No production / fixture / assert-script file is touched in this task.
  </acceptance_criteria>
  <done>The integration tier proves file-less results (global TS2318 and tool ATC90002) each carry a repo-relative whole-file tsconfig location over the real cold compiler; the integration snapshot and the phase-35 proof drift-lock are green on a clean run.</done>
</task>

<task type="auto">
  <name>Task 4: Correct the shipped README SARIF file-less-diagnostic paragraph (docs consistency)</name>
  <files>packages/angular-typechecker/README.md</files>
  <read_first>
    - .planning/phases/35-automated-code-scanning-proof/35-G-35-01-CONTEXT.md (D1/D5/D6 -- the new behavior the prose must match)
    - packages/angular-typechecker/README.md (the SARIF file-less paragraph at ~lines 710-713, inside the machine-readable-output / upload-sarif section; and the "#### Run from the repository root" note immediately after at ~lines 715-721, which stays accurate and gives the artifactLocation.uri context)
  </read_first>
  <action>
    In packages/angular-typechecker/README.md, replace the now-stale file-less paragraph
    (~lines 710-713) that currently reads: "A file-less diagnostic (a whole-program error with
    no source location) is emitted as a result with no location rather than being dropped.
    GitHub cannot pin a no-location result to a line, so treat the run's exit code / `success`,
    not the SARIF alert, as the authoritative fail signal for those."

    Replace it with end-user-facing prose describing the new behavior (per CLAUDE.md
    [[changelog-readme-end-user-facing]] -- consumer language, no internal D-ids / gap-ids /
    phase numbers): a file-less diagnostic (a whole-program error with no source location, e.g.
    a missing global type or a references-only/missing-reference config error) is anchored to
    the tsconfig being checked and emitted as a whole-file alert (the whole file, no specific
    line) rather than being dropped -- so GitHub Code Scanning ingests it and surfaces it as an
    alert against that tsconfig. Drop the old "treat the exit code, not the SARIF alert, as the
    authoritative fail signal" caveat, since file-less diagnostics now DO appear as alerts.
    Keep the exit-code/`success` semantics documented elsewhere untouched; this paragraph is
    only about how file-less diagnostics surface in the SARIF.

    Do NOT touch the "#### Run from the repository root" note that follows (~lines 715-721) --
    it remains accurate (the tsconfig fallback uri is relativized the same way, so running from
    the repo root keeps it repo-relative). Docs-only; no code, no version bump.
  </action>
  <verify>
    <automated>npx nx format:check</automated>
  </verify>
  <acceptance_criteria>
    - README.md no longer contains the phrase "emitted as a result with no location" or "treat
      the run's exit code / `success`, not the SARIF alert, as the authoritative fail signal".
    - README.md's file-less paragraph now states file-less diagnostics are anchored to the
      tsconfig / surface as a whole-file Code Scanning alert (still never dropped).
    - The prose uses no internal ids (no `D-01`, `G-35-01`, `PROOF-0x`, phase numbers).
    - The "#### Run from the repository root" note is byte-unchanged.
    - `npx nx format:check` is green (Prettier accepts the edited README).
    - No code / snapshot / spec file is touched in this task.
  </acceptance_criteria>
  <done>The shipped README's SARIF file-less-diagnostic guidance matches the new whole-file fallback-location behavior; no consumer is told to ignore a file-less SARIF alert that now exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reporter output -> GitHub Code Scanning | The SARIF payload crosses into GitHub's `upload-sarif` ingestion; a malformed/rejected payload blocks the proof. |
| repo filesystem path -> emitted SARIF | `result.tsConfigPath` (a filesystem path) is rendered into `artifactLocation.uri`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-04-01 | Information Disclosure | sarif-report.ts PASS-2 fallback location (artifactLocation.uri = tsConfigPath) | low | mitigate | `relativizePath(result.tsConfigPath, pathBase)` makes the uri repo-relative + forward-slash + cross-OS-stable -- the SAME treatment already applied to `diagnostics[].file`. The only residual (a cross-drive tsconfig yielding an absolute Windows path) is the pre-existing ASVS-L1-ACCEPTED IN-03 documented in diagnostic-record.ts + 30-SECURITY.md, unchanged by this fix. |
| T-35-04-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan (SARIF-only source + spec + snapshot edits); no dependency added or upgraded, so the package-legitimacy gate does not apply. |
</threat_model>

<verification>
Run the full local battery from the repo root after Task 3 (per 35-G-35-01-CONTEXT.md
"Verification gates"; STATE note: `nx test` EXCLUDES `*.integration.spec.ts` and does NOT
type-check specs, so `nx integration` and `nx typecheck` are BOTH required):

1. `npx nx test angular-typechecker` -- unit tier green, incl. the regenerated
   `sarif-report.spec.ts.snap`.
2. `npx nx integration angular-typechecker` -- integration tier green, incl. the regenerated
   `machine-reporters-sarif.integration.spec.ts.snap` AND the sarif-proof-fixture drift-lock.
3. `npx nx typecheck angular-typechecker` -- the 3 tsc typechecks (tsconfig.spec.json +
   tsconfig.drift.json + tsconfig.tools.json). This is what catches a spec type error
   (`nx test` does not type-check specs).
4. `npx nx run-many -t lint` -- ESLint at maxWarnings:0.
5. `npx nx format:check` -- Prettier gate (the repo-level target).
6. `npm run fallow` -- structural quality gate (the earlier complexity blocker was cleared
   by quick 260721-vm1; expected green now).
7. `npm run cve-lite` -- dependency CVE gate `--fail-on high` (svgo HIGH cleared by quick
   260721-wda; no deps changed here).
8. `npx nx build angular-typechecker` -- production build (the `await import()` ESM bridge
   intact).

Additive-only spot check: `git diff` shows ONLY the six files in `files_modified` (the five
code/spec/snapshot files + `README.md`); the barrel `src/index.ts`, `src/index.drift.ts`,
`json-report.ts`, `diagnostic-record.ts`, and `packages/angular-typechecker/package.json`
are byte-unchanged. The README edit is prose-only (ships with the package, no version bump).

REAL-CI Nyquist point (PROOF-01 / PROOF-02, authoritative, after push): on the milestone PR,
the `code-scanning-proof` job's `upload-sarif` must succeed (no `locationFromSarifResult:
expected at least one location` rejection) and the "Assert proof alerts landed" step must
exit 0 with all four (family tag, severity) tuples under the `angular-typecheck-proof`
category. This is verified in CI, not locally.
</verification>

<success_criteria>
- File-less SARIF results carry a region-less whole-file location on the relativized
  `tsConfigPath`; `results.length === diagnostics.length` (never-drop) preserved.
- JSON reporter, `DiagnosticRecord`, `fingerprintOf`, the barrel + `index.drift.ts`, and the
  package version are byte-unchanged (additive-only holds; no release cut).
- Both SARIF snapshots regenerated; unit + integration tiers green on a clean (`-u`-free) run,
  incl. the sarif-proof-fixture drift-lock.
- All 8 local gates green (test, integration, typecheck, lint, format:check, fallow,
  cve-lite, build).
- The shipped README's SARIF file-less-diagnostic paragraph matches the new behavior (no
  consumer is told to ignore a file-less SARIF alert that now exists).
- In real CI, the `code-scanning-proof` job ingests the SARIF and the assert step exits 0
  (PROOF-01 / PROOF-02 close at phase verification).
</success_criteria>

<output>
Create `.planning/phases/35-automated-code-scanning-proof/35-04-SUMMARY.md` when done.
</output>
