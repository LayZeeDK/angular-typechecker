---
phase: 11-fallow-code-quality-ci-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .fallowrc.jsonc
  - package.json
  - package-lock.json
autonomous: true
requirements: [QUAL-02, QUAL-03]
worktree_provisioning: pattern-b   # CHANGES root package.json/package-lock.json -> per-worktree `npm ci`, NOT the shared node_modules junction (Pattern A is INVALID when deps change; see AGENTS.md "Parallel execution in git worktrees")
must_haves:
  truths:
    - "Covers CONTEXT.md decisions D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-11 (full rationale in 11-CONTEXT.md; D-01/02 new-only gate pinned in config, D-03/04 hand-authored .fallowrc.jsonc keys, D-05 root scope, D-06 unused-dev-dependencies off, D-07 unused-dependencies error + @angular/forms removed, D-08 no IN-05 suppression, D-09 duplicates+health defaults, D-11 fallow exact-pinned devDep)"
    - "`npx fallow audit --format json --base origin/main` exits 0 (verdict pass) on the resolved tree"
    - "The post-resolution tree has 0 dead-code findings (`@angular/forms` removed, false positives suppressed in config)"
    - "`fallow` is an EXACT-pinned (no `^`/`~`) root devDependency at version 2.103.0"
    - "`@angular/forms` is removed from root `dependencies` (verified zero usage repo-wide)"
  artifacts:
    - path: ".fallowrc.jsonc"
      provides: "fallow config resolving all current findings (entry/ignoreExports/overrides/rules/audit), one JSONC comment per declaration"
      contains: "compiler-cli-types.drift.ts"
    - path: "package.json"
      provides: "exact-pinned fallow@2.103.0 devDep; @angular/forms removed from dependencies"
      contains: "\"fallow\": \"2.103.0\""
    - path: "package-lock.json"
      provides: "regenerated lockfile with fallow@2.103.0 and without @angular/forms"
  key_links:
    - from: ".fallowrc.jsonc"
      to: "packages/angular-typechecker/src/core/compiler-cli-types.drift.ts"
      via: "entry array declares the tsconfig-files-only drift tripwire reachable"
      pattern: "compiler-cli-types\\.drift\\.ts"
    - from: ".fallowrc.jsonc"
      to: "fixtures/fault-isolation/"
      via: "overrides glob fixtures/fault-isolation/** scopes off the intentional fixture findings"
      pattern: "fixtures/fault-isolation/\\*\\*"
---

<objective>
Author the hand-written `.fallowrc.jsonc` that RESOLVES (not baselines) every current `fallow@2.103.0` finding on this repo, pin `fallow@2.103.0` as an exact root devDependency, and REMOVE the genuinely-unused `@angular/forms` prod dependency — so `npx fallow audit --format json --base origin/main` exits 0 on a fully clean (0-finding) tree.

Purpose: This is the foundation of the QUAL gate. The CI job (Plan 02) is only meaningful once the gate is provably green; this plan makes it green and installs the analyzer the CI job runs.
Output: `.fallowrc.jsonc` (new, repo root), `package.json` (fallow pinned + `@angular/forms` removed), regenerated `package-lock.json`.

Covers QUAL-02 (config resolves findings, audit exits 0, `@angular/forms` removed) and the `fallow`-devDependency half of QUAL-03 (exact pin, run via `npx fallow` after `npm ci`, never `@latest`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Source of truth for the .fallowrc.jsonc content (run-verified, verdict pass / exit 0):
@.planning/phases/11-fallow-code-quality-ci-gate/11-RESEARCH.md
# Decisions D-01..D-09 (config) + D-11 (exact-pin); the CORRECTED repo-root fixture glob:
@.planning/phases/11-fallow-code-quality-ci-gate/11-CONTEXT.md
# Pattern map: package.json exact-pin convention + @angular/forms removal target line:
@.planning/phases/11-fallow-code-quality-ci-gate/11-PATTERNS.md

# Files the config must declare/scope (read to confirm the target line refs before authoring):
@packages/angular-typechecker/src/core/compiler-cli-types.ts
@packages/angular-typechecker/tsconfig.drift.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author .fallowrc.jsonc resolving all current fallow findings</name>
  <files>.fallowrc.jsonc</files>
  <read_first>
    - .planning/phases/11-fallow-code-quality-ci-gate/11-RESEARCH.md SS "The verified `.fallowrc.jsonc` (repo root)" (lines 81-146) — the EXACT run-verified content (verdict pass / exit 0). Use it verbatim.
    - .planning/phases/11-fallow-code-quality-ci-gate/11-CONTEXT.md D-01..D-09 + D-04's CORRECTED fixture glob note.
    - .planning/phases/11-fallow-code-quality-ci-gate/11-PATTERNS.md "Verified target line references" table + "LANDMINE — the fixture glob path".
    - packages/angular-typechecker/src/core/compiler-cli-types.ts (confirm `EmitFlags` enum + `UNKNOWN_ERROR_CODE` export exist).
    - packages/angular-typechecker/tsconfig.drift.json (confirm `files: ["src/core/compiler-cli-types.drift.ts"]` — WHY the drift file is import-graph-invisible).
  </read_first>
  <action>
    Create `.fallowrc.jsonc` at the REPO ROOT using the run-verified content from 11-RESEARCH.md SS "The verified `.fallowrc.jsonc`" (lines 81-146) verbatim. It MUST contain exactly these declarations, each carrying its documented JSONC comment naming the finding it resolves:
    - `$schema` pointing at fallow's published schema URL (per 11-RESEARCH.md).
    - `entry: ["packages/angular-typechecker/src/core/compiler-cli-types.drift.ts"]` — declares the tsconfig-`files`-only drift tripwire reachable (resolves IN-02 `unused_file`). Do NOT re-list real Angular entry points: fallow auto-detects the Angular plugin and loads 60 entry points (D / open-question-1 answered in 11-RESEARCH.md).
    - `ignoreExports: [{ file: "**/compiler-cli-types.ts", exports: ["UNKNOWN_ERROR_CODE"] }]` — pins the value-mirrored type export (resolves IN-04 `unused_types`; suppressed structurally per D-02, not relying on `introduced:false`).
    - `rules: { "unused-dev-dependencies": "off" }` (D-06 — the 14 structurally-untraceable tooling deps). Leave `unused-dependencies` at its default `error` (D-07) — do NOT set it; absence == default.
    - `overrides`: (a) `{ files: ["**/compiler-cli-types.ts"], rules: { "unused-enum-members": "off" } }` scoping off the `EmitFlags` contract-mirror shim members (resolves IN-03); (b) `{ files: ["fixtures/fault-isolation/**"], rules: { "unrendered-components": "off", "unused-component-inputs": "off" } }` scoping off the intentional fixtures.
    - `audit: { "gate": "new-only" }` (D-01/D-02 — config-pinned deterministic gate).
    CRITICAL — the fixture glob is the REPO-ROOT form `fixtures/fault-isolation/**`, NOT `packages/angular-typechecker/fixtures/fault-isolation/**` (the wrong path in 11-CONTEXT.md D-04; CORRECTED in 11-RESEARCH.md/11-PATTERNS.md). The package-prefixed glob matches nothing and the gate stays red on 4 introduced fixture findings.
    Do NOT add empty `duplicates`/`health` blocks (D-09 — absence == defaults; the repo is clean on both, so defaults are zero-cost regression tripwires for all three families). Do NOT add any IN-05 / `publishConfig` suppression (D-08 — it does not reproduce in 2.103.0; fallow analyzes the root `package.json`, not the published manifest).
    Note: this file alone makes the audit `pass`/exit 0, but leaves `@angular/forms` as an inherited non-gating finding with `has_errors:true`; Task 3 removes it for a fully clean 0-finding tree (the "resolve, not baseline" intent).
  </action>
  <verify>
    <automated>git grep -n "compiler-cli-types.drift.ts" .fallowrc.jsonc && git grep -n "fixtures/fault-isolation/\*\*" .fallowrc.jsonc && git grep -n "new-only" .fallowrc.jsonc</automated>
  </verify>
  <acceptance_criteria>
    - `.fallowrc.jsonc` exists at repo root.
    - `git grep -n "compiler-cli-types.drift.ts" .fallowrc.jsonc` matches (entry declaration present).
    - `git grep -n "fixtures/fault-isolation/\*\*" .fallowrc.jsonc` matches the REPO-ROOT glob (no `packages/angular-typechecker/` prefix).
    - `git grep -n "UNKNOWN_ERROR_CODE" .fallowrc.jsonc` matches (ignoreExports present).
    - `git grep -n "unused-dev-dependencies" .fallowrc.jsonc` matches `off`; `git grep -n "unused-enum-members" .fallowrc.jsonc` matches `off`.
    - `git grep -n '"gate"' .fallowrc.jsonc` shows `new-only`.
    - NO `duplicates`/`health` blocks present; NO `publishConfig`/IN-05 suppression present.
  </acceptance_criteria>
  <done>`.fallowrc.jsonc` is authored at repo root with all six declarations and the CORRECTED repo-root fixture glob, each with its JSONC comment; no empty defaults blocks; no IN-05 suppression.</done>
</task>

<task type="auto">
  <name>Task 2: Pin fallow@2.103.0 as an exact root devDependency</name>
  <files>package.json</files>
  <read_first>
    - package.json (the existing `devDependencies` block — confirm the EXACT-pin convention used by the locked Nx/Angular deps, e.g. `"@nx/devkit": "23.0.1"`, no `^`/`~`).
    - .planning/phases/11-fallow-code-quality-ci-gate/11-PATTERNS.md SS "root `package.json` — pin `fallow`" (exact-pin convention; alphabetical placement guidance).
    - .planning/phases/11-fallow-code-quality-ci-gate/11-RESEARCH.md SS "Package Legitimacy Audit" (fallow@2.103.0 slopcheck [OK], no postinstall) + D-11.
  </read_first>
  <action>
    Add `"fallow": "2.103.0"` to the root `package.json` `devDependencies` block as an EXACT pin (NO `^`/`~`), matching the locked-dependency convention of the existing entries (per D-11, 11-PATTERNS.md). Place it in alphabetical order within the block (after `eslint-config-prettier`, before `jsdom`). Do NOT use a caret/tilde range — a fallow minor release changing a default rule must not silently flip the gate (11-RESEARCH.md Pitfall 4). `fallow@2.103.0` cleared slopcheck `[OK]` (404k weekly downloads, source repo `github.com/fallow-rs/fallow`, no `postinstall` script) per the Package Legitimacy Audit — no install-time gate needed.
    Do NOT run `npm install` in this task; the lockfile regeneration happens in Task 3 together with the `@angular/forms` removal so a single `npm install` produces a consistent lockfile.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const v=p.devDependencies.fallow; if(v!=='2.103.0'){console.error('expected 2.103.0, got',v);process.exit(1)} if(/[\^~]/.test(v)){console.error('range not allowed');process.exit(1)} console.log('OK',v)"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n '"fallow"' package.json` shows exactly `"fallow": "2.103.0"` inside `devDependencies` (no `^`/`~`).
    - The node assertion above prints `OK 2.103.0` and exits 0.
    - `fallow` does NOT appear in any `dependencies`/`peerDependencies` block, and is NOT added to `packages/angular-typechecker/package.json` (root devDep only — keeps the published package surface unchanged).
  </acceptance_criteria>
  <done>`fallow@2.103.0` is present as an exact-pinned root devDependency in alphabetical position; no version range; not in the published package manifest.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the unused @angular/forms prod dependency, regenerate the lockfile, and prove the gate is clean</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (the `dependencies` block — `@angular/forms` is at the `"@angular/forms": "22.0.4"` line).
    - .planning/phases/11-fallow-code-quality-ci-gate/11-RESEARCH.md SS "`@angular/forms`: REMOVE (verify-then-resolve, D-07)" (lines 163-167) + "Resolving the findings: live evidence" table (Config + forms removed -> 0 findings).
    - .planning/phases/11-fallow-code-quality-ci-gate/11-CONTEXT.md D-07.
  </read_first>
  <action>
    First RE-CONFIRM zero usage: run `git grep -n "@angular/forms"` across the repo. The ONLY expected hits are root `package.json` and `package-lock.json` (the `forms` substring in `exit-codes.ts` is the word "performs" — not an import; ignore it). If any genuine `import`/template/config reference to `@angular/forms` is found, STOP and treat it as a deliberate dependency: do NOT remove it; instead add `ignoreDependencies` with a documented reason to `.fallowrc.jsonc` and note the finding in the SUMMARY. (Per D-07 verify-then-resolve; the research evidence is REMOVE, but the executor's import-graph check is authoritative.)
    Assuming zero usage is reconfirmed (the expected case): delete the `"@angular/forms": "22.0.4"` line from the root `package.json` `dependencies` block. Then run `npm install` (NOT `npm ci`) ONCE to regenerate `package-lock.json` so it (a) adds `fallow@2.103.0` from Task 2 and (b) drops `@angular/forms`. This repo uses `.npmrc legacy-peer-deps=true` (committed) — `npm install` inherits it; do not pass extra peer flags.
    After the lockfile regenerates, run the gate command from repo root to PROVE it: `npx fallow audit --format json --base origin/main`. Expect verdict `pass` / exit 0 on a fully clean (0-finding) tree (11-RESEARCH.md live-evidence table, "Config + `@angular/forms` removed" row). `npx fallow` resolves the now-locked 2.103.0 (never `@latest`).
    Finally, prove the removal did not break the engine: `npx nx run-many -t typecheck-drift test -p angular-typechecker` (the existing drift gate + unit/integration suite) stays green — `@angular/forms` was never imported, so this is a regression backstop.
    Do NOT stage `node_modules`. Stage only `package.json` and `package-lock.json` (per the global rule: never `git add .`).
  </action>
  <verify>
    <automated>git grep -n "@angular/forms" package.json; test $? -ne 0 && echo "REMOVED-FROM-PACKAGE-JSON" ; npx fallow audit --format json --base origin/main; echo "fallow-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "@angular/forms" package.json` returns NOTHING (line removed); the only repo-wide `@angular/forms` reference is gone (lockfile regenerated drops it too).
    - `npx fallow audit --format json --base origin/main` exits 0 (verdict `pass`) AND reports 0 dead-code findings (`has_errors:false` on the fully clean tree).
    - `package-lock.json` contains a `fallow` 2.103.0 entry and no `@angular/forms` entry (regenerated by `npm install`, not hand-edited).
    - `npx nx run-many -t typecheck-drift test -p angular-typechecker` exits 0 (engine unaffected by the removal).
  </acceptance_criteria>
  <done>`@angular/forms` is removed (zero-usage reconfirmed), the lockfile is regenerated with `fallow@2.103.0` and without `@angular/forms`, `npx fallow audit --base origin/main` exits 0 on a 0-finding tree, and the engine suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local/CI node_modules | A new analyzer dependency (`fallow`) crosses into the dev/CI dependency tree |
| repo working tree -> fallow analyzer | fallow reads source + git history; it has no network/privileged surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering/Spoofing | `fallow` npm dependency (slopsquat / hijacked release) | mitigate | EXACT-pin `fallow@2.103.0` (Task 2, no `^`/`~`) + lockfile-pinned (Task 3 `npm install`); CI runs `npm ci` then `npx fallow` (resolves the locked version), NEVER `@latest`. Researcher slopcheck cleared 2.103.0 `[OK]` (404k wk dl, source repo `github.com/fallow-rs/fallow`, no `postinstall`). |
| T-11-02 | Tampering | `.fallowrc.jsonc` over-suppression baselining real debt | accept (controlled) | "Resolve, not baseline": false positives suppressed STRUCTURALLY (entry/ignoreExports/overrides) with one documented JSONC reason each (D-02/D-03); the genuine `@angular/forms` finding is REMOVED at source, not ignored (Task 3, D-07). `unused-dependencies` stays `error`; only the structurally-untraceable `unused-dev-dependencies` is `off` (owned by `@nx/dependency-checks`, D-06). |
| T-11-SC | Tampering | npm install of `fallow` | mitigate | Package Legitimacy Audit table present in 11-RESEARCH.md (`fallow` `[OK]`, no `[ASSUMED]`/`[SUS]`/`[SLOP]`); no `postinstall` script; no blocking-human checkpoint required because the audit cleared the package. |

No `[ASSUMED]`/`[SUS]` packages -> no legitimacy checkpoint task needed (per the Package Legitimacy Gate; the audit table in 11-RESEARCH.md cleared `fallow`).
</threat_model>

<verification>
- `npx fallow audit --format json --base origin/main` exits 0 (verdict pass) on a 0-finding tree.
- `git grep -n '"fallow"' package.json` shows the exact pin `2.103.0`; `git grep -n "@angular/forms" package.json` returns nothing.
- `package-lock.json` regenerated (fallow added, `@angular/forms` dropped) — not hand-edited.
- `npx nx run-many -t typecheck-drift test -p angular-typechecker` stays green (removal did not regress the engine).
- `.fallowrc.jsonc` uses the CORRECTED repo-root fixture glob `fixtures/fault-isolation/**`.
</verification>

<success_criteria>
QUAL-02 satisfied: a hand-authored `.fallowrc.jsonc` resolves the current findings (false positives suppressed structurally with documented reasons; `@angular/forms` removed) so `npx fallow audit --base origin/main` exits 0 on a fully clean tree. The `fallow`-devDependency half of QUAL-03 satisfied: `fallow@2.103.0` exact-pinned as a root devDep, lockfile-pinned, run via `npx fallow` (never `@latest`).
</success_criteria>

<output>
Create `.planning/phases/11-fallow-code-quality-ci-gate/11-01-SUMMARY.md` when done.
</output>
