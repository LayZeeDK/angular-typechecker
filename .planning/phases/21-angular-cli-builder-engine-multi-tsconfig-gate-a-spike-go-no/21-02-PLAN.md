---
phase: 21-angular-cli-builder-engine-multi-tsconfig-gate-a-spike-go-no
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - packages/angular-typechecker/src/executors/typecheck/schema.d.ts
  - packages/angular-typechecker/src/executors/typecheck/schema.json
  - packages/angular-typechecker/src/executors/typecheck/schema-parity.spec.ts
  - packages/angular-typechecker/src/builders/typecheck/schema.json
  - packages/angular-typechecker/src/executors/typecheck/normalize-options.ts
  - packages/angular-typechecker/src/core/run-typecheck.ts
  - packages/angular-typechecker/src/core/multi-tsconfig.integration.spec.ts
  - fixtures/multi-tsconfig-array/**
autonomous: true
requirements: [ENG-01]
must_haves:
  truths:
    - "runTypecheck accepts tsConfigPath: string | readonly string[]; an array runs each entry through the existing single-tsConfig gather logic, UNIONs the raw per-entry diagnostics, and runs ONE finalize over the COMBINED declared input set"
    - "An array [appLeaf, specLeaf] surfaces planted diagnostics from BOTH leaves; neither leaf's in-project files are dropped as out of the other leaf's input set"
    - "tsConfig: ['x'] yields a CoreResult with the same diagnostics/counts as tsConfig: 'x'"
    - "The single-string runTypecheck path and the entire Nx executor path (single-string tsConfig) are byte-unchanged"
    - "Both the executor and the builder schema.json accept tsConfig as a string OR a non-empty array of strings (oneOf)"
    - "Honors CONTEXT.md decision D-06 (tsConfig string|string[] union-then-single-finalize over the combined input set) and the D-04 GATE dependency (proceeds only after Plan 01 returned GO)"
  artifacts:
    - path: "packages/angular-typechecker/src/core/run-typecheck.ts"
      provides: "Widened CoreOptions.tsConfigPath + handleMultiTsConfig (union-then-single-finalize array branch)"
      contains: "handleMultiTsConfig"
    - path: "packages/angular-typechecker/src/executors/typecheck/schema.d.ts"
      provides: "TypecheckExecutorOptions.tsConfig widened to string | string[]"
      contains: "string | string[]"
    - path: "packages/angular-typechecker/src/executors/typecheck/schema.json"
      provides: "Executor tsConfig oneOf string|array"
      contains: "oneOf"
    - path: "packages/angular-typechecker/src/builders/typecheck/schema.json"
      provides: "Builder tsConfig oneOf string|array"
      contains: "oneOf"
    - path: "packages/angular-typechecker/src/executors/typecheck/normalize-options.ts"
      provides: "Array-aware absolute path resolution (map resolveOne over entries)"
    - path: "packages/angular-typechecker/src/core/multi-tsconfig.integration.spec.ts"
      provides: "Real-compiler proof of the array union + combined boundary + ['x']==='x'"
    - path: "fixtures/multi-tsconfig-array"
      provides: "Hermetic co-located app+spec leaves with a planted diagnostic in each"
  key_links:
    - from: "runTypecheck (Array.isArray branch)"
      to: "handleMultiTsConfig -> finalize + buildFinalizeFilter(combined rootNamePaths)"
      via: "union raw per-entry diagnostics then ONE finalize"
      pattern: "handleMultiTsConfig"
    - from: "normalize-options"
      to: "coreOptions.tsConfigPath (string | readonly string[])"
      via: "Array.isArray(options.tsConfig) ? map(resolveOne) : resolveOne"
      pattern: "Array\\.isArray"
---

<objective>
ENG-01: widen the engine's `tsConfig` to `string | string[]` additively. An array runs each entry
through the EXISTING single-`tsConfig` gather logic, UNIONs the raw per-entry diagnostics, then runs
ONE `finalize` (boundary filter + `ts.sortAndDeduplicateDiagnostics` + explicit category counts) over
the union -- reusing the shipped `handleSolutionWalk` union-then-single-`finalize` tail and the v0.2.0
input-set-membership boundary over the COMBINED declared input sets. The single-string behavior and
the entire Nx path stay byte-unchanged.

Purpose: give a per-project Angular CLI `typecheck` target the `tsConfig: [buildLeaf, specLeaf]` shape
Phase 22's generator will wire, without re-implementing aggregation or breaking the boundary.

Output: a widened type + both schemas (`oneOf`) + array-aware `normalize-options` + a core
`handleMultiTsConfig`, proven by a hermetic real-compiler integration spec.

GATED: this plan proceeds ONLY after Plan 01's GATE A' checkpoint returned GO (D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-angular-cli-builder-engine-multi-tsconfig-gate-a-spike-go-no/21-RESEARCH.md
@.planning/phases/21-angular-cli-builder-engine-multi-tsconfig-gate-a-spike-go-no/21-PATTERNS.md

Project rules: @CLAUDE.md and @AGENTS.md. Windows arm64; `git grep`/`rg` never `grep`; blank lines
around control-flow/returns + always-braces JS/TS; ASCII only; Prettier `singleQuote:true`; NEVER
mutate committed fixtures (fixtures/gate-b-error, fixtures/sibling-import, etc.) -- build a NEW hermetic
fixture; the TypeScript LSP feed is NOT authoritative -- trust `nx test`/`nx build`.

<interfaces>
<!-- Extracted from src/core/run-typecheck.ts + walk-references.ts this session. Use directly. -->

CoreOptions.tsConfigPath is currently `string` (run-typecheck.ts line ~21). Widen to
`string | readonly string[]`. NormalizedOptions.coreOptions carries it.

The shipped union-then-single-finalize tail to MIRROR (handleSolutionWalk, run-typecheck.ts ~441-531,
and the per-leaf accumulation in walk-references.ts ~195-289):
  - per entry: `const parsed = ng.readConfiguration(entry, { suppressOutputPathCheck: true });`
    then `throwIfInfrastructureFailure(ng, ts, parsed.errors)`, then
    `const result = runNoEmitCompilation(ng, parsed);`
  - accumulate the RAW union: push `...parsed.errors` then `...result.diagnostics`; sum
    `rootNamesCount += parsed.rootNames.length`; push `...parsed.rootNames` into combined `rootNamePaths`;
    push `...detectUncheckedDeclaredFiles(ts, parsed, entry)` (dedupe across entries with a Set, as
    walk-references does).
  - then `throwIfInfrastructureFailure(ng, ts, union)` over the whole union.
  - then ONE `finalize(ts, options.tsConfigPath, rootNamesCount, [...configDiagnostics, ...union], start,
    buildFinalizeFilter(ts, <first-entry parsed>, options, ts.sys.useCaseSensitiveFileNames, combinedRootNamePaths))`.
  - attach notTypeCheckedDeclaredFiles via `presentIfNonEmpty` (the []->undefined idiom).

Reusable module-scoped helpers (DO NOT re-implement): `throwIfInfrastructureFailure`,
`runNoEmitCompilation` (from gather-diagnostics), `finalize`, `buildFinalizeFilter`, `presentIfNonEmpty`,
`resolveFilterBasePath`. `buildFinalizeFilter`'s `inputTs` arg = the COMBINED rootNamePaths (this is the
input-set-membership boundary over the combined declared set).

normalize-options.ts current single-string resolution (~48-50):
  `const tsConfigPath = isAbsolute(options.tsConfig) ? options.tsConfig : joinPathFragments(context.root, options.tsConfig);`
`joinPathFragments` (from @nx/devkit, already imported line 4) is REQUIRED for POSIX-separator
stability on Windows arm64 -- do NOT switch to node:path.join.

Executor schema-parity.spec.ts reads only Object.keys(properties) + required + defaults (never
tsConfig.type), so widening tsConfig to oneOf does NOT break it -- ADD a new assertion that tsConfig has
a `oneOf` with a string branch and an array-of-strings branch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen the type + both schemas + array-aware normalize-options</name>
  <files>packages/angular-typechecker/src/executors/typecheck/schema.d.ts, packages/angular-typechecker/src/executors/typecheck/schema.json, packages/angular-typechecker/src/builders/typecheck/schema.json, packages/angular-typechecker/src/executors/typecheck/normalize-options.ts, packages/angular-typechecker/src/executors/typecheck/schema-parity.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/executors/typecheck/schema.d.ts (widen tsConfig)
    - packages/angular-typechecker/src/executors/typecheck/schema.json (widen tsConfig.type -> oneOf)
    - packages/angular-typechecker/src/builders/typecheck/schema.json (widen identically)
    - packages/angular-typechecker/src/executors/typecheck/normalize-options.ts (array resolution seam)
    - packages/angular-typechecker/src/executors/typecheck/schema-parity.spec.ts (add oneOf assertion)
    - 21-RESEARCH.md "ENG-01: the tsConfig array seam" (seams 1-3)
  </read_first>
  <behavior>
    - schema.d.ts: `TypecheckExecutorOptions.tsConfig` is `string | string[]`.
    - executor + builder schema.json: `tsConfig` is `oneOf: [{type:string}, {type:array, items:{type:string}, minItems:1}]` with the array-inclusive description; nothing else in either schema changes.
    - normalize-options: given `tsConfig: "a"` (relative) it resolves to `joinPathFragments(context.root, "a")`; given `tsConfig: ["a","b"]` it resolves to `[join(root,"a"), join(root,"b")]`; an absolute entry passes through; the coreOptions.tsConfigPath type is `string | readonly string[]`.
    - schema-parity.spec.ts: a new assertion proves tsConfig.oneOf has a string branch and an array-of-strings (minItems 1) branch; the existing EXPECTED_KEYS/required/defaults/cli/version assertions still pass.
  </behavior>
  <action>
    Widen `schema.d.ts` `tsConfig: string` -> `tsConfig: string | string[]` (seam 1); nothing else.
    In BOTH `src/executors/typecheck/schema.json` and `src/builders/typecheck/schema.json`, replace
    `tsConfig`'s `"type": "string"` with a `oneOf` of `{ "type": "string" }` and `{ "type": "array",
    "items": { "type": "string" }, "minItems": 1 }`, and update the description to "Path (or array of
    paths) to the tsconfig(s) to type-check. Resolved relative to the workspace root when not absolute."
    Keep the executor schema's `cli`/`version`/`$id` and the builder schema's sanitization untouched
    (seam 2). In `normalize-options.ts` add a `resolveOne(p)` mirroring the existing `isAbsolute ? p :
    joinPathFragments(context.root, p)` and set `tsConfigPath = Array.isArray(options.tsConfig) ?
    options.tsConfig.map(resolveOne) : resolveOne(options.tsConfig)` (seam 3). Add the oneOf assertion to
    `schema-parity.spec.ts`.
  </action>
  <verify>
    <automated>nx test angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `nx test angular-typechecker` exits 0.
    - schema.d.ts declares `tsConfig: string | string[]`.
    - Both schema.json files have `tsConfig.oneOf` with a string branch and an array branch (items type string, minItems 1); the executor schema still has `cli:"nx"`/`version:2`, the builder schema still has neither.
    - schema-parity.spec.ts asserts the oneOf shape and all pre-existing parity assertions pass.
  </acceptance_criteria>
  <done>The option type + both schemas accept string|array and normalize-options resolves array entries; parity guards are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add handleMultiTsConfig to the core (union-then-single-finalize)</name>
  <files>packages/angular-typechecker/src/core/run-typecheck.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/run-typecheck.ts (CoreOptions, runTypecheck top, handleSolutionWalk tail, buildFinalizeFilter, finalize, presentIfNonEmpty)
    - packages/angular-typechecker/src/core/walk-references.ts (the per-leaf accumulation loop ~195-289 to mirror)
    - 21-RESEARCH.md "handleMultiTsConfig design (reuse, do NOT re-implement)" + "WRONG approach" + "Design decisions the planner must lock"
    - 21-PATTERNS.md "src/core/run-typecheck.ts (MODIFIED)"
  </read_first>
  <behavior>
    - CoreOptions.tsConfigPath is `string | readonly string[]`.
    - runTypecheck, at the top (after loadCompilerCli/loadTypescript), branches: `if (Array.isArray(options.tsConfigPath)) return handleMultiTsConfig(ng, ts, options, start);` before the existing single-string body.
    - handleMultiTsConfig over `[appLeaf, specLeaf]` (each with a planted error) unions BOTH errors and returns them in one CoreResult; neither leaf's in-project file is dropped by the combined boundary.
    - handleMultiTsConfig over a SINGLE-element array `[x]` returns a CoreResult with the same diagnostic codes + errorCount/warningCount as the single-string `x` path.
    - A zero-rootNames array entry contributes 0 root names and feeds the coverage-incomplete signal (mirror the walk's `zero-root-names` handling, NOT the direct path's hard 90001).
    - A per-entry UNKNOWN_ERROR_CODE (500) re-throws as TypecheckInfrastructureError (never a counted type error).
  </behavior>
  <action>
    Widen `CoreOptions.tsConfigPath` to `string | readonly string[]` (seam 4). Add the `Array.isArray`
    branch at the top of `runTypecheck` returning `handleMultiTsConfig(ng, ts, options, start)`. Implement
    `handleMultiTsConfig` as the surviving-leaf tail of `handleSolutionWalk`, sourced from the EXPLICIT
    path list `options.tsConfigPath` instead of resolved references: loop each entry ->
    `readConfiguration(entry, { suppressOutputPathCheck: true })` -> `throwIfInfrastructureFailure(ng, ts,
    parsed.errors)` per entry -> if `parsed.rootNames.length === 0` record it as a zero-rootNames entry
    (contributes 0, feeds coverage-incomplete -- mirror walk-references, NOT a hard 90001) and continue
    -> else `runNoEmitCompilation(ng, parsed)` and accumulate the RAW union (`...parsed.errors`,
    `...result.diagnostics`), `rootNamesCount += parsed.rootNames.length`, combined `rootNamePaths`
    (`...parsed.rootNames`), and deduped `notTypeCheckedDeclaredFiles`
    (`...detectUncheckedDeclaredFiles(ts, parsed, entry)`). Keep the FIRST entry's `parsed` for the
    finalize filter's basePath (array entries are expected co-located in one project dir; document this as
    a limitation -- input-set membership, not basePath, is the primary boundary). After the loop:
    `throwIfInfrastructureFailure(ng, ts, union)`, then ONE `finalize(ts, options.tsConfigPath,
    rootNamesCount, [...configDiagnostics, ...union], start, buildFinalizeFilter(ts, firstParsed, options,
    ts.sys.useCaseSensitiveFileNames, combinedRootNamePaths))`, and attach `notTypeCheckedDeclaredFiles`
    via `presentIfNonEmpty`. `configDiagnostics` here is empty ([]) -- the array path has no single parsed
    config to prepend (each entry's parse errors are already in the union). NEVER call `runTypecheck` per
    entry and merge CoreResults (double-finalizes + breaks the combined boundary -- RESEARCH "WRONG
    approach"). Keep runTypecheck under the cognitive-complexity budget by extracting handleMultiTsConfig
    as its own function (mirror how handleSolutionWalk is extracted).
  </action>
  <verify>
    <automated>nx build angular-typechecker && nx test angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `nx build angular-typechecker && nx test angular-typechecker` exits 0.
    - run-typecheck.ts has a `handleMultiTsConfig` function reached via `Array.isArray(options.tsConfigPath)`.
    - CoreOptions.tsConfigPath is `string | readonly string[]`.
    - handleMultiTsConfig unions per-entry raw diagnostics and calls `finalize` exactly ONCE with `buildFinalizeFilter(..., combinedRootNamePaths)`.
    - No per-entry `runTypecheck` call and no per-entry `finalize` (grep the diff: exactly one added `finalize(` call in handleMultiTsConfig).
  </acceptance_criteria>
  <done>The core accepts an array tsConfigPath and aggregates via union-then-single-finalize over the combined input set, reusing the shipped helpers.</done>
</task>

<task type="auto">
  <name>Task 3: Hermetic ENG-01 integration spec + fixture</name>
  <files>packages/angular-typechecker/src/core/multi-tsconfig.integration.spec.ts, fixtures/multi-tsconfig-array/**</files>
  <read_first>
    - packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts (shape to mirror: findWorkspaceRoot, runTypecheck direct call, assert off CoreResult)
    - fixtures/gate-b-error/ (co-located leaf tsconfig shape: extends ../../tsconfig.base.json, noEmit, strictTemplates, files/include)
    - fixtures/solution-style/ (app + spec leaf layout)
    - .planning/spikes/CONVENTIONS.md (hermetic-fixture rule -- NEVER mutate committed fixtures)
    - 21-PATTERNS.md "ENG-01 integration spec + fixture (NEW)"
  </read_first>
  <action>
    Build a NEW hermetic fixture `fixtures/multi-tsconfig-array/` with co-located leaves: `tsconfig.app.json`
    (files: the component .ts) and `tsconfig.spec.json` (files/include: the .spec.ts), each extending
    `../../tsconfig.base.json` with `noEmit`, `strictTemplates`, mirroring gate-b-error's leaf shape. Plant
    ONE known diagnostic in the app component (e.g. a TS2322 or an NG8002 template error) and ONE in the
    spec file (e.g. a distinct TS2322); the spec imports the component so both are in one dependency graph.
    Author `src/core/multi-tsconfig.integration.spec.ts` (integration tier, `dependsOn: build`; mirror
    run-typecheck.integration.spec.ts): (a) `runTypecheck({ tsConfigPath: [appLeaf, specLeaf] })` surfaces
    BOTH planted codes and keeps both leaves' in-project files (combined boundary -- neither dropped);
    (b) `runTypecheck({ tsConfigPath: [appLeaf] })` yields the same diagnostic codes + errorCount/warningCount
    as `runTypecheck({ tsConfigPath: appLeaf })` (`["x"]`===`"x"`); (c) the single-string `appLeaf` path is
    unchanged from its pre-ENG-01 behavior (same codes). Use the `NG(code) = -990000 - code` helper for any
    NG assertion. NEVER mutate fixtures/gate-b-error or any other committed fixture.
  </action>
  <verify>
    <automated>nx build angular-typechecker && nx run angular-typechecker:integration</automated>
  </verify>
  <acceptance_criteria>
    - `nx run angular-typechecker:integration` exits 0 (the new spec runs in the integration tier).
    - The array `[appLeaf, specLeaf]` result contains BOTH planted diagnostic codes.
    - The single-element `[appLeaf]` result equals the single-string `appLeaf` result (same codes + counts).
    - fixtures/multi-tsconfig-array is a NEW directory; no committed fixture was modified (`git status` shows only additions under fixtures/multi-tsconfig-array).
  </acceptance_criteria>
  <done>A hermetic real-compiler fixture proves the array union + combined-input-set boundary + ['x']===['x'] equivalence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer options -> engine | `tsConfig` (now string | array) crosses into normalize-options + the core |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering (Input Validation, V5) | `tsConfig` array entries | mitigate | Schema `oneOf` with `array.items.type:string` + `minItems:1`; normalize-options resolves each entry to an absolute path via joinPathFragments; the core still requires absolute paths and never reads process.cwd(). |
| T-21-05 | Repudiation (false PASS via array aggregation) | handleMultiTsConfig union/finalize | mitigate | Single finalize over the COMBINED input set (never per-entry) so no leaf's real diagnostic is dropped as "out of the other's set"; a per-entry UNKNOWN_ERROR_CODE (500) still re-throws as TypecheckInfrastructureError; a zero-rootNames entry feeds the coverage-incomplete signal, never a silent pass. Proven by the hermetic fixture (planted error in EACH leaf must surface). |
| T-21-06 | Tampering (fixture integrity) | committed fixtures | mitigate | New hermetic fixture only; committed fixtures are never mutated (perturbing them breaks existing specs + the Nx graph). |
</threat_model>

<verification>
- `nx build angular-typechecker && nx test angular-typechecker` green (unit + schema parity).
- `nx run angular-typechecker:integration` green (the new hermetic multi-tsconfig spec).
- `nx lint angular-typechecker` + `nx run angular-typechecker:format:check` green.
- The single-string executor path and the Nx path are byte-unchanged (no change to executor.ts; the single-string runTypecheck body is untouched below the array branch).
</verification>

<success_criteria>
- `tsConfig` accepts `string | string[]`; an array unions per-entry diagnostics and filters over the combined declared input set (ENG-01).
- The single-string behavior and the entire Nx path are byte-unchanged (widening only).
- Both schemas accept the oneOf string|array; `["x"]` behaves identically to `"x"`.
</success_criteria>

<output>
Create `.planning/phases/21-angular-cli-builder-engine-multi-tsconfig-gate-a-spike-go-no/21-02-SUMMARY.md` when done.
</output>
