---
phase: 07-release-pr-workflow-and-clean-changelog
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
autonomous: true
requirements: [REL-01, REL-03]
must_haves:
  truths:
    - "AGENTS.md release-mechanics describes the Release-PR flow: cut on release/* branch -> PR (carries code + .planning/) -> merge commit -> tag the MERGE COMMIT -> push tag -> publish"
    - "AGENTS.md states the cut creates NO tag (git.tag:false) and pushes nothing (push:false + createRelease:false)"
    - "The VERIFIED 0.x bump-shift table + the createRelease LANDMINE + the literal-version gotcha are KEPT (not regressed)"
    - "AGENTS.md documents that main is PR-only (empty bypass) and the D-12 enforcement-toggle recovery"
  artifacts:
    - path: "AGENTS.md"
      provides: "rewritten release-mechanics section for the Release-PR flow + ruleset note"
      contains: "release/"
  key_links:
    - from: "AGENTS.md (release-mechanics)"
      to: "nx.json release.git.tag:false (Plan 01) + ci.yml + the live ruleset (Plan 04)"
      via: "documents the cut-on-branch / PR / tag-merge-commit / PR-only-main flow consistent with the locked decisions"
      pattern: "git\\.tag"
---

<objective>
Rewrite the AGENTS.md release-mechanics section for the Release-PR flow (D-17). The current section documents the OLD "cut locally on main -> curate -> push the tag from main" flow and claims the cut CREATES a tag -- both are now wrong after the REL-01 `git.tag:false` flip.

Per the RESEARCH "AGENTS.md Rewrite Scope (D-17)" table:
- KEEP (do not regress): the "Conventional Commits drive the changelog/version" intro + the 0.x bump-shift table (VERIFIED against nx 23.0.1), "Only commits that touch the published project count", Gotcha 1 (literal-version pin), Gotcha 3's `createRelease:"github"` LANDMINE sub-block (source-verified).
- UPDATE: the "Always confirm with a dry run" note to require the UNIFIED `npx nx release --dry-run` (the `nx release version` subcommand REJECTS the top-level `release.git` block).
- STRENGTHEN: Gotcha 2 (curate the scope) -- cite that the live dry-run leaks `**06-02:**` plan-id scopes.
- REWRITE: Gotcha 3's lead paragraph + the "Quick checklist" steps -- the cut on a `release/*` branch, `git.tag:false` means NO tag at cut time, PR carries code + `.planning/` (D-07), merge as a MERGE COMMIT (D-04), then tag the MERGE COMMIT `angular-typechecker@x.y.z` (no `v`, D-05) and push to fire the frozen release.yml.
- ADD: a short note that `main` is now PR-only (empty bypass, D-06) and the D-12 recovery toggle (admins EDIT the ruleset `enforcement: disabled`, push the fix, re-enable) so a future agent does not attempt a direct push.

Purpose: AGENTS.md governs how every AI agent cuts a release here; it has been factually wrong twice before, so the rewrite must be verified against the actual nx 23.0.1 behavior + the locked decisions. This is the documentation deliverable for REL-01 + REL-03.
Output: a rewritten release-mechanics section + a Default-branch-ruleset note in AGENTS.md.

CODE-REVIEW GATE (AGENTS.md's own rule): per AGENTS.md lines 6-17, any change to AGENTS.md MUST be code-reviewed; the phase `code_review_gate` satisfies this. The executor must call this out in the SUMMARY and ensure the change is factually accurate against nx 23.0.1, internally consistent, and ASCII-only. Do NOT cut a real release, push a tag, or change live config in this plan -- it edits documentation only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-release-pr-workflow-and-clean-changelog/07-CONTEXT.md
@.planning/phases/07-release-pr-workflow-and-clean-changelog/07-RESEARCH.md
@.planning/phases/07-release-pr-workflow-and-clean-changelog/07-PATTERNS.md
@AGENTS.md
@CHANGELOG.md

<interfaces>
<!-- The locked decisions the rewrite must encode (do NOT relitigate): -->
D-01: nx.json release.git.tag:false (keep commit:true, push:false, createRelease:false)
D-02: cut on a release/* branch via `nx release --skip-publish` (no native nx release-PR mode)
D-03: tag-after-merge is MANUAL: `git tag angular-typechecker@x.y.z <merge-sha> && git push origin angular-typechecker@x.y.z`
D-04: merge method = MERGE COMMITS; tag the MERGE COMMIT
D-05: tag name EXACTLY `angular-typechecker@x.y.z` (no `v`); tagged tree must carry the bump (verify `git show <tag>:packages/angular-typechecker/package.json`)
D-06: main is PR-only, empty bypass (owner included)
D-07: .planning/ rides IN the PR (NOT stripped)
D-12: lockout recovery = toggle ruleset enforcement:disabled, push fix, re-enable
D-13/D-14: curate CHANGELOG.md in the PR; GitHub Release via `gh release create --notes-file <section> --verify-tag` (NEVER --generate-notes)

<!-- KEEP-verbatim anchors (RESEARCH AGENTS.md Rewrite Scope table): -->
- the 0.x bump-shift table (feat/fix both PATCH in 0.x; breaking -> minor)
- the createRelease:"github" LANDMINE block (GIT_PUSH_FALSE_WITH_CREATE_RELEASE / silent push)
- Gotcha 1 (literal version bypasses the 0.x shift)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Gotcha 3 + the Quick checklist; UPDATE the dry-run note; STRENGTHEN Gotcha 2</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (the file being modified; the release-mechanics sections -- "Conventional Commits..." intro + 0.x table (lines ~19-99), Gotcha 1 (lines 103-113), Gotcha 2 (lines 115-122), Gotcha 3 lead + LANDMINE (lines 124-154), the Quick checklist (lines 156-168), and the code-review rule at lines 6-17)
    - .planning/phases/07-release-pr-workflow-and-clean-changelog/07-RESEARCH.md ("## AGENTS.md Rewrite Scope (D-17)" -- the authoritative KEEP-vs-REWRITE table + the internal-consistency checks; "### Runbook 1" + "### Runbook 4" -- the exact branch-cut and tag-after-merge sequences to document)
    - .planning/phases/07-release-pr-workflow-and-clean-changelog/07-PATTERNS.md ("AGENTS.md" section -- KEEP-verbatim anchors + the rewrite targets with exact line numbers)
    - .planning/phases/07-release-pr-workflow-and-clean-changelog/07-CONTEXT.md (<decisions> D-01..D-17 -- the source of truth for the new flow; D-17 reconciliation requirements)
  </read_first>
  <action>
    Edit AGENTS.md per the RESEARCH "AGENTS.md Rewrite Scope (D-17)" table. (1) UPDATE the "Always confirm with a dry run" note: state that the UNIFIED `npx nx release --dry-run` must be used (the `nx release version` subcommand REJECTS the top-level `release.git` block; cite this as newly verified against nx 23.0.1). (2) STRENGTHEN Gotcha 2: cite that the live dry-run this session PROVED the raw nx changelog leaks `**06-02:**` plan-id scopes (concrete evidence for curating). (3) REWRITE Gotcha 3's lead paragraph: replace "cut locally on main -> curate -> push tag from main" + "the cut creates ... the tag" with the Release-PR flow per D-01/D-02/D-03/D-04/D-05/D-07 -- cut on a `release/*` branch via `npx nx release --skip-publish`; with `git.tag:false` the cut creates NO tag (and `push:false`+`createRelease:false` push nothing); curate CHANGELOG.md + amend onto the version commit; push the branch + open a PR that CARRIES code + `.planning/` (D-07); self-merge once `ci` is green as a MERGE COMMIT (D-04); then `git tag angular-typechecker@x.y.z <merge-sha>` (no `v`, D-05), verify `git show <tag>:packages/angular-typechecker/package.json` carries the bump, and `git push origin angular-typechecker@x.y.z` to fire the frozen release.yml; create the GitHub Release with `gh release create angular-typechecker@x.y.z --notes-file <curated section> --verify-tag` (NEVER --generate-notes, D-14). KEEP the createRelease:"github" LANDMINE sub-block VERBATIM. (4) REWRITE the "Quick checklist" steps to the branch-cut -> dry-run -> curate -> PR -> merge -> tag-merge-commit -> push-tag -> gh-release sequence; state the cut no longer creates a tag at all. (5) ADD a short note (new gotcha or appended paragraph): `main` is now PR-only with EMPTY bypass (D-06) so even the owner cannot direct-push; document the D-12 recovery (admins EDIT the ruleset `enforcement: disabled`, push the fix, re-enable -- prefer this over a standing bypass actor). Reconcile explicitly: AGENTS.md's 0.x-adjusted bump column stays the operative one vs. CLAUDE.md's post-1.0 mapping note; note the `angular-typechecker-release-mechanics` memory point 6 already anticipates this flow. Do NOT regress the 0.x bump table, Gotcha 1, or "Only commits that touch the published project count". ASCII only (no em dashes, curly quotes, ellipsis); preserve the file's existing Markdown voice.
  </action>
  <verify>
    <automated>rg -n 'release/\*|release/x\.y\.z|merge commit|MERGE COMMIT' AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'release/' AGENTS.md` shows the cut happens on a `release/*` branch
    - `rg -n 'merge commit|MERGE COMMIT' AGENTS.md` shows the tag targets the merge commit
    - `rg -n 'git\.tag' AGENTS.md` documents that `git.tag:false` means the cut creates no tag
    - `rg -n 'GIT_PUSH_FALSE_WITH_CREATE_RELEASE' AGENTS.md` still returns the KEPT LANDMINE block
    - `rg -n 'generate-notes' AGENTS.md` shows `--generate-notes` is documented as forbidden (use `--notes-file`)
    - `rg -n 'enforcement' AGENTS.md` shows the D-12 recovery toggle + the PR-only main note
    - `rg -n 'nx release version' AGENTS.md` shows the unified-command dry-run note (the version subcommand rejects the release.git block)
    - The 0.x bump-shift table is still present (`rg -n 'EFFECT NOW' AGENTS.md` returns a hit) -- not regressed
    - No non-ASCII characters introduced: `rg -nP "[^\x00-\x7F]" AGENTS.md` returns nothing
  </acceptance_criteria>
  <done>AGENTS.md release-mechanics describes the Release-PR flow (branch cut, no-tag cut, PR carrying .planning/, merge-commit, tag-the-merge-commit, gh release via --notes-file) and the PR-only-main + D-12 recovery, while keeping the VERIFIED 0.x table, Gotcha 1, and the createRelease LANDMINE; ASCII-only; flagged for the code-review gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> agent behavior | AGENTS.md instructions drive every future AI-agent release cut; a wrong instruction silently propagates (it has been wrong twice before -- the file's own code-review rule exists for this). |
| documented flow -> public artifact | The documented `--notes-file`/scope-hygiene discipline is the human-side control that keeps internal scopes/PII out of the public changelog + Release notes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering (instruction integrity) | AGENTS.md release-mechanics | mitigate | Rewrite verified against nx 23.0.1 + the locked D-01..D-17; KEEP the source-verified 0.x table + createRelease LANDMINE verbatim; AGENTS.md's own code-review rule (the phase code_review_gate) checks factual accuracy. |
| T-07-09 | Information Disclosure | documented release-notes step | mitigate | Document `gh release create --notes-file` (NEVER `--generate-notes`, which leaks PR-title scopes) + scope hygiene (D-15) + public-email-only; consistent with the release-hygiene spec's public-email assertion. |
| T-07-10 | Elevation of Privilege | documented direct-push to main | mitigate | Document that `main` is PR-only with empty bypass (D-06) and the D-12 enforcement-toggle recovery, so an agent does not attempt (and a doc does not imply) a direct push. |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install (docs-only change). No legitimacy checkpoint required. |
</threat_model>

<verification>
- The acceptance-criteria `rg` checks above all pass.
- The change is consistent with Plan 01's nx.json `git.tag:false`, Plan 02's ci.yml `ci` gate, and Plan 04's PR-only ruleset (no contradiction across the four plans).
- The AGENTS.md change is surfaced for the phase `code_review_gate` (AGENTS.md's own rule) and recorded as such in the SUMMARY.
</verification>

<success_criteria>
- AGENTS.md release-mechanics + Quick checklist describe the Release-PR flow (D-17) accurately against nx 23.0.1 and the locked decisions, including the no-tag cut, the PR carrying `.planning/`, the merge-commit tag, the `--notes-file` GitHub Release, the PR-only `main`, and the D-12 recovery.
- The VERIFIED 0.x bump table, Gotcha 1, and the createRelease LANDMINE are kept (not regressed); the dry-run note requires the unified command; ASCII-only.
- The change is flagged for the code-review gate per AGENTS.md's own rule.
</success_criteria>

<output>
Create `.planning/phases/07-release-pr-workflow-and-clean-changelog/07-03-SUMMARY.md` when done.
</output>
