---
phase: 06-full-e2e-matrix-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/angular-typechecker-matrix-e2e/project.json
  - e2e/angular-typechecker-matrix-e2e/vitest.config.mts
  - e2e/angular-typechecker-matrix-e2e/tsconfig.json
  - e2e/angular-typechecker-matrix-e2e/tsconfig.spec.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/nx.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/package.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/apps/app/project.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/apps/app/tsconfig.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/apps/app/tsconfig.app.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/apps/app/src/app.component.ts
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/local-lib/project.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/local-lib/tsconfig.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/local-lib/tsconfig.lib.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/local-lib/tsconfig.spec.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/local-lib/src/local-lib.component.ts
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/local-lib/src/local-lib.component.spec.ts
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/buildable-lib/project.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/buildable-lib/tsconfig.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/buildable-lib/tsconfig.lib.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/buildable-lib/ng-package.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/buildable-lib/src/buildable-lib.component.ts
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/publishable-lib/project.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/publishable-lib/tsconfig.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/publishable-lib/tsconfig.lib.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/publishable-lib/ng-package.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/publishable-lib/package.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/publishable-lib/src/publishable-lib.component.ts
  - e2e/angular-typechecker-matrix-e2e/src/matrix-5types.int.spec.ts
autonomous: false
requirements: [TEST-03]
must_haves:
  truths:
    - 'The executor type-checks an application project from the installed tarball (green) and reports an injected TS2322 (non-zero exit)'
    - 'The executor type-checks a local non-buildable library from the installed tarball, green + injected error'
    - 'The executor type-checks a buildable library (hand-authored @nx/angular:ng-packagr-lite build target, NO @nx/angular dep) from the installed tarball, green + injected error'
    - 'The executor type-checks a publishable library (hand-authored @nx/angular:package build target + importPath, NO @nx/angular dep) from the installed tarball, green + injected error'
    - 'The executor type-checks a spec tsconfig (tsConfig points at tsconfig.spec.json, a distinct file set) from the installed tarball, green + injected error'
    - 'A clean npm install of the shaped consumer-workspace fixture succeeds with NO legacy-peer-deps override (B-03 honesty invariant preserved)'
  artifacts:
    - path: 'e2e/angular-typechecker-matrix-e2e/project.json'
      provides: 'matrix-e2e Nx project (@nx/vitest:test, implicitDependencies:[angular-typechecker], tags:[scope:fixture])'
      contains: 'angular-typechecker-matrix-e2e'
    - path: 'e2e/angular-typechecker-matrix-e2e/vitest.config.mts'
      provides: 'serialized e2e vitest config cloned from install-e2e'
      contains: 'singleFork'
    - path: 'e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/nx.json'
      provides: 'self-contained fixture workspace keyed on published executor id'
      contains: 'angular-typechecker:angular-typecheck'
    - path: 'e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/package.json'
      provides: 'fixture deps, EXACT pins, NO @nx/angular (OQ-1)'
      contains: '22.0.4'
    - path: 'e2e/angular-typechecker-matrix-e2e/src/matrix-5types.int.spec.ts'
      provides: '5-type e2e spec, it.each over 5 targets, green + injected TS2322'
      contains: 'INJECTED_TS_CODE'
  key_links:
    - from: 'e2e/angular-typechecker-matrix-e2e/src/matrix-5types.int.spec.ts'
      to: 'the packed angular-typechecker tarball'
      via: 'nx build --skip-nx-cache -> npm pack --json -> cpSync fixture into tmp -> npm install <tgz> -> nx run <target>'
      pattern: 'npm pack'
    - from: 'fixtures/consumer-workspace/{apps,libs}/*/project.json'
      to: 'the published executor'
      via: 'executor: angular-typechecker:angular-typecheck per project type'
      pattern: 'angular-typechecker:angular-typecheck'
---

<objective>
Create the new `angular-typechecker-matrix-e2e` Nx e2e project and a single self-contained
`consumer-workspace` fixture carrying all five project-type shapes, then author the 5-type e2e
spec that installs the packed tarball ONCE and runs the executor against each type (green +
injected `TS2322`). This is the bulk of TEST-03: validate the executor end-to-end across
application, local (non-buildable) library, buildable library, publishable library, and spec
tsconfig, against the INSTALLED package.

Purpose: TEST-03 demands all five project types be validated against the real installed tarball
(today only `consumer-app`, an application, is validated by the Phase-5 install-e2e smoke). The
buildable/publishable shapes are hand-authored WITHOUT an `@nx/angular` dependency (OQ-1) so the
Phase-5 B-03 clean-install honesty invariant is preserved. A clean-install spike runs FIRST as a
blocking gate before the fixture is locked.

Output: a new e2e Nx project (cloned from install-e2e), a 5-type consumer-workspace fixture, and
`matrix-5types.int.spec.ts`. The pnpm fixture, mixed-case spec extensions, and `ci.yml` are
separate plans (06-02, 06-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-full-e2e-matrix-ci/06-CONTEXT.md
@.planning/phases/06-full-e2e-matrix-ci/06-RESEARCH.md
@.planning/phases/06-full-e2e-matrix-ci/06-PATTERNS.md

<interfaces>
<!-- Analog shapes already read from the codebase. Use these directly; no exploration needed. -->

install-e2e project.json (e2e/angular-typechecker-install-e2e/project.json) -- clone, rename three strings:

```json
{
  "name": "angular-typechecker-install-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "e2e/angular-typechecker-install-e2e/src",
  "tags": ["scope:fixture"],
  "implicitDependencies": ["angular-typechecker"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": { "reportsDirectory": "coverage/e2e/angular-typechecker-install-e2e" }
    }
  }
}
```

install-e2e vitest.config.mts (every serialization knob is load-bearing; change ONLY `name` + `cacheDir`):

```typescript
export default defineConfig(() => ({
  root: __dirname,
  cacheDir: '../../node_modules/.vite/e2e/angular-typechecker-install-e2e',
  plugins: [nxViteTsPaths(), nxCopyAssetsPlugin([])],
  test: {
    name: 'angular-typechecker-install-e2e',
    watch: false,
    globals: true,
    environment: 'node',
    include: ['src/**/*.int.spec.ts'],
    reporters: ['default'],
    pool: 'forks',
    poolOptions: { forks: { singleFork: true } },
    fileParallelism: false,
    sequence: { concurrent: false },
    testTimeout: 300000,
    hookTimeout: 300000,
  },
}));
```

Imports at top of that file: `import { defineConfig } from 'vitest/config';`, `import { nxViteTsPaths } from '@nx/vite/plugins/nx-tsconfig-paths.plugin';`, `import { nxCopyAssetsPlugin } from '@nx/vite/plugins/nx-copy-assets.plugin';`

consumer-app/nx.json (self-contained, PUBLISHED executor-id key) -- reuse whole file:

```json
{
  "$schema": "node_modules/nx/schemas/nx-schema.json",
  "analytics": false,
  "namedInputs": {
    "default": ["{projectRoot}/**/*", "sharedGlobals"],
    "production": ["default", "!{projectRoot}/eslint.config.mjs", "!{projectRoot}/**/?(*.)+(spec|test).[jt]s?(x)?(.snap)", "!{projectRoot}/tsconfig.spec.json"],
    "sharedGlobals": []
  },
  "targetDefaults": {
    "angular-typechecker:angular-typecheck": {
      "cache": true,
      "outputs": [],
      "inputs": ["production", "{projectRoot}/tsconfig*.json", "{projectRoot}/package.json", "^default", { "dependentTasksOutputFiles": "**/*.{d.ts,d.cts,d.mts,tsbuildinfo}", "transitive": true }, { "externalDependencies": ["typescript", "@angular/compiler-cli"] }]
    }
  }
}
```

consumer-app/package.json (EXACT pins; do NOT add @nx/angular -- OQ-1):

```json
{
  "name": "consumer-app",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "dependencies": { "@angular/common": "22.0.4", "@angular/compiler": "22.0.4", "@angular/core": "22.0.4", "rxjs": "7.8.2", "zone.js": "0.16.0" },
  "devDependencies": { "@angular/compiler-cli": "22.0.4", "@nx/devkit": "23.0.1", "nx": "23.0.1", "typescript": "6.0.3" }
}
```

consumer-app/tsconfig.json (no tsconfig.base.json extension -- deliberate self-contained shape):

```json
{ "files": [], "include": [], "references": [{ "path": "./tsconfig.lib.json" }] }
```

consumer-app/tsconfig.lib.json (proven self-contained leaf; `types: []`, `strictTemplates: true`):

```json
{
  "compilerOptions": { "noEmit": true, "target": "es2022", "module": "preserve", "moduleResolution": "bundler", "strict": true, "skipLibCheck": true, "experimentalDecorators": false, "emitDecoratorMetadata": false, "lib": ["es2022", "dom"], "types": [] },
  "angularCompilerOptions": { "strictTemplates": true },
  "include": ["src/**/*.ts"]
}
```

consumer-app/src/app.component.ts (standalone component the spec injects TS2322 into):

```typescript
import { Component } from '@angular/core';

@Component({ selector: 'app-root', standalone: true, template: '<p>{{ label }}</p>' })
export class AppComponent {
  readonly label: string = 'angular-typechecker matrix app';
}
```

install-smoke.int.spec.ts harness (the verbatim-reuse target) -- key spans:

- imports + `INJECTED_TS_CODE = 'TS2322'` (lines 1-29)
- `NX_RUNNER_ENV_KEYS` (8 keys) + `buildCleanEnv()` (lines 57-96): strips the 8 NX\_\* vars + `npm_config_legacy_peer_deps` + `NPM_CONFIG_LEGACY_PEER_DEPS`; returns `{...cleaned, NX_DAEMON:'false', FORCE_COLOR:'0'}`
- `run(cwd)` (lines 112-132): `execSync('npx nx run ' + target + ' --output-style=static', {cwd, env, encoding:'utf8'})`, catch captures `{stdout: execError.stdout+execError.stderr, code: execError.status ?? 1}`
- `beforeAll` (lines 134-153): `nx build angular-typechecker --skip-nx-cache` -> `npm pack --json` from distDir -> `tarballPath`
- `afterAll` (lines 155-161): `rmSync(tarballPath, {force:true})`
- clean-install honesty (lines 168-187): `cpSync(fixtureDir, tmp)`, `writeFileSync(join(tmp,'.npmrc'),'')`, `npm install <tgz>` with `env:{...env, npm_config_userconfig: join(tmp,'.npmrc.nonexistent')}`
- 4-way green/injected assertion (lines 207-236): `expect(green.code).toBe(0)`; inject; `expect(bad.code).not.toBe(0)`; `expect(bad.stdout).toContain(INJECTED_TS_CODE)`; `expect(bad.stdout).not.toMatch(/ERR_REQUIRE_ESM/)`; `expect(bad.stdout).not.toContain('infrastructure error')`
  </interfaces>
  </context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: OQ-1 clean-install spike (BLOCKING-before-lock)</name>
  <files>e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/package.json</files>
  <read_first>
    - .planning/phases/06-full-e2e-matrix-ci/06-CONTEXT.md (OQ-1 block, lines 350-364)
    - .planning/phases/06-full-e2e-matrix-ci/06-RESEARCH.md (OQ-1 resolution lines 53, 412-415; Pitfall 5 lines 281-283)
    - e2e/angular-typechecker-install-e2e/fixtures/consumer-app/package.json (the EXACT-pins analog to mirror)
    - .planning/phases/06-full-e2e-matrix-ci/06-PATTERNS.md (consumer-workspace/package.json section, lines 196-222)
  </read_first>
  <action>
    Build the consumer-workspace fixture package.json FIRST (the shape Task 2 will commit): copy
    consumer-app/package.json verbatim, change "name" to "consumer-workspace", keep the EXACT pins
    (@angular/common|compiler|core 22.0.4, rxjs 7.8.2, zone.js 0.16.0; devDeps @angular/compiler-cli
    22.0.4, @nx/devkit 23.0.1, nx 23.0.1, typescript 6.0.3). Do NOT add @nx/angular (OQ-1 / D-07):
    the executor never RUNS the build targets, only reads each project's tsConfig, so build-target
    executor names are purely structural markers; adding @nx/angular@23.0.1 re-introduces the
    Angular-22-vs-@nx/angular-23 peer mismatch the root .npmrc papers over and burns the B-03
    honesty signal.

    Run a CLEAN npm install of this package.json in an isolated tmp dir with NO legacy-peer-deps
    override: write an empty .npmrc into the tmp dir, copy ONLY the package.json there, and run
    `npm install` with env `npm_config_userconfig` pointed at a non-existent path so no ancestor or
    user ~/.npmrc reintroduces the override. (Mirror the install-smoke honesty pattern: empty
    .npmrc + non-existent userconfig.)

    Interpret the result:
    - If the clean install SUCCEEDS (no ERESOLVE): record PASS in the SUMMARY and proceed to Task 2
      with this package.json shape locked. This auto-resolves the checkpoint.
    - If it ERESOLVEs: STOP. Do NOT auto-add a peer override (that would burn the B-03 invariant).
      Surface the exact ERESOLVE output and present the remediation options to the user (scope a
      documented override to this one fixture / widen the override / await an @nx/angular 23.1.x
      that admits Angular 22) per OQ-1. This is the escalation point -- do not self-decide.

  </action>
  <verify>
    <automated>cd "$(mktemp -d)" && cp "D:/projects/github/LayZeeDK/angular-typechecker/e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/package.json" . && printf '' > .npmrc && npm install --userconfig ./.npmrc.nonexistent 2>&1 | tee /tmp/oq1-install.log; rg -q 'ERESOLVE' /tmp/oq1-install.log && echo OQ1_ERESOLVE_ESCALATE || echo OQ1_CLEAN_OK</automated>
  </verify>
  <acceptance_criteria>
    - consumer-workspace/package.json exists with EXACT pins (contains "22.0.4", "23.0.1", "6.0.3") and does NOT contain the string "@nx/angular"
    - The clean install (empty .npmrc, non-existent userconfig, NO legacy-peer-deps flag) either prints OQ1_CLEAN_OK (no ERESOLVE) OR halts with the ERESOLVE output surfaced to the user as an escalation (never auto-adds an override)
    - SUMMARY records the spike outcome (PASS or ESCALATED)
  </acceptance_criteria>
  <resume-signal>If clean: auto-resume. If ERESOLVE: user selects a remediation (do NOT auto-add the override).</resume-signal>
  <done>The consumer-workspace package.json is locked with EXACT pins and no @nx/angular, and a clean npm install (no legacy-peer-deps) is proven to succeed (B-03 preserved) OR the ERESOLVE was escalated to the user without auto-patching.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the matrix-e2e Nx project + the 5-type consumer-workspace fixture</name>
  <files>e2e/angular-typechecker-matrix-e2e/project.json, e2e/angular-typechecker-matrix-e2e/vitest.config.mts, e2e/angular-typechecker-matrix-e2e/tsconfig.json, e2e/angular-typechecker-matrix-e2e/tsconfig.spec.json, e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/nx.json, e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/apps/app/{project.json,tsconfig.json,tsconfig.app.json,src/app.component.ts}, e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/local-lib/{project.json,tsconfig.json,tsconfig.lib.json,tsconfig.spec.json,src/local-lib.component.ts,src/local-lib.component.spec.ts}, e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/buildable-lib/{project.json,tsconfig.json,tsconfig.lib.json,ng-package.json,src/buildable-lib.component.ts}, e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/publishable-lib/{project.json,tsconfig.json,tsconfig.lib.json,ng-package.json,package.json,src/publishable-lib.component.ts}</files>
  <read_first>
    - e2e/angular-typechecker-install-e2e/project.json (exact analog to clone)
    - e2e/angular-typechecker-install-e2e/vitest.config.mts (clone verbatim; change name + cacheDir only)
    - e2e/angular-typechecker-install-e2e/tsconfig.json AND tsconfig.spec.json (exact analogs -- these extend ../../tsconfig.base.json; see 06-PATTERNS.md lines 132-159)
    - e2e/angular-typechecker-install-e2e/fixtures/consumer-app/{nx.json,package.json,project.json,tsconfig.json,tsconfig.lib.json,src/app.component.ts} (the self-contained-fixture analog)
    - .planning/phases/06-full-e2e-matrix-ci/06-RESEARCH.md (Pattern 1 lines 169-206: the 5 type shapes + ng-package.json minimal shape line 204)
    - .planning/phases/06-full-e2e-matrix-ci/06-PATTERNS.md (every fixture-file section, lines 75-328)
    - libs/typecheck-consumer/project.json (in-repo local-lib shape reference)
  </read_first>
  <action>
    Create the new Nx e2e project `e2e/angular-typechecker-matrix-e2e/` by cloning install-e2e's
    project.json (set name "angular-typechecker-matrix-e2e", sourceRoot
    "e2e/angular-typechecker-matrix-e2e/src", reportsDirectory
    "coverage/e2e/angular-typechecker-matrix-e2e", tags ["scope:fixture"], implicitDependencies
    ["angular-typechecker"]); clone vitest.config.mts verbatim changing ONLY name
    "angular-typechecker-matrix-e2e" and cacheDir
    "../../node_modules/.vite/e2e/angular-typechecker-matrix-e2e"; clone tsconfig.json +
    tsconfig.spec.json verbatim (they extend ../../tsconfig.base.json and reference the spec
    tsconfig -- these are the PROJECT tsconfigs, not the fixture's).

    Build the single self-contained `fixtures/consumer-workspace/` with its own nx.json (reuse
    consumer-app/nx.json verbatim -- keyed on the PUBLISHED id angular-typechecker:angular-typecheck,
    NO tsconfig.base.json) and the package.json already locked in Task 1.

    Author the five project-type shapes (D-07 / RESEARCH Pattern 1). Every angular-typecheck target
    uses executor `angular-typechecker:angular-typecheck` with `includeDeps: true`; every leaf
    tsconfig mirrors consumer-app/tsconfig.lib.json (noEmit, target es2022, module preserve,
    moduleResolution bundler, strict, skipLibCheck, lib [es2022,dom], types [], strictTemplates
    true, no base extension). Each project gets a tsconfig.json with files [] / include [] /
    references to its leaf tsconfig, and ONE committed-clean standalone component (the matrix spec
    injects TS2322 into a per-run TMP copy):
      - apps/app: projectType "application", tsConfig "apps/app/tsconfig.app.json" (the leaf;
        EXCLUDE *.spec.ts/*.test.ts so the spec-tsconfig type stays a distinct file set), one
        app.component.ts.
      - libs/local-lib: projectType "library", NO build target, tsConfig
        "libs/local-lib/tsconfig.lib.json", one local-lib.component.ts. ALSO add a sibling spec
        target `angular-typecheck-spec` whose tsConfig is "libs/local-lib/tsconfig.spec.json" (the
        5th project type lives here as a sibling target, not a 6th project). Author
        local-lib/tsconfig.spec.json including the *.spec.ts source set + test-runner ambient types
        (`types: ["node"]`) -- a DISTINCT file set the app/lib targets exclude (RESEARCH A3) -- and
        one local-lib.component.spec.ts (committed-clean) it picks up.
      - libs/buildable-lib: projectType "library", a STRUCTURAL `build` target with executor
        "@nx/angular:ng-packagr-lite" and options.project "libs/buildable-lib/ng-package.json"
        (NEVER run -- OQ-1; the executor is never resolved at graph or run time), plus the
        angular-typecheck target on tsconfig.lib.json. Author ng-package.json minimal shape
        `{ "dest": "../../dist/buildable-lib", "lib": { "entryFile": "src/index.ts" } }` (omit
        $schema; build never runs), and one buildable-lib.component.ts.
      - libs/publishable-lib: projectType "library", a STRUCTURAL `build` target with executor
        "@nx/angular:package" and options.project "libs/publishable-lib/ng-package.json", plus an
        importPath-style per-lib package.json `{ "name": "@fixtures/publishable-lib", "version":
        "0.0.0", "private": true }`, plus ng-package.json minimal shape (dest
        "../../dist/publishable-lib"), plus the angular-typecheck target on tsconfig.lib.json, and
        one publishable-lib.component.ts.

    Use per D-07/OQ-1: NO @nx/angular dep; the @nx/angular:* executor names are structural markers
    only. ASCII-only; single quotes in TS; blank lines around control flow per project style.

  </action>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && npx nx show projects 2>&1 | rg -q 'angular-typechecker-matrix-e2e' && echo PROJECT_REGISTERED && rg -q 'angular-typechecker:angular-typecheck' e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/apps/app/project.json && rg -L '@nx/angular' e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/package.json && echo NO_NX_ANGULAR_DEP</automated>
  </verify>
  <acceptance_criteria>
    - `npx nx show projects` lists `angular-typechecker-matrix-e2e`
    - vitest.config.mts contains `singleFork`, `fileParallelism: false`, `sequence`, `environment: 'node'`, `testTimeout: 300000` (serialization cloned verbatim)
    - fixtures/consumer-workspace/nx.json contains the targetDefault key `angular-typechecker:angular-typecheck`
    - All five type shapes exist: apps/app/project.json (tsConfig tsconfig.app.json), libs/local-lib/project.json (no build target; sibling `angular-typecheck-spec` target on tsconfig.spec.json), libs/buildable-lib/project.json (build executor `@nx/angular:ng-packagr-lite`), libs/publishable-lib/project.json (build executor `@nx/angular:package` + importPath package.json)
    - fixtures/consumer-workspace/package.json does NOT contain `@nx/angular` (grep returns no match)
    - Every leaf tsconfig has `strictTemplates: true`, `types: []` (except the spec tsconfig which adds test ambient types), and no `tsconfig.base.json` extension
  </acceptance_criteria>
  <done>The matrix-e2e project is registered in the Nx graph and the consumer-workspace fixture carries all five hand-authored project-type shapes with no @nx/angular dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author the 5-type e2e spec (install tarball once, it.each over 5 targets)</name>
  <files>e2e/angular-typechecker-matrix-e2e/src/matrix-5types.int.spec.ts</files>
  <read_first>
    - e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts (THE harness to reuse verbatim -- buildCleanEnv lines 57-96, run() lines 112-132, beforeAll lines 134-153, afterAll lines 155-161, clean-install honesty lines 168-187, 4-way assertion lines 207-236)
    - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/** (the fixture authored in Task 2 -- the 5 target ids + the per-type source files to inject into)
    - .planning/phases/06-full-e2e-matrix-ci/06-PATTERNS.md (matrix-5types.int.spec.ts section, lines 332-393; Pitfall 3 no-op-exit-0)
    - .planning/phases/06-full-e2e-matrix-ci/06-RESEARCH.md (Pitfall 3 lines 273-275: assert the full TS2322 token never a bare 2322)
  </read_first>
  <behavior>
    - For EACH of the 5 targets, the executor runs green (exit 0) against the committed-clean fixture: `app:angular-typecheck`, `local-lib:angular-typecheck`, `buildable-lib:angular-typecheck`, `publishable-lib:angular-typecheck`, `local-lib:angular-typecheck-spec`
    - For EACH of the 5 targets, injecting a TS2322 into that type's source (a per-run TMP copy) yields a non-zero exit AND stdout contains the full token `TS2322` AND NOT `ERR_REQUIRE_ESM` AND NOT `infrastructure error`
    - The spec-tsconfig target's injected error lands in the *.spec.ts file (the distinct file set), proving the spec tsconfig is a genuinely separate check
    - The tarball is built + packed + installed ONCE (beforeAll); the 5 targets share the one install (D-07)
  </behavior>
  <action>
    Author `e2e/angular-typechecker-matrix-e2e/src/matrix-5types.int.spec.ts` by reusing the
    install-smoke harness verbatim: copy the imports + `INJECTED_TS_CODE = 'TS2322'` constant,
    `NX_RUNNER_ENV_KEYS` + `buildCleanEnv()`, the `beforeAll` pack-to-tmp
    (`nx build angular-typechecker --skip-nx-cache` -> `npm pack --json` from
    dist/packages/angular-typechecker -> tarballPath), and the `afterAll` tgz cleanup. Resolve
    `fixtureDir` to e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace.

    Parameterize `run(cwd, target)` (add a `target` arg to the install-smoke `run` helper) so it
    execSyncs `npx nx run ${target} --output-style=static`. NEVER pipe nx through head/rg (the pipe
    tail's exit code masks Nx's). Use `FORCE_COLOR=0` via buildCleanEnv, NEVER the `--no-color` CLI
    flag (the schema's additionalProperties:false rejects the forwarded color:false option).

    In a single `beforeAll`/per-suite setup: install the tarball ONCE into one tmp consumer-workspace
    (cpSync the fixture into mkdtempSync, write empty .npmrc, `npm install <tgz>` with
    npm_config_userconfig -> a non-existent path -- B-03 honesty; let a real ERESOLVE FAIL the test,
    do NOT auto-add the override). Then drive `it.each` over the 5 targets, each a named case. For
    each case: run green (expect code 0); inject a deliberate TS2322 into THAT type's source in a
    way the per-run tmp copy isolates (build the broken line via JSON.stringify -- no quote-escaping
    hazard, ASCII-only); run again and apply the 4-way assertion (non-zero + contains TS2322 + not
    ERR_REQUIRE_ESM + not 'infrastructure error'). For the spec target, inject into the *.spec.ts
    source so the distinct file set is what surfaces the error. Restore/discard via the per-run tmp
    (rmSync in finally) so the committed fixture is never mutated.

    Map each target to its injection source file:
      - app:angular-typecheck -> apps/app/src/app.component.ts
      - local-lib:angular-typecheck -> libs/local-lib/src/local-lib.component.ts
      - buildable-lib:angular-typecheck -> libs/buildable-lib/src/buildable-lib.component.ts
      - publishable-lib:angular-typecheck -> libs/publishable-lib/src/publishable-lib.component.ts
      - local-lib:angular-typecheck-spec -> libs/local-lib/src/local-lib.component.spec.ts

  </action>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && npx nx run-many -t test -p angular-typechecker-matrix-e2e 2>&1 | tee /tmp/matrix-e2e.log; rg -q 'TS2322' /tmp/matrix-e2e.log && echo MATRIX_E2E_RAN</automated>
  </verify>
  <acceptance_criteria>
    - `npx nx run-many -t test -p angular-typechecker-matrix-e2e` exits 0 (all 5 type cases pass green + injected)
    - The spec asserts, for EACH of the 5 targets: non-zero exit on injection + stdout contains the full `TS2322` token + NOT `ERR_REQUIRE_ESM` + NOT `'infrastructure error'`
    - The spec installs the tarball ONCE (one `npm pack` + one `npm install`) and shares it across the 5 `it.each` cases (D-07)
    - The spec uses `buildCleanEnv` (strips the 8 NX_* runner vars + both legacy-peer-deps env forms) and the empty-.npmrc + non-existent-userconfig honesty pattern (B-03)
    - matrix-5types.int.spec.ts contains `it.each` (or equivalent named per-type cases) over exactly the 5 targets
  </acceptance_criteria>
  <done>All five project types are validated end-to-end against the installed tarball: each runs green and reports an injected TS2322 with no ERR_REQUIRE_ESM, proving the packaged executor resolves and runs per type. TEST-03's 5-type criterion is met (pnpm + mixed-case are in 06-02).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                 | Description                                                                                             |
| ---------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| packed tarball -> tmp consumer-workspace | This repo's freshly-packed `dist/` is installed into an isolated tmp dir; no third-party registry fetch |
| fixture deps -> npm registry             | EXACT-pinned, already-vetted locked stack (@angular 22.0.4 / nx 23.0.1 / ts 6.0.3); no new package      |

## STRIDE Threat Register

| Threat ID | Category  | Component                 | Disposition | Mitigation Plan                                                                                                                                                                            |
| --------- | --------- | ------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-06-01   | Tampering | tarball install in spec   | accept      | The installed artifact is THIS repo's own `npm pack` of `dist/` (no registry fetch); the Phase-5 tarball-audit gate already asserts no install scripts. Local fixture, no secrets.         |
| T-06-02   | Tampering | fixture package.json deps | mitigate    | EXACT version pins (no ranges) of the already-vetted locked stack; NO new external package; clean install (no legacy-peer-deps) surfaces a real ERESOLVE rather than masking it            |
| T-06-SC   | Tampering | npm install in spec       | mitigate    | slopcheck N/A (no new packages introduced); the OQ-1 clean-install spike is a blocking checkpoint that surfaces any peer-resolution finding for human escalation rather than auto-patching |

No new application input-handling surface: this plan adds test fixtures + an e2e spec only (local files, no secrets, no untrusted input crosses a boundary).
</threat_model>

<verification>
- `npx nx show projects` lists `angular-typechecker-matrix-e2e`.
- `npx nx run-many -t test -p angular-typechecker-matrix-e2e` exits 0 with all 5 type cases green + injected-error.
- `fixtures/consumer-workspace/package.json` contains no `@nx/angular` (OQ-1).
- A clean `npm install` of the fixture package.json succeeds with no legacy-peer-deps override (B-03), OR the ERESOLVE was escalated to the user (never auto-patched).
</verification>

<success_criteria>
The executor is validated end-to-end across all five project types (application, local
non-buildable library, buildable library, publishable library, spec tsconfig) against the
installed tarball, each green + injected-`TS2322`, with the consumer-workspace fixture carrying no
`@nx/angular` dependency and installing cleanly without a peer override.
</success_criteria>

<output>
Create `.planning/phases/06-full-e2e-matrix-ci/06-01-SUMMARY.md` when done (record the OQ-1 spike
outcome: PASS clean-install or ESCALATED).
</output>
