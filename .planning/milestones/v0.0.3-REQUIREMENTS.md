# Requirements (ARCHIVED): angular-typechecker v0.0.3 (Engine hardening)

> **ARCHIVED 2026-06-30 at milestone close.** All 16 v0.0.3 requirements SATISFIED
> (verified with code evidence). Audit: `.planning/milestones/v0.0.3-MILESTONE-AUDIT.md`
> -- 16/16 requirements, 4/4 phases verified `passed`, 18/18 cross-phase links, Nyquist
> COMPLIANT, zero accumulated tech debt. Shipped live as `angular-typechecker@0.0.3`.

**Defined:** 2026-06-29
**Core Value:** Deliver the complete Angular type-check (TypeScript + template type-check + extended NG8xxx) for any project type without building the app or running the tests -- faster, in isolation, and more completely than the build's coupled check or a bare `ngc --noEmit`.

**Milestone goal:** Harden the EXISTING whole-program no-emit `runTypecheck` engine -- close real correctness/completeness holes, make diagnostic gathering resilient instead of all-or-nothing, and make Angular-version drift fail loudly -- all verified against stable Angular 22.0.4 and WITHOUT migrating off `performCompilation` to `NgtscProgram`.

**Grounding:** every requirement traces to a verified finding in `.planning/research/prior-art/PRIOR-ART-SUMMARY.md` (the bracketed `#N` refers to that summary's numbered improvement table).

## v0.0.3 Requirements -- Outcomes

### Correctness & Completeness (COR) -- Phase 8

- [x] **COR-01** -- *validated.* Config-resolution `UNKNOWN_ERROR_CODE` (500) detected immediately after the config parse and re-thrown as `TypecheckInfrastructureError`, never folded into reported diagnostics or counted as a type error. -- [#1]
- [x] **COR-02** -- *validated.* Global / location-less TypeScript semantic diagnostics (e.g. TS2318) gathered via `program.getTsProgram().getGlobalDiagnostics()` (7th unconditional getter) and reported. -- [#2]
- [x] **COR-03** -- *validated.* A diagnostic whose `file.fileName` is present-but-empty is treated as file-less (reported, never silently dropped by the project-boundary filter). -- [#5]
- [x] **COR-04** -- *validated (reframed).* Pure framework-agnostic `toExitCode` -> `0`/`1`/`2` policy as the single source of truth; the Nx executor surfaces infra distinctly within Nx's `{ success }` contract; the literal OS exit code `2` is delivered by the DEFERRED standalone CLI surface. -- [Q3 decision; reframed 2026-06-29 after nx-source prior-art]

### Resilience (RES) -- Phase 9

- [x] **RES-01** [GATE / spike] -- *validated.* **GO = HYBRID** (recorded in `09-RES-01-SPIKE.md`): gather the non-template set once whole-program + loop the template/extended families per file. SIMPLE rejected. -- [#3 open question; the only true unknown]
- [x] **RES-02** -- *validated (reframed 2026-06-29).* Angular gathering is fault-isolated per file (HYBRID): one component's `FatalDiagnosticError` yields exactly one diagnostic and does NOT collapse the run; surviving files' TS + Angular NON-template diagnostics are still reported, on the existing `api.Program` surface; a LOUD never-silent suppression notice fires on a TCB-generation Fatal. **Known limitation deferred to REP-RES-02b** (surviving files' TEMPLATE/extended recovery; same limit as `@angular/build`). -- [#3]
- [x] **RES-03** -- *validated.* A throwing `realpath()` in the boundary filter is caught (falls back to the unresolved path). -- [#4]
- [x] **RES-04** -- *validated.* The no-emit override sets `suppressOutputPathCheck: true`. -- [#6]

### Drift-hardening & Maintainability (HARD) -- Phase 10

- [x] **HARD-01** -- *validated.* Build-time `tsconfig.drift.json` (classic `moduleResolution: node`) type-checked in CI as its own `typecheck-drift` target; real `api.Program` must stay assignable TO the vendored shim (real->shim only); NG error-code + `UNKNOWN_ERROR_CODE` encoding mirrored. -- [#7] [D-07 wording fix]
- [x] **HARD-02** -- *validated.* Fabricated `EmitFlags.None = 0` corrected against the real enum; `emitFlags: 0` call site retained as a documented literal. -- [#8]
- [x] **HARD-03** -- *validated.* Every divergence carries a greppable `// angular-typechecker: vendored -- <reason>` marker (6 total). -- [#9]
- [x] **HARD-04** -- *validated.* `getNgStructuralDiagnostics()` RETAINED (documented forward-compatible no-op-tolerant) and covered by the HARD-01 getter-set assertion. -- [#10, reversed from "drop it"]
- [x] **HARD-05** -- *validated.* Regression spec asserts no `TS-99` substring survives the `color: false` output path (through the real `cli.formatDiagnostics`). -- [Q4 color-rewrite guard]

### Code-Quality Gate (QUAL) -- Phase 11 (added 2026-06-30 during Phase 11 planning)

- [x] **QUAL-01** -- *validated.* Dedicated, path-gated, SHA-pinned `fallow` CI job (ubuntu-latest, Node 24, `fetch-depth: 0`, `persist-credentials: false`) running `npx fallow audit --format human --base origin/main` (gate `new-only`), wired into the `ci` aggregate's `needs:` + gate expression; single required check stays `ci` (NO ruleset change). Proven RED on introduced dead code via a throwaway PR. -- [CONTEXT D-01/D-10/D-12/D-13/D-14]
- [x] **QUAL-02** -- *validated.* Hand-authored `.fallowrc.jsonc` RESOLVES the repo's current findings (NOT baselined): drift tripwire as `entry` point; `EmitFlags` shim scoped off; `UNKNOWN_ERROR_CODE` via `ignoreExports`; `fixtures/fault-isolation/**` off; `unused-dev-dependencies` off; `@angular/forms` removed (truly unused). Post-phase `fallow audit --base origin/main` exits 0. -- [CONTEXT D-02..D-09]
- [x] **QUAL-03** -- *validated.* `fallow@2.103.0` exact root `devDependency` (run via `npx`); `tools/act/act-compat.sh` asserts `ci/fallow` selection; actionlint stays green; `ci.yml` security posture preserved (SHA-pinned actions, `contents: read`, no PR-metadata interpolation). `code_quality.fallow.enabled: true` kept. -- [CONTEXT D-11/D-12/D-15]

## Deferred (tracked, not in this milestone)

- **OBS-01**: `totalFilesCount` field on `CoreResult` (`@nx/js` parity). Deferred pending charter-fit.
- **REP-RES-02b**: Recover SURVIVING files' Angular TEMPLATE/extended (NG8xxx) diagnostics after a TCB-generation `FatalDiagnosticError`. Needs `OptimizeFor.SingleFile`-per-file priming; lands on the `NgtscProgram` incremental surface. The v0.0.3 RES-02 reframe delivered run-level resilience + a loud suppression notice instead. -- [RES-02 reframe; web research + 5-lens Opus panel 2026-06-29]
- Carried forward from v0.0.1 (unchanged): INF / GEN / SUR / REP / SUP families. Full detail: `.planning/milestones/v0.0.1-REQUIREMENTS.md`.

## Out of Scope (this milestone)

| Feature | Reason |
|---------|--------|
| `NgtscProgram` migration / incremental / `--watch` | RES-02 stayed on the existing `api.Program` surface; the incremental engine is a separate (REP) milestone |
| Machine-readable reporters (JSON / SARIF / GitHub annotations) | REP family; output-layer feature, not engine hardening |
| Any new executor option or feature surface (INF / GEN / SUR) | This milestone improved the existing engine only |
| `totalFilesCount` (OBS-01) | Deferred (see above); decide charter-fit later |
| Emit / build output, auto-fix of diagnostics | Unchanged project-level exclusions (no-emit type-checker) |

## Traceability (final)

| Requirement | Phase | Status |
|-------------|-------|--------|
| COR-01 | Phase 8 | Complete |
| COR-02 | Phase 8 | Complete |
| COR-03 | Phase 8 | Complete |
| COR-04 | Phase 8 | Complete |
| RES-01 | Phase 9 | Complete |
| RES-02 | Phase 9 | Complete |
| RES-03 | Phase 9 | Complete |
| RES-04 | Phase 9 | Complete |
| HARD-01 | Phase 10 | Complete |
| HARD-02 | Phase 10 | Complete |
| HARD-03 | Phase 10 | Complete |
| HARD-04 | Phase 10 | Complete |
| HARD-05 | Phase 10 | Complete |
| QUAL-01 | Phase 11 | Complete |
| QUAL-02 | Phase 11 | Complete |
| QUAL-03 | Phase 11 | Complete |

**Coverage:** 16/16 mapped, 0 unmapped, 16/16 SATISFIED.

---
*Requirements defined: 2026-06-29. Archived: 2026-06-30 at v0.0.3 milestone close.*
