---
phase: 28-shipped-tarball-e2e-real-clone-uat
plan: 03
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - .github/workflows/ci.yml
  - packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts
autonomous: true
requirements: [VER-04]

must_haves:
  truths:
    - "The CI e2e coverage gains an OS axis for ONLY angular-typechecker-cli-e2e: it runs on BOTH Linux (existing dynamic matrix) AND Windows (a dedicated job), Node 24 (D-04)."
    - "The Windows leg is a SEPARATE dedicated e2e-windows job (research option b), leaving the verified-live Linux dynamic matrix + discover contract + GUARD-01b's four assertions completely untouched (D-05)."
    - "The Windows leg benefits from the bounded Verdaccio ECONNREFUSED retry landed in Plan 01 (D-06)."
    - "A Windows-leg failure fails the required ci aggregate check (e2e-windows is in the ci job needs)."
    - "A new GUARD-01f asserts the OS-axis wiring so it cannot silently drift, the same way GUARD-01b guards the Linux matrix."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "e2e-windows job (runs-on: windows-latest, PROJECT env, SHA-pinned uses:, shell:bash run steps) + e2e-windows added to the ci aggregate needs"
      contains: "e2e-windows"
    - path: "packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts"
      provides: "GUARD-01f describe block asserting the four OS-axis wiring facts"
      contains: "GUARD-01f"
  key_links:
    - from: ".github/workflows/ci.yml e2e-windows job"
      to: "angular-typechecker-cli-e2e project"
      via: "nx run-many -t e2e -p \"$PROJECT\" with PROJECT env"
      pattern: "run-many -t e2e"
    - from: ".github/workflows/ci.yml ci job needs"
      to: "e2e-windows"
      via: "needs list membership + contains(needs.*.result,'failure') gate"
      pattern: "e2e-windows"
    - from: "packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts GUARD-01f"
      to: ".github/workflows/ci.yml"
      via: "readFileSync + line-level regex assertions"
      pattern: "windows-latest"
---

<objective>
Add the Windows CI leg for the standalone-CLI tarball e2e -- a SEPARATE dedicated `e2e-windows` job
(research recommendation, option b) that runs ONLY `angular-typechecker-cli-e2e` on `windows-latest`
(Node 24) -- and a new GUARD-01f asserting the OS-axis wiring cannot silently drift.

Purpose: VER-04 SC-2. The `.cmd`/`.ps1` bin shim is the one genuinely Windows-divergent CLI surface,
so this single project (not the whole e2e tier) gets a Windows runner (D-04). Option b is chosen
(D-05) because it leaves the verified-live Linux dynamic matrix + `discover` contract + GUARD-01b's
four assertions completely intact -- avoiding the `matrix.include` merge trap that would silently drop
the Linux `cli-e2e` leg. This is a deliberate, plan-surfaced RISK: departing from the repo's Linux-only
heavy-e2e default.
Output: an additive `e2e-windows` job wired into the `ci` aggregate + a GUARD-01f drift guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-PATTERNS.md
@.planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-RESEARCH.md
@.planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-01-SUMMARY.md

<!-- Analog sources to MIRROR. -->
@.github/workflows/ci.yml
@packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts

<interfaces>
Live ci.yml job keys (verified 2026-07-16): changes(:48), test(:98), discover(:153), e2e(:204),
fallow(:279), format-lint(:313), act-compat(:346), lint-workflows(:362), scoped-name-guard(:387),
ci(:415). The `e2e` job (:204-262) is a 1-D dynamic matrix
`project: ${{ fromJSON(needs.discover.outputs.projects || '[...]') }}`, `runs-on: ubuntu-latest`
FIXED, and reaches run steps via `PROJECT: ${{ matrix.project }}` env (NEVER interpolated into a run
command) consumed as `npx nx run-many -t typecheck -p "$PROJECT"` then `-t e2e -p "$PROJECT"`. The `ci`
aggregate job `needs: [changes, discover, test, e2e, fallow, format-lint, act-compat, lint-workflows,
scoped-name-guard]` with a `contains(needs.*.result, 'failure') || contains(needs.*.result,
'cancelled')` gate. The `changes` job gates the heavy jobs via `if: ${{ needs.changes.outputs.code != 'false' }}`.
GUARD-01b lives at ci-e2e-coverage-guard.spec.ts:253-398; extractE2eJobLines / job-key-slice helpers at
:108-129; enumerateE2eProjects at :52-63.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the dedicated e2e-windows job + wire it into the ci aggregate needs</name>
  <read_first>
    - .github/workflows/ci.yml (the e2e job :204-262 for the step sequence + PROJECT env pattern; the test job's windows cells :105-111 confirming windows-latest + Node 24; the ci aggregate needs :415-427)
    - .planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-RESEARCH.md (the full e2e-windows YAML :144-173 + the OS-axis section + A1 shell:bash note)
    - .planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-PATTERNS.md (ci.yml modified assignment + Node-24/corepack coupling carry-forward)
  </read_first>
  <action>
    Add a NEW top-level job `e2e-windows` to ci.yml: `runs-on: windows-latest`, gated `needs: changes` +
    `if: ${{ needs.changes.outputs.code != 'false' }}` (same negative-if form the heavy jobs use),
    `env: { NX_DAEMON: false, PROJECT: angular-typechecker-cli-e2e }` -- hardcode the single project name as
    an ENV value, NEVER `${{ matrix.project }}` inline in a run step (the no-command-injection invariant).
    Steps mirror the Linux e2e job: checkout (persist-credentials:false), setup-node (node-version:24,
    cache:npm), `corepack enable`, pnpm/action-setup (version:11.9.0), `npm ci`,
    `npx nx run-many -t typecheck -p "$PROJECT"`, `npx nx run-many -t e2e -p "$PROJECT"`. Pin `shell: bash`
    on the run steps so `-p "$PROJECT"` quoting is byte-identical to the Linux job and the GUARD-01f regex
    matches (A1). Pin EVERY `uses:` to a 40-char commit SHA (match the existing pins in ci.yml for
    actions/checkout, actions/setup-node, pnpm/action-setup -- copy the exact SHAs already used elsewhere in
    the file; do NOT introduce a mutable tag). Carry the Node-24/corepack coupling comment (corepack ships
    in Node 24, removed in 25+). Then ADD `e2e-windows` to the `ci` aggregate `needs` list so the
    `contains(needs.*.result, 'failure')` gate covers it. Do NOT touch the Linux `e2e` dynamic matrix or the
    `discover` job (option b's whole point).
  </action>
  <acceptance_criteria>
    - ci.yml has a job `e2e-windows` with `runs-on: windows-latest`, `env.PROJECT: angular-typechecker-cli-e2e`, and a run step `npx nx run-many -t e2e -p "$PROJECT"`.
    - `e2e-windows` appears in the `ci` job `needs` list.
    - Every `uses:` in the e2e-windows job is 40-char SHA-pinned (no `@vN` / branch tag).
    - No `${{ matrix.project }}` or any `${{ }}` interpolation appears inside a `run:` command in the new job.
    - The existing `e2e` (Linux) matrix and `discover` job are byte-unchanged.
    - `actionlint` (via the existing lint-workflows path) parses ci.yml without error.
  </acceptance_criteria>
  <verify>
    <automated>git grep -n "e2e-windows" .github/workflows/ci.yml && rg -q "runs-on: windows-latest" .github/workflows/ci.yml</automated>
  </verify>
  <done>A dedicated Windows job runs only angular-typechecker-cli-e2e on windows-latest, is a required part of the ci gate, uses only SHA-pinned actions and the PROJECT env pattern, and leaves the Linux wiring untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add GUARD-01f asserting the OS-axis wiring</name>
  <read_first>
    - packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts (the GUARD-01b describe :253-398 -- its readFileSync(ci.yml) + line-level regex idioms with the ^(?!\s*#) comment-exclusion prefix + fail-loud messages; the extractE2eJobLines / job-key-slice helpers :108-129; enumerateE2eProjects :52-63)
    - .github/workflows/ci.yml (the e2e-windows job added in Task 1)
    - .planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-PATTERNS.md (GUARD-01f delta -- the four wiring facts)
  </read_first>
  <action>
    Add a new `GUARD-01f` describe block to ci-e2e-coverage-guard.spec.ts asserting the four OS-axis wiring
    facts: (1) an `e2e-windows` job exists with `runs-on: windows-latest`; (2) it runs
    `nx run-many -t e2e -p "$PROJECT"` with `PROJECT: angular-typechecker-cli-e2e`; (3) `e2e-windows` is in
    the `ci` job's `needs` list; (4) `angular-typechecker-cli-e2e` is in `enumerateE2eProjects(...)` (so it
    ALSO runs on Linux via the dynamic matrix). Add a new `e2e-windows:` job-slicer by generalizing
    extractE2eJobLines's job-key regex `/^ {2}<name>:\s*$/`. Reuse the existing comment-exclusion prefix and
    fail-loud message style. Keep GUARD-01b's existing four assertions UNTOUCHED and green (option b changes
    no Linux wiring).
  </action>
  <acceptance_criteria>
    - `nx test angular-typechecker` passes with a new GUARD-01f describe present (`git grep -c "GUARD-01f" packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts` >= 1).
    - GUARD-01f asserts all four facts: windows-latest job, `run-many -t e2e -p "$PROJECT"` + `PROJECT: angular-typechecker-cli-e2e`, membership in `ci` needs, and membership in enumerateE2eProjects.
    - GUARD-01b's existing assertions remain byte-unchanged and green.
    - Deleting the `e2e-windows` job from ci.yml (mentally) would fail GUARD-01f (fail-loud) -- the assertion is not a tautology.
  </acceptance_criteria>
  <verify>
    <automated>npx nx test angular-typechecker</automated>
  </verify>
  <done>GUARD-01f locks the Windows OS-axis wiring against silent drift, mirroring GUARD-01b for the Linux matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow inputs (matrix/project name, action refs) -> CI runner shell | A project name or action ref reaching a shell command or a mutable action tag can execute attacker-controlled code on the runner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering / Elevation of Privilege | e2e-windows job run steps | mitigate | The project name reaches run steps via the `PROJECT` env var (hardcoded value), NEVER `${{ }}`-interpolated into a `run:` command -- preserving the verified-live no-command-injection invariant. Acceptance criteria grep enforces no `${{ }}` inside `run:`. |
| T-28-05 | Tampering | e2e-windows `uses:` actions | mitigate | Every `uses:` is 40-char commit-SHA-pinned (Dependabot-managed), copied from the existing pins in ci.yml -- no mutable tag that a tj-actions-class repoint could hijack. |
| T-28-02 | Elevation of Privilege / Repudiation | Verdaccio publish on the Windows runner | mitigate | The Windows leg runs the SAME createVerdaccioGlobalSetup 127.0.0.1 SAFETY refuse-gate (Plan 01); no publish leaves the local loopback registry. |
</threat_model>

<verification>
- `git grep -n "e2e-windows" .github/workflows/ci.yml` shows the job and its membership in the `ci` needs list.
- `nx test angular-typechecker` green with GUARD-01f present and GUARD-01b unchanged.
- The Linux `e2e` dynamic matrix and `discover` job are byte-unchanged (option b).
- Real-CI confirmation: the `e2e-windows` job runs `angular-typechecker-cli-e2e` green on `windows-latest` (surfaced at phase verification; the Windows leg is the authoritative proof of the `.cmd` shim path).
</verification>

<success_criteria>
The CI e2e coverage gains a Windows axis for exactly one project (`angular-typechecker-cli-e2e`) via a
dedicated `e2e-windows` job that is a required part of the `ci` gate, uses only SHA-pinned actions and
the `PROJECT` env pattern, leaves the verified-live Linux wiring untouched, and is locked against silent
drift by GUARD-01f (VER-04 SC-2).
</success_criteria>

<output>
Create `.planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-03-SUMMARY.md` when done.
</output>
