---
phase: 02-core-type-check-engine-gatherer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/core/run-typecheck.ts
  - packages/angular-typechecker/src/core/compiler-cli-types.ts
  - packages/angular-typechecker/src/core/gather-diagnostics.spec.ts
  - packages/angular-typechecker/src/core/gate-b.spec.ts
  - packages/angular-typechecker/src/index.ts
  - packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts
  - packages/angular-typechecker/src/core/infra-failure.spec.ts
  - packages/angular-typechecker/tsconfig.lib.json
autonomous: true
requirements:
  - ENG-01
  - ENG-02
  - ENG-04
must_haves:
  truths:
    - "runTypecheck returns errorCount counted explicitly by category Error (not total - errorCount)"
    - "runTypecheck returns warningCount counted explicitly by category Warning"
    - "runTypecheck prepends parsed.errors so a malformed tsconfig is never silently clean"
    - "runTypecheck applies the full D-05 emit-neutralizing override so a composite/emitDeclarationOnly base does not emit a bogus TS5053"
    - "runTypecheck re-throws an infrastructure failure (UNKNOWN_ERROR_CODE 500) instead of counting it as a type error"
    - "The unconditional gatherer surfaces TS2322 AND NG8109 in one pass against the real compiler via runTypecheck"
    - "CoreResult has no public codes field; specs derive codes from diagnostics"
  artifacts:
    - path: "packages/angular-typechecker/src/core/run-typecheck.ts"
      provides: "The real runTypecheck engine + CoreResult contract (D-01..D-06)"
      contains: "rootNamesCount"
    - path: "packages/angular-typechecker/src/core/compiler-cli-types.ts"
      provides: "Widened CompilerCli shim exposing UNKNOWN_ERROR_CODE for D-06"
      contains: "UNKNOWN_ERROR_CODE"
    - path: "packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts"
      provides: "Real-compiler end-to-end proof of the new contract on the gate-b-error fixture"
    - path: "packages/angular-typechecker/src/core/infra-failure.spec.ts"
      provides: "D-06 re-throw proof (focused stub returning a code 500 diagnostic)"
  key_links:
    - from: "run-typecheck.ts"
      to: "gatherAllDiagnostics"
      via: "performCompilation gatherDiagnostics hook"
      pattern: "gatherDiagnostics:\\s*gatherAllDiagnostics"
    - from: "run-typecheck.ts"
      to: "ng.UNKNOWN_ERROR_CODE"
      via: "infrastructure-failure detection by code 500"
      pattern: "UNKNOWN_ERROR_CODE"
---

<objective>
Grow the Phase-1 tracer-bullet `run-typecheck.ts` into the real framework-agnostic engine and lock the `CoreResult` contract that every downstream slice (config-resolution slice 02-02, diagnostic-catalog slice 02-03, and Phases 3/4) consumes.

This slice delivers a thin but COMPLETE engine capability end-to-end: `runTypecheck(options)` loads ESM `@angular/compiler-cli` lazily, resolves the tsconfig, applies the full D-05 emit-neutralizing override, gathers ALL diagnostics unconditionally (ENG-02 differentiator), re-throws genuine infrastructure failures (D-06), and returns the structured `CoreResult` with explicit category counts (D-01, fixing MD-02). The dropped-config-errors fix (D-03 part 1: prepend `parsed.errors`) and the zero-rootNames guard (D-03 part 2) are implemented here as one cohesive function rewrite, then exercised end-to-end against the existing `gate-b-error` fixture (catalog F1+F7) through `runTypecheck` itself.

Purpose: Establishes the load-bearing contract. Without the new `CoreResult` shape and the engine body, slices 02-02 and 02-03 cannot assert anything. This is the "type-checker that lies" correctness slice: a malformed config, a solution-style config, and a masked ESM-load failure must each produce a deterministic non-zero signal rather than a false PASS.

Output: A rewritten `run-typecheck.ts`, a widened `compiler-cli-types.ts` shim, the LW-01 one-line gatherer-spec fix, the `gate-b.spec.ts` `codes` reconcile, an end-to-end integration spec, a focused D-06 re-throw spec, and the `tsconfig.lib.json` exclude extended for ALL Phase-2 fixture dirs (so slices 02-02/02-03 only ADD files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-type-check-engine-gatherer/02-CONTEXT.md
@.planning/phases/02-core-type-check-engine-gatherer/02-RESEARCH.md
@.planning/phases/02-core-type-check-engine-gatherer/02-PATTERNS.md

<interfaces>
<!-- Current CoreResult (REPLACE per D-01) and the shim surface to widen. Executor uses these directly. -->

Current `src/core/run-typecheck.ts` `CoreResult` (drops public `codes`, adds `tsConfigPath` + `rootNamesCount`):
```typescript
export interface CoreResult {
  diagnostics: readonly ts.Diagnostic[];
  codes: number[];        // REMOVE (D-01)
  errorCount: number;
  warningCount: number;   // currently `length - errorCount` -- the MD-02 bug to fix
  durationMs: number;
}
```

Target `CoreResult` (D-01, verbatim from 02-CONTEXT.md lines 27-35 / 02-RESEARCH.md lines 190-198):
```typescript
export interface CoreResult {
  tsConfigPath: string;                  // resolved absolute path actually checked (D-07b)
  rootNamesCount: number;                // input file count; 0 => the D-03 guard fired
  diagnostics: readonly ts.Diagnostic[]; // GENUINE compiler diagnostics only (D-06); config errors prepended (D-03)
  errorCount: number;                    // category === Error (explicit)
  warningCount: number;                  // category === Warning (explicit, NOT total - errorCount)
  durationMs: number;
}
```

Current `CompilerCli` interface (`src/core/compiler-cli-types.ts` lines 34-39) declares `readConfiguration`, `performCompilation`, `defaultGatherDiagnostics`, `EmitFlags`. WIDEN to add `readonly UNKNOWN_ERROR_CODE: typeof UNKNOWN_ERROR_CODE` sourced from `.../node_modules/@angular/compiler-cli/src/transformers/api` (value 500), mirroring the existing `readonly EmitFlags` declaration.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen the type shim and rewrite the runTypecheck engine (D-01..D-06)</name>
  <files>packages/angular-typechecker/src/core/run-typecheck.ts, packages/angular-typechecker/src/core/compiler-cli-types.ts, packages/angular-typechecker/src/index.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/run-typecheck.ts (the Phase-1 seed being grown in place; current minimal override + the MD-02 `length - errorCount` bug + the dropped `parsed.errors`)
    - packages/angular-typechecker/src/core/compiler-cli-types.ts (the shim to widen; mirror the `readonly EmitFlags: typeof EmitFlags` pattern at line 38)
    - packages/angular-typechecker/src/core/gather-diagnostics.ts (the unconditional gatherer — KEEP, wired via gatherDiagnostics)
    - packages/angular-typechecker/src/core/compiler-loader.ts (memoized await import — KEEP; the loadTypescript memoization in run-typecheck.ts lines 56-67 stays)
    - .planning/phases/02-core-type-check-engine-gatherer/02-CONTEXT.md (D-01..D-06 verbatim; the D-05 override object at lines 53-71; the synthesized-guard spec in D-03)
    - .planning/phases/02-core-type-check-engine-gatherer/02-RESEARCH.md (the runTypecheck skeleton lines 256-300; V-2 keep BOTH noEmit+emitFlags:0; V-3 gate on code===500 NOT source==='angular'; the finalize() count pair)
    - .planning/phases/02-core-type-check-engine-gatherer/02-PATTERNS.md (run-typecheck rewrite section lines 33-64; compiler-cli-types widen section lines 68-85)
  </read_first>
  <action>
    Widen `compiler-cli-types.ts`: import `UNKNOWN_ERROR_CODE` from `'../../../../node_modules/@angular/compiler-cli/src/transformers/api'` and add `readonly UNKNOWN_ERROR_CODE: typeof UNKNOWN_ERROR_CODE;` to the `CompilerCli` interface, mirroring the existing `readonly EmitFlags` declaration (per D-06; value is 500).

    Rewrite the `runTypecheck` body in `run-typecheck.ts` to the D-01..D-06 contract. Keep the existing imports, the `loadTypescript()` memoization (lines 56-67), the bare `performance.now()` usage, and the fresh-options-per-call JSDoc (lines 19-27).

    Contract changes (cite the D-IDs in code comments):
    - CoreResult (D-01): REMOVE `codes: number[]`; ADD `tsConfigPath: string` and `rootNamesCount: number`. Final field order exactly: `tsConfigPath`, `rootNamesCount`, `diagnostics`, `errorCount`, `warningCount`, `durationMs`.
    - D-03 part 1 (fixes MD-01): after `ng.readConfiguration(options.tsConfigPath)`, capture `const configDiagnostics = [...parsed.errors]` and prepend it to the final diagnostics array. Never drop `parsed.errors`.
    - D-03 part 2 / D-03a: if `parsed.rootNames.length === 0`, SHORT-CIRCUIT (skip `performCompilation`) and synthesize ONE `ts.Diagnostic` with `category: ts.DiagnosticCategory.Error`, a private `angular-typechecker`-namespace numeric code (e.g. `90001`, chosen outside the TS range and outside the Angular `-99xxxx`/`500` space — Claude's discretion per CONTEXT), `file: undefined`, `start: undefined`, `length: undefined`, and an actionable `messageText`. The message MUST name the leaf tsconfigs literally: `tsconfig.app.json` / `tsconfig.lib.json` / `tsconfig.spec.json`. Branch the wording on `parsed.projectReferences?.length` (solution-style / references-only "point at a leaf tsconfig" vs "empty project"). Return with `rootNamesCount: 0` and the synthesized Error prepended after `configDiagnostics`. Do NOT gate on TS18003 (D-03a; it is suppressed when `references` is present).
    - D-05 + D-02: when rootNames is non-empty, build a FRESH per-call options object spreading `...parsed.options` then the full emit-neutralizing override verbatim from 02-CONTEXT.md lines 53-71 (`noEmit: true`, `composite: false`, `declaration: false`, `declarationMap: false`, `emitDeclarationOnly: false`, `incremental: false`, `tsBuildInfoFile: undefined`, `sourceMap: undefined`, `inlineSourceMap: undefined`, `inlineSources: undefined`, `declarationDir: undefined`, `mapRoot: undefined`, `sourceRoot: undefined`) plus `diagnostics: false` (D-02). Keep `composite: false` as the gatekeeper. Replace the current minimal `{ ...parsed.options, noEmit: true }`.
    - D-05a / V-2: KEEP `emitFlags: 0 as EmitFlags` AND `noEmit: true` — both load-bearing, neither decorative. Keep `gatherDiagnostics: gatherAllDiagnostics` (ENG-02).
    - D-06 / V-3 / L-3: after `performCompilation`, if `result.diagnostics.some((d) => d.code === ng.UNKNOWN_ERROR_CODE)` then THROW a dedicated error (define a small `TypecheckInfrastructureError extends Error` in this file carrying the 500 diagnostic's `messageText`). Detect by `code === 500` only; do NOT add a `source === 'angular'` clause.
    - D-01 counts (fixes MD-02): on the combined `[...configDiagnostics, ...result.diagnostics]` array, compute `errorCount` by `category === ts.DiagnosticCategory.Error` and `warningCount` by `category === ts.DiagnosticCategory.Warning` — NEVER `length - errorCount`.

    Update `src/index.ts`: ensure it still exports `runTypecheck` + `CoreOptions`/`CoreResult` types; export `TypecheckInfrastructureError` so the Phase-4 executor can catch it. No `codes` type remains to export.

    Apply project JS/TS style: blank lines around control flow and returns; always braces on control-flow bodies.
  </action>
  <verify>
    <automated>npx nx build angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "rootNamesCount" packages/angular-typechecker/src/core/run-typecheck.ts` returns the field in the `CoreResult` interface and in both return paths.
    - `git grep -c "codes" packages/angular-typechecker/src/core/run-typecheck.ts` does NOT match a `codes:` field on `CoreResult` (the public field is removed per D-01).
    - `git grep -n "UNKNOWN_ERROR_CODE" packages/angular-typechecker/src/core/compiler-cli-types.ts` shows it added to the `CompilerCli` interface.
    - `git grep -n "composite: false" packages/angular-typechecker/src/core/run-typecheck.ts` matches (the D-05 gatekeeper is present).
    - `git grep -n "diagnostics: false" packages/angular-typechecker/src/core/run-typecheck.ts` matches (D-02).
    - `git grep -n "DiagnosticCategory.Warning" packages/angular-typechecker/src/core/run-typecheck.ts` matches (explicit Warning count, not `length - errorCount`).
    - `git grep -v '^\s*//' packages/angular-typechecker/src/core/run-typecheck.ts | grep -c "length - errorCount"` returns 0 (the MD-02 bug is gone).
    - The synthesized zero-rootNames message names at least one of `tsconfig.app.json` / `tsconfig.lib.json` / `tsconfig.spec.json`: `git grep -n "tsconfig.spec.json\|tsconfig.app.json\|tsconfig.lib.json" packages/angular-typechecker/src/core/run-typecheck.ts` matches.
    - `npx nx build angular-typechecker` succeeds (TS 6 / module:nodenext clean; LSP diagnostics are NOT authoritative — the build is the gate).
  </acceptance_criteria>
  <done>
    The engine compiles under module:nodenext to CommonJS; `CoreResult` carries the D-01 shape; the D-05 override, D-02 suppression, D-03 prepend+guard, and D-06 re-throw are all present and cite their decision IDs.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile Phase-1 specs and add the end-to-end + D-06 integration proofs</name>
  <files>packages/angular-typechecker/src/core/gather-diagnostics.spec.ts, packages/angular-typechecker/src/core/gate-b.spec.ts, packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts, packages/angular-typechecker/src/core/infra-failure.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/gate-b.spec.ts (the Phase-1 differential proof; the `result.codes` reads at lines 102-103 to reconcile; the fixture-path resolution idiom at lines 1-2, 34-39 to copy)
    - packages/angular-typechecker/src/core/gather-diagnostics.spec.ts (line 1 barrel import to fix — LW-01)
    - packages/angular-typechecker/src/core/run-typecheck.ts (the rewritten engine from Task 1 — the contract under test)
    - fixtures/gate-b-error/tsconfig.app.json (the F1+F7 fixture reused for the end-to-end proof: TS2322 + NG8109 in one program)
    - fixtures/gate-b-error/tsconfig.lib.json (the second project-type variant)
    - .planning/phases/02-core-type-check-engine-gatherer/02-RESEARCH.md (Open Question 2 lines 534-537 — the focused-stub D-06 mechanism; V-3 gate on code 500)
    - .planning/phases/02-core-type-check-engine-gatherer/02-PATTERNS.md (gate-b reconcile section lines 108-121; the NG() helper lines 149-151; the "No Analog" D-06 stub note lines 321)
  </read_first>
  <action>
    LW-01 one-line fix in `gather-diagnostics.spec.ts`: change line 1 from `import type { Program } from '@angular/compiler-cli'` to `import type { Program } from './compiler-cli-types'`. No other change to that spec.

    Reconcile `gate-b.spec.ts` (L-8): the timing test (lines 102-103) reads `result.codes`, removed by D-01. Replace with a local derive: `const codes = result.diagnostics.map((diagnostic) => diagnostic.code);` then assert `codes` contains `TS2322` and `NG8109`. The `codesFor()` helper (lines 49-66) maps off raw `performCompilation` result, NOT off `CoreResult`, so it is UNAFFECTED — do not change it. Do NOT delete this file; it remains the differential proof.

    Create `src/core/run-typecheck.integration.spec.ts` (REAL compiler, D-07c — calls `runTypecheck` directly, one performCompilation per fixture). Copy the fixture-path resolution idiom from `gate-b.spec.ts` lines 1-2, 34-39 (`packageRoot`/`workspaceRoot`/`join(workspaceRoot, 'fixtures', 'gate-b-error', ...)`). Define the `NG` helper: `const NG = (code: number): number => -990000 - code;`. Assert against the `gate-b-error` app and lib tsconfigs (use `describe.each` over both, mirroring lines 68-71):
    - ENG-01: `runTypecheck({ tsConfigPath: appTsConfig })` resolves; `result.rootNamesCount > 0`; `result.diagnostics` is an array.
    - ENG-02 (the multi-error F7 proof): `const codes = result.diagnostics.map((d) => d.code)` contains BOTH `2322` (raw TS) AND `NG(8109)` (-998109) in ONE pass — the no-short-circuit differentiator.
    - ENG-04 / D-01: `result.errorCount` counts the TS2322 (assert `result.errorCount >= 1`); `result.errorCount + result.warningCount <= result.diagnostics.length` (the documented invariant); `result.warningCount` reflects the extended Warning-category diagnostics.
    - D-01 no-public-codes: assert at type/runtime level that `'codes' in result === false`.

    Create `src/core/infra-failure.spec.ts` (the single justified Phase-2 mock per RESEARCH Open Q2 — broad mocking is Phase-3 TEST-01). Stub the loaded namespace's `performCompilation` (via `vi` mocking of `./compiler-loader` `loadCompilerCli`, or a local stub) to return `{ diagnostics: [{ code: 500, category: Error, file: undefined, messageText: 'simulated internal crash' }], program: undefined }` and a `readConfiguration` returning a non-empty `rootNames`. Assert `runTypecheck(...)` REJECTS / throws `TypecheckInfrastructureError` (D-06). Add a contrasting case: a stubbed result whose only diagnostic is a normal `2322` (category Error) does NOT throw and is counted in `errorCount` (proves only code-500 is treated as infra, not type errors).
  </action>
  <verify>
    <automated>npx nx build angular-typechecker && npx nx test angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "from '@angular/compiler-cli'" packages/angular-typechecker/src/core/gather-diagnostics.spec.ts` returns NOTHING (LW-01: barrel import gone).
    - `git grep -n "result.codes" packages/angular-typechecker/src/core/gate-b.spec.ts` returns NOTHING (reconciled to `result.diagnostics.map(...)`).
    - `npx nx test angular-typechecker` is green, including: the `run-typecheck.integration.spec.ts` case asserting a single program's codes contain BOTH `2322` AND `-998109`; the invariant `errorCount + warningCount <= diagnostics.length`; and `infra-failure.spec.ts` asserting `runTypecheck` throws on a returned code-500 diagnostic and does NOT throw on a code-2322 diagnostic.
    - The GATE B differential spec still passes: `defaultGatherDiagnostics` codes contain `2322` but NOT `-998109` while the all-getter contains both.
    - `npx nx build angular-typechecker` succeeds before the test run so the GATE A static spec reads fresh `dist/`.
  </acceptance_criteria>
  <done>
    All Phase-1 specs are reconciled to the new contract and green; the new end-to-end integration spec proves the unconditional gather + explicit counts against the real compiler; the focused D-06 spec proves the infra-failure re-throw distinguishes code 500 from a real type error.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend tsconfig.lib.json exclude for all Phase-2 fixture dirs</name>
  <files>packages/angular-typechecker/tsconfig.lib.json</files>
  <read_first>
    - packages/angular-typechecker/tsconfig.lib.json (line 26 already excludes `fixtures/gate-b-error/**/*`; extend additively)
    - .planning/phases/02-core-type-check-engine-gatherer/02-RESEARCH.md (Fixture Catalog lines 317-340 — the new fixture dir names: ts-baseline, ng-baseline, extended, composite-triangle, solution-style, spec-tsconfig)
    - .planning/phases/02-core-type-check-engine-gatherer/02-PATTERNS.md (tsconfig.lib.json EXTEND section lines 266-268)
  </read_first>
  <action>
    Extend the `exclude` array in `tsconfig.lib.json` with the new Phase-2 fixture directory globs so broken fixture `.ts` are never compiled into the published package. Add globs covering every fixture dir slices 02-02 and 02-03 will create. To be future-proof and avoid same-file conflicts with the parallel Wave-2 slices, add a single broad `fixtures/**/*` exclusion entry (in addition to keeping the existing explicit `fixtures/gate-b-error/**/*` line, or replacing it with the broad glob). This makes 02-02 and 02-03 fixture-only file additions safe without re-touching this config.

    Confirm the broad exclude does not remove any `src/**/*.ts` from the lib build (fixtures live at workspace-root `fixtures/`, outside `src/`).
  </action>
  <verify>
    <automated>npx nx build angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "fixtures" packages/angular-typechecker/tsconfig.lib.json` shows a glob that covers all `fixtures/**` (e.g. `fixtures/**/*`), not only `gate-b-error`.
    - `npx nx build angular-typechecker` still succeeds and emits `dist/packages/angular-typechecker/src/index.js` (the published surface is unaffected; only fixtures are excluded).
  </acceptance_criteria>
  <done>
    `tsconfig.lib.json` excludes all `fixtures/**` so Wave-2 slices add fixtures without recompiling broken sources into the package and without re-touching this file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer tsconfig -> runTypecheck | The sole untrusted input is `tsConfigPath` (absolute) + the resolved tsconfig contents. `ng.readConfiguration` parses it; the core never `eval`/execs consumer config. |
| ESM compiler-cli load -> core | A failed `await import('@angular/compiler-cli')` is an environment/install error, not a type result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / Repudiation-of-trust | `runTypecheck` count logic | mitigate | "A type-checker that LIES": MD-02 conflation fixed by explicit category counting (D-01) — `warningCount` is `category === Warning`, never `length - errorCount`, so Suggestion/Message can never be miscounted as warnings or hide errors. Task 1 + the invariant assertion in Task 2. |
| T-02-02 | Tampering / Repudiation-of-trust | `runTypecheck` config handling | mitigate | Dropped `parsed.errors` (MD-01) closed by D-03 prepend; a malformed/unreadable config surfaces as a returned Error diagnostic (non-zero `errorCount`), never a silent clean. Task 1; full guard proof lands in slice 02-02. |
| T-02-03 | Tampering | `performCompilation` no-emit path | mitigate | D-05 full override + `noEmit: true` + `emitFlags: 0` (V-2: both load-bearing) prevent emit side-effects into the consumer tree. Task 1; composite-triangle regression proof in slice 02-03. |
| T-02-04 | Tampering | masked ESM-load / internal crash | mitigate | D-06 / L-3 / V-3: a returned `UNKNOWN_ERROR_CODE` (500) is detected by code (NOT `source === 'angular'`) and RE-THROWN as `TypecheckInfrastructureError`, so an infra crash is never counted as 0/1 type errors. Task 1 implements; Task 2 `infra-failure.spec.ts` proves it. Phase-1 GATE B already asserts `not.toContain(500)`. |
| T-02-05 | Information Disclosure (low) | re-thrown error messageText | accept | The re-thrown infra error carries the compiler's `messageText` (may include absolute paths/stack). Acceptable for a local CI/agent type-checker; output relativization is Phase 3 (OUT-02). Do not log secrets/env. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies this phase (pure code + test against the locked, already-installed toolchain). No supply-chain delta; the s1ngularity/publish-hardening controls are Phase 5. |
</threat_model>

<verification>
- `npx nx build angular-typechecker` succeeds (module:nodenext, CommonJS emit, GATE A `import(` retained).
- `npx nx test angular-typechecker` is green: gatherer unit spec (LW-01 fixed), GATE B differential, GATE A static, the new `run-typecheck.integration.spec.ts`, and `infra-failure.spec.ts`.
- Quick run for fast specs: `npx nx test angular-typechecker -- --exclude '**/*.integration.spec.ts'`.
</verification>

<success_criteria>
- `CoreResult` matches D-01 exactly (no public `codes`; `tsConfigPath` + `rootNamesCount` present; explicit category counts).
- The D-05 override, D-02 suppression, D-03 prepend + zero-rootNames guard, and D-06 re-throw are all implemented and cite their decision IDs in comments.
- The unconditional gatherer surfaces TS2322 AND NG8109 in one real-compiler pass via `runTypecheck`; the documented invariant holds.
- D-06 re-throw is proven for code 500 and NOT triggered by a normal type error.
- `tsconfig.lib.json` excludes all `fixtures/**` so Wave-2 slices are fixture-only additions.
</success_criteria>

<output>
Create `.planning/phases/02-core-type-check-engine-gatherer/02-01-SUMMARY.md` when done.
</output>
