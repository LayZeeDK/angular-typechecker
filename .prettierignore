# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.angular

# GSD planning artifacts are machine-generated (each phase/quick task emits new,
# un-Prettier'd markdown). Prettier does NOT own them: the format-lint gate only
# checks PR-changed files, so gating .planning would force a format pass on every
# code PR's planning churn for zero real benefit. Real source/config/docs outside
# .planning stay Prettier-owned.
/.planning/

# Generated lockfiles -- never reformat (Prettier would churn generated content
# and can break lockfile integrity / the matrix-e2e consumer-workspace install).
package-lock.json
pnpm-lock.yaml
yarn.lock

# cve-lite-cli ratchet baseline -- machine-generated (cve-lite rewrites it in its
# own format on every re-baseline), so Prettier does not own it.
/.cve-lite/

# Whitespace-sensitive diagnostic fixtures -- Angular templates whose exact
# whitespace/reflow changes which NG diagnostics fire and HOW MANY, breaking the
# exact-count assertions in extended-catalog.integration.spec.ts. ONLY the
# templates proven diagnostic-sensitive are listed (verified by reformatting +
# running the suite); other fixture templates stay Prettier-owned.
#   - extended-batch-fn: Prettier reflow drops NG8111 (uninvokedFunctionInEventBinding)
#   - extended-batch-expression: Prettier reflow drops NG8114 (unparenthesizedNullishCoalescing)
/fixtures/extended-batch-fn/error.component.html
/fixtures/extended-batch-expression/error.component.html

# Phase 35 SARIF proof fixture -- the proof component's external template
# whitespace/reflow governs which NG diagnostics fire (NG8002 template-type-check +
# NG8101 extended-diagnostics), the same diagnostic-sensitivity class as the two
# templates above. The fixture's .ts/.json stay Prettier-owned.
/tools/sarif-proof-fixture/proof.component.html

# Committed authentic `ng new` + `ng g library` output (the ng-cli-e2e fixture,
# regenerated via its REGENERATE.md) -- NOT Prettier-owned. Reformatting it would
# destroy Angular CLI output fidelity, so the whole fixture dir is ignored.
/e2e/angular-typechecker-ng-cli-e2e/fixtures/ng-cli-workspace/
