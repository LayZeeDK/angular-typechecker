---
phase: 27-bin-shell-cross-platform-packaging
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - packages/angular-typechecker/src/cli/bin-static.spec.ts
  - e2e/angular-typechecker-install-e2e/src/tarball-audit.e2e.spec.ts
autonomous: true
requirements: [VER-03, PKG-01, CLI-01]
must_haves:
  truths:
    - "The built dist bin.js first line is a `\\r`-free `#!/usr/bin/env node`."
    - "The built bin.js require graph (static transitive walk from bin.js following relative requires) never reaches `@nx/*` or `nx`."
    - "The packed tarball package.json `bin` maps BOTH `angular-typechecker` and `atc` to `./src/cli/bin.js`, that file ships inside the tarball, its first line is a `\\r`-free shebang, and publint --strict passes."
    - "Covers CONTEXT decisions D-10 (bin-static.spec.ts test-tier static guard: shebang byte-check + nx-free require-graph walk on the built bin.js) and D-11 (extend the existing tarball-audit.e2e.spec.ts with the publint bin audit, no new dependency/project)."
  artifacts:
    - path: "packages/angular-typechecker/src/cli/bin-static.spec.ts"
      provides: "VER-03 static guard: shebang byte-check + nx-free require walk on the built bin.js"
      min_lines: 40
    - path: "e2e/angular-typechecker-install-e2e/src/tarball-audit.e2e.spec.ts"
      provides: "Extended with the published-tarball bin map + LF shebang audit (publint covers the bin)"
      contains: "src/cli/bin.js"
  key_links:
    - from: "packages/angular-typechecker/src/cli/bin-static.spec.ts"
      to: "dist/packages/angular-typechecker/src/cli/bin.js"
      via: "project.json targets.build.options.outputPath (NOT a hard-coded dist path)"
      pattern: "build\\.options\\.outputPath"
    - from: "e2e/angular-typechecker-install-e2e/src/tarball-audit.e2e.spec.ts"
      to: "packed tarball package.json bin + shipped src/cli/bin.js"
      via: "npm pack + extract, read manifest.bin + the shipped bin.js first line"
      pattern: "manifest\\.bin"
---

<objective>
Write the two verification guards that prove Phase-27's packaging survived the build and the pack: a `test`-tier static guard on the BUILT `bin.js`, and a published-artifact audit on the PACKED tarball.

Purpose: The build/config work (27-01) is only proven by these standing specs. `bin-static.spec.ts` proves the built `bin.js` has a `\r`-free shebang and an nx-free require graph (VER-03). The tarball-audit extension proves the published `package.json` maps both bin names to a shipped `bin.js` with a clean shebang and that publint passes (PKG-01 / CLI-01 published halves). Both are new coverage over shipped machinery -- re-implementing the existing dist-read / pack-extract scaffolding is the trap.

Output: `src/cli/bin-static.spec.ts` (new) + an extended `tarball-audit.e2e.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-bin-shell-cross-platform-packaging/27-CONTEXT.md
@.planning/phases/27-bin-shell-cross-platform-packaging/27-RESEARCH.md
@.planning/phases/27-bin-shell-cross-platform-packaging/27-PATTERNS.md
@.planning/phases/27-bin-shell-cross-platform-packaging/27-01-SUMMARY.md

<interfaces>
<!-- The dist-read scaffolding to MIRROR (from gate-a-static.spec.ts) + the built require shape. -->

From packages/angular-typechecker/src/executors/typecheck/gate-a-static.spec.ts (the model):
- Imports: `readFileSync` (node:fs), `dirname`/`join` (node:path), `fileURLToPath` (node:url), `describe`/`expect`/`it` (vitest), `findWorkspaceRoot` (@workspace/test-util).
- `packageRoot = join(dirname(fileURLToPath(import.meta.url)), '..', '..', '..')` -- gate-a walks UP 3 (it sits at src/executors/typecheck/). bin-static sits at src/cli/ -> walk UP **2** (`'..','..'`).
- `interface BuildTarget { options: { outputPath: string } }` + `interface ProjectJson { targets: { build: BuildTarget } }` (reuse verbatim).
- `distRoot = join(workspaceRoot, projectJson.targets.build.options.outputPath)` -- derive from project.json, do NOT hard-code `dist/...`.
- `function stripCommentLines(code)` -- filters full-line `//`/`*`/`/*` lines (copy verbatim).

Built CJS require shape (confirmed 2026-07-16, double-quoted, extensionless, relative):
- `const run_typecheck_1 = require("../core/run-typecheck");`  (relative -> follow)
- `const tslib_1 = require("tslib");`  (bare -> check, do not follow)
- `const node_fs_1 = require("node:fs");`  (builtin -> check, do not follow)
- nx pattern to assert-absent: `/^(@nx\/|nx\/|nx$)/` (matches @nx/devkit, nx, nx/...; NOT node:*, tslib, typescript, @angular/*). @angular/compiler-cli never appears (reached via `await import()`).

From e2e/angular-typechecker-install-e2e/src/tarball-audit.e2e.spec.ts (the spec to EXTEND):
- `beforeAll` packs fresh dist via `npm pack --json --pack-destination`, sets `filePaths` (package-relative, no `package/` prefix), extracts into `extractDir/package`.
- `REQUIRED_FILES` (lines 34-52) = the positive file-set the "ships the required published files" `it` iterates.
- `interface TarballManifest { scripts?: Record<string, string>; }` (line 81-83) -- extend it.
- The install-scripts `it` (lines 215-224) reads `join(extractDir, 'package', 'package.json')` -> the manifest-read pattern to mirror.
- The existing `npx publint "<tgz>" --strict` assertion (lines 162-172) already runs; publint's `bin` rule requires the referenced file to start with a shebang.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: bin-static.spec.ts -- shebang + nx-free require-graph guard (VER-03)</name>
  <files>packages/angular-typechecker/src/cli/bin-static.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/executors/typecheck/gate-a-static.spec.ts (the EXACT model: packageRoot/workspaceRoot/distRoot derivation lines 24-51, BuildTarget/ProjectJson interfaces lines 32-42, stripCommentLines lines 81-86, the readFileSync -> stripCommentLines -> expect assertion shape lines 88-118)
    - packages/angular-typechecker/src/cli/bin.ts (the file under guard -- confirm its shebang + single `./main` import)
    - packages/angular-typechecker/project.json (confirm `targets.build.options.outputPath` + that `test` has `dependsOn: ["build"]`)
    - .planning/phases/27-bin-shell-cross-platform-packaging/27-PATTERNS.md (the bin-static.spec.ts section, lines 102-164)
    - .planning/phases/27-bin-shell-cross-platform-packaging/27-RESEARCH.md (the VER-03 static guard section, lines 367-425)
  </read_first>
  <action>
    Create `src/cli/bin-static.spec.ts` mirroring `gate-a-static.spec.ts`. Reuse its dist-read scaffolding: same imports, the `BuildTarget`/`ProjectJson` interfaces verbatim, `stripCommentLines` verbatim, and `distRoot = join(workspaceRoot, projectJson.targets.build.options.outputPath)`. THE ONE DIFFERENCE: `packageRoot = join(dirname(fileURLToPath(import.meta.url)), '..', '..')` -- walk UP 2 (src/cli -> src -> packageRoot), NOT 3. Compute `binJsPath = join(distRoot, 'src', 'cli', 'bin.js')`.

    Assertion family (a) -- shebang first-line byte-check: read `binJsPath` with `readFileSync(path, 'utf8')`, split on `\n`, assert `content.split('\n')[0] === '#!/usr/bin/env node'` (a trailing `\r` makes it `'#!/usr/bin/env node\r'` and fails equality) AND `expect(firstLine).not.toContain('\r')`.

    Assertion family (b) -- static transitive nx-free require-graph walk: a small recursive function starting at `binJsPath` with a `visited` Set of absolute paths. Per file: `readFileSync` -> `stripCommentLines` -> extract every specifier via `/require\(\s*["']([^"']+)["']\s*\)/g`. Assert NO specifier matches `/^(@nx\/|nx\/|nx$)/`, failing with the offending file + specifier if any does. FOLLOW only relative specifiers (start with `.`): resolve `join(dirname(file), spec + '.js')`, recurse if unvisited. Bare/builtin specifiers (`tslib`, `node:*`, `typescript`, `@angular/*`) are CHECKED but not followed. The walk terminates (relative requires form a DAG within dist/.../src/); `@angular/compiler-cli` never appears (it is reached via `await import()`), so no special-casing.

    Static/test-tier ONLY. Do NOT add a runtime `require.cache` probe (that is Phase 28 / VER-04). No cold-start-budget assertion (deferred). No `project.json` change -- the `test` tier's Vitest glob picks the file up and `test` `dependsOn: ["build"]` builds dist first.
  </action>
  <acceptance_criteria>
    - `packages/angular-typechecker/src/cli/bin-static.spec.ts` exists and derives `distRoot` from `project.json` `build.options.outputPath` (no hard-coded `dist/...` string).
    - `packageRoot` walks UP exactly 2 levels (`'..','..'`).
    - It asserts the built `bin.js` first line === `#!/usr/bin/env node` AND `.not.toContain('\r')`.
    - It walks the built `bin.js` require graph and asserts no specifier matches `/^(@nx\/|nx\/|nx$)/`.
    - `nx test angular-typechecker` is green INCLUDING bin-static.spec.ts (build runs first via dependsOn).
    - `nx lint angular-typechecker` stays green at maxWarnings:0 (the spec's imports clear the src/cli/** ban).
  </acceptance_criteria>
  <verify>
    <automated>nx test angular-typechecker</automated>
  </verify>
  <done>bin-static.spec.ts asserts a `\r`-free `#!/usr/bin/env node` shebang and an nx-free require graph on the built bin.js, riding the `test` tier with no project.json change.</done>
</task>

<task type="auto">
  <name>Task 2: Extend tarball-audit.e2e.spec.ts with the published bin audit (PKG-01 / CLI-01)</name>
  <files>e2e/angular-typechecker-install-e2e/src/tarball-audit.e2e.spec.ts</files>
  <read_first>
    - e2e/angular-typechecker-install-e2e/src/tarball-audit.e2e.spec.ts (the whole existing spec: beforeAll pack/extract lines 114-151, REQUIRED_FILES lines 34-52, TarballManifest interface lines 81-83, the publint assertion lines 162-172, the manifest-read pattern in the install-scripts it lines 215-224, the ships-required-files it lines 192-196)
    - packages/angular-typechecker/package.json (confirm the `bin` map added in 27-01 -> both names to ./src/cli/bin.js)
    - .planning/phases/27-bin-shell-cross-platform-packaging/27-PATTERNS.md (the tarball-audit section, lines 279-308)
    - .planning/phases/27-bin-shell-cross-platform-packaging/27-RESEARCH.md (the PKG-01 publint tarball audit section, lines 472-511)
  </read_first>
  <action>
    EXTEND the existing spec -- do NOT create a new file, do NOT touch the pack/extract scaffolding.

    (1) Add `'src/cli/bin.js'` to `REQUIRED_FILES` so the existing "ships the required published files" `it` asserts the compiled bin ships. (The leak guard already excludes `.spec.`, and `bin-static.spec.ts` is excluded from the tarball by `tsconfig.lib.json`, so no leak.)

    (2) Extend the `TarballManifest` interface with `bin?: Record<string, string>;` (keep the existing `scripts?`).

    (3) Add a NEW describe/it block with a DISTINCT heading -- e.g. `CLI-01/PKG-01: the packed tarball ships a runnable bin` (the existing `describe('PKG-02: ...')` at line 161 is a v0.2.0 id collision; do not reuse it). In it, read `join(extractDir, 'package', 'package.json')` (mirror the install-scripts manifest-read) and assert `manifest.bin?.['angular-typechecker'] === './src/cli/bin.js'` AND `manifest.bin?.['atc'] === './src/cli/bin.js'`. Assert `filePaths` contains `'src/cli/bin.js'` (mirror `expect(filePaths).toContain(required)`). Then read `join(extractDir, 'package', 'src', 'cli', 'bin.js')` and assert `content.split('\n')[0] === '#!/usr/bin/env node'` AND `.not.toContain('\r')` (the PUBLISHED-artifact half of the shebang guard; the dist half is 27-02 Task 1).

    (4) publint coverage is automatic once `bin` exists -- the existing `npx publint "<tgz>" --strict` assertion already runs, and publint's `bin` rule requires the referenced file to start with a shebang, so a missing/CRLF/mispathed bin fails publint. Keep the assertion; add no new publint wiring.

    Apply the repo JS/TS style (blank lines around control flow) to the new block.
  </action>
  <acceptance_criteria>
    - `REQUIRED_FILES` includes `'src/cli/bin.js'`.
    - `TarballManifest` has `bin?: Record<string, string>` (and still `scripts?`).
    - A new describe/it block (heading NOT `PKG-02`) asserts the packed manifest `bin` maps `angular-typechecker` AND `atc` both to `./src/cli/bin.js`, that `filePaths` contains `src/cli/bin.js`, and that the shipped `src/cli/bin.js` first line === `#!/usr/bin/env node` with `.not.toContain('\r')`.
    - `nx e2e angular-typechecker-install-e2e` is green (pack + extract + publint + bin audit).
    - The existing publint/attw/leak/install-scripts assertions still pass (no regression to the shared scaffolding).
  </acceptance_criteria>
  <verify>
    <automated>nx e2e angular-typechecker-install-e2e</automated>
  </verify>
  <done>The tarball audit proves the packed manifest maps both bin names to a shipped `./src/cli/bin.js` whose first line is a `\r`-free shebang, and publint --strict passes for the bin -- closing PKG-01's published-artifact half in-phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build host -> dist bin.js | The static guard reads the BUILT bytes to prove the shebang + nx-free graph survived emit. |
| dist -> packed tarball (published artifact) | `npm pack` applies the `files` allowlist; the audit reads the REAL shipped manifest + bin.js, not the source tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05 | Tampering | A CRLF shebang or nx-in-graph regression shipping undetected | mitigate | THIS plan is the mitigation: the dist byte-check + require walk (Task 1) and the published-tarball shebang + publint audit (Task 2) fail LOUDLY on any regression. |
| T-27-06 | Tampering / Malicious Code | An install lifecycle script (`postinstall` etc.) sneaking into the published tarball (the s1ngularity payload vector) | accept (already guarded) | The existing `INSTALL_SCRIPT_KEYS` tarball-audit assertion is unchanged and stays green -- the bin adds no script. |
| T-27-07 | Denial of Service | The published bin dragging `@nx/*`/nx transitively (crash on a non-Nx consumer) | mitigate | The static require-graph walk (Task 1) proves the built `bin.js` graph is nx-free; the runtime `require.cache` probe on the INSTALLED bin is Phase 28 (VER-04). |
| T-27-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this phase. `publint@0.3.21` + `@arethetypeswrong/cli@0.18.4` are already root devDependencies (verified); nothing new to slopcheck. |

No HIGH-severity threat: the specs are read-only audits of the build/pack output; they introduce no runtime surface, no network, no secrets.
</threat_model>

<verification>
- `nx test angular-typechecker` green INCLUDING bin-static.spec.ts (built bin.js shebang + nx-free walk).
- `nx e2e angular-typechecker-install-e2e` green INCLUDING the new bin describe block + the unchanged publint/attw/leak/install-scripts assertions.
- `nx lint angular-typechecker` green at maxWarnings:0 (bin-static.spec.ts clears the src/cli/** ban).
</verification>

<success_criteria>
- The built `bin.js` starts with a `\r`-free `#!/usr/bin/env node` and its require graph never reaches `@nx/*`/nx (VER-03).
- The packed tarball `bin` maps both names to a shipped `./src/cli/bin.js` with a clean shebang and publint passes (PKG-01, CLI-01 published halves).
</success_criteria>

<output>
Create `.planning/phases/27-bin-shell-cross-platform-packaging/27-02-SUMMARY.md` when done.
</output>
