---
phase: quick-260630-fg0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/core/filter-diagnostics.ts
  - packages/angular-typechecker/src/core/filter-diagnostics.spec.ts
  - packages/angular-typechecker/src/core/run-typecheck.ts
  - packages/angular-typechecker/src/core/diagnostic-codes.ts
  - packages/angular-typechecker/src/core/compiler-cli-types.ts
  - packages/angular-typechecker/src/executors/angular-typecheck/executor.spec.ts
  - packages/angular-typechecker/src/core/infra-failure.spec.ts
  - packages/angular-typechecker/src/core/run-typecheck.spec.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - 'A throwing realpath KEEPS the diagnostic (never suppresses it); a symlink whose realpath would resolve in-project can no longer cause a false PASS (#1).'
    - 'The prior T1 test asserting suppress-on-throw is INVERTED to keep-on-throw in the SAME commit, so the suite is green (T1 coupling).'
    - 'A structurally-absent Program (no 500 diagnostic) raises a TypecheckInfrastructureError instead of a bare TypeError (#3 defense-in-depth).'
    - "diagnostic-codes.ts no longer claims detection scans the post-filter 'reported set'; it documents the PRE-filter gathered set in finalize (#2)."
    - 'compiler-cli-types.ts no longer pins a drifting run-typecheck.ts line number for the emitFlags cast (S1).'
    - 'TemplateCheckAborted.code is documented as retained/contract-pinned and is NOT dropped (S2).'
    - 'errorCount 0 + templateCheckAborted set still yields { success: true } with a logger.warn (advisory-not-verdict, pinned by a new test) (S3).'
    - 'Added tests pin: config-500 with non-empty rootNames (S5a), the explicit warningCount split via a mixed Error+Warning set (S5c), and the .ngtypecheck.tsx pass-through (S5d).'
  artifacts:
    - path: 'packages/angular-typechecker/src/core/filter-diagnostics.ts'
      provides: 'Canonicalizer returns undefined on a throwing realpath; loop keeps that diagnostic'
      contains: 'string | undefined'
    - path: 'packages/angular-typechecker/src/core/run-typecheck.ts'
      provides: 'program-undefined defense-in-depth guard + S2 doc note'
      contains: 'result.program === undefined'
    - path: 'packages/angular-typechecker/src/core/diagnostic-codes.ts'
      provides: 'Corrected PRE-filter gathered-set comments'
      contains: 'PRE-filter'
  key_links:
    - from: 'filter-diagnostics.ts createCanonicalizer catch'
      to: 'filterDiagnostics per-diagnostic loop'
      via: 'undefined sentinel return -> canonicalFile === undefined keep+continue'
      pattern: 'canonicalFile === undefined'
    - from: 'filter-diagnostics.ts keep-on-throw fix'
      to: 'filter-diagnostics.spec.ts T1 test'
      via: 'same-commit inversion to kept.length 1 / suppressedCount 0'
      pattern: 'RES-03.*KEPT'
---

<objective>
Address the second-round `/pr-review-toolkit:review-pr` findings on PR #11 (branch
`gsd/v0.0.3-engine-hardening`, HEAD 13aa9ff). One correctness edge fix (#1 realpath
keep-not-suppress, which inverts one prior test), two comment/guard fixes (#2, #3), two
documentation polish edits (S1, S2), and four additive pinning tests (S3, S5a, S5c, S5d).

Purpose: close the only behavioral false-PASS edge surfaced by the review (a symlink whose
realpath throws was being SUPPRESSED, masking an error), tighten an infra-class reachability
guard, and correct/strengthen documentation + test coverage so the engine's invariants are
pinned. All changes are surgical and confined to `packages/angular-typechecker/src/**`.

Output: corrected `filter-diagnostics.ts` (+ inverted T1 test), a defense-in-depth guard and
two doc edits in core, and four new tests across three spec files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Runs on the MAIN TREE (single-plan wave -- per AGENTS.md "Single-plan wave: skip worktrees").
No worktree isolation, no `node_modules` junction; the main checkout has real deps so the
executor can run `npx nx test angular-typechecker` directly.

ASCII-only in all source, comments, and commit messages (no em/en dashes, curly quotes,
ellipsis, emoji). JS/TS style: blank lines around control flow and `return`; always braces
on control-flow bodies. Preserve `src/core/**` purity -- NO `console`, NO `process`, NO
logging added in core (eslint bans both under `**/src/core/**`).

Commit on the current branch into PR #11. Conventional Commits, clean release-meaningful
scopes (`core`), NO quick-id in scopes, NO AI attribution trailers.
</execution_context>

<context>
@.planning/quick/260630-fg0-address-second-round-pr-review-findings/260630-fg0-CONTEXT.md
@.planning/quick/260630-fg0-address-second-round-pr-review-findings/260630-fg0-RESEARCH.md

<interfaces>
<!-- Verified against the working tree at HEAD 13aa9ff. Executor uses these directly; no codebase exploration needed. -->

filter-diagnostics.ts (current shape to edit):

- `createCanonicalizer(options): (filePath: string) => string` at :115; catch at :131-138 sets `resolved = filePath` at :137.
- Per-diagnostic loop :78-103; file-less keep idiom at :85-89 (`kept.push(diagnostic); continue;`); `const canonicalFile = canonicalize(diagnostic.file.fileName)` at :91; node_modules/isUnderDir branch at :93-100 (`suppressedCount++` at :97); in-project `kept.push` at :102.

run-typecheck.ts (current shape to edit):

- Post-compilation 500 scan: :240-248 (`const infrastructureFailure = result.diagnostics.find(...)` then `if (infrastructureFailure !== undefined) throw new TypecheckInfrastructureError(ts.flattenDiagnosticMessageText(...))`).
- `return finalize(...)` begins at :256; first `result.program` deref at :268-270 (`useCaseSensitiveFileNames: result.program.getTsProgram().useCaseSensitiveFileNames()`).
- Nothing between :248 and :268 reads `result.program` (verified) -- insert the #3 guard after the 500-scan `if` closes (after :248) and before `return finalize(...)` (:256).
- `TemplateCheckAborted` interface :80-83; `code: number;` at :81; existing doc comment :73-79.
- `TypecheckInfrastructureError` class :99-104. Existing infra throws use `ts.flattenDiagnosticMessageText(<diag>.messageText, '\n')`; the #3 guard has NO diagnostic, so it passes a LITERAL `angular-typecheck:`-prefixed message (matches the synthesized zero-rootNames messages and the executor's infra prefix).

diagnostic-codes.ts (comments to edit):

- :71 `* the reported set is the signal that drives the loud RES-02 suppression notice.`
- :86 `* scans the reported set for this exact value to flag the template-check abort.`

compiler-cli-types.ts (comment to edit):

- :98 `* (\`emitFlags: 0 as EmitFlags\`, run-typecheck.ts:229) -- a bare \`: EmitFlags = 0\`` (the actual statement is at run-typecheck.ts:232; :229 is the comment block start).

executor.spec.ts (test harness to reuse):

- Hoisted `mocks` :10-29 (`runTypecheck`, `renderReport`, `evaluateResult`, `normalizeOptions`, `loggerError/loggerInfo/loggerWarn`); `@nx/devkit` logger mock :55-64.
- `coreResult(errorCount): CoreResult` :66-76 (suppressedCount 0, warningCount 0).
- `abortedCoreResult(fileName): CoreResult` :80-85 (`{ ...coreResult(1), templateCheckAborted: { code: -993004, fileName } }`).
- Existing RES-02 warn tests :147-193 (assert `mocks.loggerWarn` / `mocks.loggerError` with `expect.stringContaining`).

infra-failure.spec.ts (test harness to reuse):

- `compilerCliStub` :25-36 (`readConfiguration`, `performCompilation`); default `readConfiguration` returns `rootNames: ['/virtual/error.component.ts']`.
- `errorDiagnostic(code, message): ts.Diagnostic` :52-61 (file-less, `category: 1`).
- `fileDiagnostic(code, fileName)` :66-75; `fakeProgram(): unknown` :81-87.
- Main describe :89-226; COR-01 config-resolution describe :239-316 (the 500-scan + 5012 cases; reuse the `readConfiguration.mockReturnValue({...})` shape at :248-265).

run-typecheck.spec.ts (test harness to reuse):

- `detectTemplateCheckAborted` + `resolveFilterBasePath` imports :11-14; `TCB_GENERATION_FATAL_DIAGNOSTIC_CODE` import :9.
- `diagnostic(code, fileName?): ts.Diagnostic` :63-73 inside the `detectTemplateCheckAborted (RES-02 reframe)` describe :57-164.
- Existing `.ngtypecheck.ts` positive test :109-124; non-shim leave-unchanged test :126-137.
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: keep-on-throw realpath fix (#1) + inverted T1 test</name>
  <files>packages/angular-typechecker/src/core/filter-diagnostics.ts, packages/angular-typechecker/src/core/filter-diagnostics.spec.ts</files>
  <behavior>
    - A throwing `options.realpath()` now causes the canonicalizer to return `undefined` (not the raw fallback path), and the per-diagnostic loop KEEPS that diagnostic (`kept.length === 1`, `suppressedCount === 0`) -- a throw cannot prove out-of-project, so the fail-safe bias keeps it.
    - The in-project keep companion test (spec :114-126) and the non-throwing pnpm-symlink success test (spec :88-106) stay green unchanged.
    - The success path (memoization + `\\`->`/` normalize + case-fold) is byte-unchanged: every non-throwing test (out-of-project / case-fold / store-segment, the 11 other suppression assertions) stays green.
  </behavior>
  <action>
    In `filter-diagnostics.ts`: widen `createCanonicalizer`'s return type from `(filePath: string) => string` to `(filePath: string) => string | undefined` (declaration at :115-117 and the inner returned arrow at :120). In the catch block (:131-138), REPLACE the raw fallback `resolved = filePath;` (:137) with `return undefined;`. Do NOT cache `undefined` (a transient EACCES could resolve on a later call; the loop is single-pass so not caching the failure is the conservative choice). Reframe the existing catch comment to the locked intent: a throwing realpath cannot PROVE out-of-project, so signal `undefined` and let the caller KEEP the diagnostic (fail-safe); still silent because core is PURE (no console/process). Leave the SUCCESS path untouched: realpath resolves -> normalize -> case-fold -> `cache.set` -> return canonical.

    In the `filterDiagnostics` loop, after the canonicalize call at :91, add a guard BEFORE the node_modules/isUnderDir branch (:93) that mirrors the file-less keep idiom at :85-89: when `canonicalFile === undefined`, `kept.push(diagnostic)` then `continue` (blank line between the push and continue, per JS/TS style). This MUST NOT reach the `suppressedCount++` at :97 -- the diagnostic is kept, identical accounting to the file-less keep. `canonicalBase` at :73 also calls the canonicalizer; for completeness null-guard it too -- but because the loop `continue`s on `canonicalFile === undefined` first, a `undefined` base only matters when a file DID resolve, which is the over-keep-safe direction. Keep the base guard minimal (it is not load-bearing for #1's correctness); do not over-engineer.

    SAME COMMIT -- INVERT the prior T1 test in `filter-diagnostics.spec.ts` at :135-147 (added last round, commit 6e890eb). This is the load-bearing coupling: shipping the fix without flipping T1 leaves a red suite. Flip `expect(result.kept).toHaveLength(0)` (:145) to `toHaveLength(1)` and `expect(result.suppressedCount).toBe(1)` (:146) to `toBe(0)`. Rename the `it(...)` title (:135) to reflect KEEP, e.g. `'RES-03: a throwing realpath is caught and the diagnostic is KEPT (cannot prove out-of-project, fail-safe)'`. Rewrite the comment block at :128-134 to: a throwing realpath cannot PROVE the file is out-of-project, so the canonicalizer signals `undefined` and the diagnostic is KEPT (fail-safe bias for a correctness tool); accepts a minor over-keep (a genuinely out-of-project file whose realpath throws is now reported) which is the correct direction -- never silently drop a diagnostic on an unprovable boundary; this is a failing-then-passing change (pre-fix it asserted kept 0 / suppressed 1, the buggy behavior). Keep the existing throwing stub and the `/ws/sibling-lib/src/b.ts` input unchanged.

    Implements CONTEXT #1 (locked Option A refined) and its MANDATORY coupled T1 inversion. ASCII-only; preserve core purity.

  </action>
  <verify>
    <automated>npx nx test angular-typechecker --testFile=filter-diagnostics.spec.ts</automated>
    All filterDiagnostics tests pass, including the renamed RES-03 KEEP test (kept 1 / suppressed 0), the in-project KEEP companion (kept 1 / suppressed 0), and every non-throwing suppression test unchanged.
  </verify>
  <done>
    `createCanonicalizer` returns `string | undefined`; the catch returns `undefined`; the loop keeps a `canonicalFile === undefined` diagnostic without incrementing `suppressedCount`; the T1 spec asserts kept 1 / suppressed 0 with the rewritten title + comment; the full filter-diagnostics suite is green.
  </done>
</task>

<task type="auto">
  <name>Task 2: program-undefined guard (#3) + S1/S2 documentation edits + #2 comment fix</name>
  <files>packages/angular-typechecker/src/core/run-typecheck.ts, packages/angular-typechecker/src/core/diagnostic-codes.ts, packages/angular-typechecker/src/core/compiler-cli-types.ts</files>
  <action>
    #3 guard (run-typecheck.ts): insert a `result.program === undefined` guard AFTER the post-compilation 500-scan `if` block closes (after :248) and BEFORE `return finalize(...)` (:256) -- nothing between :248 and :268 reads `result.program`, so this is the correct seam. The guard throws `new TypecheckInfrastructureError(<literal message>)` with an `angular-typecheck:`-prefixed literal (NO diagnostic to flatten, unlike the two existing 500-scan throws which flatten `messageText`) stating the compiler returned no Program -- that `performCompilation` produced neither a Program nor an `UNKNOWN_ERROR_CODE` diagnostic, and that this is an infrastructure failure, not a type error. The message wording must match the style of the existing infra throws and the synthesized zero-rootNames messages (literal `angular-typecheck:` prefix). Frame the guard comment as DEFENSE-IN-DEPTH: the real `PerformCompilationResult.program` is OPTIONAL in `@angular/compiler-cli` (perform_compile.d.ts:29) while the vendored shim narrows it non-optional (compiler-cli-types.ts:182-185) to match the engine's guarded usage; a `{ program: undefined }` return WITHOUT a 500 diagnostic is type-permitted but NOT observed in v22.0.4 source; this guard converts that hypothetical bare TypeError into the SAME infra-class failure, and is DISJOINT from the post-compilation 500 scan above (which handles UNKNOWN_ERROR_CODE) so there is no double-handling. Per CONTEXT #3, this is a RUNTIME defense, NOT a type change -- KEEP the shim `program` non-optional (do not touch compiler-cli-types.ts:182-185); TS will treat the access as always-defined, which is fine. Use braces + blank lines per JS/TS style.

    #2 (diagnostic-codes.ts): fix the stale "reported set" comments at :71 and :86. Detection scans the PRE-filter gathered set in `finalize` (the raw `diagnostics` arg it receives -- run-typecheck.ts passes `diagnostics`, NOT the post-boundary-filter `reported` set; the in-code comment at run-typecheck.ts:406-418 is already correct). Reword both lines to say the detection keys off "the PRE-filter gathered set (the raw `diagnostics` `finalize` receives), NOT the post-boundary-filter `reported` set" so this sibling file matches run-typecheck.ts's already-correct wording.

    S1 (compiler-cli-types.ts): de-pin the drifting line number at :98. The text currently reads `(\`emitFlags: 0 as EmitFlags\`, run-typecheck.ts:229)`; the actual statement is at run-typecheck.ts:232 (:229 is the comment block start). Per CONTEXT (locked "replace the line pin with a symbol reference"), do NOT change it to `:232` -- replace the line pin with a SYMBOL reference, e.g. ``(`emitFlags: 0 as EmitFlags` at the `performCompilation` call site in run-typecheck.ts)`` so the comment no longer carries a brittle line number.

    S2 (run-typecheck.ts): DOCUMENT, do NOT drop `TemplateCheckAborted.code` (the `code: number;` field at :81). A reviewer flagged it as adapter-unused (the executor reads only `.fileName`), but it IS asserted by infra-failure.spec.ts:219-221 and run-typecheck.spec.ts:94-97 -- dropping it breaks both. Add a one-line note at/above :81 that `code` is RETAINED as the detector's public shape (always `NG(3004) === -993004` at v22.0.4), pinned by the detector/drift tests, even though the current adapter consumes only `fileName`. The existing doc comment at :73-79 already documents WHAT `code` is; S2 adds WHY it is kept despite being adapter-unused. The field MUST remain present.

    All edits ASCII-only; no `console`/`process` added (core purity preserved -- all changes are comments plus one runtime throw of the existing infra error class).

  </action>
  <verify>
    <automated>npx nx test angular-typechecker --testFile=infra-failure.spec.ts && npx nx test angular-typechecker --testFile=run-typecheck.spec.ts</automated>
    Existing infra-failure and run-typecheck suites stay green (the `TemplateCheckAborted.code` assertions at infra-failure.spec.ts:219-221 and run-typecheck.spec.ts:94-97 still pass; the #3 guard does not regress the existing 500-scan / fakeProgram paths). A full `npx nx build angular-typechecker` succeeds (the drift guard confirms the shim `program` stays non-optional and the new throw type-checks).
  </verify>
  <done>
    run-typecheck.ts has the #3 `result.program === undefined` guard (defense-in-depth comment, literal `angular-typecheck:` message, disjoint-from-500-scan) inserted between :248 and :256, plus the S2 retention note above `TemplateCheckAborted.code` (field NOT dropped); diagnostic-codes.ts:71 and :86 say "PRE-filter gathered set"; compiler-cli-types.ts:98 uses a symbol reference instead of `run-typecheck.ts:229`; build + the two existing suites are green.
  </done>
</task>

<task type="auto">
  <name>Task 3: additive pinning tests (S3, S5a, S5c, S5d)</name>
  <files>packages/angular-typechecker/src/executors/angular-typecheck/executor.spec.ts, packages/angular-typechecker/src/core/infra-failure.spec.ts, packages/angular-typechecker/src/core/run-typecheck.spec.ts</files>
  <action>
    S3 (executor.spec.ts -- NOT evaluate-result.spec.ts; `logger.warn` is the executor's responsibility, never reached from `evaluateResult`): add ONE test pinning the locked advisory-not-verdict policy (09-RES-02-DECISION.md): `errorCount 0 + templateCheckAborted set -> { success: true }` STILL emits the loud `logger.warn`. The existing `abortedCoreResult` builds on `coreResult(1)` (errorCount 1); S3 pins the DISTINCT abort-with-ZERO-errors case. Inline a `{ ...coreResult(0), templateCheckAborted: { code: -993004, fileName: '/ws/libs/x/poison.component.ts' } }` as the `mocks.runTypecheck.mockResolvedValue(...)`, stub `mocks.evaluateResult.mockReturnValue({ success: true })` (models `evaluateResult` seeing errorCount 0 -- the executor delegates the verdict to it), import + call the executor, then assert: `expect(result).toEqual({ success: true })` (verdict NOT forced false by the abort), `expect(mocks.loggerWarn).toHaveBeenCalledOnce()` (the notice still fires), `expect(mocks.loggerError).not.toHaveBeenCalled()` (not an infra error). Do NOT add a builder if an inline literal is cleaner; reuse the hoisted mocks + `coreResult`. Do NOT force `success: false` -- that contradicts the locked policy.

    S5a (infra-failure.spec.ts, COR-01 describe :239-316): add a sibling to the existing config-500 test that uses NON-empty `rootNames` (the existing one uses `rootNames: []`). Set `compilerCliStub.readConfiguration.mockReturnValue({...})` with `rootNames: ['/virtual/error.component.ts']` AND a single code-500 `errorDiagnostic(UNKNOWN_ERROR_CODE, ...)` in `errors` (reuse the shape at :248-265). Assert `rejects.toBeInstanceOf(TypecheckInfrastructureError)` AND `expect(compilerCliStub.performCompilation).not.toHaveBeenCalled()` -- proving the config-500 scan (run-typecheck.ts:163-174) fires regardless of rootNames (it precedes the zero-rootNames guard and short-circuits before performCompilation). Pins the scan as rootNames-INDEPENDENT.

    S5c (infra-failure.spec.ts, main describe :89-226): add a `warningDiagnostic(code, message)` builder modeled on `errorDiagnostic` (:52-61) but with `category: 0 /* ts.DiagnosticCategory.Warning */` (do NOT overload `errorDiagnostic` or `fileDiagnostic`). Add a test that stubs `compilerCliStub.performCompilation.mockReturnValue({ diagnostics: [errorDiagnostic(2322, ...), warningDiagnostic(<some-warning-code>, ...)], program: fakeProgram() })` and asserts `result.errorCount === 1` AND `result.warningCount === 1` -- exercising the EXPLICIT category split in finalize (run-typecheck.ts:399-404) and guarding the MD-02 `length - errorCount` anti-bug (current multi-diagnostic tests are all-Error). `finalize` is PRIVATE, so this drives it through `runTypecheck` via the stubbed `performCompilation` (the only harness that can return a mixed-category set). Use FILE-LESS builders so the boundary filter keeps both regardless and the count logic is isolated.

    S5d (run-typecheck.spec.ts, `detectTemplateCheckAborted` describe :57-164): add a `.ngtypecheck.tsx` pass-through negative case using the existing `diagnostic(code, fileName?)` builder (:63-73). The shim regex is `/\.ngtypecheck\.ts$/` (`.ts$`-anchored), so a `.ngtypecheck.tsx` input does NOT match and passes through UNCHANGED. Assert `detectTemplateCheckAborted([diagnostic(TCB_GENERATION_FATAL_DIAGNOSTIC_CODE, '/ws/app/poison.component.ngtypecheck.tsx')])?.fileName` equals `'/ws/app/poison.component.ngtypecheck.tsx'` (verbatim, NOT stripped). Pins the `$` anchor; mirrors the existing `.ngtypecheck.ts` positive test (:109-124) and the non-shim leave-unchanged test (:126-137). Reference the documented LIMITATION (run-typecheck.ts:476-483) that `.tsx` sources collapse to `<name>.ngtypecheck.ts`, so this is a negative-case anchor guard.

    All new tests ASCII-only; follow JS/TS style (blank lines around control flow/returns, braces). Reuse existing hoisted mocks and builders verbatim where possible.

  </action>
  <verify>
    <automated>npx nx test angular-typechecker --testFile=executor.spec.ts && npx nx test angular-typechecker --testFile=infra-failure.spec.ts && npx nx test angular-typechecker --testFile=run-typecheck.spec.ts</automated>
    The new S3 test (abort + errorCount 0 -> { success: true } + loggerWarn once + no loggerError), S5a (config-500 + non-empty rootNames rejects + performCompilation never called), S5c (errorCount 1 / warningCount 1 from a mixed set), and S5d (.ngtypecheck.tsx pass-through unchanged) all pass alongside the existing tests.
  </verify>
  <done>
    Four new tests exist and pass: S3 in executor.spec.ts (advisory-not-verdict pin), S5a + S5c in infra-failure.spec.ts (config-500 rootNames-independence + the warningCount split with a new `warningDiagnostic` builder), and S5d in run-typecheck.spec.ts (.ngtypecheck.tsx pass-through). No existing test regressed.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                  | Description                                                                                              |
| ----------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| consumer filesystem -> core canonicalizer | `realpath` may throw (EACCES / broken symlink / permission-denied junction) on consumer-controlled paths |
| `@angular/compiler-cli` -> core engine    | `performCompilation` return shape (`program` optional in the real types) crosses into the engine         |

## STRIDE Threat Register

| Threat ID | Category               | Component                              | Disposition | Mitigation Plan                                                                                                                                                                                      |
| --------- | ---------------------- | -------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-fg0-01  | Tampering              | filter-diagnostics.ts canonicalizer    | mitigate    | #1 keep-on-throw: a throwing realpath can no longer suppress a real diagnostic (false PASS). Fail-safe bias KEEPS the diagnostic; pinned by the inverted T1 test.                                    |
| T-fg0-02  | Denial of Service      | run-typecheck.ts post-compilation path | mitigate    | #3 guard converts a structurally-absent `program` (type-permitted but unobserved) from a bare TypeError into a TypecheckInfrastructureError the executor maps to a clean infra failure, not a crash. |
| T-fg0-03  | Information Disclosure | core/\*\* purity                       | accept      | No `console`/`process`/logging added in core (eslint-enforced under `**/src/core/**`); the keep-on-throw signal is a pure return value. Low risk: no PII, internal dev tool.                         |
| T-fg0-SC  | Tampering              | npm/pip/cargo installs                 | accept      | No package-manager installs in this task -- pure source + test edits on the existing dependency set (Pattern A / no `package.json` change). No legitimacy gate needed.                               |

</threat_model>

<verification>
Run the full project suite plus build (authoritative signal per CLAUDE.md "TypeScript LSP
diagnostics are not authoritative"; ignore stale `new-diagnostics` noise):

```
npx nx test angular-typechecker
npx nx build angular-typechecker
```

- All tests green, including the inverted T1 (RES-03 KEEP) and the four new pinning tests.
- The build (drift guard) confirms the shim `program` stays non-optional and the #3 throw +
  the widened `createCanonicalizer` return type type-check.
- No `console`/`process` introduced in `src/core/**` (eslint passes): `npx nx lint angular-typechecker`.
  </verification>

<success_criteria>
Measurable completion:

- [ ] #1: `createCanonicalizer` returns `string | undefined`; a throwing realpath returns `undefined` and the loop KEEPS the diagnostic without incrementing `suppressedCount` (no false PASS). The SUCCESS path (memoization + case-fold) is byte-unchanged.
- [ ] T1 coupling: the prior suppress-on-throw test (filter-diagnostics.spec.ts:135-147) is INVERTED to kept 1 / suppressed 0 with a rewritten title + comment, in the SAME commit as the fix (suite green).
- [ ] #3: a `result.program === undefined` defense-in-depth guard raises `TypecheckInfrastructureError` (literal `angular-typecheck:` message, disjoint from the 500 scan); the shim `program` stays non-optional (runtime defense, not a type change).
- [ ] #2: diagnostic-codes.ts:71 and :86 say "PRE-filter gathered set", not "reported set".
- [ ] S1: compiler-cli-types.ts:98 uses a symbol reference, not `run-typecheck.ts:229`.
- [ ] S2: `TemplateCheckAborted.code` is documented as retained/contract-pinned and is NOT dropped.
- [ ] S3: a new executor.spec.ts test pins `errorCount 0 + templateCheckAborted set -> { success: true }` WITH a `logger.warn` (advisory-not-verdict; verdict NOT forced false).
- [ ] S5a / S5c / S5d: the three new core tests (config-500 rootNames-independence, mixed Error+Warning warningCount split via a new `warningDiagnostic` builder, `.ngtypecheck.tsx` pass-through) pass.
- [ ] `npx nx test angular-typechecker`, `npx nx build angular-typechecker`, and `npx nx lint angular-typechecker` all pass.
- [ ] Commits use clean conventional scopes (`core` for #1/#3/#2; `core` for the doc edits; `test(core)` for the tests), NO quick-id in scopes, NO AI attribution, ASCII-only.

Consciously EXCLUDED (per CONTEXT --analyze; do NOT plan or implement):

- [x] S4 (drift-direction comment) -- REFUTED: the prose is already correct (`AssertAssignable<From, To extends From>` used as `<Real, Shim>` asserts Real assignable to Shim). compiler-cli-types.drift.ts left unchanged.
- [x] S5(b) (includeDeps e2e) -- REFUTED DUPLICATE: already covered at run-typecheck.integration.spec.ts:129-145. No second includeDeps test.
- [x] S6 (NG() 4-digit dev-time guard) -- DECLINED: all callers pass literals; prior task rated this over-engineering. Consistent to decline.
- [x] Forcing `success: false` when templateCheckAborted is set -- PROHIBITED: contradicts the locked 09-RES-02-DECISION.md advisory-not-verdict policy. S3 only PINS the green-with-warn behavior.
      </success_criteria>

<output>
Create `.planning/quick/260630-fg0-address-second-round-pr-review-findings/260630-fg0-SUMMARY.md` when done.
</output>
