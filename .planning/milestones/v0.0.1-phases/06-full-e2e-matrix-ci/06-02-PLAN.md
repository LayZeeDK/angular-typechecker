---
phase: 06-full-e2e-matrix-ci
plan: 02
type: execute
wave: 1
depends_on: ['06-01']
files_modified:
  - e2e/angular-typechecker-matrix-e2e/src/matrix-5types.int.spec.ts
  - e2e/angular-typechecker-matrix-e2e/src/pnpm-symlink.int.spec.ts
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/pnpm-lock.yaml
autonomous: true
requirements: [TEST-03]
user_setup: []

must_haves:
  truths:
    - 'The executor type-checks the INSTALLED tarball (npm) green (exit 0) for all five committed project types: app, local-lib, buildable-lib, publishable-lib, and the spec-tsconfig sibling target'
    - "Each of the five types reports an injected TS2322 (non-zero exit + the rendered TS2322 token in stdout + NO ERR_REQUIRE_ESM + NO 'infrastructure error') so a no-op exit-0 cannot pass as green"
    - 'The executor type-checks the installed tarball under a pnpm symlinked node_modules layout (green) and reports an injected error (non-zero exit) with includeDeps:true so the .pnpm/ store is traversed'
    - 'A Linux-validated realpath regression-guard proves a non-realpath startsWith filter would mis-suppress an in-project diagnostic resolved through a pnpm symlink, OR (documented fallback when the layout does not cross a boundary) asserts the symlinked layout simply works'
  artifacts:
    - path: 'e2e/angular-typechecker-matrix-e2e/src/matrix-5types.int.spec.ts'
      provides: 'TEST-03 5-type e2e: install the tarball ONCE, it.each over the 5 committed targets, green + injected TS2322 per type'
      contains: 'it.each'
    - path: 'e2e/angular-typechecker-matrix-e2e/src/pnpm-symlink.int.spec.ts'
      provides: 'pnpm symlinked-store e2e + Linux realpath regression-guard (OUT-02 backstop)'
      contains: 'pnpm add'
    - path: 'e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/pnpm-lock.yaml'
      provides: 'committed pnpm lockfile so the pnpm fixture install is reproducible (or --no-frozen-lockfile fallback)'
      min_lines: 1
  key_links:
    - from: 'e2e/angular-typechecker-matrix-e2e/src/matrix-5types.int.spec.ts'
      to: 'the 5 committed consumer-workspace targets (PUBLISHED executor id)'
      via: 'execSync npx nx run <target> --output-style=static over it.each'
      pattern: 'angular-typecheck'
    - from: 'e2e/angular-typechecker-matrix-e2e/src/pnpm-symlink.int.spec.ts'
      to: 'pnpm .pnpm/ symlinked store'
      via: 'pnpm add <tgz> --no-frozen-lockfile + lstatSync/realpathSync probe before the filter assertion'
      pattern: 'realpathSync'
---

<objective>
Author the two `angular-typechecker-matrix-e2e` end-to-end specs that close TEST-03's full
project-type validation against the INSTALLED tarball: (1) the 5-type matrix spec that installs the
packed tarball once into the committed `consumer-workspace` fixture and runs the executor green +
injected-error for each of the five project types, and (2) the pnpm symlink spec that installs the
tarball under pnpm's `.pnpm/` symlinked layout and carries the OUT-02 realpath regression-guard.

Purpose: this is the slow gating backstop the earlier phases deferred -- proof that the published
package resolves and the check actually runs across ALL five Angular project shapes and across both
on-disk node_modules layout classes (npm hoisted + pnpm symlinked), not just the single application
type the Phase-5 install-e2e smoke covers.

Output: `matrix-5types.int.spec.ts`, `pnpm-symlink.int.spec.ts`, and a committed `pnpm-lock.yaml`.

NOTE (worktree-hostile): both specs do a real `nx build --skip-nx-cache` -> `npm pack` -> tmp install
-> nested `nx run`. They run on the MAIN tree, fully serialized (the cloned `vitest.config.mts`:
`singleFork`, `fileParallelism:false`). The pnpm realpath guard CANNOT be authored/validated on the
Windows arm64 dev box (Git Bash `ln -s` copies); it is truly exercised on the Linux CI leg via the
draft PR (RD-10). The 5-type npm spec DOES run locally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-full-e2e-matrix-ci/06-01-SUMMARY.md
@.planning/phases/06-full-e2e-matrix-ci/06-PATTERNS.md
@.planning/phases/06-full-e2e-matrix-ci/06-RESEARCH.md

<interfaces>
<!-- Committed 06-01 fixture targets (PUBLISHED executor id) the matrix spec drives. Verified from
     e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/**/project.json this session.
     Executor never RUNS the build targets -- it only reads each project's tsConfig (OQ-1 resolved). -->

Five committed targets (run as `nx run <target>`):
app:angular-typecheck -> tsConfig apps/app/tsconfig.app.json (application)
local-lib:angular-typecheck -> tsConfig libs/local-lib/tsconfig.lib.json (local non-buildable lib)
buildable-lib:angular-typecheck -> tsConfig libs/buildable-lib/tsconfig.lib.json (buildable lib; build=@nx/angular:ng-packagr-lite, never run)
publishable-lib:angular-typecheck -> tsConfig libs/publishable-lib/tsconfig.lib.json (publishable lib; build=@nx/angular:package, never run)
local-lib:angular-typecheck-spec -> tsConfig libs/local-lib/tsconfig.spec.json (spec tsconfig; distinct \*.spec.ts file set)

Committed-clean injection sites (replace the unique label string to introduce a TS2322; mutate the
TMP copy only -- the per-run workspace is rmSync'd in finally):
app apps/app/src/app.component.ts label: 'angular-typechecker matrix app'
local-lib libs/local-lib/src/local-lib.component.ts label: 'angular-typechecker matrix local lib'
buildable-lib libs/buildable-lib/src/buildable-lib.component.ts label: 'angular-typechecker matrix buildable lib'
publishable-lib libs/publishable-lib/src/publishable-lib.component.ts label: 'angular-typechecker matrix publishable lib'
spec type libs/local-lib/src/local-lib.component.spec.ts (inject the error HERE so it lands in the spec file set, proving the spec tsconfig is checked)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 5-type matrix e2e spec (TEST-03)</name>
  <files>e2e/angular-typechecker-matrix-e2e/src/matrix-5types.int.spec.ts</files>
  <read_first>
    - e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts (the harness to reuse VERBATIM: `INJECTED_TS_CODE = 'TS2322'`, `NX_RUNNER_ENV_KEYS` + `buildCleanEnv`, `RunResult`/`run(cwd)`, the `beforeAll` pack-to-tmp [`nx build angular-typechecker --skip-nx-cache` -> `npm pack --json` from distDir -> tarballPath], the `afterAll` rmSync of the tgz, the empty-`.npmrc` + `npm_config_userconfig` honesty pattern, and the 4-way green/injected assertion)
    - 06-PATTERNS.md section "matrix-5types.int.spec.ts (test, e2e)" (the exact harness-reuse map, including parameterizing `run(cwd, target)` per type)
    - 06-RESEARCH.md Pattern 3 (reuse install-e2e harness) + Pitfall 3 (the green+TS2322 pairing prevents a lying no-op exit 0) + Pattern 1 (the 5 type shapes)
    - the committed fixture project.json files + component sources (see <interfaces> above) for the exact target ids + injection label strings
  </read_first>
  <action>
    Clone the install-smoke harness into a new spec. Reuse `buildCleanEnv` and the `beforeAll`
    pack-to-tmp / `afterAll` tgz-cleanup VERBATIM (do not abbreviate the `NX_RUNNER_ENV_KEYS` list or
    the dual legacy-peer-deps env strip -- this is the Phase-4 nested-nx isolation invariant). Change
    `fixtureDir` to resolve the committed `consumer-workspace` fixture
    (`e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace`), resolving `workspaceRoot` 3
    dirs up from the spec file as install-smoke does.

    Parameterize the `run` helper to take the target id: `run(cwd, target)` running
    `npx nx run ${target} --output-style=static` (keep the try/catch that captures non-zero exit +
    stdout+stderr; NEVER pipe through head/rg -- the pipe tail masks Nx's exit code; use FORCE_COLOR=0
    via buildCleanEnv, NEVER the `--no-color` CLI flag which the schema's additionalProperties:false
    rejects).

    Install the tarball ONCE into ONE tmp consumer-workspace in setup (per D-07: one Angular+Nx
    install, not five): `cpSync` the fixture into `mkdtempSync`, write an empty `.npmrc`,
    `npm install <tarballPath>` with `npm_config_userconfig` pointed at a non-existent path (B-03
    honesty -- a clean install must succeed without legacy-peer-deps or surface a REAL ERESOLVE; do
    NOT auto-add an override, escalate). Sanity-check the installed
    `node_modules/angular-typechecker/executors.json` exposes the `angular-typecheck` executor (the
    cheap resolves-from-install check from install-smoke).

    Then `it.each` over a table of the five {label, target, injectionFile, originalLabel} rows from
    <interfaces>: assert GREEN (`run(tmp, target).code === 0`), inject a TS2322 by inserting a
    `readonly broken: number = ${JSON.stringify('str')};` line ahead of the unique label line (build
    the broken line via JSON.stringify -- no quote-escaping hazard, ASCII only), assert the injected
    `replace` actually changed the source (`injected !== original`), run again, and apply the 4-way
    assertion (non-zero exit + stdout contains `INJECTED_TS_CODE` + NOT `ERR_REQUIRE_ESM` + NOT
    `'infrastructure error'`). Restore the original source after each row so the shared tmp install is
    reusable across rows (write `original` back in a finally). For the spec-type row, inject into
    `local-lib.component.spec.ts` (so the error provably lands in the spec file set), NOT the
    component.

    Use the `INJECTED_TS_CODE = 'TS2322'` token constant (assert the full token, never a bare 2322
    substring). Match TS style: blank lines around control flow/returns, braces always, single quotes.

  </action>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && npx nx run angular-typechecker-matrix-e2e:test 2>&1 | rg -q "matrix-5types" && echo MATRIX_5TYPE_RAN</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/angular-typechecker-matrix-e2e/src/matrix-5types.int.spec.ts` exists and uses `it.each` over exactly the five committed targets (app, local-lib, buildable-lib, publishable-lib, local-lib spec)
    - `npx nx run angular-typechecker-matrix-e2e:test` exits 0 on the main tree with all five rows green-then-TS2322 passing (the spec-type row injects into local-lib.component.spec.ts)
    - the spec installs the tarball ONCE (one `npm install`), not per-type; the install uses an empty `.npmrc` + non-existent `npm_config_userconfig` (no leaked legacy-peer-deps)
    - each injected-error assertion checks non-zero exit AND `stdout.toContain('TS2322')` AND `not.toMatch(/ERR_REQUIRE_ESM/)` AND `not.toContain('infrastructure error')`
    - no `--no-color` CLI flag; no `| head` / `| rg` piping of the nx run
  </acceptance_criteria>
  <done>`matrix-5types.int.spec.ts` exists; `nx run angular-typechecker-matrix-e2e:test` passes locally with the five-type green+injected matrix.</done>
</task>

<task type="auto">
  <name>Task 2: pnpm symlink e2e spec + committed lockfile (TEST-03 / OUT-02 backstop)</name>
  <files>e2e/angular-typechecker-matrix-e2e/src/pnpm-symlink.int.spec.ts, e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/pnpm-lock.yaml</files>
  <read_first>
    - e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts (same harness -- reuse buildCleanEnv, the pack-to-tmp beforeAll, the empty-.npmrc honesty pattern, the green/injected 4-way assertion)
    - 06-PATTERNS.md section "pnpm-symlink.int.spec.ts (test, e2e)" (the pnpm install line, includeDeps:true, the Linux-only realpath PROBE-before-assert, the Windows-fallback WARNING)
    - 06-RESEARCH.md "Install the tarball under pnpm in a tmp dir (D-09)" + Pitfall 1 (Windows `ln -s` produces a COPY -> guard is dead locally; validate on Linux CI) + B-02 resolution (pnpm top-level node_modules/<pkg> is a symlink into .pnpm/; Node resolves to real location)
    - packages/angular-typechecker/src/core/filter-diagnostics.ts (the realpath-FIRST canonicalizer + node_modules-segment exclusion the guard backstops) -- so the guard's intent matches the real filter
  </read_first>
  <action>
    Author `pnpm-symlink.int.spec.ts` reusing the install-smoke harness (`buildCleanEnv`, the
    pack-to-tmp `beforeAll` capturing `tarballPath`, the empty-`.npmrc` honesty pattern, the green +
    injected 4-way assertion). Differences from the npm spec:

    - Install via pnpm into the tmp consumer-workspace:
      `execSync(`pnpm add ${JSON.stringify(tarballPath)} --no-frozen-lockfile`, { cwd: tmp, env })`
      (the committed `pnpm-lock.yaml` makes the install reproducible; `--no-frozen-lockfile` is the
      documented fallback if the runner pnpm version differs from the lockfile's -- A1).
    - Run a single representative target (`app:angular-typecheck`, which already wires
      `includeDeps: true`) so pnpm's `.pnpm/`-symlinked store is genuinely traversed; assert green
      then injected-TS2322 with the same 4-way assertion.
    - REALPATH PROBE BEFORE the regression assertion (Pitfall 1 / B-02): using `lstatSync` +
      `realpathSync` on the installed `node_modules/angular-typechecker` path, assert it IS a symlink
      AND its realpath crosses into a `.pnpm/` segment. Gate the boundary-crossing regression-guard
      behind that probe: if (and only if) the probe confirms a boundary-crossing symlink, assert that
      a path resolved THROUGH the symlink that a NAIVE non-realpath `startsWith(basePath)` filter
      would mis-suppress is correctly KEPT (construct it by calling the exported `filterDiagnostics`
      directly with the probed real/symlink paths, OR by asserting the executor keeps an in-project
      diagnostic that only resolves in-project after realpath). If the probe does NOT find a
      boundary-crossing symlink (the Windows dev-box reality), FALL BACK to asserting the symlinked
      layout simply WORKS (green + injected) and leave the load-bearing realpath proof to the unit
      tier (06-03). Document the fallback inline.

    Generate the committed `pnpm-lock.yaml` by running a one-time `pnpm install` in the fixture at the
    pinned pnpm version (11.9.0) and committing the resulting lockfile; if generating it on the dev
    box is impractical, commit a minimal placeholder and rely on `--no-frozen-lockfile` (A1) -- but
    PREFER a real committed lockfile. Match TS style throughout.

  </action>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && test -f e2e/angular-typechecker-matrix-e2e/src/pnpm-symlink.int.spec.ts && test -f e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/pnpm-lock.yaml && node -e "const s=require('fs').readFileSync('e2e/angular-typechecker-matrix-e2e/src/pnpm-symlink.int.spec.ts','utf8'); const ok=/pnpm add/.test(s)&&/realpathSync/.test(s)&&/lstatSync/.test(s)&&/TS2322/.test(s); process.exit(ok?0:1)" && echo PNPM_SPEC_OK</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/angular-typechecker-matrix-e2e/src/pnpm-symlink.int.spec.ts` exists and contains `pnpm add`, `lstatSync`, `realpathSync`, and the `TS2322` token assertion
    - a committed `pnpm-lock.yaml` exists under `fixtures/consumer-workspace/` (or the spec uses `--no-frozen-lockfile` with a documented A1 rationale)
    - the spec includes the symlink+boundary-crossing PROBE that gates the realpath regression-guard, with the documented "symlinked layout simply works" fallback when the probe finds no boundary-crossing symlink
    - the install uses an empty `.npmrc` + non-existent `npm_config_userconfig` (B-03 honesty preserved under pnpm)
    - the verify above passes; the spec's full pnpm-on-Linux assertions are authoritatively validated on the Linux CI leg via the draft PR (RD-10), not locally on Windows arm64
  </acceptance_criteria>
  <done>`pnpm-symlink.int.spec.ts` + `pnpm-lock.yaml` exist; the structural verify passes; the Linux realpath behavior is validated on the draft-PR e2e leg.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                   | Description                                                                                                                                                       |
| ---------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| repo source -> packed tarball -> tmp consumer install      | the only artifact installed is THIS repo's freshly-packed `dist/` (no third-party registry fetch); the fixture deps are EXACT-pinned already-vetted Angular/Nx/TS |
| spec -> nested `nx run` / `npm install` / `pnpm add` shell | fixed target ids + fixed flags only; no untrusted string reaches the shell                                                                                        |

## STRIDE Threat Register

| Threat ID  | Category  | Component                                                     | Disposition | Mitigation Plan                                                                                                                                                                         |
| ---------- | --------- | ------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-02-01 | Tampering | the installed tarball runs a postinstall in the tmp consumer  | accept      | the tarball is this repo's own `npm pack`; the Phase-5 PKG-02 audit already asserts no install scripts; no registry fetch introduces new code                                           |
| T-06-02-02 | Spoofing  | a leaked `legacy-peer-deps` masks a real consumer ERESOLVE    | mitigate    | empty `.npmrc` in the tmp workspace + non-existent `npm_config_userconfig` + the `buildCleanEnv` env strip (B-03 honesty); a real ERESOLVE is a finding to ESCALATE, never auto-patched |
| T-06-02-03 | Tampering | a no-op exit 0 passes as a green check (a lying type-checker) | mitigate    | the green + injected-TS2322 4-way assertion per type (non-zero exit + full TS2322 token + no ERR_REQUIRE_ESM + no infrastructure error)                                                 |

> Local fixtures only; no secrets, no new network/CI attack surface beyond the already-vetted tarball.
> </threat_model>

<verification>
- `npx nx run angular-typechecker-matrix-e2e:test` exits 0 on the main tree (5-type matrix green; pnpm spec green or documented Windows fallback).
- The realpath regression-guard's Linux-only teeth are validated on the draft-PR `e2e` leg (RD-10), not locally.
</verification>

<success_criteria>

- TEST-03: the executor is validated end-to-end across all five project types against the installed npm tarball (green + injected error per type).
- OUT-02 backstop: the pnpm symlinked-store layout is exercised with `includeDeps:true` + a realpath regression-guard (or documented fallback).
- Both specs reuse the hardened install-e2e serialized harness and the B-03 clean-install honesty pattern verbatim.
  </success_criteria>

<output>
Create `.planning/phases/06-full-e2e-matrix-ci/06-02-SUMMARY.md` when done.
</output>
