# Phase 11: Fallow code-quality CI gate - Pattern Map

**Mapped:** 2026-06-30
**Files analyzed:** 5 (1 new, 4 modified)
**Analogs found:** 4 / 5 (the `.fallowrc.jsonc` has no in-repo analog; its source of truth is 11-RESEARCH.md)

This phase delivers CI / config infrastructure, NOT application source. There is no
controller/service/component role here -- the roles are `config` and `ci-workflow`. The two
richest analogs are IN-FILE: the existing `test` / `e2e` jobs in `.github/workflows/ci.yml`
(copy verbatim) and the existing `assert_selected` lines in `tools/act/act-compat.sh`.

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `.fallowrc.jsonc` (NEW, repo root) | config (tool config) | static / batch analysis | none in repo | NO ANALOG -- use 11-RESEARCH.md SS "The verified `.fallowrc.jsonc`" verbatim |
| `.github/workflows/ci.yml` (MODIFY) | ci-workflow (add `fallow` job + aggregate edit) | request-response (CI gate) | `e2e` job + `ci` aggregate IN THE SAME FILE | exact (in-file sibling) |
| `tools/act/act-compat.sh` (MODIFY) | test (workflow dry-run assertion) | batch / static | existing `assert_selected "$PR_PLAN" "ci/e2e"` line | exact (in-file sibling) |
| root `package.json` (MODIFY) | config (dependency manifest) | n/a | existing `devDependencies` block + removal from `dependencies` | exact |
| root `package-lock.json` (MODIFY) | config (lockfile, generated) | n/a | regenerated by `npm install`, never hand-edited | n/a (tool-generated) |

## Pattern Assignments

### `.github/workflows/ci.yml` -- add the `fallow` job (ci-workflow, request-response)

**Analog:** the `e2e` job (`ci.yml:123-143`) and the `test` job (`ci.yml:84-114`), same file.
The `fallow` job copies the path-gating, checkout, setup-node, and `npm ci` shape verbatim
and reuses the EXACT SHA pins already present.

**Action SHA pins to reuse VERBATIM** (extracted from the existing jobs -- do NOT invent new pins; Dependabot keeps these in lockstep):

| Action | Pin (40-char SHA + version comment) | Seen at |
|--------|-------------------------------------|---------|
| `actions/checkout` | `actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1` | `ci.yml:51,101,130,152,168` |
| `actions/setup-node` | `actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0` | `ci.yml:104,133` |

**Path-gating pattern to copy** (the load-bearing NEGATIVE `!= 'false'` form -- `ci.yml:124-125`, identical at `ci.yml:85-86`):
```yaml
  e2e:
    needs: changes
    if: ${{ needs.changes.outputs.code != 'false' }}
    runs-on: ubuntu-latest
```
The comment at `ci.yml:121-122` documents WHY the negative form is required: under `act -n`
the filter output is empty and `'' != 'false'` is true, so the job stays in the act plan and
the act-compat assertions keep passing.

**Checkout + setup-node + `npm ci` step pattern to copy** (from `e2e`, `ci.yml:129-140`):
```yaml
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0
        with:
          node-version: 24
          cache: npm
      - run: npm ci
```

**`fallow`-specific deltas from that template** (per D-12, D-13, 11-RESEARCH.md SS "CI wiring"):
- ADD `fetch-depth: 0` under the checkout `with:` block (LOAD-BEARING for `new-only`
  base-snapshot attribution -- the only checkout in this file that needs it; every other
  checkout uses the default shallow depth). Keep `persist-credentials: false`.
- The run step is `npx fallow audit --format json --base origin/main` with
  `env: FALLOW_AUDIT_BASE: origin/main`.

**Exact job YAML the planner can hand to the executor** (slot it as a sibling of `e2e`, after `e2e` / before `act-compat`; verified content lives in 11-RESEARCH.md SS "CI wiring: the exact `fallow` job YAML", lines 177-209):
```yaml
  fallow:
    needs: changes
    if: ${{ needs.changes.outputs.code != 'false' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1
        with:
          persist-credentials: false
          fetch-depth: 0
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5.0.0
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx fallow audit --format json --base origin/main
        env:
          FALLOW_AUDIT_BASE: origin/main
```
(11-RESEARCH.md carries an explanatory comment block above the `fallow:` key -- preserve it;
it matches the in-file commenting density of the `test`/`e2e`/`changes` jobs.)

---

### `.github/workflows/ci.yml` -- edit the `ci` aggregate `needs:` (ci-workflow)

**Analog:** the existing `ci` aggregate job (`ci.yml:193-205`).

**Current `needs:` list** (`ci.yml:194`):
```yaml
  ci:
    needs: [changes, test, e2e, act-compat, lint-workflows]
```

**One-line change** -- add `fallow` to the list (per D-10, 11-RESEARCH.md:212-216):
```yaml
  ci:
    needs: [changes, test, e2e, fallow, act-compat, lint-workflows]
```

**Do NOT touch the gate expression** (`ci.yml:200`). It already globs every listed job:
```yaml
          if [ "${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') }}" = "true" ]; then
```
`needs.*` auto-includes `fallow` once it is in the `needs:` list. `'skipped'` is
intentionally NOT in the fail set (`ci.yml:188-192` documents the path-skip tolerance), so a
planning/docs-only PR that path-skips `fallow` keeps `ci` green. No gate-expression edit.

---

### `tools/act/act-compat.sh` -- add the `ci/fallow` assertion (test, batch)

**Analog:** the existing `pull_request`-block assertions (`act-compat.sh:111-115`):
```bash
assert_selected "$PR_PLAN" "ci/test-" "pull_request"
assert_selected "$PR_PLAN" "ci/e2e" "pull_request"
assert_selected "$PR_PLAN" "ci/act-compat" "pull_request"
assert_selected "$PR_PLAN" "ci/lint-workflows" "pull_request"
assert_selected "$PR_PLAN" "ci/ci" "pull_request"
```

**One-line change** -- job id `fallow` (no `name:` set) maps to act label `ci/fallow`
(act labels jobs `<workflow>/<jobid>`; every existing assertion uses `ci/<jobid>`). Add
alongside the other `ci/*` lines in the `pull_request` block (per D-15, 11-RESEARCH.md:224-232):
```bash
assert_selected "$PR_PLAN" "ci/fallow" "pull_request"
```

**Do NOT add** to the other blocks (verified in 11-RESEARCH.md:232):
- `PUSH_MAIN_PLAN` (`act-compat.sh:121-123`) only asserts a representative subset
  (`ci/test-`, `ci/ci`) -- a push-main `ci/fallow` assertion is OPTIONAL; omit for the
  minimal change.
- `PUSH_TAG_PLAN` / `DISPATCH_PLAN` (`act-compat.sh:127-133`) assert only `release/publish`
  (those are `release.yml` jobs); `fallow` is in `ci.yml`, so adding it there would be
  noise. Leave them unchanged.

---

### root `package.json` -- pin `fallow`, remove `@angular/forms` (config)

**Analog:** the existing `devDependencies` block (`package.json:7-47`) and the `dependencies`
block (`package.json:48-56`).

**Exact-pin convention** -- the locked Nx/Angular deps in this manifest use EXACT pins (no
`^`/`~`), e.g. `package.json:8-26` (`"@nx/devkit": "23.0.1"`, `"@angular/cli": "22.0.4"`).
Add `fallow` the same way (per D-11, 11-RESEARCH.md:50,57-62):
```jsonc
"devDependencies": {
  ...
  "fallow": "2.103.0",   // exact pin -- alphabetical placement (after "eslint-config-prettier", before "jsdom")
  ...
}
```
Do NOT use `^`/`~` -- a fallow minor release changing a default rule must not silently flip
the gate (Pitfall 4).

**Remove `@angular/forms`** (the verify-then-resolve item, D-07; 11-RESEARCH.md SS
"`@angular/forms`: REMOVE", lines 163-167). It is at `package.json:52`:
```jsonc
"@angular/forms": "22.0.4",   // <- DELETE this line
```
`[VERIFIED: git grep]` `@angular/forms` appears ONLY at `package.json:52` (+ lockfile) -- zero
`import` / template / config references anywhere in `packages/`, `fixtures/`, `e2e/`,
`tools/`. Removal yields the 0-finding clean tree. The executor must re-confirm with
`git grep -n "@angular/forms"` before deleting, then run `npm install` to regenerate the
lockfile.

---

### root `package-lock.json` -- regenerated (config, tool-generated)

**No analog / no hand-edit.** After editing `package.json` (add `fallow`, remove
`@angular/forms`), run `npm install` to regenerate `package-lock.json`. Never hand-edit the
lockfile. NOTE (AGENTS.md "Parallel execution"): this is a dependency-CHANGING edit -- if any
plan runs in a worktree it needs Pattern B (per-worktree `npm ci`), NOT the shared
`node_modules` junction.

---

### `.fallowrc.jsonc` (NEW, repo root) -- NO IN-REPO ANALOG

There is no existing fallow config or comparable tool-config in this repo to copy. The
**source of truth is 11-RESEARCH.md SS "The verified `.fallowrc.jsonc` (repo root)"
(lines 81-144)** -- that exact content was run live and produced `verdict: pass` / exit 0.
The planner should reference it directly; the closest structural reference (the `init`-emitted
shape with `$schema` + JSONC comments) is documented in 11-DISCUSS-RESEARCH.md SS "Config
schema" (lines 51-65).

**Verified target line references the config must address** (re-confirmed this session against the actual source):

| Config key | Target (file:line) | Resolves |
|------------|--------------------|----------|
| `entry: ["packages/angular-typechecker/src/core/compiler-cli-types.drift.ts"]` | the drift tripwire, reachable ONLY via `tsconfig.drift.json` `files` (`tsconfig.drift.json:12`) | IN-02 `unused_file` |
| `overrides` `unused-enum-members: off` on `**/compiler-cli-types.ts` | `EmitFlags` enum, `compiler-cli-types.ts:109-117` (7 members) | IN-03 `unused_enum_members` |
| `ignoreExports` `UNKNOWN_ERROR_CODE` on `**/compiler-cli-types.ts` | `compiler-cli-types.ts:130` | IN-04 `unused_types` |
| `overrides` `unrendered-components` + `unused-component-inputs` off on `fixtures/fault-isolation/**` | the 3 fixture components + `someInput` | 4 fixture findings |
| `rules` `unused-dev-dependencies: off` | the 14 structurally-untraceable dev deps | dev-dep false positives (D-06) |
| `audit.gate: "new-only"` | n/a (config-level) | deterministic gate (D-02) |

**LANDMINE -- the fixture glob path** (Pitfall 1, 11-RESEARCH.md:11,262-265): use
`fixtures/fault-isolation/**` (REPO-ROOT relative), NOT
`packages/angular-typechecker/fixtures/fault-isolation/**` (the WRONG path written in
11-CONTEXT.md D-04 and its canonical-refs). `[VERIFIED: git ls-files this session]` the
fixtures are at repo-root:
- `fixtures/fault-isolation/non-template-error.component.ts`
- `fixtures/fault-isolation/survivor.component.ts`
- `fixtures/fault-isolation/tcb-poison.component.ts`

The package-prefixed glob matches NOTHING and the gate would still fail on 4 introduced
fixture findings.

**Do NOT add empty `duplicates` / `health` blocks** (D-09, 11-RESEARCH.md:146): absence ==
defaults; the repo is clean on both (0 clone groups, 0 complexity findings), so the defaults
are zero-cost regression tripwires.

**Do NOT suppress IN-05** (D-08): `publishConfig.provenance` does NOT reproduce in 2.103.0
(fallow analyzes ROOT `package.json`, not the published manifest). No `ignore` for it.

## Shared Patterns

### CI security posture (apply to the new `fallow` job)
**Source:** `ci.yml:1-19` (threat-model header) + every job's checkout step.
**Apply to:** the new `fallow` job.
- SHA-pinned actions (reuse the EXACT existing pins above -- `ci.yml:51,101,104,130,133`).
- Top-level `permissions: contents: read` (`ci.yml:27-28`) -- the job re-grants NOTHING.
  `npx fallow audit` reads git + filesystem only.
- `persist-credentials: false` on the checkout (`ci.yml:53,102,131,153,169` -- present on
  EVERY checkout in the file).
- No PR-metadata interpolation in run steps (fixed target ids + flags only; the run step is
  `npx fallow audit --format json --base origin/main` with no `${{ github.event.* }}`).

### Path-gating contract (apply to the new `fallow` job)
**Source:** the `changes` job (`ci.yml:46-71`) + the `test`/`e2e` consumers (`ci.yml:85-86`, `124-125`).
**Apply to:** the new `fallow` job.
`needs: changes` + `if: ${{ needs.changes.outputs.code != 'false' }}` (the NEGATIVE form is
load-bearing for `act -n` plan stability under empty filter output).

### Aggregate-gate membership (apply to the `ci` job edit)
**Source:** the `ci` aggregate (`ci.yml:193-205`).
**Apply to:** the `ci.needs` list edit.
New jobs join via the `needs:` list only; the `contains(needs.*.result, ...)` gate
(`ci.yml:200`) globs every listed job automatically -- no gate-expression edit. `'skipped'`
stays out of the fail set (path-skip tolerance).

## No Analog Found

| File | Role | Data Flow | Reason |
|------|------|-----------|--------|
| `.fallowrc.jsonc` | config (tool config) | static analysis | No fallow config (or comparable analyzer config) exists in the repo. Source of truth: 11-RESEARCH.md SS "The verified `.fallowrc.jsonc`" (verbatim, run-verified content). Structural reference for the `$schema` + JSONC-comment idiom: 11-DISCUSS-RESEARCH.md SS "Config schema". |

## Metadata

**Analog search scope:** `.github/workflows/`, `tools/act/`, repo-root `package.json` /
`package-lock.json`, `packages/angular-typechecker/src/core/` (drift + shim source),
`packages/angular-typechecker/tsconfig.drift.json`, `fixtures/fault-isolation/`.
**Files scanned (read in full or targeted):** `ci.yml`, `act-compat.sh`, root `package.json`,
`compiler-cli-types.ts` (L105-140), `tsconfig.drift.json`; plus `git ls-files` /
`git grep` verification of fixture paths and `@angular/forms` usage.
**Pattern extraction date:** 2026-06-30

## PATTERN MAPPING COMPLETE

**Phase:** 11 - fallow-code-quality-ci-gate
**Files classified:** 5
**Analogs found:** 4 / 5

### Coverage
- Files with exact analog: 4 (`ci.yml` job + aggregate, `act-compat.sh`, `package.json`)
- Files with role-match analog: 0
- Files with no analog: 1 (`.fallowrc.jsonc` -- source of truth is 11-RESEARCH.md, run-verified)

### Key Patterns Identified
- The new `fallow` job COPIES the in-file `e2e` job verbatim (path-gating + checkout +
  setup-node + `npm ci`), reusing the EXACT existing SHA pins, plus two `fallow` deltas:
  `fetch-depth: 0` on checkout (load-bearing for `new-only` attribution) and the
  `npx fallow audit` run step with `FALLOW_AUDIT_BASE`.
- The `ci` aggregate change is ONE line (`fallow` into `needs:`); the `contains(needs.*.result, ...)`
  gate auto-includes it -- no gate-expression edit.
- `act-compat.sh` gets ONE `assert_selected "$PR_PLAN" "ci/fallow" "pull_request"` line,
  matching the existing `ci/<jobid>` family.
- `package.json`: exact-pin `fallow` `2.103.0` (no `^`/`~`) + DELETE `@angular/forms`
  (verified zero usage), then `npm install` regenerates the lockfile.
- `.fallowrc.jsonc` is new with no in-repo analog -- use 11-RESEARCH.md's run-verified
  content; the only correction the planner must honor is the REPO-ROOT fixture glob
  (`fixtures/fault-isolation/**`, NOT the package-prefixed path in CONTEXT.md).

### File Created
`.planning/phases/11-fallow-code-quality-ci-gate/11-PATTERNS.md`

### Ready for Planning
Pattern mapping complete. The planner can reference the analog jobs/lines (with file:line) in
PLAN.md actions and hand the executor the run-verified `.fallowrc.jsonc` + `fallow` job YAML
from 11-RESEARCH.md.
