---
phase: 09-resilience-per-file-fault-isolation-boundary-robustness
plan: 02
type: execute
wave: 2
depends_on: ['09-01']
files_modified:
  - packages/angular-typechecker/src/core/gather-diagnostics.ts
  - packages/angular-typechecker/src/core/fault-isolation.integration.spec.ts
autonomous: true
requirements: [RES-02]
tags: [angular, compiler-cli, diagnostics, fault-isolation, resilience]

must_haves:
  truths:
    - "One component's TCB-phase FatalDiagnosticError yields exactly ONE diagnostic and does NOT abandon the remaining files' Angular diagnostics (RES-02 / SC2 / D-04/D-05)."
    - "The surviving component's template/extended diagnostic is reported post-change (today it vanishes via the whole-program early-return) — proven failing-then-passing."
    - 'The per-file gathering uses program.getNgSemanticDiagnostics(sf.fileName) over getTsProgram().getSourceFiles() skipping isDeclarationFile, OptimizeFor.WholeProgram never SingleFile (D-07), implementing the shape recorded by the RES-01 GO decision (SIMPLE or HYBRID; HYBRID is the safe superset default; D-04/D-06).'
    - 'No catch-all per-file try/catch is added — a non-fatal/infra throw still surfaces as UNKNOWN_ERROR_CODE 500 -> TypecheckInfrastructureError (Phase 8 infra-vs-type policy preserved, D-05).'
    - 'The diagnostic SET on clean / ordinarily-erroring programs is unchanged; determinism is preserved by the existing ts.sortAndDeduplicateDiagnostics in finalize (D-06).'
  artifacts:
    - path: 'packages/angular-typechecker/src/core/gather-diagnostics.ts'
      provides: 'Per-file (SIMPLE) or whole-program-non-template + per-file (HYBRID) Angular gathering, per the RES-01 GO decision; COR-02 getGlobalDiagnostics (line 35) retained'
      contains: 'getNgSemanticDiagnostics'
    - path: 'packages/angular-typechecker/src/core/fault-isolation.integration.spec.ts'
      provides: 'The failing-then-passing multi-file isolation proof against fixtures/fault-isolation/'
      contains: 'fault-isolation'
  key_links:
    - from: 'gather-diagnostics.ts'
      to: 'program.getNgSemanticDiagnostics(sf.fileName)'
      via: 'per-file loop over getTsProgram().getSourceFiles() skipping isDeclarationFile'
      pattern: "getNgSemanticDiagnostics\\(sf\\.fileName\\)"
    - from: 'fault-isolation.integration.spec.ts'
      to: 'fixtures/fault-isolation/tsconfig.app.json'
      via: 'runTypecheck against the multi-file fixture; diagnosticsOnFile(survivor) goes 0 -> >= 1'
      pattern: 'fault-isolation'
---

<objective>
RES-02: Fault-isolate Angular diagnostic gathering per file so a single component's `FatalDiagnosticError` yields exactly one diagnostic and does NOT abandon the remaining files' Angular diagnostics — implemented on the EXISTING `api.Program` surface (no `NgtscProgram` migration), per the shape the RES-01 gate decided.

Edit point: replace the single whole-program `program.getNgSemanticDiagnostics()` (`gather-diagnostics.ts:34`) with a per-file loop. COR-02's `getTsProgram().getGlobalDiagnostics()` (line 35) STAYS. The per-file overload `getNgSemanticDiagnostics(sf.fileName)` delegates to `getDiagnosticsForFile(sf, OptimizeFor.WholeProgram)`, which has its OWN per-file `isFatalDiagnosticError` try/catch — one Fatal yields one diagnostic and the loop continues (the `@angular/build` north star). Do NOT add a catch-all: a non-fatal/infra throw must STILL collapse to `UNKNOWN_ERROR_CODE 500` -> `TypecheckInfrastructureError` (Phase 8 policy, D-05).

GATE: This plan depends on 09-01. The executor MUST read `09-01-SUMMARY.md` / `09-RES-01-SPIKE.md` FIRST and implement the recorded shape: SIMPLE (per-file loop only, ONLY if positively proven loss-free) or HYBRID (residual whole-program non-template call + per-file loop — the safe superset; the default on any inconclusive result per D-03).
Output: The modified gatherer + a new `fault-isolation.integration.spec.ts` failing-then-passing proof against `fixtures/fault-isolation/` (created in 09-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-CONTEXT.md
@.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-RESEARCH.md
@.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-PATTERNS.md
@.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-01-SUMMARY.md
@.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-RES-01-SPIKE.md
@.planning/phases/08-correctness-completeness-fixes/08-CONTEXT.md

<interfaces>
<!-- The gatherer's current body and the shim overload it loops with. Extracted from the repo. -->
From packages/angular-typechecker/src/core/gather-diagnostics.ts (current body — line 34 is the RES-02 replace target; line 35 STAYS):
```ts
all.push(...program.getTsOptionDiagnostics());
all.push(...program.getNgOptionDiagnostics());
all.push(...program.getTsSyntacticDiagnostics());
all.push(...program.getTsSemanticDiagnostics());
all.push(...program.getNgStructuralDiagnostics());
all.push(...program.getNgSemanticDiagnostics());                 // <== RES-02 REPLACE (:34)
all.push(...program.getTsProgram().getGlobalDiagnostics());      // COR-02 / D-04 <== STAYS (:35)
```
From packages/angular-typechecker/src/core/compiler-cli-types.ts (the overload — ALREADY declared, no shim change, D-04):
```ts
getNgSemanticDiagnostics(
  fileName?: string,
  cancellationToken?: ts.CancellationToken,
): readonly ts.Diagnostic[];
// Program.getTsProgram() returns ts.Program & { useCaseSensitiveFileNames(): boolean }
// -> getSourceFiles() and getGlobalDiagnostics() are reachable.
```
Determinism guarantee (do NOT add manual dedup): `ts.sortAndDeduplicateDiagnostics` already runs in `finalize` (run-typecheck.ts:347), so HYBRID's per-file template duplicates of the whole-program output are removed for free (D-06).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the whole-program getNgSemanticDiagnostics call with the RES-01-decided per-file shape</name>
  <files>packages/angular-typechecker/src/core/gather-diagnostics.ts</files>
  <read_first>
    - .planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-01-SUMMARY.md AND 09-RES-01-SPIKE.md (THE GATE — read the recorded GO decision FIRST; implement SIMPLE or HYBRID exactly as recorded; if the artifact is missing or ambiguous, default to HYBRID per D-03)
    - packages/angular-typechecker/src/core/gather-diagnostics.ts (the full current body — the single getNgSemanticDiagnostics() at :34 to replace; the COR-02 getGlobalDiagnostics() at :35 that STAYS; the file header comment explaining the unconditional all-getter)
    - packages/angular-typechecker/src/core/compiler-cli-types.ts (lines 60-80 — the getNgSemanticDiagnostics(fileName?) overload; confirm Program.getTsProgram() exposes getSourceFiles())
    - .planning/phases/08-correctness-completeness-fixes/08-CONTEXT.md (D-05/D-06 — the infra-vs-type / TypecheckInfrastructureError policy RES-02 must NOT blur; COR-02 added getGlobalDiagnostics at gather-diagnostics.ts:35 which must be left intact)
    - .planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-RESEARCH.md (Pattern 2 — the SIMPLE loop shape; Pattern 3 — the d.file === file filter risk; Open Q3 — the lowest-risk HYBRID structure; D-05/D-06/D-07 — no catch-all, skip isDeclarationFile, WholeProgram never SingleFile)
    - .planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-PATTERNS.md ("gather-diagnostics.ts (MODIFY, RES-02, line 34)" — the exact SIMPLE and HYBRID shapes + the carried block-blank-line style)
  </read_first>
  <action>
    Read the RES-01 GO decision FIRST. Implement the recorded shape:

    - SIMPLE (ONLY if 09-RES-01-SPIKE.md records SIMPLE with positive proof): replace the line-34 whole-program call with a loop over `program.getTsProgram().getSourceFiles()` that `continue`s on `sf.isDeclarationFile` and `all.push(...program.getNgSemanticDiagnostics(sf.fileName))`.

    - HYBRID (if recorded HYBRID, OR the artifact is missing/ambiguous — the D-03 safe-superset default): KEEP a residual whole-program `all.push(...program.getNgSemanticDiagnostics())` (file-less-safe non-template set) AND add the per-file loop below it. `sortAndDeduplicateDiagnostics` in `finalize` removes the per-file template duplicates the whole-program call also produced (Open Q3 — the lowest-risk HYBRID; do NOT manually filter or dedup).

    Constraints (both shapes): use `OptimizeFor.WholeProgram` implicitly via the `fileName` overload — NEVER `SingleFile` (D-07). Skip `sf.isDeclarationFile` (D-06). Do NOT add any try/catch around the per-file calls — rely on `getDiagnosticsForFile`'s OWN `isFatalDiagnosticError` try/catch; a non-fatal/infra throw must STILL escape to `performCompilation`'s outer catch -> one 500 -> `TypecheckInfrastructureError` (D-05; the Anti-Pattern the research forbids). LEAVE line 35 (`getTsProgram().getGlobalDiagnostics()`, COR-02) intact. Update the file header comment to document the per-file isolation and cite the RES-01 decision (SIMPLE | HYBRID). Follow CLAUDE.md JS/TS style: braces always; blank line before/after the loop and around `return`.

  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- gather-diagnostics.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `gather-diagnostics.ts` contains a per-file call `program.getNgSemanticDiagnostics(sf.fileName)` inside a loop over `program.getTsProgram().getSourceFiles()` that `continue`s on `sf.isDeclarationFile`.
    - The implemented shape matches the recorded RES-01 GO decision (SIMPLE vs HYBRID); if HYBRID, a residual whole-program `getNgSemanticDiagnostics()` (no arg) is retained.
    - No try/catch is added inside the gatherer (the file contains no `catch` token introduced by this change).
    - Line 35 `program.getTsProgram().getGlobalDiagnostics()` (COR-02) is unchanged and still present.
    - `OptimizeFor.SingleFile` does NOT appear anywhere in the change.
    - `npx nx test angular-typechecker -- gather-diagnostics.spec.ts` exits 0 (the existing gatherer unit coverage still passes — the diagnostic SET is unchanged on clean/ordinarily-erroring programs).
    - `npx nx build angular-typechecker` exits 0 (drift guard).
  </acceptance_criteria>
  <done>The gatherer computes the Angular families per-file (isolated) in the RES-01-decided shape, COR-02's global-diagnostics getter is retained, no catch-all was added, and the existing gatherer coverage is green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the failing-then-passing multi-file fault-isolation integration proof</name>
  <files>packages/angular-typechecker/src/core/fault-isolation.integration.spec.ts</files>
  <read_first>
    - fixtures/fault-isolation/ (created in 09-01 — the TCB-poison component A + the survivor component B + tsconfig.app.json; confirm both are listed in files[])
    - packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts (lines 1-65 — copy the header scaffolding, the `NG()` helper at :17, and the `diagnosticsOnFile` helper at :54-65 VERBATIM)
    - packages/angular-typechecker/src/core/extended.promotion.integration.spec.ts (the single-fixture single-tsconfig shape; the `import { NG } from './diagnostic-codes'` alternative)
    - .planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-RESEARCH.md ("RES-02 multi-file fixture" — the failing-then-passing assertion design; Pitfall 4 — NG() for Angular extended codes, raw for TS; Pitfall 5 — inherited 30000ms timeout, no per-file timeout)
    - .planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-PATTERNS.md ("fault-isolation.integration.spec.ts (NEW, RES-02)" — the header/path scaffolding, the NG() + diagnosticsOnFile helpers, the failing-then-passing differentiator)
  </read_first>
  <action>
    Create `packages/angular-typechecker/src/core/fault-isolation.integration.spec.ts` using the real-compiler integration scaffolding: copy the header (`packageRoot`/`workspaceRoot`), the `NG()` negative-code helper, and the `diagnosticsOnFile(diagnostics, absolutePath)` helper from `run-typecheck.integration.spec.ts`. Point `faultIsolationTsConfig` at `join(workspaceRoot, 'fixtures', 'fault-isolation', 'tsconfig.app.json')`.

    The spec calls `await runTypecheck({ tsConfigPath: faultIsolationTsConfig })` and asserts the isolation contract:
    - Component A (TCB-poison): exactly ONE Fatal-derived diagnostic is present on A's file (the IMPORT_GENERATION_FAILURE Fatal -> `err.toDiagnostic()`), present BOTH pre- and post-change.
    - Component B (survivor): `diagnosticsOnFile(result.diagnostics, survivorAbsolutePath)` has length `>= 1` — the plain template error (NG8109 via `NG(8109)`, and/or a raw TS2322) SURVIVES. This is the failing-then-passing differentiator: pre-RES-02 (whole-program early-return) this length is 0; post-RES-02 it is `>= 1`.

    Assert Angular extended codes via `NG()` (e.g. `NG(8109)`), TS codes raw (e.g. `2322`). Do NOT add a per-file `testTimeout` (inherited 30000ms). ASCII-only. If the survivor's exact code depends on the fixture authored in 09-01, derive the assertion from what the fixture actually produces (verify by running, since the fixture and this spec are both in scope this phase).

  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- fault-isolation.integration.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `fault-isolation.integration.spec.ts` exists, runs `runTypecheck` against `fixtures/fault-isolation/tsconfig.app.json`, and uses the `NG()` + `diagnosticsOnFile` helpers (copied from the analog).
    - The spec asserts the survivor component B has `>= 1` diagnostic on its file (post-RES-02) — the diagnostic that vanishes pre-change.
    - The spec asserts component A's single Fatal-derived diagnostic is present (one diagnostic, not zero, not the whole program abandoned).
    - Angular extended codes are asserted via `NG()`; TS codes raw (Pitfall 4 — no bare `8109`).
    - No per-file `testTimeout` added (Pitfall 5).
    - `npx nx test angular-typechecker -- fault-isolation.integration.spec.ts` exits 0 against the post-change gatherer.
  </acceptance_criteria>
  <done>A real-compiler multi-file integration spec proves one poisoned component yields one diagnostic and the survivor component's diagnostic is still reported — the SC2 failing-then-passing isolation proof.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                  | Description                                                                                                                                                                       |
| --------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| consumer source/template -> compiler diagnostic gathering | A poisoned component (untrusted template input) must not be able to suppress (hide) the rest of the program's Angular diagnostics — a "lying clean / silently incomplete" verdict |

## STRIDE Threat Register

| Threat ID | Category                                                 | Component                                                          | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --------- | -------------------------------------------------------- | ------------------------------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-09-01   | Denial of Service / Repudiation (the tool under-reports) | gather-diagnostics.ts Angular gathering (line 34)                  | mitigate    | RES-02 / D-04/D-05: per-file `getNgSemanticDiagnostics(sf.fileName)` gathering so one component's TCB-phase `FatalDiagnosticError` yields exactly one diagnostic (via `getDiagnosticsForFile`'s own `isFatalDiagnosticError` try/catch) and the remaining files' diagnostics still report. Proven by the multi-file fault-isolation integration spec (one poisoned + one survivor). The marquee correctness/robustness control. |
| T-09-04   | Tampering with the verdict's meaning                     | gather-diagnostics.ts (no catch-all) + run-typecheck.ts infra path | mitigate    | D-05: NO catch-all per-file try/catch is added — a non-fatal/infra throw still escapes to `performCompilation`'s outer catch -> `UNKNOWN_ERROR_CODE 500` -> `TypecheckInfrastructureError` (Phase 8 infra-vs-type policy preserved). The per-file loop isolates the FatalDiagnosticError class ONLY. Regression-covered by the retained infra-failure + gatherer unit suites + the full-suite no-set-change check.              |
| T-09-SC   | Tampering                                                | npm/pip/cargo installs                                             | accept      | No package installs in this phase (RESEARCH "Package Legitimacy Audit: Not applicable").                                                                                                                                                                                                                                                                                                                                        |

</threat_model>

<verification>
- GATE: 09-01's recorded GO decision (SIMPLE | HYBRID) is read and implemented; if absent/ambiguous, HYBRID is used (D-03).
- `npx nx test angular-typechecker -- gather-diagnostics.spec.ts` exits 0 (diagnostic SET unchanged on clean/ordinarily-erroring programs).
- `npx nx test angular-typechecker -- fault-isolation.integration.spec.ts` exits 0 (the survivor diagnostic survives post-change).
- `npx nx build angular-typechecker` exits 0 (drift guard).
- Full suite green: `npx nx test angular-typechecker` (no regression across COR/ENG/OUT coverage — the per-file loop did not change the diagnostic set elsewhere).
</verification>

<success_criteria>

- SC2 satisfied: one component's `FatalDiagnosticError` yields exactly one diagnostic and does NOT abandon the remaining files' Angular diagnostics — the surviving files' template/extended diagnostics are still reported, on the existing `api.Program` surface per the RES-01 decision (no `NgtscProgram` migration).
- The Phase 8 infra-vs-type policy is preserved (no catch-all; non-fatal escapes stay infra).
- No package added; no shim widening (D-04).
  </success_criteria>

<output>
Create `.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-02-SUMMARY.md` when done. The SUMMARY MUST state which shape (SIMPLE | HYBRID) was implemented and why.
</output>
