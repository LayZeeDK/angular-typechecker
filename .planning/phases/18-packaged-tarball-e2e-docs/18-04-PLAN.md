---
phase: 18-packaged-tarball-e2e-docs
plan: 04
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-a/**
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-b/**
  - e2e/angular-typechecker-install-e2e/src/storybook-tarball.int.spec.ts
autonomous: true
requirements: [SB-06]

must_haves:
  truths:
    - "The SHIPPED tarball, installed into a generator-scaffolded Layout-A project via nx add + nx g angular-typechecker:configuration + nx typecheck, catches a planted *.stories.ts error (non-zero exit + the DISTINCT full code token)"
    - "The Layout-B centralized-host case catches a planted aggregated story error (and the external-templateUrl NG8002 kill-shot) -- the milestone's motivating silent-false-pass case"
    - "A clean baseline run exits 0 (forced-SB10 node_modules .d.ts errors do not leak in-project)"
    - "stdout never matches /ERR_REQUIRE_ESM/ (CJS->ESM bridge survived packaging) and never contains 'infrastructure error'"
    - "@storybook/angular@10.4.6 is force-installed --legacy-peer-deps in a SEPARATE step; the angular-typechecker install carries NO peer-resolution override (B-03)"
  artifacts:
    - path: "e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-a"
      provides: "Committed Layout-A generator-shaped Storybook consumer workspace with clean stories"
    - path: "e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-b"
      provides: "Committed Layout-B centralized-host workspace: aggregated stories/components + an external templateUrl"
    - path: "e2e/angular-typechecker-install-e2e/src/storybook-tarball.int.spec.ts"
      provides: "Criterion 1 proof, a NEW spec FILE in the existing serialized e2e project"
  key_links:
    - from: "e2e/angular-typechecker-install-e2e/src/storybook-tarball.int.spec.ts"
      to: "the shipped tarball"
      via: "nx add angular-typechecker at local Verdaccio (or npm install <tgz>) with NO override"
      pattern: "nx add angular-typechecker|npm install"
    - from: "e2e/angular-typechecker-install-e2e/src/storybook-tarball.int.spec.ts"
      to: "@storybook/angular@10.4.6"
      via: "separate --legacy-peer-deps install step"
      pattern: "legacy-peer-deps"
---

<objective>
Prove the SHIPPED artifact (the packed dist tarball, not just the local build) catches a planted
`*.stories.ts` type error via `nx add` + `nx g angular-typechecker:configuration` + `nx typecheck`
on committed generator-shaped Storybook fixtures -- Layout A and the Layout-B centralized host.
This is SB-06 criterion 1, NON-NEGOTIABLE (0.0.1-0.1.0 shipped source and a local-build test would
not have caught the packaging defect).

Purpose: the local build and the in-repo integration tier cannot prove the published package. Only
the tarball/`nx add` install does.

Output: two committed Storybook consumer fixtures + one NEW `*.int.spec.ts` FILE inside the existing
serialized `angular-typechecker-install-e2e` project (NEVER a new e2e project -- shared-tarball race).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-packaged-tarball-e2e-docs/18-CONTEXT.md
@.planning/phases/18-packaged-tarball-e2e-docs/18-RESEARCH.md
@.planning/phases/18-packaged-tarball-e2e-docs/18-PATTERNS.md

<interfaces>
<!-- @workspace/test-util exports (libs/test-util/src/index.ts): findWorkspaceRoot, buildCleanEnv,
     commandSucceeds, run, sh, removeTmpDir, writeVerdaccioNpmrc, expectSeededTypecheckTargetDefault,
     readTypecheckTargetDefault. -->
<!-- Serialization is INHERITED by staying in this project (e2e vitest.config.mts lines 33-39:
     pool 'forks', singleFork true, fileParallelism false, sequence.concurrent false, timeouts 300000).
     global-setup.ts builds dist ONCE + publishes to Verdaccio ONCE; specs consume inject('verdaccioUrl')/inject('verdaccioToken'). -->
<!-- B-03 clean env: const env = buildCleanEnv({ stripAllNpmConfig: true });  -- strips process-wide
     npm_config_registry + any leaked legacy-peer-deps so a real ERESOLVE on OUR peers surfaces. -->
<!-- Verdaccio nx-add path (nx-add-npm.int.spec.ts): cpSync fixture -> writeVerdaccioNpmrc(tmp,url,token)
     -> sh('npm install', {cwd:tmp, env:{...env, npm_config_userconfig: join(tmp,'.npmrc.nonexistent')}})
     -> sh('npx nx add angular-typechecker', {cwd:tmp, env:npmEnv}). -->
<!-- Planted-error discipline (generator-e2e.int.spec.ts): DISTINCT full 'TSxxxx'/'NGxxxx' tokens (never
     bare 4-digit), inject via JSON.stringify for ASCII safety; expect(bad.code).not.toBe(0);
     expect(bad.stdout).toContain(CODE); expect(bad.stdout).not.toMatch(/ERR_REQUIRE_ESM/);
     expect(bad.stdout).not.toContain('infrastructure error'); finally { removeTmpDir(tmp); } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Committed Layout-A Storybook consumer fixture</name>
  <files>e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-a/**</files>
  <read_first>
    - e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/ (package.json pinned @angular/*@22.0.4 + nx@23.0.1 + typescript@6.0.3, project.json, nx.json, tsconfig.json/lib/spec, src -- the runnable-workspace skeleton to copy)
    - fixtures/layout-a-storybook and fixtures/layout-a-storybook-clean (the Layout-A .storybook/ + story tsconfig shapes)
    - .planning/phases/18-packaged-tarball-e2e-docs/18-RESEARCH.md (RQ3 committed-fixtures + Assumption A4) and 18-PATTERNS.md (committed fixture skeleton, lines 267-316)
  </read_first>
  <action>
    Assemble a runnable Layout-A Nx consumer workspace under `fixtures/consumer-storybook-a` from the
    `consumer-generator` skeleton (pinned deps, project.json, nx.json, tsconfigs, src), layering a
    `.storybook/` config + at least one CLEAN `*.stories.ts` following the committed
    `fixtures/layout-a-storybook-clean` shape: the `.storybook/tsconfig.json` is injected into the
    project's `references[]` (Layout A per-project scaffold, board D1). Story files carry a CLEAN baseline
    (the spec plants the error at runtime) and a clearly labelled clean anchor line the spec will replace.
    Commit MINUS `node_modules`. Do NOT declare `@storybook/angular` as a product dependency of the plugin;
    it is a fixture-install-time dep only (the spec force-installs it). Add the fixture's own package.json
    Storybook devDependency omitted -- the spec installs it explicitly with `--legacy-peer-deps`.
  </action>
  <verify>
    <automated>test -f e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-a/project.json && test -d e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-a/.storybook && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - the fixture is a runnable Nx workspace (project.json + nx.json + tsconfig.json + pinned package.json) mirroring consumer-generator
    - a clean `*.stories.ts` exists with a clearly labelled clean anchor line for runtime error injection
    - `.storybook/tsconfig.json` is wired into the project references (Layout A shape)
    - no `node_modules` committed
  </acceptance_criteria>
  <done>A committed, runnable Layout-A Storybook consumer fixture with clean stories exists.</done>
</task>

<task type="auto">
  <name>Task 2: Committed Layout-B centralized-host Storybook consumer fixture</name>
  <files>e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-b/**</files>
  <read_first>
    - e2e/angular-typechecker-install-e2e/fixtures/consumer-generator/ (skeleton)
    - fixtures/layout-b-host, fixtures/layout-b-aggregated, fixtures/layout-b-aggregated-clean, fixtures/external-template-tripwire (the host/aggregated/external-template shapes)
    - .planning/phases/18-packaged-tarball-e2e-docs/18-CONTEXT.md (specifics: Layout B host shape -- host tsconfig references ONLY ./.storybook/tsconfig.json; paths via extends)
    - .planning/phases/18-packaged-tarball-e2e-docs/18-RESEARCH.md (RQ3 Layout B fixture; board CONSENSUS D2 kill-shot)
  </read_first>
  <action>
    Assemble a runnable Layout-B centralized-host workspace under `fixtures/consumer-storybook-b` from the
    skeleton, following the committed `fixtures/layout-b-host` + `fixtures/layout-b-aggregated-clean` +
    `fixtures/external-template-tripwire` shapes: a host solution `tsconfig.json` (`files: [], references:
    [{ path: './.storybook/tsconfig.json' }]`) whose `.storybook/tsconfig.json` `include` reaches
    aggregated cross-project CLEAN stories/components INCLUDING at least one component with an external
    `templateUrl` `.html` (the branch-4a kill-shot surface). All sources start CLEAN. Provide a labelled
    clean anchor in an aggregated `*.stories.ts` AND a labelled clean anchor in the external `.html`
    template (so the spec can plant an NG8002). Commit MINUS `node_modules`.
  </action>
  <verify>
    <automated>test -f e2e/angular-typechecker-install-e2e/fixtures/consumer-storybook-b/.storybook/tsconfig.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - host `tsconfig.json` is `files: [], references: [{ path: './.storybook/tsconfig.json' }]`
    - the `.storybook/tsconfig.json` `include` reaches aggregated stories/components across projects
    - at least one aggregated component uses an external `templateUrl` `.html` with a clean anchor for NG8002 injection
    - a clean aggregated `*.stories.ts` exists with a clean anchor for the story-error injection
    - no `node_modules` committed
  </acceptance_criteria>
  <done>A committed, runnable Layout-B centralized-host fixture (aggregated + external template) with clean sources exists.</done>
</task>

<task type="auto">
  <name>Task 3: storybook-tarball.int.spec.ts -- criterion 1 for both layouts</name>
  <files>e2e/angular-typechecker-install-e2e/src/storybook-tarball.int.spec.ts</files>
  <read_first>
    - e2e/angular-typechecker-install-e2e/src/nx-add-npm.int.spec.ts (the Verdaccio nx-add path -- RESEARCH-recommended criterion-1 install)
    - e2e/angular-typechecker-install-e2e/src/generator-e2e.int.spec.ts (DISTINCT-token discipline, JSON.stringify injection, buildCleanEnv, run/skipNxCache, removeTmpDir)
    - e2e/angular-typechecker-install-e2e/src/global-setup.ts and vitest.config.mts (the serialized shared-dist harness this spec inherits)
    - libs/test-util/src/index.ts (@workspace/test-util surface)
    - .planning/phases/18-packaged-tarball-e2e-docs/18-RESEARCH.md (RQ3 per-run recipe steps 1-8; Open Q1 install-path decision; Pitfalls 4/5/6)
  </read_first>
  <action>
    Add a NEW spec FILE inside `angular-typechecker-install-e2e` (never a new project; serialization is
    inherited). RESOLVE Open Q1: use the Verdaccio `nx add` path (RESEARCH recommendation -- the most
    literal criterion-1 proof; global-setup already publishes dist to Verdaccio) and STATE that choice in
    a spec header comment. Per-run recipe for EACH layout (A and B) as two `it` blocks sharing a helper:
    (1) `mkdtempSync` + `cpSync(fixtureDir, tmp)`; (2) `env = buildCleanEnv({ stripAllNpmConfig: true })`;
    (3) `writeVerdaccioNpmrc(tmp, url, token)`; (4) force-install Storybook as a SEPARATE explicit step
    `sh('npm install @storybook/angular@10.4.6 --legacy-peer-deps', {cwd:tmp, env})`; (5) install
    angular-typechecker with NO override via `sh('npx nx add angular-typechecker', {cwd:tmp, env:{...env,
    npm_config_userconfig: join(tmp,'.npmrc.nonexistent')}})`; (6) `sh('npx nx g
    angular-typechecker:configuration <project> --skipFormat', ...)`; (7) CLEAN baseline
    `run(tmp,'<project>:typecheck',{env,skipNxCache:true})` asserts `code === 0` (forced-SB10 node_modules
    .d.ts errors do not leak); (8) plant the error (replace the labelled clean anchor via JSON.stringify)
    and re-run, asserting `bad.code !== 0`, `bad.stdout` contains the DISTINCT full token (Layout A: e.g.
    `TS2322`; Layout B: a DISTINCT token PLUS the external-template `NG8002`), `bad.stdout` NOT matching
    `/ERR_REQUIRE_ESM/`, and NOT containing `'infrastructure error'`. `finally { removeTmpDir(tmp); }`.
    Use a DISTINCT code per layout/leaf (Pitfall 6). Give each `it` a 300000 timeout like the siblings.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker-install-e2e -- --run storybook-tarball</automated>
  </verify>
  <acceptance_criteria>
    - the spec is a new FILE in e2e/angular-typechecker-install-e2e/src (NOT a new project)
    - a header comment states the resolved install path (Verdaccio `nx add`) and why
    - Layout A: clean baseline exits 0; planted error gives `bad.code !== 0` AND `bad.stdout` contains the full distinct TS token
    - Layout B: planted aggregated story error AND external-template NG8002 give `bad.code !== 0` AND `bad.stdout` contains the distinct token(s) incl. `NG8002`
    - both cases assert `bad.stdout` does NOT match `/ERR_REQUIRE_ESM/` and does NOT contain `'infrastructure error'`
    - `@storybook/angular@10.4.6 --legacy-peer-deps` is a separate step; the angular-typechecker install carries NO override
    - `npx nx test angular-typechecker-install-e2e` green
  </acceptance_criteria>
  <done>The shipped tarball catches planted story errors on both layouts through the full nx add + configuration + typecheck sequence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry / Verdaccio -> tmp workspace | The e2e installs a pinned test-fixture package (`@storybook/angular@10.4.6`) and the freshly-published angular-typechecker into a throwaway tmp workspace. |
| local Verdaccio -> nx add | `nx add angular-typechecker` resolves the SHIPPED dist from local Verdaccio (global-setup published it); the safety gate asserts the registry URL is localhost. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-SC | Tampering / Elevation (supply chain) | `@storybook/angular@10.4.6` force-install | mitigate | TEST-FIXTURE ONLY (never a product dependency; the plugin ships zero Storybook coupling). Version-pinned; pre-vetted across spikes 006-008; present on npm (18-RESEARCH Package Legitimacy Audit -> Approved). `--legacy-peer-deps` scoped to Storybook ONLY; installed into a throwaway tmp workspace (`removeTmpDir` cleanup). NOT [ASSUMED]/[SUS] -> no blocking-human checkpoint required. |
| T-18-04-S | Spoofing (registry) | Verdaccio resolution | mitigate | `buildCleanEnv({ stripAllNpmConfig: true })` + the localhost-registry safety assert ensure the install reads only from local Verdaccio, not a public proxy. |
| T-18-04-B03 | Tampering (masked ERESOLVE) | angular-typechecker install | mitigate | Install OUR tarball with NO peer-resolution override + a nonexistent `npm_config_userconfig`, so a real ERESOLVE on our published peers surfaces (B-03 honesty) rather than being masked. |
</threat_model>

<verification>
- `npx nx test angular-typechecker-install-e2e` green (serialized singleFork; inherits global-setup).
- `npx nx run-many -t test --parallel=1` green at the wave gate (prevents the shared-tarball race).
- Both layouts: clean baseline exits 0; planted error exits non-zero with the distinct token(s) and no ERR_REQUIRE_ESM / infrastructure-error.
</verification>

<success_criteria>
- The shipped tarball catches a planted story error on Layout A and Layout B via `nx add` + `nx g configuration` + `nx typecheck` (criterion 1).
- Forced-SB10 does not leak node_modules `.d.ts` errors in-project (clean baseline is green).
- Install honesty (B-03) preserved; the e2e stays inside the serialized project.
</success_criteria>

<output>
Create `.planning/phases/18-packaged-tarball-e2e-docs/18-04-SUMMARY.md` when done.
</output>
