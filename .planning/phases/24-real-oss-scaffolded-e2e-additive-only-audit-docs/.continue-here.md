---
context: phase
phase: 24-real-oss-scaffolded-e2e-additive-only-audit-docs
status: in_progress
last_updated: 2026-07-11T22:25:49.530Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: The CLI x yarn e2e failure is NOT an `exports` / product bug. Do NOT re-attempt
      exposing `./collection.json` (or builders/executors/generators) in the package.json
      `exports` map as the fix — that was tried, rebuilt, re-run, STILL failed identically, and
      reverted. Angular CLI resolves the schematics collection by FILESYSTEM-JOIN from the package
      dir (`path.resolve(pkgDir, pkg.schematics)`), not via the `exports` subpath. The real cause
      is that yarn installs the WRONG published `angular-typechecker@0.2.0` (public npm, no
      schematics) instead of the Verdaccio local dist.

**Do not proceed until the box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| exports-hypothesis | Misdiagnosed "does not support schematics" as `exports` hiding `collection.json`; applied + reverted. | blocking | Root cause is the wrong INSTALLED version (require.resolve probe: version 0.2.0 + `schematics=undefined` + old exports = the npmjs milestone artifact). Verify the installed artifact FIRST, before touching product `exports`. |
| blind-e2e-iteration | Each yarn e2e run is ~60-100s (globalSetup build+publish + real yarn install of Angular 22); tmp is `removeTmpDir`-cleaned. | advisory | Diagnose via an in-test probe (the `atc-probe.cjs` require.resolve pattern already in the spec) or keep the tmp dir; don't re-run blind. |

<current_state>
Mid-way through adding a CLI x yarn e2e (`e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-yarn.e2e.spec.ts`,
NEW + UNCOMMITTED + FAILING + carrying TEMP DIAGNOSTIC code). Everything else for v0.2.1 Phase 24 is
DONE + committed (15 commits on `gsd/v0.2.1-angular-cli-workspace-support`): the ACV-01 gate + the
generator fix + all 4 dedicated-agent gates + phase.complete + learnings + the PASSED milestone audit
+ the full-matrix unit spec + a /gsd-code-review --fix. package.json stays at 0.2.0 (Release-PR is
human-gated + not cut). complete-milestone (archiving) was intentionally deferred.
</current_state>

<completed_work>
See HANDOFF.json `completed_this_session` (ACV-01 fix 1837b25 ... milestone audit 72b5fa6 ...
full-matrix spec 8edef6c ... code-review-fix e833afd). All green: nx test(349)/lint/typecheck/build/format.
</completed_work>

<remaining_work>
1. DEBUG + FIX the CLI x yarn e2e (yarn installs the wrong published 0.2.0). Was about to run /gsd-debug.
2. Finalize the yarn e2e (drop the temp probe; assert auto-wire-all + clean baseline + planted
   per-project scoping under real yarn; flat + workspace layouts) THEN code-review it.
3. OPTIONAL: committed CLI x pnpm SHADOWING dup e2e (the real bug scenario, currently manual ACV-01).
4. HUMAN-GATED: complete-milestone v0.2.1 + Release-PR (see HANDOFF.json task 4).
</remaining_work>

<decisions_made>
See HANDOFF.json `decisions`. Headlines: (a) yarn failure = test-harness registry bug, not product;
(b) exports fix REFUTED + reverted; (c) duplicate-name (kind x layout) e2e coverage already satisfied
by existing fixtures + the yarn workspace layout; (d) documented how dup names are handled (Nx merges
same-root / throws loud on different-root; CLI reads angular.json directly).
</decisions_made>

<blockers>
- yarn 4 installs `angular-typechecker@0.2.0` from public npm (no schematics) not the Verdaccio local
  dist -> `ng add`/`ng g` "does not support schematics". Version collision (local 0.2.0 == npmjs 0.2.0)
  is the aggravator. See HANDOFF.json `blockers` for candidate fixes + the probe diagnostic.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — full structured state, evidence, candidate fixes, next_action.
2. `e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-yarn.e2e.spec.ts` — the failing spec + the
   `atc-probe.cjs` diagnostic (TEMP; remove once fixed).
3. `e2e/angular-typechecker-install-e2e/src/nx-add-yarn.e2e.spec.ts` — the WORKING yarn reference
   (same `.yarnrc.yml` npmRegistryServer:Verdaccio, NO writeVerdaccioNpmrc, installs the local dist).
4. `e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run.e2e.spec.ts` — ACV-02 (npm, works from Verdaccio).
5. `e2e/angular-typechecker-ng-cli-e2e/src/global-setup.ts` + `.verdaccio/config.yml` — how the dist
   is built + published once + the angular-typechecker no-proxy block.

## Infrastructure State
- Verdaccio: stood up per-run by the ng-cli-e2e globalSetup (build dist ONCE + publish 0.2.0 ONCE);
  no-proxy for angular-typechecker, proxy for everything else. Not running now.
- OSS clones (uncommitted scratch): D:/projects/github/{bluehalo/ngx-leaflet@818e9ae,
  realworld-angular/realworld-angular@9e3528f} — realworld still has a pnpm node_modules from ACV-01.
- Branch `gsd/v0.2.1-angular-cli-workspace-support`, ~124 commits ahead of origin/main, tree clean
  except the untracked yarn spec.

<context>
Resume by running /gsd-debug on the yarn version-resolution (the user paused right before it). The
mental model: the product is correct; the e2e harness resolves the wrong artifact under yarn. Confirm
yarn's effective registry + what it actually fetched (the version-collision at 0.2.0 is the prime
suspect), then make the harness install the Verdaccio local dist. Do NOT touch product `exports`.
Once green, finalize + code-review the spec.
</context>

<next_action>
Start with: /gsd-debug (resume the yarn "installs wrong angular-typechecker" investigation). First
re-read HANDOFF.json + diff the new yarn spec against the working nx-add-yarn.e2e.spec.ts; run a
`corepack yarn config get npmRegistryServer` + registry-source probe before any fix.
</next_action>
