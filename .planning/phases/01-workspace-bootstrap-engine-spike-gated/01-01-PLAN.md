---
phase: 01-workspace-bootstrap-engine-spike-gated
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nx.json
  - package.json
  - tsconfig.base.json
  - eslint.config.mjs
  - .gitignore
  - .editorconfig
  - .prettierrc
  - apps/.gitkeep
autonomous: false
requirements: [WS-01, CMP-01]
must_haves:
  truths:
    - "The repo is an Nx 23 integrated Angular monorepo created via create-nx-workspace despite the pre-existing .git/ + .planning/"
    - "Pre-bootstrap HEAD SHA is unchanged after bootstrap (no history rewrite)"
    - ".planning/ and CLAUDE.md are restored byte-identical (no clobber of tracked files)"
    - "nx report resolves nx 23.0.1 and the @nx/* plugins"
  artifacts:
    - path: "nx.json"
      provides: "Nx 23 integrated workspace config (classic project.json layout, --preset=apps)"
    - path: "package.json"
      provides: "Root workspace manifest with nx 23.0.1 + @nx/* devDependencies pinned exact"
    - path: "tsconfig.base.json"
      provides: "Workspace TS base config the plugin + fixture extend"
  key_links:
    - from: "repo root"
      to: ".git/ (preserved)"
      via: "Mechanism B copy-over-preserved-.git"
      pattern: "HEAD unchanged"
---

<objective>
Bootstrap the Nx 23 / Angular 22 / TypeScript 6 integrated Angular monorepo IN-PLACE over the
existing `.git/`, `.planning/`, and `CLAUDE.md` using the move-aside + create-nx-workspace-in-temp +
copy + restore mechanism (D-01/D-02/D-03, Mechanism B). This is the first vertical-slice increment:
it produces the workspace skeleton every later plan builds on, without clobbering tracked files or
rewriting git history.

Purpose: `create-nx-workspace .` is a HARD ERROR on a non-empty dir; the repo already holds tracked
planning artifacts and live `.git/`. Mechanism B is the only safe path (D-02). This is destructive-
adjacent over a live `.git/`, so verification (no clobber, HEAD intact, planning files restored) is
mandatory acceptance criteria and the final commit is gated on a human review of the full `git status`.

Output: an empty integrated Nx 23 workspace (`--preset=apps`) at the repo root with `nx.json`,
root `package.json` (nx + @nx/* pinned exact per D-15), `tsconfig.base.json`, `eslint.config.mjs`,
`.gitignore`, and the preserved `.planning/` + `CLAUDE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-bootstrap-engine-spike-gated/01-CONTEXT.md
@.planning/phases/01-workspace-bootstrap-engine-spike-gated/01-RESEARCH.md
@./CLAUDE.md

<notes>
- Platform is Windows arm64 / PowerShell Core; the bootstrap runbook is Git Bash. Use the Bash tool
  for the runbook (it is Git Bash). ASCII only; never `git add .`/`-A`/`-u` (CLAUDE.md) -- stage
  generated files by name.
- The bootstrap runbook is `01-RESEARCH.md` "Bootstrap runbook (Mechanism B)" (lines ~514-551).
  Follow it step-for-step. The AI-agent env note (CNW reads `CLAUDECODE`/`OPENCODE` and switches to
  non-interactive NDJSON) is why the explicit flags below are mandatory (D-03).
- This plan is `autonomous: false`: Task 3 is a `checkpoint:human-verify` blocking gate that reviews
  the full `git status` diff and HEAD-unchanged check BEFORE the commit. Do NOT commit before the
  human approves the diff.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-bootstrap state and move planning artifacts aside</name>
  <files>(no repo files yet) scratch dir from mktemp -d; moves .planning/ + CLAUDE.md aside</files>
  <read_first>
    - .planning/phases/01-workspace-bootstrap-engine-spike-gated/01-RESEARCH.md (Bootstrap runbook, steps 0-1; Runtime State Inventory "Tracked files to preserve")
    - .planning/phases/01-workspace-bootstrap-engine-spike-gated/01-CONTEXT.md (D-01, D-02, D-03)
    - ./CLAUDE.md (git rules: no `git add .`; shell rules: no `cd <path> &&`; prefer `git mv`)
  </read_first>
  <action>
    Confirm a clean tree with `git status --porcelain` (MUST be empty; abort if not). Capture
    `HEAD_BEFORE=$(git rev-parse HEAD)` and `BR=$(git rev-parse --abbrev-ref HEAD)` (the actual
    branch is `main`; align `--defaultBase` to it per D-03). Create a scratch dir OUTSIDE the repo
    via `mktemp -d`. Move `.planning/` and `CLAUDE.md` into the scratch dir with plain `mv` (keep out
    of index churn) so the repo root then holds ONLY `.git/`. Record `HEAD_BEFORE`, `BR`, and the
    scratch path in the SUMMARY for the later restore + verification steps.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain)" && test -d "$SCRATCH/.planning" && test -f "$SCRATCH/CLAUDE.md" && test ! -e ./.planning && test ! -e ./CLAUDE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` was empty before any move (clean tree precondition met).
    - `HEAD_BEFORE` SHA captured and recorded; `BR === "main"`.
    - After the move, repo root contains only `.git/` (no `.planning/`, no `CLAUDE.md`).
    - `.planning/` and `CLAUDE.md` exist intact in the scratch dir.
  </acceptance_criteria>
  <done>Tracked planning artifacts are safely outside the repo; pre-bootstrap HEAD recorded; root holds only `.git/`.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the workspace in a temp sibling and copy over the preserved .git/</name>
  <files>nx.json, package.json, tsconfig.base.json, eslint.config.mjs, .gitignore, .editorconfig, .prettierrc</files>
  <read_first>
    - .planning/phases/01-workspace-bootstrap-engine-spike-gated/01-RESEARCH.md (Bootstrap runbook steps 2-5; Pitfall 5 dotfile copy; "AI-agent env note"; Runtime State Inventory git-init scoping note)
    - .planning/phases/01-workspace-bootstrap-engine-spike-gated/01-CONTEXT.md (D-03 flag verification, D-04 preset=apps, D-15 exact pins)
    - .planning/phases/01-workspace-bootstrap-engine-spike-gated/01-RESEARCH.md (Standard Stack -> Installation block: exact root devDependency pins)
  </read_first>
  <action>
    First run `npx create-nx-workspace@23.0.1 --help` and confirm the exact spelling of
    `--preset`, `--nxCloud`, `--skipGit`, `--defaultBase`, `--packageManager`, `--no-interactive`
    (D-03 -- CNW 23 flags). Then generate into a temp sibling dir (e.g. `atc-temp`):
    `npx create-nx-workspace@23.0.1 atc-temp --preset=apps --packageManager=npm --nxCloud=skip
    --skipGit --no-interactive --defaultBase="$BR"`. `--skipGit` + generation into a NAMED subdir
    guarantees CNW's `git init` runs only inside `atc-temp/` and never touches the root `.git/`
    (Nx-source-verified, D-02). Copy generated contents dotfile-safe over the preserved `.git/`:
    `cp -R atc-temp/. ./` (the trailing `/.` copies dotfiles -- Pitfall 5; if `.gitignore` does not
    land, fall back to `(shopt -s dotglob; cp -R atc-temp/* ./)` or `rsync -a --exclude node_modules
    atc-temp/ ./`). Remove `./node_modules` and `atc-temp/` (do not carry the temp install). Restore
    `.planning/` + `CLAUDE.md` from the scratch dir with `mv`. Run `npm install` at the root, then
    pin the locked root devDependencies EXACT (D-15): `npm install --save-dev --save-exact
    nx@23.0.1 @nx/devkit@23.0.1 @nx/js@23.0.1 @nx/plugin@23.0.1 @nx/vitest@23.0.1 @nx/angular@23.0.1
    typescript@6.0.3 @angular/compiler-cli@22.0.4` (compiler-cli at the ROOT as a devDependency so the
    spike app + core resolve it; it stays a PEER in the plugin's own manifest -- D-14 watch-note).
    Run `npx nx report` to confirm resolution.
  </action>
  <verify>
    <automated>npx nx report 2>&1 | rg -q "nx +23\.0\.1" && test -f ./.gitignore && test -f ./nx.json && test -d ./.planning && test -f ./CLAUDE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `create-nx-workspace@23.0.1 --help` flag spellings confirmed before the real run (D-03).
    - Generated with `--preset=apps --packageManager=npm --nxCloud=skip --skipGit --no-interactive --defaultBase=main`.
    - `.gitignore` and `.editorconfig` landed at the root (dotfile copy succeeded -- Pitfall 5).
    - `./node_modules` from the temp copy removed; `atc-temp/` removed.
    - `.planning/` + `CLAUDE.md` restored at the root.
    - `npx nx report` shows `nx 23.0.1` and the `@nx/*` plugins at `23.0.1`.
    - `npm ls nx @angular/compiler-cli typescript` shows `23.0.1` / `22.0.4` / `6.0.3` (CMP-01).
  </acceptance_criteria>
  <done>An empty integrated Nx 23 workspace exists at the repo root over the preserved `.git/`, with planning artifacts restored and the locked toolchain resolved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the bootstrap did not clobber tracked files, then commit</name>
  <files>.git/ (preserved), .planning/ (restored), CLAUDE.md (restored)</files>
  <action>
    Pause for human verification BEFORE committing. Present the HEAD-unchanged check and the full
    `git status` diff for review; commit the bootstrap only after the human approves the diff. Do NOT
    use `git add .`/`-A`/`-u` -- stage the generated workspace files by name (CLAUDE.md git rules).
  </action>
  <what-built>
    Mechanism B bootstrap: the Nx 23 integrated Angular monorepo was generated in a temp sibling and
    copied over the preserved root `.git/`; `.planning/` + `CLAUDE.md` were restored; the locked
    toolchain (nx 23.0.1 / @angular/compiler-cli 22.0.4 / typescript 6.0.3) is installed and
    `nx report` resolves.
  </what-built>
  <how-to-verify>
    1. Verify HEAD is unchanged: run `test "$(git rev-parse HEAD)" = "<HEAD_BEFORE from Task 1>"` --
       MUST print nothing / exit 0 (no history rewrite).
    2. Review the FULL `git status` (not `--porcelain`): confirm it shows ONLY intended additions
       (nx.json, package.json, tsconfig.base.json, eslint.config.mjs, .gitignore, .editorconfig,
       app/lib placeholder dirs) and that NO `.planning/**` or `CLAUDE.md` line appears as modified
       or deleted (they were preserved/restored byte-identical).
    3. Confirm `.planning/` and `CLAUDE.md` are present and unchanged (`git status` shows them clean).
    4. Spot-check `nx.json` is the classic `--preset=apps` shape (no TS-solution `isTsSolutionSetup`
       artifacts) and `npx nx report` lists nx 23.0.1.
    If all four hold, this is GO to commit. If `git status` shows any unexpected modification/deletion
    of tracked planning files, or HEAD changed, this is a NO-GO -- do NOT commit; restore from scratch.
  </how-to-verify>
  <verify>
    <automated>test "$(git rev-parse HEAD)" = "$HEAD_BEFORE" && git status --porcelain 2>&1 | rg -vq "^.. (\.planning|CLAUDE\.md)" && echo "HEAD-INTACT"</automated>
  </verify>
  <acceptance_criteria>
    - HEAD SHA equals the captured `HEAD_BEFORE` (no history rewrite).
    - Full `git status` shows only intended workspace additions; no `.planning/**`/`CLAUDE.md` modified or deleted.
    - Human approved the diff before commit; files staged by name (no `git add .`).
  </acceptance_criteria>
  <done>The bootstrap is committed only after a human confirms HEAD is intact and no tracked planning files were clobbered.</done>
  <resume-signal>Type "approved" to commit the bootstrap, or describe the unexpected diff to roll back.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> dev machine | `create-nx-workspace@23.0.1` + pinned packages downloaded and executed during scaffold |
| temp sibling dir -> repo root | generated files copied over the live `.git/` (state-preservation hazard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `npx create-nx-workspace` arbitrary code on scaffold | mitigate | Pin `@23.0.1` exact; explicit flags only; built-in `--preset=apps` (no third-party preset, `--trustThirdPartyPreset` default false); review full `git status` of generated files at the blocking human checkpoint before commit (V14) |
| T-01-02 | Tampering | Dotfile-copy clobbers / drops tracked files (`.gitignore`, `.planning/`) over live `.git/` | mitigate | Move `.planning/`+`CLAUDE.md` aside first; `--skipGit` + named-subdir generation provably never touches root `.git/`; post-copy `git status` + HEAD-unchanged verification at the blocking checkpoint |
| T-01-03 | Spoofing/Tampering | Slopsquatted/typosquatted dependency | mitigate | All packages are official `nrwl`/`angular`/`microsoft` org packages, pinned EXACT (D-15); Package Legitimacy Audit in 01-RESEARCH.md (no new/untrusted packages) |
| T-01-SC | Tampering | npm installs (supply chain) | mitigate | EXACT pins; `--nxCloud=skip` avoids onboarding network calls; no postinstall scripts introduced by our code; all packages [Approved] in the legitimacy audit (no [ASSUMED]/[SUS]/[SLOP]) |
</threat_model>

<verification>
- `git status` clean before bootstrap; HEAD SHA unchanged after (no history rewrite).
- `.planning/` + `CLAUDE.md` restored byte-identical (clean in `git status`).
- `npx nx report` resolves nx 23.0.1 + @nx/* 23.0.1; `npm ls nx @angular/compiler-cli typescript`
  shows 23.0.1 / 22.0.4 / 6.0.3 (CMP-01).
- `.gitignore` + `.editorconfig` present at root (dotfile copy verified).
- Human-approved full `git status` diff before commit.
</verification>

<success_criteria>
The repo is an Nx 23 integrated Angular monorepo (created via `create-nx-workspace --preset=apps`)
with the locked toolchain installed, the pre-existing `.git/` preserved (HEAD unchanged), and
`.planning/` + `CLAUDE.md` restored intact -- verified by a human review of the full `git status`
before commit. (WS-01, CMP-01)
</success_criteria>

<output>
Create `.planning/phases/01-workspace-bootstrap-engine-spike-gated/01-01-SUMMARY.md` when done.
Record `HEAD_BEFORE`, `BR`, the scratch path used, the exact CNW flags confirmed via `--help`, and
the `nx report` toolchain versions.
</output>
