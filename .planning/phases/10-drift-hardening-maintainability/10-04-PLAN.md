---
phase: 10-drift-hardening-maintainability
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/core/ts99-leak.integration.spec.ts
autonomous: true
requirements: [HARD-05]
must_haves:
  truths:
    - "A regression spec feeds a REAL NG8xxx fixture's diagnostics through renderReport(..., { color: false }) and asserts the output renders an NG#### label and contains NO `TS-99` substring (D-11)"
    - "The spec exercises the REAL cli.formatDiagnostics (via the renderReport seam) -- a ts.formatDiagnostics fake would pass vacuously while production leaks TS-998xxx"
  artifacts:
    - path: "packages/angular-typechecker/src/core/ts99-leak.integration.spec.ts"
      provides: "HARD-05 TS-99 leak regression on the color:false output path via the real cli.formatDiagnostics"
      contains: "not.toContain('TS-99')"
  key_links:
    - from: "ts99-leak.integration.spec.ts"
      to: "renderReport (loads real cli.formatDiagnostics) + a real NG8xxx fixture via runTypecheck"
      via: "runTypecheck -> renderReport({ color: false }) -> assert NG#### present, TS-99 absent"
      pattern: "renderReport"
---

<objective>
Author a HARD-05 regression spec asserting that no `TS-99` substring (a raw,
un-rewritten negative NG code) survives the `color: false` output path. It feeds a
REAL NG8xxx-producing fixture's diagnostics through `renderReport(..., { color: false })`
-- the seam that loads the real `cli.formatDiagnostics`, which internally rewrites
`TS-998xxx` to `NG8xxx` via Angular's `replaceTsWithNgInErrors` -- and asserts the
output contains an `NG####` label and NO `TS-99`.

Purpose: This is the only requirement that guards a RUNTIME rewrite, not a type or
config. The rewrite is Angular's (`replaceTsWithNgInErrors`, NOT exported at runtime
despite its `.d.ts` declaration), so the spec MUST go through the real
`cli.formatDiagnostics` via `renderReport` -- a `ts.formatDiagnostics` fake does NOT
rewrite and would pass vacuously while production leaks `TS-998xxx`. Independent of
Plans 01-03 (new file; exercises the existing render seam against a committed
fixture) -- runs in Wave 1 in parallel.
Output: 1 new integration-tier spec file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-drift-hardening-maintainability/10-CONTEXT.md
@.planning/phases/10-drift-hardening-maintainability/10-RESEARCH.md
@.planning/phases/10-drift-hardening-maintainability/10-PATTERNS.md
@packages/angular-typechecker/src/core/render-report.ts
@packages/angular-typechecker/src/core/diagnostic-codes.ts

<interfaces>
<!-- The render seam (render-report.ts:61-73): -->
<!--   renderReport(result: Pick<CoreResult,'diagnostics'>, options: { color: boolean; pathBase?; failFast? }): Promise<string> -->
<!--   It loads the REAL cli.formatDiagnostics (loadCompilerCli) -> the rewrite path. -->

<!-- The real fixture (extended-promoted, NG8101 as Error -- a real NG8xxx producer): -->
<!--   runTypecheck({ tsConfigPath: extendedPromotedTsConfig }) -> result.diagnostics has d.code === NG(8101) -->
<!--   (extended.promotion.integration.spec.ts:38-50 proves NG(8101) is present.) -->

<!-- The encoding helper to import (diagnostic-codes.ts:39): NG(code) = -990000 - code. -->
<!--   NG(8101) === -998101. Assert NG codes symbolically (NG(8101)), never the bare 4-digit. -->

<!-- VERIFIED live: cli.formatDiagnostics([{code: ngErrorCode(8109)}], host) -> contains "NG8109", no "TS-99"; -->
<!--   ts.formatDiagnostics -> "TS-998109" (LEAKS). replaceTsWithNgInErrors is undefined at runtime. -->
<!-- ANTI-PATTERN: format-report.spec.ts:62 uses a ts.formatDiagnostics fake for OTHER assertions -- -->
<!--   do NOT copy that for the TS-99 case (it would pass vacuously). -->

<!-- The existing positive analog (render-report.spec.ts:74-81): asserts out.toContain('NG8109'). -->
<!-- HARD-05 = the same shape (real NG8xxx through renderReport color:false) PLUS not.toContain('TS-99'). -->

<!-- Fixture-path idiom (extended.promotion.integration.spec.ts:27-35): -->
<!--   const packageRoot = join(dirname(fileURLToPath(import.meta.url)), '..', '..'); -->
<!--   const workspaceRoot = join(packageRoot, '..', '..'); -->
<!--   join(workspaceRoot, 'fixtures', 'extended-promoted', 'tsconfig.app.json') -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the HARD-05 TS-99 leak regression spec via the real cli.formatDiagnostics seam</name>
  <files>packages/angular-typechecker/src/core/ts99-leak.integration.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/render-report.ts:61-73 (the `renderReport` seam that loads the REAL `cli.formatDiagnostics` -> the rewrite path; the `{ color: false }` ANSI-strip route)
    - packages/angular-typechecker/src/core/render-report.spec.ts:74-81 (the EXISTING "forwards an NG-encoded diagnostic code through to formatReport output" positive case -- `out.toContain('NG8109')`; HARD-05 is the same shape plus the negative `not.toContain('TS-99')`) and :7-9 (the ESC-from-charcode ASCII convention)
    - packages/angular-typechecker/src/core/extended.promotion.integration.spec.ts:1-50 (the `extended-promoted` fixture path + `runTypecheck` producing `d.code === NG(8101)`; the fixture-path resolution idiom)
    - 10-RESEARCH.md "Pattern 5" (the VERIFIED real-vs-fake behavior), Pitfall 2 (spec passes but production leaks TS-99), the anti-pattern "do NOT write the HARD-05 spec against ts.formatDiagnostics or a vi.fn fake" + "do NOT call cli.replaceTsWithNgInErrors directly (undefined at runtime)", assumption A3 (extended-promoted NG8101 is the recommended reuse; extended-v13 also works)
    - 10-PATTERNS.md "HARD-05 leak spec" section + "Shared Patterns > Real-cli.formatDiagnostics rendering seam" + "ASCII-only / ESC-from-charcode"
    - packages/angular-typechecker/src/core/diagnostic-codes.ts:39 (`NG`)
  </read_first>
  <action>
    Create `packages/angular-typechecker/src/core/ts99-leak.integration.spec.ts` (integration tier). Resolve the `extended-promoted` fixture `tsconfig.app.json` via the `fileURLToPath`/`packageRoot`/`workspaceRoot` idiom (A3: `extended-promoted` carries NG8101 as a real NG8xxx producer; `extended-v13` is an acceptable alternative). In the test: `const result = await runTypecheck({ tsConfigPath: extendedPromotedTsConfig });` then sanity-assert a real NG8xxx is present (`expect(result.diagnostics.some((d) => d.code === NG(8101))).toBe(true)` -- import `NG` from `./diagnostic-codes`; assert symbolically, never the bare `8101`). Render via the REAL seam: `const out = await renderReport({ diagnostics: result.diagnostics }, { color: false });`. Assert BOTH the positive `expect(out).toMatch(/NG\d{4}/)` (an NG#### label rendered) AND the negative `expect(out).not.toContain('TS-99')` (no raw un-rewritten negative NG code). MUST go through `renderReport` (the real `cli.formatDiagnostics`) -- do NOT build a `ts.formatDiagnostics` fake or a `vi.fn` (they skip `replaceTsWithNgInErrors` and pass vacuously while leaking `TS-998xxx`); do NOT call `cli.replaceTsWithNgInErrors` directly (undefined at runtime). Name the top-level describe/it so `-t "TS-99"` selects it. ASCII only -- the assertion string is the literal ASCII `TS-99`; if any ANSI control char assertion is needed use `String.fromCharCode(0x1b)`.
  </action>
  <verify>
    <automated>npx nx run angular-typechecker:test -t "TS-99"</automated>
  </verify>
  <acceptance_criteria>
    - `npx nx run angular-typechecker:test -t "TS-99"` passes (real NG8xxx flows through `renderReport({ color: false })`; output has `NG####`, no `TS-99`)
    - The spec calls `renderReport(...)` (the real `cli.formatDiagnostics` seam) -- `git grep -c "ts.formatDiagnostics\|vi.fn\|replaceTsWithNgInErrors" -- packages/angular-typechecker/src/core/ts99-leak.integration.spec.ts` returns 0 (no fake, no direct rewrite call)
    - The spec asserts BOTH `out.toMatch(/NG\d{4}/)` (positive) AND `out.not.toContain('TS-99')` (negative)
    - The NG8xxx sanity check uses the symbolic `NG(8101)` from `diagnostic-codes.ts`, never the bare `8101`
    - The spec uses a real fixture via `runTypecheck` (not a hand-built diagnostic with a fabricated negative code)
  </acceptance_criteria>
  <done>A regression spec proves no `TS-99` survives the `color:false` path while an `NG####` label renders, exercising the real `cli.formatDiagnostics` rewrite; green against Angular 22.0.4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ts99-leak spec -> renderReport -> real cli.formatDiagnostics | The spec exercises the production render path against a committed fixture; the rewrite runs inside the already-installed `@angular/compiler-cli`; no external/untrusted input |
| spec -> extended-promoted fixture | Reads a committed in-repo fixture tsconfig + sources; no network, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Spoofing / Tampering | A fake formatter makes the spec pass while production leaks TS-99 | mitigate | The spec MUST go through `renderReport` (real `cli.formatDiagnostics`); the acceptance grep forbids `ts.formatDiagnostics`/`vi.fn`/`replaceTsWithNgInErrors` in the file (RESEARCH Pitfall 2 / anti-patterns) |
| T-10-04-02 | Denial of service | `runTypecheck` on the fixture is slow/flaky (cold compiler) | accept | Reuses an existing small fixture; the existing 30000ms testTimeout (STATE [08-02]) covers cold-compiler warmup; same convention as the existing integration specs |
| T-10-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs (D-03: zero new dependency); no supply-chain surface added |
</threat_model>

<verification>
- `npx nx run angular-typechecker:test -t "TS-99"` passes (NG#### rendered, TS-99 absent).
- The spec routes through `renderReport` (real `cli.formatDiagnostics`), not a fake; uses a real NG8xxx fixture; asserts the symbolic `NG(8101)`.
</verification>

<success_criteria>
- HARD-05: no `TS-99` substring survives the `color: false` output path; an `NG####` label renders; the spec exercises the real `cli.formatDiagnostics` rewrite (not a vacuous fake).
</success_criteria>

<output>
Create `.planning/phases/10-drift-hardening-maintainability/10-04-SUMMARY.md` when done.
</output>
