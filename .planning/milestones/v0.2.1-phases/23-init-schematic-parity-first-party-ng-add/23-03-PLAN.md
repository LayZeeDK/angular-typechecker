---
phase: 23-init-schematic-parity-first-party-ng-add
plan: 03
type: execute
wave: 2
depends_on: [23-01, 23-02]
files_modified:
  - packages/angular-typechecker/src/generators/ng-add/generator.ts
  - packages/angular-typechecker/src/generators/ng-add/schema.json
  - packages/angular-typechecker/src/generators/ng-add/schema.d.ts
  - packages/angular-typechecker/src/generators/ng-add/ng-add.spec.ts
  - packages/angular-typechecker/src/schematics/ng-add/schematic.ts
  - packages/angular-typechecker/collection.json
  - packages/angular-typechecker/src/schematics/configuration/nx-generators-surface-regression.spec.ts
autonomous: true
requirements: [NGADD-01]

must_haves:
  truths:
    - "NGADD-01 (D-01/D-09): ngAddGenerator(tree, schema) is an Nx-devkit generator re-exported via `export default convertNxGenerator(ngAddGenerator)` at src/schematics/ng-add/schematic.ts, registered under the reserved `ng-add` name in collection.json -- NOT a hand-written @angular-devkit/schematics Rule (an install-orchestration schematic composing the shared core; in-charter per D-09)."
    - "NGADD-01 (D-02/D-03): ngAddGenerator enumerates getProjects(tree), filters projectType in {application, library} (skips e2e/other), and COMPOSES configurationGenerator(tree, { project, skipFormat: true }) per in-scope project -- it re-implements NO per-project wiring; idempotency + collision-by-builder-id are inherited from the Phase-22 write-fork."
    - "NGADD-01 (RF-01): ngAddGenerator defensively moves any dependencies['angular-typechecker'] entry to devDependencies via updateJson and returns VOID (never a GeneratorCallback -- no redundant npm install task)."
    - "NGADD-01 (RF-02): ngAddGenerator guards on tree.exists('angular.json') -- when ABSENT it does the devDependency-ensure + prints guidance ONLY, wiring no targets and seeding no nx.json."
    - "NGADD-01 (D-06): the shared NO_CACHING_NOTICE (imported from init/generator.ts) is printed ONCE via logger.info after wiring; formatFiles runs ONCE at the end (skipFormat:true on every inner configurationGenerator call)."
    - "NGADD-01: collection.json declares `ng-add`; generators.json does NOT declare ng-add (Nx `nx add` runs `<pkg>:init`, UNCHANGED -- Pitfall 5); the collection stays Nx-invisible via `generators ?? schematics`."
  artifacts:
    - path: "packages/angular-typechecker/src/generators/ng-add/generator.ts"
      provides: "The composed ngAddGenerator (guard + defensive devDep move + enumerate-filter-compose loop + notice-once)."
      contains: "configurationGenerator"
      min_lines: 30
    - path: "packages/angular-typechecker/src/generators/ng-add/schema.json"
      provides: "Minimal ng-add schema: optional project? + skipFormat?."
      contains: "skipFormat"
    - path: "packages/angular-typechecker/src/generators/ng-add/schema.d.ts"
      provides: "NgAddGeneratorSchema type."
      contains: "NgAddGeneratorSchema"
    - path: "packages/angular-typechecker/src/schematics/ng-add/schematic.ts"
      provides: "convertNxGenerator(ngAddGenerator) thin re-export."
      contains: "convertNxGenerator"
    - path: "packages/angular-typechecker/src/generators/ng-add/ng-add.spec.ts"
      provides: "angular.json-seeded spec: auto-wire-all, idempotency, throw-on-non-ours, skip e2e/other, RF-02 guard, devDep move, notice-once."
      min_lines: 100
  key_links:
    - from: "src/generators/ng-add/generator.ts"
      to: "src/generators/configuration/generator.ts (configurationGenerator)"
      via: "await configurationGenerator(tree, { project, skipFormat: true }) per in-scope project"
      pattern: "configurationGenerator\\(tree"
    - from: "src/generators/ng-add/generator.ts"
      to: "src/generators/init/generator.ts (NO_CACHING_NOTICE)"
      via: "import { NO_CACHING_NOTICE } -- single-source notice (D-06)"
      pattern: "NO_CACHING_NOTICE"
    - from: "collection.json#schematics.ng-add.factory"
      to: "src/schematics/ng-add/schematic.ts"
      via: "factory path -> convertNxGenerator(ngAddGenerator)"
      pattern: "convertNxGenerator"
---

<objective>
Ship the first-party `ng-add` schematic (NGADD-01): a composed `ngAddGenerator` that, on an Angular CLI
workspace, iterates `angular.json#projects`, wires a `typecheck` target into EVERY application + library
project by composing the shipped `configurationGenerator`, ensures the devDependency defensively, prints
the shared no-caching notice once, and returns VOID -- plus the `ng-add` schema, the `convertNxGenerator`
re-export, the `collection.json` `ng-add` entry, and the auto-wire-all/idempotency/guard spec.

Purpose: `ng add angular-typechecker` becomes the install convenience that wires the whole workspace, while
the Phase-22 `configuration` schematic stays the single-project entry point. `ng-add` COMPOSES the shared
write-fork -- it re-implements nothing. This is the only genuinely new logic in the phase, and even it is
assembled from shipped pieces (`getProjects` + `configurationGenerator` + `logger.info`). Depends on
Plan 01 (imports `NO_CACHING_NOTICE`) and Plan 02 (the ACP-01 peers/lint gate must be green before this
plan's `nx lint` verification).

Output: `src/generators/ng-add/{generator.ts,schema.json,schema.d.ts,ng-add.spec.ts}`,
`src/schematics/ng-add/schematic.ts`, the `collection.json` ng-add entry, and the extended
`nx-generators-surface-regression.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-init-schematic-parity-first-party-ng-add/23-CONTEXT.md
@.planning/phases/23-init-schematic-parity-first-party-ng-add/23-RESEARCH.md
@.planning/phases/23-init-schematic-parity-first-party-ng-add/23-PATTERNS.md

<interfaces>
<!-- Contracts + exact analog shapes. Use directly; no codebase exploration needed. -->

From @nx/devkit (already a pinned dependency):
  getProjects(tree: Tree): Map<string, ProjectConfiguration>   // reads the angular.json polyfill; projectType normalized
  updateJson(tree, path, updater): void
  formatFiles(tree): Promise<void>
  logger.info(message: string): void
  convertNxGenerator(generator): (options) => Rule

The SHARED generator to COMPOSE (src/generators/configuration/generator.ts, UNCHANGED -- do not fork it):
  export default async function configurationGenerator(tree, schema): Promise<void>;
  // On an angular.json tree its CLI branch writes architect.typecheck = { builder: 'angular-typechecker:typecheck',
  //   options.tsConfig: [buildLeaf, specLeaf] }, is idempotent for OUR target (preserves user keys), and
  //   THROWS a located error on a same-named NON-ours target. It does NOT call initGenerator on the CLI
  //   branch and does NOT call logger.info -- so the only notice on ng-add's main path is ng-add's own.
  //   schema: { project: string; tsConfig?: string; targetName?: string; skipFormat?: boolean }

The single-source notice to IMPORT (Plan 01 exports it):
  import { NO_CACHING_NOTICE } from '../init/generator';

Analog to CLONE for the composed generator's default-export signature:
  - src/generators/init/generator.ts L61-64 (async function default export, Tree + schema -> Promise<void>)
Analog for the thin re-export (whole file):
  - src/schematics/configuration/schematic.ts
Analog for the schema shapes:
  - src/generators/init/schema.json (skipFormat + cli:nx + additionalProperties:false header)
  - src/generators/configuration/schema.json (the `project` string property + its $default argv index 0)
  - src/generators/configuration/schema.d.ts (the interface shape)
Analog for the spec substrate (whole file, 248 lines -- reuse its helpers):
  - src/generators/configuration/configuration-angular-cli.spec.ts (writeAngularJson/writeLeaf/assertCliSubstrate/seedNgxLeafletWorkspace + the readProjectConfiguration read-back)
Analog for the surface-regression extension:
  - src/schematics/configuration/nx-generators-surface-regression.spec.ts (as extended by Plan 01 to also read collection.json)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The composed ngAddGenerator + schema + schema.d.ts + the auto-wire-all/idempotency/guard spec</name>
  <files>packages/angular-typechecker/src/generators/ng-add/generator.ts, packages/angular-typechecker/src/generators/ng-add/schema.json, packages/angular-typechecker/src/generators/ng-add/schema.d.ts, packages/angular-typechecker/src/generators/ng-add/ng-add.spec.ts</files>
  <behavior>
    - Auto-wire-all: seed the 2-project ngx-leaflet workspace (app root "" + lib projects/ngx-leaflet, each with build+spec leaves); after ngAddGenerator(tree, {}) BOTH projects carry `typecheck` -> `{ executor: 'angular-typechecker:typecheck', options: { tsConfig: [<build leaf>, <spec leaf>] } }` (read back via readProjectConfiguration; the polyfill normalizes architect->targets/builder->executor).
    - Idempotent for OUR target: a second ngAddGenerator run leaves the targets unchanged and preserves user-added options/configurations (inherited from configurationGenerator).
    - Throws on a same-named NON-ours target: a project pre-seeded with a `typecheck` target using a different builder makes ngAddGenerator reject with /already has a "typecheck" target/.
    - Skips e2e/other project types: a project seeded with a missing or non-{application,library} projectType receives NO typecheck target.
    - RF-02 guard: on a tree WITHOUT angular.json (createTreeWithEmptyWorkspace then keep nx.json OR delete it but never write angular.json), ngAddGenerator wires NO target, seeds NO nx.json targetDefaults, prints guidance via logger.info, and (see devDep move) still ensures the devDependency.
    - devDependency move (RF-01 backstop): with angular-typechecker seeded in package.json `dependencies`, ngAddGenerator deletes it from `dependencies` and sets it under `devDependencies` (same version).
    - Notice ONCE: vi.spyOn(logger, 'info') -- on the angular.json main path NO_CACHING_NOTICE is logged exactly once (configurationGenerator logs nothing on the CLI branch).
  </behavior>
  <read_first>
    - packages/angular-typechecker/src/generators/configuration/generator.ts (whole file -- the CLI branch L233-275 ngAddGenerator composes; do NOT modify it)
    - packages/angular-typechecker/src/generators/init/generator.ts (the NO_CACHING_NOTICE export from Plan 01 + the default-export signature)
    - packages/angular-typechecker/src/generators/init/schema.json + src/generators/configuration/schema.json + configuration/schema.d.ts (the schema shapes to mirror)
    - packages/angular-typechecker/src/generators/configuration/configuration-angular-cli.spec.ts (the whole substrate harness to reuse)
    - 23-PATTERNS.md L118-226 (the ngAddGenerator structure: guard, defensive devDep move, compose loop, format-once, notice-once + the schema shapes) + the anti-patterns L186-188
    - 23-RESEARCH.md L161-199 (Patterns 1-3), L216-220 (anti-patterns), L306-322 (the minimal schema), L370-377 (the NGADD-01 test map)
  </read_first>
  <action>
    (1) Create `src/generators/ng-add/schema.d.ts`: `export interface NgAddGeneratorSchema { project?: string;
    skipFormat?: boolean; }` (neither required; default is auto-wire-all).

    (2) Create `src/generators/ng-add/schema.json` cloning the `init/schema.json` header (`$schema`,
    `$id: "NgAddGeneratorSchema"`, `title`, `cli: "nx"`, `type: "object"`, `additionalProperties: false`)
    with `properties`: `project` (string, description "Scope wiring to a single project; default all app +
    library projects.") and `skipFormat` (boolean, default false). NO `required` array. Schematic schemas
    natively accept `cli:"nx"`/`$id` -- no sanitized copy needed (the Phase-21 sanitized-schema concern is
    Architect-BUILDER-only).

    (3) Create `src/generators/ng-add/generator.ts` -- the ONE new logic file. Default-export
    `async function ngAddGenerator(tree: Tree, schema: NgAddGeneratorSchema): Promise<void>`. Import
    `{ formatFiles, getProjects, logger, updateJson }` + `type { Tree }` from `@nx/devkit`,
    `configurationGenerator from '../configuration/generator'`, `{ NO_CACHING_NOTICE } from '../init/generator'`,
    and `type { NgAddGeneratorSchema } from './schema'`. Body, in order:
      (a) Defensive devDependency ensure (RF-01 backstop, unconditional -- always safe): `updateJson(tree,
          'package.json', (pkg) => { ... })` -- if `pkg.dependencies?.['angular-typechecker']` exists, delete
          it from `dependencies`, `pkg.devDependencies ??= {}`, and set
          `pkg.devDependencies['angular-typechecker'] ??= version`. Return pkg. Do NOT use
          `addDependenciesToPackageJson` (it cannot move deps->devDeps and returns an install callback).
      (b) RF-02 guard: `if (!tree.exists('angular.json')) { logger.info(<guidance string>); return; }` -- no
          target wiring, no nx.json. The guidance string is end-user-facing (no internal ids): e.g. that no
          angular.json was found, angular-typechecker was ensured as a devDependency, and to wire targets run
          `ng add` from an Angular CLI workspace (or use the Nx `nx g angular-typechecker:configuration`/`nx add`
          path). Return VOID.
      (c) Enumerate + filter + compose: `for (const [name, project] of getProjects(tree)) { if
          (project.projectType === 'application' || project.projectType === 'library') { ... } }`. If
          `schema.project` is set, restrict to that single project (still via configurationGenerator). Inside,
          `await configurationGenerator(tree, { project: name, skipFormat: true })`. Do NOT re-implement leaf
          resolution / collision / idempotency -- all inherited.
      (d) `if (!schema.skipFormat) { await formatFiles(tree); }` ONCE after the loop.
      (e) `logger.info(NO_CACHING_NOTICE);` ONCE.
      (f) Return VOID (no GeneratorCallback -- avoids a redundant npm install task; matches the sibling
          generators which return void).

    (4) Create `src/generators/ng-add/ng-add.spec.ts` covering every case in <behavior>, reusing the
    `configuration-angular-cli.spec.ts` substrate helpers (createTreeWithEmptyWorkspace + tree.delete('nx.json')
    + writeAngularJson/writeLeaf/assertCliSubstrate + the seedNgxLeafletWorkspace shape). For the devDep-move
    case, seed package.json dependencies via writeJson/updateJson. For notice-once, `vi.spyOn(logger, 'info')`.

    JS/TS style: blank lines around control-flow/return; braces on all bodies. ASCII only.
  </action>
  <verify>
    <automated>nx test angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `nx test angular-typechecker` exits 0, including all ng-add.spec.ts cases (auto-wire-all, idempotency, throw-on-non-ours, skip-e2e/other, RF-02 guard, devDep move, notice-once).
    - `git grep -n "configurationGenerator(tree" -- packages/angular-typechecker/src/generators/ng-add/generator.ts` matches (composition, not re-implementation).
    - `git grep -n "NO_CACHING_NOTICE" -- packages/angular-typechecker/src/generators/ng-add/generator.ts` matches (imported from init, single source -- D-06).
    - ngAddGenerator returns void: `git grep -n "addDependenciesToPackageJson\|GeneratorCallback\|installPackagesTask" -- packages/angular-typechecker/src/generators/ng-add/generator.ts` returns NO matches (anti-pattern absent).
    - `git grep -n "getProjects" -- packages/angular-typechecker/src/generators/ng-add/generator.ts` matches (enumeration via the devkit polyfill, not a raw JSON walk).
  </acceptance_criteria>
  <done>ngAddGenerator composes configurationGenerator per app+library project, defensively ensures the devDependency, guards the no-angular.json case, prints the shared notice once, returns void, and every NGADD-01 behavior is green.</done>
</task>

<task type="auto">
  <name>Task 2: ng-add convertNxGenerator re-export + collection.json ng-add entry + surface-regression (ng-add stays out of generators.json)</name>
  <files>packages/angular-typechecker/src/schematics/ng-add/schematic.ts, packages/angular-typechecker/collection.json, packages/angular-typechecker/src/schematics/configuration/nx-generators-surface-regression.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/schematics/configuration/schematic.ts (the convertNxGenerator re-export template)
    - packages/angular-typechecker/collection.json (as extended by Plan 01 to declare configuration + init -- add ng-add alongside)
    - packages/angular-typechecker/generators.json (must stay configuration + init ONLY -- do NOT add ng-add here)
    - packages/angular-typechecker/src/schematics/configuration/nx-generators-surface-regression.spec.ts (as extended by Plan 01 -- extend further for ng-add)
    - 23-PATTERNS.md L30-64 (the re-export) + L230-253 (the collection.json entry) + L351-362 (the surface-regression) + 23-RESEARCH.md L216-220 (anti-pattern: never register ng-add in generators.json), L260-264 (Pitfall 5)
  </read_first>
  <action>
    (1) Create `src/schematics/ng-add/schematic.ts` (~3 lines + doc comment) cloning
    `src/schematics/configuration/schematic.ts`: `import { convertNxGenerator } from '@nx/devkit'`, `import
    ngAddGenerator from '../../generators/ng-add/generator'`, `export default convertNxGenerator(ngAddGenerator)`.

    (2) Edit `collection.json`: ADD an `ng-add` entry (the reserved name) alongside configuration + init:
    `factory: "./src/schematics/ng-add/schematic"`, `schema: "./src/generators/ng-add/schema.json"`, and an
    end-user-facing `description` (e.g. "Add angular-typechecker and wire a typecheck target into every app +
    library project."). Keep JSON Prettier-clean; ASCII only.

    (3) Extend `nx-generators-surface-regression.spec.ts`: add assertions that `collection.json` now declares
    the `ng-add` schematic factory (`./src/schematics/ng-add/schematic`) AND that `generators.json` does NOT
    declare an `ng-add` generator (Nx `nx add` runs `<pkg>:init`, never `ng-add` -- Pitfall 5; registering it
    in generators.json would change the nx add surface + add an nx-plugin-checks burden). Keep all existing
    assertions (generators/schematics fields, configuration + init factories from Plan 01).

    Do NOT touch `generators.json`. JS/TS style: blank lines around control-flow; braces on all bodies.
  </action>
  <verify>
    <automated>nx test angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `nx test angular-typechecker` exits 0, including the extended surface-regression spec.
    - `nx build angular-typechecker` exits 0 AND `test -f dist/packages/angular-typechecker/collection.json` succeeds (the ng-add factory compiles + collection.json ships).
    - `nx lint angular-typechecker` exits 0 (the full ACP-01 gate incl. Plan 02's peers is green now that Wave 1 merged).
    - `git grep -n "convertNxGenerator" -- packages/angular-typechecker/src/schematics/ng-add/schematic.ts` matches; `git grep -n "ng-add" -- packages/angular-typechecker/collection.json` matches.
    - `git grep -n "ng-add" -- packages/angular-typechecker/generators.json` returns NO matches (nx add UNCHANGED).
    - The surface-regression spec asserts BOTH the collection.json ng-add entry AND the absence of ng-add from generators.json.
  </acceptance_criteria>
  <done>The ng-add schematic re-export exists, collection.json declares ng-add, generators.json is unchanged (proven Nx-invisible + nx add unchanged), and the full test/build/lint suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ng add invocation (CLI flags + workspace angular.json/package.json) -> schematic writes | ngAddGenerator reads angular.json#projects + package.json and writes per-project targets + the devDependency classification via the virtual Tree. No network, no secrets, no user code execution. Threats are ASVS L1 config-write-correctness + dependency-hygiene. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | wiring a typecheck target into a wrong project (non-app/lib, or a name collision) | mitigate | Filter strictly on `projectType in {application, library}` (skips e2e/other -- Pitfall 3); the inherited configurationGenerator collision-by-builder-id THROWS a located error on a same-named non-ours target rather than clobbering (proven in ng-add.spec.ts). |
| T-23-02b | Tampering | angular-typechecker left in `dependencies` on the install-skipped edge | mitigate | Defensive `updateJson` move deps->devDeps (RF-01 backstop); ng-add.spec.ts asserts the move. |
| T-23-03 | Tampering | redundant `npm install` from a returned GeneratorCallback (lockfile churn) | mitigate | ngAddGenerator returns VOID; no addDependenciesToPackageJson / installPackagesTask (grep-asserted absent). |
| T-23-04 | Denial of Service (bad UX) | malformed angular.json / missing project / unresolvable leaves | mitigate | configurationGenerator throws clear located errors; leaves are existence-probed; RF-02 guard handles the no-angular.json tree. |
| T-23-05 | Tampering | ng-add registered where Nx sees it -> changes `nx add` | mitigate | ng-add lives in collection.json ONLY, never generators.json; `generators ?? schematics` keeps it Nx-invisible; surface-regression asserts ng-add absent from generators.json (nx add UNCHANGED -- SC4). |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | N/A -- Plan 23-03 installs no external packages (RESEARCH Package Legitimacy Audit: no new runtime packages). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `nx test angular-typechecker` green (all ng-add behaviors + the surface-regression).
- `nx build angular-typechecker` green; `dist/packages/angular-typechecker/collection.json` present.
- `nx lint angular-typechecker` green (the full ACP-01 dependency-checks gate, now that Wave 1 merged).
- Nx `nx add angular-typechecker` behavior unchanged (ng-add absent from generators.json; nx add runs `<pkg>:init`) -- SC4.
</verification>

<success_criteria>
- NGADD-01: `ng add angular-typechecker` runs a first-party `ng-add` schematic that iterates `angular.json#projects`, wires a `typecheck` target into EVERY application + library project (idempotent; skips existing OUR target; skips e2e/other; throws on a non-ours same-named target), ensures the devDependency, and prints the "no target caching on Angular CLI" notice once. The Nx `nx add` behavior is unchanged.
</success_criteria>

<output>
Create `.planning/phases/23-init-schematic-parity-first-party-ng-add/23-03-SUMMARY.md` when done.
</output>
