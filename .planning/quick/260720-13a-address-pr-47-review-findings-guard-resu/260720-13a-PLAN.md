---
phase: quick-260720-13a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/core/walk-references.ts
  - packages/angular-typechecker/src/core/walk-references.spec.ts
  - packages/angular-typechecker/src/core/infra-failure.spec.ts
  - libs/test-util/src/lib/redact-volatile.ts
  - libs/test-util/src/lib/redact-volatile.spec.ts
autonomous: true
requirements:
  - PR47-F1-walk-program-guard
  - PR47-F2-redact-present-only

must_haves:
  truths:
    - "A surviving leaf whose performCompilation returns { program: undefined } plus an UNKNOWN_ERROR_CODE (500) surfaces as a classified TypecheckInfrastructureError on BOTH the solution-walk and multi-tsconfig paths, never a raw TypeError."
    - "The infra-crashed leaf contributes 0 authored files (totalFilesCount stays verdict-neutral) and the already-pushed 500 stays in the diagnostics union for the caller to classify."
    - "redactVolatile injects the version placeholder ONLY when a version key is already present; a payload without version keeps no version key."
    - "The JSON and SARIF integration snapshots are byte-identical after the redaction change (version present in the real payloads)."
  artifacts:
    - packages/angular-typechecker/src/core/walk-references.ts
    - packages/angular-typechecker/src/core/walk-references.spec.ts
    - packages/angular-typechecker/src/core/infra-failure.spec.ts
    - libs/test-util/src/lib/redact-volatile.ts
    - libs/test-util/src/lib/redact-volatile.spec.ts
  key_links:
    - "packages/angular-typechecker/src/core/walk-references.ts gatherLeafInto -> the result.program === undefined early-return guard, mirroring run-typecheck.ts's direct-path guard ordering"
    - "packages/angular-typechecker/src/core/run-typecheck.ts handleSolutionWalk (post-walk throwIfInfrastructureFailure) and handleMultiTsConfig (post-loop throwIfInfrastructureFailure) classify the unioned 500"
    - "libs/test-util/src/lib/redact-volatile.ts redactVolatile JSON branch + redactRunVersion driver branch -> present-only spread"
---

<objective>
Address two confirmed PR #47 max code-review findings on branch
gsd/v0.2.3-machine-readable-reporters. Both are behavior-preserving root-cause
fixes in a single shared function each; no product surface changes.

Purpose:
- Finding 1 (correctness): a surviving walk/array leaf that crashes during Program
  construction (program:undefined + a 500) currently throws a RAW TypeError from
  `gatherLeafInto`'s source-file deref BEFORE the caller can re-classify the 500 as a
  TypecheckInfrastructureError. Fix once in the shared `gatherLeafInto` so BOTH the
  solution-walk and multi-tsconfig paths inherit the guard.
- Finding 2 (test-coverage): `redactVolatile` re-injects the version placeholder
  unconditionally, so a dropped tool-version regression is masked by the JSON/SARIF
  snapshots. Make redaction replace-only-when-present.

Output: two guarded source functions + focused unit tests, existing snapshots
unchanged.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@packages/angular-typechecker/src/core/walk-references.ts
@packages/angular-typechecker/src/core/run-typecheck.ts
@packages/angular-typechecker/src/core/walk-references.spec.ts
@packages/angular-typechecker/src/core/infra-failure.spec.ts
@libs/test-util/src/lib/redact-volatile.ts

Project style (CLAUDE.md / AGENTS.md): single quotes; blank lines around control-flow
and `return`; braces on every control-flow body. `nx test` (vitest/esbuild) does NOT
type-check specs, so run `nx typecheck` too (per the verify-format-and-lint learning).
Windows arm64: filter stdout with `rg`, never `grep`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard gatherLeafInto against a program:undefined infra-crash before the source-file deref</name>
  <files>packages/angular-typechecker/src/core/walk-references.ts, packages/angular-typechecker/src/core/walk-references.spec.ts, packages/angular-typechecker/src/core/infra-failure.spec.ts</files>
  <behavior>
    - Solution-walk (walk-references.spec.ts, direct walkReferences call): a single
      surviving leaf whose performCompilation returns program:undefined AND a
      diagnostic with code UNKNOWN_ERROR_CODE (500) -> walkReferences RESOLVES (no raw
      TypeError), walk.rawDiagnostics contains the 500 code, walk.totalFilesCount === 0.
    - Multi-tsconfig (infra-failure.spec.ts, runTypecheck with an array tsConfigPath
      ['/virtual/a.json','/virtual/b.json']): performCompilation returns program:undefined
      + a 500 -> the promise REJECTS with an instance of TypecheckInfrastructureError
      (never a raw TypeError).
    - Unchanged: a surviving leaf with a valid Program still contributes its authored
      source files (the existing walk/total-files-count specs stay green with no edits).
  </behavior>
  <action>
    In `gatherLeafInto` (packages/angular-typechecker/src/core/walk-references.ts), add
    an early-return guard AFTER the existing `acc.rawDiagnostics` / `rootNamesCount` /
    `rootNamePaths` / `notTypeCheckedDeclaredFiles` pushes (so the 500 carried in
    `result.diagnostics` is already in `acc.rawDiagnostics`) and BEFORE the OBS-01
    source-file loop that derefs `result.program.getTsProgram().getSourceFiles()`. The
    guard: when `result.program === undefined`, `return` (void). Mirror the direct
    path's guard shape in run-typecheck.ts (the `if (result.program === undefined)` at
    the direct single-leaf path) so the comparison type-checks against the same shim.
    Do NOT throw here and do NOT import TypecheckInfrastructureError -- walk-references
    stays pure and free of the run-typecheck import cycle; the caller's post-walk
    throwIfInfrastructureFailure (handleSolutionWalk) and post-loop
    throwIfInfrastructureFailure (handleMultiTsConfig) classify the already-unioned 500.
    Add an explanatory comment on the guard naming the ordering parity with the direct
    path and that the crashed leaf contributes 0 authored files (verdict-neutral). Update
    the adjacent OBS-01 comment clause that currently claims `result.program` is
    unconditionally live, so it reflects the new guard.

    Tests: in walk-references.spec.ts, extend the LeafSpec/stubCompilerCli helper with an
    optional flag so one leaf's performCompilation can return { diagnostics, program:
    undefined } (default stays a valid empty-source Program so every existing test is
    byte-unchanged); add ONE test for the solution-walk behavior above. In
    infra-failure.spec.ts (which already mocks ./compiler-loader with the hoisted
    performCompilation stub), add ONE test driving runTypecheck with an ARRAY tsConfigPath
    where performCompilation returns program:undefined + a 500, asserting
    `.rejects.toBeInstanceOf(TypecheckInfrastructureError)`.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- run walk-references infra-failure &amp;&amp; npx nx typecheck angular-typechecker</automated>
  </verify>
  <done>Both new tests pass (solution-walk resolves with the 500 in the union + totalFilesCount 0; multi-tsconfig rejects with TypecheckInfrastructureError). The full angular-typechecker suite and nx typecheck stay green. No existing spec assertions changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make redactVolatile replace the version placeholder only when present</name>
  <files>libs/test-util/src/lib/redact-volatile.ts, libs/test-util/src/lib/redact-volatile.spec.ts</files>
  <behavior>
    - JSON payload WITH a version key -> redactVolatile sets version to '[version]'.
    - JSON payload WITHOUT a version key -> redactVolatile returns it with NO version key
      injected (structure otherwise identical).
    - SARIF run WITH tool.driver.version -> redactRunVersion maps it to '[version]'.
    - SARIF run WITHOUT tool.driver.version -> redactRunVersion returns the run with NO
      driver.version injected.
    - The JSON + SARIF integration snapshots (machine-reporters-json / -sarif) stay
      unchanged (real payloads always carry version).
  </behavior>
  <action>
    In libs/test-util/src/lib/redact-volatile.ts, JSON branch: replace the unconditional
    `return { ...record, version: VERSION_PLACEHOLDER }` with a present-only guard -- when
    `'version' in record`, return `{ ...record, version: VERSION_PLACEHOLDER }`; otherwise
    return `record` unchanged. In `redactRunVersion`, after narrowing `driver` to a
    record, only set the placeholder when the version key is present: when `'version' in`
    the driver record, return the spread with `version: VERSION_PLACEHOLDER`; otherwise
    return `runRecord` unchanged (matching the existing missing-intermediate-level
    short-circuits). Behavior stays identical when version IS present. Update the file's
    head-comment so the JSON/SARIF bullet points state "maps ... when present".

    Tests: create libs/test-util/src/lib/redact-volatile.spec.ts importing redactVolatile
    from './redact-volatile'. Assert: (a) a JSON payload `{ version: 'x', ...rest }` is
    redacted to `[version]` and preserves rest; (b) a JSON payload with no version key
    returns an object still lacking a version key (`'version' in result` is false); (c) a
    SARIF `{ runs: [{ tool: { driver: { name, version } } }] }` maps driver.version to
    `[version]`; (d) a SARIF run whose driver omits version keeps no version key. Follow
    project style (single quotes, blank lines around returns).
  </action>
  <verify>
    <automated>npx nx test test-util &amp;&amp; npx nx typecheck test-util &amp;&amp; npx nx test angular-typechecker -- run machine-reporters</automated>
  </verify>
  <done>The redact-volatile unit tests pass for both present and absent version cases on the JSON and SARIF branches; the machine-reporters-json and machine-reporters-sarif integration snapshots pass unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiler-cli -> core walk | performCompilation may return { program: undefined } + a 500; the core must classify it as infrastructure, never deref it into a raw crash. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13a-01 | Denial of Service | gatherLeafInto source-file deref | low | mitigate | Early-return guard on `result.program === undefined` so a crashed leaf yields a classified TypecheckInfrastructureError, not an uncaught TypeError. |
| T-13a-02 | Tampering | redactVolatile masking a dropped version | low | mitigate | Replace-only-when-present so a dropped tool-version regression fails the snapshot instead of being silently re-injected. |

No new external inputs, dependencies, or product surface. Both changes are
behavior-preserving for the real (version-present, Program-defined) paths.
</threat_model>

<verification>
- `npx nx test angular-typechecker` (full suite) green, including the two new tests.
- `npx nx test test-util` green, including the new redact-volatile.spec.ts.
- `npx nx typecheck test-util` green (the new redact-volatile.spec.ts is type-checked; vitest does not type-check specs).
- `npx nx typecheck angular-typechecker` green (specs type-checked; vitest does not).
- machine-reporters-json / machine-reporters-sarif snapshots unchanged.
</verification>

<success_criteria>
- A program:undefined + 500 surviving leaf classifies as TypecheckInfrastructureError on
  both the solution-walk and multi-tsconfig paths (proven by the two new tests); no raw
  TypeError.
- redactVolatile injects the placeholder only when version is present, on both the JSON
  and SARIF branches, without changing the existing integration snapshots.
- No existing spec assertions modified beyond the additive stub flag in
  walk-references.spec.ts.
</success_criteria>

<output>
Create `.planning/quick/260720-13a-address-pr-47-review-findings-guard-resu/260720-13a-SUMMARY.md` when done.
</output>
