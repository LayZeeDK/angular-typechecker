---
phase: 36-code-scanning-gating-scanned-files-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - packages/angular-typechecker/README.md
  - packages/angular-typechecker/src/code-scanning-docs.spec.ts
autonomous: true
requirements: [GATE-02, DOC-01]

must_haves:
  truths:
    - "GATE-02 (D-04, D-05): AGENTS.md documents the human-run ruleset runbook -- add 'Require code scanning results' for BOTH angular-typechecker AND fallow, Evaluate mode FIRST, probe a `.planning/`-only PR AND a code PR + confirm neither is blocked, THEN flip to Active; `enforcement: disabled` recovery (reusing the existing Lockout-recovery pointer); fork-PR deadlock documented as an accepted limitation (read-only token -> upload skipped -> no analysis -> blocked)."
    - "GATE-02 (D-04): the runbook is documentation ONLY -- the agent NEVER flips the `main` ruleset via `gh api`; the toggle is a human maintainer action, real-CI-only, performed after this phase's PR merges."
    - "DOC-01 (D-05): the README `### SARIF and GitHub Code Scanning` section documents that GitHub's tool-status 'Scanned files' panel is CodeQL-only telemetry that third-party SARIF cannot populate (spike evidence: node-sarif-builder auto-emits `run.artifacts` yet the panel stays empty), framed as a known GitHub limitation and not a defect, in end-user language, with NO GitHub Issue filed."
    - "DOC-01: a `code-scanning-docs.spec.ts` content-tripwire (mirroring angular-cli-docs.spec.ts) locks the claim -- asserting the exact heading on the raw README string and `Scanned files`, `a GitHub limitation`, `CodeQL`, `run.artifacts` on the normalized-whitespace string so it survives prose re-wrapping."
    - "D-05: AGENTS.md needs NO tripwire -- the phase `code_review_gate` reviews the runbook for factual accuracy (the AGENTS.md self-governance rule), so an added AGENTS.md tripwire would be YAGNI."
    - "D-06: docs/test-only, additive-only vs @0.2.3 -- README is the ONLY changed file in the package `files` allowlist and its delta is the DOC-01 prose subsection only; no `src/**` runtime change, no `package.json`/manifest change, no version bump."
  artifacts:
    - "AGENTS.md (MOD: new ruleset-enablement runbook subsection extending 'The default-branch ruleset: main is PR-only' + 'Lockout recovery')"
    - "packages/angular-typechecker/README.md (MOD: new `#### The \"Scanned files\" panel stays empty (a GitHub limitation)` sub-subsection under `### SARIF and GitHub Code Scanning`)"
    - "packages/angular-typechecker/src/code-scanning-docs.spec.ts (NEW: `describe('README ### SARIF and GitHub Code Scanning -- Scanned-files limitation (docs tripwire)', ...)`)"
  key_links:
    - "The README prose (Task 2) MUST contain the exact tokens the tripwire (Task 3) asserts: `Scanned files`, `a GitHub limitation`, `CodeQL`, `run.artifacts` (the `####` heading text `The \"Scanned files\" panel stays empty (a GitHub limitation)` already carries the first two)."
    - "The AGENTS.md change MUST be code-reviewed (AGENTS.md self-governance rule at the top of the file) -- satisfied by the phase's `code_review_gate`; flag it in the SUMMARY so the reviewer knows this file is in scope."
    - "The ruleset TOGGLE is out-of-band GitHub repo state (not any committed file) and is HUMAN-only + real-CI-only (D-04, never-approve-deployments posture) -- the plan ships only the runbook; it is NOT an in-phase task."
    - "Public-repo hygiene: the shipped README + AGENTS.md prose is generic CI/docs and MUST contain no work email/domain; the repo's allowlist-inversion email-hygiene check in the normal battery flags any non-approved email-shaped token."
---

<objective>
Ship the human-run GATE-02 ruleset runbook (AGENTS.md) and the DOC-01 Scanned-files-limitation note (shipped README), plus a content-tripwire that locks the README claim against drift. The SARIF -> Code Scanning pipeline and the "Scanned files"-panel-is-inert finding are already spike-proven (PR #53, auto-memory `code-scanning-sarif-empirical-behavior`) -- this plan is prose + one tripwire, not derivation.

Purpose: the GitHub side of GATE-02 (a documented, human-executed, Evaluate-first runbook for enabling "Require code scanning results" on `main` without a lockout) and DOC-01 (an end-user explanation that the empty "Scanned files" panel is a known GitHub CodeQL-only limitation, not an angular-typechecker defect).
Output: an AGENTS.md runbook subsection, a README `####` sub-subsection, and a `code-scanning-docs.spec.ts` tripwire. Docs/test-only; README is the only changed file in the package `files` allowlist; no production code, no manifest change, no version bump (D-06). The agent NEVER flips the `main` ruleset -- that toggle is a human, real-CI-only step (D-04).
</objective>

<artifacts>
This plan produces the following NEW symbols (there are NO new runtime `src/**` symbols, NO `package.json` change):
- New file `packages/angular-typechecker/src/code-scanning-docs.spec.ts` with `describe('README ### SARIF and GitHub Code Scanning -- Scanned-files limitation (docs tripwire)', ...)`.
- New README sub-subsection heading `#### The "Scanned files" panel stays empty (a GitHub limitation)`.
- New AGENTS.md subsection documenting the "Require code scanning results" ruleset-enablement runbook.
</artifacts>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-code-scanning-gating-scanned-files-documentation/36-CONTEXT.md
@.planning/phases/36-code-scanning-gating-scanned-files-documentation/36-RESEARCH.md
@.planning/phases/36-code-scanning-gating-scanned-files-documentation/36-PATTERNS.md
@.planning/phases/36-code-scanning-gating-scanned-files-documentation/36-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the human-run "Require code scanning results" ruleset runbook to AGENTS.md (D-04, D-05)</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md (the WHOLE file: the top-of-file "Changing this file ... MUST be code-reviewed" self-governance rule; the `## The default-branch ruleset: main is PR-only` section (~224) and the `**Lockout recovery (the cost of the empty bypass):**` paragraph (~236-241) the runbook extends; the `## Parallel execution in git worktrees` heading (~243) it must be inserted BEFORE)
    - .planning/phases/36-code-scanning-gating-scanned-files-documentation/36-RESEARCH.md (Section 4 -- the verified GitHub UI labels "Required tools and alert thresholds" / "Add tool" / per-tool "Alerts"+"Security alerts" thresholds / enforcement dropdown, the fixed 6-step runbook order, the `[ASSUMED]` Evaluate-plan-tier + "Ruleset Insights" caveats to flag)
    - .planning/phases/36-code-scanning-gating-scanned-files-documentation/36-PATTERNS.md (## `AGENTS.md` -- the exact sections to extend + the 6-item runbook content + the "MUST be code-reviewed" flag)
  </read_first>
  <action>
Add a NEW subsection to `AGENTS.md`, inserted AFTER the "Lockout recovery" paragraph (end of the `## The default-branch ruleset: main is PR-only` section) and BEFORE `## Parallel execution in git worktrees`. Use an `###` heading such as `### Enabling the "Require code scanning results" ruleset (human-run, real-CI-only)`. Keep it ASCII-only (no emoji / box-drawing / em-dash / curly-quotes; use `--` for dashes, straight quotes). Match the surrounding AGENTS.md prose style. Content -- the fixed order (do NOT reorder; do NOT skip Evaluate):

1. Add the rule. Settings -> Rules -> Rulesets -> the `main` ruleset -> add "Require code scanning results". Under "Required tools and alert thresholds", "Add tool" for BOTH `angular-typechecker` AND `fallow`. Set the alert threshold conservatively so this is an ANALYSIS-EXISTENCE gate, not a second findings gate -- findings already gate via `ci`'s `test` (`nx run-many -t typecheck`) and the `fallow` job. The load-bearing block is the MISSING-analysis block, which fires regardless of the alert threshold.
2. Evaluate mode FIRST -- it records would-be blocks without blocking (GitHub's recommended de-risking). Flag the `[ASSUMED]` caveat: for repo-level rulesets, "Evaluate" availability may depend on the GitHub plan tier; if unavailable, verify on a throwaway-branch-scoped ruleset and lean on the `enforcement: disabled` recovery below -- confirm in the live UI at runbook time.
3. Probe. Push a deliberate `.planning/`-only PR AND a code-touching PR; confirm in the ruleset's evaluation view (labeled "Ruleset Insights" `[ASSUMED]` -- verify the exact label live) that NEITHER would be blocked: the planning-only PR now produces an angular-typechecker analysis (via the un-path-gate) + fallow; the code PR produces both plus the proof.
4. Flip to Active ONLY after step 3 confirms no would-be block.
5. Recovery. If it wedges the empty-bypass `main` merge button, toggle the ruleset `enforcement` to `disabled`, merge the fix, then re-enable -- exactly as the "Lockout recovery" paragraph above. NEVER add a standing bypass actor.
6. Fork-PR deadlock (accepted limitation, DOCUMENT it). A fork PR gets a read-only token -> the upload steps skip -> no analysis -> the ruleset blocks it. Low practical impact here (no external contributors; the maintainer self-merges); a future external contributor's PR needs a maintainer-side re-run or the `enforcement: disabled` toggle. Un-path-gating cannot fix forks -- the token is read-only.

Open the subsection with one line stating the agent NEVER flips the `main` ruleset (D-04): this is a human maintainer action, real-CI-only (GitHub ingestion + ruleset evaluation are provable only on GitHub), consistent with the repo's human-only-control posture for `main` protections. Do NOT include any `gh api` call that mutates the ruleset. Do NOT put a work email or its bare domain anywhere.
  </action>
  <verify>
    <automated>npx prettier --check AGENTS.md</automated>
    <human-check>The phase code_review_gate reviews this AGENTS.md change for factual accuracy against the live ci.yml + GitHub ruleset UI (the AGENTS.md self-governance rule); confirm the 6-step order and the human-only / Evaluate-first / enforcement-disabled-recovery / fork-PR-deadlock content are all present and correct.</human-check>
  </verify>
  <done>AGENTS.md carries the human-run "Require code scanning results" runbook (both tools, Evaluate first, probe two PR kinds, then Active; enforcement-disabled recovery; fork-PR deadlock accepted), states the agent never flips the ruleset, is prettier-clean, and is queued for the code_review_gate.</done>
  <acceptance_criteria>
    - AGENTS.md has a new `###` subsection between the "Lockout recovery" paragraph and `## Parallel execution in git worktrees`.
    - The runbook documents, in this order: add the rule for BOTH angular-typechecker AND fallow (conservative threshold, existence gate), Evaluate mode first, probe a `.planning/`-only PR AND a code PR, flip to Active, `enforcement: disabled` recovery, fork-PR deadlock accepted.
    - It states the agent NEVER flips the `main` ruleset (human, real-CI-only) and contains NO ruleset-mutating `gh api` call.
    - Content is ASCII-only, contains no work email/domain, and `npx prettier --check AGENTS.md` passes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add the DOC-01 "Scanned files" limitation sub-subsection to the shipped README (D-05)</name>
  <files>packages/angular-typechecker/README.md</files>
  <read_first>
    - packages/angular-typechecker/README.md (the `### SARIF and GitHub Code Scanning` section at ~694-722: the existing prose, the whole-file-alert paragraph at ~710-714, and the `#### Run from the repository root` sub-subsection at ~716-722 to mirror; the `## Storybook` heading at ~724 that must stay AFTER the new subsection)
    - .planning/phases/36-code-scanning-gating-scanned-files-documentation/36-RESEARCH.md (Section 6.2 -- the exact end-user draft; Section 6.3 -- the tripwire tokens the prose must contain)
    - .planning/phases/36-code-scanning-gating-scanned-files-documentation/36-PATTERNS.md (## `packages/angular-typechecker/README.md` -- the sibling `####` shape + the DOC-01 draft + the end-user-language / additive-only-delta constraint)
  </read_first>
  <action>
Add a new `####` sub-subsection under `### SARIF and GitHub Code Scanning`, placed AFTER `#### Run from the repository root` (after ~line 722) and BEFORE `## Storybook` (~line 724). Heading: `#### The "Scanned files" panel stays empty (a GitHub limitation)`. Use the RESEARCH Section 6.2 end-user draft. The prose MUST state, in consumer-facing language (no board/phase/G-gate/internal jargon per the "CHANGELOG + README must be end-user-facing" rule): on an alert's detail page GitHub shows a "Scanned files" tool-status panel; for angular-typechecker -- and any third-party SARIF tool -- that panel stays empty; this is a GitHub limitation, NOT a defect in angular-typechecker or its SARIF; GitHub fills "Scanned files" only from its own CodeQL analysis telemetry and SARIF has no field a third-party tool can use to populate it; angular-typechecker's SARIF is well-formed and its alerts, rule descriptions, and file locations all appear normally -- only that one CodeQL-specific panel is blank; emitting the optional SARIF `run.artifacts` list does not change it (the panel ignores it); so an empty "Scanned files" panel is expected and can be ignored. Do NOT file or suggest filing a GitHub Issue. Keep it ASCII-only (use `--` for dashes, straight quotes) and Prettier-wrapped (~80 columns) so `format:check` stays green. The prose MUST contain the exact tokens Task 3 asserts: `Scanned files`, `a GitHub limitation`, `CodeQL`, `run.artifacts` (the heading already carries `Scanned files` and `a GitHub limitation`). Do NOT put a work email or its bare domain anywhere. This is the ONLY changed file in the package `files` allowlist -- the additive audit vs @0.2.3 must show ONLY this prose delta.
  </action>
  <verify>
    <automated>npx prettier --check packages/angular-typechecker/README.md</automated>
  </verify>
  <done>The README `### SARIF and GitHub Code Scanning` section carries a `#### The "Scanned files" panel stays empty (a GitHub limitation)` sub-subsection in end-user language, before `## Storybook`, containing the tokens `Scanned files`/`a GitHub limitation`/`CodeQL`/`run.artifacts`, no Issue suggested, prettier-clean.</done>
  <acceptance_criteria>
    - A `#### The "Scanned files" panel stays empty (a GitHub limitation)` sub-subsection exists under `### SARIF and GitHub Code Scanning`, after `#### Run from the repository root` and before `## Storybook`.
    - The prose contains `Scanned files`, `a GitHub limitation`, `CodeQL`, and `run.artifacts`; frames it as a known GitHub limitation / not a defect; and does NOT suggest filing an Issue.
    - Language is end-user-facing (no internal/board/phase jargon), ASCII-only, no work email/domain.
    - `npx prettier --check packages/angular-typechecker/README.md` passes; `git diff` on the package shows ONLY this README prose delta (no `src/**`/manifest change).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add code-scanning-docs.spec.ts README content-tripwire (mirror angular-cli-docs.spec.ts)</name>
  <files>packages/angular-typechecker/src/code-scanning-docs.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/angular-cli-docs.spec.ts (the WHOLE file to mirror: the `readFileSync('../README.md')` + `normalized = readme.replace(/\s+/g, ' ')` setup, the header comment explaining the false-assurance guard, and the `expect(readme).toContain(heading)` + `expect(normalized).toContain(claim)` shape)
    - packages/angular-typechecker/README.md (the DOC-01 subsection Task 2 added -- the source of truth the tripwire asserts)
    - .planning/phases/36-code-scanning-gating-scanned-files-documentation/36-PATTERNS.md (## `code-scanning-docs.spec.ts` -- the exact assertions + the coverage nuance)
    - .planning/phases/36-code-scanning-gating-scanned-files-documentation/36-VALIDATION.md (Wave 0 -- the docs-tripwire requirement + the path-gated-`test` coverage nuance)
  </read_first>
  <action>
Create `packages/angular-typechecker/src/code-scanning-docs.spec.ts` mirroring `angular-cli-docs.spec.ts` VERBATIM in shape: the same `node:fs`/`node:path`/`node:url` + vitest imports, the same `readmePath = join(dirname(fileURLToPath(import.meta.url)), '../README.md')` read, and `const normalized = readme.replace(/\s+/g, ' ')`. Add a header comment: this is a false-assurance docs tripwire for the DOC-01 "Scanned files" limitation claim; it is a pure deterministic fs read (no compiler/build) so it runs in the fast `nx test` loop; assertions normalize whitespace so the CLAIM is locked, not its line breaks. Also note the coverage nuance in the comment: the `test` target is path-gated on `code`, so a README-only PR (`*.md` -> `code=false`) skips this tripwire -- the SAME coverage the other `*-docs.spec.ts` tripwires already have (accept parity); this phase's own PR touches `ci.yml` (`code=true`), so the tripwire IS exercised at phase verification.

Write a `describe('README ### SARIF and GitHub Code Scanning -- Scanned-files limitation (docs tripwire)', ...)` with `it` assertions:
  - `expect(readme).toContain('### SARIF and GitHub Code Scanning')` (exact heading on the RAW string).
  - `expect(normalized).toContain('Scanned files')`.
  - `expect(normalized).toContain('a GitHub limitation')`.
  - `expect(normalized).toContain('CodeQL')`.
  - `expect(normalized).toContain('run.artifacts')` (locks the spike evidence that the panel ignores `run.artifacts`).

Do NOT add a Markdown parser or any new dependency (pure fs read + `.toContain`, the repo's standing no-parser precedent). Do NOT duplicate assertions owned by other docs tripwires (storybook / angular-cli / standalone-cli / machine-readable).
  </action>
  <verify>
    <automated>npx nx test angular-typechecker</automated>
  </verify>
  <done>`npx nx test angular-typechecker` passes including the new `code-scanning-docs.spec.ts`, which locks the DOC-01 README claim (`Scanned files` / `a GitHub limitation` / `CodeQL` / `run.artifacts`) against future prose gutting.</done>
  <acceptance_criteria>
    - `packages/angular-typechecker/src/code-scanning-docs.spec.ts` exists, mirrors `angular-cli-docs.spec.ts` (README read + normalized-whitespace), and adds no new dependency.
    - It asserts the exact `### SARIF and GitHub Code Scanning` heading on the raw string and `Scanned files`/`a GitHub limitation`/`CodeQL`/`run.artifacts` on the normalized string.
    - `npx nx test angular-typechecker` passes; `npx nx run-many -t lint` (maxWarnings:0) and `npx nx format:check` stay green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped README (npm tarball) -> public consumers | The DOC-01 prose ships in the published package (`files` allowlist includes `README.md`). |
| committed AGENTS.md -> public repo readers + all future AI agents | An inaccurate runbook silently misguides every agent; AGENTS.md self-governs (must be code-reviewed). |
| GitHub `main` ruleset -> merge button | GATE-02 adds a hard `main` control; a mis-sequenced enablement can lock out ALL merges (empty bypass). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-04 | Tampering / Information disclosure | README DOC-01 claim | low | mitigate | `code-scanning-docs.spec.ts` normalized-whitespace tripwire locks the claim; a deletion/gutting fails `nx test` on any code-touching PR (same coverage as the sibling docs tripwires). |
| T-36-05 | Tampering | AGENTS.md runbook accuracy | medium | mitigate | The AGENTS.md self-governance rule routes this change through the phase `code_review_gate`, which verifies the runbook against the live `ci.yml` + GitHub ruleset UI before it is "done". |
| T-36-06 | Information disclosure | public-repo identity/domain hygiene in the shipped README + AGENTS.md | low | mitigate | The added prose is generic CI/docs with no email; the repo's allowlist-inversion email-hygiene check in the normal battery flags any non-approved email-shaped token. |
| T-36-07 | Denial of Service (self-lockout) | `main` "Require code scanning results" enablement | medium | mitigate | The documented runbook enforces Evaluate-mode-first + throwaway-PR probe before Active, and the `enforcement: disabled` recovery -- the enablement is human-run and real-CI-only (D-04), never an agent action. |
| T-36-SC | Tampering (supply chain) | packages | low | accept | Docs/test-only; no package install, no new dependency, no version bump (D-06). No `[ASSUMED]`/`[SUS]` package -> no legitimacy checkpoint. |

**Note:** GATE-02 strengthens the security posture (a second hard gate on `main`); the empty-bypass lockout risk is mitigated entirely by the human-run Evaluate-first runbook + the `enforcement: disabled` recovery.
</threat_model>

<verification>
- `npx nx test angular-typechecker` green -- includes the new `code-scanning-docs.spec.ts` tripwire.
- `npx prettier --check AGENTS.md packages/angular-typechecker/README.md` green (both are Prettier-owned; README is in the `files` allowlist).
- `npx nx run-many -t lint` (maxWarnings:0) + `npx nx format:check` green.
- The AGENTS.md runbook is reviewed by the phase `code_review_gate` (AGENTS.md self-governance rule) for factual accuracy against the live `ci.yml` + ruleset UI -- note in the SUMMARY that AGENTS.md is in this phase's changed-file set.
- Additive audit vs `@0.2.3`: only `README.md` is in the package `files` allowlist and its delta is the DOC-01 prose subsection only; no `src/**` runtime change, no `package.json`/manifest change; no version bump (D-06).
- MANUAL / REAL-CI-ONLY (post-merge, human-run, D-04 -- NOT an in-phase task, do not claim it is locally verifiable): enabling the "Require code scanning results" ruleset on `main` (Evaluate mode -> `.planning`-only + code probe PRs -> Ruleset Insights confirm neither is blocked -> Active; `enforcement: disabled` recovery; fork-PR deadlock accepted), per the AGENTS.md runbook. GitHub ingestion + ruleset evaluation are provable only on GitHub, and flipping the `main` ruleset is a human-only control. The agent NEVER performs the toggle.
</verification>

<success_criteria>
- AGENTS.md carries the human-run, Evaluate-first "Require code scanning results" runbook (both tools; probe two PR kinds; then Active; enforcement-disabled recovery; fork-PR deadlock accepted), stating the agent never flips the `main` ruleset (GATE-02, D-04/D-05); it is queued for the code_review_gate.
- The shipped README documents the CodeQL-only "Scanned files" panel as a known GitHub limitation (not a defect), citing the `run.artifacts`-inert spike evidence, in end-user language, no Issue filed (DOC-01, D-05).
- `code-scanning-docs.spec.ts` locks the README claim against drift; no AGENTS.md tripwire is added (code_review_gate covers it, D-05).
- Docs/test-only; README is the only changed tarball file and shows only an additive prose delta; no version bump (D-06).
</success_criteria>

<output>
Create `.planning/phases/36-code-scanning-gating-scanned-files-documentation/36-02-SUMMARY.md` when done.
</output>
