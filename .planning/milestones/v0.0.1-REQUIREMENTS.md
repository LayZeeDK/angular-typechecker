# Requirements Archive: angular-typechecker v0.0.1

**Status:** SHIPPED 2026-06-29 -- all 34 requirements VALIDATED
**Defined:** 2026-06-27
**Archived:** 2026-06-29 (at milestone completion)
**Audit:** see `v0.0.1-MILESTONE-AUDIT.md` (PASSED: 34/34 requirements, 8/8 phases, 4/4 E2E flows, 0 integration gaps, Nyquist COMPLIANT)

**Core Value:** Deliver the complete Angular type-check (TypeScript + template type-check + extended diagnostics) for any project type without building the app or running the tests -- faster, in isolation, and more completely than the build's coupled check or a bare `ngc --noEmit`.

> All v0.0.1 requirements were verified SATISFIED with live evidence (build + test + diagnostic-code probes + npm registry / GitHub ruleset / git state) across 8 verified phases. Outcomes recorded inline.

## v0.0.1 Requirements (all VALIDATED)

### Workspace & Plugin Scaffold (WS)

- [x] **WS-01**: Nx 23 integrated Angular monorepo (via `create-nx-workspace`) hosting the `angular-typechecker` plugin package. -- Validated (Phase 1; in-place Mechanism B bootstrap, no clobber of `.git`/`.planning/`).
- [x] **WS-02**: Plugin builds via `@nx/js:tsc` to CommonJS `.js` + `.d.ts`, compiled with `module: nodenext` (build-time check asserts emitted executor `.js` still contains `import(`). -- Validated (Phase 1; GATE A).
- [x] **WS-03**: Plugin unit/integration tests run via `@nx/vitest:test` (Vitest). -- Validated (Phase 1).
- [x] **WS-04**: ESLint + Prettier configured, including `@nx/dependency-checks` and module-boundary enforcement of `core/` vs adapters. -- Validated (Phase 3).

### Type-Check Engine (ENG)

- [x] **ENG-01**: Framework-agnostic core (`runTypecheck(options)`) runs `@angular/compiler-cli` whole-program, no-emit, against a given tsconfig. -- Validated (Phase 2).
- [x] **ENG-02**: Custom gatherer collects ALL diagnostics unconditionally (TS + template type-check + extended NG8xxx), modeled on `@angular/build`, never short-circuiting like `ngc`'s default gatherer. -- Validated (Phase 2; NG8109 surfaced where `ngc` suppresses it).
- [x] **ENG-03**: Core loads ESM `@angular/compiler-cli` via `await import()` and runs under the supported Node range. -- Validated (Phase 1; no `ERR_REQUIRE_ESM`).
- [x] **ENG-04**: Structured result (errors/warnings as `ts.Diagnostic[]` + counts) with `strictTemplates` honored; extended-diagnostic categories respected. -- Validated (Phase 2).

### Executor (EXE)

- [x] **EXE-01**: `angular-typecheck` Nx executor wraps the core; settable as any Angular project's target. -- Validated (Phase 4; sub-50-line adapter).
- [x] **EXE-02**: Required `tsConfig` option (single tsconfig per target), overridable; spec/unit-test checking via a target pointed at `tsconfig.spec.json`. -- Validated (Phase 2).
- [x] **EXE-03**: Default full / report-all mode (matches `tsc --noEmit`); opt-in fail-fast. -- Validated (Phase 3).
- [x] **EXE-04**: Excludes out-of-project + `node_modules` diagnostics by default; opt-in `includeDeps`. -- Validated (Phase 3).
- [x] **EXE-05**: `--max-warnings=<n>` (0 = fail on any warning); errors always fail; project-configured categories respected. -- Validated (Phase 3).
- [x] **EXE-06**: Executor target is Nx-cacheable (`cache: true`, `outputs: []`, correct per-tsconfig + dependency-source inputs + `externalDependencies`). -- Validated (Phase 4).
- [x] **EXE-07**: Shipped as CommonJS executor that loads ESM compiler-cli via dynamic `import()` with no `import()`->`require()` downlevel. -- Validated (Phase 4; verified through the packed tarball).

### Diagnostics & Output (OUT)

- [x] **OUT-01**: Default human output via `@angular/compiler-cli` `formatDiagnostics` (NG codes + template codeframes; superset of `tsc`). -- Validated (Phase 3).
- [x] **OUT-02**: Diagnostics filtered on absolute realpath-normalized `fileName` (pnpm-symlink / case-insensitive-FS safe); CI annotation paths workspace-root-relative. -- Validated (Phase 3; mixed-case backstop in Phase 6).
- [x] **OUT-03**: Clear non-zero exit on diagnostics; deterministic, idempotent (agent-ready) output. -- Validated (Phase 3).

### Testing (TEST)

- [x] **TEST-01**: Unit tests (Vitest) mock `@angular/compiler-cli` and cover gatherer, filtering, tsconfig resolution, modes, `--max-warnings`. -- Validated (Phase 3).
- [x] **TEST-02**: Integration tests run the real compiler against fixtures and assert exact diagnostic codes/counts across the v13->v22 catalog. -- Validated (Phase 2).
- [x] **TEST-03**: Executor validated across all five project types (application, local non-buildable library, buildable library, publishable library, spec tsconfig). -- Validated (Phase 6; incl. pnpm + mixed-case).
- [x] **TEST-04**: Dependency-error-busts-cache correctness test verifies a downstream type change invalidates the consumer's cache. -- Validated (Phase 4).
- [x] **TEST-05**: One real-workspace e2e smoke (tarball install) early; full real-workspace matrix in late phase(s). -- Validated (Phase 5 smoke + Phase 6 matrix).

### Packaging & Release (PKG)

- [x] **PKG-01**: `package.json` declares `@nx/devkit` as a pinned dependency (no `nx`), `@angular/compiler-cli` + `typescript` as peers, with `files`/`exports`/`executors` fields and `nx`/`nx-plugin` keywords. -- Validated (Phase 5).
- [x] **PKG-02**: `executors.json`/`schema.json` (v2, `cli: "nx"`, `outputCapture`) copied into `dist` and present in the tarball (verified by `publint` + `attw --pack`). -- Validated (Phase 5).
- [x] **PKG-03**: Published to npm (MIT, 0.x semver) via `nx release` using npm Trusted Publishers (OIDC) + provenance. -- Validated (Phase 5; 0.0.1 published live).
- [x] **PKG-04**: `SECURITY.md` present and release CI hardened (read-only default permissions, no untrusted `pull_request_target`, SHA-pinned actions, manual-approval publish environment). -- Validated (Phase 5).
- [x] **PKG-05**: Tokenless OIDC steady-state publish verified on 0.0.2 (real release authenticates via the registered Trusted Publisher with NO token, SLSA v1 provenance, sets `dist-tags.latest`, cuts a GitHub Release). -- Validated (Phase 5.1; `_npmUser` = OIDC synthetic user).

### Compatibility (CMP)

- [x] **CMP-01**: Supports Nx 23 + Angular 22 + TypeScript `>=6.0 <6.1` (the only TS-6 pairing). -- Validated (Phase 1; nx 23.0.1 / @angular/compiler-cli 22.0.4 / typescript 6.0.3 pinned exact).
- [x] **CMP-02**: `engines.node = ^22.22.3 || ^24.15.0 || ^26.0.0` (Angular-Nx intersection). -- Validated (Phase 1).

### Continuous Integration (CI)

- [x] **CI-01**: GitHub Actions runs unit + integration on a Node 22/24/26 x Linux/Windows/macOS matrix (free standard public-repo runners); heavy e2e gate Linux-only. -- Validated (Phase 6; SC3 GREEN on real runners).

### Release Process (REL)

- [x] **REL-01** (Release-PR flow): Releases no longer push version/changelog commits directly to `main`. `nx.json` has `release.git.tag:false` (+ `commit:true`, `push:false`, `createRelease:false`); the cut produces a version + curated CHANGELOG commit with no tag/push; merges via PR; maintainer tags the merge commit, firing the unchanged OIDC publish. -- Validated (Phase 7).
- [x] **REL-02** (Branch-protection switch): `main` requires a PR satisfying `ci` + `Analyze (actions)` + `Analyze (javascript-typescript)`, force-push/deletion blocked, empty bypass; the temporary v0.0.1 ruleset deleted; Release-tag ruleset retained. -- Validated (Phase 7; ruleset 18229122 active).
- [x] **REL-03** (Clean changelog): Public CHANGELOG.md + GitHub Release notes carry NO internal GSD phase/plan scope; release created with `--notes-file` (never `--generate-notes`). -- Validated (Phase 7).

## Outcomes summary

- **Validated:** 34/34 (all requirements shipped and verified).
- **Adjusted:** none.
- **Dropped:** none.
- **Scope additions during the milestone:** PKG-05 (Phase 5.1 insertion); REL-01/REL-02/REL-03 (Phase 7 insertion). The original "26 total" header was a source miscount; the true enumerated count was 30, plus these 4 insertions = 34.

## Deferred to later milestones (carried forward)

- **Inference (INF):** `createNodesV2` inferred `angular-typecheck` targets; optional `typecheck` target override.
- **Install & Generators (GEN):** `nx add` / `ng add`; a config generator.
- **Other surfaces (SUR):** standalone CLI binary; Angular CLI builder via `convertNxExecutor`; Storybook story type-check.
- **Reporters & performance (REP):** JSON/SARIF/GitHub-annotation reporters; `NgtscProgram` incremental + `--watch`.
- **Broader support (SUP):** Jest (ESM-only, spike-gated); wider Nx (22/21) + older/future Angular; AI-agent skill distribution.

## Out of Scope (unchanged)

| Feature                        | Reason                                                                                        |
| ------------------------------ | --------------------------------------------------------------------------------------------- |
| Emit / build output            | This is a no-emit type-checker; building is a separate concern                                |
| Auto-fix of diagnostics        | Type errors are not mechanically fixable; out of scope                                        |
| In-executor `--watch` (v0.0.1) | Needs `NgtscProgram` incremental; deferred. Editor Language Service + Nx cache cover the loop |
| Per-rule severity CLI flags    | Angular owns severities via tsconfig `extendedDiagnostics` -- avoid two sources of truth      |
| Type-coverage percentage       | Different tool category; not the goal                                                         |
| Multiple tsconfigs per target  | Hurts cache granularity/reporting; use one target per tsconfig (or TS project references)     |
| MCP tool for agents            | Nx's direction is CLI + a skill; agent layer is a skill, not MCP                              |
| Reporter zoo                   | Ship a focused set (pretty now; JSON/SARIF later) -- ESLint's documented regret               |

---

_Requirements defined: 2026-06-27 -- Archived complete: 2026-06-29_
