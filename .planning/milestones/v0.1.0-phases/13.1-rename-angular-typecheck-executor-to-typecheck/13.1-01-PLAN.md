---
phase: 13.1-rename-angular-typecheck-executor-to-typecheck
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [EXEC-01]
files_modified:
  # Shipped package identity (Task 1) — git mv the dir FIRST, then edit contents
  - packages/angular-typechecker/executors.json
  - packages/angular-typechecker/src/executors/angular-typecheck/  # -> src/executors/typecheck/ via `git mv`
  - packages/angular-typechecker/src/executors/typecheck/executor.ts
  - packages/angular-typechecker/src/executors/typecheck/executor.spec.ts
  - packages/angular-typechecker/src/executors/typecheck/gate-a-static.spec.ts
  - packages/angular-typechecker/src/executors/typecheck/normalize-options.ts
  - packages/angular-typechecker/src/executors/typecheck/normalize-options.spec.ts
  - packages/angular-typechecker/src/executors/typecheck/schema.json
  - packages/angular-typechecker/src/executors/typecheck/schema.d.ts
  - packages/angular-typechecker/src/executors/typecheck/schema-parity.spec.ts
  - packages/angular-typechecker/src/core/run-typecheck.ts
  # Workspace config + dev-repo consumers + docs (Task 2)
  - nx.json
  - packages/angular-typechecker/src/core/nx-target-defaults.spec.ts
  - libs/typecheck-consumer/project.json
  - libs/typecheck-consumer/src/lib/consumer.component.ts
  - libs/typecheck-walk-consumer/project.json
  - libs/typecheck-walk-consumer/src/lib/walk-consumer.component.ts
  - README.md
  - packages/angular-typechecker/README.md
  - CLAUDE.md
  # e2e fixtures + specs (Task 3)
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-app/project.json
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-app/nx.json
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-app/src/app.component.ts
  - e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts
  - e2e/angular-typechecker-install-e2e/src/tarball-audit.int.spec.ts
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/nx.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/apps/app/project.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/apps/app/src/app.component.ts
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/buildable-lib/project.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/buildable-lib/src/buildable-lib.component.ts
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/buildable-lib/src/index.ts
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/local-lib/project.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/local-lib/src/local-lib.component.spec.ts
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/publishable-lib/project.json
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/publishable-lib/src/index.ts
  - e2e/angular-typechecker-matrix-e2e/fixtures/consumer-workspace/libs/publishable-lib/src/publishable-lib.component.ts
  - e2e/angular-typechecker-matrix-e2e/src/matrix-5types.int.spec.ts
  - e2e/angular-typechecker-matrix-e2e/src/pnpm-symlink.int.spec.ts
  - e2e/angular-typechecker-cache-e2e/src/executor-parity.int.spec.ts
  - e2e/angular-typechecker-cache-e2e/src/cache-busts-on-dep-error.int.spec.ts
  - e2e/angular-typechecker-cache-e2e/src/cache-busts-on-spec-edit.int.spec.ts

must_haves:
  truths:
    - "The published Nx executor resolves under the id `angular-typechecker:typecheck` (build + all three e2e projects green)."
    - "`nx build angular-typechecker` and the full in-plugin unit/integration suite pass with the renamed identity."
    - "The install/cache/matrix e2e projects resolve and run the executor under the new id and new target names (`typecheck`, `typecheck-spec`)."
    - "No stale `angular-typecheck` executor token from EITHER family remains in shipped source/config/docs (dual negative grep gate == 0)."
    - "The package name `angular-typechecker` is not corrupted: `typecheckr` == 0 and the `angular-typechecker` count is non-decreasing (>= 172)."
    - "The WALK-02 caching contract VALUE is preserved byte-for-byte under both re-keyed nx.json targetDefaults blocks (only the key string changed)."
    - "The change is committed as a breaking `feat!` (or `BREAKING CHANGE:` footer) whose diff touches `packages/angular-typechecker/`; no `nx release` is run."
  artifacts:
    - path: "packages/angular-typechecker/executors.json"
      provides: "Executor manifest exposing the key `typecheck` (id `angular-typechecker:typecheck`) with impl/schema paths under src/executors/typecheck/"
      contains: "\"typecheck\""
    - path: "packages/angular-typechecker/src/executors/typecheck/executor.ts"
      provides: "The moved executor implementation (git mv preserves history); default export bound as `typecheckExecutor`"
      contains: "typecheckExecutor"
    - path: "packages/angular-typechecker/src/executors/typecheck/schema.json"
      provides: "Executor option schema with `$id` = TypecheckExecutorOptions; properties/additionalProperties unchanged"
      contains: "TypecheckExecutorOptions"
    - path: "nx.json"
      provides: "targetDefaults re-keyed to angular-typechecker:typecheck and @angular-typechecker/angular-typechecker:typecheck; WALK-02 value preserved"
      contains: "angular-typechecker:typecheck"
    - path: "packages/angular-typechecker/src/core/nx-target-defaults.spec.ts"
      provides: "WALK_TARGET_DEFAULT_KEYS asserting the two new targetDefaults keys"
      contains: "typecheck"
  key_links:
    - from: "packages/angular-typechecker/executors.json"
      to: "packages/angular-typechecker/src/executors/typecheck/executor"
      via: "implementation path (extensionless; Nx require()s executor.js)"
      pattern: "src/executors/typecheck/executor"
    - from: "consumer project.json target"
      to: "angular-typechecker:typecheck (unscoped) / @angular-typechecker/angular-typechecker:typecheck (scoped)"
      via: "executor field resolution"
      pattern: ":typecheck"
    - from: "nx.json targetDefaults key"
      to: "the WALK-02 cacheable value block (cache/outputs:[]/default-based inputs/^default)"
      via: "targetDefaults[<id>] lookup by re-keyed id"
      pattern: "angular-typechecker(/@angular-typechecker/angular-typechecker)?:typecheck"
---

<objective>
Rename the single shipped Nx executor `angular-typechecker:angular-typecheck` -> `angular-typechecker:typecheck` EVERYWHERE it is referenced, with executor BEHAVIOR unchanged. This is a MECHANICAL, behavior-preserving identity rename. It is the BREAKING change that drives the milestone's `0.0.3 -> 0.1.0` minor bump (EXEC-01), and it lands before the Phase 14 generator so the generator targets the new id from the start.

Purpose: A single atomic identity rename across two token families (kebab-case short-name + Pascal/camel symbols), the impl directory (`git mv`), `executors.json`, `nx.json` targetDefaults (both id forms), all fixtures/consumers/specs, and the README recipes — proven complete by a dual negative grep gate + corruption canary and proven behavior-unchanged by the existing suite + e2e re-run.

Output: The renamed executor identity across the whole tree; a rebuilt `dist/` under the new path; all gates green (build, typecheck-drift, in-plugin test, 3 e2e, lint, format:check, grep gates); a single breaking `feat!` commit touching `packages/angular-typechecker/`.

CRITICAL — this is ONE atomic plan in ONE wave, `autonomous: true`. An intermediate split state (some files renamed, others not) breaks the build AND executor resolution, so the rename CANNOT be divided across parallel plans or waves. Do NOT invent new capabilities, aliases, or design changes — only the id/name/path/symbol moves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase requirement + goal (authoritative rename target enumeration)
@.planning/REQUIREMENTS.md
# ROADMAP Phase 13.1 = goal + SC1-SC5. REQUIREMENTS EXEC-01 = full rename spec.

# USER DECISIONS (LOCKED) + the authoritative rename inventory + validation strategy
@.planning/phases/13.1-rename-angular-typecheck-executor-to-typecheck/13.1-CONTEXT.md
@.planning/phases/13.1-rename-angular-typecheck-executor-to-typecheck/13.1-RESEARCH.md
@.planning/phases/13.1-rename-angular-typecheck-executor-to-typecheck/13.1-VALIDATION.md
# RESEARCH.md is the PATTERN MAP: § Complete Rename Inventory (two token families +
# per-file breakdown), § Nx Executor Rename Mechanics, § Runtime State Inventory,
# § Validation Architecture. CONTEXT.md D-01..D-06 are LOCKED — do NOT re-litigate.

# Project instructions (LOAD-BEARING for this phase)
@AGENTS.md
@CLAUDE.md
# AGENTS.md: Conventional Commits drive the changelog; `main` is PR-only; this phase
#   lands a BREAKING commit but does NOT run `nx release`. Windows-arm64 git rules:
#   `git mv` for the dir rename; stage files by NAME (never `git add .`/`-A`/`-u`);
#   NO AI attribution in commits.
# CLAUDE.md: Windows-arm64 tooling — use `git grep` for tracked-file search and
#   `rg -P` (PCRE lookahead) for the negative gates. NEVER a bare `angular-typecheck`
#   -> `typecheck` global replace (corrupts the package name).

<locked_decisions>
- D-01 Hard rename, NO back-compat alias / duplicate key / shim.
- D-02 Full identity rename: `git mv` the impl dir (preserve history), rename schema `$id`
  `AngularTypecheckExecutorOptions` -> `TypecheckExecutorOptions` and the matching TS interface;
  the schema `title` ("Angular type-check executor") is descriptive prose and STAYS.
- D-03 Anchor EVERY replacement on an executor-only token; NEVER a bare global replace.
- D-04 Do NOT touch `CHANGELOG.md` history, `.planning/`, or `AGENTS.md`. README prose
  mentions are MANDATORY to update; the `CLAUDE.md ## Project` prose mention is low-impact.
- D-05 (LOCKED by orchestrator OQ2) Human-facing message prefixes `angular-typecheck: ...`
  -> the STABLE package name `angular-typechecker: ...` (3 in executor.ts + 1 prose in
  executor.ts + 1 in run-typecheck.ts). Update any spec asserting the old prefix in lockstep.
  This makes the package-name gate NON-DECREASING (>= 172), not strictly "unchanged".
- D-06 Commit as a single breaking `feat!` / `BREAKING CHANGE:` touching the package. Do NOT
  run `nx release` / cut a version here — the 0.1.0 cut happens later at the milestone Release PR.
- OQ1 (LOCKED) RENAME the camelCase default-export function `angularTypecheckExecutor` ->
  `typecheckExecutor` (no runtime coupling; keeps the SC4 gate clean).
- OQ3 (LOCKED) DO NOT add a standing stale-token guard test. Use the ONE-SHOT SC4 grep gate
  as a verification step only. Defer a GUARD-01-style standing guard.
- Pitfall 6 (LOCKED OUT OF SCOPE) The package README Option B `production` input is a WALK-02
  doc nit — the rename MUST still update the executor id in that block, but MUST NOT change
  `production` -> `default` there. Flag it for a reviewer; do not expand scope.
</locked_decisions>

<interfaces>
<!-- Current executors.json contract (VERIFIED live). After rename: key `typecheck`,
     paths under ./src/executors/typecheck/. Executor resolves via <package>:<key>. -->
executors.json (BEFORE): executors["angular-typecheck"] = {
  implementation: "./src/executors/angular-typecheck/executor",
  schema: "./src/executors/angular-typecheck/schema.json",
  outputCapture: "direct-nodejs", description: "..." }

<!-- nx.json targetDefaults (VERIFIED live, lines 44-75) — TWO id forms, identical VALUE.
     Rename touches ONLY the KEY string; the value block is the WALK-02 caching contract
     and MUST be preserved byte-for-byte. -->
nx.json targetDefaults keys (BEFORE):
  "angular-typechecker:angular-typecheck"                          (unscoped published id)
  "@angular-typechecker/angular-typechecker:angular-typecheck"     (scoped workspace id)
Both values: { cache:true, outputs:[], inputs:[ "default",
  "{projectRoot}/tsconfig*.json", "{projectRoot}/package.json",
  "{workspaceRoot}/tsconfig.base.json", "^default",
  { dependentTasksOutputFiles:"**/*.{d.ts,d.cts,d.mts,tsbuildinfo}", transitive:true },
  { externalDependencies:["typescript","@angular/compiler-cli"] } ] }

<!-- Nx mechanics confirmed (RESEARCH § Nx Executor Rename Mechanics):
     - schema `$id` has NO Nx resolver coupling (Nx locates schema via PATH, not `$id`);
       rename is pure hygiene. schema-parity.spec.ts does NOT assert `$id`.
     - default-export function NAME has NO runtime coupling (Nx binds the default export).
     - build target `assets` glob executors.json -> "." and non-.ts files preserving
       structure, so after the `git mv` + path update the built
       dist/.../executors/typecheck/{executor.js,schema.json} resolves. -->
</interfaces>

<baseline_counts>
<!-- VERIFIED live 2026-07-01 (matches RESEARCH.md). Use as the invariant baselines. -->
Family 1 (kebab `angular-typecheck` NOT followed by `er`): 80 occ / 34 files
Family 2 (`AngularTypecheck` | `angularTypecheck`):        21 occ /  7 files (all in the moved dir)
Package name `angular-typechecker`:                        172 occ / 54 files  (MUST stay >= 172)
Corruption canary `typecheckr`:                              0  (MUST stay 0)
</baseline_counts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv the impl directory + rename the shipped-package executor identity</name>
  <files>packages/angular-typechecker/src/executors/angular-typecheck/ (git mv -> .../typecheck/), packages/angular-typechecker/executors.json, packages/angular-typechecker/src/executors/typecheck/{executor.ts,executor.spec.ts,gate-a-static.spec.ts,normalize-options.ts,normalize-options.spec.ts,schema.json,schema.d.ts,schema-parity.spec.ts}, packages/angular-typechecker/src/core/run-typecheck.ts</files>
  <read_first>
    - packages/angular-typechecker/executors.json (current key + impl/schema paths)
    - 13.1-RESEARCH.md § Complete Rename Inventory — Family 1 per-file rows for executors.json (3), executor.ts (4), run-typecheck.ts (1), gate-a-static.spec.ts (1); AND § Family 2 (all 21 Pascal/camel symbols, 7 files, all in the moved dir)
    - 13.1-CONTEXT.md D-02 (dir + `$id` + interface), D-05 (message prefixes), and the locked OQ1 (rename `angularTypecheckExecutor` -> `typecheckExecutor`)
    - Each of the 8 files under src/executors/typecheck/ AFTER the move, plus src/core/run-typecheck.ts, before editing its tokens
  </read_first>
  <action>
    STEP 1 (FIRST, before any content edit): run `git mv packages/angular-typechecker/src/executors/angular-typecheck packages/angular-typechecker/src/executors/typecheck` from the repo root to move all 8 tracked files atomically with history preserved. Do NOT delete+recreate.

    STEP 2 — executors.json: rename the `executors` key `angular-typecheck` -> `typecheck` (published id becomes `angular-typechecker:typecheck`); update `implementation` to `./src/executors/typecheck/executor` and `schema` to `./src/executors/typecheck/schema.json`. Leave `outputCapture` and `description` unchanged.

    STEP 3 — Family 2 identity symbols (all inside the moved dir): rename the TS interface + schema `$id` `AngularTypecheckExecutorOptions` -> `TypecheckExecutorOptions` in schema.d.ts (declaration), schema.json (`$id` only — KEEP the descriptive `title` "Angular type-check executor"), executor.ts (import + param type), normalize-options.ts (import + JSDoc + param), normalize-options.spec.ts (all refs), schema-parity.spec.ts (comments/title refs). Rename the default-export function `angularTypecheckExecutor` -> `typecheckExecutor` in executor.ts (declaration) and executor.spec.ts (the `describe` label) per locked OQ1.

    STEP 4 — Family 1 shipped tokens (D-05 message prefixes + prose + dist path): in executor.ts change the 3 human-facing message/log prefixes `angular-typecheck:` -> `angular-typechecker:` and reword the 1 prose mention ("re-run angular-typecheck") to use `typecheck` or the package name; in run-typecheck.ts change the 1 message prefix `angular-typecheck:` -> `angular-typechecker:`; in gate-a-static.spec.ts change the built dist-path segment `executors/angular-typecheck` -> `executors/typecheck`. If executor.spec.ts or gate-a-static.spec.ts ASSERT the old `angular-typecheck:` prefix text, update those assertions to `angular-typechecker:` in lockstep.

    Anchor every edit on an executor-only token (D-03) — never a bare `angular-typecheck` -> `typecheck` sweep (it would corrupt `angular-typechecker`). Do NOT run `nx build` in this task; fixtures still reference the old id, so a full build only goes green after Task 3. This task's gate is scoped grep + directory-move assertions.
  </action>
  <verify>
    <automated>test ! -d packages/angular-typechecker/src/executors/angular-typecheck && test -d packages/angular-typechecker/src/executors/typecheck && rg -P "angular-typecheck(?!er)" packages/angular-typechecker/executors.json packages/angular-typechecker/src/executors/typecheck packages/angular-typechecker/src/core/run-typecheck.ts | wc -l | rg -q '^0$' && rg "AngularTypecheck|angularTypecheck" packages/angular-typechecker/src/executors/typecheck | wc -l | rg -q '^0$' && rg -q "\"typecheck\"" packages/angular-typechecker/executors.json && rg -q "TypecheckExecutorOptions" packages/angular-typechecker/src/executors/typecheck/schema.json && rg "typecheckr" packages/angular-typechecker | wc -l | rg -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - The old dir `packages/angular-typechecker/src/executors/angular-typecheck/` no longer exists; `packages/angular-typechecker/src/executors/typecheck/` exists with all 8 files (`git status` shows renames, history preserved).
    - `executors.json`: `executors` key is `typecheck`; `implementation` = `./src/executors/typecheck/executor`; `schema` = `./src/executors/typecheck/schema.json`.
    - `schema.json` `$id` = `TypecheckExecutorOptions`; `title` unchanged. `schema.d.ts` interface renamed to `TypecheckExecutorOptions`.
    - `executor.ts` default export is `typecheckExecutor`; its 3 message prefixes and run-typecheck.ts's 1 prefix read `angular-typechecker:`.
    - `gate-a-static.spec.ts` reads the dist path `executors/typecheck/executor.js`.
    - `rg -P "angular-typecheck(?!er)"` and `rg "AngularTypecheck|angularTypecheck"` scoped to executors.json + the moved dir + run-typecheck.ts each return 0; `typecheckr` in the package returns 0.
  </acceptance_criteria>
  <done>The shipped-package executor identity (manifest key, impl/schema paths, directory, `$id`, TS interface, default-export name, message prefixes) is fully renamed with zero stale tokens from either family in the shipped source touched by this task; history preserved via `git mv`.</done>
</task>

<task type="auto">
  <name>Task 2: Re-key nx.json targetDefaults (WALK-02 value preserved) + assertion spec + dev-repo consumers + docs</name>
  <files>nx.json, packages/angular-typechecker/src/core/nx-target-defaults.spec.ts, libs/typecheck-consumer/project.json, libs/typecheck-consumer/src/lib/consumer.component.ts, libs/typecheck-walk-consumer/project.json, libs/typecheck-walk-consumer/src/lib/walk-consumer.component.ts, README.md, packages/angular-typechecker/README.md, CLAUDE.md</files>
  <read_first>
    - nx.json targetDefaults blocks (both id forms; the WALK-02 value block that must be preserved byte-for-byte)
    - packages/angular-typechecker/src/core/nx-target-defaults.spec.ts (WALK_TARGET_DEFAULT_KEYS + describe label + comments — 7 occ per RESEARCH)
    - libs/typecheck-consumer/project.json + libs/typecheck-walk-consumer/project.json (target name + scoped executor ref, 2 occ each)
    - README.md (root, 6 occ) and packages/angular-typechecker/README.md (9 occ) — recipe id/target/run commands + prose
    - 13.1-RESEARCH.md § Nx Executor Rename Mechanics (the "VALUE preserved, only KEY re-keyed" rule) and Pitfall 6 (package README `production` OUT OF SCOPE)
    - CLAUDE.md `## Project` section (the 1 low-impact prose mention)
  </read_first>
  <action>
    In nx.json, re-key BOTH targetDefaults entries: `angular-typechecker:angular-typecheck` -> `angular-typechecker:typecheck` and `@angular-typechecker/angular-typechecker:angular-typecheck` -> `@angular-typechecker/angular-typechecker:typecheck`. Change ONLY the key string in each — the value block (`cache:true`, `outputs:[]`, the `default`-based `inputs` incl. `{projectRoot}/tsconfig*.json`, `^default`, `dependentTasksOutputFiles`, `externalDependencies`) is the WALK-02 caching contract and MUST stay byte-for-byte identical (SC2).

    In nx-target-defaults.spec.ts, update `WALK_TARGET_DEFAULT_KEYS` (the 2 keys) to the new ids, and update the `describe` label + any comments that name the old short-name.

    In libs/typecheck-consumer/project.json and libs/typecheck-walk-consumer/project.json, rename the target NAME key `"angular-typecheck"` -> `"typecheck"` and the scoped executor ref `@angular-typechecker/angular-typechecker:angular-typecheck` -> `...:typecheck`. Update the discretionary `angular-typecheck` comment mention in each lib's component (consumer.component.ts / walk-consumer.component.ts) to `typecheck` (D-05).

    In README.md (root) and packages/angular-typechecker/README.md, update the consumer recipe: the executor id (preserving scoped-vs-unscoped guidance), the `typecheck` target block, and the `nx run <proj>:...` / `npx nx ...` run-command tokens. In the package README Option B targetDefaults block, update the executor id ONLY — do NOT change its `production` input to `default` (Pitfall 6, OUT OF SCOPE; leave a brief note in the plan SUMMARY flagging it for a reviewer).

    In CLAUDE.md `## Project`, update the low-impact prose mention ("ships a single Nx executor (`angular-typecheck`)") to `typecheck` for accuracy (D-04).

    Anchor every edit on an executor-only token (D-03). Do NOT run the full build here (e2e fixtures still hold the old id until Task 3).
  </action>
  <verify>
    <automated>rg -P "angular-typecheck(?!er)" nx.json packages/angular-typechecker/src/core/nx-target-defaults.spec.ts libs/typecheck-consumer libs/typecheck-walk-consumer README.md packages/angular-typechecker/README.md CLAUDE.md | wc -l | rg -q '^0$' && rg -q "angular-typechecker:typecheck" nx.json && rg -q "@angular-typechecker/angular-typechecker:typecheck" nx.json && rg "typecheckr" nx.json | wc -l | rg -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - nx.json has exactly the two re-keyed targetDefaults keys `angular-typechecker:typecheck` and `@angular-typechecker/angular-typechecker:typecheck`; `git diff nx.json` shows ONLY the two key strings changed — no change inside either value block (WALK-02 preserved byte-for-byte).
    - nx-target-defaults.spec.ts `WALK_TARGET_DEFAULT_KEYS` lists the two new keys; describe label + comments updated.
    - Both dev-repo libs' project.json use target name `typecheck` and the scoped executor id `@angular-typechecker/angular-typechecker:typecheck`.
    - Root + package READMEs use `angular-typechecker:typecheck` in the recipe/run commands; the package README Option B block's `production` input is UNCHANGED (flagged for reviewer, not fixed here).
    - `rg -P "angular-typecheck(?!er)"` scoped to all Task-2 files returns 0.
  </acceptance_criteria>
  <done>Both nx.json targetDefaults are re-keyed with the WALK-02 value preserved byte-for-byte; the assertion spec, both dev-repo consumers (target name + scoped id), the README recipes, and the CLAUDE.md prose all use the new id/name with zero stale tokens; the package README `production` nit is left in place and flagged.</done>
</task>

<task type="auto">
  <name>Task 3: Re-point all e2e fixtures, consumer nx.json files, and e2e specs</name>
  <files>e2e/angular-typechecker-install-e2e/** (consumer-app project.json + nx.json + app.component.ts, install-smoke.int.spec.ts, tarball-audit.int.spec.ts), e2e/angular-typechecker-matrix-e2e/** (consumer-workspace nx.json + apps/app + libs/{buildable-lib,local-lib,publishable-lib} project.json + component/index comments, matrix-5types.int.spec.ts, pnpm-symlink.int.spec.ts), e2e/angular-typechecker-cache-e2e/src/{executor-parity,cache-busts-on-dep-error,cache-busts-on-spec-edit}.int.spec.ts</files>
  <read_first>
    - 13.1-RESEARCH.md § Complete Rename Inventory — the per-file Family 1 rows for every e2e file (fixture project.json target names + executor refs, consumer nx.json targetDefaults keys, `executors['angular-typecheck']` single-quoted manifest asserts, tarball-audit packed paths, TARGET consts, matrix `-spec` sibling, fixture-source comments)
    - 13.1-CONTEXT.md § code_context (two published-id forms; matrix `local-lib` has BOTH `typecheck` and `typecheck-spec` sibling targets; tarball-audit expected paths must move to src/executors/typecheck/)
    - Each e2e project.json / nx.json / *.int.spec.ts before editing (unscoped published id `angular-typechecker:typecheck` is what the tarball-installed fixtures use)
  </read_first>
  <action>
    In the install-e2e fixture: consumer-app/project.json — rename the target NAME `"angular-typecheck"` -> `"typecheck"` and the unscoped executor ref `angular-typechecker:angular-typecheck` -> `angular-typechecker:typecheck`; consumer-app/nx.json — re-key the targetDefaults `angular-typechecker:angular-typecheck` -> `angular-typechecker:typecheck` (value unchanged); consumer-app/src/app.component.ts — update the comment mention. install-smoke.int.spec.ts — update the TARGET const AND the single-quoted `executorsManifest.executors['angular-typecheck']` assert -> `['typecheck']`. tarball-audit.int.spec.ts — move the expected packed-file paths `src/executors/angular-typecheck/{schema.json,executor.js}` -> `src/executors/typecheck/{...}`.

    In the matrix-e2e fixture: consumer-workspace/nx.json — re-key the targetDefaults (value unchanged); apps/app + libs/buildable-lib + libs/publishable-lib project.json — target name + executor ref; libs/local-lib project.json — rename BOTH targets (`typecheck` and the sibling `typecheck-spec`) and their executor refs; update fixture-source comments in apps/app/src/app.component.ts, libs/buildable-lib/src/{buildable-lib.component.ts,index.ts}, libs/local-lib/src/local-lib.component.spec.ts, libs/publishable-lib/src/{index.ts,publishable-lib.component.ts}. matrix-5types.int.spec.ts — update the manifest assert, the 5 project-qualified target refs (incl. the `-spec` one), and the comment. pnpm-symlink.int.spec.ts — update the TARGET const, the manifest assert, and the comment.

    In cache-e2e specs: executor-parity.int.spec.ts — update the TARGET const, the `it()` titles, and comments; cache-busts-on-dep-error.int.spec.ts — update the TARGET const `typecheck-consumer:angular-typecheck` -> `typecheck-consumer:typecheck`; cache-busts-on-spec-edit.int.spec.ts — update the TARGET const `typecheck-walk-consumer:angular-typecheck` -> `typecheck-walk-consumer:typecheck`.

    Anchor every edit on an executor-only token (D-03). The `angular-typechecker-*-e2e` project directory names contain the PACKAGE name (followed by `er`) and MUST NOT change. Do NOT run the e2e suite yet — Task 4 rebuilds first, then runs it.
  </action>
  <verify>
    <automated>rg -P "angular-typecheck(?!er)" e2e | wc -l | rg -q '^0$' && rg "AngularTypecheck|angularTypecheck" e2e | wc -l | rg -q '^0$' && rg -q "src/executors/typecheck/" e2e/angular-typechecker-install-e2e/src/tarball-audit.int.spec.ts && rg -q "'typecheck'" e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts && rg "typecheckr" e2e | wc -l | rg -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - Every e2e fixture project.json uses target name `typecheck` (and the matrix local-lib also `typecheck-spec`) with the correct executor id form (unscoped `angular-typechecker:typecheck`).
    - Both consumer nx.json files are re-keyed to `angular-typechecker:typecheck` with their value blocks unchanged.
    - install-smoke / matrix-5types / pnpm-symlink assert `executors['typecheck']` (single-quoted); tarball-audit expects packed paths under `src/executors/typecheck/`.
    - executor-parity / cache-busts-on-dep-error / cache-busts-on-spec-edit TARGET consts use `:typecheck`.
    - `rg -P "angular-typecheck(?!er)" e2e` and `rg "AngularTypecheck|angularTypecheck" e2e` each return 0; the `angular-typechecker-*-e2e` directory names are unchanged.
  </acceptance_criteria>
  <done>All e2e fixtures (project.json target names + executor refs, consumer nx.json targetDefaults, fixture-source comments) and all e2e specs (manifest asserts, TARGET consts, tarball packed paths, titles/comments) reference the new id/target names with zero stale tokens from either family in `e2e/`.</done>
</task>

<task type="auto">
  <name>Task 4: Rebuild, run the full verification gate + one-shot SC4 grep gates, commit as breaking feat!</name>
  <files>(no source edits; rebuild dist/, run gates, then git commit) — the commit stages the files touched in Tasks 1-3 by name</files>
  <read_first>
    - 13.1-VALIDATION.md § Sampling Rate (the exact ordered command set) and § Wave 0 Requirements (rebuild before the suite reads built paths; the one-shot SC4 gate)
    - 13.1-RESEARCH.md § Runtime State Inventory (stale dist/ + Nx cache must be busted with `--skip-nx-cache`; rebuild so `dist/.../executors/typecheck/{executor.js,schema.json}` exists before gate-a-static / tarball-audit run)
    - AGENTS.md § "Conventional Commits drive the changelog" (breaking commit shape; do NOT run `nx release`) and the git staging rules (stage by name; no AI attribution)
  </read_first>
  <action>
    Run the full verification set in order (each command uses `--skip-nx-cache`; optionally `nx reset` first to clear daemon-held old-id hashes):
      1. `npx nx build angular-typechecker --skip-nx-cache` (rebuilds dist/ at the new src/executors/typecheck/ path).
      2. `npx nx run-many -t typecheck-drift test -p angular-typechecker --skip-nx-cache` (in-plugin unit/integration incl. schema-parity + nx-target-defaults).
      3. `npx nx run-many -t test -p angular-typechecker-install-e2e angular-typechecker-cache-e2e angular-typechecker-matrix-e2e --skip-nx-cache` (tarball install/matrix/cache e2e).
      4. `npx nx run-many -t lint --skip-nx-cache` (`@nx/dependency-checks`, `maxWarnings:0`).
      5. `npx nx format:check` — if edited files drifted, run `npx nx format:write` on THOSE files only and re-stage (do NOT reformat files outside this phase's edits).

    Then run the one-shot SC4 completeness gate (repo-wide, using the exact exclusions):
      - `rg -P "angular-typecheck(?!er)" -g '!.planning/**' -g '!CHANGELOG.md' -g '!package-lock.json'` -> 0 matches.
      - `rg "AngularTypecheck|angularTypecheck" -g '!.planning/**' -g '!CHANGELOG.md'` -> 0 matches.
      - `rg "typecheckr" -g '!.planning/**'` -> 0 matches (corruption canary).
      - `git grep -c "angular-typechecker" -- ':!.planning/' ':!CHANGELOG.md'` summed >= 172 (non-decreasing; D-05 may raise it by up to 4).

    Once ALL gates are green, stage the changed files BY NAME (never `git add .`/`-A`/`-u`; include the `git mv` renames) and commit a SINGLE breaking commit with NO AI attribution. Use a release-meaningful scope (e.g. `executor`), NOT a plan-id scope: subject `feat(executor)!: rename angular-typecheck executor to typecheck` with a `BREAKING CHANGE:` footer stating the id moved `angular-typechecker:angular-typecheck` -> `angular-typechecker:typecheck` and consumers must update `project.json`/`nx.json`. Do NOT run `nx release` or create any tag — the 0.1.0 cut happens later at the milestone Release PR (D-06).
  </action>
  <verify>
    <automated>rg -P "angular-typecheck(?!er)" -g '!.planning/**' -g '!CHANGELOG.md' -g '!package-lock.json' | wc -l | rg -q '^0$' && rg "AngularTypecheck|angularTypecheck" -g '!.planning/**' -g '!CHANGELOG.md' | wc -l | rg -q '^0$' && rg "typecheckr" -g '!.planning/**' | wc -l | rg -q '^0$' && [ "$(git grep -c 'angular-typechecker' -- ':!.planning/' ':!CHANGELOG.md' | awk -F: '{s+=$NF} END{print s}')" -ge 172 ]</automated>
  </verify>
  <acceptance_criteria>
    - `npx nx build angular-typechecker --skip-nx-cache` green; `dist/packages/angular-typechecker/src/executors/typecheck/{executor.js,schema.json}` exists.
    - `npx nx run-many -t typecheck-drift test -p angular-typechecker --skip-nx-cache` green (schema-parity + nx-target-defaults pass).
    - `npx nx run-many -t test -p angular-typechecker-install-e2e angular-typechecker-cache-e2e angular-typechecker-matrix-e2e --skip-nx-cache` green (all three e2e resolve + run the executor under the new id/target names).
    - `npx nx run-many -t lint --skip-nx-cache` green (dependency-checks honest; 0 warnings); `npx nx format:check` clean.
    - SC4 gate: `angular-typecheck(?!er)` == 0, `AngularTypecheck|angularTypecheck` == 0, `typecheckr` == 0, `angular-typechecker` count >= 172.
    - HEAD is a single breaking commit (`feat(executor)!` or `BREAKING CHANGE:` footer) whose diff touches `packages/angular-typechecker/`; no `nx release` run; no tag created; no AI attribution in the message; files staged by name.
  </acceptance_criteria>
  <done>The renamed executor builds and passes the full gate (build, typecheck-drift, in-plugin test, 3 e2e, lint, format:check) against fresh execution (`--skip-nx-cache`); the dual negative grep gate + corruption canary + non-decreasing package-name count all pass; the change is committed as one breaking `feat!` touching the package with no release cut.</done>
</task>

</tasks>

<multi_source_coverage_audit>
Every source item is COVERED by this single plan (no gaps, no splits, no deferrals):

| Source | Item | Coverage |
|--------|------|----------|
| GOAL (ROADMAP 13.1) | Rename executor id/key/paths/`$id`, targetDefaults (local+scoped), fixtures, specs, README; behavior unchanged; breaking bump | Tasks 1-4 |
| REQ | EXEC-01 (full rename spec) | Tasks 1-4 (frontmatter `requirements: [EXEC-01]`) |
| ROADMAP SC1 | executors.json key `typecheck`, impl/schema paths + `$id` renamed; build + suite pass | Task 1 (rename) + Task 4 (build/schema-parity) |
| ROADMAP SC2 | targetDefaults re-keyed (both forms); nx-target-defaults.spec asserts new keys; WALK-02 preserved | Task 2 |
| ROADMAP SC3 | fixtures/consumers use new id + target names; integration/unit/e2e pass | Task 2 (dev libs) + Task 3 (e2e) + Task 4 (run) |
| ROADMAP SC4 | README + docs use new id; NO stale `angular-typecheck` executor ref anywhere | Task 2 (docs) + Task 4 (dual grep gate) |
| ROADMAP SC5 | breaking `feat!` / `BREAKING CHANGE:` touching the package | Task 4 (commit) |
| RESEARCH | Family 1 (kebab, 80/34) | Tasks 1-3 |
| RESEARCH | Family 2 (Pascal/camel, 21/7, all in moved dir) | Task 1 |
| RESEARCH | `git mv` dir move (8 files, FIRST) | Task 1 |
| RESEARCH | Nx mechanics (`$id`/fn-name no coupling; assets glob) | Tasks 1-2 (encoded as facts) |
| RESEARCH | Runtime State (rebuild + `--skip-nx-cache`; stale dist/cache) | Task 4 |
| RESEARCH | Validation Architecture (re-point tests + grep gates) | Task 4 |
| CONTEXT | D-01 hard rename (no alias) | Task 1 (no alias added) |
| CONTEXT | D-02 full identity (dir+`$id`+interface) | Task 1 |
| CONTEXT | D-03 anchored edits (never bare replace) | Tasks 1-3 (per-token) + Task 4 (canary) |
| CONTEXT | D-04 don't touch CHANGELOG/.planning/AGENTS; README mandatory | Task 2 + exclusions in Task 4 gate |
| CONTEXT | D-05 message prefix -> package name (locked) | Task 1 |
| CONTEXT | D-06 breaking commit, NO `nx release` | Task 4 |
| CONTEXT | OQ1 rename `angularTypecheckExecutor` (locked) | Task 1 |
| CONTEXT | OQ3 no standing guard, one-shot gate only (locked) | Task 4 |
| CONTEXT | Pitfall 6 `production` OUT OF SCOPE, flag reviewer (locked) | Task 2 (id updated, `production` left; SUMMARY note) |

Exclusions (correctly NOT in scope, per CONTEXT.md): the PACKAGE name `angular-typechecker` / scope `@angular-typechecker`; `CHANGELOG.md` history; `.planning/`; `AGENTS.md`; any new capability (alias, inference, generators). No item is MISSING.
</multi_source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This phase is a pure identifier rename with behavior explicitly unchanged. It adds/modifies no code that crosses a trust boundary; no new untrusted input path is introduced. The executor option schema (`properties`, `additionalProperties:false`) is byte-identical in content — only its `$id` string moves. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-01 | Tampering | Rename correctness (prefix-corruption of `angular-typechecker` / stale-token residue) | mitigate | This is a build-integrity risk, not a security threat. Mitigated by the dual negative grep gate + corruption canary (`typecheckr` == 0) + non-decreasing package-name count (Task 4), plus the full build/test/e2e re-run. |
| T-13.1-SC | Tampering | npm/pip/cargo installs (supply chain) | accept | NO packages are installed by this phase (zero new dependencies — RESEARCH § Package Legitimacy Audit "Not applicable"). `@nx/dependency-checks` lint continues to police the existing peer/dependency classification and must pass post-rename (Task 4). No `[ASSUMED]`/`[SUS]`/`[SLOP]` package to gate. |

**Security disposition (RESEARCH § Security Domain):** No threats at or above `high`. ASVS V2 (auth), V3 (session), V4 (access control), V5 (input validation), V6 (cryptography) are all NOT APPLICABLE — no such code is added or modified; the executor option schema content is unchanged. The release/publish posture (OIDC, provenance, PR-only `main`, manual tag) is unchanged and out of this phase's scope (D-06). No security surface exists to mitigate; T-13.1-01 is recorded for completeness as a build-integrity (not security) risk fully covered by the verification gates.
</threat_model>

<verification>
Run the full ordered set from a clean state (mirrors `.github/workflows/ci.yml`; every command `--skip-nx-cache`, optionally `nx reset` first):

1. `npx nx build angular-typechecker --skip-nx-cache`
2. `npx nx run-many -t typecheck-drift test -p angular-typechecker --skip-nx-cache`
3. `npx nx run-many -t test -p angular-typechecker-install-e2e angular-typechecker-cache-e2e angular-typechecker-matrix-e2e --skip-nx-cache`
4. `npx nx run-many -t lint --skip-nx-cache`
5. `npx nx format:check`
6. One-shot SC4 gate:
   - `rg -P "angular-typecheck(?!er)" -g '!.planning/**' -g '!CHANGELOG.md' -g '!package-lock.json'` -> 0
   - `rg "AngularTypecheck|angularTypecheck" -g '!.planning/**' -g '!CHANGELOG.md'` -> 0
   - `rg "typecheckr" -g '!.planning/**'` -> 0
   - `git grep -c "angular-typechecker" -- ':!.planning/' ':!CHANGELOG.md'` summed >= 172

Behavior-preservation proof: the existing suite + e2e encode the executor's behavior; a green run after a pure-identity diff (ids/names/paths/symbols only) IS the proof that behavior is unchanged.
</verification>

<success_criteria>
- SC1: `executors.json` exposes the key `typecheck` (id `angular-typechecker:typecheck`); impl dir + `implementation`/`schema` paths + schema `$id` renamed; `nx build angular-typechecker` and the full test suite pass. schema-parity.spec green.
- SC2: `nx.json` targetDefaults re-keyed to `angular-typechecker:typecheck` and `@angular-typechecker/angular-typechecker:typecheck`; `nx-target-defaults.spec.ts` asserts the new keys; the WALK-02 value (`default` input, `outputs:[]`, `{projectRoot}/tsconfig*.json`, `^default`) is preserved byte-for-byte.
- SC3: every fixture/consumer `project.json` uses `angular-typechecker:typecheck` and the new target names (`typecheck`, matrix `typecheck-spec`); all integration/unit/e2e specs pass.
- SC4: README + docs use the new id; NO stale `angular-typecheck` executor reference remains in shipped source/config/docs (dual negative grep gate == 0); package name not corrupted (`typecheckr` == 0; count non-decreasing >= 172).
- SC5: a single breaking `feat!` / `BREAKING CHANGE:` commit touching `packages/angular-typechecker/`; NO `nx release` run, NO tag created.
</success_criteria>

<output>
Create `.planning/phases/13.1-rename-angular-typecheck-executor-to-typecheck/13.1-01-SUMMARY.md` when done.

Include in the SUMMARY:
- Confirmation that `git mv` preserved history for the 8 moved files.
- The final grep-gate numbers (both families == 0; `typecheckr` == 0; `angular-typechecker` count, noting the +N from the D-05 message-prefix change).
- The exact commit SHA + subject (breaking `feat!`), and explicit confirmation NO `nx release` / tag was run.
- A REVIEWER FLAG for Pitfall 6: the package README Option B `production` input was intentionally left unchanged (WALK-02 doc nit, out of EXEC-01 scope) — recommend a follow-up.
</output>
