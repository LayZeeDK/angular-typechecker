---
context: spike
phase: 36-code-scanning-gating-scanned-files-documentation
spike: 012-code-scanning-gate-single-run-multiproject
status: resolved-pending-followups
last_updated: 2026-07-23T17:18:46.445Z
---

# Continue Here — spike 012 (GATE-02) RESOLVED, follow-ups pending

## Critical Anti-Patterns (do NOT repeat)

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| Chasing SARIF shape for a gate failure | Spent many CI cycles on single-run vs multi-run and head-ref vs merge-ref `upload-sarif` -- ALL red herrings for the "1 configuration not found" gate failure. | blocking | For a Code Scanning merge-gate failure, FIRST check for ORPHANED base-branch configs: compare `(analysis_key, category, environment)` tuples on `refs/heads/main` vs the PR. `GET code-scanning/analyses?tool_name=...&ref=refs/heads/main` and look for a category no current PR reproduces. |
| Concluding "GitHub limitation" | Wrongly cited roadmap #847 as an unresolved limitation. It is CLOSED/GA; the feature works for third-party tools. | advisory | Verify a "limitation" claim against issue STATE before concluding; a GA feature failing is a config issue on our side. |
| Live-toggling main protection to diagnose | Repeatedly PUT the active main ruleset to test. | advisory | Always add-verify-rollback with the CodeQL-only backup ready (scratchpad `ruleset-18229122-backup.json`); never leave a misbehaving gate on empty-bypass main. This was user-authorized this session only. |

<current_state>
Milestone v0.2.4 (PR #55) is MERGED to main (966a7c6). main is at 44e4306 (clean, in sync with origin).
GATE-02 is RESOLVED and the angular-typechecker Code Scanning merge gate is ACTIVE on main
(ruleset 18229122: CodeQL + angular-typechecker, all 5 rule types intact), proven working on
probe PRs #64 (planning-only) + #65 (code) -- both CLEAN, check = success "No new alerts". All
probe PRs closed + branches deleted. The gate-02 memory has been corrected.
</current_state>

<completed_work>
- Merged milestone PR #55 (v0.2.4) to main.
- Quick task 260722-g6y: RED-SARIF proof moved to its own Code Scanning tool (in PR #55).
- Spike 012: proved the gate WORKS; root cause = ORPHANED angular-typechecker config on main
  (category rename); deleted 4 orphaned analyses via the Code Scanning API; gate enabled + verified.
</completed_work>

<remaining_work>
1. Update AGENTS.md GATE-02 runbook (orphan-cleanup prerequisite + transient/permanent insight +
   no-ci.yml-change + keep red-proof off + fallow not gated). Needs code review + a PR.
2. Write this spike's README + MANIFEST row (this dir is otherwise empty). Land via a planning PR.
3. Fix the fallow file-less SARIF upload bug (LOW urgency; rare).
4. Phase 36 completion (now UNBLOCKED): flip 36-UAT.md test 2 -> passed, re-verify, phase.complete 36,
   extract-learnings, milestone v0.2.4 close.
5. v0.2.4 release (human-gated).
</remaining_work>

<decisions_made>
- GATE-02 fixed by deleting orphaned Code Scanning configs, NOT any ci.yml change (single-run/head-ref
  were red herrings).
- Gate left ACTIVE on main (user choice, authorized).
- Experimental single-run change to main (41ac49a) reverted (44e4306).
</decisions_made>

<blockers>
- main is PR-only (empty-bypass ruleset). Follow-up docs land via a planning PR, not a direct push.
</blockers>

## Infrastructure State
- Ruleset 18229122 (main): active, code_scanning tools = [CodeQL, angular-typechecker]; existence
  thresholds none/none on angular-typechecker. Backup (CodeQL-only) at the session scratchpad.
- main atc Code Scanning config: clean single category `angular-typecheck` (orphaned `angular-typechecker` deleted).

<next_action>
Start with Phase 36 completion (remaining task 4): flip 36-UAT.md test 2 to passed citing the
gate-active + #64/#65 evidence, re-verify via gsd-verifier to passed, then phase.complete 36.
Land the AGENTS.md runbook update + this spike's docs via a planning PR. Full state in
.planning/HANDOFF.json (primary resume source).
</next_action>
