---
phase: 04-nx-executor-adapter-cacheable-target
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/core/render-report.ts
  - packages/angular-typechecker/src/core/render-report.spec.ts
  - packages/angular-typechecker/src/index.ts
  - packages/angular-typechecker/src/executors/angular-typecheck/normalize-options.ts
  - packages/angular-typechecker/src/executors/angular-typecheck/normalize-options.spec.ts
  - packages/angular-typechecker/src/executors/angular-typecheck/executor.ts
  - packages/angular-typechecker/src/executors/angular-typecheck/executor.spec.ts
  - packages/angular-typechecker/src/executors/angular-typecheck/schema.json
  - packages/angular-typechecker/src/executors/angular-typecheck/schema.d.ts
  - packages/angular-typechecker/src/executors/angular-typecheck/schema-parity.spec.ts
  - packages/angular-typechecker/executors.json
autonomous: true
requirements: [EXE-01, EXE-07]
decisions: [D-01, D-02, D-03, D-04, D-06]

must_haves:
  truths:
    - 'A complete sub-50-line executor.ts composes normalizeOptions -> runTypecheck -> renderReport -> evaluateResult -> { success }'
    - 'renderReport(result, { pathBase, color, failFast }) is exported from core, loads ng + ts internally, delegates to formatReport'
    - 'normalizeOptions resolves a workspace-root-relative tsConfig to absolute and splits reporter-only knobs out of CoreOptions'
    - 'The executor catches TypecheckInfrastructureError (logger.error + { success: false }) and re-throws every other error'
    - 'schema.json carries tsConfig (required) + includeDeps + maxWarnings (no default) + failFast + version 2; schema.d.ts is in lockstep'
    - 'executors.json declares outputCapture direct-nodejs'
  artifacts:
    - path: 'packages/angular-typechecker/src/core/render-report.ts'
      provides: 'The renderReport core seam (D-02)'
      contains: 'loadCompilerCli'
    - path: 'packages/angular-typechecker/src/executors/angular-typecheck/normalize-options.ts'
      provides: 'Pure option mapper, rel->abs tsConfig (D-01/D-03)'
      contains: 'joinPathFragments'
    - path: 'packages/angular-typechecker/src/executors/angular-typecheck/executor.ts'
      provides: 'Completed adapter (D-01/D-04)'
      contains: 'renderReport'
    - path: 'packages/angular-typechecker/src/executors/angular-typecheck/schema.json'
      provides: 'v0.0.1 executor public contract (D-06)'
      contains: 'maxWarnings'
    - path: 'packages/angular-typechecker/executors.json'
      provides: 'outputCapture direct-nodejs (D-04)'
      contains: 'direct-nodejs'
  key_links:
    - from: 'packages/angular-typechecker/src/executors/angular-typecheck/executor.ts'
      to: 'packages/angular-typechecker/src/core/render-report.ts'
      via: 'import + await renderReport'
      pattern: 'renderReport'
    - from: 'packages/angular-typechecker/src/core/render-report.ts'
      to: 'packages/angular-typechecker/src/core/format-report.ts'
      via: 'delegates to formatReport with injected ng/ts'
      pattern: 'formatReport'
    - from: 'packages/angular-typechecker/src/index.ts'
      to: 'packages/angular-typechecker/src/core/render-report.ts'
      via: 'barrel export'
      pattern: 'renderReport'
---

<objective>
Complete the executor-adapter vertical slice: a real user can run the `angular-typecheck` executor end-to-end (normalize -> typecheck -> render -> verdict) through a thin sub-50-line adapter, with the full v0.0.1 schema (all three modes) and `outputCapture` wired. This is the first user-runnable surface (EXE-01) and carries the build-time half of EXE-07 forward (the completed adapter still builds under `module: nodenext`).

Purpose: Phase 3 left a stub executor (`{ success: errorCount === 0 }` only). This plan grows it into the full D-01 composition by first adding the `renderReport` core seam (D-02 compile-blocker) that the adapter needs to render output, then completing `normalize-options.ts` + `executor.ts`, then extending the schema to the locked v0.0.1 contract.

Output: `render-report.ts` (+ spec + barrel export), `normalize-options.ts` (+ spec), completed `executor.ts` (+ spec), extended `schema.json`/`schema.d.ts` (+ parity spec), `executors.json` with `outputCapture`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-nx-executor-adapter-cacheable-target/04-CONTEXT.md
@.planning/phases/04-nx-executor-adapter-cacheable-target/04-RESEARCH.md
@.planning/phases/04-nx-executor-adapter-cacheable-target/04-PATTERNS.md
@.planning/phases/04-nx-executor-adapter-cacheable-target/04-VALIDATION.md

# Core composition targets (read to ground identifiers)

@packages/angular-typechecker/src/core/run-typecheck.ts
@packages/angular-typechecker/src/core/format-report.ts
@packages/angular-typechecker/src/core/evaluate-result.ts
@packages/angular-typechecker/src/index.ts

<interfaces>
<!-- Contracts the adapter composes. Use these directly; no exploration needed. -->

From src/core/run-typecheck.ts:

- `interface CoreOptions { tsConfigPath: string; includeDeps?: boolean; pathBase?: string; }`
- `interface CoreResult { tsConfigPath; rootNamesCount; diagnostics: readonly ts.Diagnostic[]; errorCount; warningCount; suppressedCount; durationMs; }`
- `class TypecheckInfrastructureError extends Error`
- `async function runTypecheck(options: CoreOptions): Promise<CoreResult>`

From src/core/format-report.ts:

- `interface FormatOptions { pathBase?: string; color: boolean; failFast?: boolean; }`
- `function formatReport(diagnostics: readonly ts.Diagnostic[], ng: Pick<CompilerCli, 'formatDiagnostics'>, ts_: typeof import('typescript'), options: FormatOptions): string`

From src/core/evaluate-result.ts:

- `interface EvaluateOptions { maxWarnings?: number; }`
- `function evaluateResult(result: Pick<CoreResult, 'errorCount' | 'warningCount'>, options?: EvaluateOptions): { success: boolean }`

From src/core/compiler-loader.ts (barrel-exported):

- `async function loadCompilerCli(): Promise<CompilerCli>`

NOTE: `loadTypescript` is PRIVATE to run-typecheck.ts (memo at lines ~340-351); it is NOT exported from the barrel. This is the reason renderReport must exist (D-02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the renderReport core seam (D-02) and export it from the barrel</name>
  <files>packages/angular-typechecker/src/core/render-report.ts, packages/angular-typechecker/src/core/render-report.spec.ts, packages/angular-typechecker/src/index.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/format-report.ts (the delegate target; signature `formatReport(diagnostics, ng, ts_, options)` at lines 57-62; `FormatOptions` at lines 34-38)
    - packages/angular-typechecker/src/core/run-typecheck.ts (the private `loadTypescript` memo to COPY VERBATIM, lines ~340-351; `CoreResult` type)
    - packages/angular-typechecker/src/core/compiler-loader.ts (the `loadCompilerCli` memo shape, lines 16-20)
    - packages/angular-typechecker/src/core/format-report.spec.ts (the injected-ng fake idiom + `String.fromCharCode(0x1b)` ANSI constant)
    - packages/angular-typechecker/src/index.ts (the barrel to extend)
    - 04-PATTERNS.md section "src/core/render-report.ts" (analogs 1-3) and "src/core/render-report.spec.ts"
  </read_first>
  <behavior>
    - renderReport(result, { color: false }) strips ANSI from the formatted output (color:false path)
    - renderReport(result, { color: true }) keeps ANSI (color:true path)
    - renderReport forwards pathBase and failFast through to formatReport (NG code present in output, paths relativized when pathBase set)
    - renderReport delegates to formatReport with a loaded ng + ts (real loaders, integration-flavored unit test)
  </behavior>
  <action>
    Create `src/core/render-report.ts` exporting an interface `RenderOptions { pathBase?: string; color: boolean; failFast?: boolean; }` (mirror `FormatOptions`) and `async function renderReport(result: Pick<CoreResult, 'diagnostics'>, options: RenderOptions): Promise<string>`. Internally: `const ng = await loadCompilerCli();` (import from `./compiler-loader`), `const ts_ = await loadTypescript();` where `loadTypescript` is a MODULE-PRIVATE memo COPIED VERBATIM from run-typecheck.ts lines ~340-351 (the `cachedTypescript` closure + `(await import('typescript'))` with `.default ?? loaded`). Then `return formatReport(result.diagnostics, ng, ts_, { pathBase: options.pathBase, color: options.color, failFast: options.failFast });`. Use `import type ts from 'typescript'` + `import type { CoreResult } from './run-typecheck'` + `import { formatReport } from './format-report'`. Do NOT (a) add a `formatted` field to CoreResult, or (b) re-export `loadTypescript` from the barrel (D-02 anti-leak). Do NOT import `@nx/devkit` (core/** lint ban, Phase-3 D-11). Add to `src/index.ts`: `export { renderReport } from './core/render-report';` and `export type { RenderOptions } from './core/render-report';`. Create `render-report.spec.ts` mirroring format-report.spec.ts: build a synthetic `ts.Diagnostic[]`, assert renderReport forwards color (ANSI present/absent via the `ESC = String.fromCharCode(0x1b)` constant), failFast truncation, and pathBase relativization, exercising the REAL loaders. Apply CLAUDE.md TS style (blank lines around control flow/returns; braces on all bodies; singleQuote).
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- --run render-report</automated>
  </verify>
  <acceptance_criteria>
    - `render-report.ts` exports `renderReport` and `RenderOptions`; contains `loadCompilerCli` and a private `loadTypescript` (NOT exported from index.ts)
    - `git grep -n "renderReport" -- packages/angular-typechecker/src/index.ts` returns the barrel export line
    - `git grep -n "loadTypescript" -- packages/angular-typechecker/src/index.ts` returns NO match (anti-leak rule held)
    - `git grep -n "@nx/devkit" -- packages/angular-typechecker/src/core/render-report.ts` returns NO match (core/** boundary)
    - `npx nx test angular-typechecker -- --run render-report` passes; both color edges (color:true keeps ANSI, color:false strips it) are asserted
  </acceptance_criteria>
  <done>renderReport seam exists, is barrel-exported, never leaks loadTypescript or imports devkit, and its spec passes with both color edges covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write normalize-options.ts (D-01/D-03) and its spec</name>
  <files>packages/angular-typechecker/src/executors/angular-typecheck/normalize-options.ts, packages/angular-typechecker/src/executors/angular-typecheck/normalize-options.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/executors/angular-typecheck/executor.ts (the existing stub; the type-only `ExecutorContext` import convention at line 1)
    - packages/angular-typechecker/src/executors/angular-typecheck/schema.d.ts (the option interface this maps from)
    - packages/angular-typechecker/src/core/run-typecheck.ts (CoreOptions shape, lines 13-26)
    - packages/angular-typechecker/src/core/evaluate-result.spec.ts (pure-fn table-test idiom)
    - 04-PATTERNS.md section "src/executors/angular-typecheck/normalize-options.ts" (the exact resolution + return shape)
    - 04-RESEARCH.md Code Examples "normalize-options (D-01/D-03)"
  </read_first>
  <behavior>
    - A relative tsConfig (e.g. "libs/x/tsconfig.lib.json") resolves to joinPathFragments(context.root, tsConfig) (absolute, POSIX separators)
    - An absolute tsConfig is passed through unchanged
    - maxWarnings undefined stays undefined (NOT defaulted to 0)
    - failFast and includeDeps default to false when absent
    - pathBase === context.root; color === (process.stdout.isTTY === true)
  </behavior>
  <action>
    Create `normalize-options.ts` exporting `interface NormalizedOptions { coreOptions: CoreOptions; maxWarnings?: number; failFast: boolean; color: boolean; }` and a pure `function normalizeOptions(options: AngularTypecheckExecutorOptions, context: ExecutorContext): NormalizedOptions`. Imports: `import { isAbsolute } from 'node:path';`, `import type { ExecutorContext } from '@nx/devkit';`, `import { joinPathFragments } from '@nx/devkit';`, `import type { CoreOptions } from '../../core/run-typecheck';`, `import type { AngularTypecheckExecutorOptions } from './schema';`. Resolution per D-03: `const tsConfigPath = isAbsolute(options.tsConfig) ? options.tsConfig : joinPathFragments(context.root, options.tsConfig);` (use `joinPathFragments`, NEVER `node:path.join` -- POSIX-separator stability on Windows arm64). Return `{ coreOptions: { tsConfigPath, includeDeps: options.includeDeps ?? false, pathBase: context.root }, maxWarnings: options.maxWarnings, failFast: options.failFast ?? false, color: process.stdout.isTTY === true }`. Forward `maxWarnings` AS-IS (no `?? 0`; `evaluateResult` defensively treats undefined/negative/NaN as unset). Create `normalize-options.spec.ts` (table-style after evaluate-result.spec.ts): build a minimal `{ root: '/ws' } as ExecutorContext`; assert (a) relative -> joined absolute, (b) absolute -> unchanged, (c) the knob split (maxWarnings undefined stays undefined, failFast/includeDeps default false, pathBase === root). Apply CLAUDE.md TS style.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- --run normalize-options</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "joinPathFragments" -- packages/angular-typechecker/src/executors/angular-typecheck/normalize-options.ts` returns a match; `git grep -n "path.join\|node:path'" -- packages/angular-typechecker/src/executors/angular-typecheck/normalize-options.ts` shows ONLY `isAbsolute` imported from node:path (no `join`)
    - normalize-options.spec.ts asserts BOTH the relative and the absolute tsConfig case (both edges of D-03)
    - The spec asserts `maxWarnings` undefined stays undefined and pathBase === context.root
    - `npx nx test angular-typechecker -- --run normalize-options` passes
  </acceptance_criteria>
  <done>normalizeOptions is a pure mapper that resolves rel/abs tsConfig with joinPathFragments and splits reporter knobs out of CoreOptions; both resolution edges tested green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete executor.ts (D-01/D-04), extend the schema (D-06), add outputCapture (D-04), and add the parity + executor specs</name>
  <files>packages/angular-typechecker/src/executors/angular-typecheck/executor.ts, packages/angular-typechecker/src/executors/angular-typecheck/executor.spec.ts, packages/angular-typechecker/src/executors/angular-typecheck/schema.json, packages/angular-typechecker/src/executors/angular-typecheck/schema.d.ts, packages/angular-typechecker/src/executors/angular-typecheck/schema-parity.spec.ts, packages/angular-typechecker/executors.json</files>
  <read_first>
    - packages/angular-typechecker/src/executors/angular-typecheck/executor.ts (the stub to grow in place; preserve the default-export signature)
    - packages/angular-typechecker/src/executors/angular-typecheck/schema.json + schema.d.ts (extend in lockstep)
    - packages/angular-typechecker/executors.json (add outputCapture)
    - packages/angular-typechecker/src/core/infra-failure.spec.ts (the vi.mock + catch/re-throw assertion idiom)
    - packages/angular-typechecker/src/package-manifest.spec.ts (readFileSync + JSON.parse contract-test idiom)
    - 04-PATTERNS.md sections "executor.ts (MODIFY)", "executor.spec.ts", "schema.json + schema.d.ts", "schema-parity.spec.ts", "executors.json"
    - 04-RESEARCH.md Code Examples "Executor (D-01)", "schema.json extension (D-06)", "executors.json - add outputCapture (D-04)"
  </read_first>
  <behavior>
    - executor maps errorCount===0 to { success: true } and >0 to { success: false } (via evaluateResult)
    - executor writes the rendered report through process.stdout.write (NOT logger.info)
    - a TypecheckInfrastructureError thrown by runTypecheck is caught -> logger.error + { success: false }
    - any non-infra error thrown by runTypecheck is RE-THROWN (not swallowed)
    - schema.json properties keys === AngularTypecheckExecutorOptions keys (parity)
  </behavior>
  <action>
    Grow `executor.ts` into the D-01 composition WITHOUT changing the default-export signature `(options, context): Promise<{ success: boolean }>` (un-underscore `_context` -> `context`; it is now used). Imports add `import { logger } from '@nx/devkit';`, `import { renderReport } from '../../core/render-report';`, `import { runTypecheck, TypecheckInfrastructureError } from '../../core/run-typecheck';`, `import { evaluateResult } from '../../core/evaluate-result';`, `import { normalizeOptions } from './normalize-options';`. Body: destructure `{ coreOptions, maxWarnings, failFast, color } = normalizeOptions(options, context);` then a try block: `const result = await runTypecheck(coreOptions); const report = await renderReport(result, { pathBase: coreOptions.pathBase, color, failFast }); process.stdout.write(report); return evaluateResult(result, { maxWarnings });`. Catch: `if (error instanceof TypecheckInfrastructureError) { logger.error('angular-typecheck: the Angular compiler failed to run (infrastructure error, not a type error): ' + error.message); return { success: false }; } throw error;`. Use `process.stdout.write` for the report body, `logger.error` ONLY for the infra meta message (D-04: logger.info would corrupt byte-deterministic codeframes). Keep the file under ~50 lines.

    Extend `schema.json` per D-06: add `"version": 2` at top level; keep `additionalProperties: false` and `cli: "nx"`; properties = `tsConfig` (string, required, update description to "Path to the tsconfig to type-check. Resolved relative to the workspace root when not absolute."), `includeDeps` (boolean, `"default": false`), `maxWarnings` (number, NO `default` key -- a `default: 0` is an un-loosenable footgun since NG8xxx default to warning), `failFast` (boolean, `"default": false`, description "Report only the first error (output brevity) - NOT a speed-up; all diagnostics are still gathered."). NO `aliases`. NO `mode` enum. Update `schema.d.ts` in lockstep to `{ tsConfig: string; includeDeps?: boolean; maxWarnings?: number; failFast?: boolean; }`.

    Add `"outputCapture": "direct-nodejs"` to the `angular-typecheck` entry in `executors.json` (alongside implementation/schema/description) -- D-04.

    Create `executor.spec.ts` (after infra-failure.spec.ts): vi.mock the core modules (`./normalize-options`, `../../core/run-typecheck`, `../../core/render-report`, `../../core/evaluate-result`) or mock `./compiler-loader` to drive runTypecheck deterministically; assert (a) errorCount 0 -> { success: true }; (b) errorCount > 0 -> { success: false }; (c) a thrown `TypecheckInfrastructureError` -> logger.error called + returns { success: false }; (d) a thrown plain `Error` is RE-THROWN (`await expect(...).rejects.toBeInstanceOf(Error)` and NOT TypecheckInfrastructureError); (e) spy on `process.stdout.write` to assert the report goes there (not logger.info).

    Create `schema-parity.spec.ts` (after package-manifest.spec.ts): `readFileSync` + `JSON.parse` of `schema.json`; assert `Object.keys(schema.properties).sort()` deep-equals the literal expected key array `['failFast', 'includeDeps', 'maxWarnings', 'tsConfig']` (the AngularTypecheckExecutorOptions key set); fail loudly on drift. Apply CLAUDE.md TS style throughout.

  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- --run executor schema-parity</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "process.stdout.write" -- packages/angular-typechecker/src/executors/angular-typecheck/executor.ts` returns a match; `git grep -n "logger.info" -- packages/angular-typechecker/src/executors/angular-typecheck/executor.ts` returns NO match
    - `git grep -n "throw error" -- packages/angular-typechecker/src/executors/angular-typecheck/executor.ts` returns a match (re-throw path present)
    - schema.json contains `"version": 2`, a `maxWarnings` property with NO `default` key, `additionalProperties: false`; `git grep -n "aliases\|\"mode\"" -- packages/angular-typechecker/src/executors/angular-typecheck/schema.json` returns NO match
    - schema.d.ts declares `tsConfig`, `includeDeps?`, `maxWarnings?`, `failFast?`
    - `git grep -n "direct-nodejs" -- packages/angular-typechecker/executors.json` returns a match
    - executor.spec.ts asserts BOTH the infra-catch ({ success: false }) AND the re-throw (non-infra error propagates) edges
    - `npx nx test angular-typechecker -- --run executor schema-parity` passes
  </acceptance_criteria>
  <done>The executor is a complete sub-50-line adapter writing to raw stdout, catching infra errors and re-throwing all others; the schema is the locked v0.0.1 contract in lockstep with schema.d.ts; outputCapture is set; both error edges and key parity are tested green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                                                                                              |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| Nx CLI -> executor | Executor options (`tsConfig`, `includeDeps`, `maxWarnings`, `failFast`) cross from the consumer's target configuration into the adapter. |
| adapter -> core    | Normalized options cross from the devkit-aware tier into the framework-agnostic core (absolute path required).                           |

## STRIDE Threat Register

| Threat ID | Category               | Component                      | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                          |
| --------- | ---------------------- | ------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-04-01   | Tampering              | schema.json (executor input)   | mitigate    | `additionalProperties: false` + typed props + `required: ["tsConfig"]` (D-06) is the Nx-enforced validation layer; the key-parity spec keeps schema.json and schema.d.ts from drifting.                                                                                                  |
| T-04-02   | Repudiation            | executor.ts error handling     | mitigate    | D-01: re-throw any non-`TypecheckInfrastructureError` (never swallow). A swallowed error -> false `{ success: true }` is "a type-checker that lies". executor.spec.ts asserts the re-throw edge.                                                                                         |
| T-04-03   | Information disclosure | maxWarnings (malformed number) | accept      | `evaluateResult` defensively treats negative/NaN as unset (verified evaluate-result.ts:48-53); no crash, no inverted verdict. Low risk: build-tool input, no PII.                                                                                                                        |
| T-04-04   | Tampering              | tsConfig path handling         | mitigate    | The adapter only resolves rel->abs via `joinPathFragments(context.root, ...)`; it does not interpolate the path into a shell and does not read `process.cwd()`. The core requires an absolute path (D-03). No traversal beyond the consumer's own intent (they own their target config). |
| T-04-SC   | Tampering              | npm/pip/cargo installs         | mitigate    | No new packages are installed in this plan (RESEARCH Package Legitimacy Audit: zero new installs). slopcheck N/A.                                                                                                                                                                        |

</threat_model>

<verification>
- `npx nx test angular-typechecker` passes (full unit tier including the new render-report, normalize-options, executor, and schema-parity specs).
- `npx nx build angular-typechecker` succeeds and the completed executor still builds under `module: nodenext` (GATE A invariant held: the built `core/compiler-loader.js` retains a literal `import(`). Run on the MAIN tree (real node_modules; the deep-import shim needs it -- STATE [01-03 CAVEAT], D-17).
- `npx nx lint angular-typechecker` passes (exit 0): the new `core/render-report.ts` imports NO `@nx/devkit`; `executor.ts` + `normalize-options.ts` are the only devkit consumers.
</verification>

<success_criteria>

- The executor composes normalizeOptions -> runTypecheck -> renderReport -> evaluateResult -> { success } in under ~50 lines (EXE-01).
- renderReport is a core seam that loads ng + ts internally and delegates to formatReport, barrel-exported, never leaking loadTypescript (D-02).
- tsConfig resolves workspace-root-relative via joinPathFragments; reporter knobs are split out of CoreOptions (D-01/D-03).
- The executor catches infra errors and re-throws everything else (D-01); writes the report to raw stdout (D-04).
- schema.json is the locked v0.0.1 contract (4 props, version 2, no maxWarnings default) in lockstep with schema.d.ts (D-06); executors.json has outputCapture direct-nodejs (D-04).
- Build is green under module: nodenext (EXE-07 build-time half preserved).
  </success_criteria>

<output>
Create `.planning/phases/04-nx-executor-adapter-cacheable-target/04-01-SUMMARY.md` when done.
</output>
