---
phase: quick-260712-ft9
plan: ft9
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [FT9-01]
files_modified:
  # EXTERNAL deliverable (sandbox; NOT committed to this repo):
  - D:/projects/sandbox/vanilla-ng-add-repro/package.json
  - D:/projects/sandbox/vanilla-ng-add-repro/collection.json
  - D:/projects/sandbox/vanilla-ng-add-repro/index.js
  - D:/projects/sandbox/vanilla-ng-add-repro/vanilla-repro.mjs
  - D:/projects/sandbox/vanilla-ng-add-repro/FINDINGS.md
  # IN-REPO (committed; closes the OPEN QUESTION the repro answers):
  - .planning/debug/resolved/cli-yarn-e2e-wrong-version.md
  - .planning/todos/pending/readme-yarn-ng-add-caveat.md

must_haves:
  truths:
    - "The repro yields a CLEAR discriminating verdict -- NX-SPECIFIC vs GENERAL Angular-CLI-under-yarn bug -- backed by the captured gate logs (G1 registry-metadata, G3 createSchematic probe, [G3-CATCH]) and the on-disk VANILLA_NG_ADD_RAN.txt marker outcome for the first-run yarn `ng add`."
    - "The npm control wires the SAME vanilla package (marker present under npm), proving the package is well-formed regardless of the yarn outcome -- so a yarn no-wire cannot be dismissed as a malformed repro."
    - "The interpretation matrix is applied: yarn wires => NX-SPECIFIC (no upstream angular/angular-cli issue warranted); yarn no-wire + npm wires => GENERAL CLI bug (upstream issue warranted, still human-gated); both fail => repro malformed (fix the repro, not a finding)."
    - "The verdict is recorded in the sandbox FINDINGS.md AND the OPEN QUESTION in .planning/debug/resolved/cli-yarn-e2e-wrong-version.md is resolved with the evidence."
  artifacts:
    - path: "D:/projects/sandbox/vanilla-ng-add-repro/index.js"
      provides: "Zero-import CJS ng-add factory (exports.default) that writes the /VANILLA_NG_ADD_RAN.txt marker -- the cleanest possible non-nx control"
      contains: "exports.default"
    - path: "D:/projects/sandbox/vanilla-ng-add-repro/collection.json"
      provides: "The reserved `ng-add` schematic entry -> ./index (the Gate-3 probe target)"
      contains: "ng-add"
    - path: "D:/projects/sandbox/vanilla-ng-add-repro/package.json"
      provides: "`schematics: ./collection.json` (the Gate-1 registry-metadata field)"
      contains: "schematics"
    - path: "D:/projects/sandbox/vanilla-ng-add-repro/vanilla-repro.mjs"
      provides: "Adapted Verdaccio harness: publish vanilla + fresh yarn-4 workspace + npm control + G1/G2/G3 cli.js patch + marker assertion"
      contains: "vanilla-ng-add-repro"
    - path: "D:/projects/sandbox/vanilla-ng-add-repro/FINDINGS.md"
      provides: "The verdict + the captured yarn/npm gate logs + marker outcomes"
      contains: "VERDICT"
  key_links:
    - from: "D:/projects/sandbox/vanilla-ng-add-repro (published to Verdaccio)"
      to: "the fresh yarn-4 + npm CLI workspaces"
      via: "npm publish -> corepack yarn ng add / npm ng add against 127.0.0.1 Verdaccio"
      pattern: "ng add vanilla-ng-add-repro"
    - from: "the patched @angular/cli add gates (G1/G2/G3/[G3-CATCH])"
      to: "the marker file + the verdict"
      via: "gate-by-gate stderr logs discriminate probe-throw vs clean createSchematic"
      pattern: "\\[G3"
---

<objective>
Answer the OPEN QUESTION pinned in `.planning/debug/resolved/cli-yarn-e2e-wrong-version.md`
(Post-24-05 root-cause pin): is the yarn-4 `ng add` no-autowire failure a GENERAL
Angular-CLI-under-yarn probe bug, or SPECIFIC to nx-based (`convertNxGenerator`) factories
whose `@nx/devkit -> nx -> ora -> log-symbols -> chalk` transitive chain throws under yarn's
hoist?

The discriminator is a VANILLA (Nx-free) zero-dependency schematic. If even a schematic that
imports NOTHING fails the yarn Gate-3 probe, the bug is in the CLI (fileable upstream). If it
wires cleanly, the failure needs nx's transitive chain -- so the accurate scope stays "loading
the nx-based factory throws in the yarn probe", and NO upstream angular/angular-cli issue is
warranted.

Purpose: settle whether to file an upstream issue and how to phrase the README yarn caveat.
Output: the sandbox repro (3 package files + harness + FINDINGS.md) and the resolved OPEN QUESTION.

ISOLATION / where this runs:
- Run on the MAIN checkout, NOT a worktree. The harness reuses the repo's `@nx/js`
  `startLocalRegistry` + committed `.verdaccio/config.yml`, needs real `node_modules` + network,
  and reaches an EXTERNAL sandbox path (`D:\projects\sandbox\`). A worktree provides none of that.
- The DELIVERABLE (the vanilla package + harness + FINDINGS.md) lives OUTSIDE this repo, in
  `D:\projects\sandbox\vanilla-ng-add-repro\`. It is NOT committed. Only the quick-task artifacts
  (PLAN/SUMMARY/RESEARCH/VERIFICATION here under `.planning/quick/`) plus the in-repo debug-doc /
  todo updates in Task 3 are committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260712-ft9-create-vanilla-angular-cli-schematic-min/260712-ft9-RESEARCH.md
@.planning/debug/resolved/cli-yarn-e2e-wrong-version.md
@e2e/angular-typechecker-ng-cli-e2e/src/global-setup.ts

<interfaces>
<!-- Source harness to adapt (job-tmp; copy it into the sandbox and edit). It already does
     startLocalRegistry + real token mint + Verdaccio publish + fresh yarn-4 node-modules
     workspace + the G1/G2/G3 + [G3-CATCH] cli.js patch + first/second-run ng add. -->
Source harness: C:\Users\LarsGyrupBrinkNielse\.claude\jobs\95152ba0\tmp\verdaccio-repro.mjs

Load-bearing facts read from the harness + research (do NOT re-derive):
- Registry is rooted at the repo: createRequire(REPO + '/package.json') resolves
  @nx/js/plugins/jest/local-registry; startLocalRegistry({ listenAddress: '127.0.0.1',
  clearStorage: true }); SAFETY gate refuses any registry not starting http://127.0.0.1:.
- Publish is `npm publish "<pkgDir>" --registry <url> --userconfig <publish.npmrc>`.
- The per-workspace .yarnrc.yml is load-bearing: nodeLinker node-modules, npmRegistryServer +
  npmAuthToken = Verdaccio, unsafeHttpWhitelist [127.0.0.1], npmMinimalAgeGate 0,
  enableGlobalCache false, enableMirror false; workspace yarn.lock is written EMPTY to escape
  the parent ~/.claude yarn project; packageManager pinned yarn@4.17.0 (corepack).
- cli.js gate patch anchors (see harness lines 117-141): G1 `context.hasSchematics = !!manifest.schematics`,
  G2 resolvePackageJson + on-disk localManifest, G3 `collection.createSchematic(this.schematicName, true)`
  and the bare `catch {}` at that call site -> [G3-CATCH]. Keep the patch VERBATIM (it logs generically).

Factory export contract (research mechanism fact #2, highest-risk false negative):
- ExportStringRef does `require(mod)['default']`, so the factory MUST be `exports.default = fn`,
  NOT `module.exports = fn` (the latter -> undefined -> createSchematic throws -> FALSE "general bug").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the vanilla zero-dependency ng-add package in the sandbox</name>
  <files>
    D:/projects/sandbox/vanilla-ng-add-repro/package.json
    D:/projects/sandbox/vanilla-ng-add-repro/collection.json
    D:/projects/sandbox/vanilla-ng-add-repro/index.js
  </files>
  <action>
    Create the 3-file package EXACTLY per RESEARCH.md section 1 (authoritative design -- do not
    re-derive or add anything). No TypeScript, no tsc build, ZERO dependencies (no @nx/devkit, no
    nx, no convertNxGenerator, no @angular-devkit/schematics, no chalk/ora/log-symbols).
    - package.json: name `vanilla-ng-add-repro`, version `0.0.1`, and `"schematics": "./collection.json"`
      (the Gate-1 registry-metadata field). Nothing else needed -- main/types are irrelevant to
      ng-add resolution.
    - collection.json: a single reserved-name `ng-add` schematic whose `factory` is `"./index"`
      (the Gate-3 probe target). Omit the optional `schema`.
    - index.js: `'use strict';` then `exports.default = function (_options) { return function (tree, context) { ... } }`.
      The rule (tree, context) => logs an info line via context.logger and calls
      `tree.create('/VANILLA_NG_ADD_RAN.txt', ...)` then `return tree;`. MUST be `exports.default`
      (never `module.exports = fn`) -- this is the single highest-risk false negative (research fact #2).
    Discovery mechanism to preserve: `ng add` finds the collection via package.json `schematics`
    (Gate 1) and runs the schematic named `ng-add` from collection.json (the Gate-3 `createSchematic`
    probe target). Do NOT pre-add this package to any workspace -- a fresh FIRST-RUN is mandatory
    (research fact #4: an already-installed package short-circuits past the probe).
  </action>
  <verify>
    <automated>test -f D:/projects/sandbox/vanilla-ng-add-repro/package.json && test -f D:/projects/sandbox/vanilla-ng-add-repro/collection.json && rg -q "exports\.default" D:/projects/sandbox/vanilla-ng-add-repro/index.js && rg -q '"schematics"' D:/projects/sandbox/vanilla-ng-add-repro/package.json && rg -q '"ng-add"' D:/projects/sandbox/vanilla-ng-add-repro/collection.json && node -e "const f=require('D:/projects/sandbox/vanilla-ng-add-repro/index.js'); if(typeof f.default!=='function') throw new Error('exports.default not a function')"</automated>
  </verify>
  <done>The 3 files exist; index.js's `exports.default` is a function (require + typeof check passes); package.json carries `schematics: ./collection.json`; collection.json declares the `ng-add` schematic -> ./index.</done>
</task>

<task type="auto">
  <name>Task 2: Adapt + run the discriminating harness (yarn + npm control), capture gate logs</name>
  <files>
    D:/projects/sandbox/vanilla-ng-add-repro/vanilla-repro.mjs
    D:/projects/sandbox/vanilla-ng-add-repro/yarn-run.log
    D:/projects/sandbox/vanilla-ng-add-repro/npm-run.log
  </files>
  <action>
    Copy the source harness (C:\Users\LarsGyrupBrinkNielse\.claude\jobs\95152ba0\tmp\verdaccio-repro.mjs)
    to D:/projects/sandbox/vanilla-ng-add-repro/vanilla-repro.mjs and edit per RESEARCH.md section 2:
    - Keep the repo-rooted startLocalRegistry, the 127.0.0.1 SAFETY gate, and the real token mint
      VERBATIM (createRequire still rooted at REPO = D:/projects/github/LayZeeDK/angular-typechecker).
    - Publish the vanilla package DIR instead of the dist:
      `npm publish "D:/projects/sandbox/vanilla-ng-add-repro" --registry <url> --userconfig <publish.npmrc>`.
      REMOVE the dist provenance-strip block (vanilla has no publishConfig).
    - YARN leg: keep the fresh yarn-4 node-modules workspace setup (copy the repo's
      e2e/angular-typechecker-ng-cli-e2e/fixtures/ng-cli-workspace, drop package-lock.json, write
      EMPTY yarn.lock, pin packageManager yarn@4.17.0, write the .yarnrc.yml with enableMirror:false
      + npmMinimalAgeGate 0 + Verdaccio registry/token + unsafeHttpWhitelist 127.0.0.1 + the .npmrc).
      Swap the install target: `corepack yarn ng add vanilla-ng-add-repro --skip-confirmation --verbose`
      (use --skip-confirmation, NEVER --dry-run -- dry-run does not commit the tree so the marker
      never lands). Keep the G1/G2/G3 + [G3-CATCH] cli.js patch verbatim.
      Replace the WIRED? check with the marker assertion:
      `existsSync(join(WS, 'VANILLA_NG_ADD_RAN.txt'))` -> log `[MARKER yarn] present/ABSENT`.
    - NPM CONTROL leg (new): a SECOND fresh workspace (same fixture copy, npm not yarn -- keep
      package-lock dropped, write a .npmrc pointing at Verdaccio, no yarn config). Patch that
      workspace's @angular/cli cli.js the SAME way, run `npm exec ng add vanilla-ng-add-repro --skip-confirmation`
      (or `npx ng add ...`) and assert the marker -> log `[MARKER npm] present/ABSENT`. This proves
      the package is well-formed independent of the yarn result.
    - Run the harness ONCE from the repo root, teeing stderr to yarn-run.log + npm-run.log (or a
      single combined log). NX_DAEMON=false is fine; the harness forks nx for the registry only.
    Report BOTH legs' gate lines: [G1], [G2], [G3] or [G3-CATCH], and the two [MARKER ...] lines.
    (Do NOT build the OPTIONAL heavy-import variant -- YAGNI for the binary question, per research pitfall 6.)
  </action>
  <verify>
    <automated>node D:/projects/sandbox/vanilla-ng-add-repro/vanilla-repro.mjs 2>&1 | tee D:/projects/sandbox/vanilla-ng-add-repro/run.log; rg -q "\[G1\]" D:/projects/sandbox/vanilla-ng-add-repro/run.log && rg -q "\[MARKER yarn\]" D:/projects/sandbox/vanilla-ng-add-repro/run.log && rg -q "\[MARKER npm\]" D:/projects/sandbox/vanilla-ng-add-repro/run.log</automated>
  </verify>
  <done>The harness runs to completion against 127.0.0.1 Verdaccio; the log contains [G1] and either [G3] (clean) or [G3-CATCH] (throw) for the yarn leg, the yarn marker outcome, and the npm-control marker outcome. Enough gate evidence is captured to apply the interpretation matrix.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the interpretation matrix; record the verdict and resolve the OPEN QUESTION</name>
  <files>
    D:/projects/sandbox/vanilla-ng-add-repro/FINDINGS.md
    .planning/debug/resolved/cli-yarn-e2e-wrong-version.md
    .planning/todos/pending/readme-yarn-ng-add-caveat.md
  </files>
  <action>
    Apply RESEARCH.md section 2's interpretation matrix to the Task-2 gate logs + marker outcomes:
    - yarn marker PRESENT (+ [G3] OK) AND npm marker present => VERDICT: NX-SPECIFIC. The failure
      needs the @nx/devkit->nx->chalk/log-symbols transitive chain under yarn's hoist. Do NOT file
      an "ng add broken under yarn" upstream issue; accurate scope stays "loading the nx-based
      factory throws in the yarn probe."
    - yarn marker ABSENT (+ [G3-CATCH] fired / "no ng add actions") AND npm marker present =>
      VERDICT: GENERAL Angular-CLI-under-yarn bug. A dependency-free schematic fails the yarn probe;
      fileable upstream (bare `catch {}` masks the error AND the yarn-4 `ng add` probe path is broken).
    - both ABSENT => the repro is malformed (most likely the exports.default gotcha) -- fix the repro,
      NOT a real finding; loop back to Task 1.
    Write D:/projects/sandbox/vanilla-ng-add-repro/FINDINGS.md: the VERDICT (one of the three), the
    verbatim [G1]/[G2]/[G3]/[G3-CATCH] lines and both [MARKER ...] outcomes as evidence, and the
    upstream-attribution recommendation.
    Then close the loop in-repo (committed): resolve the "OPEN QUESTION" section at the tail of
    .planning/debug/resolved/cli-yarn-e2e-wrong-version.md with the verdict + a pointer to the
    sandbox FINDINGS.md, and annotate .planning/todos/pending/readme-yarn-ng-add-caveat.md with the
    upstream-issue decision the verdict implies (file / do-not-file). ASCII only; no emoji/box-drawing.
    NOTE: filing any actual upstream angular/angular-cli issue stays human-gated -- this task only
    records whether one is warranted.
  </action>
  <verify>
    <automated>rg -q "VERDICT" D:/projects/sandbox/vanilla-ng-add-repro/FINDINGS.md && rg -qi "NX-SPECIFIC|GENERAL Angular-CLI|malformed" D:/projects/sandbox/vanilla-ng-add-repro/FINDINGS.md && git -C D:/projects/github/LayZeeDK/angular-typechecker diff --name-only | rg -q "cli-yarn-e2e-wrong-version.md"</automated>
  </verify>
  <done>FINDINGS.md states the verdict with the captured gate logs + marker outcomes as evidence; the debug doc's OPEN QUESTION is resolved with that verdict; the yarn-caveat todo carries the upstream-issue decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness -> package registry | `npm publish` must reach ONLY the local Verdaccio, never registry.npmjs.org |
| Verdaccio catch-all `'**'` uplink | `vanilla-ng-add-repro` matches the proxy block; a name collision could pull a stranger's tarball |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ft9-01 | Information Disclosure | `npm publish` of the vanilla package | mitigate | Inherit the harness's 127.0.0.1 SAFETY gate verbatim: refuse any registry not starting `http://127.0.0.1:` BEFORE publishing. Publish only to local Verdaccio. |
| T-ft9-02 | Tampering | Verdaccio `'**'` catch-all serving a wrong tarball | accept | Verdaccio serves locally-published packages from storage FIRST; the name `vanilla-ng-add-repro` is unique (not on npm), so the local publish wins. enableMirror:false prevents the locator-keyed stale-mirror trap. |
| T-ft9-SC | Tampering | package-manager installs (yarn/npm in throwaway workspaces) | accept | The only published package is the hand-authored ZERO-dependency vanilla repro; all other installs are the pinned repo fixture deps from Verdaccio in disposable sandbox workspaces. No new third-party dependency is introduced to this repo. |
</threat_model>

<verification>
- The harness completes against a 127.0.0.1 Verdaccio (SAFETY gate never tripped to public npm).
- Gate evidence captured for BOTH legs: yarn ([G1], [G3] or [G3-CATCH], [MARKER yarn]) and the npm
  control ([MARKER npm]).
- The npm control marker is PRESENT (well-formedness proven) -- if it is absent, the repro is
  malformed and Task 1 must be revisited before drawing any verdict.
- FINDINGS.md + the resolved OPEN QUESTION agree on a single verdict.
</verification>

<success_criteria>
- A clear discriminating verdict (NX-SPECIFIC | GENERAL CLI bug | malformed-repro) is produced,
  backed by the captured gate logs + marker outcomes.
- The npm control wires the same package, ruling out a malformed-repro explanation for any yarn no-wire.
- The verdict is recorded in the sandbox FINDINGS.md and the debug doc's OPEN QUESTION is resolved,
  with the upstream-attribution decision noted in the yarn-caveat todo (filing itself stays human-gated).
</success_criteria>

<output>
Create `.planning/quick/260712-ft9-create-vanilla-angular-cli-schematic-min/260712-ft9-SUMMARY.md` when done.
The sandbox deliverable (package + harness + FINDINGS.md) stays external and uncommitted.
</output>
