---
phase: 25-extract-the-advisory-notice-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/core/logger.ts
  - packages/angular-typechecker/src/core/emit-advisory-notices.ts
  - packages/angular-typechecker/src/core/emit-advisory-notices.spec.ts
  - packages/angular-typechecker/src/executors/typecheck/executor.ts
autonomous: true
requirements: [CLI-04]

must_haves:
  truths:
    - "The Nx executor emits every advisory through ONE emitAdvisoryNotices(result, logger) call, not five inline warn*(result) calls; it passes its own @nx/devkit logger in directly with zero adapter (structural assignability; the Logger contract lives in a dedicated core/logger.ts) (D-02)."
    - "core/logger.ts and core/emit-advisory-notices.ts import no nx/@nx/*/@angular-devkit/*, no console, no process (clean under the src/core/** D-11 lint boundary at maxWarnings:0)."
    - "The executor's advisory output is byte-identical to angular-typechecker@0.2.1: every existing executor + builder spec stays green with NO assertion edits -- those existing specs are the byte-identical regression guard, so no new snapshot/golden fixture is added (D-10)."
    - "A new unit spec drives emitAdvisoryNotices against a mock Logger and asserts each notice's EXACT message text + stream routing (info for the node_modules-suppressed count, warn for every other advisory, error for none), and a clean CoreResult emits nothing."
    - "nx test, nx build, nx lint (maxWarnings:0), nx typecheck, and format:check are all green."
  artifacts:
    - path: "packages/angular-typechecker/src/core/logger.ts"
      provides: "The structural Logger seam contract (info/warn/error), imports nothing (D-01/D-03)."
      contains: "interface Logger"
    - path: "packages/angular-typechecker/src/core/emit-advisory-notices.ts"
      provides: "emitAdvisoryNotices + the five private advisory helpers + skippedReferenceVerdictNote, moved verbatim (D-04/D-05/D-06)."
      exports: ["emitAdvisoryNotices"]
      min_lines: 120
    - path: "packages/angular-typechecker/src/core/emit-advisory-notices.spec.ts"
      provides: "Unit spec: exact-string + stream-routing assertions against a mock Logger + a clean-result silent case (D-09)."
      contains: "emitAdvisoryNotices"
    - path: "packages/angular-typechecker/src/executors/typecheck/executor.ts"
      provides: "Executor swapped to one emitAdvisoryNotices(result, logger) call; five helpers + skippedReferenceVerdictNote + unused type imports deleted; infra catch/logger.error kept (D-07/D-08)."
      contains: "emitAdvisoryNotices(result, logger)"
  key_links:
    - from: "packages/angular-typechecker/src/executors/typecheck/executor.ts"
      to: "packages/angular-typechecker/src/core/emit-advisory-notices.ts"
      via: "import { emitAdvisoryNotices } from '../../core/emit-advisory-notices'"
      pattern: "emitAdvisoryNotices\\(result, logger\\)"
    - from: "packages/angular-typechecker/src/core/emit-advisory-notices.ts"
      to: "packages/angular-typechecker/src/core/logger.ts"
      via: "import type { Logger } from './logger'"
      pattern: "from './logger'"
---

<objective>
Extract the five Nx-executor advisory `warn*` helpers into a new pure,
logger-injected `core/emit-advisory-notices.ts` module behind a structural
`core/logger.ts` `Logger` seam, and swap the executor to a single
`emitAdvisoryNotices(result, logger)` call -- with byte-identical observable
behavior vs `angular-typechecker@0.2.1` (CLI-04).

Purpose: create the reusable advisory seam the Phase-26 CLI drives without ever
importing `executor.ts` (which drags `@nx/devkit`/`chalk` -- the 24-06 crash
class) or duplicating five message helpers. Additive/internal only (ADD-01): no
public-API change, no verdict change, no new dependency.

Output: two new `core/` files, one new unit spec, and a swapped executor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-extract-the-advisory-notice-seam/25-CONTEXT.md
@.planning/phases/25-extract-the-advisory-notice-seam/25-PATTERNS.md

# Source of truth for the verbatim move (helper bodies + strings + line anchors):
@packages/angular-typechecker/src/executors/typecheck/executor.ts
# The mock-logger structure + coreResult() factory to copy for the new spec:
@packages/angular-typechecker/src/executors/typecheck/executor.spec.ts
# The lint boundary both new core files land under (D-11):
@packages/angular-typechecker/eslint.config.mjs

<interfaces>
<!-- Contracts the executor already has in context; do NOT re-explore the codebase. -->

New Logger seam to CREATE (core/logger.ts, D-01/D-03 -- imports NOTHING):
```typescript
export interface Logger {
  info(message: string): void;
  warn(message: string): void;
  error(message: string): void; // D-03: reserved for the CLI infra path (Phase 26); no advisory uses it
}
```

CoreResult advisory fields the moved helpers read (from core/run-typecheck.ts, type-only import):
- templateCheckAborted?: { code: number; fileName: string | undefined }
- skippedReferences?: readonly SkippedReference[]
- suppressedThirdParty: number            (always present)
- suppressedInGraphErrorCount: number     (always present)
- suppressedInGraphWarningCount: number   (always present)
- suppressedInGraphFiles: readonly string[] (always present)
- notTypeCheckedDeclaredFiles?: readonly string[]
- bundlerQueryImports?: readonly string[]

SkippedReference['reason'] union (from core/walk-references.ts, type-only import),
switched on by skippedReferenceVerdictNote:
  'out-of-project' | 'zero-root-names' | 'self-reference' | 'duplicate' | 'not-found'
(note branches on 'not-found' and 'zero-root-names'; all others hit the default tail)

@nx/devkit's `logger` is STRUCTURALLY ASSIGNABLE to Logger (it exposes info/warn/error) --
the executor passes `logger` in directly with ZERO adapter/wrapper (D-02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Logger seam and the pure advisory module (verbatim move)</name>
  <files>packages/angular-typechecker/src/core/logger.ts, packages/angular-typechecker/src/core/emit-advisory-notices.ts</files>
  <read_first>
    - packages/angular-typechecker/src/executors/typecheck/executor.ts (the five helper bodies + skippedReferenceVerdictNote at lines ~88-264; the strings are the byte-for-byte source)
    - packages/angular-typechecker/eslint.config.mjs (the **/src/core/**/*.ts D-11 block that both new files must satisfy)
    - packages/angular-typechecker/src/core/walk-references.ts (SkippedReference interface, for the type-only import path)
    - .planning/phases/25-extract-the-advisory-notice-seam/25-PATTERNS.md (per-helper anchors + the purity-doc-header convention from diagnostic-codes.ts/exit-codes.ts)
  </read_first>
  <action>
    Create packages/angular-typechecker/src/core/logger.ts exporting the structural `Logger` interface with `info(message: string): void`, `warn(message: string): void`, and `error(message: string): void` (D-01/D-03). It imports NOTHING. Open it with a one-paragraph doc comment stating it is the seam contract every adapter injects and imports nothing so it can never reach nx/console/process (the D-11 boundary), matching the purity-doc convention in diagnostic-codes.ts/exit-codes.ts. Keep `error` even though no advisory uses it (D-03 freezes the full seam shape for the Phase-26 CLI infra path).

    Create packages/angular-typechecker/src/core/emit-advisory-notices.ts. Type-only import `Logger` from `./logger`, `CoreResult` from `./run-typecheck`, and `SkippedReference` from `./walk-references` (all core-internal, permitted by D-11). MOVE the five private helpers (`warnTemplateCheckAborted`, `warnSkippedReferences`, `warnSuppressed`, `warnNotTypeChecked`, `warnBundlerQueryImports`) and `skippedReferenceVerdictNote` from executor.ts VERBATIM -- literal cut-paste, never retype (Pitfall 1: a dropped boundary space is a byte-diff that still passes the substring assertions). The ONLY per-helper edit is appending `, logger: Logger` to each signature; `skippedReferenceVerdictNote` keeps its `(reason: SkippedReference['reason']): string` signature unchanged. Preserve every message string, `+` concatenation, `${...}` interpolation, doc comment, and the blank-line-around-control-flow style byte-for-byte. Preserve `warnSuppressed`'s TWO independent `if` blocks in order (logger.info node_modules count FIRST, then logger.warn coverage-incomplete -- Pitfall 2), and `warnSkippedReferences`'s per-reference `for` loop emitting ONE logger.warn per reference (Pitfall 3). Export a public `emitAdvisoryNotices(result: CoreResult, logger: Logger): void` that calls the five helpers in the LOCKED order (D-05): warnTemplateCheckAborted, warnSkippedReferences, warnSuppressed, warnNotTypeChecked, warnBundlerQueryImports -- a straight sequence mirroring executor.ts lines 53-57. Do NOT touch src/index.ts (no barrel change -- ADD-01).
  </action>
  <acceptance_criteria>
    - packages/angular-typechecker/src/core/logger.ts exists and exports `interface Logger` with exactly the three methods `info`/`warn`/`error`, each `(message: string): void`. The file has zero `import` statements.
    - packages/angular-typechecker/src/core/emit-advisory-notices.ts exists and exports `emitAdvisoryNotices`, containing the five helper functions + `skippedReferenceVerdictNote` as private (non-exported) functions.
    - Its only imports are type-only: `Logger` from `./logger`, `CoreResult` from `./run-typecheck`, `SkippedReference` from `./walk-references`. No `nx`/`@nx/*`/`@angular-devkit/*`/`console`/`process` reference.
    - A git diff of the moved helper bodies against executor.ts shows the ONLY interior change per helper is the appended `, logger: Logger` parameter -- no character between any pair of backticks changed.
    - `emitAdvisoryNotices` calls the five helpers in the exact order: warnTemplateCheckAborted -> warnSkippedReferences -> warnSuppressed -> warnNotTypeChecked -> warnBundlerQueryImports.
    - `npx nx build angular-typechecker` succeeds; `npx nx lint angular-typechecker` passes at maxWarnings:0 (the new core files trip no banned-import/no-console/process rule); `npx nx typecheck angular-typechecker` is clean.
    - src/index.ts is byte-unchanged (no new export).
  </acceptance_criteria>
  <verify>
    <automated>npx nx build angular-typechecker && npx nx lint angular-typechecker && npx nx typecheck angular-typechecker</automated>
  </verify>
  <done>Both new core files exist, lint-clean under the D-11 boundary, and build/typecheck pass; the helper bodies are a byte-for-byte move with only the added logger parameter.</done>
</task>

<task type="auto">
  <name>Task 2: Add the byte-exact unit spec for emitAdvisoryNotices (D-09)</name>
  <files>packages/angular-typechecker/src/core/emit-advisory-notices.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/emit-advisory-notices.ts (the moved helper bodies -- copy each EXACT expected message string from here)
    - packages/angular-typechecker/src/executors/typecheck/executor.spec.ts (the coreResult()/abortedCoreResult()/skippedRefsCoreResult()/suppressedCoreResult()/notTypeCheckedCoreResult()/bundlerQueryCoreResult() factories at lines 67-150 -- reuse the required-vs-optional CoreResult shape)
    - packages/angular-typechecker/src/core/evaluate-result.spec.ts (the direct-call pure-core spec convention: no vi.mock)
    - .planning/phases/25-extract-the-advisory-notice-seam/25-PATTERNS.md (the mockLogger() + info-vs-warn routing summary)
  </read_first>
  <action>
    Create packages/angular-typechecker/src/core/emit-advisory-notices.spec.ts (unit `test` tier). Import `emitAdvisoryNotices` from `./emit-advisory-notices` and `Logger` type from `./logger`; import `describe`/`expect`/`it`/`vi` from vitest. Build the logger as a plain object `{ info: vi.fn(), warn: vi.fn(), error: vi.fn() } satisfies Logger` (NO `vi.mock` -- the seam is injected). Copy the `coreResult()` factory shape verbatim from executor.spec.ts (the four `suppressed*` fields set to `0`/`[]`; the four optional advisory fields omitted when clean). Assert per notice using EXACT full-string equality (toHaveBeenCalledWith with the COMPLETE message string copied byte-for-byte from the moved helper -- NOT stringContaining; this spec is the byte-exact anchor D-09 requires), plus stream routing: (1) templateCheckAborted -> one logger.warn (cover both a named file and the `an unknown file` fallback); (2) skippedReferences -> one logger.warn PER reference, covering the `not-found`, `zero-root-names`, and a default-tail reason so each `skippedReferenceVerdictNote` branch is exercised; (3) suppressed -> the node_modules count via logger.info AND the coverage-incomplete line via logger.warn, asserting info fires for `suppressedThirdParty > 0` and warn fires when either in-graph count > 0, in that sub-order; (4) notTypeCheckedDeclaredFiles -> one logger.warn; (5) bundlerQueryImports -> one logger.warn. Assert `logger.error` is NEVER called by any advisory. Add a clean-CoreResult case asserting `logger.info`/`logger.warn`/`logger.error` are all NOT called. Follow the repo JS/TS style (blank lines around control flow/returns, braces on all bodies, single quotes, ASCII-only).
  </action>
  <acceptance_criteria>
    - packages/angular-typechecker/src/core/emit-advisory-notices.spec.ts exists and contains NO `vi.mock(...)` call (the logger is a plain injected `vi.fn()` object).
    - Each of the five advisories has at least one assertion using full-string `toHaveBeenCalledWith('<exact message>')` (exact equality, not `expect.stringContaining`), with the string byte-identical to the corresponding helper body.
    - The suppressed case asserts `logger.info` receives the node_modules-suppressed count line AND `logger.warn` receives the coverage-incomplete line (both fired from the same call, info before warn).
    - The skippedReferences case asserts one `logger.warn` per reference and covers the `not-found`, `zero-root-names`, and a default-tail reason (exercising all three `skippedReferenceVerdictNote` branches via exact verdict-note text).
    - A clean-CoreResult test asserts `logger.info`, `logger.warn`, and `logger.error` were each not called.
    - Every advisory assertion confirms `logger.error` was not called.
    - `npx nx test angular-typechecker` is green (the new spec passes AND the existing executor/builder specs remain green -- executor.ts is unchanged in this task).
  </acceptance_criteria>
  <verify>
    <automated>npx nx test angular-typechecker</automated>
  </verify>
  <done>The new unit spec passes with exact-string + routing assertions and a clean-result silent case; the full unit tier stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Swap the executor to one emitAdvisoryNotices call and delete the moved code</name>
  <files>packages/angular-typechecker/src/executors/typecheck/executor.ts</files>
  <read_first>
    - packages/angular-typechecker/src/executors/typecheck/executor.ts (call site lines 53-57; the five helpers + skippedReferenceVerdictNote lines ~88-264 to delete; type imports at lines 6 and 11; the infra catch at lines 75-85 to KEEP)
    - packages/angular-typechecker/src/executors/typecheck/executor.spec.ts (the byte-identical regression guard -- must NOT be edited, and must NOT gain a vi.mock of the new module -- Pitfall 5)
    - .planning/phases/25-extract-the-advisory-notice-seam/25-PATTERNS.md (the executor MODIFY edit list Edit 1-5)
  </read_first>
  <action>
    Edit packages/angular-typechecker/src/executors/typecheck/executor.ts (D-07/D-08). Edit 1: replace the five inline calls at lines 53-57 (`warnTemplateCheckAborted(result);` ... `warnBundlerQueryImports(result);`) with a single `emitAdvisoryNotices(result, logger);`, keeping the surrounding comment intent. Edit 2: add `import { emitAdvisoryNotices } from '../../core/emit-advisory-notices';` alongside the existing relative core imports (`evaluateResult`, `renderReport`). Edit 3: delete the five helper definitions AND `skippedReferenceVerdictNote` (and their doc blocks) -- lines ~88-264. Edit 4: delete the now-unused type imports `import type { CoreResult } from '../../core/run-typecheck';` (line 6) and `import type { SkippedReference } from '../../core/walk-references';` (line 11) -- maxWarnings:0 will fail on unused imports (Pitfall 4). Keep the value import `import { runTypecheck, TypecheckInfrastructureError } from '../../core/run-typecheck';` and all other surviving imports (`ExecutorContext` + `logger` from `@nx/devkit`, `normalizeOptions`, `TypecheckExecutorOptions`). Edit 5: DO NOT touch the infra catch block (lines 75-82) -- the `logger.error(...)` over a thrown `TypecheckInfrastructureError` STAYS (D-08, adapter error-handling, not an advisory). Update the executor's top doc comment only as needed to reference the single `emitAdvisoryNotices` call instead of the five `warn*(result)` helpers (prose accuracy; no behavioral change). Do NOT add `vi.mock('../../core/emit-advisory-notices')` to executor.spec.ts and do NOT edit any executor.spec.ts assertion (Pitfall 5 / D-10: the real emitAdvisoryNotices must keep running against the mocked @nx/devkit logger so the existing notice assertions remain the byte-identical guard).
  </action>
  <acceptance_criteria>
    - executor.ts contains exactly one advisory emission site: `emitAdvisoryNotices(result, logger);` and imports it from `../../core/emit-advisory-notices`.
    - executor.ts no longer defines `warnTemplateCheckAborted`, `warnSkippedReferences`, `warnSuppressed`, `warnNotTypeChecked`, `warnBundlerQueryImports`, or `skippedReferenceVerdictNote` (a git grep for these symbols in executor.ts returns nothing; the sole definitions now live in emit-advisory-notices.ts).
    - executor.ts no longer imports `CoreResult` or `SkippedReference`; `runTypecheck` + `TypecheckInfrastructureError` and all other imports remain and are still referenced.
    - The infra catch block still calls `logger.error(...)` on a `TypecheckInfrastructureError` and returns `{ success: false }` (unchanged).
    - executor.spec.ts is byte-unchanged: no new `vi.mock` and no assertion edits.
    - `npx nx test angular-typechecker` and `npx nx run angular-typechecker:integration` are green -- ALL existing executor + builder + builder.integration specs pass with no assertion changes (the byte-identical proof).
    - `npx nx build angular-typechecker`, `npx nx lint angular-typechecker` (maxWarnings:0), `npx nx typecheck angular-typechecker`, and `npx nx format:check` are all green.
  </acceptance_criteria>
  <verify>
    <automated>npx nx run-many -t build test integration lint typecheck --projects=angular-typechecker --skip-nx-cache && npx nx format:check --base origin/main</automated>
  </verify>
  <done>The executor drives advisories through the single injected-logger call, the moved code + unused imports are gone, the infra path is untouched, and the full suite (unit + integration + lint + typecheck + build + format) is green with no spec edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This phase is a PURE INTERNAL REFACTOR (move five existing advisory helpers into a
pure core module behind an injected `Logger`). It introduces NO new trust boundary,
NO new input, NO new external surface, and installs ZERO packages. The advisory
strings interpolate only values already present on `CoreResult` (the consumer's own
resolved tsconfig paths + integer counts) that the current helpers already render.

| Boundary | Description |
|----------|-------------|
| (none new) | No new boundary. The logger sink (stderr/stdout via @nx/devkit) and the `CoreResult` inputs are unchanged from `angular-typechecker@0.2.1`. |

## STRIDE Threat Register

The only real risk class is a byte-diff / behavioral regression in advisory output --
a CORRECTNESS risk, not a security vulnerability.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering (regression) | The verbatim move of the five helpers into `core/emit-advisory-notices.ts` | mitigate | Literal cut-paste (never retype); the existing `executor.spec.ts` + `builder.spec.ts` + `builder.integration.spec.ts` stay green with NO assertion edits (D-10 byte-identical guard), AND the new `emit-advisory-notices.spec.ts` asserts EXACT full-string message text + stream routing (D-09 byte-exact anchor). Pitfalls 1-3 (whitespace/concatenation, two-stream sub-order, per-reference loop) explicitly guarded. |
| T-25-02 | Tampering (boundary erosion) | The two new `core/**` files reaching a framework/IO import | mitigate | The existing `src/core/**` D-11 ESLint block bans `nx`/`@nx/*`/`@angular-devkit/*`/`yargs` (incl. type-only), `no-console`, and `process.exit`; `nx lint` at maxWarnings:0 is a hard gate. `Logger` is homegrown (no `@nx/devkit` type import). |
| T-25-03 | Information disclosure (content isolation) | Advisory messages naming files | accept | Unchanged vs 0.2.1: notices name only the consumer's OWN resolved paths (never a dependency's diagnostic text); this is a property of the verbatim-preserved helper bodies. No new exposure introduced by the move. |
| T-25-SC | Tampering (supply chain) | npm/pip/cargo installs | not-applicable | This phase installs ZERO packages (RESEARCH.md: "no new runtime or dev dependencies"). No Package Legitimacy Audit / slopcheck / install checkpoint is warranted; no `[ASSUMED]`/`[SUS]`/`[SLOP]` packages exist. |
</threat_model>

<verification>
Phase-level checks (run on the merged tree after all three tasks):

- `npx nx test angular-typechecker` -- the new `emit-advisory-notices.spec.ts` passes AND every existing executor spec passes with no assertion edits (byte-identical guard, D-10).
- `npx nx run angular-typechecker:integration` -- `builder.integration.spec.ts` stays green (the builder is still `convertNxExecutor(typecheckExecutor)`; no engine fork).
- `npx nx build angular-typechecker` -- compiles under `module: nodenext`; the two new core files ship.
- `npx nx lint angular-typechecker` -- maxWarnings:0; catches any banned import in the new core files (T-25-02) and the unused-import deletion in the executor (Pitfall 4).
- `npx nx typecheck angular-typechecker` -- `@nx/devkit`'s `logger` type-checks against `Logger` with no adapter (D-02); the type-only `CoreResult`/`SkippedReference` imports resolve.
- `npx nx format:check --base origin/main` -- Prettier clean (a required CI gate; verify-format-and-lint-before-release memory).
</verification>

<success_criteria>
- [ ] `core/emit-advisory-notices.ts` renders all five advisories through an injected `Logger`, importing no nx/@nx/@angular-devkit, no console, no process (enforced by the `core/**` lint boundary). [Criterion 1]
- [ ] The Nx executor injects its `@nx/devkit` logger via one `emitAdvisoryNotices(result, logger)` call; notice output is byte-identical to `angular-typechecker@0.2.1` -- all existing executor + builder specs green with no behavioral diff. [Criterion 2]
- [ ] A unit spec drives `emit-advisory-notices` against a mock `Logger` asserting each notice's exact message text + stream routing (info/warn), plus a clean-result silent case. [Criterion 3]
- [ ] CLI-04 addressed; ADD-01 additive-only holds (no barrel/public-API change); zero new dependency.
</success_criteria>

<output>
Create `.planning/phases/25-extract-the-advisory-notice-seam/25-01-SUMMARY.md` when done.
</output>
