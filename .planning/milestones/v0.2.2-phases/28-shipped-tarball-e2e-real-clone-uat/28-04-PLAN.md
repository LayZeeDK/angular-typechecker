---
phase: 28-shipped-tarball-e2e-real-clone-uat
plan: 04
type: execute
wave: 3
depends_on: [28-01, 28-02, 28-03]
files_modified:
  - .planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-04-UAT.md
autonomous: false
requirements: [VER-05]

must_haves:
  truths:
    - "A 28-04-UAT.md artifact, modeled on 24-ACV-01-UAT.md / 24-HUMAN-UAT.md, gives a reproducible human-run procedure for the shipped bin at real project tsconfigs (D-08)."
    - "The procedure covers BOTH workspace kinds on-stack Angular 22: a real Angular CLI (angular.json) workspace (ngx-leaflet, realworld-angular) AND a real Nx workspace (radix-ng/primitives primary, analogjs/analog alt), clones UNCOMMITTED and pinned by URL + SHA (D-09)."
    - "Per clone the procedure asserts RED (plant a distinct diagnostic CODE per leaf -> exit 1, code in stdout, no ERR_REQUIRE_ESM, no 'infrastructure error'; revert), GREEN (clean -> exit 0), and BAD-PATH (nonexistent tsconfig -> exit 2; optional --nonsense / missing -c -> exit 2)."
    - "Every invocation uses -c/--tsConfig, NEVER -p (a -p invocation is itself a valid exit-2 usage-error cell); both bin names appear; the npx path uses npx angular-typechecker, never npx atc (D-08)."
    - "VER-05 is HUMAN-RUN: the --auto --chain pipeline PRODUCES the checklist but the phase must NOT mark VER-05 done from automation -- a blocking human checkpoint gates it."
  artifacts:
    - path: ".planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-04-UAT.md"
      provides: "frontmatter (status/scope/gate/substrate/outcome) + About-this-gate + numbered per-clone Tests (expected/steps/result/evidence) + Summary tally + Notes; status set to a pending-human-run value"
      contains: "pending-human-run"
  key_links:
    - from: "28-04-UAT.md steps"
      to: "the shipped atc / angular-typechecker bin"
      via: "atc -c <real tsconfig> against uncommitted SHA-pinned clones"
      pattern: "atc -c"
---

<objective>
Produce the human-run real-clone UAT artifact `28-04-UAT.md` (VER-05): a reproducible procedure that
runs the SHIPPED bin at REAL project tsconfigs in on-stack Angular 22 OSS clones of BOTH kinds -- a real
Nx workspace AND a real Angular CLI workspace -- asserting planted RED / clean GREEN / bad-path -> `2`
(the ACV-01 pattern), with clones uncommitted and pinned by URL + SHA.

Purpose: VER-05 is the human-run confidence gate on top of the CI-authoritative VER-04 (Plans 01-03).
Per D-08 the `--auto --chain` pipeline PRODUCES the checklist but a HUMAN runs it -- so this plan writes
the artifact autonomously and then a BLOCKING human checkpoint gates the actual run; VER-05 must NOT be
auto-marked done from automation.
Output: the `28-04-UAT.md` checklist/script + a human-run verification gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-CONTEXT.md
@.planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-RESEARCH.md
@.planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-PATTERNS.md

<!-- Analog artifacts to MIRROR (shape + substrate). -->
@.planning/milestones/v0.2.1-phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-ACV-01-UAT.md
@.planning/milestones/v0.2.1-phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-HUMAN-UAT.md

<interfaces>
Shipped CLI flag contract (FROZEN): input is `-c`/`--tsConfig` (repeatable, REQUIRED). `-p`/`--project`
is UNREGISTERED -> unknown-flag usage error -> exit 2. Exit codes: 0 clean / 1 verdict-fail / 2
infrastructure-or-usage. Both bin names (`angular-typechecker`, `atc`) resolve to the same bin. The safe
uninstalled invocation is `npx angular-typechecker` (NEVER `npx atc` -- `atc@0.0.6` is an unrelated
published package).

Substrate (D-09):
- Angular CLI kind (carry-forward SHAs): bluehalo/ngx-leaflet @ 818e9ae55240b570397ede5a15cb4d466785abdc
  (app ngx-leaflet-demo + library ngx-leaflet) + realworld-angular/realworld-angular @
  9e3528ff27bad5fedaefb879ccc4aaf4717b137b (app-only).
- Nx workspace kind: radix-ng/primitives (primary) + analogjs/analog (alt) -- SHAs NOT recorded; pin
  FRESH at UAT time and record in the artifact. PITFALLS candidate starting points (re-verify on-stack):
  radix-ng/primitives @ 4a7390a2b058457aa47c6f3e0e03b69b70dee025, analogjs/analog @
  04e32e2a873cc3a3d0d037cc24be5ad02ddb363a.
- Planted diagnostic CODES per leaf (ACV-01): app component tsconfig.app.json -> TS2322; app spec
  tsconfig.spec.json -> TS2345; library tsconfig.lib.json -> TS2554.
- Windows/MSYS manual runs use `/d/...` NOT `D:/...` paths (Git Bash mis-parses the drive letter).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the 28-04-UAT.md human-run procedure</name>
  <read_first>
    - .planning/milestones/v0.2.1-phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-ACV-01-UAT.md (frontmatter status/scope/gate/substrate/outcome; About-this-gate; numbered ### N. tests each with expected/steps (fenced bash)/result/evidence; Summary tally; Gaps; Notes)
    - .planning/milestones/v0.2.1-phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-HUMAN-UAT.md (executed-evidence log shape)
    - .planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-RESEARCH.md (Real-Clone UAT section :427-450 -- substrate table, the -c/--tsConfig correction, the RED/GREEN/bad-path assertion shape)
    - .planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-PATTERNS.md (28-UAT.md assignment + the shipped-surface delta)
  </read_first>
  <action>
    Write `.planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-04-UAT.md` modeled on 24-ACV-01-UAT.md.
    Frontmatter: `status: pending-human-run` (so the phase is NOT auto-marked done), plus scope/gate
    (manual, D-08)/substrate (on-stack Angular 22, both kinds)/outcome. An "About this gate" note stating
    VER-05 is the human-run confidence gate ON TOP of the CI-authoritative VER-04 (`angular-typechecker-cli-e2e`,
    Plans 01-03). Then numbered `### N. <title>` tests, one group per clone (D-09):
      1. ngx-leaflet @818e9ae (app+lib): per-leaf `atc -c tsconfig.app.json`, `-c projects/ngx-leaflet/tsconfig.lib.json`, `-c tsconfig.spec.json`.
      2. realworld-angular @9e3528f (app-only): `atc -c tsconfig.app.json`, `-c tsconfig.spec.json`.
      3. radix-ng/primitives @<PIN FRESH>: `atc -c packages/primitives/tsconfig.json` (solution reference-walk) + per-leaf lib/spec.
      4. analogjs/analog @<PIN FRESH>: point `atc -c` at an Angular library package's tsconfig.
    Each test carries three assertion shapes with fenced bash `steps:` and empty `result:`/`evidence:` for the
    human to fill: RED (plant a distinct CODE per leaf via a source edit -> exit 1, stdout contains the TSxxxx,
    no `ERR_REQUIRE_ESM`, no "infrastructure error"; revert), GREEN (clean -> exit 0), BAD-PATH
    (`atc -c does-not-exist.json` -> exit 2; optional `atc --nonsense` and bare `atc` (no -c) -> exit 2, and a
    `-p` cell explicitly noted as a valid exit-2 usage error). Use `-c`/`--tsConfig` in EVERY invocation
    (NEVER `-p` except as the explicit usage-error cell). Exercise BOTH bin names (`angular-typechecker`, `atc`)
    at least once; note `npx angular-typechecker` for the uninstalled canonical path and forbid `npx atc`.
    Include the clone/checkout + shipped-tarball install steps (install by name from a published/packed
    artifact) and the Windows/MSYS `/d/...` path note. Add a `## Summary` tally and a note that the human
    must pin + record the FRESH Nx-kind SHAs (repos move) using the PITFALLS candidates as a starting point
    after re-verifying on-stack Angular 22. ASCII only.
  </action>
  <acceptance_criteria>
    - `.planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-04-UAT.md` exists with `status: pending-human-run` in the frontmatter.
    - `git grep -c "atc -c" .planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-04-UAT.md` >= 1 and the file contains all four substrate repos (ngx-leaflet, realworld-angular, radix-ng/primitives, analogjs/analog) with the two carry-forward SHAs (818e9ae..., 9e3528f...) literal.
    - The file contains RED/GREEN/BAD-PATH assertion shapes and the exit codes 0/1/2, references `ERR_REQUIRE_ESM`, and NEVER instructs `npx atc` (`git grep -c "npx atc" ...` == 0).
    - The file is ASCII-only (no non-ASCII bytes).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-04-UAT.md && rg -q "pending-human-run" .planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-04-UAT.md && ! rg -q "npx atc" .planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-04-UAT.md</automated>
  </verify>
  <done>The reproducible human-run UAT procedure exists, covers both workspace kinds with SHA-pinned clones, uses -c/--tsConfig throughout, and is marked pending-human-run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human runs the real-clone UAT (VER-05 -- NOT auto-completed)</name>
  <read_first>
    - .planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-04-UAT.md (the procedure written in Task 1)
  </read_first>
  <what-built>
    A reproducible UAT procedure (`28-04-UAT.md`) that runs the shipped `atc`/`angular-typechecker` bin at
    real project tsconfigs in on-stack Angular 22 OSS clones of both kinds. The `--auto --chain` pipeline
    produced this checklist; per D-08 a HUMAN must run it -- VER-05 is NOT satisfied by automation.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-04-UAT.md`.
    2. Clone + `git checkout <sha>` each substrate repo (carry-forward SHAs for the Angular CLI kind; pin
       FRESH + record SHAs for the Nx kind after re-verifying on-stack Angular 22). Clones stay UNCOMMITTED.
    3. Install the shipped tarball into each clone (by name from a local publish, or a packed tarball --
       on Windows/MSYS use `/d/...` paths).
    4. For each clone run the RED / GREEN / BAD-PATH cells with `atc -c <tsconfig>`: clean -> exit 0; planted
       diagnostic CODE -> exit 1 with the TSxxxx in stdout (no `ERR_REQUIRE_ESM`, no "infrastructure error");
       `atc -c does-not-exist.json` -> exit 2; revert plants after each RED.
    5. Fill the `result:` / `evidence:` fields and the `## Summary` tally; set the frontmatter `status`
       to the executed outcome (pass/fail) and record the FRESH Nx-kind SHAs.
    6. Report the outcome. If any clone fails, treat it like a verification gap (fix via
       `/gsd-plan-phase 28 --gaps`) rather than marking VER-05 done.
  </how-to-verify>
  <acceptance_criteria>
    - The human has executed the procedure against all four substrate clones and recorded result/evidence for every RED/GREEN/BAD-PATH cell.
    - The frontmatter `status` is updated from `pending-human-run` to the executed outcome, with the FRESH Nx-kind SHAs recorded.
    - VER-05 is marked done ONLY after the human confirms the run (not from automation).
  </acceptance_criteria>
  <resume-signal>Type "approved" (all clones pass) or describe the failing clone/cell.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted OSS clone content -> the type-check engine + the human's dev host | Real third-party clones run on a developer machine; their content and any install scripts are untrusted. |
| npx name resolution -> npm registry | An `npx atc` in an uninstalled directory would fetch a foreign package. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Tampering | real OSS clone content (VER-05) | mitigate | The shipped engine only READS tsconfigs/sources via `ts`/`fs` -- no `exec` of clone content, no shell-out of user input. Clones stay UNCOMMITTED and pinned by URL + SHA (reproducible, auditable, never merged into the repo). |
| T-28-03 | Malicious code / Supply chain | UAT npx / bin invocation | mitigate | The UAT runs the installed `.bin/atc` / `.bin/angular-typechecker` by path and uses `npx angular-typechecker` for the npx path; the artifact NEVER instructs `npx atc` (enforced by acceptance grep `npx atc == 0`). |
</threat_model>

<verification>
- `28-04-UAT.md` exists, is ASCII-only, marked `status: pending-human-run`, covers both workspace kinds with SHA-pinned clones, uses `-c`/`--tsConfig` throughout, and never instructs `npx atc`.
- A human runs the procedure and records the outcome; VER-05 is closed only on the human's confirmation (blocking checkpoint -- not auto-advanced).
</verification>

<success_criteria>
The shipped bins are proven (by a human) to run correctly at real project tsconfigs in on-stack Angular 22
OSS clones of both kinds -- planted RED / clean GREEN / bad-path -> `2` -- with the procedure and evidence
captured in `28-04-UAT.md` (VER-05). The gate is human-run, not auto-completed.
</success_criteria>

<output>
Create `.planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-04-SUMMARY.md` when done (record the artifact + that VER-05 awaits / received the human run).
</output>
