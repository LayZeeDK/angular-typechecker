---
phase: quick-260712-n7z
plan: n7z
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [N7Z-01]
files_modified:
  - e2e/angular-typechecker-ng-cli-e2e/src/global-setup.ts
  - e2e/angular-typechecker-install-e2e/src/global-setup.ts

must_haves:
  truths:
    - "`NX_DAEMON=false npx nx run-many -t e2e --parallel=1` runs GREEN across all four e2e projects -- the previously-failing angular-typechecker-ng-cli-e2e included -- with no 'already invoked by a parent Nx process in this chain' exit(1)."
    - "Both registry-starting global-setups clear the inherited NX_INVOCATION_ROOT_PID from process.env immediately BEFORE calling startLocalRegistry, so each forked `nx run <root>:local-registry` keys the TaskInvocationTracker on its OWN pid and the two serialized forks never collide."
    - "The ci-e2e-coverage-guard GUARD-01/01b/01c/01d stay green: no e2e/typecheck target, no type:e2e tag, and no ci.yml `run-many -t e2e --parallel=1` line changed."
    - "Both files remain byte-identical apart from the added comment + one-line delete: the 127.0.0.1 loopback, the SAFETY non-local publish gate, clearStorage, the real-token mint, and the provenance strip are all preserved unchanged; no package.json version mutated (release-branch safe)."
  artifacts:
    - path: "e2e/angular-typechecker-ng-cli-e2e/src/global-setup.ts"
      provides: "delete process.env.NX_INVOCATION_ROOT_PID before startLocalRegistry, with an ASCII explanatory comment"
      contains: "delete process.env.NX_INVOCATION_ROOT_PID"
    - path: "e2e/angular-typechecker-install-e2e/src/global-setup.ts"
      provides: "verbatim-sibling copy of the same delete + comment before startLocalRegistry"
      contains: "delete process.env.NX_INVOCATION_ROOT_PID"
  key_links:
    - from: "e2e/*/src/global-setup.ts"
      to: "@nx/js startLocalRegistry -> fork(nx run <root>:local-registry)"
      via: "process.env with NX_INVOCATION_ROOT_PID removed before the fork inherits it"
      pattern: "delete process\\.env\\.NX_INVOCATION_ROOT_PID"
---

<objective>
Resolve the e2e local-registry flakiness where `nx run-many -t e2e --parallel=1` fails on
angular-typechecker-ng-cli-e2e with "Task <root>:local-registry was already invoked by a
parent Nx process in this chain." Apply the web-researched, source-verified Option A fix:
clear the inherited NX_INVOCATION_ROOT_PID immediately before startLocalRegistry in the two
registry-starting global-setups.

Purpose: The exact failing command is the release-blocking CI `e2e` job
(`.github/workflows/ci.yml:204`, enforced by GUARD-01/01b). CI is only green today because
recent commits are `.planning/`-only and the `e2e` job is path-skipped; the release PR
carrying `e2e/**` code WILL trigger run-many across all four projects and go red without this
fix. This unblocks the v0.2.1 release.

Output: A one-line `delete process.env.NX_INVOCATION_ROOT_PID;` + an ASCII explanatory comment
added to BOTH e2e global-setup files, verified by a full green `run-many -t e2e` and the
coverage-guard/lint staying green. Test-harness only -- no source, no package.json, no version
change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260712-n7z-before-phase-completion-research-on-the-/260712-n7z-RESEARCH.md
@e2e/angular-typechecker-ng-cli-e2e/src/global-setup.ts
@e2e/angular-typechecker-install-e2e/src/global-setup.ts
@packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts

Root cause (verified in installed nx 23.0.1 source): `nx run-many -t e2e` sets a single
`NX_INVOCATION_ROOT_PID` in every forked task env (`nx/dist/src/tasks-runner/task-env.js`,
`?? String(process.pid)`). Each `<project>:e2e` vitest process inherits it; both
registry-starting global-setups call `@nx/js` `startLocalRegistry`, which
`fork(nx run <root>:local-registry)` with the inherited `process.env` and NO env override
(`@nx/js/.../start-local-registry.js`). Both forks then build a `TaskInvocationTracker` keyed
on the SAME root PID and register the SAME task id `<root>:local-registry`; the second
registration violates the `(rootPID, taskId)` uniqueness constraint and `process.exit(1)`s
(`nx/dist/src/tasks-runner/task-orchestrator.js`). Deleting the inherited root PID before the
fork makes each forked registry its OWN root (Nx's documented `?? process.pid` fallback), so
the two serialized forks never collide -- mirroring the repo's existing `buildCleanEnv`
hygiene (`NX_INVOCATION_ROOT_PID` is already in `NX_RUNNER_ENV_KEYS`); the registry fork is
the one nested `nx` call that never received the cleaned env because `startLocalRegistry`
takes no `env` param.

Insertion point in BOTH files: immediately BEFORE the `const stop = await startLocalRegistry({`
call (ng-cli-e2e ~line 102, install-e2e ~line 97), after the existing multi-line comment that
explains the `listenAddress` / clearStorage behavior. Do NOT touch anything else.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clear NX_INVOCATION_ROOT_PID before startLocalRegistry in both e2e global-setups, then verify green</name>
  <files>e2e/angular-typechecker-ng-cli-e2e/src/global-setup.ts, e2e/angular-typechecker-install-e2e/src/global-setup.ts</files>
  <action>
In BOTH files, use the Edit tool to insert an ASCII comment block + one statement on the line
IMMEDIATELY BEFORE `const stop = await startLocalRegistry({` (the existing multi-line comment
about `listenAddress '127.0.0.1'` ends just above that call -- insert AFTER that comment,
right before the `const stop` line). The two files are verbatim copies today; add the
IDENTICAL text to both so they stay verbatim siblings.

The inserted text (ASCII only, no Unicode; keep the 2-space file indentation; blank line after
the statement per the project's blank-line-around-statements convention):

  // Nx 23's TaskInvocationTracker keys a per-root-PID uniqueness constraint on task
  // invocations (nx tasks-runner/task-orchestrator.js). Under `nx run-many -t e2e`, both
  // registry-starting e2e projects inherit the SAME NX_INVOCATION_ROOT_PID, and
  // startLocalRegistry forks `nx run <root>:local-registry` with the inherited env, so the
  // second fork collides ("already invoked by a parent Nx process in this chain") and
  // process.exit(1)s. Clearing the inherited root PID makes each forked registry its own
  // root (Nx's own `?? process.pid` fallback), so the two serialized forks never collide.
  // Matches buildCleanEnv's NX_RUNNER_ENV_KEYS hygiene; startLocalRegistry takes no env
  // param, so we clear process.env before it forks.
  delete process.env.NX_INVOCATION_ROOT_PID;

This is the ONLY change: do NOT alter the 127.0.0.1 listenAddress, the SAFETY non-local
publish gate, clearStorage, the mintCiToken call, the buildCleanEnv/stripAllNpmConfig block,
the provenance strip, or the publish invocation. Do NOT touch any package.json or bump any
version (release-branch safety). Stage the two files by name only (never `git add .`).
  </action>
  <verify>
    <automated>NX_DAEMON=false npx nx test angular-typechecker --skip-nx-cache</automated>
    Then confirm the two edits landed (tracked files):
    `git grep -c "delete process.env.NX_INVOCATION_ROOT_PID" -- "e2e/angular-typechecker-ng-cli-e2e/src/global-setup.ts" "e2e/angular-typechecker-install-e2e/src/global-setup.ts"` -> each file reports 1.
    Then lint stays clean:
    `NX_DAEMON=false npx nx lint angular-typechecker --skip-nx-cache`.
    Then the AUTHORITATIVE end-to-end gate (SLOW -- several minutes: Verdaccio build+publish +
    npm/yarn/pnpm installs across four projects; run with a generous timeout or in the
    background, and do NOT treat a long runtime as failure):
    `NX_DAEMON=false npx nx run-many -t e2e --parallel=1` -> GREEN for all four e2e projects,
    including angular-typechecker-ng-cli-e2e, with NO "already invoked by a parent Nx process
    in this chain" error.
  </verify>
  <done>
Both global-setup files carry the identical delete + ASCII comment immediately before
startLocalRegistry (git grep reports 1 each); `nx test angular-typechecker` (GUARD-01/01b/01c/01d
green) and `nx lint angular-typechecker` pass at maxWarnings:0; and
`NX_DAEMON=false npx nx run-many -t e2e --parallel=1` completes GREEN across all four e2e
projects. No file changed apart from the added comment + one line; no package.json / version
mutated.
  </done>
</task>

</tasks>

<verification>
- Fix is present and identical in both registry-starting global-setups (verbatim siblings).
- The exact release-blocking CI command `nx run-many -t e2e --parallel=1` passes locally,
  including the previously-failing ng-cli-e2e project.
- ci-e2e-coverage-guard GUARD-01/01b/01c/01d stay green (targets, tags, and the ci.yml
  run-many lines are untouched).
- `nx lint angular-typechecker` clean at maxWarnings:0.
- No production surface, no package.json, and no version changed (additive / release-branch
  safe).
</verification>

<success_criteria>
`NX_DAEMON=false npx nx run-many -t e2e --parallel=1` is deterministically GREEN across all
four e2e projects, the fix is a comment + one-line delete duplicated verbatim into both
global-setups, all guards + lint stay green, and nothing else (source, config, version) is
touched.
</success_criteria>

<output>
Create `.planning/quick/260712-n7z-before-phase-completion-research-on-the-/260712-n7z-SUMMARY.md` when done
</output>
