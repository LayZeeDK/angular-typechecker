import type { Meta, StoryObj } from '@storybook/angular';

import { ExtComponent } from './ext.component';

const meta: Meta<ExtComponent> = {
  title: 'Ext',
  component: ExtComponent,
};
export default meta;

export const Primary: StoryObj<ExtComponent> = {};
