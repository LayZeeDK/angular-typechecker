---
context: phase
phase: 35-automated-code-scanning-proof
task: null
total_tasks: 3
status: paused
last_updated: 2026-07-21T21:11:12.145Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each was discovered through a real failure this session.
> Acknowledge each explicitly before touching the SARIF reporter or re-running the proof.

- [ ] CONSTRAINT: **GitHub Code Scanning rejects no-location SARIF results** — a result with no `locations` key causes GitHub to reject the ENTIRE SARIF file (`locationFromSarifResult: expected at least one location`). The reporter's D-01 behavior (`sarif-report.ts:28/202` — a file-less record becomes a no-location result) is directly incompatible with GitHub ingestion. The G-35-01 fix MUST give file-less diagnostics a synthetic location; it therefore REVERSES locked decision D-01 and changes published SARIF output — treat it as a deliberate design change, not a patch.
- [ ] CONSTRAINT: **The proof is real-CI-only (Nyquist point)** — no local gate (SARIF schema validation, the drift-lock integration spec, the `ASSERT_ALERTS_FILE` unit seam, `act -n`) can prove GitHub ingestion. Only the `code-scanning-proof` job on a real PR run against `main` exercises it. Every local check can be green while ingestion is red. Verify G-35-01's fix by pushing to PR #55 and confirming the `code-scanning-proof` job GREEN — not by local gates alone.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| No-location SARIF result | Emitting a diagnostic with no `locations` (D-01) makes GitHub reject the whole file; caught only in real CI on the first proof run (upload-sarif `Analysis upload status is failed`). | blocking | The G-35-01 gap plan MUST attach a synthetic fallback location to file-less diagnostics in `formatSarifReport` PASS-2 AND update the drift-lock integration spec to expect it. |
| Trusting local gates for ingestion | Local schema/drift-lock/unit/act all passed while GitHub rejected the SARIF. | blocking | After the fix, push to PR #55 and read the `code-scanning-proof` job result; do not declare done on local green. |
| Stale LSP feed on sarif-report.ts | The passive `new-diagnostics` feed reported a phantom "declared but never read" on `categoryMatches` mid-edit; disproven against `nx test` + the compiler. | advisory | Trust `nx test` / `tsc --noEmit` / `fallow audit`, never the ambient LSP feed, for load-bearing decisions on this file. |
| Overrides break `npm ci` on npm 10 vs 11 | The version-keyed override form (`pkg@x.y.z`) is applied by npm 11 but not npm 10, breaking `npm ci` on the Node-22 runner. | advisory | For the svgo fix use plain `npm update svgo` (NOT an override) — a safe version is already in range, so no override is needed. If an override ever is, use the nested form. |

<current_state>
Phase 35 is EXECUTED (all 3 plans have SUMMARYs) and code-reviewed, but NOT complete.
Verification returned `human_needed`, and the autonomous real-CI UAT on PR #55 turned the
`code-scanning-proof` job RED, surfacing gap **G-35-01**. Two CI blockers remain on PR #55:
G-35-01 (the non-required `code-scanning-proof` job) and a new cve-lite HIGH (svgo@4.0.1,
which fails the REQUIRED `ci` aggregate). The fallow complexity blocker was fixed this
session and is GREEN in real CI. HEAD = `f89bcb4` on `gsd/v0.2.4-...` (PR #55, draft), all pushed.
</current_state>

<completed_work>

- 35-02 (assert-code-scanning.mjs gh-api poll/set-membership + subprocess spec) — Done (`0006ecf`)
- 35-03 (PR-only code-scanning-proof CI job + act-compat) — Done (`4822717`)
- Deep code review (12 files); CR-01 (trailing-slash `categoryMatches`) + WR-01 (`--paginate`) fixed — Done (`28bcfa0`, REVIEW `0019ec7`, REVIEW-FIX `5218c2d`)
- verify_phase_goal (gsd-verifier) -> `human_needed`; 35-VERIFICATION.md + 35-UAT.md — Done (`faa41e8`)
- Autonomous real-CI UAT on PR #55 -> found G-35-01; 35-UAT.md status `issues` — Done (`7140278`)
- Fix red CI fallow complexity blocker (quick 260721-vm1: split `buildRuleMeta` into per-family builders) -> fallow GREEN in real CI — Done (`439532f`, tracking `f89bcb4`)
</completed_work>

<remaining_work>

Run the resume sequence IN ORDER:
1. `/gsd-core:quick --full` Address the svgo@4.0.1 High severity vulnerability
   - Fix = `npm update svgo` (lockfile-only; safe version already permitted within range; dev-scope transitive; NO overrides, NO major bump). Verify `npm run cve-lite` exit 0 + `npm ci` still resolves + `npm run fallow`. Skill: `.claude/skills/atc-use-cve-lite-cli/`. Root lockfile only — outside the release project graph, no version change.
2. `/gsd-core:plan-phase 35 --gaps`
   - **The gap G-35-01 is in `35-UAT.md` (## Gaps), NOT in `35-VERIFICATION.md` gaps_found** — feed it to the planner explicitly.
   - Fix: attach a synthetic fallback location (artifactLocation -> the project tsconfig or the referenced missing file; region line 1) to file-less diagnostics in `formatSarifReport` PASS-2 (`sarif-report.ts:196-219`), so they are ingestible AND still never dropped. This REVERSES D-01 — update the drift-lock `machine-reporters-sarif.integration.spec.ts` to expect the new location, and confirm JSON stays consistent (the D-13 shared projection feeds both).
3. `/gsd-core:execute-phase 35 --gaps-only`
4. After it lands: re-push, confirm the `code-scanning-proof` job GREEN in real CI, then close the 3 UAT items with `/gsd-core:verify-work 35` (that auto-transitions the phase to complete).
5. Only once the phase genuinely closes: `/gsd-core:secure-phase 35`, `/gsd-core:validate-phase 35` (fills the draft 35-VALIDATION.md), `/gsd-core:extract-learnings 35`.
</remaining_work>

<decisions_made>

- Ran all execution on the MAIN working tree (no worktree isolation) — single-plan waves + dependency-heavy verification need real node_modules (AGENTS.md).
- CR-01 fix is defensive: `categoryMatches()` tolerates `CATEGORY` OR `CATEGORY + '/'` (upload-sarif category-input trailing-slash; the real string is still unobserved because the upload never processed).
- Fallow blocker fixed by extraction (behavior-preserving), not a threshold bump.
- G-35-01 handled as a deliberate D-01 REVERSAL via gap-closure, not an inline patch (published-SARIF-output change).
- cve-lite/svgo ordered FIRST as a standalone `quick --full` task (per user) — clears the required ci gate cheaply.
</decisions_made>

<blockers>
- G-35-01 (technical): GitHub rejects the file-less no-location tool result -> blocks phase completion + the code-scanning-proof job. Fix = the gap plan (reverses D-01). No workaround.
- cve-lite HIGH svgo@4.0.1 (external, new OSV advisory): fails the REQUIRED ci aggregate on PR #55. Workaround/fix = `npm update svgo`.
</blockers>

## Required Reading (in order)
1. `.planning/phases/35-automated-code-scanning-proof/35-UAT.md` — the G-35-01 gap (root cause + candidate fix + why it blocks).
2. `.planning/phases/35-automated-code-scanning-proof/35-VERIFICATION.md` — the human_needed verdict + the 3 real-CI human-verification items.
3. `.planning/phases/35-automated-code-scanning-proof/35-CONTEXT.md` — locked decisions D-01..D-04 (esp. D-01, which G-35-01 reverses, and D-04 additive-only).
4. `packages/angular-typechecker/src/core/sarif-report.ts` — the reporter; `formatSarifReport` PASS-2 (`:196-219`) is the file-less-location site to fix; `buildRuleMeta` was just refactored (leave it).
5. `.claude/skills/atc-use-cve-lite-cli/` — the svgo/cve-lite fix workflow + the npm 10/11 override trap.
6. `AGENTS.md` — release/CI/default-branch mechanics (PR-only main; milestone-ships-as-single-PR; D-04 additive posture).

## Infrastructure State
- Branch `gsd/v0.2.4-enhanced-sarif-reporting-for-github-code-scanning`, PR #55 (draft, base main), HEAD `f89bcb4`, fully pushed.
- Latest CI run (`29867662039`, f89bcb4): fallow ✅, code-scanning (dogfood) ✅, code-scanning-proof ❌ (G-35-01), cve-lite ❌ (svgo), ci aggregate ❌ (only because of cve-lite).
- Working tree clean except `.planning/config.json` (ephemeral `_auto_chain_active` flip — leave it).
- No running servers/watchers; no async external jobs.

<context>
The phase's PROOF HARNESS worked exactly as intended: the autonomous real-CI UAT caught a
genuine SARIF->Code-Scanning contract defect (G-35-01) that local gates and the dogfood job
could not. The phase's own deliverables (isolated fixture, gh-api assert, PR-only CI job) are
correct and sound — the blocker is a REPORTER defect surfaced by the fixture. The next session
is mechanical-then-design: clear the cheap cve-lite gate first, then do the deliberate D-01
reversal for G-35-01, re-prove in real CI, and close via verify-work.
</context>

<next_action>
Start with: `/gsd-core:quick --full Address the svgo@4.0.1 High severity vulnerability`
(then `/gsd-core:plan-phase 35 --gaps`, then `/gsd-core:execute-phase 35 --gaps-only`).
</next_action>
