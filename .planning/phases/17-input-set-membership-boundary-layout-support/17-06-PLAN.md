---
phase: 17-input-set-membership-boundary-layout-support
plan: 06
type: execute
wave: 4
depends_on: ["17-03", "17-04", "17-05"]
files_modified:
  - fixtures/layout-a-storybook/**
  - fixtures/layout-a-storybook-clean/**
  - fixtures/layout-b-host/**
  - fixtures/layout-b-host-clean/**
  - packages/angular-typechecker/src/core/layout-a.integration.spec.ts
  - packages/angular-typechecker/src/core/layout-b.integration.spec.ts
autonomous: true
requirements: [SB-01, SB-03]

must_haves:
  truths:
    - "A broken *.stories.ts FAILS the verdict and a clean one PASSES, under BOTH Layout A and Layout B (criterion 1)"
    - "An aggregated external-templateUrl NG8002 FAILS with the .html/component codeframe (criterion 2, the kill-shot)"
    - "An imported dependency project's internal error is NOT reported in the codeframe; the verdict is non-clean coverage-incomplete (criterion 3, R1)"
    - "A clean Layout-B host reports suppressedInGraphErrorCount == 0 && suppressedInGraphWarningCount == 0 on the structured result (criterion 4)"
    - "No Layout-A regression: the shipped walk still type-checks the Layout-A story surface (criterion 5)"
  artifacts:
    - path: "fixtures/layout-b-host/.storybook/tsconfig.json"
      provides: "widened cross-project include reaching aggregated stories/components outside the host dir"
      contains: "include"
    - path: "packages/angular-typechecker/src/core/layout-b.integration.spec.ts"
      provides: "real-compiler proof of criteria 1(B)/2/3/4 via runTypecheck against the host fixture"
    - path: "packages/angular-typechecker/src/core/layout-a.integration.spec.ts"
      provides: "real-compiler proof of criteria 1(A)/5"
  key_links:
    - from: "layout-b-host aggregated component external templateUrl"
      to: "NG8002 kept via branch 4a"
      via: "runTypecheck -> keep() relatedInformation owner in inputTs"
      pattern: "998002|NG8002"
    - from: "layout-b-host dependency project internal error"
      to: "absent from diagnostics + suppressedInGraphErrorCount >= 1"
      via: "keep() isolation (not a rootName, not under base)"
      pattern: "suppressedInGraph"
---

<objective>
Prove the phase's 5 success criteria end-to-end against REAL cold-compiler fixtures (D-09
item 3, the MINIMUM integration proof): broken/clean `*.stories.ts` fails/passes under BOTH
Layout A and Layout B; the aggregated external-`templateUrl` NG8002 kill-shot; dependency
isolation (content isolation + R1 coverage-incomplete verdict); a clean Layout-B host with
`suppressedInGraph == 0`; no Layout-A regression.

Purpose: the unit tiers prove `keep()` mechanics; this plan proves the WHOLE pipeline
(walk -> inputTs union -> keep -> split counter -> coverage-incomplete verdict) delivers the
milestone value against actual Angular diagnostics. Fixtures are plain Angular (NO
`@storybook/angular` install -- a widened-include host reproduces Layout B; A2).
Output: four in-repo fixtures + two `*.integration.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

READ-FIRST source-of-truth:
@.planning/phases/17-input-set-membership-boundary-layout-support/17-CONTEXT.md
@.planning/phases/17-input-set-membership-boundary-layout-support/17-RESEARCH.md
@.claude/skills/spike-findings-angular-typechecker/references/storybook-input-set-boundary.md

Fixture + integration-spec conventions to mirror:
@packages/angular-typechecker/src/core/walk-references.integration.spec.ts
@fixtures/solution-style/tsconfig.json
@fixtures/solution-style/tsconfig.app.json
@fixtures/ng-baseline/error.component.ts

The pipeline under test (17-03/04/05 outputs):
@packages/angular-typechecker/src/core/run-typecheck.ts
@.planning/phases/17-input-set-membership-boundary-layout-support/17-03-SUMMARY.md
@.planning/phases/17-input-set-membership-boundary-layout-support/17-04-SUMMARY.md
@.planning/phases/17-input-set-membership-boundary-layout-support/17-05-SUMMARY.md

Cross-platform notes: use `@workspace/test-util` `findWorkspaceRoot` + `fixtureTsConfig(name)`
(the walk integration spec's idiom); NG codes are negative-encoded (`NG8002 === -998002`,
`ngCodeOf`); cold-compiler timeout is inherited from `vitest.config.mts` (30000) -- do NOT add
a per-file testTimeout. Fixtures live at the workspace root `fixtures/`, OUT of the package's
`tsconfig.lib.json` `include`, so they are never compiled into the plugin build.
</context>

<tasks>

<task type="auto">
  <name>Task 1: build Layout-A + Layout-B fixtures (plain Angular, no @storybook/angular)</name>
  <files>fixtures/layout-a-storybook/**, fixtures/layout-a-storybook-clean/**, fixtures/layout-b-host/**, fixtures/layout-b-host-clean/**</files>
  <read_first>
    - fixtures/solution-style/ (the reference solution + app-leaf tsconfig shape; `files: []` + `references` on the solution, `noEmit`/strict/strictTemplates on the leaf, extends ../../tsconfig.base.json)
    - fixtures/ng-baseline/error.component.ts + error.component.html (the external templateUrl + NG8xxx codeframe pattern)
    - .claude/skills/spike-findings-angular-typechecker/references/storybook-input-set-boundary.md (Layout B host shape: host solution references ONLY `./.storybook/tsconfig.json`; `.storybook/tsconfig.json` include reaches out via relative globs; aggregation is a manual recipe edit)
    - 17-CONTEXT.md <specifics> (Layout B host shape; forced-SB constraint is docs-only, NOT needed for fixtures)
  </read_first>
  <action>
    Create four fixtures under `fixtures/` (mirror `solution-style/`'s extends/strict/strictTemplates shape; stories are PLAIN Angular `.ts` -- do NOT import `@storybook/angular`, so the only diagnostics are the planted ones; a file is a "story" by name/location, never a filename allowlist):
    - `layout-a-storybook/` -- a project solution `tsconfig.json` (`files: []`, references `./tsconfig.app.json` + `./.storybook/tsconfig.json`); `.storybook/tsconfig.json` includes a `*.stories.ts` in the project's own `src/`; the story has a DELIBERATE TS type error (e.g. assign a `string` to a `number`-typed component input, or a bad template binding for an NG8xxx). This is the broken-A input.
    - `layout-a-storybook-clean/` -- identical shape, but the story is CLEAN (no error). This is the clean-A input.
    - `layout-b-host/` -- a HOST solution `tsconfig.json` that references ONLY `./.storybook/tsconfig.json`; `.storybook/tsconfig.json` `include` reaches OUTSIDE the host dir via relative globs to an aggregated project dir in the fixture tree (e.g. `../aggregated/**/*.ts`), materializing an aggregated `*.stories.ts`, an aggregated `*.component.ts` with an EXTERNAL `templateUrl` pointing at a sibling `.html`, and the external `.html`. Plant TWO errors: (i) a TS type error in the aggregated `*.stories.ts` (broken aggregated story), and (ii) an NG8002 in the aggregated component's external `.html` template (the kill-shot). Add a SEPARATE `dependency/` project (its own tsconfig leaf, NOT in the widened include, imported by an aggregated component) that contains an INTERNAL type error -- for the isolation proof.
    - `layout-b-host-clean/` -- the SAME host+aggregated shape but with a CLEAN aggregated story AND a clean external-template component AND (to exercise D-04a) at least one aggregated component using an external `.html` template that is CLEAN -- so a correct classification yields `suppressedInGraph == 0`.
    Keep all fixtures minimal; reuse `../../tsconfig.base.json` extends and `angularCompilerOptions.strictTemplates: true` so NG8xxx promote. Do NOT install any package.
  </action>
  <verify>
    <automated>test -f fixtures/layout-b-host/.storybook/tsconfig.json && test -f fixtures/layout-a-storybook/tsconfig.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Four fixture roots exist with the shapes above; `layout-b-host/.storybook/tsconfig.json` `include` reaches outside the host dir.
    - The Layout-B aggregated component uses an EXTERNAL `templateUrl` (.html on disk) carrying an NG8002; the dependency project is NOT in the widened include.
    - No fixture imports `@storybook/angular`; no package is installed.
  </acceptance_criteria>
  <done>Plain-Angular Layout-A (broken+clean) and Layout-B host (broken+clean, with aggregated external template + isolated dependency) fixtures exist.</done>
</task>

<task type="auto">
  <name>Task 2: Layout-A integration spec (criteria 1(A) + 5)</name>
  <files>packages/angular-typechecker/src/core/layout-a.integration.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/walk-references.integration.spec.ts (findWorkspaceRoot + fixtureTsConfig + `runTypecheck({ tsConfigPath })` + codesOf idiom)
    - packages/angular-typechecker/src/core/evaluate-result.ts (evaluateResult for the pass/fail assertion)
  </read_first>
  <action>
    Create `layout-a.integration.spec.ts` calling `runTypecheck({ tsConfigPath: fixtureTsConfig('layout-a-storybook') })` and asserting the broken story's diagnostic is in `result.diagnostics` (`errorCount >= 1`) AND `evaluateResult(result).success === false`; then `runTypecheck({ tsConfigPath: fixtureTsConfig('layout-a-storybook-clean') })` asserting `errorCount === 0`, `suppressedInGraphErrorCount === 0`, and `evaluateResult(result).success === true` (criterion 1 Layout A). Add a no-regression assertion (criterion 5): the story leaf's file IS represented in the run (rootNamesCount > 0 and the story error surfaces), proving the shipped walk still type-checks the Layout-A story surface. Inherit the config testTimeout.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- layout-a</automated>
  </verify>
  <acceptance_criteria>
    - Broken Layout-A story -> `errorCount >= 1` and `evaluateResult(result).success === false`.
    - Clean Layout-A story -> `errorCount === 0`, `suppressedInGraphErrorCount === 0`, `success === true`.
    - `npx nx test angular-typechecker -- layout-a` green.
  </acceptance_criteria>
  <done>Layout A broken-fails / clean-passes + no walk regression proven end-to-end.</done>
</task>

<task type="auto">
  <name>Task 3: Layout-B integration spec (criteria 1(B) + 2 + 3 + 4)</name>
  <files>packages/angular-typechecker/src/core/layout-b.integration.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/walk-references.integration.spec.ts (the idiom)
    - packages/angular-typechecker/src/core/diagnostic-codes.ts (`NG`, `ngCodeOf`; NG8002 === -998002)
    - 17-RESEARCH.md "Pitfall 3" (isolation = dependency error ABSENT from codeframe AND verdict non-clean via coverage-incomplete -- do NOT assert success:true for the isolation case)
  </read_first>
  <action>
    Create `layout-b.integration.spec.ts` calling `runTypecheck({ tsConfigPath: fixtureTsConfig('layout-b-host') })`:
    - criterion 1(B): the aggregated broken `*.stories.ts` error is in `result.diagnostics` and `evaluateResult(result).success === false`.
    - criterion 2 (kill-shot): the aggregated external-`templateUrl` NG8002 (code `NG(8002)` / `-998002`) is in `result.diagnostics` with its `.html` (or component) codeframe -- assert the code is present AND the diagnostic carries a `.html` file or a `.ts` relatedInformation owner (proving branch 4a kept it).
    - criterion 3 (isolation, R1): the `dependency/` project's INTERNAL error code is ABSENT from `result.diagnostics` (content isolation) AND `result.suppressedInGraphErrorCount >= 1` and `evaluateResult(result).success === false` (coverage-incomplete -- NOT success:true).
    Then `runTypecheck({ tsConfigPath: fixtureTsConfig('layout-b-host-clean') })`:
    - criterion 4: `result.suppressedInGraphErrorCount === 0 && result.suppressedInGraphWarningCount === 0` (the clean host, incl. its clean external template, is classified in-graph by construction) AND `evaluateResult(result).success === true`; assert both structured counts are present on the result (structured-result surfacing; the stdout surfacing is proven in 17-05 executor.spec).
    Inherit the config testTimeout.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- layout-b</automated>
  </verify>
  <acceptance_criteria>
    - Broken aggregated story -> `success === false`; the NG8002 external-template diagnostic is present with its .html/component codeframe (kill-shot).
    - The dependency's internal error code is ABSENT from diagnostics AND `suppressedInGraphErrorCount >= 1` AND `success === false` (R1 isolation).
    - Clean host -> `suppressedInGraphErrorCount === 0 && suppressedInGraphWarningCount === 0` AND `success === true`.
    - `npx nx test angular-typechecker` full suite green.
  </acceptance_criteria>
  <done>All Layout-B criteria (1B/2/3/4) proven end-to-end against the widened-include host fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| aggregated cross-project files -> host input set | files outside the host dir enter via the widened include; the boundary must keep aggregated first-party files while isolating dependencies |
| external .html template -> owning component .ts | branch 4a's attribution decides whether an aggregated template error is reported |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-14 | Tampering (false pass on a real story error) | Layout-B aggregated out-of-dir story dropped | mitigate | the broken-aggregated-story fixture asserts `success === false`; input-set membership keeps the aggregated rootName |
| T-17-02 | Tampering (external-template error silently dropped) | branch 4a on the aggregated `.html` | mitigate | the kill-shot fixture asserts the NG8002 is present with its codeframe; the D-09.2 tripwire (17-07) guards the attribution invariant |
| T-17-15 | Information disclosure / false verdict (dependency error) | isolation classification | mitigate | the isolation fixture asserts the dep error CODE is absent from diagnostics (content isolation) AND the verdict is coverage-incomplete (R1) |
| T-17-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | N/A -- fixtures are plain Angular, no install |
</threat_model>

<verification>
- `npx nx test angular-typechecker -- layout-a layout-b` green.
- Per wave merge: full suite + build + lint + format:check (CI gates).
</verification>

<success_criteria>
- All 5 phase success criteria proven end-to-end against real cold-compiler fixtures.
- No `@storybook/angular` install; fixtures are plain Angular widened-include hosts.
- Isolation asserts content-isolation + R1 coverage-incomplete (not success:true).
</success_criteria>

<output>
Create `.planning/phases/17-input-set-membership-boundary-layout-support/17-06-SUMMARY.md` when done.
</output>
