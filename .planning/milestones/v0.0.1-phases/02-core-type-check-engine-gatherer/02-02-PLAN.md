---
phase: 02-core-type-check-engine-gatherer
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - fixtures/config-broken/error.component.ts
  - fixtures/config-broken/error.component.html
  - fixtures/config-broken/error.component.spec.ts
  - fixtures/config-broken/tsconfig.spec.json
  - fixtures/config-broken/tsconfig.malformed.json
  - fixtures/solution-style/tsconfig.json
  - fixtures/solution-style/tsconfig.app.json
  - fixtures/solution-style/error.component.ts
  - packages/angular-typechecker/src/core/config-resolution.integration.spec.ts
autonomous: true
requirements:
  - EXE-02
  - ENG-01
must_haves:
  truths:
    - "runTypecheck against a tsconfig.spec.json reports the planted spec-file type error (specs are type-checked, not just built)"
    - "runTypecheck against a malformed/nonexistent tsconfig reports errorCount >= 1 (never silently clean)"
    - "runTypecheck against a references-only / solution-style tsconfig returns rootNamesCount 0 AND errorCount 1 with a message naming a leaf tsconfig"
  artifacts:
    - path: "fixtures/solution-style/tsconfig.json"
      provides: "The references-only silent-lie input shape (D-03/D-03a regression proof)"
      contains: "references"
    - path: "fixtures/config-broken/tsconfig.spec.json"
      provides: "A spec tsconfig pointing at a planted spec-file error (EXE-02 differentiator vs a build check)"
    - path: "packages/angular-typechecker/src/core/config-resolution.integration.spec.ts"
      provides: "Real-compiler proof of D-03 fail-loud config handling + EXE-02 spec checking"
  key_links:
    - from: "config-resolution.integration.spec.ts"
      to: "runTypecheck"
      via: "direct call against committed fixture tsconfigs (D-07c)"
      pattern: "runTypecheck\\(\\{\\s*tsConfigPath"
    - from: "solution-style/tsconfig.json"
      to: "the ATC zero-rootNames guard"
      via: "rootNamesCount === 0 short-circuit"
      pattern: "rootNamesCount"
---

<objective>
Deliver the fail-loud config-resolution slice end-to-end: prove via the REAL compiler that `runTypecheck` (a) type-checks a `tsconfig.spec.json` (the EXE-02 differentiator vs a build check), (b) never reports a silent clean on a malformed/nonexistent tsconfig (D-03 prepend of `parsed.errors`, MD-01), and (c) returns a deterministic non-zero signal (`rootNamesCount === 0` AND `errorCount === 1`) on a references-only / solution-style tsconfig instead of "0 files / 0 errors" (D-03/D-03a, ROADMAP success criterion 3).

This slice is a thin but COMPLETE capability proven end-to-end through `runTypecheck` against committed fixtures (D-07c). It owns the project-type-coverage requirement that a spec tsconfig is checked (D-07b: app + local-lib are already covered by the reused `gate-b-error` fixture; this slice adds the named `tsconfig.spec.json` differentiator) and the MD-01 / solution-style regression proofs.

Purpose: Closes the "type-checker that lies via config" threat surface end-to-end. The engine logic was implemented in 02-01; this slice provides the committed fixtures and the integration assertions that prove the D-03 guard fires correctly across the real silent-lie inputs.

Output: New committed fixtures (a spec-tsconfig with a planted error, a malformed-tsconfig input, a solution-style references-only tsconfig) and `config-resolution.integration.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-type-check-engine-gatherer/02-CONTEXT.md
@.planning/phases/02-core-type-check-engine-gatherer/02-RESEARCH.md
@.planning/phases/02-core-type-check-engine-gatherer/02-PATTERNS.md
@.planning/phases/02-core-type-check-engine-gatherer/02-01-SUMMARY.md

<interfaces>
<!-- The CoreResult contract this slice asserts against (locked by 02-01, D-01). -->
```typescript
export interface CoreResult {
  tsConfigPath: string;
  rootNamesCount: number;                // 0 => the D-03 zero-rootNames guard fired
  diagnostics: readonly ts.Diagnostic[]; // config errors prepended (D-03)
  errorCount: number;                    // category === Error
  warningCount: number;                  // category === Warning
  durationMs: number;
}
```
`runTypecheck({ tsConfigPath })` requires an ABSOLUTE path (D-04). Tests build it via `join(workspaceRoot, 'fixtures', ...)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the config-resolution fixtures (spec, malformed, solution-style)</name>
  <files>fixtures/config-broken/error.component.ts, fixtures/config-broken/error.component.html, fixtures/config-broken/error.component.spec.ts, fixtures/config-broken/tsconfig.spec.json, fixtures/config-broken/tsconfig.malformed.json, fixtures/solution-style/tsconfig.json, fixtures/solution-style/tsconfig.app.json, fixtures/solution-style/error.component.ts</files>
  <read_first>
    - fixtures/gate-b-error/tsconfig.app.json (the fixture tsconfig pattern to copy: extends ../../tsconfig.base.json, strictTemplates true, files[])
    - fixtures/gate-b-error/tsconfig.lib.json (the lib variant with include[])
    - fixtures/gate-b-error/error.component.ts (the broken-standalone-component shape; do NOT add @ts-nocheck)
    - .planning/phases/02-core-type-check-engine-gatherer/02-RESEARCH.md (Fixture Catalog lines 334-340 — spec tsconfig + solution-style requirements; D-03a TS18003-suppression note)
    - .planning/phases/02-core-type-check-engine-gatherer/02-PATTERNS.md (solution-style fixture section lines 243-256 verbatim shape; spec-tsconfig note lines 216; fixture tsconfig pattern lines 203-217)
  </read_first>
  <action>
    Create `fixtures/config-broken/` with a spec-source and tsconfigs (per D-07b, EXE-02):
    - `error.component.ts` + `error.component.html`: mirror the `gate-b-error` broken-standalone-component shape (a deliberate TS2322 such as `count: number = 'not a number'`). Do NOT add `@ts-nocheck`.
    - `error.component.spec.ts`: a `*.spec.ts` source with a planted spec-file type error (e.g. a typed assertion or assignment that fails type-check).
    - `tsconfig.spec.json`: model on the `gate-b-error` lib variant (extends `../../tsconfig.base.json`, `strictTemplates: true`) but named `tsconfig.spec.json`, pointing at the `error.component.spec.ts` source. This proves specs are type-checked (the named differentiator vs a build check — ROADMAP criterion 3 / EXE-02).
    - `tsconfig.malformed.json`: an intentionally malformed/unresolvable tsconfig (e.g. `extends` a nonexistent path) that `ng.readConfiguration` returns as `parsed.errors`. If invalid JSON is awkward to commit, point `extends` at a nonexistent file — that yields a config error without a parse failure of THIS file.

    Create `fixtures/solution-style/` (the silent-lie input, D-03/D-03a) — copy the exact shape from 02-PATTERNS.md lines 248-255:
    - `tsconfig.json`: `{ "extends": "../../tsconfig.base.json", "compileOnSave": false, "files": [], "references": [{ "path": "./tsconfig.app.json" }] }` — references-only, so `rootNames` is empty AND TS18003 is suppressed.
    - `tsconfig.app.json`: a leaf tsconfig (model on `gate-b-error/tsconfig.app.json`) referencing `error.component.ts` so the references target resolves; this is the leaf the guard message should steer the user toward.
    - `error.component.ts`: a minimal standalone component (valid or broken — either is fine; the point is the SOLUTION tsconfig produces zero rootNames regardless).

    All fixtures live at workspace-root `fixtures/`, out of the project graph, already covered by the broad `fixtures/**/*` exclude in `tsconfig.lib.json` (added by 02-01). ASCII-only content.
  </action>
  <verify>
    <automated>npx nx build angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `ls fixtures/config-broken/tsconfig.spec.json fixtures/config-broken/tsconfig.malformed.json fixtures/solution-style/tsconfig.json fixtures/solution-style/tsconfig.app.json` all exist.
    - `git grep -n "references" fixtures/solution-style/tsconfig.json` matches and `git grep -n '"files": \[\]' fixtures/solution-style/tsconfig.json` matches (the references-only silent-lie shape).
    - `fixtures/config-broken/tsconfig.spec.json` points at `error.component.spec.ts` (the spec-checking differentiator).
    - `git grep -n "ts-nocheck" fixtures/config-broken fixtures/solution-style` returns nothing.
    - `npx nx build angular-typechecker` still succeeds — the new fixture `.ts` are excluded from the lib build (no broken fixture source compiled into the package).
  </acceptance_criteria>
  <done>
    The spec-tsconfig (with a planted spec-file error), the malformed-config input, and the solution-style references-only tsconfig are committed under `fixtures/`, modeled on the `gate-b-error` shapes, and excluded from the package build.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author config-resolution.integration.spec.ts (D-03 + EXE-02 proofs)</name>
  <files>packages/angular-typechecker/src/core/config-resolution.integration.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/gate-b.spec.ts (the fixture-path resolution idiom lines 1-2, 34-39 + describe.each pattern lines 68-71 to copy)
    - packages/angular-typechecker/src/core/run-typecheck.ts (the contract under test — the D-03 guard + prepend implemented in 02-01)
    - fixtures/config-broken/tsconfig.spec.json (Task 1 output)
    - fixtures/config-broken/tsconfig.malformed.json (Task 1 output)
    - fixtures/solution-style/tsconfig.json (Task 1 output)
    - .planning/phases/02-core-type-check-engine-gatherer/02-RESEARCH.md (Requirement->Test Map rows D-03/D-03a/EXE-02 lines 445/449/450; D-07c direct-runTypecheck)
    - .planning/phases/02-core-type-check-engine-gatherer/02-PATTERNS.md (direct-runTypecheck driver pattern lines 144-151)
  </read_first>
  <action>
    Create `src/core/config-resolution.integration.spec.ts` (REAL compiler, D-07c). Copy the fixture-path resolution idiom from `gate-b.spec.ts` lines 1-2, 34-39 (`packageRoot`/`workspaceRoot`/`join(workspaceRoot, 'fixtures', ...)`). Call `runTypecheck({ tsConfigPath })` directly (never share a program across fixtures). Assert:

    - EXE-02 (spec tsconfig checked): `runTypecheck({ tsConfigPath: <fixtures/config-broken/tsconfig.spec.json> })` -> `result.rootNamesCount > 0` and `result.errorCount >= 1` with the planted spec-file error code present in `result.diagnostics.map((d) => d.code)`. This proves specs are type-checked, not just built.

    - D-03 / MD-01 (malformed config not silently clean): `runTypecheck({ tsConfigPath: <fixtures/config-broken/tsconfig.malformed.json> })` -> `result.errorCount >= 1` (NOT 0 / not a false clean); the prepended `parsed.errors` entry is present in `result.diagnostics`. Assert it does NOT throw (config errors are RETURNED, not thrown — D-03 part 3).

    - D-03 / D-03a (solution-style guard fires): `runTypecheck({ tsConfigPath: <fixtures/solution-style/tsconfig.json> })` -> `result.rootNamesCount === 0` AND `result.errorCount === 1` AND the synthesized diagnostic's `messageText` (string-coerce via `ts.flattenDiagnosticMessageText` or direct string read) names a leaf tsconfig (matches `/tsconfig\.(app|lib|spec)\.json/`). This is the ROADMAP criterion-3 / MD-01 silent-lie regression proof; TS18003 must NOT be the gate (assert the result does NOT depend on a `18003` code being present).

    Name the file `*.integration.spec.ts` so the quick-run `--exclude '**/*.integration.spec.ts'` filter skips it (full suite runs it). Apply project JS/TS style (blank lines around control flow/returns, braces always).
  </action>
  <verify>
    <automated>npx nx build angular-typechecker && npx nx test angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `npx nx test angular-typechecker` is green including `config-resolution.integration.spec.ts`.
    - The solution-style case asserts EXACTLY `result.rootNamesCount === 0` AND `result.errorCount === 1` AND a `messageText` matching `/tsconfig\.(app|lib|spec)\.json/`.
    - The malformed-config case asserts `result.errorCount >= 1` and that `runTypecheck` does NOT throw.
    - The spec-tsconfig case asserts `result.rootNamesCount > 0` and the planted spec-file error code is in `result.diagnostics.map(d => d.code)`.
    - `git grep -n "runTypecheck(" packages/angular-typechecker/src/core/config-resolution.integration.spec.ts` shows direct `runTypecheck` calls (D-07c), not raw `performCompilation`.
  </acceptance_criteria>
  <done>
    The integration spec proves end-to-end via the real compiler that a spec tsconfig is type-checked (EXE-02), a malformed config is never silently clean (D-03/MD-01), and a solution-style references-only config returns the deterministic `rootNamesCount: 0` / `errorCount: 1` guard with a leaf-tsconfig-naming message (D-03/D-03a).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer tsconfig -> runTypecheck | Malformed / references-only / spec tsconfigs are the untrusted config inputs this slice exercises. `ng.readConfiguration` parses; no eval/exec. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02b | Tampering / Repudiation-of-trust | malformed tsconfig handling | mitigate | "Type-checker that LIES" via config: the malformed-config fixture + integration assertion prove `errorCount >= 1` (never a false clean) — the D-03 MD-01 regression gate. Task 1 fixture + Task 2 assertion. |
| T-02-06 | Tampering / Repudiation-of-trust | solution-style / references-only tsconfig | mitigate | The silent-lie input (`{files:[],references:[...]}`) is proven to yield `rootNamesCount: 0` + `errorCount: 1` (the ATC guard), NOT "0 files / 0 errors". Gate is `rootNames.length === 0`, NEVER TS18003 (D-03a / L-2). Task 1 fixture + Task 2 assertion. |
| T-02-07 | Tampering / Repudiation-of-trust | spec tsconfig | mitigate | EXE-02: a `tsconfig.spec.json` with a planted error is proven to be type-checked (reports the error), closing the "specs silently skipped" gap that distinguishes this tool from a build check. Task 1 fixture + Task 2 assertion. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies (fixtures + a Vitest spec only). No supply-chain delta. |
</threat_model>

<verification>
- `npx nx build angular-typechecker` succeeds (fixtures excluded from the lib build).
- `npx nx test angular-typechecker` green including the new `config-resolution.integration.spec.ts`.
- Quick run skips it: `npx nx test angular-typechecker -- --exclude '**/*.integration.spec.ts'`.
</verification>

<success_criteria>
- A spec tsconfig is proven type-checked (EXE-02, ROADMAP criterion 3).
- A malformed/nonexistent tsconfig is never silently clean (D-03/MD-01).
- A solution-style references-only tsconfig returns `rootNamesCount: 0` + `errorCount: 1` with a leaf-tsconfig-naming message (D-03/D-03a, ROADMAP criterion 3).
</success_criteria>

<output>
Create `.planning/phases/02-core-type-check-engine-gatherer/02-02-SUMMARY.md` when done.
</output>
