---
phase: 24-real-oss-scaffolded-e2e-additive-only-audit-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/README.md
  - packages/angular-typechecker/src/angular-cli-docs.spec.ts
  - CHANGELOG.md
autonomous: true
requirements: [ACD-01]

must_haves:
  truths:
    - "A consumer reading the README learns how to enable and run the Angular CLI surface: `ng add` auto-wires every app+library, `ng generate ...:configuration <project>` wires one, and `ng run <project>:typecheck` runs the complete check."
    - "The README documents the per-project target shape (tsConfig array), the nx-transitive + .nx/ + no-caching notes, and the off-stack `--legacy-peer-deps` note -- without weakening the existing Storybook 'not supported' caveat."
    - "A docs content tripwire fails loudly if any load-bearing `## Angular CLI` claim is deleted, softened, or over-claimed."
    - "The CHANGELOG carries a 0.2.1 entry in end-user language with no internal ids and no version/tag cut."
    - "D-06: the README `## Angular CLI` section carries every enumerated item -- ng add auto-wire-all, single-project ng generate ...:configuration, ng run <project>:typecheck, per-project targets, the tsConfig array, the nx-transitive + .nx/ + no-caching notes, and the off-stack --legacy-peer-deps note -- in end-user language."
  artifacts:
    - path: "packages/angular-typechecker/README.md"
      provides: "The `## Angular CLI` end-user section"
      contains: "## Angular CLI"
    - path: "packages/angular-typechecker/src/angular-cli-docs.spec.ts"
      provides: "Filesystem-read tripwire locking the Angular CLI doc claims"
      min_lines: 25
    - path: "CHANGELOG.md"
      provides: "The curated 0.2.1 prose entry"
      contains: "Angular CLI"
  key_links:
    - from: "packages/angular-typechecker/src/angular-cli-docs.spec.ts"
      to: "packages/angular-typechecker/README.md"
      via: "readFileSync('../README.md') + normalized toContain assertions"
      pattern: "README\\.md"
---

<objective>
Document the new Angular CLI surface (shipped in Phases 21-23) in end-user language and lock the load-bearing claims with a content tripwire. No production code changes.

Purpose: ACD-01 requires a README `## Angular CLI` section covering the full consumer flow plus a curated CHANGELOG entry (PROSE ONLY -- the 0.2.0 -> 0.2.1 version cut is the separate human-gated Release-PR flow, AGENTS.md). The docs tripwire mirrors `storybook-docs.spec.ts` so a deleted/softened claim fails on every PR.

Output: the `## Angular CLI` README section, `src/angular-cli-docs.spec.ts`, and a 0.2.1 CHANGELOG entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-RESEARCH.md
@.planning/phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-PATTERNS.md

<interfaces>
<!-- Verified consumer-facing command syntax (RESEARCH.md ACD-01). -->

Consumer commands to document:
  ng add angular-typechecker                          # auto-wires typecheck into every app + library; ensures devDep; prints no-caching notice; on-stack Ng22 needs NO --legacy-peer-deps
  ng generate angular-typechecker:configuration <p>   # wires ONE project's typecheck target
  ng run <project>:typecheck                          # runs the complete Angular type-check; exit verdict = BuilderOutput.success (identical to the Nx executor)

Per-project target shape written into angular.json:
  architect.typecheck = { builder: "angular-typechecker:typecheck", options: { tsConfig: [<build leaf>, <spec leaf>] } }
  # app example: [tsconfig.app.json, tsconfig.spec.json]
  # lib example: [projects/<lib>/tsconfig.lib.json, projects/<lib>/tsconfig.spec.json]

Required notes (D-06):
  - nx pulled transitively + a .nx/ cache dir may appear + NO target caching on the Angular CLI path (Angular CLI has no task-result cache to seed)
  - off-stack Angular < 22 consumers hit the @angular/compiler-cli ^22.0.0 / typescript >=6.0.0 <6.1.0 peer cap -> install with --legacy-peer-deps (Pitfall 6; guidance, not a test tier)

Existing README heading order (do not disturb): ... ## Storybook (line ~379) -> ### Storybook Composition -> ## Limitations (~485). Place `## Angular CLI` as a new top-level section (planner discretion; after ## How it compares or before/after ## Storybook).

The Storybook docs tripwire (src/storybook-docs.spec.ts) asserts the README contains
  'Angular CLI Storybook setup' and 'is not supported'
-- do NOT delete or weaken that text; the new section is about general Angular CLI typecheck support, which is NOT contradictory.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README `## Angular CLI` section (ACD-01)</name>
  <files>packages/angular-typechecker/README.md</files>
  <read_first>
    - packages/angular-typechecker/README.md (the file being modified -- read the `## Storybook` section at ~line 379 for the length/tone precedent, and the existing heading order)
    - 24-RESEARCH.md "ACD-01: Docs" -> "Verified command syntax" + "Required notes" + "Tension to reconcile"
    - 24-PATTERNS.md section "packages/angular-typechecker/README.md -- `## Angular CLI` section"
    - packages/angular-typechecker/src/storybook-docs.spec.ts (to see which existing Storybook strings must NOT be broken -- 'Angular CLI Storybook setup', 'is not supported')
  </read_first>
  <action>
    Add a new top-level `## Angular CLI` section to `packages/angular-typechecker/README.md` (placement is planner discretion; keep it a sibling of `## Storybook`). Cover, in end-user language ([[changelog-readme-end-user-facing]] -- no internal ids, phase/plan numbers, or board jargon): the `ng add angular-typechecker` auto-wire-all behavior (every `application` + `library` project gains a `typecheck` target; idempotent; ensures the devDependency; prints a no-caching notice; on-stack Angular 22 needs no `--legacy-peer-deps`); `ng generate angular-typechecker:configuration <project>` for wiring a single project added later; `ng run <project>:typecheck` (exit verdict identical to the Nx executor); the per-project target shape (`architect.typecheck = { builder: "angular-typechecker:typecheck", options: { tsConfig: [<build leaf>, <spec leaf>] } }` with the app/lib leaf examples); how the `tsConfig` array unions the build + spec leaves so one target checks the project's COMPLETE leaf set; the `nx`-transitive + `.nx/` + no-target-caching notes; and the off-stack `--legacy-peer-deps` note for Angular < 22 consumers. Word the section so it does NOT appear to contradict the existing Storybook "Angular CLI Storybook setup ... is not supported" caveat, and do NOT delete or soften that caveat (its tripwire would fail). Do not touch any other section.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - README contains a `## Angular CLI` heading with subsections/prose covering: `ng add angular-typechecker`, `ng generate angular-typechecker:configuration`, `ng run <project>:typecheck`, the `tsConfig` array per-project target shape, the `nx`-transitive / `.nx/` / no-caching notes, and the off-stack `--legacy-peer-deps` note.
    - The existing Storybook tripwire still passes: `npx nx test angular-typechecker` green (the strings `'Angular CLI Storybook setup'` and `'is not supported'` remain present).
    - No internal ids / phase-plan numbers / board jargon appear in the new section.
  </acceptance_criteria>
  <done>The README documents the complete Angular CLI consumer flow in end-user language without weakening the Storybook caveat.</done>
</task>

<task type="auto">
  <name>Task 2: Angular CLI docs content tripwire (src/angular-cli-docs.spec.ts)</name>
  <files>packages/angular-typechecker/src/angular-cli-docs.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/storybook-docs.spec.ts (EXACT analog: readFileSync('../README.md') + `normalized = readme.replace(/\s+/g,' ')` + `toContain` claim locks)
    - packages/angular-typechecker/README.md (the `## Angular CLI` section written in Task 1 -- lock the exact claim strings it carries)
    - 24-RESEARCH.md "Docs content tripwire (RECOMMEND: add one)"
    - 24-PATTERNS.md section "src/angular-cli-docs.spec.ts"
  </read_first>
  <action>
    Create `packages/angular-typechecker/src/angular-cli-docs.spec.ts` mirroring `storybook-docs.spec.ts`: read `../README.md`, normalize whitespace (`readme.replace(/\s+/g, ' ')`) so claims survive re-wrapping, and assert (via `toContain`) the LOAD-BEARING `## Angular CLI` claims are present: the `ng add` auto-wire-all claim (every application + library gets a `typecheck` target), the `ng run <project>:typecheck` run claim, the no-target-caching notice, the `nx`-transitive / `.nx/` note, and the off-stack `--legacy-peer-deps` note. Choose exact substrings that actually appear in the Task-1 prose (normalize-tolerant). This is a pure filesystem read (no compiler load) so it rides the fast `test` loop and fails loudly if a claim is deleted or softened. Do NOT re-assert or duplicate the Storybook claims (that is `storybook-docs.spec.ts`' job).
  </action>
  <verify>
    <automated>npx nx test angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `src/angular-cli-docs.spec.ts` reads `../README.md`, normalizes whitespace, and asserts (>= 4 distinct `toContain` claims) the auto-wire-all, `ng run`, no-caching, `nx`-transitive/`.nx/`, and off-stack `--legacy-peer-deps` claims.
    - `npx nx test angular-typechecker` includes the new spec and is green.
    - Deleting the `## Angular CLI` section would make this spec RED (verify by reasoning -- the assertions target strings only that section provides).
  </acceptance_criteria>
  <done>The load-bearing Angular CLI doc claims are locked by a standing content tripwire that runs on every PR.</done>
</task>

<task type="auto">
  <name>Task 3: Curated CHANGELOG 0.2.1 prose entry (ACD-01, prose only)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (the file being modified -- read the `## 0.2.0 (2026-07-07)` entry at ~line 5 for the style precedent: bold headline sentence, prose paragraph, a `### Features` list)
    - 24-RESEARCH.md "CHANGELOG" (the content to describe; PROSE ONLY, no release cut)
    - AGENTS.md "Releases go through a Release PR" (why this is prose-only -- no `nx release`, no version bump, no tag here)
    - 24-PATTERNS.md section "CHANGELOG.md -- 0.2.1 entry"
  </read_first>
  <action>
    Add a `## 0.2.1` entry to `CHANGELOG.md` above the `0.2.0` entry, mirroring the 0.2.0 style (bold headline + prose + a short features list). Describe, in END-USER language: Angular CLI (`angular.json`) workspace support -- `ng add angular-typechecker` auto-wires a `typecheck` target into every application + library; `ng run <project>:typecheck` runs the complete Angular type-check with an exit verdict identical to the Nx surface; per-project scoping via the `tsConfig` array (build + spec leaves); and notes on `nx` transitivity / no target caching on the Angular CLI path and the off-stack `--legacy-peer-deps` guidance for Angular < 22 consumers. NO internal ids, phase/plan numbers, or board jargon ([[changelog-readme-end-user-facing]]). Do NOT run `nx release`, do NOT bump `package.json`, do NOT create a tag -- the version cut is the separate human-gated Release-PR flow (AGENTS.md). A dated header is optional (the release date is set at cut time); if included, mark it clearly as pending or omit the date.
  </action>
  <verify>
    <automated>npx nx run angular-typechecker:format:check 2>/dev/null || npx prettier --check CHANGELOG.md packages/angular-typechecker/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` has a `## 0.2.1` entry describing Angular CLI workspace support in end-user language, styled like the 0.2.0 entry.
    - No internal ids / phase-plan numbers / board jargon in the entry.
    - `package.json` version is UNCHANGED (still 0.2.0) and NO git tag is created -- confirm `git diff --name-only` shows only CHANGELOG.md/README.md/the new spec, never package.json.
    - Prettier `--check` passes on the edited docs (formatting gate).
  </acceptance_criteria>
  <done>A curated end-user 0.2.1 CHANGELOG entry exists as prose; no version cut occurred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README prose -> consumer trust | An over-claimed or softened support statement misleads consumers about what is verified; the docs tripwire is the control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Information disclosure / Repudiation | README `## Angular CLI` claims (softened/over-claimed/deleted) | mitigate | `src/angular-cli-docs.spec.ts` content tripwire locks the load-bearing claims; the existing `storybook-docs.spec.ts` caveat is preserved (not weakened), keeping the security-relevant "not supported" statement intact |
| T-24-04 | Tampering | accidental version/tag cut in a prose-only docs change | mitigate | CHANGELOG is prose-only; acceptance criteria assert `package.json` unchanged and no tag; the Release-PR flow is the sole cut path (AGENTS.md) |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no package (docs + one filesystem-read spec); no [ASSUMED]/[SUS] dependency (RESEARCH.md Package Legitimacy Audit) |

No `high`+ severity threat exists in this plan's surface (documentation + a filesystem-read tripwire; no runtime code, no network, no auth). Stated explicitly per the security context.
</threat_model>

<verification>
- `npx nx test angular-typechecker` -- the new `angular-cli-docs.spec.ts` AND the existing `storybook-docs.spec.ts` both green.
- Prettier `--check` clean on README.md + CHANGELOG.md.
- `git diff --name-only` shows no change to `packages/angular-typechecker/package.json` and no new tag.
</verification>

<success_criteria>
- ACD-01: README `## Angular CLI` section (all D-06 enumerated contents) + a curated end-user CHANGELOG 0.2.1 entry, with a content tripwire locking the claims and the Storybook caveat preserved. No release cut.
</success_criteria>

<output>
Create `.planning/phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-02-SUMMARY.md` when done.
</output>
