---
phase: 27-bin-shell-cross-platform-packaging
plan: 03
type: execute
wave: 3
depends_on: [27-01, 27-02]
files_modified:
  - .planning/phases/27-bin-shell-cross-platform-packaging/27-ADDITIVE-AUDIT.md
autonomous: true
requirements: [ADD-01]
must_haves:
  truths:
    - "The barrel-drift tripwire stays green -- the 5 barrel exports (runTypecheck, TypecheckInfrastructureError, CoreOptions, CoreResult, SkippedReference) are byte-unchanged."
    - "The public surface vs `angular-typechecker@0.2.1` (Nx executor id, runTypecheck/CoreResult/CoreOptions barrel, Angular CLI builder, generator/executor/builder schemas, executors/generators/builders/collection json) is UNCHANGED -- no narrow/remove/rename."
    - "The `bin` field + `src/cli/**` (bin.ts, bin-static.spec.ts) are net-new additive; the v0.3.0 breaking-change escape hatch stays untriggered."
    - "Covers CONTEXT decision D-12 (additive-only audit vs the angular-typechecker@0.2.1 tag: barrel-drift tripwire green + per-path public-surface git-diff + 27-ADDITIVE-AUDIT.md on the 24-ADDITIVE-AUDIT.md model)."
  artifacts:
    - path: ".planning/phases/27-bin-shell-cross-platform-packaging/27-ADDITIVE-AUDIT.md"
      provides: "The ADD-01 additive-only audit doc (verdict + guard cross-check + git-diff verdict per path + new-file additions + disposition)"
      contains: "ADDITIVE-ONLY"
  key_links:
    - from: ".planning/phases/27-bin-shell-cross-platform-packaging/27-ADDITIVE-AUDIT.md"
      to: "angular-typechecker@0.2.1 git tag"
      via: "git diff angular-typechecker@0.2.1..HEAD -- <public-surface paths>"
      pattern: "angular-typechecker@0\\.2\\.1"
---

<objective>
Prove the whole v0.2.2 milestone additive-only vs the concrete `angular-typechecker@0.2.1` tag and record the verdict in `27-ADDITIVE-AUDIT.md`.

Purpose: The additive-only charter (ADD-01) forbids any breaking change to the Nx executor id, the `runTypecheck`/`CoreResult`/`CoreOptions` public API, the Angular CLI builder, or the generator/executor/builder schemas. Phases 25-27 touched `core/` internals and added `src/cli/**` + the `bin` field -- none of which is public surface. This audit is the standing tripwire cross-check + a one-off git-diff, the same discipline as v0.2.1's 24-ADDITIVE-AUDIT.md, and it must run LAST (after all phase files are committed) so the git-diff is clean.

Output: `27-ADDITIVE-AUDIT.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-bin-shell-cross-platform-packaging/27-CONTEXT.md
@.planning/phases/27-bin-shell-cross-platform-packaging/27-RESEARCH.md
@.planning/phases/27-bin-shell-cross-platform-packaging/27-PATTERNS.md
@.planning/phases/27-bin-shell-cross-platform-packaging/27-01-SUMMARY.md
@.planning/phases/27-bin-shell-cross-platform-packaging/27-02-SUMMARY.md
@.planning/milestones/v0.2.1-phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-ADDITIVE-AUDIT.md

<interfaces>
<!-- The public-surface paths to diff + the drift tripwire wiring + the doc model. -->

Baseline tag: `angular-typechecker@0.2.1` (confirmed present via `git tag -l`). NOTE: baseline is 0.2.1, NOT 0.2.0 (v0.2.1 shipped after Phase 24).

Barrel-drift tripwire (leg a): `packages/angular-typechecker/src/index.drift.ts` + `packages/angular-typechecker/tsconfig.drift.json` lock the 5 exports (`runTypecheck`, `TypecheckInfrastructureError`, `CoreOptions`, `CoreResult`, `SkippedReference`); it runs via the `typecheck` target's `tsc --noEmit -p .../tsconfig.drift.json`. A removed/renamed export fails `tsc` LOUDLY.

Public-surface paths to git-diff (leg b), relative to repo root:
- packages/angular-typechecker/src/index.ts
- packages/angular-typechecker/src/executors/typecheck/schema.json
- packages/angular-typechecker/src/builders/typecheck/schema.json
- packages/angular-typechecker/src/generators/configuration/schema.json
- packages/angular-typechecker/src/generators/init/schema.json
- packages/angular-typechecker/executors.json
- packages/angular-typechecker/generators.json
- packages/angular-typechecker/builders.json
- packages/angular-typechecker/collection.json

Net-new confirmation (leg c): `git cat-file -e angular-typechecker@0.2.1:packages/angular-typechecker/src/cli/bin.ts` reports the path absent at the tag; the `bin` field did not exist in the 0.2.1 manifest.

Doc model (leg d): 24-ADDITIVE-AUDIT.md sections -- (1) Verdict paragraph, (2) Guard cross-check map table, (3) Git-diff verdict per audited path table, (4) New-file additions table, (5) ADD-01 disposition paragraph.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the additive-only audit and write 27-ADDITIVE-AUDIT.md (ADD-01)</name>
  <files>.planning/phases/27-bin-shell-cross-platform-packaging/27-ADDITIVE-AUDIT.md</files>
  <read_first>
    - .planning/milestones/v0.2.1-phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-ADDITIVE-AUDIT.md (the section structure + table shapes to mirror; retarget baseline 0.2.0 -> 0.2.1)
    - packages/angular-typechecker/src/index.drift.ts + packages/angular-typechecker/tsconfig.drift.json (the barrel tripwire wiring)
    - packages/angular-typechecker/package.json (confirm the `bin` field added in 27-01 is the only manifest addition and version is still 0.2.1)
    - .planning/phases/27-bin-shell-cross-platform-packaging/27-RESEARCH.md (the ADD-01 additive-only audit section, lines 513-567 -- the exact git-diff commands + expected verdict)
    - .planning/phases/27-bin-shell-cross-platform-packaging/27-PATTERNS.md (the 27-ADDITIVE-AUDIT.md section, lines 312-322)
  </read_first>
  <action>
    Leg (a) -- barrel-drift: run `nx typecheck angular-typechecker` and confirm the `src/index.drift.ts` tripwire is green (the 5 exports byte-unchanged; a rename/removal would fail `tsc`).

    Leg (b) -- git-diff the public surface: run `git diff angular-typechecker@0.2.1..HEAD -- <path>` for each public-surface path listed in the interfaces block (index.ts, both executor/builder schemas, both generator schemas, executors.json, generators.json, builders.json, collection.json). Record the verdict per path (expected: all UNCHANGED / empty diff -- Phases 25-27 touched only `core/` internals + added `src/cli/**` + the `bin` field; the Phase-25 executor logger swap is internal + observably identical).

    Leg (c) -- net-new confirmation: `git cat-file -e angular-typechecker@0.2.1:packages/angular-typechecker/src/cli/bin.ts` (and `src/cli/bin-static.spec.ts`) report the paths absent at the tag; confirm the `bin` field did not exist in the 0.2.1 manifest (net-new additive, not a change to an existing contract).

    Leg (d) -- write `27-ADDITIVE-AUDIT.md` (ASCII-only) modeled on `24-ADDITIVE-AUDIT.md`, baseline `angular-typechecker@0.2.1`, with: (1) a Verdict paragraph asserting ADDITIVE-ONLY HOLDS and v0.3.0 is NOT triggered; (2) a Guard cross-check map table (the barrel drift tripwire + the existing nx-surface / schema-parity regression specs, all present + green -- reference the counts from the phase's green `nx test` run); (3) a Git-diff verdict per audited path table (UNCHANGED / WIDEN-ONLY / net-new); (4) a New-file additions table listing `src/cli/bin.ts`, `src/cli/bin-static.spec.ts`, and the `package.json` `bin` field; (5) an ADD-01 disposition paragraph.

    If ANY audited public-surface path shows a NON-additive change (narrow/remove/rename), STOP and surface it as a blocker -- do not paper over it in the doc (that would be the v0.3.0 escape-hatch trigger).
  </action>
  <acceptance_criteria>
    - `nx typecheck angular-typechecker` is green (the barrel-drift tripwire passes -- 5 exports byte-unchanged).
    - Each public-surface `git diff angular-typechecker@0.2.1..HEAD -- <path>` is empty (UNCHANGED) on the audited paths; any non-empty diff is documented as WIDEN-ONLY or flagged as a blocker.
    - `git cat-file -e angular-typechecker@0.2.1:packages/angular-typechecker/src/cli/bin.ts` confirms `bin.ts` is net-new (absent at the tag).
    - `27-ADDITIVE-AUDIT.md` exists, is ASCII-only, states baseline `angular-typechecker@0.2.1`, contains the Verdict / Guard cross-check / Git-diff verdict / New-file additions / Disposition sections, and concludes ADDITIVE-ONLY HOLDS with v0.3.0 untriggered.
    - The doc's New-file additions table lists `src/cli/bin.ts`, `src/cli/bin-static.spec.ts`, and the `bin` field.
  </acceptance_criteria>
  <verify>
    <automated>nx typecheck angular-typechecker</automated>
  </verify>
  <done>The barrel-drift tripwire is green, every audited public-surface path is UNCHANGED vs the 0.2.1 tag, the bin field + src/cli/** are confirmed net-new, and 27-ADDITIVE-AUDIT.md records the ADDITIVE-ONLY verdict with v0.3.0 untriggered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| released 0.2.1 contract -> HEAD | The audit compares the public surface at the last shipped tag against the current tree to catch an accidental breaking change before release. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-08 | Tampering / Repudiation | An accidental narrow/remove/rename of the public API, executor id, builder, or a schema shipping as a "patch" (a silent breaking change) | mitigate | THIS plan is the mitigation: the standing barrel-drift tripwire (`nx typecheck`) + the per-path git-diff vs the 0.2.1 tag; any non-additive change is a hard blocker (v0.3.0 escape-hatch trigger), never papered over. |
| T-27-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this phase -- the audit is read-only git/typecheck; nothing to slopcheck. |

No HIGH-severity threat: the audit is a read-only comparison; it introduces no runtime surface, no network, no secrets.
</threat_model>

<verification>
- `nx typecheck angular-typechecker` green (barrel-drift tripwire).
- Full phase suite green before the doc's "all guards green" verdict is written: `nx run-many -t build test typecheck lint` + `nx e2e angular-typechecker-install-e2e`.
- Every audited public-surface `git diff angular-typechecker@0.2.1..HEAD` empty on the listed paths.
</verification>

<success_criteria>
- The milestone is proven additive-only vs `angular-typechecker@0.2.1`: barrel drift green, no public-surface break, `bin` + `src/cli/**` net-new, v0.3.0 untriggered (ADD-01).
- `27-ADDITIVE-AUDIT.md` records the verdict, modeled on 24-ADDITIVE-AUDIT.md.
</success_criteria>

<output>
Create `.planning/phases/27-bin-shell-cross-platform-packaging/27-03-SUMMARY.md` when done.
</output>
