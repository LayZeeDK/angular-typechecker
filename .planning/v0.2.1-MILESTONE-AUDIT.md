---
milestone: v0.2.1
milestone_name: Angular CLI workspace support
audited: 2026-07-15
status: passed
scores:
  requirements: 16/16
  phases: 4/4
  integration: 8-of-8-wired
  flows: 4/4 proven
nyquist:
  compliant_phases: [21, 22, 23, 24]
  partial_phases: []
  missing_phases: []
  overall: compliant
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt: []
resolved_debt:
  - phase: 22-configuration / 23-init
    items:
      - "RESOLVED (quick 260715-jho, 2026-07-15): the yarn-4 `ng generate angular-typechecker:configuration <project>` path was EMPIRICALLY EXERCISED on a real yarn 4 (berry) workspace against the Verdaccio-published local dist and did NOT crash with `chalk.blue is not a function`. The convertNx `configuration` (and, by the identical `convertNxGenerator` factory-load mechanism, `init`) schematics are SAFE on yarn 4 -- the RESEARCH's HIGH-confidence crash prediction did NOT reproduce in this environment. The `configuration` command was run directly (`corepack yarn add -D angular-typechecker` -> `corepack yarn ng generate angular-typechecker:configuration ng-cli-workspace`); it wired the app typecheck target with the two-element `[tsconfig.app.json, tsconfig.spec.json]` array, `ng run <app>:typecheck` was GREEN on the clean scaffold and caught a planted TS2322 (app) + TS2345 (spec), with no chalk.blue / ERR_REQUIRE_ESM / infrastructure error. Locked by the committed CI-authoritative cell `e2e/angular-typechecker-ng-cli-e2e/src/ng-generate-configuration-yarn.e2e.spec.ts` (ran, 1 passed, ~84s -- NOT skipped). Per YAGNI the convertNx configuration/init schematics were NOT refactored (Task 2 skipped): they are verified safe as-is. The README `## Angular CLI` `ng generate` docs need no yarn caveat -- verified accurate. Affected REQ-IDs: ACS-01, ACS-03 (secondarily ACS-04, ACV-02). [Superseded 260715-mia: the Angular CLI `init` SCHEMATIC was subsequently dropped for surface symmetry; only `configuration` remains a convertNx Angular CLI schematic. The init GENERATOR + `nx add` -> `<pkg>:init` / `nx g :init` path (ACS-03's no-stray-nx.json guarantee) are UNCHANGED.]"
---

# Milestone v0.2.1 Audit: Angular CLI workspace support

**Verdict: PASSED (no open items).** All 16 requirements are code-level satisfied (3-source
cross-referenced), all 4 phases are verified + secured + Nyquist-validated, and BOTH the primary
`ng add` auto-wire-all and the secondary `ng generate :configuration` -> `ng run :typecheck` flows
are e2e-proven on npm/yarn/pnpm. Additive-only vs `angular-typechecker@0.2.0` HOLDS -- the milestone
stays on the 0.2.x line (no v0.3.0 trigger). Package version intentionally still `0.2.0`; the 0.2.1
cut is the deferred human-gated Release-PR (AGENTS.md).

The former open item (a cross-phase integration WARNING: the secondary `ng generate
angular-typechecker:configuration` / `:init` commands are still `convertNxGenerator`-based and might
hit the yarn-4 `chalk.blue is not a function` crash that 24-06 removed from `ng-add`) was RESOLVED on
2026-07-15 by quick task `260715-jho`: the yarn-4 `ng generate :configuration` path was EMPIRICALLY
EXERCISED on a real yarn 4 workspace and did NOT crash. The RESEARCH's HIGH-confidence crash
prediction did not reproduce; the convertNx `configuration`/`init` schematics are verified SAFE on
yarn 4, so per YAGNI they were NOT refactored. Locked by the new CI-authoritative e2e cell (see
Resolved Debt below).

**This audit refreshes the 2026-07-11 `passed` verdict** to cover gap-closure plans 24-04 (nx direct
dependency), 24-05 (yarn/pnpm CLI e2e), and 24-06 (nx-free vanilla ng-add -> yarn first-run
auto-wire), plus the 2026-07-15 ACV-01 real-clone re-run (commit `895ee43`, both gates PASS).

## Phases (4/4)

| Phase | Verify | Secure | Nyquist |
|-------|--------|--------|---------|
| 21 angular-cli-builder + engine multi-tsConfig + GATE A' spike | passed 4/4 | verified | compliant |
| 22 configuration schematic (angular.json write-fork) | passed 11/11 | threats_open 0 | compliant |
| 23 init parity + first-party ng-add | passed 4/4 | verified | compliant |
| 24 real-OSS + scaffolded e2e + additive-only audit + docs | passed 6/6 (code) + ACV-01 human gate re-run PASS | SECURED 0-open | compliant |

Note: Phase 24's `24-VERIFICATION.md` frontmatter still reads `status: human_needed` -- that single
human item (re-run the ACV-01 real-clone tarball gate against post-24-06 HEAD) was RESOLVED on
2026-07-15 by quick task `260715-ig5` (commit `895ee43`): both gates PASS on the nx-free vanilla
ng-add (ngx-leaflet @818e9ae npm app+lib first-run auto-wire-all + clean per-project scoping;
realworld-angular @9e3528f pnpm-workspace name-collision wired the full `[tsconfig.app.json,
tsconfig.spec.json]` array, build leaf not dropped). The frontmatter was simply not re-flipped.

## Requirements (16/16 satisfied, 3-source cross-reference)

Sources: phase `VERIFICATION.md` status + `REQUIREMENTS.md` traceability (`Complete`) + `SUMMARY`
frontmatter `requirements_completed`. All three agree except ENG-01 and ACB-02, whose SUMMARY
frontmatter is empty BY DESIGN (the 21-01/21-02 SUMMARYs record them as "substantively delivered,
formally closed by the phase verifier after GATE A' GO"); both are marked SATISFIED with codebase
evidence in `21-VERIFICATION.md` and confirmed WIRED by the integration checker -- treated as
satisfied, not partial.

| REQ | Phase | Final | 3-source note |
|-----|-------|-------|---------------|
| ENG-01 | 21 | satisfied | passed + verifier-closed + WIRED (normalize-options -> handleMultiTsConfig union) |
| ACB-01 | 21 | satisfied | 21-03 frontmatter + WIRED (`convertNxExecutor` builder == executor) |
| ACB-02 | 21 | satisfied | passed + verifier-closed + GATE A' spike 011 GO + real `ng run` |
| ACB-03 | 21 | satisfied | 21-03 frontmatter + `executors ?? builders` regression guard |
| ACS-01 | 22 | satisfied | 22-01 frontmatter + write-fork leaf-array guard + yarn-4 `ng generate :configuration` e2e-verified-safe (260715-jho) |
| ACS-02 | 22 | satisfied | 22-01 frontmatter + Nx else-branch byte-unchanged |
| ACS-04 | 22 | satisfied | 22-02 frontmatter + `generators ?? schematics` guard |
| COV-01 | 22 | satisfied | 22-01 frontmatter + per-project no-bleed guard + ACV-01/ACV-02 |
| ACS-03 | 23 | satisfied | 23-01 frontmatter + init GENERATOR early-return fork seeds no stray nx.json (nx g / nx add -> <pkg>:init, UNCHANGED); Angular CLI init SCHEMATIC dropped for surface symmetry (260715-mia) |
| NGADD-01 | 23 (+24-06) | satisfied | 23-02/23-03 frontmatter + 24-06 vanilla nx-free schematic (first-run yarn auto-wire) |
| ACP-01 | 23 | satisfied | 23-02 frontmatter + optional-peer manifest contract |
| ACV-01 | 24 | satisfied | 24-03 frontmatter + real-clone gate re-run PASS post-24-06 (`895ee43`) |
| ACV-02 | 24 | satisfied | 24-03/24-05/24-06 frontmatter + committed CI e2e (npm/yarn flat+workspace/pnpm) |
| ACV-03 | 24 | satisfied | 24-01 frontmatter + builder-over-BuilderContext parity spec |
| ACP-02 | 24 | satisfied | 24-01/24-04/24-06 frontmatter + additive-only audit + `nx` direct dep + barrel drift tripwire |
| ACD-01 | 24 | satisfied | 24-02/24-06 frontmatter + README `## Angular CLI` + docs tripwire (yarn caveat retired) |

No unsatisfied requirements. No orphaned requirements (all 16 appear in a phase VERIFICATION.md).
The former `*` yarn-4 `ng generate` verification gap on ACS-01/ACS-03 is CLOSED (260715-jho): both
are now e2e-verified-safe on yarn 4, in addition to unit + npm/pnpm coverage.

## Cross-Phase Integration (8/8 seams WIRED)

gsd-integration-checker (fresh `nx build` + dist inspection + 48 integration-seam specs green)
traced every link end-to-end:

- Shared `src/core/angular-cli-wiring.ts` is a true single source of truth feeding BOTH the Nx
  `configuration` generator AND the vanilla `ng-add` schematic (no drift). WIRED.
- All 4 manifest surfaces resolve to shipped dist impls: `executors.json` / `builders.json` /
  `generators.json` / `collection.json`; `files` whitelist ships all four; no `*.spec.js` leaked. WIRED.
- Nx surface provably unchanged: `nx run :typecheck` (`executors ?? builders`), `nx g :configuration`
  (`generators ?? schematics`), `nx add` -> `<pkg>:init` (ng-add absent from `generators.json`). WIRED.
- ENG-01 `tsConfig: string | string[]` flows schema -> normalize -> core `handleMultiTsConfig` ->
  single `finalize` over the combined input set. WIRED.
- Builder `builder.js` has NO static `require('@angular/compiler-cli')`; the dynamic `import()` CJS->ESM
  bridge survives. WIRED.
- `nx` is a direct `^23.0.0` dependency so `@nx/devkit`'s `require('nx/src/devkit-exports')` resolves
  under yarn. WIRED.

**8th seam WIRED (former WARNING, RESOLVED 260715-jho):** the `ng generate :configuration`
flow still loads `@nx/devkit` via `convertNxGenerator` -- the exact path 24-06 removed from `ng-add`
-- but the yarn-4 `ng generate :configuration` path is now EMPIRICALLY EXERCISED on a real yarn 4
workspace and does NOT crash (`chalk.blue is not a function` did not reproduce). Verified safe on
npm/yarn/pnpm; see Resolved Debt. (Superseded 260715-mia: the Angular CLI `init` SCHEMATIC was
subsequently dropped for surface symmetry, so `configuration` is now the ONLY convertNx Angular CLI
schematic; the init GENERATOR + `nx add` -> `<pkg>:init` / `nx g :init` path are UNCHANGED.)

## E2E Flows (4/4 proven)

1. `ng add` auto-wire-all -> per-project `ng run :typecheck` scoping -- CI-authoritative ACV-02
   (`angular-typechecker-ng-cli-e2e`, npm + yarn flat + yarn workspace + pnpm collision). PROVEN.
2. ACV-01 real-clone final gate (ngx-leaflet npm app+lib; realworld-angular pnpm app): pack SHIPPED
   tarball -> `ng add` -> plant -> `ng run` -> assert. PROVEN (re-run `895ee43`, both PASS post-24-06).
3. Builder-over-`BuilderContext` == Nx executor `{ success }` parity (ACV-03). PROVEN.
4. `ng generate angular-typechecker:configuration <project>` -> `ng run :typecheck` on a real yarn 4
   workspace -- CI-authoritative cell `ng-generate-configuration-yarn.e2e.spec.ts` (260715-jho):
   `corepack yarn add -D angular-typechecker` -> `ng generate :configuration` wired the app target
   with the `[tsconfig.app.json, tsconfig.spec.json]` array, clean `ng run` GREEN, planted TS2322 +
   TS2345 caught, no chalk.blue / ERR_REQUIRE_ESM. PROVEN (ran, 1 passed, ~84s -- not skipped).

## Nyquist Compliance

All 4 phases `nyquist_compliant: true`, `wave_0_complete: true`. Phase 24's VALIDATION.md was
re-audited after 24-06 (0 gaps, 0 new tests generated -- 24-06 shipped `angular-cli-wiring.spec` 18
+ `ng-add.spec` 13, which already cover the surface).

## Resolved Debt / Deferred

**Resolved (was the one reviewable item):**

**Phase 22/23 -- yarn-4 `ng generate :configuration` / `:init` load path -- RESOLVED (verified safe,
quick 260715-jho, 2026-07-15).** The two schematics remain `convertNxGenerator`-based (their compiled
`schematic.js` does `require('@nx/devkit')`, which pulls nx's transitive `ora -> log-symbols -> chalk`
chain -- the mechanism that threw `chalk.blue is not a function` for `ng-add` before 24-06 made it
vanilla nx-free). The recommended arbiter was executed: a new CI-authoritative cell,
`e2e/angular-typechecker-ng-cli-e2e/src/ng-generate-configuration-yarn.e2e.spec.ts`, runs a REAL
`corepack yarn add -D angular-typechecker` -> `corepack yarn ng generate
angular-typechecker:configuration ng-cli-workspace` on a real yarn 4 (berry) workspace against the
Verdaccio-published local dist. It did NOT crash: the RESEARCH's HIGH-confidence crash prediction did
not reproduce in this environment. The cell RAN (1 passed, ~84s -- not skipped) and asserts the wired
app target (`[tsconfig.app.json, tsconfig.spec.json]`), a GREEN clean `ng run :typecheck`, and a
planted TS2322 (app) + TS2345 (spec) caught with no chalk.blue / ERR_REQUIRE_ESM / infrastructure
error. Per YAGNI the convertNx `configuration`/`init` schematics were NOT refactored -- they are
verified safe on yarn 4 as-is (`init` shares the identical `convertNxGenerator` factory-load path, so
the factory-load crash surface is settled for both). The README `## Angular CLI` `ng generate` docs
need no yarn caveat. Affected REQ-IDs: ACS-01, ACS-03.

[Superseded 260715-mia: the Angular CLI `init` SCHEMATIC was subsequently dropped for surface
symmetry; only `configuration` remains a convertNx Angular CLI schematic. The init GENERATOR + its
CLI early-return fork + the `nx add` -> `<pkg>:init` / `nx g :init` path (ACS-03's no-stray-nx.json
guarantee) are UNCHANGED.]

**Deferred:**

**Out-of-scope deferrals (tracked as Future Requirements in STATE.md, NOT debt from this milestone):**
Storybook SB-08, FsTree helpers, WALK-FUT-01/02, REP-RES-02b, OBS-01, standalone CLI, JSON/SARIF
reporters -- excluded by charter.

## Notable milestone findings

1. **The ACV-01 real-clone gate earned its keep (twice).** Its first run caught a charter-level
   silent-under-check defect (the CLI `configuration`/`ng-add` generator dropped the app build leaf
   on an Angular-CLI + pnpm-workspace + name-collision workspace) that the clean scaffolded ACV-02
   e2e structurally could not reproduce -- fixed by reading `angular.json` directly. Its 2026-07-15
   re-run confirmed the 24-06 nx-free rewrite still passes both clones.
2. **24-06 fixed yarn first-run auto-wire by removing `@nx/devkit` from the `ng-add` load path.** The
   refreshed integration check flagged that the SAME hardening was not applied to
   `configuration`/`init`; the follow-up (260715-jho) empirically settled it -- the yarn-4
   `ng generate :configuration` path does NOT crash on a real yarn 4 workspace, so the convertNx
   schematics were left in place (verified safe, no speculative refactor). The RESEARCH's
   HIGH-confidence crash prediction did not reproduce -- a useful reminder that the arbiter e2e, not
   the mechanism analysis, is the source of truth.
