---
phase: quick-260720-2m2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/test-util/src/lib/cli-e2e.ts
  - libs/test-util/src/lib/validate-sarif.ts
  - libs/test-util/src/index.ts
  - e2e/angular-typechecker-install-e2e/src/install-smoke.e2e.spec.ts
  - e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run.e2e.spec.ts
  - packages/angular-typechecker/README.md
  - CHANGELOG.md
  - packages/angular-typechecker/src/cli/parse-args.ts
  - packages/angular-typechecker/src/core/sarif-report.ts
autonomous: true
requirements: [F1, F2, F3, F4, F5, L2, L4]

must_haves:
  truths:
    - "F1: the stdout-purity check no longer false-fails when a machine payload legitimately carries an ATC-synthesized diagnostic message (90001/90002) whose text begins with the tool name -- structural JSON.parse/validateSarif is the sole purity signal."
    - "F2: the README and CHANGELOG scope the byte-pure-stdout claim to the standalone CLI and ng run; the Nx executor path is documented as framed by Nx task-runner chrome."
    - "F3: the README JSON example carries an illustrative version placeholder, not a real released version string."
    - "F4: the sarif-report.ts comments state results reference rules by ruleId only and no ruleIndex is emitted (valid SARIF)."
    - "F5: the README documents that --fail-fast affects the human report only; json and sarif always carry every diagnostic."
    - "L2: parse-args.ts imports the canonical ReportFormat type (type-only) and derives its runtime format allow-list from one source; the CLI graph stays nx-free."
    - "L4: the SARIF schema read + ajv.compile happen lazily on first validateSarif() call, not at module load; the public signature is unchanged."
  artifacts:
    - libs/test-util/src/lib/cli-e2e.ts
    - libs/test-util/src/lib/validate-sarif.ts
    - libs/test-util/src/index.ts
    - e2e/angular-typechecker-install-e2e/src/install-smoke.e2e.spec.ts
    - e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run.e2e.spec.ts
    - packages/angular-typechecker/README.md
    - CHANGELOG.md
    - packages/angular-typechecker/src/cli/parse-args.ts
    - packages/angular-typechecker/src/core/sarif-report.ts
  key_links:
    - "libs/test-util/src/lib/cli-e2e.ts defines+exports ADVISORY_NOTICE_PREFIX (line 253); libs/test-util/src/index.ts re-exports it (line 41); both e2e specs import it -- removing the export requires deleting all three import/re-export sites."
    - "packages/angular-typechecker/src/core/render-report.ts exports the canonical ReportFormat type (line 14); packages/angular-typechecker/src/executors/typecheck/normalize-options.ts already imports it type-only (line 6) -- parse-args.ts follows that precedent."
    - "machine-readable-docs.spec.ts + standalone-cli-docs.spec.ts are pure fs-read tripwires that lock README/CHANGELOG content -- Task 2 edits must keep every locked token present and the forbidden phrases absent."
---

<objective>
Transcribe 7 pre-triaged, verified-anchor PR #47 code-review findings into executable fixes, grouped into 3 atomic commits by concern.

Purpose: close the triaged thermos findings before the v0.2.3 milestone-close / Release-PR. Findings were already triaged (file:line anchors + fix intent locked) -- do NOT re-triage, re-investigate approach, or expand scope.
Output: 9 files edited across 3 commits (test-util robustness; docs accuracy; source-comment + type-sharing).

Scope guards (triaged out -- do NOT touch): core/run-typecheck.ts length (health.ignore FAL-10, review-blessed); the Windows cross-drive SARIF absolute-path residual (documented/accepted).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
@packages/angular-typechecker/src/core/render-report.ts
@packages/angular-typechecker/src/core/emit-advisory-notices.ts
@packages/angular-typechecker/src/machine-readable-docs.spec.ts
@packages/angular-typechecker/src/standalone-cli-docs.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: test-util robustness -- de-booby-trap the stdout-purity check (F1) and lazy-load the SARIF validator (L4)</name>
  <files>libs/test-util/src/lib/cli-e2e.ts, libs/test-util/src/index.ts, libs/test-util/src/lib/validate-sarif.ts, e2e/angular-typechecker-install-e2e/src/install-smoke.e2e.spec.ts, e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run.e2e.spec.ts</files>
  <action>
F1 (stdout-purity needle collision): the exported ADVISORY_NOTICE_PREFIX = 'angular-typechecker:' is an unsound purity needle. It collides on TWO axes: (a) ATC's OWN synthesized diagnostic messages carry the same prefix (the 90001 zero-root-names guard in core/run-typecheck.ts and the 90002 referenced-tsconfig-not-found synthesizeReferenceNotFoundDiagnostic in core/walk-references.ts land in diagnostics[].message / results[].message.text -- legitimate PAYLOAD data), and (b) the Nx executor id 'angular-typechecker:typecheck' contains the prefix as a substring. So a future not-found / empty-solution fixture under --format json|sarif would false-fail the check on legitimate payload data. Apply the finding's PREFERRED structural-purity fix: rely on the structural parse that ALREADY discriminates leaked prose from payload data, and remove the unsound needle at every site it routes through (root-cause fix, not per-site patch):
  - libs/test-util/src/lib/cli-e2e.ts: in assertMachineFormatParity, the standalone-CLI path parses `.stdout` ALONE via JSON.parse(clean.stdout)/JSON.parse(red.stdout) (json) and validateSarif(clean.stdout)/validateSarif(red.stdout) (sarif) -- a whole-stdout parse succeeding IS the structural proof that stdout is exactly one JSON/SARIF object with no extraneous non-payload prose (any leading `> nx run` echo or trailing advisory text breaks the parse). Remove the four `expect(...).not.toContain(ADVISORY_NOTICE_PREFIX)` assertions (the current lines 286, 292, 319, 325). Keep every structural parse/validate assertion.
  - libs/test-util/src/lib/cli-e2e.ts: delete the `export const ADVISORY_NOTICE_PREFIX = 'angular-typechecker:';` declaration (line 253) AND rewrite/remove its misleading head comment (lines ~246-253) -- the comment's claim that the trailing colon discriminates the notice from the tool name is FALSE (ATC synthesized diagnostic messages carry the prefix, and the executor id contains it). Replace it with a one-line note on assertMachineFormatParity that stdout purity is proven structurally by the whole-stream JSON.parse/validateSarif, so a payload legitimately carrying an ATC-synthesized `angular-typechecker:`-prefixed diagnostic message never false-fails.
  - libs/test-util/src/index.ts: remove the ADVISORY_NOTICE_PREFIX re-export (line 41).
  - e2e/angular-typechecker-install-e2e/src/install-smoke.e2e.spec.ts: remove the ADVISORY_NOTICE_PREFIX import (line 15) and the two `expect(jsonPayload|sarifPayload).not.toContain(ADVISORY_NOTICE_PREFIX)` assertions (lines 196, 203). The payload boundary stays proven by extractJsonPayload + JSON.parse / validateSarif on the extracted slice.
  - e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run.e2e.spec.ts: remove the ADVISORY_NOTICE_PREFIX import (line 8) and the two assertions (lines 186, 193). Same structural guarantee via extractJsonPayload + parse/validate.
Do NOT re-scope the needle onto the framing material: `angular-typechecker:` still collides with the executor id there, so a framing-scoped needle is equally unsound; the structural parse is the sound signal. Do NOT switch to an advisory-only sentinel: no single sentinel spans all five emit-advisory-notices.ts messages, so it would weaken the check.

L4 (eager schema compile): libs/test-util/src/lib/validate-sarif.ts does readFileSync + new Ajv + addFormats + ajv.compile as top-level module side effects (lines 26-32), and libs/test-util/src/index.ts re-exports validateSarif, so every spec importing anything from @workspace/test-util eagerly parses the 109 KB schema and compiles ajv. Wrap the schema read + Ajv construction + addFormats + compile in a memoized lazy getter (a module-level `let compiled: ValidateFunction | undefined;` plus a `getValidator()` that builds+caches on first call), and have validateSarif() call getValidator() instead of the module-load constant. Keep the public validateSarif signature and return shape byte-identical. Update the module-header sentence that says the schema "is compiled ONCE at module load" to state it is compiled lazily on first validateSarif() call and memoized.

Style: braces on all control-flow bodies; blank lines around control-flow and returns; ASCII only.
  </action>
  <verify>
    <automated>npx nx test test-util</automated>
  </verify>
  <done>Task 1 done when: `npx nx test test-util` passes; ADVISORY_NOTICE_PREFIX no longer exists anywhere (git grep returns zero matches); validate-sarif.ts performs no readFileSync/ajv.compile at module load (only inside the memoized getter); the two e2e specs and cli-e2e.ts compile with no dangling import of the removed export.</done>
</task>

<task type="auto">
  <name>Task 2: docs accuracy -- scope the byte-pure-stdout claim, neutralize the version example, document --fail-fast in machine formats (F2, F3, F5)</name>
  <files>packages/angular-typechecker/README.md, CHANGELOG.md</files>
  <action>
F2 (README overclaims byte-pure stdout for the Nx executor): README.md lines 613-618 (the paragraph right after the Nx executor `format` config example at 595-611) says the machine payload is written to stdout with "nothing else goes there" and that `... --format json > report.json` captures a clean payload. That is TRUE for the standalone CLI (npx angular-typechecker) and `ng run <project>:typecheck`, but FALSE for `nx typecheck`: Nx's task runner frames stdout with a leading `> nx run <proj>:typecheck ...` echo (plus a Node NO_COLOR/FORCE_COLOR warning) and a trailing ` NX  Successfully ran target ...` summary (the install-smoke e2e documents this and uses extractJsonPayload to recover the payload). Rewrite the paragraph to scope the clean-stdout guarantee to the standalone CLI and `ng run`, and add a short consumer-facing caveat that the Nx executor's stdout is framed by Nx's task-runner output, so for a byte-pure payload capture via the standalone CLI or `ng run`, or strip Nx's `> nx run` / ` NX ` framing lines. Keep the exit-code-parity sentence. End-user language only; no internal jargon.
  CHANGELOG.md lines 17-22 (the 0.2.3 Features "JSON output" bullet): the clause "It is written to standard output on its own ... so a script can capture the payload and parse it directly" blanket-promises pure stdout. Soften it so it does not promise byte-pure stdout for the Nx executor (e.g. note the standalone CLI / ng run write the payload alone on stdout while the Nx executor's stdout also carries Nx's task-runner output). End-user language only.

F3 (stale example version): README.md line 630 ships `"version": "0.2.2"` inside the 0.2.3 JSON example. The field is populated dynamically from the installed tool version, so it is illustrative. Replace the value with a neutral placeholder (`"x.y.z"`) so it does not go stale each release; if not already clear from context, note in the surrounding prose that the field carries the installed tool version.

F5 (undocumented --fail-fast vs machine formats): failFast is forwarded to the human formatter only; json/sarif always emit every diagnostic (render-report.ts + json-report.ts map all diagnostics) -- intentional (machine consumers want completeness) but undocumented. Add one sentence near the `--fail-fast` row (README.md line 528) stating that --fail-fast affects the human report only and that json/sarif always carry every diagnostic. Keep the `--fail-fast` flag token present in the table.

TRIPWIRE CONSTRAINTS (machine-readable-docs.spec.ts + standalone-cli-docs.spec.ts, pure fs reads -- keep green): the README must keep `## Machine-readable output`, its ToC anchor `[Machine-readable output](#machine-readable-output)`, `--format` with `human`/`json`/`sarif`, `upload-sarif`, `artifactLocation.uri`, `repository root`, and the flag tokens (`--fail-fast`, `--format`, etc.). The README normalized text must NOT contain `non-goal` (case-insensitive) or `lands in a later release`. The CHANGELOG `## 0.2.3` entry must NOT match internal-id / board-jargon patterns (DOC-01, CLI-0N, phase-N, Layout A-C, input-set, SB-N, G-gate, REP/FMT/VER/ADD/OBS/CLIX-N). ASCII only.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker</automated>
  </verify>
  <done>Task 2 done when: `npx nx test angular-typechecker` passes (machine-readable-docs.spec.ts + standalone-cli-docs.spec.ts green); README scopes byte-pure stdout to the standalone CLI / ng run and documents the Nx-executor framing caveat; README line ~630 no longer shows a real released version; a --fail-fast-vs-machine-formats sentence exists near the flag row; CHANGELOG 0.2.3 JSON bullet no longer blanket-promises pure stdout; no forbidden tripwire phrases present.</done>
</task>

<task type="auto">
  <name>Task 3: source-comment + type-sharing -- single-source the CLI format enum type (L2) and correct the SARIF ruleIndex comment (F4)</name>
  <files>packages/angular-typechecker/src/cli/parse-args.ts, packages/angular-typechecker/src/core/sarif-report.ts</files>
  <action>
L2 (inlined format union): parse-args.ts inlines the literal union `'human' | 'json' | 'sarif'` ~5 times -- ParsedOptions.format (line 36), validateFormat's return type Validated<...> (line 250), the runtime `['human', 'json', 'sarif']` validator array (line 251), the `as ...` cast (line 258), and buildParsedOptions' `format:` param type (line 279). Consolidate against the canonical `ReportFormat` exported from core/render-report.ts (line 14), following the existing type-only precedent in executors/typecheck/normalize-options.ts (line 6):
  - Add `import type { ReportFormat } from '../core/render-report';` to parse-args.ts. It MUST be a TYPE-ONLY import so nothing pulls render-report's runtime deps (compiler-cli / json-report / sarif-report) into the nx-free CLI graph.
  - Replace the four TYPE occurrences (lines 36, 250, 258, 279) with `ReportFormat`.
  - Derive the runtime allow-list from ONE source: declare a single module-level `const REPORT_FORMATS: readonly ReportFormat[] = ['human', 'json', 'sarif'];` (typed by ReportFormat so the compiler flags any drift between the runtime list and the canonical type) and use it in validateFormat's membership check (replacing the inline `['human', 'json', 'sarif'].includes(raw)` at line 251). The `raw ?? 'human'` default and the cast stay, now typed via ReportFormat.
Do NOT add a runtime import of render-report; the type is type-only and the runtime list is local. bin-static.spec.ts (the CLI require-graph guard) and `nx run-many -t typecheck` must stay clean.

F4 (comment-rot in sarif-report.ts): both comments claim node-sarif-builder owns/computes the ruleIndex linkage -- the module header (~lines 26-29: "owns the `ruleIndex` linkage ... results set only `ruleId`") and the result-loop comment (~lines 75-77: "The builder computes ruleIndex when a result's ruleId matches a rule id ... NEVER hand-compute ruleIndex"). Verified against node-sarif-builder@4.1.0: addResult() only pushes the result with `ruleId`; it never sets `ruleIndex`. A result with `ruleId` and no `ruleIndex` is valid SARIF (GitHub Code Scanning links `ruleId` to rules[].id). Correct BOTH comments to state that results reference their rule by `ruleId` only and no `ruleIndex` is emitted (valid SARIF); keep the "each of the 18 NG rules is added once" and "add the 18-NG8xxx catalog rules ONCE" facts. Comment-only -- no behavior change.

Style: braces on all control-flow bodies; blank lines around control-flow and returns; ASCII only. Commit scope: `refactor(core)` (the canonical type + the SARIF comment both live in core; the CLI consumes the core type).
  </action>
  <verify>
    <automated>npx nx run-many -t typecheck && npx nx test angular-typechecker && npx nx lint angular-typechecker</automated>
  </verify>
  <done>Task 3 done when: `npx nx run-many -t typecheck` is clean; `npx nx test angular-typechecker` passes (parse-args.spec.ts + bin-static.spec.ts green, CLI graph still nx-free); `npx nx lint angular-typechecker` passes at maxWarnings:0; parse-args.ts has exactly one type-only ReportFormat import + one REPORT_FORMATS runtime list and no remaining inline `'human' | 'json' | 'sarif'` unions; both sarif-report.ts comments state results reference rules by ruleId only with no ruleIndex emitted.</done>
</task>

</tasks>

<verification>
Primary gates (unit / typecheck / lint):
- `npx nx test test-util` (Task 1)
- `npx nx test angular-typechecker` (Tasks 2 + 3; docs tripwires + parse-args + bin-static)
- `npx nx run-many -t typecheck` (Task 3; CLI graph nx-free, no dangling type refs)
- `npx nx lint angular-typechecker` (Task 3; maxWarnings:0)

Heavy tier (orchestrator runs separately, cite only): `npx nx run-many -t e2e` exercises the two edited e2e specs against Verdaccio; the fixtures run clean / plant TS2322, so removing the ADVISORY_NOTICE_PREFIX assertions keeps them green.

Cross-cutting: `git grep -n "ADVISORY_NOTICE_PREFIX"` returns zero matches after Task 1; no forbidden tripwire phrases (`non-goal`, `lands in a later release`) in the README; ASCII only across all 9 files.
</verification>

<success_criteria>
All 7 findings addressed across 3 atomic commits with public-clean scopes:
- Commit 1 `test(test-util): ...` -- F1 (structural stdout purity; needle removed at all sites) + L4 (lazy memoized SARIF validator).
- Commit 2 `docs: ...` -- F2 (byte-pure stdout scoped to standalone CLI / ng run; Nx framing caveat) + F3 (illustrative version placeholder) + F5 (--fail-fast affects human report only).
- Commit 3 `refactor(core): ...` -- L2 (single-source ReportFormat type-only + one runtime list) + F4 (corrected SARIF ruleIndex comments).
Primary gates green; triaged-out items (run-typecheck.ts length, cross-drive SARIF residual) untouched.
</success_criteria>

<output>
Create `.planning/quick/260720-2m2-address-triaged-pr47-thermos-review-find/260720-2m2-SUMMARY.md` when done.
</output>
