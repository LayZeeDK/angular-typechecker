---
phase: 03-filtering-modes-output-quality-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/core/filter-diagnostics.ts
  - packages/angular-typechecker/src/core/filter-diagnostics.spec.ts
  - packages/angular-typechecker/src/core/run-typecheck.ts
  - packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts
  - fixtures/sibling-import/main-lib/tsconfig.lib.json
  - fixtures/sibling-import/main-lib/main.component.ts
  - fixtures/sibling-import/dependency-lib/dependency.ts
autonomous: true
requirements: [EXE-04, OUT-02, TEST-01]

must_haves:
  truths:
    - "By default, out-of-project + node_modules diagnostics are excluded from the reported set; opt-in includeDeps surfaces them"
    - "A diagnostic on a directory like node_modules-tools is NOT misclassified as a node_modules path (segment test, not substring)"
    - "File-less diagnostics (config errors, the zero-rootNames guard) are NEVER filtered"
    - "runTypecheck returns FILTERED + SORTED diagnostics plus a suppressedCount scalar; counts are computed POST-filter"
    - "includeDeps: true folds suppressed diagnostics back into the reported set and resets suppressedCount to 0"
  artifacts:
    - path: "packages/angular-typechecker/src/core/filter-diagnostics.ts"
      provides: "Pure filterDiagnostics(diagnostics, options) -> { kept, suppressedCount }"
      exports: ["filterDiagnostics", "FilterOptions", "FilterResult"]
      min_lines: 40
    - path: "packages/angular-typechecker/src/core/filter-diagnostics.spec.ts"
      provides: "Pure-function unit coverage of EXE-04 + OUT-02 with hand-built ts.Diagnostic[]"
    - path: "packages/angular-typechecker/src/core/run-typecheck.ts"
      provides: "CoreOptions.includeDeps/pathBase, CoreResult.suppressedCount, finalize wires filter + sort"
      contains: "suppressedCount"
  key_links:
    - from: "packages/angular-typechecker/src/core/run-typecheck.ts"
      to: "packages/angular-typechecker/src/core/filter-diagnostics.ts"
      via: "import { filterDiagnostics } and call inside finalize on the normal (post-performCompilation) path"
      pattern: "filterDiagnostics\\("
    - from: "packages/angular-typechecker/src/core/run-typecheck.ts"
      to: "ts.sortAndDeduplicateDiagnostics"
      via: "sort the kept diagnostics before counting"
      pattern: "sortAndDeduplicateDiagnostics"
---

<objective>
Deliver the project-boundary FILTER vertical slice and wire it into the `runTypecheck` seam: out-of-project + `node_modules` diagnostics are excluded by default (opt-in `includeDeps`), filtered on absolute realpath-normalized `fileName` via the program host's `getCanonicalFileName` semantics + `realpath`, with a `suppressedCount` scalar on `CoreResult`. This is the shared `CoreOptions`/`CoreResult` extension that the verdict and formatter slices compose with.

Purpose: EXE-04 (boundary filter) + OUT-02 (realpath-normalized filtering) + the D-02 `CoreResult` extension that completes the per-project, Nx-cacheable lean result. The unconditional all-getter gather (Phase-2 D-16) is NEVER touched; filtering is a SEPARATE post-`performCompilation` pass.
Output: New pure `filter-diagnostics.ts` + spec; extended `run-typecheck.ts` (`includeDeps`/`pathBase` on `CoreOptions`, `suppressedCount` on `CoreResult`, filter + `ts.sortAndDeduplicateDiagnostics` wired into `finalize`); extended integration spec; a new `fixtures/sibling-import/` fixture (a `main-lib` that imports a `dependency-lib` sibling via `paths`, landing OUTSIDE `basePath`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-filtering-modes-output-quality-gates/03-CONTEXT.md
@.planning/phases/03-filtering-modes-output-quality-gates/03-RESEARCH.md
@.planning/phases/03-filtering-modes-output-quality-gates/03-PATTERNS.md

<interfaces>
<!-- Current core contract the executor extends. Extracted from the live repo; use directly, no exploration needed. -->

From packages/angular-typechecker/src/core/run-typecheck.ts (Phase-2, the file this plan EDITS):
```typescript
export interface CoreOptions {
  tsConfigPath: string;            // ADD: includeDeps?: boolean (D-07), pathBase?: string (D-08; runTypecheck IGNORES it)
}
export interface CoreResult {
  tsConfigPath: string;
  rootNamesCount: number;          // 0 => the zero-rootNames guard fired (no Program; suppressedCount = 0)
  diagnostics: readonly ts.Diagnostic[];   // becomes FILTERED + SORTED (D-02/D-09)
  errorCount: number;              // category === Error, counted POST-filter
  warningCount: number;            // category === Warning, counted POST-filter
  durationMs: number;              // ADD: suppressedCount: number near here (D-02)
}
// finalize(ts, tsConfigPath, rootNamesCount, diagnostics, start): CoreResult  -- counts categories; the wiring point
// Two call sites: zero-rootNames guard path (line ~95, NO Program) and the normal path (line ~156, has result.program)
```

From packages/angular-typechecker/src/core/gather-diagnostics.ts (UNCHANGED -- do not edit):
```typescript
export function gatherAllDiagnostics(program: Program): readonly ts.Diagnostic[]  // the all-getter; filtering is SEPARATE
```

ts.Program host (verified this phase): result.program.getTsProgram() exposes useCaseSensitiveFileNames(): boolean.
parsed.options.basePath (from ng.readConfiguration) = the leaf tsconfig dir = the in-project baseline (D-05). NEVER parsed.options.rootDir (it is the workspace root "." in this --preset=apps repo and would mark every file in-project).
ts.sortAndDeduplicateDiagnostics<T extends ts.Diagnostic>(diagnostics: readonly T[]): SortedReadonlyArray<T>  (public, typescript.d.ts:8574).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 -- failing pure-function spec for filterDiagnostics (EXE-04, OUT-02)</name>
  <files>packages/angular-typechecker/src/core/filter-diagnostics.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/gather-diagnostics.spec.ts (THE canonical pure-function unit idiom: type-only `ts` import, named `{ describe, expect, it, vi }` from vitest, a `diagnostic(...)` factory casting a literal `as ts.Diagnostic`, assertions off the return)
    - .planning/phases/03-filtering-modes-output-quality-gates/03-RESEARCH.md "Code Examples" (the `diag(fileName, code)` factory ~447-456 and the exact filterDiagnostics assertions ~458-486)
    - .planning/phases/03-filtering-modes-output-quality-gates/03-PATTERNS.md "filter-diagnostics.spec.ts" section (factory + no-Program guidance)
  </read_first>
  <behavior>
    - keeps in-project, suppresses out-of-project + node_modules (D-05/D-06): given `basePath` `/ws/proj`, a diag under `/ws/proj/src/a.component.ts` is kept; `/ws/sibling-lib/src/b.ts` (out of project), `/ws/proj/node_modules/x/y.d.ts` (node_modules segment), and `/ws/proj-other/src/c.ts` (NOT a segment-bounded prefix) are suppressed -> kept length 2 (the in-project one + the file-less one), suppressedCount 3
    - file-less diagnostic (file === undefined) is ALWAYS kept (D-03): a `diag(undefined)` survives filtering
    - node_modules-tools is NOT misclassified (segment test, D-06): `/ws/proj/node_modules-tools/z.ts` is kept
    - realpath is applied BEFORE case-fold (OUT-02): inject a `realpath` stub that maps a symlink path to a real path under basePath; assert it is kept
    - case-insensitive FS fold (OUT-02): with `useCaseSensitiveFileNames: false`, `/WS/PROJ/src/A.ts` under basePath `/ws/proj` is kept
    - `.ngtypecheck.ts` shadow file under basePath is kept (Pitfall 1): `/ws/proj/src/a.component.ngtypecheck.ts` is kept
    - includeDeps: true folds everything back, suppressedCount 0 (D-07): an `/elsewhere/x.ts` diag is kept with suppressedCount 0
  </behavior>
  <action>Author `filter-diagnostics.spec.ts` mirroring the `gather-diagnostics.spec.ts` idiom exactly: type-only `import type ts from 'typescript'`, named `{ describe, expect, it }` from vitest, an `import { filterDiagnostics } from './filter-diagnostics'`, and a local `diag(fileName: string | undefined, code = 2322)` factory that builds a `ts.Diagnostic` literal with `category: 0`, `file: fileName === undefined ? undefined : ({ fileName } as ts.SourceFile)`. Cover every behavior above as separate `it(...)` cases citing the D-NN/req-id in the test name. Inject `realpath: (p) => p` (identity) for the default cases and a mapping stub for the symlink case; pass `useCaseSensitiveFileNames` explicitly per case. NO `@angular/compiler-cli` mock (D-13). The spec MUST fail initially because `filter-diagnostics.ts` does not yet exist -- that is the RED state. Follow CLAUDE.md JS/TS style: braces on every block, blank lines around control flow/return, `singleQuote`.</action>
  <verify>
    <automated>npx nx test angular-typechecker -- filter-diagnostics.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - filter-diagnostics.spec.ts exists and imports `filterDiagnostics` from './filter-diagnostics'
    - The spec run FAILS (RED) with a module-not-found / undefined-export error for filter-diagnostics (the implementation lands in Task 2)
    - No `@angular/compiler-cli` import appears anywhere in the spec (D-13)
    - Cases exist for: in-project kept, out-of-project suppressed, node_modules-segment suppressed, node_modules-tools kept, file-less kept, realpath-before-casefold, case-insensitive fold, .ngtypecheck shadow kept, includeDeps folds back
  </acceptance_criteria>
  <done>filter-diagnostics.spec.ts committed and red, encoding all EXE-04 + OUT-02 behaviors with hand-built diagnostics and zero compiler mock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement filterDiagnostics (realpath-first + case-fold + path-segment containment)</name>
  <files>packages/angular-typechecker/src/core/filter-diagnostics.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/filter-diagnostics.spec.ts (the contract from Task 1)
    - packages/angular-typechecker/src/core/gather-diagnostics.ts (the dep-free, `import type`-only header + single named `export function` + `for...of` accumulate shape to mirror)
    - packages/angular-typechecker/src/core/diagnostic-codes.ts (the "production-importable, intentionally DEPENDENCY-FREE" pure-module shape)
    - .planning/phases/03-filtering-modes-output-quality-gates/03-RESEARCH.md Pattern 1 (~189-242) + Pattern 2 (~245-272: createCanonicalizer, isNodeModulesPath, isUnderDir)
    - .planning/phases/03-filtering-modes-output-quality-gates/03-RESEARCH.md "Risks / Landmines" 2 (the executor.ts:135/146/151 naive filter this REPLACES -- do NOT copy)
  </read_first>
  <action>Create `filter-diagnostics.ts` as a pure, dependency-free `core/` module: `import type ts from 'typescript'` only (no runtime import; survives the D-11 ban with zero churn). Export `interface FilterOptions { basePath: string; includeDeps: boolean; useCaseSensitiveFileNames: boolean; realpath: (p: string) => string }`, `interface FilterResult { kept: ts.Diagnostic[]; suppressedCount: number }`, and `function filterDiagnostics(diagnostics: readonly ts.Diagnostic[], options: FilterOptions): FilterResult`. When `includeDeps` is true, return `{ kept: [...diagnostics], suppressedCount: 0 }` (D-07). Otherwise build a canonicalizer that applies `options.realpath(filePath)`, normalizes `\\` to `/`, then lower-cases ONLY when `useCaseSensitiveFileNames` is false (realpath FIRST, then case-fold -- D-06/Pitfall 3); memoize per-directory canonicalization in a `Map` cache, NOT a realpath syscall per diagnostic (Claude's discretion scale item). Classify each diagnostic: keep when `diagnostic.file === undefined` (D-03 -- never filter config/guard diagnostics); else suppress when `isNodeModulesPath(canonicalFile)` (path-SEGMENT test: `canonicalFile.split('/').includes('node_modules')`, so `node_modules-tools` does NOT match) OR not `isUnderDir(canonicalFile, canonicalBase)` (segment-bounded prefix: equal, or starts with `canonicalDir + '/'`); count suppressions in `suppressedCount`. Add a decision-cited doc comment naming D-05/D-06/D-07 and the WHY, matching the density of `gather-diagnostics.ts`. CLAUDE.md JS/TS style throughout.</action>
  <verify>
    <automated>npx nx test angular-typechecker -- filter-diagnostics.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - filter-diagnostics.ts exports `filterDiagnostics`, `FilterOptions`, `FilterResult`
    - The Task-1 spec now passes GREEN (all cases)
    - node_modules detection uses a path-SEGMENT test (split on '/' + includes), NOT `.includes('node_modules')` substring
    - containment uses a segment-bounded prefix (`dir + '/'`), NOT bare `startsWith`
    - canonicalization applies realpath BEFORE case-fold; no `toLowerCase()` before realpath
    - The module has zero runtime imports (type-only `import type ts` is the only import)
  </acceptance_criteria>
  <done>filterDiagnostics implemented as a pure dep-free module; the Wave 0 spec is green; the prior-art naive-filter landmines (substring node_modules, bare startsWith, toLowerCase-before-realpath) are all avoided.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the filter + sort into runTypecheck/finalize; extend CoreOptions/CoreResult; add the integration proof + sibling-import fixture</name>
  <files>packages/angular-typechecker/src/core/run-typecheck.ts, packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts, fixtures/sibling-import/main-lib/tsconfig.lib.json, fixtures/sibling-import/main-lib/main.component.ts, fixtures/sibling-import/dependency-lib/dependency.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/run-typecheck.ts (the live file: `CoreOptions` ~10, `CoreResult` ~21-33, the zero-rootNames guard path ~95-105, the normal path ~114-162 holding `result.program`, and `finalize` ~206-228)
    - packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts (the fixture-path resolution idiom ~19-24, the `NG()` helper ~17, `describe.each` ~26-29, assert-off-CoreResult idiom)
    - packages/angular-typechecker/src/core/config-resolution.integration.spec.ts (the `flattenDiagnosticMessageText` helper + fixture-path idiom ~32-56)
    - fixtures/gate-b-error/tsconfig.app.json + fixtures/gate-b-error/error.component.ts (the fixture tsconfig + standalone-component conventions to mirror; header comment that the file is OUT of the project graph; NEVER @ts-nocheck)
    - .planning/phases/03-filtering-modes-output-quality-gates/03-PATTERNS.md "run-typecheck.ts (EDIT IN PLACE)" + "run-typecheck.integration.spec.ts (EXTEND)" + "fixtures/<sibling-import>" sections
    - .planning/phases/03-filtering-modes-output-quality-gates/03-RESEARCH.md "finalize extension" (~378-379) + Pitfall 5 (TS6059 insurance ~425-428)
  </read_first>
  <action>Extend `CoreOptions` with `includeDeps?: boolean` (D-07, default false) and `pathBase?: string` (D-08; `runTypecheck` IGNORES it -- it exists for adapter/API discoverability, the formatter consumes it in plan 03), each with a decision-cited comment. Extend `CoreResult` with `suppressedCount: number` (D-02) near `durationMs`, keeping the field-comment style. Wire the filter into the NORMAL path only: after the D-06 infrastructure-failure check, pass the live `result.program.getTsProgram()` host (for `useCaseSensitiveFileNames()`), `parsed.options.basePath` (the D-05 baseline -- NEVER `parsed.options.rootDir`), `options.includeDeps ?? false`, and `ts.sys.realpath` into `filterDiagnostics`, producing `{ kept, suppressedCount }`. Sort `kept` with `ts.sortAndDeduplicateDiagnostics` BEFORE counting (D-09). Thread `suppressedCount` and the sorted+filtered diagnostics into `finalize` so `errorCount`/`warningCount` are counted POST-filter on the sorted set. The zero-rootNames guard path has NO `Program`: there `suppressedCount = 0` and its single file-less diagnostic is never filtered -- make the `program`/filter inputs optional (or keep a second guard-path shape) so the guard stays clean; config-error diagnostics prepended via the existing `[...configDiagnostics]` are file-less and survive. Create `fixtures/sibling-import/`: a `dependency-lib/dependency.ts` (a tiny exported symbol with a deliberate type error, e.g. a value whose declared type is violated), a `main-lib/main.component.ts` (a standalone component that imports the dependency-lib symbol via a `paths` alias so its diagnostic lands in the SIBLING dir outside `basePath`, plus optionally an in-project error so an in-project diagnostic is also present), and a `main-lib/tsconfig.lib.json` mirroring `fixtures/gate-b-error/tsconfig.app.json` (extends `../../../tsconfig.base.json`, `noEmit: true`, `strictTemplates: true`, with a `paths` mapping to the dependency-lib) listing only the `main-lib` files. Keep the fixture under `fixtures/` (a DISCOVERED dir -- NEVER `tmp/`/`dist/`/`cache/`, Landmine 5); header comment that it is out of the plugin build (`include: ["src/**/*.ts"]`); NEVER `@ts-nocheck`. Extend `run-typecheck.integration.spec.ts` with: (a) a default-filter case asserting the dependency-lib diagnostic is suppressed (`suppressedCount >= 1`) while an in-project diagnostic is kept; (b) an `includeDeps: true` case asserting the sibling diagnostic folds back (`suppressedCount: 0`, the sibling code now appears in `diagnostics`); (c) a TS6059-does-NOT-appear assertion on the sibling-import fixture (Pitfall 5 insurance -- the Phase-2 no-emit override neutralizes the emit-layout diagnostic). Use the established `NG()` helper + fixture-path resolution. CLAUDE.md JS/TS style.</action>
  <verify>
    <automated>npx nx test angular-typechecker -- run-typecheck.integration.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - run-typecheck.ts: `CoreOptions` has `includeDeps?: boolean` and `pathBase?: string`; `CoreResult` has `suppressedCount: number`
    - run-typecheck.ts imports and calls `filterDiagnostics(` on the normal path, passing `parsed.options.basePath` (NOT `parsed.options.rootDir`) and `result.program.getTsProgram()` host case-sensitivity
    - run-typecheck.ts calls `ts.sortAndDeduplicateDiagnostics` on the kept set before counting
    - The zero-rootNames guard path returns `suppressedCount: 0` and never filters its file-less diagnostic
    - fixtures/sibling-import/ exists under fixtures/ (not an Nx-excluded dir) with main-lib + dependency-lib and a `paths`-aliased cross-import; no `@ts-nocheck`
    - The extended integration spec passes: default suppresses the sibling diagnostic with `suppressedCount >= 1`; `includeDeps: true` yields `suppressedCount: 0` and surfaces the sibling code; TS6059 does NOT appear
    - `npx nx test angular-typechecker` (full suite) is green
  </acceptance_criteria>
  <done>The filter is wired into the only place holding the live Program; CoreOptions/CoreResult carry the D-07/D-08/D-02 extensions; the real-compiler integration proves default-suppress, includeDeps-folds-back, and no-TS6059 against a committed sibling-import fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer tsconfig + source -> runTypecheck | The developer's OWN tsconfig and source files. No untrusted external input crosses here; this is a developer-invoked static checker over their own code. |
| diagnostic fileName -> boundary classification | The compiler-produced `file.fileName` (absolute, forward-slash) is classified against `basePath`. A misclassification is a correctness concern (a type-checker that lies), not an external attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | filterDiagnostics path classification | mitigate | Path-traversal misclassification (a crafted `fileName` shown as in-project, or an in-project file wrongly suppressed) is prevented by realpath-first + path-SEGMENT containment + node_modules-by-segment (D-06). This is the design center, verified by the unit cases (node_modules-tools kept, segment-bounded prefix). Correctness, not a security boundary. |
| T-03-02 | Tampering | suppressedCount / POST-filter counts | accept | Counts are derived purely from compiler-produced diagnostics; no external mutation surface. The invariant `errorCount + warningCount <= diagnostics.length` holds on the filtered set. Low-value, no PII, developer-local. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan (RESEARCH "Package Legitimacy Audit": Phase 3 installs ZERO new packages; all APIs from already-locked typescript@6.0.3 / @angular/compiler-cli@22.0.4 peers). No install task, no legitimacy checkpoint needed. |

No `high`-severity threats apply (RESEARCH Security Domain: internal post-processing of the developer's own type-check output; no auth/session/access-control, no network, no filesystem-write capability).
</threat_model>

<verification>
- `npx nx test angular-typechecker -- filter-diagnostics.spec.ts` green (EXE-04 + OUT-02 pure tier)
- `npx nx test angular-typechecker -- run-typecheck.integration.spec.ts` green (D-02 default-suppress + includeDeps-folds-back + no-TS6059)
- `npx nx test angular-typechecker` full suite green (no Phase-2 regression)
- `npx nx build angular-typechecker` green (the built compiler-loader.js still retains literal `import(` -- GATE A invariant; this plan does not touch compiler-loader.ts but the build must stay clean)
</verification>

<success_criteria>
- EXE-04: out-of-project + node_modules diagnostics excluded by default; opt-in `includeDeps` surfaces them (proven unit + integration)
- OUT-02: diagnostics filtered on absolute realpath-normalized `fileName` via case-fold + path-segment containment (proven unit, with pnpm/case backstop deferred to Phase 6)
- TEST-01 (partial): the `filter-diagnostics` unit tier exists and covers project-boundary filtering with hand-built diagnostics
- D-02/D-05/D-06/D-07 traceable: `suppressedCount` scalar, `basePath` baseline (not `rootDir`), realpath+segment filter, `includeDeps` fold-back
</success_criteria>

<output>
Create `.planning/phases/03-filtering-modes-output-quality-gates/03-01-SUMMARY.md` when done
</output>
