---
phase: 28-shipped-tarball-e2e-real-clone-uat
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - e2e/angular-typechecker-cli-e2e/src/cli-exit-codes-yarn.e2e.spec.ts
  - e2e/angular-typechecker-cli-e2e/src/cli-exit-codes-pnpm.e2e.spec.ts
  - e2e/angular-typechecker-cli-e2e/src/nx-free-runtime.e2e.spec.ts
autonomous: true
requirements: [VER-04]

must_haves:
  truths:
    - "Installing the published package with yarn 4 (flat AND workspace layouts, corepack) then invoking the .bin shim returns literal 0/1/2 for both bin names (D-03)."
    - "Installing the published package with pnpm (symlinked layout, strictDepBuilds:false) then invoking the .bin shim returns literal 0/1/2 for both bin names (D-03)."
    - "The installed bin's output never matches /ERR_REQUIRE_ESM/ -- the await import('@angular/compiler-cli') bridge survived install un-downleveled (D-07, SC-3)."
    - "A runtime module-graph probe confirms the INSTALLED bin's require cache never reaches @nx/* or nx/ at run time, complementing Phase 27's static dist-graph walk (D-07, SC-3)."
  artifacts:
    - path: "e2e/angular-typechecker-cli-e2e/src/cli-exit-codes-yarn.e2e.spec.ts"
      provides: "yarn flat + workspace install-by-name + shim 0/1/2, corepack availability skipIf"
      contains: "runShim"
    - path: "e2e/angular-typechecker-cli-e2e/src/cli-exit-codes-pnpm.e2e.spec.ts"
      provides: "pnpm install-by-name + shim 0/1/2, strictDepBuilds:false, pnpm availability skipIf"
      contains: "runShim"
    - path: "e2e/angular-typechecker-cli-e2e/src/nx-free-runtime.e2e.spec.ts"
      provides: "runtime require-cache probe (no @nx/*/nx/) + ERR_REQUIRE_ESM negative assertion"
      contains: "ERR_REQUIRE_ESM"
  key_links:
    - from: "e2e/angular-typechecker-cli-e2e/src/cli-exit-codes-yarn.e2e.spec.ts"
      to: "node_modules/.bin/{angular-typechecker,atc}"
      via: "runShim over the yarn node-modules-linker shim"
      pattern: "runShim"
    - from: "e2e/angular-typechecker-cli-e2e/src/nx-free-runtime.e2e.spec.ts"
      to: "node_modules/angular-typechecker/src/cli/bin.js"
      via: "node -r hook <installed bin.js> require-cache dump"
      pattern: "require\\.cache"
---

<objective>
Complete the VER-04 PM matrix: yarn (flat + workspace) and pnpm install-by-name + shim `0`/`1`/`2`
cells, plus the D-07 runtime nx-free / ESM-bridge probe on the INSTALLED bin (SC-3).

Purpose: Extends the npm baseline (Plan 01) across the remaining package managers so the shim-path +
literal-exit-code coverage is proven for every PM (D-03), and adds the RUNTIME half Phase 27 D-10
deferred -- the installed bin never emits `ERR_REQUIRE_ESM` and its require cache never reaches
`@nx/*`/`nx/` at run time (SC-3). Split from Plan 01 purely for the 2-3-task context budget.
Output: three new spec files in the existing `angular-typechecker-cli-e2e` project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-PATTERNS.md
@.planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-RESEARCH.md
@.planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-01-SUMMARY.md

<!-- Analog sources to MIRROR. -->
@e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-yarn.e2e.spec.ts
@e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-pnpm.e2e.spec.ts
@e2e/angular-typechecker-matrix-e2e/src/pnpm-symlink.e2e.spec.ts
@packages/angular-typechecker/src/cli/bin-static.spec.ts
@e2e/angular-typechecker-cli-e2e/src/cli-exit-codes.e2e.spec.ts

<interfaces>
runShim(consumerDir, binName, args, env) from @workspace/test-util (Plan 01) returns
`{ code: number; stdout: string }` reading the literal spawnSync status. Installed bin path:
`node_modules/angular-typechecker/src/cli/bin.js`. NX specifier pattern (from bin-static.spec.ts):
`/^(@nx\/|nx\/|nx$)/`; a require-cache key matching `/node_modules[\\/](@nx[\\/]|nx[\\/])/` means nx
was loaded at run time. Flag contract: `-c`/`--tsConfig` (required); `-p` is an unknown flag -> exit 2.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: yarn (flat + workspace) exit-code spec</name>
  <read_first>
    - e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-yarn.e2e.spec.ts (YARN_VERSION const, commandSucceeds corepack availability guard :66/:91-94, it.skipIf(!corepackAvailable).each(['flat','workspace']) :160, setupYarnWorkspace :98-157 with the full .yarnrc.yml)
    - e2e/angular-typechecker-cli-e2e/src/cli-exit-codes.e2e.spec.ts (the npm baseline runShim assertion shape to reuse)
    - .planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-PATTERNS.md (cli-exit-codes-yarn assignment + delta)
  </read_first>
  <behavior>
    - Both layouts (flat + workspace): clean -> 0; planted TSxxxx -> 1 (code in stdout); nonexistent tsconfig -> 2; unknown flag AND missing -c -> 2; for both bin names.
    - it.skipIf(!corepackAvailable) so the cell skips cleanly where corepack yarn 4.17.0 is unavailable.
    - Every RED run: stdout not.toMatch(/ERR_REQUIRE_ESM/) and not.toContain('infrastructure error').
  </behavior>
  <action>
    Create `src/cli-exit-codes-yarn.e2e.spec.ts` by copying the ng-add-ng-run-yarn mechanics VERBATIM:
    `const YARN_VERSION = '4.17.0'`; the `commandSucceeds('corepack yarn@' + YARN_VERSION + ' --version', ...)`
    availability guard; `it.skipIf(!corepackAvailable).each(['flat','workspace'] as const)`; the
    `setupYarnWorkspace(...)` helper (drop the npm lockfile, set `packageManager: yarn@4.17.0`, add
    `workspaces:['projects/*']` for the workspace layout, write the full load-bearing `.yarnrc.yml`:
    nodeLinker: node-modules, npmRegistryServer, npmAuthToken, unsafeHttpWhitelist:[127.0.0.1],
    npmMinimalAgeGate:0, enableImmutableInstalls:false, per-fixture cacheFolder, enableGlobalCache:false,
    enableMirror:false) + writeVerdaccioNpmrc. Install: `sh('corepack enable', ...)`, `sh('corepack yarn install', ...)`,
    then install the package BY NAME via `corepack yarn add -D angular-typechecker`. DELTA: replace the ng-adapter
    surface (ng add / ng run / typecheckTarget / assertPerProjectScoping) with runShim exit-code assertions
    against the cli-consumer fixture; tmp prefix `atc-cli-yarn-${layout}-`. Re-assert the 127.0.0.1 safety echo.
  </action>
  <acceptance_criteria>
    - `nx run angular-typechecker-cli-e2e:e2e` runs the yarn spec: both flat + workspace layouts return 0 clean, 1 planted (stdout contains the planted TSxxxx), 2 for nonexistent-tsconfig AND unknown-flag AND missing-`-c`, both bin names -- OR skips cleanly if corepack yarn is unavailable.
    - The shim is invoked through `node_modules/.bin` via runShim (source grep shows no `node .../bin.js` and no `npx atc`).
    - Every RED assertion also asserts `not.toMatch(/ERR_REQUIRE_ESM/)`.
  </acceptance_criteria>
  <verify>
    <automated>npx nx run angular-typechecker-cli-e2e:e2e</automated>
  </verify>
  <done>yarn flat + workspace installs resolve the .bin shim and return literal 0/1/2 for both bins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pnpm exit-code spec</name>
  <read_first>
    - e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-pnpm.e2e.spec.ts (commandSucceeds('pnpm --version') guard :97-105, stripAllNpmConfig env :90, drop the npm lockfile, the strictDepBuilds:false comment ~:120-135)
    - e2e/angular-typechecker-matrix-e2e/src/pnpm-symlink.e2e.spec.ts (pure pnpm-install-and-resolve reference for the symlinked .bin layout)
    - e2e/angular-typechecker-cli-e2e/src/cli-exit-codes.e2e.spec.ts (runShim assertion shape)
    - .planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-PATTERNS.md (cli-exit-codes-pnpm assignment + delta)
  </read_first>
  <behavior>
    - clean -> 0; planted TSxxxx -> 1 (code in stdout); nonexistent tsconfig -> 2; unknown flag AND missing -c -> 2; for both bin names.
    - it.skipIf(!pnpmAvailable) so the cell skips cleanly if pnpm is unavailable.
    - pnpm 11 build-script gate does NOT ERR_PNPM_IGNORED_BUILDS-fail the install (strictDepBuilds:false posture).
  </behavior>
  <action>
    Create `src/cli-exit-codes-pnpm.e2e.spec.ts` copying the ng-add-ng-run-pnpm provisioning: the
    `commandSucceeds('pnpm --version', ...)` guard + `it.skipIf(!pnpmAvailable)`, `stripAllNpmConfig:true`,
    drop the npm lockfile, and carry the `strictDepBuilds:false` posture (write it into the consumer .npmrc)
    so pnpm 11's build-script gate does not ERR_PNPM_IGNORED_BUILDS-fail the install (same class as the
    nx-add-fails-on-pnpm-workspaces memory). Install the package BY NAME via `pnpm add -D angular-typechecker`
    (pnpm reads the fixture .npmrc from writeVerdaccioNpmrc). DELTA: replace ng add / scoping with runShim
    exit-code cells; the pnpm symlinked layout still creates `node_modules/.bin/` shims. Re-assert the
    127.0.0.1 safety echo and the RED /ERR_REQUIRE_ESM/ negative assertion.
  </action>
  <acceptance_criteria>
    - `nx run angular-typechecker-cli-e2e:e2e` runs the pnpm spec: 0 clean, 1 planted (stdout contains the planted TSxxxx), 2 for nonexistent-tsconfig AND unknown-flag AND missing-`-c`, both bin names -- OR skips cleanly if pnpm is unavailable.
    - The install does not fail with ERR_PNPM_IGNORED_BUILDS (strictDepBuilds:false posture present in the consumer .npmrc).
    - The shim is invoked through `node_modules/.bin` via runShim (source grep shows no `npx atc`).
  </acceptance_criteria>
  <verify>
    <automated>npx nx run angular-typechecker-cli-e2e:e2e</automated>
  </verify>
  <done>pnpm install resolves the symlinked .bin shim and returns literal 0/1/2 for both bins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: runtime nx-free / ESM-bridge probe on the installed bin (SC-3, D-07)</name>
  <read_first>
    - packages/angular-typechecker/src/cli/bin-static.spec.ts (the STATIC dist-graph walk this is the runtime counterpart to :14-17; the NX_SPECIFIER pattern concept)
    - e2e/angular-typechecker-cli-e2e/src/cli-exit-codes.e2e.spec.ts (the npm install-by-name provisioning to reuse for a real installed bin)
    - .planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-RESEARCH.md (the runtime require-cache probe code example + Open Question 2)
    - .planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-PATTERNS.md (nx-free-runtime assignment)
  </read_first>
  <behavior>
    - After a real install + a real type-check run of the INSTALLED bin, require.cache has ZERO keys matching /node_modules[\\/](@nx[\\/]|nx[\\/])/ (exit-hook dump == []).
    - Any real shim run's captured output does NOT match /ERR_REQUIRE_ESM/ (the await import ESM bridge survived install un-downleveled).
  </behavior>
  <action>
    Create `src/nx-free-runtime.e2e.spec.ts`. Provision an npm install-by-name consumer (reuse the Plan 01
    baseline shape). Write a tiny `dump-require-cache.cjs` hook into the consumer tmp dir whose
    `process.on('exit')` filters `Object.keys(require.cache)` by `/node_modules[\\/](@nx[\\/]|nx[\\/])/` and
    writes the matches (JSON) to `process.env.ATC_CACHE_OUT`. Run the INSTALLED bin directly with the hook
    preloaded: `execSync('node -r "<hook>" "<node_modules/angular-typechecker/src/cli/bin.js>" -c tsconfig.json', { maxBuffer: 20MB })`
    inside a try/catch (a planted-error run still exits non-zero; the exit hook still fires). Assert the dumped
    array `toEqual([])` -- the runtime require graph never reaches `@nx/*` or `nx/` (D-07). Add the ESM-bridge
    half: assert a real run's captured output `not.toMatch(/ERR_REQUIRE_ESM/)`. Per Open Question 2, the
    require-cache probe runs via `node -r hook bin.js` directly (the shim path is proven separately by the
    exit-code specs).
  </action>
  <acceptance_criteria>
    - `nx run angular-typechecker-cli-e2e:e2e` runs the probe: the dumped require-cache nx array `toEqual([])`.
    - The captured output of a real installed-bin run asserts `not.toMatch(/ERR_REQUIRE_ESM/)`.
    - `git grep -n "bin.js" e2e/angular-typechecker-cli-e2e/src/nx-free-runtime.e2e.spec.ts` shows the installed path `node_modules/angular-typechecker/src/cli/bin.js`.
  </acceptance_criteria>
  <verify>
    <automated>npx nx run angular-typechecker-cli-e2e:e2e</automated>
  </verify>
  <done>The installed bin loads no @nx/*/nx/ modules at run time and never emits ERR_REQUIRE_ESM -- the runtime complement to Phase 27's static guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process -> installed CLI (OS process boundary) | Each PM's `.bin` shim is spawned; the runtime module graph is inspected. |
| consumer fixture install -> npm registry | yarn/pnpm nested installs must resolve ONLY from Verdaccio. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03 | Malicious code / Supply chain | yarn/pnpm/probe specs | mitigate | Invoke the installed `.bin/{angular-typechecker,atc}` shim BY PATH via runShim (and `node -r hook bin.js` for the probe); NEVER `npx atc`. Source grep in acceptance criteria enforces no `npx atc`. |
| T-28-02 | Elevation of Privilege / Repudiation | Verdaccio publish + install-by-name | mitigate | Reuse the createVerdaccioGlobalSetup 127.0.0.1 SAFETY refuse-gate (Plan 01); each spec re-asserts `verdaccioUrl.startsWith('http://127.0.0.1:')` and installs BY NAME from Verdaccio (never a foreign registry). |
| T-28-06 | Information disclosure / Tampering | pnpm build-script execution | mitigate | `strictDepBuilds:false` skips ALL dependency build scripts during the pnpm install (no arbitrary postinstall runs), mirroring the more-restrictive posture proven in Phase 24's pnpm CLI e2e. |
</threat_model>

<verification>
- `nx run angular-typechecker-cli-e2e:e2e` green across all specs (npm from Plan 01 + yarn flat/workspace + pnpm + the runtime probe), or the yarn/pnpm cells skip cleanly where the PM is unavailable.
- The runtime require-cache probe returns an empty nx set; no `ERR_REQUIRE_ESM` in any captured output.
</verification>

<success_criteria>
The VER-04 PM matrix is complete: yarn (flat + workspace) and pnpm both install the published package
by name and return literal `0`/`1`/`2` through the real `.bin` shim for both bin names, and the D-07
runtime probe proves the installed bin loads no `@nx/*`/`nx/` at run time and never emits
`ERR_REQUIRE_ESM` (SC-3).
</success_criteria>

<output>
Create `.planning/phases/28-shipped-tarball-e2e-real-clone-uat/28-02-SUMMARY.md` when done.
</output>
