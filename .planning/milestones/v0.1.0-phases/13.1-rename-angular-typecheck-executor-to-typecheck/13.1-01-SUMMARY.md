---
phase: 13.1-rename-angular-typecheck-executor-to-typecheck
plan: 01
subsystem: infra
tags: [nx, nx-executor, rename, breaking-change, angular, executors-json, target-defaults]

# Dependency graph
requires:
  - phase: 13-engine-solution-tsconfig-reference-walking
    provides: the shipped `performCompilation` walk engine + the WALK-02 cacheable targetDefaults contract that this rename re-keys byte-for-byte
provides:
  - The published Nx executor now resolves under the id `angular-typechecker:typecheck` (key `typecheck`, impl/schema under `src/executors/typecheck/`)
  - Both `nx.json` targetDefaults id forms re-keyed to `:typecheck` with the WALK-02 value preserved byte-for-byte
  - All dev-repo consumers, e2e fixtures/specs, and README recipes reference the new id + target names (`typecheck`, `typecheck-spec`)
  - A breaking `feat!` commit that will drive the milestone 0.0.3 -> 0.1.0 minor bump (cut later at the Release PR)
affects: [phase-14-typecheck-configuration-generator, phase-15-generator-e2e-ci-self-audit-guard, milestone-release-pr]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Identity rename discipline: anchor every edit on an executor-only token; NEVER a bare `angular-typecheck` -> `typecheck` sweep (prefix-corrupts the package name)."
    - "Completeness proof for a rename: dual negative grep gate (two token families) + corruption canary + non-decreasing package-name count, run repo-wide with exclusions."

key-files:
  created: []
  modified:
    - packages/angular-typechecker/executors.json
    - packages/angular-typechecker/src/executors/typecheck/ (git mv from angular-typecheck/, 8 files)
    - packages/angular-typechecker/src/core/run-typecheck.ts
    - packages/angular-typechecker/src/core/nx-target-defaults.spec.ts
    - nx.json
    - libs/typecheck-consumer/project.json
    - libs/typecheck-walk-consumer/project.json
    - README.md
    - packages/angular-typechecker/README.md
    - CLAUDE.md
    - e2e/angular-typechecker-{install,matrix,cache}-e2e/** (fixtures + consumer nx.json + specs)

key-decisions:
  - "D-01 hard rename, no back-compat alias/shim (drives the breaking bump)."
  - "D-02 full identity rename: git mv the impl dir (history preserved), schema $id + TS interface -> TypecheckExecutorOptions; schema title (descriptive prose) kept."
  - "D-05 human-facing message prefixes moved to the STABLE package name `angular-typechecker:` (3 in executor.ts + 1 in run-typecheck.ts); package-name count therefore rose 172 -> 176 (non-decreasing invariant)."
  - "OQ1 default-export function renamed angularTypecheckExecutor -> typecheckExecutor (no runtime coupling; keeps the SC4 gate clean)."
  - "OQ3 no standing stale-token guard test added; SC4 one-shot grep gate used as a verification step only."
  - "D-06 committed as a single breaking feat! touching the package; NO nx release / tag cut (0.1.0 happens later at the milestone Release PR)."

patterns-established:
  - "Single-atomic-commit rename: an intermediate split state breaks the build + executor resolution, so all files change in one commit rather than per-task commits."
  - "Verify a rename against fresh execution: nx reset + --skip-nx-cache on every gate so a cached old-id PASS can never be trusted."

requirements-completed: [EXEC-01]

# Metrics
duration: 55min
completed: 2026-07-02
---

# Phase 13.1 Plan 01: Rename angular-typecheck executor to typecheck Summary

**Mechanical, behavior-preserving identity rename of the shipped Nx executor `angular-typechecker:angular-typecheck` -> `angular-typechecker:typecheck` across both token families, the impl directory (git mv), executors.json, both nx.json targetDefaults forms (WALK-02 value byte-for-byte), all consumers/fixtures/specs, and the README recipes; proven complete by a dual negative grep gate + corruption canary and behavior-unchanged by the full build/test/e2e re-run.**

## Performance

- **Duration:** ~55 min
- **Started:** 2026-07-01 (late session)
- **Completed:** 2026-07-02
- **Tasks:** 4
- **Files modified:** 40 (in the breaking commit; 8 of them git-mv renames)

## Accomplishments
- Shipped-package identity fully renamed: `executors.json` key `typecheck`, `implementation`/`schema` paths under `src/executors/typecheck/`, schema `$id` + TS interface `TypecheckExecutorOptions`, default-export `typecheckExecutor`.
- `git mv` moved all 8 executor-dir files with history preserved (`git log --follow` shows continuous history across the rename).
- Both `nx.json` targetDefaults keys re-keyed (`angular-typechecker:typecheck` and `@angular-typechecker/angular-typechecker:typecheck`); the WALK-02 cacheable value block is byte-for-byte identical (`git diff nx.json` shows ONLY the two key strings changed).
- All dev-repo consumer libs, all three e2e projects (fixtures + consumer nx.json + specs), and both README recipes reference the new id + target names (`typecheck`, matrix `typecheck-spec`).
- Full verification set green (build, typecheck-drift, in-plugin test 214/214, install/cache/matrix e2e, lint incl. `@nx/dependency-checks`, format:check) run with `--skip-nx-cache`; committed as one breaking `feat!`.

## Task Commits

This plan is ONE atomic, behavior-preserving rename — an intermediate split state (some files renamed, others not) breaks the build AND executor resolution, so it lands as a SINGLE breaking commit rather than per-task commits (as the PLAN's Task 4 directs):

1. **Tasks 1-3 (all edits) + Task 4 (rebuild/verify/commit)** - `956e657` (`feat(executor)!: rename angular-typecheck executor to typecheck`)

**Plan metadata:** (this SUMMARY + STATE.md + ROADMAP.md + REQUIREMENTS.md) - `docs(13.1-01): complete rename ... plan`

## Files Created/Modified
- `packages/angular-typechecker/executors.json` - key `typecheck`; impl/schema paths -> `src/executors/typecheck/`.
- `packages/angular-typechecker/src/executors/typecheck/*` - git mv (8 files); `$id`/interface -> `TypecheckExecutorOptions`; default export `typecheckExecutor`; message prefixes -> `angular-typechecker:`; gate-a-static dist path -> `executors/typecheck/`.
- `packages/angular-typechecker/src/core/run-typecheck.ts` - the one no-Program infra message prefix -> `angular-typechecker:` (the `/returned no Program/` assertion in infra-failure.spec.ts still matches).
- `packages/angular-typechecker/src/core/nx-target-defaults.spec.ts` - `WALK_TARGET_DEFAULT_KEYS` (2) + describe label + comments -> new ids.
- `nx.json` - both targetDefaults keys re-keyed; values preserved byte-for-byte.
- `libs/typecheck-consumer/project.json`, `libs/typecheck-walk-consumer/project.json` (+ their component comments) - target name `typecheck` + scoped executor id.
- `README.md`, `packages/angular-typechecker/README.md` - consumer recipe id/target/run commands + prose.
- `CLAUDE.md` - `## Project` prose mention `angular-typecheck` -> `typecheck`.
- `e2e/angular-typechecker-{install,matrix,cache}-e2e/**` - fixture project.json target names + executor refs, consumer nx.json targetDefaults keys, manifest asserts (`executors['typecheck']`), tarball-audit packed paths (`src/executors/typecheck/`), TARGET consts, matrix `typecheck-spec` sibling, fixture-source comments.

## Grep-Gate Results (SC4, final, post-build, repo-wide with `-g '!.planning/**' -g '!CHANGELOG.md' -g '!package-lock.json'`)
- Family 1 `angular-typecheck(?!er)`: **0** (baseline was 80/34 files).
- Family 2 `AngularTypecheck|angularTypecheck`: **0** (baseline was 21/7 files).
- Corruption canary `typecheckr`: **0** (baseline 0; unchanged — no prefix corruption).
- Package name `angular-typechecker`: **176** occ / 55 files (baseline 172/54; rose by **+4** exactly as predicted by D-05 — 3 message prefixes in executor.ts + 1 in run-typecheck.ts moved to the package name). Invariant is non-decreasing (>= 172): satisfied.

## Verification Results (all `--skip-nx-cache`, after `nx reset`)
1. `nx build angular-typechecker` - GREEN; `dist/.../src/executors/typecheck/{executor.js,schema.json}` present; old dist dir gone; built executors.json key `typecheck`.
2. `nx run-many -t typecheck-drift test -p angular-typechecker` - GREEN (27 test files, 214 tests; schema-parity + nx-target-defaults pass).
3. `nx run-many -t test -p angular-typechecker-install-e2e angular-typechecker-cache-e2e angular-typechecker-matrix-e2e` - GREEN ("Successfully ran target test for 3 projects"). The "Failed tasks: local-lib:typecheck-spec / app:typecheck" lines are the deliberate injected-error sub-runs INSIDE the e2e specs (they assert a non-zero exit); the orchestrating specs passed.
4. `nx run-many -t lint` - GREEN (`@nx/dependency-checks`, `maxWarnings:0`).
5. `nx format:check` - GREEN (exit 0).

## Commit
- **SHA:** `956e657`
- **Subject:** `feat(executor)!: rename angular-typecheck executor to typecheck`
- **Shape:** breaking (`!` + `BREAKING CHANGE:` footer), touches `packages/angular-typechecker/`, no AI attribution, files staged by name, 8 renames detected (history preserved).
- **NO `nx release` was run and NO git tag was created** — the 0.1.0 cut happens later at the milestone Release PR (D-06).

## Decisions Made
See frontmatter `key-decisions`. All were LOCKED in CONTEXT.md (D-01..D-06, OQ1, OQ3); none were re-litigated. D-05's message-prefix choice (`angular-typechecker:`) is what makes the package-name gate non-decreasing (176) rather than strictly unchanged.

## Deviations from Plan

None - plan executed exactly as written. All edits were anchored on executor-only tokens (D-03); no bare global replace; no scope expansion. (One tooling note, not a deviation: an initial `git add` invocation listed the now-nonexistent old executor directory as a trailing pathspec and git aborted the batch; re-running the `git add` without that path staged all 40 files correctly. The old-path deletions were already staged by `git mv`.)

## REVIEWER FLAG (Pitfall 6 — intentionally OUT OF SCOPE, not fixed)
`packages/angular-typechecker/README.md` Option B `targetDefaults` example (around line 105) still uses the `production` named input, whereas WALK-02 mandates `default` (the root README recipe correctly uses `default`). This is a WALK-02 documentation nit, NOT an EXEC-01 rename target — CONTEXT D-04 / Pitfall 6 explicitly kept it OUT of scope. The rename DID update the executor id in that block (`angular-typechecker:typecheck`); the `production` input was left unchanged by design. **Recommend a follow-up doc fix** (`production` -> `default`) so the package README matches the root README + WALK-02.

## Issues Encountered
None — the full gate passed on the first fresh rebuild. The e2e "Failed tasks" lines are expected injected-error sub-runs (see Verification #3), not failures.

## Next Phase Readiness
- The new executor id `angular-typechecker:typecheck` is live and proven; Phase 14's `configuration` generator can target it from the start.
- No blockers. The breaking bump is staged in the commit history (`feat!`) for the milestone Release PR to compute 0.1.0.

## Self-Check: PASSED
- Created/renamed files exist on disk: executors.json, src/executors/typecheck/{executor.ts,schema.json,schema.d.ts}, nx.json, nx-target-defaults.spec.ts — all FOUND.
- Commit `956e657` exists in `git log`.
- `git log --follow` on the moved `executor.ts` returns 6 history entries — rename preserved history.
- Repo-wide SC4 gate: both families 0, canary 0, package-name 176 (>= 172).

---
*Phase: 13.1-rename-angular-typecheck-executor-to-typecheck*
*Completed: 2026-07-02*
