---
quick_id: 260715-ig5
slug: re-run-the-acv-01-manual-real-clone-tarb
mode: quick-full
created: 2026-07-15
description: >-
  Re-run the ACV-01 manual real-clone tarball gate against post-24-06 HEAD
  (ngx-leaflet@818e9ae app+lib, realworld-angular@9e3528f app-only): pack the
  local dist -> ng add -> plant per-leaf errors -> ng run <project>:typecheck
  asserts RED, clean control GREEN.
must_haves:
  truths:
    - "The freshly built + packed tarball (post-24-06 HEAD) ships compiled .js (no raw .ts) + builders.json/collection.json/executors.json, and its schematics/ng-add/schematic.js is nx-free (zero @nx/devkit) -- the 24-06 delta."
    - "On ngx-leaflet@818e9ae, a SINGLE `ng add <tarball>` auto-wires a typecheck target into BOTH ngx-leaflet-demo AND ngx-leaflet with 2-element tsConfig arrays and no stray nx.json; clean baseline both exit 0; planted per-leaf errors give app=TS2322+TS2345 (not TS2554) and lib=TS2554 (not app codes); no ERR_REQUIRE_ESM."
    - "On realworld-angular@9e3528f, a SINGLE `ng add <tarball>` auto-wires realworld-angular with [tsconfig.app.json, tsconfig.spec.json] and no stray nx.json; clean baseline exits 0; planted TS2322 (app comp) + TS2345 (app spec) both surface and it exits non-zero; no ERR_REQUIRE_ESM. Confirms the 24-06 vanilla ng-add auto-wires first-run (no ng g workaround needed)."
  artifacts:
    - ".planning/quick/260715-ig5-.../260715-ig5-SUMMARY.md"
    - ".planning/quick/260715-ig5-.../260715-ig5-VERIFICATION.md"
    - "24-ACV-01-UAT.md refreshed with a post-24-06 re-run record"
  key_links:
    - "dist/packages/angular-typechecker (built) -> npm pack -> $ABS_TGZ"
    - "ng add $ABS_TGZ -> angular.json architect.<project>.typecheck (angular-typechecker:typecheck)"
---

# Quick Task 260715-ig5: Re-run ACV-01 real-clone gate against post-24-06 HEAD

## Objective

Confirm the ACV-01 milestone-final real-clone tarball gate still PASSES against the
post-24-06 HEAD (the nx-free vanilla `ng-add`). The prior manual UAT ran 2026-07-11,
one day before 24-06 rewrote the exact `ng add` code path; the verifier flagged this as
a LOW-risk pre-release confirmation. This re-run additionally proves the 24-06 behavior
change: a SINGLE `ng add <tarball>` auto-wires all projects on the FIRST run (the old
gate #2 pnpm `ng g` workaround should no longer be needed).

Runs on the MAIN checkout (needs the real `dist/` + external clones); NO worktree
isolation (the `worktree-for-the-e2e` anti-pattern). Clones are UNCOMMITTED scratch
reproduced by URL + pinned SHA.

## Tasks

### Task 1 -- Rebuild dist from HEAD + pack; assert vanilla nx-free ng-add
- **files:** `dist/packages/angular-typechecker/**`, `$ABS_TGZ`
- **action:** `npx nx build angular-typechecker --skip-nx-cache`; `npm pack` from the built
  dist (MSYS `/d/...` path); extract + assert the tarball ships compiled `.js` (no raw `.ts`),
  `builders.json`/`collection.json`/`executors.json`/`generators.json`, `src/index.js|.d.ts`;
  and that `package/src/schematics/ng-add/schematic.js` contains NO `@nx/devkit`/`convertNxGenerator`.
- **verify:** `tar -tzf` lists compiled `.js` + the four JSON manifests; `rg '@nx/devkit'` over the
  packed `schematic.js` is empty.
- **done:** `$ABS_TGZ` exists, is nx-free at the ng-add path, and ships compiled output.

### Task 2 -- Gate #1: ngx-leaflet (app + library)
- **files:** `/d/projects/github/bluehalo/ngx-leaflet` (external clone @ `818e9ae`)
- **action:** reset the clone to pristine (`git reset --hard 818e9ae...` + `git clean -fd`, keep
  gitignored `node_modules`); `npx ng add "$ABS_TGZ" --skip-confirmation`; assert both
  `ngx-leaflet-demo` + `ngx-leaflet` wired with 2-element `tsConfig` arrays + no `nx.json`;
  clean baseline `ng run <each>:typecheck` exit 0; plant TS2322 (app comp) + TS2345 (app spec) +
  TS2554 (lib comp); assert app=TS2322+TS2345 not TS2554, lib=TS2554 not app; `git checkout -- .`
  reverts to green.
- **verify:** angular.json shows both wired targets; clean exits 0; planted exits non-zero with the
  distinct-per-leaf codes and no cross-bleed; no `ERR_REQUIRE_ESM`.
- **done:** gate #1 PASS recorded with evidence.

### Task 3 -- Gate #2: realworld-angular (single application)
- **files:** `/d/projects/github/realworld-angular/realworld-angular` (external clone @ `9e3528f`)
- **action:** reset the clone to pristine (`git reset --hard 9e3528f...` + `git clean -fd`, keep
  `node_modules`); `npx ng add "$ABS_TGZ" --skip-confirmation`; assert `realworld-angular` wired
  with `[tsconfig.app.json, tsconfig.spec.json]` + no `nx.json`; clean baseline exit 0; plant TS2322
  (app comp) + TS2345 (app spec); assert both codes surface + non-zero exit; `git checkout -- .`
  reverts to green. Confirm the FIRST `ng add` auto-wired (no `ng g` fallback).
- **verify:** angular.json shows the wired target with the 2-element array; clean exits 0; planted
  exits non-zero with TS2322 + TS2345; no `ERR_REQUIRE_ESM`.
- **done:** gate #2 PASS recorded with evidence.

## Notes

- On-stack Angular 22 needs NO `--legacy-peer-deps`.
- `ng add` pulls `nx` transitively (via `@nx/devkit`'s peer) and may materialize a `.nx/` dir --
  expected; it does NOT create `nx.json`.
- MSYS tar: use `/d/...` not `D:/...` (Git Bash mis-parses the drive letter as a remote host).
- Package version stays 0.2.0; this is a verification re-run, no product/version change.
