---
phase: 35-automated-code-scanning-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/sarif-proof-fixture/tsconfig.json
  - tools/sarif-proof-fixture/tsconfig.fixture.json
  - tools/sarif-proof-fixture/type-error.ts
  - tools/sarif-proof-fixture/proof.component.ts
  - tools/sarif-proof-fixture/proof.component.html
  - .fallowrc.jsonc
  - .prettierignore
  - packages/angular-typechecker/src/core/machine-reporters-sarif.integration.spec.ts
autonomous: true
requirements: [PROOF-01]

must_haves:
  truths:
    - "The proof fixture lives at `tools/sarif-proof-fixture/` with NO `project.json`, so `nx run-many -t typecheck` and `nx show projects` never touch its deliberate errors, and it is not swept by `tsconfig.tools.json` (explicit allowlist) (D-01)."
    - "ONE CLI run over the fixture solution `tsconfig.json` emits exactly one diagnostic per family: TS2322 (typescript/error), NG8002 (template-type-check/error) from an EXTERNAL `.html`, a warning-level extended NG8xxx (extended-diagnostics/warning), and ATC90002 (tool/error) (D-01a, D-03d)."
    - "ATC90002 (tool/error) is synthesized by exactly ONE deliberately-absent referenced tsconfig leaf in the solution, co-existing with the surviving leaf's three diagnostics in a SINGLE SARIF run -- no second CLI invocation, no merge (D-01b)."
    - "The local drift-lock integration spec runs the shipped CLI over the fixture and asserts the emitted rules set-equal the four (family tag, severity) tuples the CI proof will check, so the fixture cannot silently drift from the reporter contract (D-03d)."
    - "The `.fallowrc.jsonc` new-only gate and `nx format:check` stay green on the new fixture (fixture scoped in fallow `overrides`; diagnostic-sensitive `.html` in `.prettierignore`)."
    - "No published-surface change: `packages/angular-typechecker/**` production code + the plugin manifest are byte-unchanged and no version bump occurs (D-04)."
  artifacts:
    - tools/sarif-proof-fixture/tsconfig.json
    - tools/sarif-proof-fixture/tsconfig.fixture.json
    - tools/sarif-proof-fixture/type-error.ts
    - tools/sarif-proof-fixture/proof.component.ts
    - tools/sarif-proof-fixture/proof.component.html
    - "machine-reporters-sarif.integration.spec.ts (new proof-fixture describe block)"
  key_links:
    - "fixture solution tsconfig -> surviving leaf (TS2322 + NG8002 + warning NG8xxx) + one MISSING reference (ATC90002) -> shipped familyOf/toSarifLevel tag each rule"
    - "drift-lock integration spec -> shipped CLI --format sarif over the fixture -> asserts the four (tag, level) tuples the CI assert (35-02) expects"
    - "fallow overrides + .prettierignore entries -> keep the new-only fallow gate and format:check green on the deliberately-broken fixture"
---

<objective>
Build the isolated one-per-family proof fixture that drives the SHIPPED angular-typechecker engine to emit exactly one diagnostic per SARIF family in a SINGLE CLI run, pre-empt the two CI gate landmines the new `tools/` fixture trips (fallow new-only + Prettier), and lock the fixture-to-reporter contract with a local integration drift-lock.

Purpose: PROOF-01's fixture half. The whole phase asserts that the shipped SARIF -> Code Scanning contract lands; this fixture is the deterministic input that makes all four families fire. It lives OUTSIDE the Nx project graph so its deliberate errors never fail the real merge gate.
Output: `tools/sarif-proof-fixture/**` (5 files, NO project.json), additive `.fallowrc.jsonc`/`.prettierignore` entries, and a new drift-lock describe block in the existing SARIF integration spec.

## Artifacts this plan produces
- `tools/sarif-proof-fixture/tsconfig.json` -- solution tsconfig (files: [], references: surviving leaf + ONE missing leaf)
- `tools/sarif-proof-fixture/tsconfig.fixture.json` -- surviving leaf (strict + strictTemplates)
- `tools/sarif-proof-fixture/type-error.ts` -- fires TS2322 (typescript/error)
- `tools/sarif-proof-fixture/proof.component.ts` -- standalone component with an external templateUrl
- `tools/sarif-proof-fixture/proof.component.html` -- fires NG8002 (template-type-check/error) + a warning NG8xxx (extended-diagnostics/warning)
- New `describe` block in `machine-reporters-sarif.integration.spec.ts` (the local drift-lock)
- Additive `overrides` entry in `.fallowrc.jsonc` + a `.prettierignore` line for the fixture `.html`
- (Deliberately NOT created: `tools/sarif-proof-fixture/tsconfig.missing.json` -- its absence is what synthesizes ATC90002; and NO `project.json`)
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-automated-code-scanning-proof/35-CONTEXT.md
@.planning/phases/35-automated-code-scanning-proof/35-RESEARCH.md
@.planning/phases/35-automated-code-scanning-proof/35-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the isolated one-per-family fixture under tools/sarif-proof-fixture/</name>
  <files>tools/sarif-proof-fixture/tsconfig.json, tools/sarif-proof-fixture/tsconfig.fixture.json, tools/sarif-proof-fixture/type-error.ts, tools/sarif-proof-fixture/proof.component.ts, tools/sarif-proof-fixture/proof.component.html</files>
  <read_first>
    - fixtures/solution-style-all-missing/tsconfig.json (the files:[] + references:[missing paths] shape that synthesizes ATC90002)
    - fixtures/layout-b-host/tsconfig.json (the solution referencing ONE real leaf)
    - fixtures/extended-content-projection/tsconfig.app.json (the surviving-leaf leaf tsconfig with strictTemplates -- copy verbatim, swap `files`)
    - fixtures/layout-b-aggregated/card.component.ts + fixtures/layout-b-aggregated/card.component.html (the PROVEN external-`.html` NG8002 shape -- unknown property on a native element)
    - fixtures/extended-content-projection/child.component.ts + fixtures/extended-content-projection/child.component.html (the PROVEN warning-level NG8011 fallback)
    - packages/angular-typechecker/src/core/diagnostic-family.ts + diagnostic-family.spec.ts (READ-ONLY: familyOf -- rawCode>=90000 -> tool; `.html`-attributed non-catalog NG -> template-type-check; 18-catalog NG -> extended-diagnostics)
    - tsconfig.tools.json (confirm the explicit-file `include` allowlist -- do NOT add the fixture to it)
    - .planning/phases/35-automated-code-scanning-proof/35-RESEARCH.md (## Architecture Patterns -> Pattern 1 + the Code Examples fixture block + the family/level mapping table)
  </read_first>
  <action>
Create the fixture directory `tools/sarif-proof-fixture/` with these files and NOTHING else (no `project.json`, no `tsconfig.missing.json`), implementing D-01/D-01a/D-01b:

1. `tsconfig.json` (SOLUTION): extends `../../tsconfig.base.json`, `compileOnSave: false`, `files: []`, `references: [ { path: "./tsconfig.fixture.json" }, { path: "./tsconfig.missing.json" } ]`. The second reference `./tsconfig.missing.json` MUST NOT exist on disk -- its absence makes the reference walk synthesize ATC90002 (REFERENCE_NOT_FOUND_DIAGNOSTIC_CODE, tool family, error) while the first reference remains a surviving leaf. Do NOT set angularCompilerOptions here.

2. `tsconfig.fixture.json` (surviving LEAF): copy `fixtures/extended-content-projection/tsconfig.app.json` verbatim (extends `../../tsconfig.base.json`; compilerOptions noEmit/target es2022/module preserve/moduleResolution bundler/strict true/emitDecoratorMetadata false/experimentalDecorators false; angularCompilerOptions strictTemplates true), and set `files: ["type-error.ts", "proof.component.ts"]`. Do NOT set `angularCompilerOptions.extendedDiagnostics.defaultCategory` -- the default (warning) is required so the extended NG8xxx lands at severity `warning` per D-03d.

3. `type-error.ts`: export a single const annotated `: number` and assign it a string literal value, so `strict: true` fires TS2322 (typescript, error). One line; self-contained, no imports.

4. `proof.component.ts`: a standalone Angular component modeled on `fixtures/layout-b-aggregated/card.component.ts` -- import Component from `@angular/core`, `@Component({ selector, standalone: true, templateUrl: './proof.component.html' })`, one plain instance property the template binds to (e.g. a readonly value). Keep it self-contained (drop any cross-dir import like card's makeTitle).

5. `proof.component.html`: fire BOTH template-family diagnostics from this external template. (a) NG8002 (template-type-check, error): bind an unknown property on a native element -- use the PROVEN `card.component.html` shape (an unknown property binding on a `<div>` referencing the component property). (b) A warning-level extended NG8xxx (extended-diagnostics, warning): on a SEPARATE element, use an invalid banana-in-box (a two-way-binding-syntax box `[( )]` around an attribute that is not two-way-bindable), which fires NG8101 (`invalidBananaInBox`) at warning level with no extra component wiring. If NG8002 and the extended code interfere or the chosen construct does not fire cleanly at warning level, adjust per the drift-lock spec in Task 3 (split into two elements/leaves -- still ONE run -- or model the extended half on the proven `fixtures/extended-content-projection` NG8011 by adding an `<ng-content select>` mismatch). The drift-lock spec is the arbiter of the exact codes.

Do NOT create a `project.json` anywhere under `tools/sarif-proof-fixture/` (D-01: keeps the fixture invisible to `nx run-many -t typecheck` / `nx show projects`). Do NOT add any fixture file to `tsconfig.tools.json` `include` (it is an explicit allowlist, so the fixture `.ts` are not swept into the tools tsc).
  </action>
  <verify>
    <automated>test ! -f tools/sarif-proof-fixture/project.json && test ! -f tools/sarif-proof-fixture/tsconfig.missing.json && (node dist/packages/angular-typechecker/src/cli/bin.js -c tools/sarif-proof-fixture/tsconfig.json --format sarif || true) | node -e "const j=JSON.parse(require('fs').readFileSync(0,'utf8'));const rules=(j.runs[0].tool.driver.rules||[]);const tags=rules.flatMap(r=>(r.properties&&r.properties.tags)||[]);const need=['typescript','template-type-check','extended-diagnostics','tool'];const miss=need.filter(t=>!tags.includes(t));if(miss.length){console.error('MISSING family tags:',miss,'got:',tags);process.exit(1)}console.log('all four family tags present:',tags)"</automated>
  </verify>
  <done>The fixture emits all four family tags in one SARIF run; no project.json and no tsconfig.missing.json; invisible to the Nx graph and the tools tsc.</done>
  <acceptance_criteria>
    - `dist` CLI (built via `npx nx build angular-typechecker` first if needed) run once over `tools/sarif-proof-fixture/tsconfig.json --format sarif` produces ONE SARIF run whose `tool.driver.rules[].properties.tags` cover all four families: typescript, template-type-check, extended-diagnostics, tool.
    - No `project.json` and no `tsconfig.missing.json` exist under the fixture dir.
    - `npx nx show projects` output does NOT contain a sarif-proof-fixture project.
    - The fixture is not referenced in `tsconfig.tools.json`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Scope the fixture out of the fallow new-only gate and Prettier (Wave 0 landmines)</name>
  <files>.fallowrc.jsonc, .prettierignore</files>
  <read_first>
    - .fallowrc.jsonc (the `overrides` array -- specifically the `fixtures/**` block with unrendered-components/unused-component-inputs/unused-files off, and the narrow `fixtures/vite-query-imports/**` unresolved-imports block; note `tools/**` appears ONLY under `health.ignore`, which is complexity-only)
    - .prettierignore (the whitespace-sensitive diagnostic-template entries for extended-batch-fn/expression)
    - .planning/phases/35-automated-code-scanning-proof/35-RESEARCH.md (## Common Pitfalls -> Pitfall 1 fallow, Pitfall 2 Prettier)
  </read_first>
  <action>
The new `tools/sarif-proof-fixture/**` files are imported by nothing (fallow `unused-files`, error-tier), the component is rendered nowhere (`unrendered-components`, warn), and the deliberately-broken template/`.ts` may trip `unresolved-imports` -- and `tools/**` is only in fallow `health.ignore` (complexity), NOT the `unused-files` overrides. Add a NEW `overrides` entry to `.fallowrc.jsonc` (mirror the existing `fixtures/**` block) with `files: ["tools/sarif-proof-fixture/**"]` and rules `unused-files: off`, `unrendered-components: off`, `unused-component-inputs: off`. Add a short comment (same style as the existing entries) explaining WHY (a CI proof fixture whose sources are intentionally unimported/unrendered/broken; the type-check contract is enforced by the drift-lock spec + the CI proof, not fallow). Then run the local fallow audit (below); if it reports `unresolved-imports` on the fixture, ALSO add `unresolved-imports: off` to the same entry (mirror the narrow `fixtures/vite-query-imports/**` precedent).

In `.prettierignore`, add a line `/tools/sarif-proof-fixture/proof.component.html` under an analogous "whitespace-sensitive diagnostic fixture" comment -- the template's exact whitespace/reflow governs which NG diagnostics fire. Keep the fixture `.ts`/`.json` Prettier-clean (do NOT ignore them).

This is a `ci`/`chore`-typed change to non-published files (does not affect the additive audit; D-04 holds).
  </action>
  <verify>
    <automated>npx fallow audit --format human --base origin/main 2>&1 | rg -i "sarif-proof-fixture" && echo "FALLOW STILL FLAGS FIXTURE -- add the missing rule" && exit 1 || echo "fallow clean on fixture"; npx prettier --check "tools/sarif-proof-fixture/**/*.{ts,json}"</automated>
  </verify>
  <done>The fallow new-only gate and `nx format:check` are green on the fixture (scoped in `.fallowrc.jsonc` overrides + `.prettierignore`).</done>
  <acceptance_criteria>
    - `npx fallow audit --format human --base origin/main` reports NO finding naming `tools/sarif-proof-fixture/*` (new-only gate green).
    - `.fallowrc.jsonc` has a new `overrides` entry scoping the fixture glob (unused-files + unrendered-components + unused-component-inputs off, plus unresolved-imports off only if fallow flagged it).
    - `.prettierignore` contains `/tools/sarif-proof-fixture/proof.component.html`.
    - `npx nx format:check` is green for the fixture `.ts`/`.json` files.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add the local drift-lock describe block to the SARIF integration spec</name>
  <files>packages/angular-typechecker/src/core/machine-reporters-sarif.integration.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/machine-reporters-sarif.integration.spec.ts (the 4 existing describe blocks; the fixture-path consts + `runSarif` helper; the `ruleIds`/`ruleById`/`rulesOf`/`expectEveryResultResolvesToItsRule` helpers; the layout-b-host block asserting NG8002->template-type-check + TS2322->typescript at error level; the extended block asserting the NG8xxx->extended-diagnostics at warning; the tool block asserting ATC90002->tool at error)
    - packages/angular-typechecker/src/core/diagnostic-family.ts (READ-ONLY family contract)
    - .planning/phases/35-automated-code-scanning-proof/35-PATTERNS.md (the "machine-reporters-sarif.integration.spec.ts (EXTEND)" section with the exact assertion shapes)
  </read_first>
  <action>
Extend `machine-reporters-sarif.integration.spec.ts` with a NEW (5th) `describe` block for the proof fixture -- the local drift-lock (Discretion, RECOMMENDED). Add a `proofFixtureTsConfig` const alongside the existing fixture-path consts pointing at `join(workspaceRoot, 'tools', 'sarif-proof-fixture', 'tsconfig.json')`, and REUSE the existing `runSarif` helper + `ruleIds`/`ruleById`/`rulesOf`/`expectEveryResultResolvesToItsRule` helpers (do NOT re-implement them).

The block runs the shipped CLI once over the proof fixture (`runSarif(proofFixtureTsConfig)`) and asserts, over the SINGLE emitted SARIF run, that the fired ruleIds map to exactly the four (family tag, severity) tuples the CI assert (35-02) will check (D-03d):
  - the TS2322 rule -> `properties.tags` `['typescript']`, `defaultConfiguration.level` `error`
  - the NG8002 rule -> tags `['template-type-check']`, level `error`
  - the extended NG8xxx rule (whatever code Task 1's template actually fired) -> tags `['extended-diagnostics']`, level `warning`
  - the ATC90002 rule -> tags `['tool']`, level `error`
Assert every result resolves to its rule (reuse `expectEveryResultResolvesToItsRule`). Prefer asserting the SET of (family tag, level) tuples present rather than an exact ruleId whitelist, so the exact extended code stays Task-1-discretionary -- but DO assert exactly one rule per family tag (four family tags present, no family missing). If the extended code fired is not what Task 1 targeted, this spec is where you discover it -- confirm the emitted code by running the spec and reconcile Task 1's template if a family is missing.

This block runs under the `integration` nx target (real cold compiler), NOT `nx test` (which excludes `*.integration.spec.ts`). Add a header comment noting it is the LOCAL drift-lock half of PROOF-01 (the reporter-emits-the-contract tripwire); the GitHub-ingestion half is real-CI-only via the 35-03 job.
  </action>
  <verify>
    <automated>npx nx integration angular-typechecker</automated>
  </verify>
  <done>`nx integration` passes; the drift-lock block asserts all four (family tag, level) tuples over one fixture run, reusing existing helpers.</done>
  <acceptance_criteria>
    - `npx nx integration angular-typechecker` passes, including the new proof-fixture describe block.
    - The new block asserts all four family tags (typescript, template-type-check, extended-diagnostics, tool) are present with the correct levels (error/error/warning/error) over ONE SARIF run of the fixture.
    - The block reuses the existing helpers (no duplicated ruleIds/ruleById/runSarif).
    - It is a `*.integration.spec.ts` block (runs under `nx integration`, not `nx test`).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo content -> Nx project graph | The fixture MUST NOT cross into the real `nx typecheck` merge gate; isolation is enforced by having NO `project.json` and no tsconfig.tools.json entry. |
| repo content -> Angular compiler | The fixture sources are trusted, committed repo content fed to the shipped engine, not attacker-controlled input (V5). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-07 | Information Disclosure / DoS (false red) | fixture deliberate errors | medium | mitigate | NO `project.json` (outside Nx graph, D-01) + fallow `overrides` scoping + explicit-allowlist `tsconfig.tools.json` -> the deliberate TS2322/NG8002/ATC90002 never fail `nx run-many -t typecheck`, the fallow new-only gate, or `format:check`. |
| T-35-09 | Tampering | fixture drift vs reporter contract | low | mitigate | The drift-lock integration spec asserts the shipped reporter's actual output over the fixture equals the four expected tuples, so a fixture (or reporter) change that breaks the contract is caught locally, not silently. |

**Note:** The load-bearing CI threats (action pin, PR-metadata injection, token scope, credential persistence, fork false-red, alerts polluting `main`) live in the 35-03 CI-job plan; this plan touches no CI trigger, secret, or write scope.
</threat_model>

<verification>
- `npx nx build angular-typechecker` then the Task-1 automated check: one CLI run over the fixture emits all four family tags in one SARIF run.
- `npx nx integration angular-typechecker` green (drift-lock block).
- `npx fallow audit --format human --base origin/main` reports nothing for the fixture; `npx nx format:check` green.
- Structural: `test ! -f tools/sarif-proof-fixture/project.json`; `npx nx show projects` excludes the fixture; `git diff --stat` touches ONLY `tools/sarif-proof-fixture/**`, `.fallowrc.jsonc`, `.prettierignore`, and the one integration spec -- `packages/angular-typechecker/src/**` production files (non-spec) and `package.json` byte-unchanged (D-04).
</verification>

<success_criteria>
- A single shipped-CLI run over `tools/sarif-proof-fixture/tsconfig.json --format sarif` emits exactly one diagnostic per family (typescript/error, template-type-check/error, extended-diagnostics/warning, tool/error) in ONE SARIF run (PROOF-01, D-01a/D-01b/D-03d).
- The fixture is invisible to the Nx typecheck gate and the tools tsc (D-01), and the fallow new-only + Prettier gates stay green.
- The drift-lock integration spec locks the fixture-to-reporter contract.
- No published surface changes; no version bump (D-04).
</success_criteria>

<output>
Create `.planning/phases/35-automated-code-scanning-proof/35-01-SUMMARY.md` when done.
</output>
