---
phase: 10-drift-hardening-maintainability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/core/compiler-cli-types.ts
  - packages/angular-typechecker/src/core/gather-diagnostics.ts
autonomous: true
requirements: [HARD-02, HARD-03, HARD-04]
must_haves:
  truths:
    - "The shim's EmitFlags enum mirrors the real @angular/compiler-cli@22.0.4 members (DTS=1, JS=2, Metadata=4, I18nBundle=8, Codegen=16, Default=19, All=31) and the fabricated None member is gone"
    - "nx build angular-typechecker stays green (the emitFlags: 0 as EmitFlags cast at run-typecheck.ts:229 is untouched and still type-checks against the corrected enum)"
    - "Every divergent vendored construct in compiler-cli-types.ts carries a greppable `// angular-typechecker: vendored -- <reason>` marker (D-09)"
    - "getNgStructuralDiagnostics() is retained in the gatherer, documented as a deliberately forward-compatible no-op-tolerant call (D-10)"
  artifacts:
    - path: "packages/angular-typechecker/src/core/compiler-cli-types.ts"
      provides: "Corrected EmitFlags enum (real members, no None) + 6 vendor markers"
      contains: "angular-typechecker: vendored"
    - path: "packages/angular-typechecker/src/core/gather-diagnostics.ts"
      provides: "Retained getNgStructuralDiagnostics() with forward-compatible documenting comment"
      contains: "getNgStructuralDiagnostics"
  key_links:
    - from: "packages/angular-typechecker/src/core/run-typecheck.ts:229"
      to: "compiler-cli-types.ts EmitFlags"
      via: "emitFlags: 0 as EmitFlags cast (load-bearing, must stay)"
      pattern: "0 as EmitFlags"
---

<objective>
Correct the vendored shim's fabricated `EmitFlags.None = 0` against the real
`@angular/compiler-cli@22.0.4` enum (HARD-02), add a greppable
`// angular-typechecker: vendored -- <reason>` marker to every divergent construct
in `compiler-cli-types.ts` (HARD-03), and document the retained
`getNgStructuralDiagnostics()` call in the gatherer as deliberately
forward-compatible (HARD-04).

Purpose: This is the "shim corrections" plan. The drift tripwire (Plan 02) asserts
against the corrected shim, so these edits must land first. HARD-02 makes the
shim's enum faithful; HARD-03 makes every divergence discoverable by a single grep;
HARD-04 reverses the earlier "consider dropping" stance into a documented,
asserted retention so a future Angular that reactivates the structural getter cannot
silently under-gather.
Output: An edited `compiler-cli-types.ts` (corrected enum + 6 markers) and a
comment-only edit to `gather-diagnostics.ts`. No runtime behavior changes (the shim
is type-only, erased at emit; the gatherer edit is a comment).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-drift-hardening-maintainability/10-CONTEXT.md
@.planning/phases/10-drift-hardening-maintainability/10-RESEARCH.md
@.planning/phases/10-drift-hardening-maintainability/10-PATTERNS.md

<interfaces>
<!-- Current shim constructs (compiler-cli-types.ts) the edits act on. -->
<!-- The EmitFlags enum TODAY (the bug HARD-02 fixes): -->
<!-- export declare enum EmitFlags { None = 0 }  (lines :89-91) -->
<!-- The real enum (verified live this session, api.d.ts:74-82, NO None member): -->
<!-- DTS=1, JS=2, Metadata=4, I18nBundle=8, Codegen=16, Default=19, All=31 -->

<!-- The 6 divergent constructs needing a vendor marker (RESEARCH HARD-03 enumeration): -->
<!-- 1. TsProgram intersection (:45-47) -- adds synthetic useCaseSensitiveFileNames() -->
<!-- 2. Program subset interface (:57-80) -- only the 7 getters the gatherer calls -->
<!-- 3. EmitFlags enum (:89-91, post-HARD-02) -- mirrors real members, only what performCompilation accepts -->
<!-- 4. UNKNOWN_ERROR_CODE literal (:100) -- hand-declared = 500 (real const is ESM-only) -->
<!-- 5. PerformCompilationResult.program non-optional (:143-146) -- real types it optional -->
<!-- 6. ParsedConfiguration subset (:109-116) -- subset of real; options adds { basePath?: string } -->

<!-- Existing marker idiom to replicate (diagnostic-codes.ts:56): -->
<!-- // angular-typechecker: vendored -- mirrors `@angular/compiler-cli` v22.0.4 ... -->

<!-- The load-bearing call site that must NOT change (run-typecheck.ts:229): -->
<!-- emitFlags: 0 as EmitFlags,   (the cast is what makes 0 acceptable; bare : EmitFlags = 0 ERRORS TS2322) -->

<!-- The retained getter in the gatherer (gather-diagnostics.ts:66): -->
<!-- all.push(...program.getNgStructuralDiagnostics()); -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the shim EmitFlags enum to mirror the real members (HARD-02)</name>
  <files>packages/angular-typechecker/src/core/compiler-cli-types.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/compiler-cli-types.ts (the shim being edited; current EmitFlags enum at :82-91, the ambient-enum rationale comment at :82-88)
    - 10-RESEARCH.md "Pattern 3" (verified EmitFlags member values) + "State of the Art" row (EmitFlags.None -> real members) + Pitfall correction in Summary item 3 (the `0 as EmitFlags` cast is load-bearing; bare `: EmitFlags = 0` ERRORS TS2322 at tsc 6.0.3 -- D-08 rationale corrected)
    - 10-PATTERNS.md "compiler-cli-types.ts (MODIFY -- HARD-02 EmitFlags pattern)" section
    - packages/angular-typechecker/src/core/run-typecheck.ts:226-229 (the `emitFlags: 0 as EmitFlags` cast and its D-05a comment -- this site is NOT edited, only confirmed)
  </read_first>
  <action>
    In `compiler-cli-types.ts`, replace the fabricated single-member `export declare enum EmitFlags { None = 0 }` (D-08) with the real members mirrored verbatim from `@angular/compiler-cli@22.0.4`: `DTS = 1`, `JS = 2`, `Metadata = 4`, `I18nBundle = 8`, `Codegen = 16`, `Default = 19`, `All = 31`. DROP the fabricated `None` member entirely. Keep the `export declare enum` form (it stays erased at emit; the existing ambient-enum rationale comment at :82-88 explaining usability as both a TYPE `0 as EmitFlags` and a VALUE namespace `readonly EmitFlags: typeof EmitFlags` on `CompilerCli` stays and should be lightly updated to note the members now mirror the real enum). Do NOT touch `run-typecheck.ts:229` -- the `emitFlags: 0 as EmitFlags` cast is load-bearing: bare assignment `0` to `EmitFlags` ERRORS TS2322 against the corrected enum, the cast is what keeps `0` (semantically "emit nothing" under `noEmit: true`) acceptable. ASCII only, no non-ASCII in comments.
  </action>
  <verify>
    <automated>npx nx build angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -c "None = 0" -- packages/angular-typechecker/src/core/compiler-cli-types.ts` returns 0 (the fabricated member is gone)
    - `git grep -n -e "DTS = 1" -e "I18nBundle = 8" -e "All = 31" -- packages/angular-typechecker/src/core/compiler-cli-types.ts` shows all three real members present
    - `npx nx build angular-typechecker` exits 0 (the `0 as EmitFlags` cast still type-checks against the corrected enum; the build is the existing drift guard)
    - `git grep -c "0 as EmitFlags" -- packages/angular-typechecker/src/core/run-typecheck.ts` returns 1 (the cast is untouched)
  </acceptance_criteria>
  <done>The shim EmitFlags enum mirrors the real 7 members with no `None`; `nx build` is green; the load-bearing cast at run-typecheck.ts:229 is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add greppable vendor markers to every divergent construct in compiler-cli-types.ts (HARD-03)</name>
  <files>packages/angular-typechecker/src/core/compiler-cli-types.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/compiler-cli-types.ts (the 6 divergent constructs; current line refs in the interfaces block above)
    - packages/angular-typechecker/src/core/diagnostic-codes.ts:52-79 (the EXACT existing `// angular-typechecker: vendored -- mirrors ... v22.0.4 ...` marker idiom to replicate)
    - 10-RESEARCH.md "HARD-03 vendor-marker enumeration" table (the 6 constructs + the `<reason>` for each)
    - 10-PATTERNS.md "compiler-cli-types.ts (MODIFY -- HARD-03 vendor-marker pattern)" + "Shared Patterns > Vendored-from-real marker"
  </read_first>
  <action>
    Add ONE marker line containing the literal token `angular-typechecker: vendored` to EACH of the 6 divergent constructs in `compiler-cli-types.ts`, following the `diagnostic-codes.ts:56` idiom (`// angular-typechecker: vendored -- <reason>`). The 6 constructs and their `<reason>` (wording per construct is Claude's discretion; the literal token is load-bearing): (1) the `TsProgram` intersection -- reason: adds synthetic `useCaseSensitiveFileNames()` not on the public `ts.Program`; (2) the `Program` subset interface -- reason: declares only the diagnostic getters the gatherer calls, a deliberate subset of the real `api.Program`; (3) the `EmitFlags` enum (post-Task-1) -- reason: mirrors the real members; declares only what `performCompilation` accepts as `emitFlags`; (4) the `UNKNOWN_ERROR_CODE` literal -- reason: hand-declared `= 500` instead of importing the ESM-only real const; (5) the non-optional `PerformCompilationResult.program` -- reason: real types it optional; narrowed non-optional to match the engine's guarded usage; (6) the `ParsedConfiguration` subset -- reason: subset of the real type; `options` adds `{ basePath?: string }`. Place each marker as a comment line inside or immediately above the construct's existing doc comment so it is greppable and co-located. Do NOT remove the existing rich WHY-comments. ASCII only.
  </action>
  <verify>
    <automated>npx nx build angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -c "angular-typechecker: vendored" -- packages/angular-typechecker/src/core/compiler-cli-types.ts` returns >= 6
    - Each of the 6 constructs (TsProgram intersection, Program interface, EmitFlags enum, UNKNOWN_ERROR_CODE, PerformCompilationResult.program, ParsedConfiguration) has a marker (confirm by reading the surrounding lines of each `git grep -n "angular-typechecker: vendored"` hit)
    - `npx nx build angular-typechecker` exits 0 (markers are comments; no type effect)
    - No non-ASCII characters introduced (markers use plain `--`, not an en/em dash)
  </acceptance_criteria>
  <done>A single `git grep "angular-typechecker: vendored" -- packages/angular-typechecker/src/core/compiler-cli-types.ts` enumerates all 6 divergent constructs; build stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Document the retained getNgStructuralDiagnostics() call as forward-compatible (HARD-04)</name>
  <files>packages/angular-typechecker/src/core/gather-diagnostics.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/gather-diagnostics.ts:57-83 (the gatherer body; the `getNgStructuralDiagnostics()` call at :66; the existing rich WHY-comment block at :1-56 whose `D-NN`/`RES-NN` decision-ref comment style to match)
    - packages/angular-typechecker/src/core/gather-diagnostics.spec.ts:53,76 (the existing assertion that `getNgStructuralDiagnostics` is called in order -- already covers the retention; NOT edited here)
    - 10-CONTEXT.md D-10 (retain it, documented as deliberately forward-compatible no-op-tolerant) + 10-RESEARCH.md HARD-04 row (present at :66; in the per-member probe set; already exercised by the spec)
    - 10-PATTERNS.md "gather-diagnostics.ts (MODIFY -- HARD-04 documenting comment)" section
  </read_first>
  <action>
    Add a documenting comment at/near the `getNgStructuralDiagnostics()` call (`gather-diagnostics.ts:66`) marking it (per D-10) as a deliberately RETAINED, forward-compatible, no-op-tolerant getter: it currently returns `[]` in practice at Angular 22.0.4 but the getter exists; retaining and asserting it (covered by the HARD-01 per-member drift probe in Plan 02 and the runtime getter-set spec in Plan 03, plus the existing `gather-diagnostics.spec.ts` call-order assertion) means a future Angular that REACTIVATES it cannot silently under-gather. Reference the decision (`HARD-04` / `D-10`) in the comment to match the existing block's `D-NN`/`RES-NN` style. This is a COMMENT-ONLY edit -- do NOT change the call, the order, or any code. ASCII only.
  </action>
  <verify>
    <automated>npx nx run angular-typechecker:test -t "gatherAllDiagnostics"</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "getNgStructuralDiagnostics" -- packages/angular-typechecker/src/core/gather-diagnostics.ts` still shows the call at the same logical position (retained, not removed)
    - A comment near the call references `HARD-04` or `D-10` and the "forward-compatible / no-op-tolerant / cannot silently under-gather" rationale (confirm by reading the lines around the call)
    - `npx nx run angular-typechecker:test -t "gatherAllDiagnostics"` passes (the existing call-order assertion is unchanged and green)
    - No code lines changed in the gatherer body beyond the added comment (the diff is comment-only)
  </acceptance_criteria>
  <done>`getNgStructuralDiagnostics()` is retained with a `D-10`/`HARD-04` forward-compatible documenting comment; the existing gather test is green; no behavior change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiler-cli-types.ts -> emitted .js | The shim is type-only (`declare` enum + interfaces); erased at emit, never shipped as runtime code. No untrusted input crosses here. |
| run-typecheck.ts:229 cast -> EmitFlags type | The `0 as EmitFlags` cast is the only consumer of the corrected enum; a fixed literal, no external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | EmitFlags correction silently breaks the no-emit guarantee | mitigate | `0` remains semantically "emit nothing"; `noEmit: true` is the orthogonal guarantee at run-typecheck.ts; `nx build` (Task 1 verify) confirms the cast still type-checks; no member value invented (all mirror the verified real enum) |
| T-10-01-02 | Information disclosure | Marker comments leak internal info into the shipped tarball | accept | Markers are source comments; the published `.d.ts` is generated by `@nx/js:tsc` and the shim is type-only -- comments do not affect the public contract; no secret/PII in marker text |
| T-10-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (D-03: zero new dependency); nothing to verify; no supply-chain surface added |
</threat_model>

<verification>
- `npx nx build angular-typechecker` green (the corrected enum + the load-bearing cast type-check; build is the existing drift guard).
- `npx nx run angular-typechecker:test -t "gatherAllDiagnostics"` green (HARD-04 retention unchanged).
- `git grep -c "angular-typechecker: vendored" -- packages/angular-typechecker/src/core/compiler-cli-types.ts` >= 6.
- `git grep -c "None = 0" -- packages/angular-typechecker/src/core/compiler-cli-types.ts` == 0.
</verification>

<success_criteria>
- HARD-02: shim `EmitFlags` mirrors the real members (DTS=1..All=31), no `None`; `nx build` green; `0 as EmitFlags` cast retained.
- HARD-03: every divergent construct in `compiler-cli-types.ts` carries the greppable vendor marker (>= 6).
- HARD-04: `getNgStructuralDiagnostics()` retained + documented forward-compatible; existing gather test green.
</success_criteria>

<output>
Create `.planning/phases/10-drift-hardening-maintainability/10-01-SUMMARY.md` when done.
</output>
