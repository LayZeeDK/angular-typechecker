---
phase: 02-core-type-check-engine-gatherer
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01-engine-core-contract
files_modified:
  - fixtures/ts-baseline/error.component.ts
  - fixtures/ts-baseline/error.component.html
  - fixtures/ts-baseline/tsconfig.app.json
  - fixtures/ng-baseline/error.component.ts
  - fixtures/ng-baseline/error.component.html
  - fixtures/ng-baseline/tsconfig.app.json
  - fixtures/extended-v13/error.component.ts
  - fixtures/extended-v13/error.component.html
  - fixtures/extended-v13/tsconfig.app.json
  - fixtures/extended-promoted/error.component.ts
  - fixtures/extended-promoted/error.component.html
  - fixtures/extended-promoted/tsconfig.app.json
  - fixtures/composite-triangle/error.component.ts
  - fixtures/composite-triangle/error.component.html
  - fixtures/composite-triangle/tsconfig.json
  - fixtures/no-emit-message/error.component.ts
  - fixtures/no-emit-message/error.component.html
  - fixtures/no-emit-message/tsconfig.app.json
  - packages/angular-typechecker/src/core/diagnostic-codes.ts
  - packages/angular-typechecker/src/core/baseline.angular13.integration.spec.ts
  - packages/angular-typechecker/src/core/extended.angular13.integration.spec.ts
  - packages/angular-typechecker/src/core/extended.angular17.integration.spec.ts
  - packages/angular-typechecker/src/core/no-emit-override.integration.spec.ts
autonomous: true
requirements:
  - TEST-02
  - ENG-04
  - ENG-02
must_haves:
  truths:
    - "Integration specs run the real compiler against committed fixtures and assert EXACT diagnostic codes/counts"
    - "A TS baseline fixture asserts code 2322; a template-driven fixture asserts code 2339"
    - "An NG baseline fixture asserts an exact NG code via the NG() helper"
    - "An extended NG8101 fixture asserts category Warning by default; a promoted variant asserts the same code as category Error in errorCount"
    - "A composite-triangle fixture proves the D-05 override neutralizes TS5053/6304/6379 (none present)"
    - "A diagnostics:true fixture proves D-02 suppresses the Time-for-diagnostics Message"
    - "Specs are organized per Angular introduction version so future codes are drop-in additive"
  artifacts:
    - path: "packages/angular-typechecker/src/core/diagnostic-codes.ts"
      provides: "The NG() / ngCodeOf() helpers shared by the integration tier (D-07d)"
      contains: "NG"
    - path: "packages/angular-typechecker/src/core/extended.angular13.integration.spec.ts"
      provides: "NG8101 default-Warning category assertion (ENG-04 / D-07d)"
    - path: "fixtures/composite-triangle/tsconfig.json"
      provides: "The D-05/L-1 composite-triangle regression input"
      contains: "composite"
  key_links:
    - from: "*.integration.spec.ts"
      to: "runTypecheck"
      via: "direct call per fixture (D-07c)"
      pattern: "runTypecheck\\(\\{\\s*tsConfigPath"
    - from: "extended.angular17.integration.spec.ts"
      to: "extendedDiagnostics.defaultCategory error"
      via: "category-promotion proof (ENG-04 / D-01 fact)"
      pattern: "DiagnosticCategory.Error"
---

<objective>
Stand up the REAL-compiler integration test tier (TEST-02) end-to-end: a representative ~6-8 differentiator fixture set (D-07a), each a committed static broken source with its own tsconfig, asserted via `runTypecheck` for EXACT codes and counts using the `NG()` helper (D-07d). This slice proves the engine's most valuable behaviors are real, not theoretical:

- the unconditional gatherer surfaces a TS error AND a template/extended error in one program (ENG-02 multi-error — reusing the F1/F7 `gate-b-error` fixture asserted in 02-01, extended here with a TS2339 template-driven case);
- extended NG8xxx diagnostics default to category Warning and auto-promote to Error in `errorCount` when a fixture sets `extendedDiagnostics.defaultCategory: "error"` (ENG-04 / D-01 category fact);
- the D-05 override neutralizes the composite/emitDeclarationOnly TS5053/6304/6379 triangle (D-05/L-1, ROADMAP criterion 1 — unproven on this classic-base workspace without a dedicated fixture);
- D-02 suppresses the "Time for diagnostics" Message.

Files are organized per Angular introduction version (`*.angularNN.integration.spec.ts`) so the full v13->v22 catalog is purely additive later (D-07a). This slice runs in parallel with 02-02 (Wave 2): both depend only on the 02-01 contract and touch disjoint files.

Purpose: TEST-02 is the phase's correctness backstop — exact-code/count assertions against the real compiler, replacing the priors' success-boolean assertions. It is also where the D-05 override and the ENG-04 category-promotion fact get proven on this workspace.

Output: New committed fixtures (ts-baseline TS2339, ng-baseline NG code, extended-v13 NG8101, extended-promoted, composite-triangle, no-emit-message), a shared `diagnostic-codes.ts` helper module, and per-version integration specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-type-check-engine-gatherer/02-CONTEXT.md
@.planning/phases/02-core-type-check-engine-gatherer/02-RESEARCH.md
@.planning/phases/02-core-type-check-engine-gatherer/02-PATTERNS.md
@.planning/phases/02-core-type-check-engine-gatherer/02-01-SUMMARY.md
@.planning/research/DIAGNOSTIC-CATALOG.md

<interfaces>
<!-- The CoreResult contract this tier asserts against (locked by 02-01, D-01). -->
```typescript
export interface CoreResult {
  tsConfigPath: string;
  rootNamesCount: number;
  diagnostics: readonly ts.Diagnostic[];
  errorCount: number;    // category === Error
  warningCount: number;  // category === Warning
  durationMs: number;
}
```
Code-encoding facts (re-verified vs installed 22.0.4 — re-grep `error_code.d.ts` at author time per D-07a / assumption A1):
- TS codes are RAW: 2322, 2339, 5053, 6304, 6379.
- NG codes are NEGATIVE-encoded for DISPLAY: `ngErrorCode(code) = parseInt('-99' + code)`. Helper: `const NG = (code) => -990000 - code;` (NG8109 -> -998109). Recovery: `Math.abs(code) - 990000`.
- NG8101 = INVALID_BANANA_IN_BOX (WARNING by default); NG8109 = INTERPOLATED_SIGNAL_NOT_INVOKED; NG8021 = DEFER_TRIGGER_MISCONFIGURATION; NG8001 = SCHEMA_INVALID_ELEMENT; NG2003 = PARAM_MISSING_TOKEN; NG8117 = UNINVOKED_FUNCTION_IN_TEXT_INTERPOLATION.
- AVOID NG8110 / NG8112 — NOT extended diagnostics (L-7).
- Count by `.category`, NEVER by code sign (L-4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the differentiator fixtures (TS2339, NG baseline, NG8101, promoted, composite-triangle, diagnostics:true)</name>
  <files>fixtures/ts-baseline/error.component.ts, fixtures/ts-baseline/error.component.html, fixtures/ts-baseline/tsconfig.app.json, fixtures/ng-baseline/error.component.ts, fixtures/ng-baseline/error.component.html, fixtures/ng-baseline/tsconfig.app.json, fixtures/extended-v13/error.component.ts, fixtures/extended-v13/error.component.html, fixtures/extended-v13/tsconfig.app.json, fixtures/extended-promoted/error.component.ts, fixtures/extended-promoted/error.component.html, fixtures/extended-promoted/tsconfig.app.json, fixtures/composite-triangle/error.component.ts, fixtures/composite-triangle/error.component.html, fixtures/composite-triangle/tsconfig.json, fixtures/no-emit-message/error.component.ts, fixtures/no-emit-message/error.component.html, fixtures/no-emit-message/tsconfig.app.json</files>
  <read_first>
    - fixtures/gate-b-error/error.component.ts (the broken-standalone-component shape to mirror; F1/F7 already covered — do NOT duplicate)
    - fixtures/gate-b-error/error.component.html (the template that triggers NG8109 via `{{ status }}`)
    - fixtures/gate-b-error/tsconfig.app.json (the fixture tsconfig pattern: extends ../../tsconfig.base.json, strictTemplates true, files[])
    - .planning/phases/02-core-type-check-engine-gatherer/02-RESEARCH.md (Fixture Catalog table lines 323-340 — F2-F6, F8; the AVOID-NG8110/8112 note; A1 re-grep instruction)
    - .planning/phases/02-core-type-check-engine-gatherer/02-PATTERNS.md (broken-source pattern lines 178-195; fixture tsconfig pattern lines 203-217; composite-triangle fixture lines 221-239 verbatim shape; the F6 promotion tsconfig note line 217; NG code names re-verified line 194)
    - .planning/research/DIAGNOSTIC-CATALOG.md (per-introduction-version organization + which codes belong to which Angular major)
    - node_modules/@angular/compiler-cli/src/ngtsc/diagnostics/src/error_code.d.ts (re-grep exact NG code numbers/names at author time — use `rg` since node_modules is gitignored)
  </read_first>
  <action>
    Author the new fixtures, each a minimal standalone component + template + its own `tsconfig.*.json` extending `../../tsconfig.base.json` with `strictTemplates: true`. ASCII-only. Do NOT add `@ts-nocheck`. Mirror the `gate-b-error` shapes. Re-verify each NG code/name against installed `error_code.d.ts` (use `rg`, node_modules is gitignored) before committing — codes 8101/8109/8021 are confirmed; F3/F4 NG names need an author-time re-grep (A1).

    - `fixtures/ts-baseline/` (F2 — TS template-driven): a component whose template references a member that does NOT exist on the class, so `strictTemplates` surfaces it as TS2339 (raw). Keep a clean class otherwise so 2339 is the only/primary error. (F1 plain TS2322 is already covered by `gate-b-error`; this fixture adds the TEMPLATE-driven TS case.)
    - `fixtures/ng-baseline/` (F3 or F4 — NG baseline): pick ONE confirmed NG baseline code and author for it. F3: an unknown element in the template -> NG8001 (SCHEMA_INVALID_ELEMENT). F4: a constructor param of a primitive type without an injection token -> NG2003 (PARAM_MISSING_TOKEN). Choose whichever you can author deterministically against 22.0.4 and document the chosen code in a comment.
    - `fixtures/extended-v13/` (F5 — NG8101 default Warning): a banana-in-box template binding mistake (`[(prop)]` on a non-two-way target, or the documented invalid-banana shape) -> NG8101, which is a WARNING by default. tsconfig has `strictTemplates: true` and NO `defaultCategory` override.
    - `fixtures/extended-promoted/` (F6 — promotion proof): the SAME extended-diagnostic shape (NG8101, or NG8109/NG8021) but the tsconfig sets `"angularCompilerOptions": { "strictTemplates": true, "extendedDiagnostics": { "defaultCategory": "error" } }`. This proves the same code lands in `errorCount` as category Error when promoted (ENG-04 / D-01 fact).
    - `fixtures/composite-triangle/` (F8 — D-05/L-1 regression): copy the verbatim shape from 02-PATTERNS.md lines 226-237 — a `tsconfig.json` setting `composite: true`, `declarationMap: true`, `emitDeclarationOnly: true` (the three options the D-05 override must neutralize) plus `strictTemplates: true` and `files: ["error.component.ts"]`. The component can carry one intended error (or be clean); the point is the override must prevent TS5053/6304/6379.
    - `fixtures/no-emit-message/` (D-02 proof): a component (clean or with one error) whose tsconfig sets `compilerOptions.diagnostics: true` (or `angularCompilerOptions` equivalent that triggers the "Time for diagnostics" Message path). This is the fixture that proves D-02's `diagnostics: false` override suppresses the category-Message entry. If `diagnostics: true` proves hard to trigger via tsconfig, the D-02 assertion in Task 2 falls back to asserting ABSENCE of any category-Message "Time for diagnostics" entry (which holds regardless — A2).

    All fixtures are at workspace-root `fixtures/`, out of the project graph, already covered by the broad `fixtures/**/*` exclude added in 02-01.
  </action>
  <verify>
    <automated>npx nx build angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `ls fixtures/ts-baseline/tsconfig.app.json fixtures/ng-baseline/tsconfig.app.json fixtures/extended-v13/tsconfig.app.json fixtures/extended-promoted/tsconfig.app.json fixtures/composite-triangle/tsconfig.json fixtures/no-emit-message/tsconfig.app.json` all exist.
    - `git grep -n "composite" fixtures/composite-triangle/tsconfig.json` matches `composite: true` and `git grep -n "emitDeclarationOnly" fixtures/composite-triangle/tsconfig.json` matches `emitDeclarationOnly: true`.
    - `git grep -n "defaultCategory" fixtures/extended-promoted/tsconfig.app.json` matches `"defaultCategory": "error"`.
    - `git grep -n "diagnostics" fixtures/no-emit-message/tsconfig.app.json` matches a `diagnostics`-enabling option (or a comment documenting the D-02 absence-assertion fallback).
    - No fixture file contains `@ts-nocheck` (`git grep -n "ts-nocheck" fixtures/` returns nothing).
    - `npx nx build angular-typechecker` still succeeds (fixtures excluded from the lib build).
  </acceptance_criteria>
  <done>
    The F2-F6 + F8 + D-02 differentiator fixtures are committed under `fixtures/`, each with its own tsconfig, NG codes re-verified against 22.0.4, modeled on the `gate-b-error` shapes, and excluded from the package build.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the shared NG() helper and the per-version integration specs (exact codes/counts)</name>
  <files>packages/angular-typechecker/src/core/diagnostic-codes.ts, packages/angular-typechecker/src/core/baseline.angular13.integration.spec.ts, packages/angular-typechecker/src/core/extended.angular13.integration.spec.ts, packages/angular-typechecker/src/core/extended.angular17.integration.spec.ts, packages/angular-typechecker/src/core/no-emit-override.integration.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/gate-b.spec.ts (fixture-path resolution lines 1-2, 34-39; describe.each lines 68-71; raw TS code + NG -998109 idiom)
    - packages/angular-typechecker/src/core/run-typecheck.ts (the contract under test, locked by 02-01)
    - fixtures/ts-baseline/ fixtures/ng-baseline/ fixtures/extended-v13/ fixtures/extended-promoted/ fixtures/composite-triangle/ fixtures/no-emit-message/ (Task 1 outputs)
    - .planning/phases/02-core-type-check-engine-gatherer/02-PATTERNS.md (NG() helper lines 149-151; exact-code+category idiom lines 154-160; promotion idiom lines 162; exact-counts idiom lines 164-168; per-version split lines 170)
    - .planning/phases/02-core-type-check-engine-gatherer/02-RESEARCH.md (Requirement->Test Map rows ENG-02/ENG-04/TEST-02/D-01/D-02/D-05 lines 443-451; the NG() helper lines 307-313)
  </read_first>
  <action>
    Create `src/core/diagnostic-codes.ts` exporting the D-07d helpers (named must-have): `export const NG = (code: number): number => -990000 - code;` and `export const ngCodeOf = (code: number): number => Math.abs(code) - 990000;`. Add a short JSDoc citing `ngErrorCode = parseInt('-99' + code)` (the two formulas agree for 4-digit NG codes). This file is production-importable but is only consumed by specs; keep it dependency-free (no compiler-cli import).

    Create the per-introduction-version integration specs (D-07c — call `runTypecheck` directly, one performCompilation per fixture; D-07d — assert EXACT codes/counts via `NG()`). Copy the fixture-path resolution idiom from `gate-b.spec.ts` lines 1-2, 34-39. Name files `*.angularNN.integration.spec.ts` so future codes are drop-in additive (D-07a):

    - `baseline.angular13.integration.spec.ts` (TS + NG baselines):
      - ts-baseline fixture -> `result.diagnostics.map((d) => d.code)` contains `2339` (raw); `result.errorCount >= 1`.
      - ng-baseline fixture -> codes contain the chosen NG baseline via `NG(<code>)`; assert `result.errorCount >= 1` (NG baseline codes are Errors).
    - `extended.angular13.integration.spec.ts` (NG8101 default Warning):
      - extended-v13 fixture -> `const banana = result.diagnostics.find((d) => d.code === NG(8101))`; `expect(banana?.category).toBe(ts.DiagnosticCategory.Warning)`; assert the code lands in `result.warningCount` (>= 1) and NOT in `errorCount` (mirror Angular's `invalid_banana_in_box_spec.ts` idiom).
    - `extended.angular17.integration.spec.ts` (promotion proof, ENG-04 / D-01):
      - extended-promoted fixture -> the SAME extended code now has `category === ts.DiagnosticCategory.Error` and is counted in `result.errorCount` (the `defaultCategory: "error"` auto-promotion). Assert the invariant `result.errorCount + result.warningCount <= result.diagnostics.length`.
    - `no-emit-override.integration.spec.ts` (D-02 + D-05/L-1):
      - composite-triangle fixture -> `const codes = result.diagnostics.map((d) => d.code)`; assert codes does NOT contain `5053`, `6304`, or `6379` (the D-05 override neutralized the triangle — ROADMAP criterion 1).
      - no-emit-message fixture -> `result.diagnostics` contains NO category-Message "Time for diagnostics" entry (D-02). Use `ts.DiagnosticCategory.Message` + a message-text check, or assert absence per the A2 fallback.

    All specs use the `*.integration.spec.ts` suffix so the quick-run filter skips them. Apply project JS/TS style (blank lines around control flow/returns, braces always). TS codes assert RAW; NG codes via `NG()`; never assert bare NG codes (L-4 / Pitfall E).
  </action>
  <verify>
    <automated>npx nx build angular-typechecker && npx nx test angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -n "export const NG" packages/angular-typechecker/src/core/diagnostic-codes.ts` matches the `-990000 - code` helper.
    - `npx nx test angular-typechecker` is green including all four new `*.integration.spec.ts` files.
    - `extended.angular13.integration.spec.ts` asserts the NG8101 diagnostic `.category === ts.DiagnosticCategory.Warning` and that it is counted in `warningCount` (default-category proof).
    - `extended.angular17.integration.spec.ts` asserts the promoted extended diagnostic `.category === ts.DiagnosticCategory.Error` counted in `errorCount`, plus the invariant `errorCount + warningCount <= diagnostics.length`.
    - `no-emit-override.integration.spec.ts` asserts the composite-triangle codes exclude `5053`, `6304`, `6379` (D-05/L-1) and the no-emit-message fixture has no "Time for diagnostics" category-Message entry (D-02).
    - `git grep -nE "toContain\(8101\)|toContain\(8109\)|toContain\(8021\)" packages/angular-typechecker/src/core/*.integration.spec.ts` returns NOTHING (no bare-NG-code assertions — all go through `NG()`; L-4 / Pitfall E).
  </acceptance_criteria>
  <done>
    The integration tier proves end-to-end against the real compiler: TS + NG baselines (exact codes), the NG8101 default-Warning category, the `defaultCategory:"error"` promotion into `errorCount` (ENG-04), the D-05 composite-triangle neutralization (criterion 1), and the D-02 Message suppression — all asserted with exact codes/counts via `NG()` and organized per Angular introduction version (TEST-02 / D-07a-d).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture tsconfig + source -> runTypecheck | Fixtures are committed, trusted, out-of-graph; they are the controlled inputs that exercise the engine's diagnostic-surfacing and override behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01b | Tampering / Repudiation-of-trust | unconditional gatherer (ENG-02) | mitigate | "Type-checker that LIES" by short-circuiting: the multi-error path (reused gate-b-error in 02-01 + the TS2339 template-driven fixture here) and the GATE B differential prove TS + template/extended diagnostics surface in ONE pass where `ngc` defaultGatherDiagnostics drops the NG codes. Task 1 fixtures + Task 2 assertions. |
| T-02-03b | Tampering | D-05 no-emit override | mitigate | The composite-triangle fixture proves the override neutralizes TS5053/6304/6379 — without it, success criterion 1 is unproven on this classic-base workspace, and a consumer's TS-solution base would fail every project for the wrong reason (a false FAIL, the inverse lie). Task 1 fixture + Task 2 assertion. |
| T-02-08 | Tampering / Repudiation-of-trust | extended-diagnostic category counting (ENG-04) | mitigate | The default-Warning vs promoted-Error assertions prove counts honor `.category` (not code sign), so a consumer's `extendedDiagnostics.defaultCategory: "error"` correctly fails the build and a default WARNING does not silently become a hidden error. Tasks 1-2 (extended-v13 + extended-promoted). |
| T-02-09 | Tampering | D-02 diagnostics Message | mitigate | The no-emit-message fixture proves `diagnostics: false` suppresses the "Time for diagnostics" category-Message, keeping counts/output clean and deterministic (agent-ready). Task 1 fixture + Task 2 assertion. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies (fixtures + specs + a dependency-free helper module). No supply-chain delta. |
</threat_model>

<verification>
- `npx nx build angular-typechecker` succeeds (fixtures excluded from the lib build).
- `npx nx test angular-typechecker` green including all four new `*.integration.spec.ts`.
- Quick run skips the integration tier: `npx nx test angular-typechecker -- --exclude '**/*.integration.spec.ts'`.
</verification>

<success_criteria>
- TEST-02: real-compiler integration specs assert EXACT codes/counts across a representative v13->v22 differentiator subset, organized per introduction version (additive growth ready).
- ENG-04: extended diagnostics default to Warning and promote to Error in `errorCount` via `defaultCategory`; the invariant `errorCount + warningCount <= diagnostics.length` holds.
- D-05/L-1: the composite-triangle fixture proves the override neutralizes TS5053/6304/6379 (ROADMAP criterion 1).
- D-02: the diagnostics-Message is suppressed.
- All NG-code assertions go through the `NG()` helper; no bare-code assertions.
</success_criteria>

<output>
Create `.planning/phases/02-core-type-check-engine-gatherer/02-03-SUMMARY.md` when done.
</output>
