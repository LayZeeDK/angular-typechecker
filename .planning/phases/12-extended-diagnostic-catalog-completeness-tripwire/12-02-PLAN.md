---
phase: 12-extended-diagnostic-catalog-completeness-tripwire
plan: 02
type: execute
wave: 2
depends_on: ['12-01']
files_modified:
  - packages/angular-typechecker/src/core/extended-catalog.integration.spec.ts
  - packages/angular-typechecker/src/core/extended.angular13.integration.spec.ts
  - packages/angular-typechecker/src/core/extended.promotion.integration.spec.ts
  - fixtures/extended-batch-expression/error.component.ts
  - fixtures/extended-batch-expression/error.component.html
  - fixtures/extended-batch-expression/tsconfig.app.json
  - fixtures/extended-batch-structural/error.component.ts
  - fixtures/extended-batch-structural/error.component.html
  - fixtures/extended-batch-structural/tsconfig.app.json
  - fixtures/extended-batch-fn/error.component.ts
  - fixtures/extended-batch-fn/error.component.html
  - fixtures/extended-batch-fn/tsconfig.app.json
  - fixtures/extended-skip-hydration/error.component.ts
  - fixtures/extended-skip-hydration/error.component.html
  - fixtures/extended-skip-hydration/tsconfig.app.json
  - fixtures/extended-unused-standalone-imports/error.component.ts
  - fixtures/extended-unused-standalone-imports/error.component.html
  - fixtures/extended-unused-standalone-imports/tsconfig.app.json
  - fixtures/extended-defer-trigger/error.component.ts
  - fixtures/extended-defer-trigger/error.component.html
  - fixtures/extended-defer-trigger/tsconfig.app.json
  - fixtures/extended-content-projection/parent.component.ts
  - fixtures/extended-content-projection/parent.component.html
  - fixtures/extended-content-projection/child.component.ts
  - fixtures/extended-content-projection/child.component.html
  - fixtures/extended-content-projection/tsconfig.app.json
autonomous: true
requirements: [CAT-01, CAT-02, CAT-04]
must_haves:
  truths:
    - 'Running the catalog spec asserts every one of the 18 extended members by exact NG() code + DiagnosticCategory + occurrence count against the real @angular/compiler-cli over committed fixtures'
    - 'The catalog is a SINGLE data-driven it.each table keyed on EXTENDED_DIAGNOSTIC_MEMBERS with introduction-version as a row field'
    - 'Any non-reproducible member appears as it.skip WITH A WRITTEN REASON and its row stays in the table (the tripwire consumes the LIST, not the test outcome)'
    - 'NG8011 is asserted as a NORMAL promotable Warning-default member (NOT it.skip-ped for promotion, NOT framed as not-promotable)'
    - 'One promotion case proves defaultCategory: error flips NG8101 from Warning to Error'
    - 'The two folded specs (extended.angular13, extended.promotion) are deleted -- one catalog of record'
    - 'Traceability -- implements CONTEXT.md decisions D-03 (batch fixtures per program), D-04 (per-row NG code/category/count shape via NG()), D-05 (single enum-keyed it.each table), D-07 (fold the two extended specs), D-08 (one NG8101 promotion proof), D-09 (NG8011 promotable)'
  artifacts:
    - path: 'packages/angular-typechecker/src/core/extended-catalog.integration.spec.ts'
      provides: 'The 18-row extended it.each catalog + the NG8101 promotion row'
      contains: 'EXTENDED_DIAGNOSTIC_MEMBERS'
      min_lines: 80
  key_links:
    - from: 'packages/angular-typechecker/src/core/extended-catalog.integration.spec.ts'
      to: './extended-catalog.members'
      via: 'the it.each table is keyed on EXTENDED_DIAGNOSTIC_MEMBERS (D-02)'
      pattern: 'EXTENDED_DIAGNOSTIC_MEMBERS'
    - from: 'packages/angular-typechecker/src/core/extended-catalog.integration.spec.ts'
      to: './run-typecheck'
      via: 'runTypecheck({ tsConfigPath }) per row against a fixtures/* tsconfig'
      pattern: 'runTypecheck'
    - from: 'packages/angular-typechecker/src/core/extended-catalog.integration.spec.ts'
      to: './diagnostic-codes'
      via: 'NG() encodes every asserted NG code (never bare)'
      pattern: "NG\\("
---

<objective>
Build the single data-driven extended-diagnostic catalog: an `it.each` table keyed on the 18
`EXTENDED_DIAGNOSTIC_MEMBERS`, each row asserting exact NG() code + `DiagnosticCategory` + occurrence
count against the real `@angular/compiler-cli` over committed fixtures, plus the one NG8101
severity-promotion proof.

Purpose: CAT-01 demands the suite prove angular-typechecker observes the COMPLETE 18-member extended
surface by exact identity; CAT-04 demands a single enum-keyed table (intro-version a row field, not a
file split); CAT-02 demands at least one promotion proof. This plan also folds the two existing NG8101
specs into the catalog of record so there is no drifting duplicate.

Output: `extended-catalog.integration.spec.ts` (the 18-row table + promotion row), ~7 new batched/own-
program fixtures for the members not already covered by existing fixtures, and the deletion of
`extended.angular13.integration.spec.ts` + `extended.promotion.integration.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-CONTEXT.md
@.planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-RESEARCH.md
@.planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-PATTERNS.md

<interfaces>
<!-- The exact contracts the executor needs. Use directly; no codebase exploration needed. -->

From packages/angular-typechecker/src/core/run-typecheck.ts:

- `export interface CoreOptions { tsConfigPath: string; /* ... */ }`
- `export async function runTypecheck(options: CoreOptions): Promise<CoreResult>;`
- `export interface CoreResult { diagnostics: readonly ts.Diagnostic[]; errorCount: number; warningCount: number; suppressedCount: number; /* ... */ }`
  (errorCount/warningCount are counted EXPLICITLY by ts.DiagnosticCategory, never `length - errorCount`;
  invariant `errorCount + warningCount <= diagnostics.length`.)

From packages/angular-typechecker/src/core/diagnostic-codes.ts:

- `export const NG = (code: number): number => -990000 - code;` (so `NG(8101) === -998101`).
  Every NG assertion routes through `NG()`; never assert a bare positive code.

From packages/angular-typechecker/src/core/extended-catalog.members.ts (created in Plan 01):

- `export const EXTENDED_DIAGNOSTIC_MEMBERS = [ ...18 camelCase values... ] as const;` (declaration order).

The cwd-independent fixture-path resolver (copy verbatim from
extended.angular13.integration.spec.ts:22-23):
`const packageRoot = join(dirname(fileURLToPath(import.meta.url)), '..', '..');`
`const workspaceRoot = join(packageRoot, '..', '..');`
Each fixture tsconfig path is `join(workspaceRoot, 'fixtures', '<scenario>', 'tsconfig.app.json')`.

The find-by-code + assert-category idiom (extended.angular13.integration.spec.ts:33-44) -- but for the
catalog use `.filter(d => d.code === NG(row.ngCode))` + `.toBe(row.expectedCount)` (EXACT count, CAT-01),
then `expect(hits[0]?.category).toBe(row.expectedCategory)`. Count by ts.DiagnosticCategory, NEVER code sign.

The it.skip gate (RESEARCH Code Examples 308-310): `const maybe = row.skipReason ? it.skip : it;`
(RESEARCH Reproducibility projects ZERO skips, but the gate must exist and the row STAYS regardless).

## The 18-member catalog rows (member -> NG code -> default category; ALL default Warning)

VERIFIED vs @angular/compiler-cli@22.0.4 (RESEARCH Standard Stack table, lines 89-108):
| Member | NG code | Default category |
|--------|---------|------------------|
| invalidBananaInBox | 8101 | Warning |
| nullishCoalescingNotNullable | 8102 | Warning |
| optionalChainNotNullable | 8107 | Warning |
| missingControlFlowDirective | 8103 | Warning |
| missingStructuralDirective | 8116 | Warning |
| textAttributeNotBinding | 8104 | Warning |
| uninvokedFunctionInEventBinding | 8111 | Warning |
| missingNgForOfLet | 8105 | Warning |
| suffixNotSupported | 8106 | Warning |
| skipHydrationNotStatic | 8108 | Warning |
| interpolatedSignalNotInvoked | 8109 | Warning |
| controlFlowPreventingContentProjection | 8011 | Warning (PROMOTABLE -- normal member, see CORRECTNESS GUARD) |
| unusedLetDeclaration | 8112 | Warning |
| uninvokedTrackFunction | 8115 | Warning |
| unusedStandaloneImports | 8113 | Warning |
| unparenthesizedNullishCoalescing | 8114 | Warning |
| uninvokedFunctionInTextInterpolation | 8117 | Warning |
| deferTriggerMisconfiguration | 8021 | Warning |

## CORRECTNESS GUARD (D-09 CORRECTED, triple-verified docs+source+runtime)

NG8011 (`controlFlowPreventingContentProjection`) IS promotable. Treat it as a NORMAL promotable member:
default Warning, promotable to Error via `extendedDiagnostics.defaultCategory: "error"`. Do NOT write an
`it.skip` "promotion" case for it, do NOT frame it as "not promotable," and NEVER assert it stays a
Warning under `defaultCategory: "error"` -- that assertion would FAIL against real Angular 22.0.4.

## Existing fixtures to REUSE (no new fixture needed for these)

- `fixtures/extended-v13/` -> invalidBananaInBox (NG8101, Warning).
- `fixtures/extended-promoted/` -> invalidBananaInBox (NG8101, promoted to Error; the CAT-02 row).
- `fixtures/gate-b-error/` -> interpolatedSignalNotInvoked (NG8109) + an incidental TS2322 (the
  fixture deliberately also carries `count: number = 'not a number'`). NOTE: a row that maps to this
  fixture must assert ONLY its NG8109 count via `.filter(d => d.code === NG(8109))`; do NOT assert
  errorCount === 0 against this fixture (TS2322 is present by design).

## Fixture-batching map (RESEARCH lines 163-181) -- the ~7 NEW fixtures this plan creates

- Batch A `fixtures/extended-batch-expression/`: nullishCoalescingNotNullable (8102),
  optionalChainNotNullable (8107), unparenthesizedNullishCoalescing (8114),
  uninvokedFunctionInTextInterpolation (8117), textAttributeNotBinding (8104), suffixNotSupported (8106).
- Batch B `fixtures/extended-batch-structural/`: missingControlFlowDirective (8103),
  missingStructuralDirective (8116), missingNgForOfLet (8105). NOTE 8103 vs 8116 may co-fire on one node
  -- split into separate programs if an exact-count assertion collides (D-03 allows it).
- Batch C `fixtures/extended-batch-fn/`: uninvokedFunctionInEventBinding (8111),
  uninvokedTrackFunction (8115), unusedLetDeclaration (8112).
- Own program `fixtures/extended-skip-hydration/`: skipHydrationNotStatic (8108) -- `[ngSkipHydration]="x"`
  static-attribute check (NOT a hydration runtime; RESEARCH refuted that hypothesis, line 227).
- Own program `fixtures/extended-unused-standalone-imports/`: unusedStandaloneImports (8113) -- a
  standalone `@Component({ imports: [SomeUnused] })` whose import is never referenced in the template.
- Own program `fixtures/extended-defer-trigger/`: deferTriggerMisconfiguration (8021) -- a `@defer` block
  with conflicting triggers (e.g. `on immediate; on timer(1s)`).
- Own program `fixtures/extended-content-projection/`: controlFlowPreventingContentProjection (8011) -- a
  two-component setup (parent with `<ng-content>` slot; child projects an `@if` block with >1 root node).

## Fixture file shapes to mirror

- tsconfig.app.json: copy `fixtures/extended-v13/tsconfig.app.json` verbatim, varying only `files`.
  (For the content-projection fixture with multiple component files, list all `.component.ts` in `files`.)
- component .ts: copy the `fixtures/extended-v13/error.component.ts` shape -- `@Component({ standalone: true,
templateUrl: './<name>.component.html' })`, a load-bearing header naming the NG code + trigger + "this
  diagnostic is the ONLY diagnostic," and class members chosen so ONLY the target diagnostic(s) fire.
  Do NOT add `@ts-nocheck`. Every member referenced in a template must be valid so no incidental TS error
  pollutes the exact-count assertion (RESEARCH Pitfall 4).
  </interfaces>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the ~7 new extended-diagnostic fixtures</name>
  <read_first>
    - fixtures/extended-v13/error.component.ts + error.component.html + tsconfig.app.json (the EXACT fixture shape to mirror -- flat dir, `strictTemplates: true`, load-bearing header, "the diagnostic is the ONLY diagnostic")
    - fixtures/ng-baseline/error.component.ts (a second example of the single-diagnostic fixture discipline)
    - .planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-RESEARCH.md (Reproducibility Analysis lines 212-237 -- the VERIFIED trigger for each member; Fixture-batching map lines 163-181)
    - .planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-PATTERNS.md (NEW fixtures section lines 234-309 -- including the NG8011 two-component note)
    - node_modules/@angular/compiler-cli/src/ngtsc/diagnostics/src/error_code.d.ts (use `rg -uu` -- confirm each NG code, e.g. line 319 for NG8011 with its example, before authoring)
  </read_first>
  <files>fixtures/extended-batch-expression/, fixtures/extended-batch-structural/, fixtures/extended-batch-fn/, fixtures/extended-skip-hydration/, fixtures/extended-unused-standalone-imports/, fixtures/extended-defer-trigger/, fixtures/extended-content-projection/</files>
  <behavior>
    Each fixture, when fed to `runTypecheck({ tsConfigPath })`, produces its target NG code(s) as the
    ONLY diagnostic(s) at the expected category:
    - Batch A: NG8102, NG8107, NG8114, NG8117, NG8104, NG8106 each fire exactly once, all Warning.
    - Batch B: NG8103, NG8116, NG8105 each fire (split into own programs if 8103/8116 counts collide).
    - Batch C: NG8111, NG8115, NG8112 each fire exactly once, all Warning.
    - extended-skip-hydration: NG8108 fires once (Warning).
    - extended-unused-standalone-imports: NG8113 fires once (Warning).
    - extended-defer-trigger: NG8021 fires once (Warning).
    - extended-content-projection: NG8011 fires once (Warning) from the two-component projection setup.
  </behavior>
  <action>
    Create each fixture as a flat (or, for content-projection, multi-component) directory under
    `fixtures/` mirroring `fixtures/extended-v13/`: a `tsconfig.app.json` (copy verbatim, vary only
    `files`), the `.component.ts` file(s), and the `.component.html` template(s). Use the VERIFIED triggers
    from RESEARCH Reproducibility Analysis (e.g. `{{ nonNullable ?? fallback }}` for NG8102; `(click)="myFunc"`
    uninvoked for NG8111; `@let notUsed = 2;` never read for NG8112; `[ngSkipHydration]="x"` for NG8108;
    a standalone unused `imports: [X]` for NG8113; `@defer (on immediate; on timer(1s))` for NG8021; a
    parent `<ng-content>` + child `@if` block with >1 root node for NG8011). Engineer each fixture so the
    target diagnostic is the ONLY diagnostic (valid class members, no incidental TS/NG error). Give each
    component .ts a load-bearing header naming the NG code, its default category, the exact trigger, and
    "this diagnostic is the ONLY diagnostic." Per D-03, if a batch's exact-count assertion (proven in
    Task 2) collides, split that member into its own program and update `files_modified` accordingly. Per
    RESEARCH Assumption A1, CONFIRM each diagnostic actually fires in a real run before relying on it --
    if a member genuinely cannot fire from a static fixture, that becomes an `it.skip` row WITH A WRITTEN
    REASON in Task 2 (its row stays); do NOT silently drop it.
  </action>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && ls fixtures/extended-batch-expression fixtures/extended-batch-structural fixtures/extended-batch-fn fixtures/extended-skip-hydration fixtures/extended-unused-standalone-imports fixtures/extended-defer-trigger fixtures/extended-content-projection</automated>
  </verify>
  <acceptance_criteria>
    - All 7 new fixture directories exist, each with a `tsconfig.app.json` extending `../../tsconfig.base.json` with `strictTemplates: true`.
    - No fixture contains `@ts-nocheck`.
    - The content-projection fixture has a parent + child component pair with an `<ng-content>` slot and an `@if` block.
    - Each component .ts opens with a header naming its target NG code and the trigger.
  </acceptance_criteria>
  <done>The 7 new fixtures exist, each engineered so its target extended diagnostic is the only diagnostic, mirroring the existing fixture discipline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the 18-row extended catalog it.each + the NG8101 promotion row</name>
  <read_first>
    - packages/angular-typechecker/src/core/extended.angular13.integration.spec.ts (the find-by-code + assert-category idiom, the cwd-independent path resolver -- this spec is folded/deleted here, D-07)
    - packages/angular-typechecker/src/core/extended.promotion.integration.spec.ts (the NG8101 promotion proof + the `errorCount + warningCount <= diagnostics.length` invariant -- folded/deleted here, D-07/D-08)
    - packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts (the `describe.each([...])` parameterization idiom)
    - packages/angular-typechecker/src/core/extended-catalog.members.ts (the source of truth the table keys on, D-02)
    - .planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-RESEARCH.md (Code Examples lines 280-318 -- the CatalogRow shape + it.each sketch; Promotability lines 239-250 -- NG8011 IS promotable)
    - .planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-PATTERNS.md (catalog spec section lines 91-164)
  </read_first>
  <files>packages/angular-typechecker/src/core/extended-catalog.integration.spec.ts</files>
  <behavior>
    - For each of the 18 `EXTENDED_DIAGNOSTIC_MEMBERS`, `runTypecheck` against the mapped fixture yields
      `result.diagnostics.filter(d => d.code === NG(row.ngCode)).length === row.expectedCount` and
      `hits[0].category === row.expectedCategory` (all Warning by default).
    - A promotion row: `runTypecheck` against `fixtures/extended-promoted/` yields the SAME NG8101 code at
      `DiagnosticCategory.Error` and `errorCount >= 1`.
    - The promotion invariant holds: `errorCount + warningCount <= diagnostics.length`.
    - NG8011's row asserts `DiagnosticCategory.Warning` (its default) -- NOT a skip, NOT a stays-Warning-
      under-promotion claim.
  </behavior>
  <action>
    Create `extended-catalog.integration.spec.ts` mirroring the three analog specs. Define a
    `CatalogRow` interface: `{ member: (typeof EXTENDED_DIAGNOSTIC_MEMBERS)[number]; ngCode: number;
    expectedCategory: ts.DiagnosticCategory; expectedCount: number; introVersion: string; fixtureTsConfig:
    string; skipReason?: string }`. Build a `CATALOG: readonly CatalogRow[]` with one row per member in
    `EXTENDED_DIAGNOSTIC_MEMBERS` order, mapping each to the fixture from `<interfaces>` (existing:
    extended-v13 for 8101, gate-b-error for 8109; new: the 7 from Task 1) with `expectedCategory:
    ts.DiagnosticCategory.Warning` and the `expectedCount` proven by a real run. Carry `introVersion` as a
    ROW FIELD (do NOT split into per-version files -- CAT-04). Drive the table with
    `describe.each(CATALOG)('extended diagnostic %s', (row) => { ... })`, gating each `it` with
    `const maybe = row.skipReason ? it.skip : it;` so a non-reproducible member stays as `it.skip` WITH A
    WRITTEN REASON and its row stays. In each `it`, call `runTypecheck({ tsConfigPath: row.fixtureTsConfig })`,
    `filter` by `d.code === NG(row.ngCode)`, assert `.length === row.expectedCount` and
    `hits[0]?.category === row.expectedCategory`. For the gate-b-error-backed row (NG8109), assert ONLY
    its filtered count/category -- do NOT assert `errorCount === 0` (TS2322 is present by design). Add a
    SEPARATE promotion `it` (folding `extended.promotion.integration.spec.ts`): `runTypecheck` against
    `fixtures/extended-promoted/tsconfig.app.json`, find NG8101, assert `DiagnosticCategory.Error`,
    `errorCount >= 1`, and the `errorCount + warningCount <= diagnostics.length` invariant -- this is the
    single CAT-02 promotion proof (D-08); add NO per-member promotion tests (YAGNI). Treat NG8011 as a
    normal Warning-default row per the CORRECTNESS GUARD. Copy the cwd-independent `packageRoot`/
    `workspaceRoot` resolver verbatim. Use the source-of-truth `(typeof EXTENDED_DIAGNOSTIC_MEMBERS)[number]`
    type for the `member` field so a member rename forces a compile error in the table too.
  </action>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && npx nx test angular-typechecker --skip-nx-cache -- extended-catalog.integration</automated>
  </verify>
  <acceptance_criteria>
    - `npx nx test angular-typechecker --skip-nx-cache -- extended-catalog.integration` passes.
    - The `CATALOG` array has exactly 18 rows (`git grep -c "ngCode:" packages/angular-typechecker/src/core/extended-catalog.integration.spec.ts` returns >= 18).
    - The `member` field type is `(typeof EXTENDED_DIAGNOSTIC_MEMBERS)[number]` (`git grep -c "EXTENDED_DIAGNOSTIC_MEMBERS" packages/angular-typechecker/src/core/extended-catalog.integration.spec.ts` returns >= 1).
    - A promotion `it` asserts NG8101 at `ts.DiagnosticCategory.Error` against `fixtures/extended-promoted/`.
    - The NG8011 row asserts `ts.DiagnosticCategory.Warning` and is NOT `it.skip`-ped; NO assertion anywhere expects NG8011 to stay a Warning under `defaultCategory: "error"`.
    - Every NG assertion routes through `NG(...)`; no bare positive NG code is asserted.
  </acceptance_criteria>
  <done>The 18-member enum-keyed catalog passes with exact code+category+count, plus the one NG8101 promotion proof; intro-version is a row field; NG8011 is a normal promotable member.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the two folded specs (D-07) and confirm one catalog of record</name>
  <read_first>
    - packages/angular-typechecker/src/core/extended.angular13.integration.spec.ts (its NG8101-warning assertion must now live in the catalog before deletion)
    - packages/angular-typechecker/src/core/extended.promotion.integration.spec.ts (its NG8101-promotion + invariant assertions must now live in the catalog before deletion)
    - packages/angular-typechecker/src/core/extended-catalog.integration.spec.ts (the catalog of record -- confirm both folded assertions are present)
  </read_first>
  <files>packages/angular-typechecker/src/core/extended.angular13.integration.spec.ts, packages/angular-typechecker/src/core/extended.promotion.integration.spec.ts</files>
  <action>
    AFTER confirming the catalog (Task 2) carries both the NG8101 Warning-default assertion (folding
    `extended.angular13`) and the NG8101 promotion + invariant assertions (folding `extended.promotion`),
    DELETE both original specs using `git rm` (per AGENTS.md, prefer `git rm`; never `git add -A`). Do NOT
    delete `baseline.angular13.integration.spec.ts` -- that is folded in Plan 03. Re-run the plugin test
    suite to confirm nothing references the deleted specs and the catalog stands alone as the single
    extended-diagnostic catalog of record. (The TESTING.md spec-count update is done in Plan 03, which
    also folds the baseline spec, so both fold deltas land in one accurate count change.)
  </action>
  <verify>
    <automated>cd "D:/projects/github/LayZeeDK/angular-typechecker" && test ! -f packages/angular-typechecker/src/core/extended.angular13.integration.spec.ts && test ! -f packages/angular-typechecker/src/core/extended.promotion.integration.spec.ts && npx nx test angular-typechecker --skip-nx-cache</automated>
  </verify>
  <acceptance_criteria>
    - Neither `extended.angular13.integration.spec.ts` nor `extended.promotion.integration.spec.ts` exists on disk.
    - The deletions are staged via `git rm` (the SUMMARY notes the `git rm` usage, not a raw delete).
    - `npx nx test angular-typechecker --skip-nx-cache` passes (no dangling reference to the deleted specs; the catalog's NG8101 warning + promotion assertions are green).
    - `baseline.angular13.integration.spec.ts` STILL exists (it is folded in Plan 03, not here).
  </acceptance_criteria>
  <done>The two extended specs are removed via `git rm` after their assertions are absorbed; the catalog is the single extended-diagnostic catalog of record and the suite is green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                                                                                                                                                                                               |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none new) | Test/doc-only phase: committed in-repo fixtures fed to the locked, lockfile-pinned `@angular/compiler-cli@22.0.4`. No runtime code path, no network, no untrusted input, no new dependency, no auth/crypto/data handling. |

## STRIDE Threat Register

| Threat ID | Category  | Component                                     | Disposition | Mitigation Plan                                                                                                                                                                 |
| --------- | --------- | --------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-12-02   | Tampering | new committed fixtures + the integration spec | accept      | No new threat surface: the only "input" is the authored-in-repo fixtures fed to the locked compiler at test time. RESEARCH section Security Domain: no ASVS category applies.   |
| T-12-SC   | Tampering | npm/pip/cargo installs                        | mitigate    | N/A -- this plan installs NO packages. If an executor unexpectedly adds a dependency, STOP and escalate to a `checkpoint:human-verify` per the security threat-model directive. |

No `high`-severity threats; nothing to block on (RESEARCH section Security Domain: no new threat surface introduced).
</threat_model>

<verification>
- `npx nx test angular-typechecker --skip-nx-cache -- extended-catalog.integration` passes (18 rows + promotion).
- `npx nx test angular-typechecker --skip-nx-cache` passes with the two folded specs deleted.
- The catalog table keys on `EXTENDED_DIAGNOSTIC_MEMBERS`; NG8011 is a normal promotable Warning row.
</verification>

<success_criteria>

- CAT-01: all 18 extended members asserted by exact NG() code + DiagnosticCategory + occurrence count.
- CAT-02: one promotion proof (NG8101 Warning -> Error under `defaultCategory: "error"`).
- CAT-04: a single enum-keyed `it.each` table with intro-version as a row field; any skip carries a written reason and its row stays.
- D-07: the two extended specs folded and deleted; one catalog of record.
  </success_criteria>

<output>
Create `.planning/phases/12-extended-diagnostic-catalog-completeness-tripwire/12-02-SUMMARY.md` when done.
</output>
