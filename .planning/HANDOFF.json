{
  "version": "1.0",
  "timestamp": "2026-07-23T17:18:46.445Z",
  "phase": "36",
  "phase_name": "code-scanning-gating-scanned-files-documentation",
  "phase_dir": ".planning/phases/36-code-scanning-gating-scanned-files-documentation",
  "plan": null,
  "task": null,
  "total_tasks": null,
  "status": "paused",
  "milestone": "v0.2.4",
  "branch": "main",
  "head": "44e4306",
  "summary": "Milestone v0.2.4 (PR #55) MERGED to main earlier this session (merge commit 966a7c6). The RED-SARIF proof was reworked into its own Code Scanning tool (quick task 260722-g6y, in PR #55). Then a LONG spike (012) resolved GATE-02: the 'Require code scanning results' merge gate for angular-typechecker WORKS -- the only blocker was an ORPHANED angular-typechecker Code Scanning config on main (category rename); deleting the 4 orphaned analyses via the Code Scanning API fixed it. The gate is now ACTIVE on main (CodeQL + angular-typechecker, user-authorized) and proven on real code+planning PRs. Pausing before the wrap-up follow-ups.",
  "completed_this_session": [
    {"item": "Resumed prior pause; merged milestone PR #55 (v0.2.4) to main as merge commit 966a7c6 (user-authorized).", "status": "done"},
    {"item": "Quick task 260722-g6y: reworked the deliberate RED SARIF proof onto its own Code Scanning tool (angular-typechecker-red-proof) with assert-then-dismiss; verified on probe PR #57; all 3 deferred research questions settled. Landed in PR #55.", "status": "done"},
    {"item": "GATE-02 RESOLVED (spike 012): the angular-typechecker merge gate WORKS. Root cause of all prior 'configuration not found' = an orphaned angular-typechecker-category config on main (pre-Phase-34 category rename to angular-typecheck). Deleted 4 orphaned analyses via Code Scanning API. Gate now active on main; proven CLEAN on probe #64 (planning-only) + #65 (code PR), check=success 'No new alerts'.", "status": "done"},
    {"item": "Corrected the gate-02 memory (was wrongly 'not viable') + MEMORY.md index.", "status": "done"}
  ],
  "remaining_tasks": [
    {"id": 1, "name": "Update AGENTS.md GATE-02 runbook: add the CRITICAL orphan-cleanup prerequisite (delete orphaned Code Scanning configs before enabling), the transient-vs-permanent 'configuration not found' insight, that the gate works with the existing multi-run + default merge-ref upload (NO ci.yml change), keep angular-typechecker-red-proof OFF the required list, note fallow not gated. AGENTS.md changes need code review + a PR.", "status": "not_started"},
    {"id": 2, "name": "Document spike 012 (README + MANIFEST row + optional wrap-up) at .planning/spikes/012-code-scanning-gate-single-run-multiproject/. Land via a planning PR (main is PR-only).", "status": "not_started"},
    {"id": 3, "name": "Fix the fallow file-less SARIF upload bug ('locationFromSarifResult: expected at least one location'; Phase-35-04-style whole-file fallback). LOW urgency -- rare, fires only on specific findings (succeeded on #65), fallow is not gated.", "status": "not_started"},
    {"id": 4, "name": "Phase 36 completion (now UNBLOCKED -- GATE-02 done): flip 36-UAT.md test 2 to passed (evidence: gate active + proven on #64/#65), re-verify via gsd-verifier to passed, phase.complete 36, extract-learnings 36 (+ global bridge), milestone v0.2.4 close (audit-milestone -> complete-milestone).", "status": "not_started"},
    {"id": 5, "name": "v0.2.4 release (human-gated): Release PR -> tag angular-typechecker@0.2.4 on the merge commit -> OIDC publish (approve npm-publish environment). Version currently held at 0.2.3.", "status": "not_started"}
  ],
  "blockers": [
    {"description": "main is PR-only (empty-bypass ruleset 18229122). Pending planning artifacts (spike docs, AGENTS.md runbook, phase-36 completion updates) must reach main via a PR, not a direct push. Enabling/altering the main ruleset is user-authorized this session but is otherwise human-only.", "type": "external", "workaround": "Land follow-up docs via a planning PR into main."}
  ],
  "human_actions_pending": [
    {"action": "The v0.2.4 release cut (Release PR + tag + OIDC publish, approve npm-publish environment).", "context": "AGENTS.md release mechanics; CI holds only id-token:write.", "blocking": false},
    {"action": "Two open Dependabot PRs (#46, #59) are now subject to the new angular-typechecker gate; they need a CI re-run to produce their analysis before merging.", "context": "Gate went active this session.", "blocking": false}
  ],
  "decisions": [
    {"decision": "GATE-02 enabled by deleting orphaned Code Scanning configs, NOT by any ci.yml change.", "rationale": "Single-run vs multi-run, head-ref vs merge-ref, and 'roadmap #847 limitation' were ALL red herrings (disproven). The gate matches (analysis_key, category, environment) tuples; the orphaned angular-typechecker config on main could never be reproduced -> permanent 'not found'. Delete-orphans is the documented fix (community #153284, github/codeql #18506, microsoft/hve-core #248).", "phase": "36"},
    {"decision": "Left the gate active on main (CodeQL + angular-typechecker) rather than rolling back.", "rationale": "User chose 'leave the gate active (GATE-02 done)' after it was proven working on both planning-only (#64) and code (#65) PRs. main-ruleset change was explicitly user-authorized this session.", "phase": "36"},
    {"decision": "My earlier experimental single-run change to main (41ac49a) was reverted (44e4306).", "rationale": "It reversed Phase-34's multi-run design and its gate-motivation turned out moot (single-run was a red herring). Reverted per user decision.", "phase": "36"}
  ],
  "uncommitted_files": [],
  "next_action": "Start with remaining task 4 (Phase 36 completion, now unblocked): flip 36-UAT.md test 2 to passed with the gate-active evidence, then re-verify -> phase.complete 36. In parallel/after, land the AGENTS.md runbook update + spike-012 docs via a planning PR (main is PR-only). See task #18 in the session task list.",
  "context_notes": "EPIC session. Milestone v0.2.4 merged. The big arc was GATE-02: I initially (wrongly) concluded the gate was unviable after many failed probes, but the user's persistence + two UI observations (the (analysis_key,category) comparison + 'configuration not found' being transient-for-live/permanent-for-orphaned) led to the real root cause: an orphaned angular-typechecker config on main from a category rename. Deleting it made the gate pass with the EXISTING setup. Red herrings burned: single-run SARIF, head-ref upload, 'GitHub limitation'. Gate is now active + proven. main is clean at 44e4306, in sync with origin, ruleset has all 5 rule types + CodeQL+angular-typechecker. All probe PRs (#57,#58,#60,#61,#62,#63,#64,#65) closed + branches deleted. Windows arm64 + Git Bash: git grep/rg, never grep/head; rtk-prefix git/gh. The gate-02 memory was corrected (it had said 'not viable')."
}
