---
phase: 36-code-scanning-gating-scanned-files-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts
autonomous: true
requirements: [GATE-01, GATE-02]

must_haves:
  truths:
    - "GATE-01 (D-02): the `code-scanning` AND `code-scanning-proof` jobs are members of the required `ci` aggregate's `needs[]`, so a real Code Scanning upload/infra failure (or a proof-contract regression) fails the required `ci` check -- reversing the deliberate prior exclusion, precedented by `cve-lite`."
    - "GATE-01 (D-03, closes RESEARCH PITFALLS P7): on a non-fork PR a silent empty/absent angular-typechecker SARIF (`steps.atc-sarif.outputs.produced == 'false'`) FAILS the `code-scanning` job loud (`exit 1`) instead of passing green; a fallow twin assertion guards `steps.fallow-sarif.outputs.produced`. NOT a findings gate (a real type error still writes a valid SARIF -> produced=true -> green)."
    - "GATE-01 (D-03): the assertion NEVER fires on a fork PR or a push event -- its `if:` requires `github.event_name == 'pull_request' && github.event.pull_request.head.repo.fork == false`."
    - "GATE-02 (D-01): the `code-scanning` job is un-path-gated (the `needs.changes.outputs.code != 'false'` `if:` line is removed) so EVERY PR -- including a `.planning/`-only PR -- produces a Code Scanning analysis on the PR ref (the status-check path-skip trick alone does NOT satisfy the ruleset)."
    - "GATE-02 (D-01a): the `code-scanning-proof` job STAYS PR-only + path-gated (`github.event_name == 'pull_request' && needs.changes.outputs.code != 'false'`); on a planning-only PR / push it resolves to `skipped`, which the `ci` aggregate drops -- no deadlock."
    - "D-05: the two stale `DELIBERATELY NOT in the ci aggregate` comments are rewritten (mirroring the `cve-lite` divergence block) so they no longer contradict the new membership, and the `cve-lite` comment's stale reference to `code-scanning` as the additive/never-a-gate job is reconciled."
    - "D-06: CI-only, additive-only vs @0.2.3 -- no `packages/angular-typechecker/src/**` runtime change, no `package.json`/manifest change, no version bump; the drift guard is a test-tier spec."
  artifacts:
    - ".github/workflows/ci.yml (MOD: `ci.needs[]` +code-scanning +code-scanning-proof; `code-scanning` path-gate removed; two `if:`-gated D-03 assertion steps added; three comment blocks rewritten)"
    - "packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts (new `describe('GATE-01/02: Code Scanning jobs are required + un-path-gated', ...)` reusing the private `extractJobLines` helper)"
  key_links:
    - "Drift-guard membership regex MUST anchor on the full list-item line (`/^\\s*code-scanning,\\s*$/m`, `/^\\s*code-scanning-proof,\\s*$/m`), NOT `\\bcode-scanning\\b` -- `code-scanning` is a substring of `code-scanning-proof` (RESEARCH Pitfall 4)."
    - "The D-03 assertion `if:` uses `steps.<id>.outputs.produced == 'false'`; the drift-guard existence check MUST discriminate it from the upload step (which uses `== 'true'`) by anchoring on `produced == 'false'`."
    - "Un-path-gate check MUST be SCOPED to the `code-scanning` job block via `extractJobLines(ci,'code-scanning')` -- `needs.changes.outputs.code` legitimately remains in `code-scanning-proof` and other jobs, so a file-wide grep would be wrong."
    - "Security invariants preserved VERBATIM on the touched jobs: SHA-pinned `upload-sarif@7188fc36... # v4.37.1`, `persist-credentials: false`, `fetch-depth: 0`, job-scoped `contents: read` + `security-events: write`, fork-PR upload gates, NO PR-metadata in any `run:` shell."
---

<objective>
Promote both Code Scanning jobs into the required `ci` aggregate and guarantee an analysis exists on every PR ref, without deadlocking planning-only or fork PRs. Four surgical `.github/workflows/ci.yml` edits (aggregate membership D-02, un-path-gate D-01, the D-03 `produced` fail-loud contract, three comment rewrites D-05) plus one in-repo drift guard that statically locks the wiring so a future regression fails loud in `nx test`.

Purpose: GATE-01 (the dogfood upload succeeding is part of the merge gate) + the CI side of GATE-02 (an analysis exists on every PR ref so GitHub's separate "Require code scanning results" ruleset cannot deadlock a planning-only PR). The two gates are ORTHOGONAL: `ci` membership (D-02) satisfies GATE-01; un-path-gating (D-01) satisfies GATE-02's analysis-existence requirement; the D-03 assertion is load-bearing for BOTH (it turns a silent empty SARIF -- which would pass GATE-01 green AND deadlock the ruleset -- into a loud red job).
Output: a modified `ci.yml` (wiring + assertions + coherent comments) and a new drift-guard `describe` block. No production code, no manifest change, no version bump (D-06). The SARIF -> Code Scanning pipeline is already spike-proven (PR #53) and the `code-scanning-proof` job is already GREEN in real CI (run 29875173270) -- do NOT re-derive it.
</objective>

<artifacts>
This plan produces the following NEW symbols (there are NO new runtime `src/**` symbols, NO barrel change, NO `package.json` change):
- ci.yml step name: `Assert angular-typechecker SARIF was produced (non-fork PR)` (a named step, NOT an `id:` step -- nothing reads its output).
- ci.yml step name: `Assert fallow SARIF was produced (non-fork PR)` (named step).
- Spec `describe` block: `GATE-01/02: Code Scanning jobs are required + un-path-gated` in `ci-e2e-coverage-guard.spec.ts` (reuses the existing private `extractJobLines`; no export added).
</artifacts>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-code-scanning-gating-scanned-files-documentation/36-CONTEXT.md
@.planning/phases/36-code-scanning-gating-scanned-files-documentation/36-RESEARCH.md
@.planning/phases/36-code-scanning-gating-scanned-files-documentation/36-PATTERNS.md
@.planning/phases/36-code-scanning-gating-scanned-files-documentation/36-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit .github/workflows/ci.yml -- aggregate membership (D-02), un-path-gate (D-01), the D-03 produced assertion, and three comment rewrites (D-05)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the WHOLE file being edited: the `code-scanning` job at ~539-613 incl. the path-gate at 541, the `atc-sarif` step id at 570 + `fallow-sarif` at 583, the fork-gated uploads at 600-613; the `code-scanning-proof` job at 654-712 -- LEAVE its `if:` at 656 unchanged; the `ci` aggregate `needs[]` at 730-743 + the Gate step at 746-753 -- LEAVE the Gate body unchanged; the `cve-lite` precedent comment at 354-369; the two `DELIBERATELY NOT` comments at 493-497 and 649-650)
    - .planning/phases/36-code-scanning-gating-scanned-files-documentation/36-PATTERNS.md (## `.github/workflows/ci.yml` -- Edits 1-4: the exact YAML for the needs[] append, the line-541 delete, the two assertion steps, and the three comment rewrites, with the security-invariants-verbatim list)
    - .planning/phases/36-code-scanning-gating-scanned-files-documentation/36-RESEARCH.md (Section 1 needs[] edit + "no Gate-step change", Section 2 the produced==true assertion shape + the fallow leg, Section 3 the exact `if:` to remove + proof stays gated, Section 5 the three comment-block rewrites)
  </read_first>
  <action>
Make FOUR edits to `.github/workflows/ci.yml`. Keep every change ASCII-only (no emoji / box-drawing / em-dash / curly-quotes); use `--` for dashes, straight quotes. Do NOT touch any job other than `code-scanning`, `code-scanning-proof`, `ci`, and the `cve-lite` comment.

Edit 1 -- aggregate membership (D-02, GATE-01). In the `ci:` job's `needs: [ ... ]` list, append two NEW list items after `scoped-name-guard,` and before the closing `]`: `code-scanning,` then `code-scanning-proof,`. Do NOT change `if: always()` or the Gate step body -- it already drops `skipped` from the fail set (`contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled')`), so a path-skipped `code-scanning-proof` on a planning-only PR resolves to `skipped` and does not deadlock (RESEARCH Section 1 confirms NO Gate-step change is needed).

Edit 2 -- un-path-gate the dogfood job (D-01, GATE-02). In the `code-scanning:` job, DELETE exactly the line `if: ${{ needs.changes.outputs.code != 'false' }}` (currently at line 541, directly under `needs: changes`). KEEP the `needs: changes` line (harmless -- it just serializes after the fast `changes` job; the minimal CONTEXT-faithful edit is to remove only the `if:`). Do NOT touch the `code-scanning-proof:` job's `if:` at line 656 -- it STAYS `github.event_name == 'pull_request' && needs.changes.outputs.code != 'false'` (D-01a).

Edit 3 -- the D-03 produced assertion (GATE-01 contract, closes PITFALLS P7). In the `code-scanning:` job, AFTER the `fallow-sarif` step (ends ~593) and BEFORE the `Upload angular-typechecker SARIF` step (~600), add TWO named steps whose exact YAML is in PATTERNS.md Edit 3 / RESEARCH Section 2. Step one `name: Assert angular-typechecker SARIF was produced (non-fork PR)` gated `if: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.fork == false && steps.atc-sarif.outputs.produced == 'false' }}` with a static `run:` body of an `echo "::error::..."` line naming why (a reporter regression / silent empty SARIF would pass this required gate green AND leave the ruleset with no analysis on the PR ref) followed by `exit 1`. Step two `name: Assert fallow SARIF was produced (non-fork PR)`, identical gate but reading `steps.fallow-sarif.outputs.produced == 'false'`, static `echo "::error::..."` + `exit 1`. CRITICAL: pure `if:`-expression gating + a STATIC `echo`/`exit 1` body only -- interpolate NO step output and NO PR metadata into the shell (preserves the top-of-file no-command-injection invariant verbatim). The fork test here (`github.event_name == 'pull_request' && ... fork == false`) fires ONLY on non-fork PRs and uses `produced == 'false'`; do NOT copy the upload step's gate verbatim (it uses `!= 'pull_request' ||` and `== 'true'`, the inverse conditions).

Edit 4 -- three comment rewrites (D-05, GATE-01 rationale). (4a) Rewrite the `code-scanning` `DELIBERATELY NOT in the ci aggregate's needs` block (~493-497) into a `cve-lite`-style rationale (mirror the block at 354-369): state it IS NOW a required `ci` member (GATE-01), reversing the prior exclusion at the maintainer's request; safe because (a) un-path-gated (D-01) so it runs on every PR incl. planning-only, producing an analysis, (b) on a fork PR the upload steps skip but the JOB still returns success, (c) `|| true` on generation means only a real infra break (`npm ci`/`nx build`) fails the job -- which SHOULD fail `ci`, (d) the D-03 `produced` assertion fails the job on a silent empty SARIF (non-fork PR); accepted tradeoff (mirror cve-lite): a Code Scanning outage / infra break can block the merge button -> recover via the branch ruleset `enforcement: disabled` toggle (AGENTS.md "Lockout recovery"). (4b) Rewrite the `code-scanning-proof` `DELIBERATELY NOT ... Promoting it ... is GATE-01 / Phase 36` block (~649-650): state it IS NOW a member (GATE-01 / Phase 36) -- a SARIF -> Code Scanning contract regression (PROOF-02) now fails the required `ci` check; it stays PR-only + path-gated (D-01a), so on a planning-only PR / push it resolves to `skipped`, which the aggregate drops -- no deadlock. (4c) Reconcile the stale clause in the `cve-lite` comment (~356-359) that calls `code-scanning` "the ADDITIVE `code-scanning` job below ... never a merge gate": since `code-scanning` is now ALSO a required gate, adjust that phrasing so the file is internally consistent (e.g. note both are now required merge gates rather than framing cve-lite as diverging FROM an additive code-scanning). Exact prose is your discretion (subject to code review); keep it factual and consistent with the new membership.

PRESERVE VERBATIM on the `code-scanning` and `code-scanning-proof` jobs (do NOT change): the SHA-pinned `github/codeql-action/upload-sarif@7188fc363630916deb702c7fdcf4e481b751f97a # v4.37.1` (add NO new action, NO new SHA), `persist-credentials: false`, `fetch-depth: 0`, the job-scoped `permissions: { contents: read, security-events: write }`, the existing fork-PR upload gate expressions, and the `env`-not-shell PR-data pattern in the proof job's assert step. Add NO new permission.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker && npx prettier --check .github/workflows/ci.yml</automated>
  </verify>
  <done>ci.yml has both jobs in `ci.needs[]`, the `code-scanning` path-gate removed, two `if:`-gated D-03 `produced=='false'` assertion steps, and three reconciled comment blocks; existing guards (GUARD-01/01b/01f) still pass and the file is prettier-clean. Task 2's new drift guard locks the semantics.</done>
  <acceptance_criteria>
    - `ci.needs[]` contains both `code-scanning` and `code-scanning-proof` as list items; the `if: always()` and Gate step body are byte-unchanged.
    - The `code-scanning` job has NO `needs.changes.outputs.code` path-gate `if:`; `needs: changes` is retained; the `code-scanning-proof` job's `if:` is byte-unchanged (`github.event_name == 'pull_request' && needs.changes.outputs.code != 'false'`).
    - The `code-scanning` job contains two named assertion steps whose `if:` is `github.event_name == 'pull_request' && github.event.pull_request.head.repo.fork == false && steps.atc-sarif.outputs.produced == 'false'` (and the `fallow-sarif` twin), each with a static `echo "::error::..."` + `exit 1` body and NO interpolated step output / PR metadata.
    - Both `DELIBERATELY NOT in the ci aggregate` comments are rewritten to reflect the new membership, and the `cve-lite` comment no longer describes `code-scanning` as additive / never-a-merge-gate.
    - No new action / SHA / permission added; `persist-credentials: false`, `fetch-depth: 0`, and the fork-PR upload gates are unchanged.
    - `npx nx test angular-typechecker` (existing guards) and `npx prettier --check .github/workflows/ci.yml` both pass; `git diff` touches ONLY `.github/workflows/ci.yml`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend ci-e2e-coverage-guard.spec.ts with the GATE-01/02 drift guard (reuse extractJobLines)</name>
  <files>packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts (the WHOLE file: the private `extractJobLines(ci, jobName)` slicer at ~110-133 to REUSE directly, and GUARD-01f at ~433-481 -- the exact precedent that reads the `ci` block and the `^(?!\s*#)` no-comment-line anchor idiom; note the `readFileSync(join(workspaceRoot, '.github','workflows','ci.yml'),'utf8')` pattern)
    - .planning/phases/36-code-scanning-gating-scanned-files-documentation/36-PATTERNS.md (## `ci-e2e-coverage-guard.spec.ts` -- the list-item-anchored membership regex, the un-path-gate assertion, and the CRITICAL substring-trap note)
    - .planning/phases/36-code-scanning-gating-scanned-files-documentation/36-VALIDATION.md (Wave 0 Requirements -- the three drift-guard assertions)
    - .github/workflows/ci.yml (the post-Task-1 state this guard asserts against)
  </read_first>
  <action>
Append a new `describe('GATE-01/02: Code Scanning jobs are required + un-path-gated', ...)` block to `packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts`. Read `ci` at the top of the describe with the same `readFileSync(join(workspaceRoot, '.github', 'workflows', 'ci.yml'), 'utf8')` pattern GUARD-01f uses, and REUSE the in-file private `extractJobLines` directly (it is in scope in the same module -- no export, no new helper, no new dependency; ponytail: reuse). Add a header comment naming GATE-01/GATE-02 and what silently breaks if each assertion regresses. Write these `it` assertions, each with a fail-loud LOCATED message:

1. Membership (GATE-01, D-02): over `extractJobLines(ci, 'ci').join('\n')`, assert `/^\s*code-scanning,\s*$/m` is true AND `/^\s*code-scanning-proof,\s*$/m` is true. Do NOT use `\bcode-scanning\b` -- `code-scanning` is a substring of `code-scanning-proof`, so the word-boundary form would match BOTH and pass even if only the proof job were listed (RESEARCH Pitfall 4). Anchor on the full list-item line.

2. Un-path-gate (GATE-02, D-01): over `extractJobLines(ci, 'code-scanning').join('\n')`, assert `/^(?!\s*#).*if:\s*\$\{\{\s*needs\.changes\.outputs\.code/m.test(block)` is `false` (the `^(?!\s*#)` prefix excludes comment lines -- the block's own rewritten comments may mention the token). SCOPE this to the `code-scanning` block via `extractJobLines` -- `needs.changes.outputs.code` legitimately remains in `code-scanning-proof` and other jobs, so a file-wide check would be wrong.

3. D-03 assertion step exists + non-fork-PR gated (GATE-01, D-03): over `extractJobLines(ci, 'code-scanning').join('\n')`, assert a step gate is present by anchoring on the DISCRIMINATOR `steps.atc-sarif.outputs.produced == 'false'` (regex e.g. `/github\.event\.pull_request\.head\.repo\.fork == false[\s\S]*?steps\.atc-sarif\.outputs\.produced == 'false'/m` on the joined block, OR a `.some(...)` over the block lines matching an `if:` line that contains BOTH `fork == false` and `produced == 'false'`). Anchor on `produced == 'false'` (NOT just `fork == false` + `produced`) so the assertion does NOT false-match the UPLOAD step (which uses `produced == 'true'`). Give the message: a dropped/weakened D-03 assertion re-opens the P7 fail-open (a silent empty SARIF passes the now-required gate green and deadlocks the ruleset).

Anti-vacuous-green: `extractJobLines` throws on a missing job, so a deleted `code-scanning`/`ci` job makes the describe fail loud rather than pass silently -- rely on that (mirror GUARD-01f). Do NOT add a YAML parser or any new dependency.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker</automated>
  </verify>
  <done>`npx nx test angular-typechecker` passes including the new GATE-01/02 describe block, which statically locks: both jobs in `ci.needs[]` (list-item anchored), `code-scanning` un-path-gated, and the D-03 `produced=='false'` non-fork-PR assertion step present.</done>
  <acceptance_criteria>
    - A new `describe('GATE-01/02: Code Scanning jobs are required + un-path-gated', ...)` exists in `ci-e2e-coverage-guard.spec.ts` and reuses the private `extractJobLines` (no export, no new helper/dependency).
    - Membership assertions use `/^\s*code-scanning,\s*$/m` and `/^\s*code-scanning-proof,\s*$/m` (list-item anchored), NOT `\bcode-scanning\b`.
    - The un-path-gate assertion is scoped to `extractJobLines(ci,'code-scanning')` and asserts the non-comment `needs.changes.outputs.code` `if:` regex returns `false`.
    - The D-03 existence assertion anchors on `produced == 'false'` (so it does not false-match the upload step's `produced == 'true'`).
    - `npx nx test angular-typechecker` passes; `npx nx run-many -t lint` (maxWarnings:0) stays green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fork PR head repo -> CI runner | The now-un-path-gated `code-scanning` job runs `npm ci` + `nx build` on every PR (incl. forks); untrusted PR code/metadata crosses here. |
| GitHub Actions expression engine -> `run:` shell | The two new D-03 assertion steps must gate via a pure `if:` expression only; nothing may be interpolated into the shell body. |
| committed `ci.yml` -> required `main` merge gate | A tampered or silently-regressed gate weakens `main` protection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-01 | Tampering | `ci.needs[]` membership / `code-scanning` path-gate | medium | mitigate | The Task-2 Wave-0 drift guard statically asserts both jobs stay in `ci.needs[]` (list-item anchored) and `code-scanning` stays un-path-gated -- a silent workflow regression fails loud in `nx test`. |
| T-36-02 | Tampering / Elevation (command injection) | the two new D-03 assertion steps | high | mitigate | Pure `if:`-expression gating + a STATIC `echo "::error::..."` / `exit 1` body; NO step output and NO PR metadata interpolated into any shell (top-of-file no-command-injection invariant preserved verbatim). |
| T-36-03 | Elevation of Privilege | un-path-gating runs `npm ci` + `nx build` on planning-only / fork PRs | low | accept | Same trusted `pull_request` code-checkout trigger the rest of the file uses -- no privileged sibling trigger, no new secret, fork-PR upload still skipped, job-scoped `contents: read` + `security-events: write` unchanged; accepted cost documented in D-01. |
| T-36-SC | Tampering (supply chain) | actions / packages | low | accept | No package install and no new marketplace action this plan; the existing `upload-sarif@7188fc363630916deb702c7fdcf4e481b751f97a # v4.37.1` SHA pin + Dependabot coverage are reused verbatim. No `[ASSUMED]`/`[SUS]` package -> no legitimacy checkpoint needed. |
</threat_model>

<verification>
- `npx nx test angular-typechecker` green -- existing GUARD-01/01b/01c/01d/01e/01f PLUS the new GATE-01/02 drift guard.
- `npx prettier --check .github/workflows/ci.yml` green (ci.yml stays Prettier-clean; the `format-lint` CI job is the authoritative gate).
- Authoritative workflow validation is the CI `lint-workflows` (actionlint) + `act-compat` (`act --validate` + `act -n` per trigger) jobs on this phase's own PR; run their local equivalents (`./actionlint`, `bash tools/act/act-compat.sh`) if `act`/`actionlint` are installed locally.
- `git diff --stat` touches ONLY `.github/workflows/ci.yml` + `packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts` -- no `src/**` runtime code, no `package.json`/manifest (additive-only, D-06); no version bump.
- REAL-CI-ONLY Nyquist point (NOT locally verifiable, do not claim it is): the required `ci` aggregate goes RED on a genuine Code Scanning upload/infra failure (or a proof-contract regression) and GREEN on a clean PR, with `code-scanning` + `code-scanning-proof` as required members. Provable only on this phase's own PR run (GitHub ingestion + the aggregate verdict); Phase 35 proof baseline run 29875173270 confirms the proof job lands + asserts.
</verification>

<success_criteria>
- `code-scanning` and `code-scanning-proof` are members of the required `ci` aggregate's `needs[]`, reversing the prior exclusion, precedented by `cve-lite` (GATE-01, D-02).
- On a non-fork PR a silent empty/absent angular-typechecker (and fallow) SARIF fails the `code-scanning` job loud; the assertion never fires on fork PRs or push, and a real type error still uploads and stays green (GATE-01, D-03; closes PITFALLS P7).
- The `code-scanning` job is un-path-gated so every PR -- including a `.planning/`-only PR -- produces a Code Scanning analysis; the proof job stays PR-only + path-gated (GATE-02, D-01/D-01a).
- The three comment blocks are internally consistent with the new membership (D-05); the drift guard statically locks all of the above.
- No production surface changed; no version bump (D-06).
</success_criteria>

<output>
Create `.planning/phases/36-code-scanning-gating-scanned-files-documentation/36-01-SUMMARY.md` when done.
</output>
