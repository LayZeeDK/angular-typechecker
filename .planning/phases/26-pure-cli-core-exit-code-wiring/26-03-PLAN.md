---
phase: 26-pure-cli-core-exit-code-wiring
plan: 03
type: execute
wave: 3
depends_on: [26-02]
files_modified:
  - packages/angular-typechecker/src/cli/main.integration.spec.ts
autonomous: true
requirements: [CLI-02, EXIT-01, PKG-03, VER-02]

must_haves:
  truths:
    - "run(argv) end-to-end against the real cold compiler returns 0 on a clean fixture; 1 on a planted TS error, a planted template/NG8xxx error, a real coverage-incomplete fixture, a --max-warnings 0 warning fixture, and a --strict warning fixture; 2 on a malformed tsconfig and on a nonexistent tsconfig"
    - "a single solution --tsConfig walks references (solution-walk path) and a two-entry --tsConfig unions (multi-tsconfig path) -- the single is a string, the pair is a string[]"
    - "a planted error code appears in the returned stdout (the report), not stderr"
    - "a relative -c resolved from a non-root CWD yields the same verdict as the canonical absolute path (path normalization / the realpath ENOENT guard proven)"
    - "Locked CONTEXT.md decisions covered by this plan: D-06, D-17"
  artifacts:
    - path: "packages/angular-typechecker/src/cli/main.integration.spec.ts"
      provides: "VER-02 real-cold-compiler end-to-end coverage of run(argv) against committed fixtures/"
      contains: "findWorkspaceRoot"
  key_links:
    - from: "packages/angular-typechecker/src/cli/main.integration.spec.ts"
      to: "./main run()"
      via: "in-process call (NO spawn, NO tarball); absolute fixture tsconfig paths via findWorkspaceRoot + join"
      pattern: "from './main'"
    - from: "packages/angular-typechecker/src/cli/main.integration.spec.ts"
      to: "fixtures/config-broken (nonexistent path)"
      via: "a --tsConfig that resolves to nothing proves the realpathSync.native ENOENT guard -> exit 2"
      pattern: "does-not-exist"
---

<objective>
Author the VER-02 integration spec: exercise run(argv) end-to-end IN-PROCESS
(no spawn, no tarball) against the committed real-compiler fixtures/, on the same
LEAN 6-cell OS x Node matrix, driving the real cold @angular/compiler-cli through
the CJS->ESM await import() bridge. This is the only tier that can prove the
correctness the unit stubs cannot fake: a REAL coverage-incomplete run exiting 1,
and a nonexistent/malformed tsconfig exiting 2 through the realpath ENOENT guard.

Purpose: The unit tier (Plan 26-02) proves the compose logic against stubs; VER-02
proves run() produces the SAME verdict and diagnostics as the executor over real
fixtures -- the CLI-02 charter -- and that the D-06 realpath guard returns 2 rather
than throwing.

Output: main.integration.spec.ts under packages/angular-typechecker/src/cli/.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-pure-cli-core-exit-code-wiring/26-CONTEXT.md
@.planning/phases/26-pure-cli-core-exit-code-wiring/26-RESEARCH.md
@.planning/phases/26-pure-cli-core-exit-code-wiring/26-PATTERNS.md
@packages/angular-typechecker/src/cli/main.ts

<interfaces>
<!-- The fixture-driven real-compiler integration pattern to mirror. -->

From packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts (the analog):
```typescript
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';
import { describe, expect, it } from 'vitest';
import { findWorkspaceRoot } from '@workspace/test-util';

const workspaceRoot = findWorkspaceRoot(dirname(fileURLToPath(import.meta.url)));
const fixtureDir = join(workspaceRoot, 'fixtures', 'gate-b-error');
const appTsConfig = join(fixtureDir, 'tsconfig.app.json');
```
- File name MUST be *.integration.spec.ts so it routes to the `integration` target (real cold compiler),
  NOT the unit `test` target. Both dependsOn: build.
- run() resolves a RELATIVE --tsConfig against process.cwd() (D-05), so pass ABSOLUTE fixture tsconfig
  paths built via join(workspaceRoot, 'fixtures', ...), EXCEPT the one PKG-03 case that overrides cwd and
  passes a relative -c.
- Angular extended codes are encoded negative: assert NG8xxx via a helper like -990000 - code, never the
  bare 8xxx (the analog's NG() helper). Raw TS codes are positive.

VERIFIED fixtures available under top-level fixtures/ (no new fixtures to create):
- clean -> 0:                       fixtures/ts-baseline  OR  fixtures/not-type-checked-clean
- planted TS error -> 1:            fixtures/gate-b-error/tsconfig.app.json (TS2322)  OR  fixtures/ng-baseline
- template / NG8xxx error -> 1:     fixtures/gate-b-error (NG8109 in the same program as TS2322)
- real coverage-incomplete -> 1:    fixtures/solution-style-empty (zero-root-names leaf)
- --max-warnings 0 -> 1:            a REPORTED in-project NG8xxx-warning fixture (an fixtures/extended-*:
                                    warningCount > 0, suppressedInGraphWarningCount == 0)
- --strict -> 1:                    --strict ALONE fails ONLY on a DROPPED in-graph warning
                                    (evaluate-result.ts:176 fires when suppressedInGraphWarningCount > 0); a
                                    plain extended-* has suppressedInGraphWarningCount == 0, so --strict alone
                                    leaves it clean -> exit 0. NO committed fixture drops an in-graph WARNING
                                    (every *.integration.spec.ts asserts suppressedInGraphWarningCount == 0;
                                    only unit specs synthesize it via literals). Drive this row by combining
                                    --strict WITH --max-warnings 0 on the SAME extended-* reported-warning
                                    fixture -> exit 1 (warnings gate); do NOT weaken the assert exitCode 1.
                                    The strict-alone dropped-warning path is already unit-proven in
                                    evaluate-result.spec.ts.
- multi --tsConfig union:           fixtures/multi-tsconfig-array (tsconfig.app.json + tsconfig.spec.json)
- single --tsConfig solution-walk:  fixtures/solution-style (a tsconfig.json solution + leaves)
- malformed tsconfig -> 2:          fixtures/config-broken/tsconfig.malformed.json (PRESENT)
- nonexistent tsconfig -> 2:        a path that resolves to nothing, e.g.
                                    fixtures/config-broken/tsconfig.does-not-exist.json -- NO fixture file
                                    exists or is to be created; nonexistence IS the case (proves the D-06
                                    realpath guard / Open Question 1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: main.integration.spec.ts -- VER-02 end-to-end run(argv) against real fixtures</name>
  <files>packages/angular-typechecker/src/cli/main.integration.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/cli/main.ts (run() -- the module under test, from Plan 26-02)
    - packages/angular-typechecker/src/core/run-typecheck.integration.spec.ts (the EXACT findWorkspaceRoot + fixtures/ + real-compiler pattern; the NG() negative-code helper; the fileName .replace(/\\/g,'/') normalization discipline)
    - packages/angular-typechecker/src/core/evaluate-result.ts (why solution-style-empty is coverage-incomplete: a zero-root-names skipped reference -> success:false; and why --max-warnings 0 on a REPORTED NG8xxx-warning fixture -> success:false, whereas --strict ALONE fails only on a DROPPED in-graph warning where suppressedInGraphWarningCount > 0 -- read the strict branch at evaluate-result.ts:176)
    - .planning/phases/26-pure-cli-core-exit-code-wiring/26-VALIDATION.md (the VER-02 integration-tier rows + fixtures this spec must satisfy)
    - .planning/phases/26-pure-cli-core-exit-code-wiring/26-PATTERNS.md (the fixture table + the discrepancy note: tsconfig.does-not-exist.json is NOT on disk -- do not create it)
  </read_first>
  <action>
    Create main.integration.spec.ts (name MUST end .integration.spec.ts). Import { describe, expect, it } from
    'vitest', findWorkspaceRoot from '@workspace/test-util', run from './main', and node:path (dirname, join) +
    node:url (fileURLToPath). Compute workspaceRoot = findWorkspaceRoot(dirname(fileURLToPath(import.meta.url)))
    and build ABSOLUTE fixture tsconfig paths via join(workspaceRoot, 'fixtures', ...). Add a negative-code
    helper (e.g. NG(code) => -990000 - code) for NG8xxx assertions; raw TS codes are positive. Call run([...])
    IN-PROCESS (NO spawn, NO tarball) and assert on the returned { exitCode, stdout, stderr }. Cover every
    VER-02 row from the interfaces block: (1) clean fixture -> exitCode 0; (2) planted TS error fixture ->
    exitCode 1 with the TS code string present in stdout (the report), asserting it is NOT in stderr;
    (3) template/NG8xxx error fixture -> exitCode 1 with the NG-encoded code present in stdout;
    (4) fixtures/solution-style-empty -> exitCode 1 (REAL coverage-incomplete -- errorCount 0 but success false;
    a case no unit stub can fake); (5) a REPORTED-warning fixtures/extended-* NG8xxx fixture (warningCount > 0,
    suppressedInGraphWarningCount == 0) with --max-warnings 0 -> exitCode 1; then, because --strict ALONE
    fails only on a DROPPED in-graph warning (evaluate-result.ts:176) and NO committed fixture drops an
    in-graph warning, drive the --strict row by combining --strict WITH --max-warnings 0 on that SAME
    extended-* fixture -> exitCode 1 (do NOT expect --strict alone on a reported-warning fixture to exit 1;
    never weaken the assert exitCode 1 to make it pass); (6) fixtures/multi-tsconfig-array passed as
    TWO -c entries -> the union path, assert the verdict/exit code; (7) fixtures/solution-style passed as a
    SINGLE -c -> the solution-walk path (assert it does NOT record a zero-root-names skip that a wrongly-passed
    one-element array would cause -- proves ARGS-03 collapse end-to-end); (8) fixtures/config-broken/
    tsconfig.malformed.json -> exitCode 2, and a NONEXISTENT path (e.g. join(fixtureDir,
    'tsconfig.does-not-exist.json') -- do NOT create the file) -> exitCode 2 with stderr mentioning the
    compiler failed to run (proves the realpath ENOENT guard returns 2, never an uncaught throw); (9) PKG-03:
    invoke run with a RELATIVE -c resolved from a non-root cwd (temporarily set process.cwd via a
    beforeEach/afterEach chdir to the fixture's parent, or pass a relative path the resolver joins against
    process.cwd()) and assert the SAME verdict as the canonical absolute-path invocation. Use exact matchers
    (toBe / toContain / not.toContain); no subjective language. Restore any chdir in afterEach. Project TS
    style; ASCII-only.
  </action>
  <verify>
    <automated>nx run angular-typechecker:integration</automated>
  </verify>
  <acceptance_criteria>
    - `nx run angular-typechecker:integration` passes including main.integration.spec.ts.
    - The clean fixture asserts exitCode 0; the planted TS, template/NG8xxx, coverage-incomplete, --max-warnings 0, and --strict cases each assert exitCode 1.
    - fixtures/solution-style-empty asserts exitCode 1 with errorCount-independent success:false (real coverage-incomplete).
    - The malformed tsconfig AND the nonexistent path each assert exitCode 2; the nonexistent case asserts stderr mentions the compiler failed to run (realpath guard proven).
    - A single-solution -c walks references (no zero-root-names skip) and a two-entry -c unions.
    - A planted diagnostic code is asserted present in stdout via toContain and absent from stderr via not.toContain.
    - A relative -c from a non-root cwd asserts the same exitCode as the canonical absolute-path run; any chdir is restored in afterEach.
  </acceptance_criteria>
  <done>nx run angular-typechecker:integration passes including main.integration.spec.ts; every VER-02 case above has a passing assertion; the REAL coverage-incomplete fixture asserts exitCode 1 (errorCount 0, success false); the malformed AND nonexistent tsconfig cases both assert exitCode 2 (the nonexistent case proves the realpath guard); the single-solution vs two-entry --tsConfig cases assert the correct path is taken; a planted code is asserted present in stdout and absent from stderr.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| argv (fixture tsconfig paths) -> run() | The spec drives run() with fixture-derived tsconfig paths; VER-02 exercises the untrusted-path handling end-to-end. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02 | Denial of Service | run() realpath ENOENT guard (verified here) | mitigate | The nonexistent-tsconfig VER-02 case is the live proof that run() returns exitCode 2 (never an uncaught throw) when the path does not exist -- the guard from Plan 26-02 is verified end-to-end. |
| T-26-04 | Information Disclosure | run() stdout/stderr routing (verified here) | mitigate | The planted-error cases assert the diagnostic code lands in stdout (the report) and NOT in stderr -- the CLI-03 routing contract is proven against the real compiler. |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | ZERO new runtime/dev dependencies this plan (test-only spec; real @angular/compiler-cli + typescript are already installed workspace peers). Nothing to audit. |
</threat_model>

<verification>
- nx run angular-typechecker:integration passes including main.integration.spec.ts (all VER-02 rows).
- nx test angular-typechecker still passes (no unit regression) and nx build angular-typechecker succeeds.
- The nonexistent-tsconfig case asserts exitCode 2 (realpath guard proven); the real coverage-incomplete fixture asserts exitCode 1.
- nx lint angular-typechecker passes at maxWarnings:0; nx format:check clean.
</verification>

<success_criteria>
- run(argv) end-to-end returns the correct literal exit code across every VER-02 case: clean -> 0; planted TS/template/NG8xxx, real coverage-incomplete, --max-warnings 0, --strict -> 1; malformed AND nonexistent tsconfig -> 2.
- The single-solution --tsConfig walks references and the two-entry --tsConfig unions (ARGS-03 proven end-to-end).
- A planted diagnostic code is present in stdout and absent from stderr (CLI-03 routing proven).
- A relative -c from a non-root CWD yields the same verdict as the canonical absolute path (PKG-03 / realpath normalization proven).
</success_criteria>

<output>
Create `.planning/phases/26-pure-cli-core-exit-code-wiring/26-03-SUMMARY.md` when done.
</output>
