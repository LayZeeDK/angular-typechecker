---
phase: 32-verification-docs-additive-audit
plan: 3
type: execute
wave: 3
depends_on: ['32-01', '32-02']
files_modified:
  - .planning/phases/32-verification-docs-additive-audit/32-ADDITIVE-AUDIT.md
autonomous: true
requirements: [ADD-01]
must_haves:
  truths:
    - "D-05: a git diff angular-typechecker@0.2.2..HEAD scoped to the PUBLISHED surface + the index.drift.ts barrel-drift tsc prove NO breaking change to the Nx executor id (angular-typechecker:typecheck), the runTypecheck/CoreResult/CoreOptions public API (only the new format option + the OPTIONAL totalFilesCount), the Angular CLI builder, the CLI flag set, or the generator schemas; renderReport stays OUT of the public barrel."
    - "D-05: the plugin package.json dependencies gained EXACTLY one entry since @0.2.2 -- node-sarif-builder@^4.1.0 -- and ajv/ajv-formats are ROOT devDependencies only (never the plugin manifest); node-sarif-builder classification is RE-CONFIRMED against the real nx lint (dependency-checks SEES the lazy import; NO ignoredDependencies needed, A1)."
    - "D-05: 32-ADDITIVE-AUDIT.md mirrors the shipped 27-ADDITIVE-AUDIT.md five-section structure and records the ADDITIVE-ONLY verdict with the v0.3.0 escape hatch UNTRIGGERED and the package version staying 0.2.2 (the bump is the later human-gated Release-PR, not this phase)."
  artifacts:
    - .planning/phases/32-verification-docs-additive-audit/32-ADDITIVE-AUDIT.md
  key_links:
    - "Baseline tag angular-typechecker@0.2.2 exists (verified); HEAD is the whole v0.2.3 milestone (Phases 30-32)."
    - "index.drift.ts (rides tsconfig.drift.json under nx typecheck) is the authoritative barrel-unchanged proof."
    - "@nx/dependency-checks (nx lint, maxWarnings:0) is the standing guard that node-sarif-builder is correctly classified with no ignore entry."
---

<objective>
ADD-01: prove the whole v0.2.3 milestone is additive-only vs `angular-typechecker@0.2.2`
and record the verdict in `32-ADDITIVE-AUDIT.md`, mirroring the shipped
`27-ADDITIVE-AUDIT.md` template.

Purpose: the release-gating verdict. The published surface (executor id, public
barrel, builder, CLI flags, generator schemas) must be byte-additive, the ONLY new
runtime dependency across the milestone is `node-sarif-builder`, and the dev-only
`ajv`/`ajv-formats` must NOT have reached the plugin manifest. This is the last
deliverable before verification, so it audits the complete final state after 32-01
+ 32-02 land.

Output: `32-ADDITIVE-AUDIT.md` with the ADDITIVE-ONLY verdict and v0.3.0 untriggered.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-verification-docs-additive-audit/32-CONTEXT.md
@.planning/phases/32-verification-docs-additive-audit/32-RESEARCH.md

# The audit template to mirror + the surfaces to audit:
@.planning/milestones/v0.2.2-phases/27-bin-shell-cross-platform-packaging/27-ADDITIVE-AUDIT.md
@packages/angular-typechecker/src/index.ts
@packages/angular-typechecker/src/index.drift.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the additive-only git-diff + dependency + barrel/lint/typecheck evidence gather</name>
  <files>.planning/phases/32-verification-docs-additive-audit/32-ADDITIVE-AUDIT.md</files>
  <read_first>
    - .planning/milestones/v0.2.2-phases/27-bin-shell-cross-platform-packaging/27-ADDITIVE-AUDIT.md (the five-section format + the git-diff verdict table shape)
    - .planning/phases/32-verification-docs-additive-audit/32-RESEARCH.md ("ADD-01 deep-dive" -- the audited-path table, the dependency-proof commands, the barrel tripwire)
    - packages/angular-typechecker/src/index.ts (the five barrel exports that must stay byte-intact)
  </read_first>
  <action>
    Confirm the baseline: `git tag -l 'angular-typechecker@0.2.2'` returns the tag,
    and capture HEAD sha. Set `BASE=angular-typechecker@0.2.2`. Run
    `git diff "$BASE"..HEAD -- <path>` for EACH published-surface path and record the
    verdict per the RESEARCH audited-path table:
    - `packages/angular-typechecker/src/index.ts` (expect UNCHANGED -- the five exports)
    - `.../src/executors/typecheck/schema.json` + `schema.d.ts` (expect WIDEN-ONLY: the optional `format` enum + `--quiet`/`--color`; pre-existing options intact)
    - `.../src/builders/typecheck/builder.ts` (expect UNCHANGED) + its `schema.json`/`schema.d.ts` (WIDEN-ONLY, mirror the executor)
    - `.../src/core/run-typecheck.ts` (expect ADDITIVE: CoreResult.totalFilesCount OPTIONAL; CoreOptions unchanged)
    - `.../executors.json`, `generators.json`, `builders.json`, `collection.json` (expect UNCHANGED -- executor id angular-typechecker:typecheck intact)
    - `.../src/generators/*/schema.json` (expect UNCHANGED)

    The DEPENDENCY proof (the ADD-01 crux): `git diff "$BASE"..HEAD -- packages/angular-typechecker/package.json`
    must show `dependencies` gained EXACTLY `node-sarif-builder` (nx was already a
    direct dep since v0.2.1). Prove ajv/ajv-formats did NOT reach the plugin manifest
    with the negated-quiet idiom `! git show HEAD:packages/angular-typechecker/package.json | rg -q 'ajv'`
    (this passes when NO `ajv` token is present). Do NOT use `rg -c 'ajv' = 0`: on
    zero matches `rg -c` prints NOTHING and exits 1, so the string compare never sees
    `0` and the gate can never pass in the desired absent state. Confirm ajv/ajv-formats
    appear ONLY in the ROOT package.json devDependencies (the root manifest is private,
    never published).

    Net-new confirmation for the reporter modules:
    `git ls-tree -r "$BASE" -- packages/angular-typechecker/src/core/sarif-report.ts`
    (and json-report.ts / diagnostic-record.ts / extended-catalog*.ts) returns 0 files
    at the tag (additive by construction).

    Re-confirm the standing guards green NOW: `nx typecheck angular-typechecker` (runs
    tsconfig.drift.json -> the index.drift.ts barrel-unchanged proof) and
    `nx lint angular-typechecker` at maxWarnings:0 (dependency-checks: node-sarif-builder
    correctly classified, NO ignoredDependencies entry needed, A1). Capture these
    results as evidence for the audit doc; do NOT re-litigate the classification
    (resolved in Phase 31).
  </action>
  <verify>
    <automated>git tag -l 'angular-typechecker@0.2.2' | rg -q 'angular-typechecker@0.2.2' &amp;&amp; ! git show HEAD:packages/angular-typechecker/package.json | rg -q 'ajv' &amp;&amp; npx nx typecheck angular-typechecker &amp;&amp; npx nx lint angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - The baseline tag resolves; every audited published-surface path has a recorded git-diff verdict (UNCHANGED / WIDEN-ONLY / ADDITIVE) matching the RESEARCH table.
    - `git show HEAD:packages/angular-typechecker/package.json` contains no `ajv` token (the negated `! ... | rg -q 'ajv'` grep passes); the plugin `dependencies` addition since @0.2.2 is exactly `node-sarif-builder`.
    - `nx typecheck angular-typechecker` (barrel drift) and `nx lint angular-typechecker` (dependency-checks) are green.
  </acceptance_criteria>
  <done>All published-surface git-diff verdicts, the single-dependency proof (node-sarif-builder only; ajv not in the plugin manifest), the net-new reporter-module confirmation, and the barrel/lint green state are captured as audit evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Author 32-ADDITIVE-AUDIT.md (the ADDITIVE-ONLY verdict)</name>
  <files>.planning/phases/32-verification-docs-additive-audit/32-ADDITIVE-AUDIT.md</files>
  <read_first>
    - .planning/milestones/v0.2.2-phases/27-bin-shell-cross-platform-packaging/27-ADDITIVE-AUDIT.md (mirror its five sections + prose style)
  </read_first>
  <action>
    Write `32-ADDITIVE-AUDIT.md` mirroring the 27-template's FIVE sections, ASCII-only
    (no em/en dashes, no unicode):
    1. Header + Verdict: baseline `angular-typechecker@0.2.2`, HEAD sha, scope = the
       whole v0.2.3 milestone (Phases 30-32), method = standing-guard cross-check +
       `git diff angular-typechecker@0.2.2..HEAD` per published path + git ls-tree
       net-new. Verdict: ADDITIVE-ONLY HOLDS; v0.3.0 NOT triggered; version stays 0.2.2.
    2. Guard cross-check map: the standing guards (index.drift.ts barrel tripwire,
       executor/builder/generator schema-parity specs, nx-surface-regression,
       @nx/dependency-checks) all present + green in this phase's nx test / nx typecheck
       / nx lint runs, plus the new VER-02 integration + VER-03 e2e proofs from 32-01/32-02.
    3. Git-diff verdict per audited path: the table from Task 1's evidence (UNCHANGED /
       WIDEN-ONLY / ADDITIVE per path), including the dependency-diff row proving ONLY
       node-sarif-builder was added and ajv/ajv-formats are root devDeps only.
    4. New-file additions (additive by construction): the src/core/{json-report,
       sarif-report,diagnostic-record,extended-catalog}.ts set + their specs were 0
       files at @0.2.2 (git ls-tree proof).
    5. ADD-01 disposition: no executor-id break, no barrel break, no builder break, no
       schema break; only additive changes + the ONE new runtime dependency
       (node-sarif-builder); charter satisfied; v0.3.0 escape hatch UNTRIGGERED;
       version stays 0.2.2 (the bump is the later human-gated Release-PR flow).
    Do NOT bump package.json, do NOT tag, do NOT publish (landmine #2).
  </action>
  <verify>
    <automated>test -f .planning/phases/32-verification-docs-additive-audit/32-ADDITIVE-AUDIT.md &amp;&amp; rg -q 'ADDITIVE-ONLY' .planning/phases/32-verification-docs-additive-audit/32-ADDITIVE-AUDIT.md &amp;&amp; rg -q 'node-sarif-builder' .planning/phases/32-verification-docs-additive-audit/32-ADDITIVE-AUDIT.md &amp;&amp; rg -qi 'v0.3.0' .planning/phases/32-verification-docs-additive-audit/32-ADDITIVE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `32-ADDITIVE-AUDIT.md` exists with all five sections; it states ADDITIVE-ONLY HOLDS and v0.3.0 UNTRIGGERED.
    - The dependency section names node-sarif-builder as the ONLY added runtime dependency and states ajv/ajv-formats are root devDeps only.
    - The doc is ASCII-only, carries no work email or bare work domain, and does not claim any version bump/tag/publish happened.
  </acceptance_criteria>
  <done>32-ADDITIVE-AUDIT.md records the milestone's additive-only verdict against @0.2.2 in the reviewed five-section format, with the v0.3.0 escape hatch untriggered and the version held at 0.2.2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published npm surface -> consumer | the additive-only audit is the control that no breaking change reaches a patch consumer |
| declared plugin dependencies -> installed graph | only node-sarif-builder may be a new runtime dep across the milestone |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-SC | Tampering | plugin `dependencies` drift | low | mitigate | The audit's dependency-diff (`git diff ...package.json` + `rg -c 'ajv' == 0`) IS the supply-chain control: it proves ONLY node-sarif-builder was added and no dev-only validator leaked into the shipped manifest. |
| T-32-04 | Elevation of privilege / contract break | published executor id / public barrel | low | mitigate | index.drift.ts barrel tsc + schema-parity specs + the git-diff table prove no consumer-observable contract narrowed/renamed (additive-only). |

No high-severity threats: this plan writes an audit document over a frozen published
surface; it adds no code, no dependency, no network surface.
</threat_model>

<verification>
Per the validation contract (landmine #4):
- `npx nx typecheck angular-typechecker` (index.drift.ts barrel-unchanged proof)
- `npx nx lint angular-typechecker` at `maxWarnings:0` (dependency-checks re-confirmation)
- `npx nx test angular-typechecker` (standing surface-regression + schema-parity specs)
- `npx nx format:check`
- the git-diff / git ls-tree evidence commands from Task 1
</verification>

<success_criteria>
- Every published-surface path has a git-diff verdict; the plugin gained exactly one runtime dependency (node-sarif-builder) since @0.2.2.
- ajv/ajv-formats are proven root devDeps only (not the plugin manifest).
- 32-ADDITIVE-AUDIT.md records ADDITIVE-ONLY HOLDS, v0.3.0 untriggered, version stays 0.2.2, in the five-section format.
- Barrel drift tsc + dependency-checks + surface-regression specs all green.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `.planning/phases/32-verification-docs-additive-audit/32-ADDITIVE-AUDIT.md` -- the ADD-01 additive-only verdict vs `angular-typechecker@0.2.2` (five-section, mirrors 27-ADDITIVE-AUDIT.md).
</artifacts>

<output>
Create `.planning/phases/32-verification-docs-additive-audit/32-03-SUMMARY.md` when done.
</output>
