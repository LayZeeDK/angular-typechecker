---
phase: 20-vite-analog-storybook-query-import-guidance-vite-client-read
plan: 05
type: execute
wave: 4
depends_on: [20-04]
files_modified: []
autonomous: false
requirements: [SB-09]

must_haves:
  truths:
    - "A locally-packed dist tarball is produced from the built package (nx build angular-typechecker -> npm pack on the dist output, NOT the published npm artifact)"
    - "Installed into a real radix-ng/primitives checkout, the bundlerQueryImports advisory FIRES on the unresolved ?query TS2307 and the run FAILs on them (never suppressed)"
    - "Adding \"types\": [\"vite/client\"] to the checked tsconfig drives the ?query TS2307 to 0 (self-gated); a plain missing module STILL FAILs TS2307"
    - "Gate B is verified INTERACTIVELY by the human; merging/releasing remain out of scope (D-10/D-11)"
  artifacts: []
  key_links:
    - from: "the built dist"
      to: "a local tarball"
      via: "npm pack on the dist output (not the published artifact)"
      pattern: "npm pack"
    - from: "the tarball installed into radix-ng"
      to: "the bundlerQueryImports advisory + vite/client resolution"
      via: "a typecheck target at apps/radix-storybook/.storybook/tsconfig.json"
      pattern: "vite/client"
---

<objective>
Gate B (user-added HARD phase-completion gate, D-10, REQUIRED + manual/interactive): verify the SB-09
behavior with the LOCALLY-PACKED dist tarball installed into a real `radix-ng/primitives` checkout --
the proven `?query` repo (spike 009: 227 -> 0). Confirm the advisory fires on the unresolved `?query`
TS2307, `"types": ["vite/client"]` drives them to 0, and a plain missing module still fails. This is
NOT baked into CI (an external large checkout) and is NOT self-approved by the autonomous chain -- a
human runs and confirms it.

Purpose: prove the shipped-shape artifact behaves on a real off-repo Vite/Analog Storybook project, not
just the hermetic in-repo fixture -- the elevated (Phase 19 D-08 -> required here) real-OSS verify.

Output: a confirmed Gate B result. This plan STOPS at verification: it does NOT merge the PR, cut or
publish any release, or approve any environment (D-11). Depends on 20-04 (branch pushed + green CI).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-vite-analog-storybook-query-import-guidance-vite-client-read/20-CONTEXT.md
@.planning/phases/20-vite-analog-storybook-query-import-guidance-vite-client-read/20-RESEARCH.md
@.planning/spikes/009-vite-ambient-shim-resolves-query-imports/README.md

<interfaces>
<!-- Gate B mechanics (from RESEARCH + MEMORY + spike 009). -->
// - Target repo: radix-ng/primitives (Angular 22.0.2 / Nx 23.1.0-beta.1 / TS 6.0.3 /
//     @storybook/angular 10.4.6 via @analogjs/storybook-angular, pnpm 11, MIT). vite present.
// - Tarball, NOT the npm artifact: `npx nx build angular-typechecker` -> `npm pack` on the DIST output
//     dir (the built package root), so the tarball ships the built .js (the 0.1.1 packageRoot fix).
// - Install into a THROWAWAY radix-ng clone; wire a `typecheck` target at
//     apps/radix-storybook/.storybook/tsconfig.json (spike 009 real-repo leg).
// - pnpm workspace install gotcha: nx add can hit ERR_PNPM_IGNORED_BUILDS -> use the
//     allowBuilds / `--ignore-scripts` workaround (MEMORY [[nx-add-fails-on-pnpm-workspaces]],
//     [[oss-real-repo-verification]]); tar needs MSYS `/d/` paths, not `D:/`.
// - Acceptance (spike 009): baseline 227 ?query TS2307 -> 0 with `"types": ["vite/client"]`; the 1
//     pre-existing plain missing module survives; a freshly planted `import x from './definitely-not-here'`
//     still errors TS2307.
// - NOT committed; the radix-ng checkout stays external.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + pack the local dist tarball</name>
  <files></files>
  <read_first>
    - MEMORY [[angular-typechecker-npm-releases-ship-source]] (0.1.1 packageRoot fix -- the tarball must ship built .js; pack the DIST, not the source root)
    - .planning/spikes/009-vite-ambient-shim-resolves-query-imports/README.md (the real-repo leg: build -> pack -> install the tarball, wire the typecheck target)
    - AGENTS.md `## Parallel execution in git worktrees` (Gate B runs on the main checkout; no worktree)
  </read_first>
  <action>
    On the main checkout, run `npx nx build angular-typechecker`, then `npm pack` on the BUILT dist
    package root (the dist output dir that carries the compiled `.js` + `package.json` -- NOT the source
    `packages/angular-typechecker` root, and NOT the published npm artifact). Capture the resulting
    `.tgz` absolute path to hand to Task 2. Do NOT publish, tag, or bump the version.
  </action>
  <verify>
    <automated>npx nx build angular-typechecker</automated>
  </verify>
  <acceptance_criteria>
    - `npx nx build angular-typechecker` exits 0 and the dist output contains compiled `.js` (e.g. `src/index.js`, the executor `.js`), not raw `.ts`.
    - `npm pack` on the dist produced an `angular-typechecker-*.tgz`; its absolute path is recorded for Task 2.
    - No publish/tag/version-bump occurred.
  </acceptance_criteria>
  <done>A locally-packed dist tarball (shipping built .js) exists and its path is known.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: Gate B -- real-OSS tarball verify against radix-ng/primitives</name>
  <action>BLOCKING human checkpoint (autonomous:false, never auto-approved -- D-10). Pause execution and present the verification in how-to-verify. The autonomous chain must NOT run this leg itself, and must NOT merge the PR, cut/publish any release, or approve any environment/deployment gate (D-11 + never-approve-deployments). Resume only on the human's confirmation.</action>
  <what-built>
    The SB-09 engine + executor advisory (bundlerQueryImports) and the README recipe, packed into a
    local dist tarball (Task 1). Now verify it on the real radix-ng/primitives Vite/Analog Storybook
    project -- the elevated, REQUIRED Gate B (D-10). This step is human-driven by design (external large
    checkout; NOT CI; NOT self-approved).
  </what-built>
  <how-to-verify>
    1. Clone (or reuse) a THROWAWAY `radix-ng/primitives` checkout under D:/projects/github/ (MSYS `/d/`
       paths for tar). Run its `pnpm install` (apply the allowBuilds / `--ignore-scripts` workaround if
       you hit ERR_PNPM_IGNORED_BUILDS -- MEMORY nx-add-fails-on-pnpm-workspaces / oss-real-repo-verification).
    2. Install the Task-1 `.tgz` into that checkout and wire a `typecheck` target at
       `apps/radix-storybook/.storybook/tsconfig.json` (spike 009 real-repo leg).
    3. BASELINE (no `vite/client`): run the typecheck. CONFIRM: it FAILs, the `?query` TS2307 are
       reported (not suppressed), and the executor emits the bundlerQueryImports advisory naming
       `vite/client` + ADVISORY + specifiers. (Spike 009 measured 227 `?query` TS2307.)
    4. FIX: add `"types": ["vite/client"]` to the checked tsconfig; re-run. CONFIRM: the `?query` TS2307
       drop to 0 and the advisory falls silent (self-gated); the 1 pre-existing plain missing-module
       TS2307 SURVIVES.
    5. NO-FALSE-PASS PROBE: with `vite/client` present, plant `import x from './definitely-not-here';`
       in a checked file and re-run. CONFIRM: it still errors TS2307 (a plain missing module is never
       resolved by the wildcard).
    6. Do NOT commit the radix-ng checkout or any of this. Do NOT merge the phase PR, cut/publish a
       release, or approve any environment (human-gated, D-11).
  </how-to-verify>
  <resume-signal>Reply "Gate B verified" with the observed baseline `?query` TS2307 count and the with-vite/client count (expected 0), or describe any deviation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local tarball -> external radix-ng checkout | The built artifact is installed and run inside a throwaway third-party repo. |
| Gate B verification -> irreversible actions | Verification must NOT cross into merge/release/publish/environment-approval. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-08 | Tampering (wrong artifact verified) | the packed tarball | mitigate | Pack the built DIST (ships `.js`, the 0.1.1 packageRoot fix), never the source root or the published npm artifact -- Task 1 asserts compiled `.js` in the dist before packing. |
| T-20-03 | Tampering (verdict integrity, real repo) | radix-ng typecheck run | mitigate | Gate B empirically re-confirms no-false-pass on a real project: baseline FAILs on the `?query` TS2307, the plain missing module survives the fix, and a freshly planted missing import still errors. |
| T-20-06 | Elevation of Privilege (unauthorized irreversible action) | merge / release / environment | mitigate | The checkpoint explicitly forbids merging/releasing/approving; it is human-gated and stops at verification (D-10/D-11 + never-approve-deployments). |
| T-20-SC | Tampering | package installs (radix-ng deps) | accept | The pnpm install runs in a THROWAWAY external checkout, not this repo; the allowBuilds/`--ignore-scripts` workaround is a known, documented interaction (not a package-identity concern). No packages are added to angular-typechecker. |
</threat_model>

<verification>
- The tarball ships built `.js` (dist pack, not source/npm artifact).
- radix-ng baseline: `?query` TS2307 reported + advisory fires + run FAILs (never suppressed).
- `"types": ["vite/client"]`: `?query` TS2307 -> 0, advisory silent; plain missing still fails; planted missing import still errors.
- Nothing committed, merged, released, or approved.
</verification>

<success_criteria>
- Locally-packed dist tarball verified on real radix-ng/primitives: advisory fires on unresolved `?query` TS2307; `vite/client` drives them to 0; plain missing still fails (D-10).
- Human confirms the gate; merge/release/publish/environment-approval remain out of scope (D-11).
</success_criteria>

<output>
Create `.planning/phases/20-vite-analog-storybook-query-import-guidance-vite-client-read/20-05-SUMMARY.md` when done, recording the observed baseline vs with-vite/client `?query` TS2307 counts. Surface to the user that BOTH Gate A (green CI) and Gate B (this) are met, and that merge/release remain human-gated.
</output>
