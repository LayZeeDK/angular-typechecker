---
phase: quick-260710-b9t
plan: b9t
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [D1, D2, D3, D4, D5]
files_modified:
  - nx.json
  - package.json
  - tsconfig.tools.json
  - fixtures/tsconfig.clean.json
  - project.json
  - apps/ng-spike-app/project.json
  - libs/typecheck-consumer-dep/project.json
  - libs/test-util/project.json
  - packages/angular-typechecker/project.json
  - packages/angular-typechecker/vitest.config.mts
  - packages/angular-typechecker/vitest.integration.config.mts
  - packages/angular-typechecker/src/core/gate-b.spec.ts
  - packages/angular-typechecker/src/core/gate-b.integration.spec.ts
  - packages/angular-typechecker/src/core/compiler-cli-types.runtime.spec.ts
  - packages/angular-typechecker/src/core/compiler-cli-types.runtime.integration.spec.ts
  - packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts
  - e2e/angular-typechecker-cache-e2e/project.json
  - e2e/angular-typechecker-install-e2e/project.json
  - e2e/angular-typechecker-matrix-e2e/project.json
  - .github/workflows/ci.yml

must_haves:
  truths:
    - "`nx run-many -t typecheck` type-checks every non-intentionally-broken *.ts/*.mts/*.js/*.mjs file (source, ~47 plugin specs, 11 tooling configs, clean fixtures) across all projects with one uniform target name."
    - "`nx run-many -t test` runs only the fast unit tier (~29 files); `nx run-many -t integration` runs the 18 real-compiler specs; `nx run-many -t e2e --parallel=1` runs the 3 tarball projects serially."
    - "Intentionally-broken fixtures stay excluded from every passing target (their brokenness is still asserted only by the integration specs)."
    - "Root package.json exposes typecheck / test / integration / e2e / lint / format:check scripts."
    - "The ci.yml `ci` aggregate is driven by the unified run-many commands with every existing security property preserved, and goes green on the PR."
  artifacts:
    - path: "tsconfig.tools.json"
      provides: "allowJs+checkJs no-emit type-check config for the 12 tooling configs"
      contains: "checkJs"
    - path: "fixtures/tsconfig.clean.json"
      provides: "solution-style references list of the empirically-classified CLEAN fixtures only"
      contains: "references"
    - path: "packages/angular-typechecker/vitest.integration.config.mts"
      provides: "integration-tier vitest config (include *.integration.spec.ts, distinct reportsDirectory)"
    - path: "nx.json"
      provides: "name-keyed `typecheck` targetDefault (cache:true, outputs:[]); executor-keyed default kept"
      contains: "\"typecheck\""
    - path: "package.json"
      provides: "root run-many scripts"
      contains: "run-many -t typecheck"
    - path: "packages/angular-typechecker/project.json"
      provides: "unified `typecheck` (commands[]) + `integration` targets; `typecheck-drift` removed"
      contains: "integration"
    - path: ".github/workflows/ci.yml"
      provides: "unified run-many commands in the test + e2e jobs"
      contains: "run-many -t typecheck"
    - path: "packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts"
      provides: "GUARD specs rewritten to the new e2e/typecheck vocabulary"
  key_links:
    - from: "nx.json name-keyed typecheck default"
      to: "nx:run-commands typecheck targets (plugin, test-util, 3x e2e)"
      via: "target-name targetDefault (no nx:run-commands executor key exists)"
      pattern: "\"typecheck\""
    - from: "packages/angular-typechecker/project.json typecheck commands[]"
      to: "tsconfig.spec.json + tsconfig.drift.json + tsconfig.tools.json"
      via: "tsc --noEmit -p (parallel:false)"
      pattern: "tsc --noEmit -p"
    - from: "project.json (root, @angular-typechecker/source) typecheck"
      to: "fixtures/tsconfig.clean.json"
      via: "angular-typechecker:typecheck with OVERRIDDEN inputs"
      pattern: "fixtures/tsconfig.clean.json"
    - from: ".github/workflows/ci.yml e2e job"
      to: "nx run-many -t e2e --parallel=1"
      via: "run step"
      pattern: "run-many -t e2e"
    - from: "ci-e2e-coverage-guard.spec.ts"
      to: "ci.yml e2e vocabulary + e2e/* project.json e2e target"
      via: "string/regex assertions"
      pattern: "run-many"
---

<objective>
Dogfood angular-typechecker on its own repo: make every non-intentionally-broken JS/TS file
(including config files) covered by an Nx `typecheck` target, unify the target vocabulary to
three run-many verbs (`typecheck` / `test` / `e2e`) plus an `integration` tier, add root
package.json scripts, simplify ci.yml onto those verbs, land on branch
`dogfood-typecheck-all-files`, open a PR into `main`, and make the `ci` aggregate green
(locally first).

Purpose: prove the tool on itself, close the coverage gaps (~47 plugin specs + 11 tooling
configs are type-checked for the FIRST time), and give CI + local dev a single uniform command
surface.

Output: unified `typecheck` targets across all projects; a 3-tier test split; root scripts; a
simplified-but-equally-hardened ci.yml; a green PR.

Honors LOCKED decisions D1-D5 and the source-verified RESEARCH mechanics. Key hard revision:
**D2's `@nx/js:tsc` half is NOT achievable** (RESEARCH Q1: it requires main+outputPath, forces
emit, treats emitSkipped as failure). Non-Angular type-checks therefore use
`nx:run-commands` + `tsc --noEmit -p` (the repo's existing typecheck-drift/typecheck-e2e
pattern). Angular projects/fixtures use `angular-typechecker:typecheck`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/quick/260710-b9t-dogfood-angular-typechecker-add-typechec/260710-b9t-CONTEXT.md
@.planning/quick/260710-b9t-dogfood-angular-typechecker-add-typechec/260710-b9t-RESEARCH.md
@AGENTS.md
@nx.json
@.github/workflows/ci.yml
@packages/angular-typechecker/project.json
@packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts

<interfaces>
<!-- Verified repo facts the executor needs. Exact JSON shapes live in RESEARCH.md (Q1-Q7). -->

Target-default precedence (RESEARCH Q2, verified in nx 23.0.1): exactly ONE targetDefault
applies per target. If the target's executor has an `<executor-id>` key, the name key is
IGNORED; else the `<targetName>` key applies. So:
- `angular-typechecker:typecheck` executor targets -> existing executor-keyed default (leave as-is).
- `nx:run-commands` typecheck targets -> the NEW name-keyed `typecheck` default (no run-commands key exists).
- Array props (inputs/outputs) on a target REPLACE the default's (target wins).

Current real projects + typecheck ownership after this task:
- ng-spike-app (app)           -> typecheck: angular-typechecker:typecheck over tsconfig.app.json   [NEW]
- typecheck-consumer           -> typecheck: angular-typechecker:typecheck over tsconfig.lib.json   [unchanged]
- typecheck-walk-consumer      -> typecheck: angular-typechecker:typecheck over tsconfig.json       [unchanged]
- typecheck-consumer-dep       -> typecheck: angular-typechecker:typecheck over tsconfig.lib.json   [NEW; dep.component.ts is committed-clean]
- test-util                    -> typecheck: nx:run-commands tsc --noEmit -p tsconfig.spec.json     [NEW]
- angular-typechecker (plugin) -> typecheck: nx:run-commands commands[] (spec + drift + tools)      [RENAME of typecheck-drift + fold]
- @angular-typechecker/source (root, ./project.json) -> typecheck: angular-typechecker:typecheck over fixtures/tsconfig.clean.json, OVERRIDDEN inputs [NEW]
- 3x e2e                       -> typecheck: nx:run-commands tsc --noEmit -p <e2e>/tsconfig.spec.json [RENAME of typecheck-e2e]; e2e: @nx/vitest:test [RENAME of test]

Verified couplings (do NOT miss):
- The two spec renames have ZERO string references anywhere (checked): all "gate-b" hits point
  at the `gate-b-error` FIXTURE dir, not the spec file. The vitest include globs catch the
  renamed files by pattern.
- `packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts` HARD-ASSERTS ci.yml facts
  that this task changes and will THROW/FAIL unless rewritten in lockstep:
    * GUARD-01  -> requires a `-p` list in the e2e job (we drop it).
    * GUARD-01b -> requires `--parallel=1` in the e2e job (KEEP; still valid).
    * GUARD-01c -> requires a `typecheck-e2e` target + `run-many -t typecheck-e2e` in ci.yml
                   (we rename to `typecheck`).
- `.prettierignore` covers `.planning/`, lockfiles, and 2 whitespace-sensitive fixture HTMLs.
  Fixture `.ts` files ARE Prettier-clean today (verified). CI runs `nx format:check` scoped to
  changed files; do NOT rely on `--all` as a gate (matches CI parity; avoids scanning the whole repo).
- tooling config files (RESEARCH Q6, 11 today): root `eslint.config.mjs`, `vitest.workspace.ts`;
  `apps/ng-spike-app/eslint.config.mjs`; `libs/test-util/{eslint.config.mjs,vitest.config.mts}`;
  `packages/angular-typechecker/{eslint.config.mjs,vitest.config.mts}`; `e2e/*/vitest.config.mts` (x3).
  This task ADDS a 12th: `packages/angular-typechecker/vitest.integration.config.mts`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify the target vocabulary in nx.json + add root package.json scripts</name>
  <files>nx.json, package.json</files>
  <action>
Per D4 + RESEARCH Q2. In nx.json `targetDefaults`, ADD a name-keyed `"typecheck": { "cache": true, "outputs": [] }` entry. KEEP the existing executor-keyed `angular-typechecker:typecheck` default exactly as-is (it shadows the name key for the Angular executor targets, which is correct) and KEEP `@nx/js:tsc`, `@nx/eslint:lint`, `@nx/vitest:test`, `@angular/build:application` untouched. Do NOT add an `nx:run-commands` key (none exists; the name key is what applies to the run-commands typecheck targets).

In the root package.json `scripts` (currently `{}`), add: `typecheck` -> `nx run-many -t typecheck`; `test` -> `nx run-many -t test`; `integration` -> `nx run-many -t integration`; `e2e` -> `nx run-many -t e2e --parallel=1` (preserve the shared-tarball serialization per D4); `lint` -> `nx run-many -t lint`; `format:check` -> `nx format:check`. Do NOT use `--all` in the format:check script (the repo gates on changed-files only). Leave `private`, `nx.includedScripts`, deps, and overrides unchanged.
  </action>
  <verify>
    <automated>node -e "const n=require('./nx.json');const p=require('./package.json');if(!n.targetDefaults.typecheck||n.targetDefaults.typecheck.cache!==true||JSON.stringify(n.targetDefaults.typecheck.outputs)!=='[]')throw new Error('nx.json typecheck default missing/wrong');if(!n.targetDefaults['angular-typechecker:typecheck'])throw new Error('executor-keyed default lost');for(const s of ['typecheck','test','integration','e2e','lint','format:check'])if(!p.scripts[s])throw new Error('missing script '+s);if(!/--parallel=1/.test(p.scripts.e2e))throw new Error('e2e script missing --parallel=1');console.log('OK')"</automated>
  </verify>
  <done>nx.json has both the name-keyed and executor-keyed typecheck defaults; root package.json exposes all 6 scripts with e2e serialized.</done>
</task>

<task type="auto">
  <name>Task 2: Plugin unified `typecheck` (spec+drift+tools) + create tsconfig.tools.json; fix surfaced spec/config type errors</name>
  <files>tsconfig.tools.json, packages/angular-typechecker/project.json</files>
  <action>
This is the biggest coverage-add (the ~47 plugin specs + the tooling configs, type-checked for the first time). Per RESEARCH Q1/Q3/Q6.

1. Create root `tsconfig.tools.json` extending `./tsconfig.base.json` with compilerOptions `noEmit:true, allowJs:true, checkJs:true, module:"esnext", moduleResolution:"bundler", target:"es2022", types:["node"], skipLibCheck:true` and an `include` array listing the 11 tooling configs enumerated in RESEARCH Q6 (root eslint.config.mjs, vitest.workspace.ts, app eslint.config.mjs, test-util eslint.config.mjs + vitest.config.mts, plugin eslint.config.mjs + vitest.config.mts, and the 3 e2e vitest.config.mts). Use the exact list/shape in RESEARCH Q6. (The 12th file, vitest.integration.config.mts, is added in Task 4 when it is created — a not-yet-existing path in `include` is silently skipped by tsc, so ordering is safe either way.)

2. In `packages/angular-typechecker/project.json`, REMOVE the `typecheck-drift` target and REPLACE it with a `typecheck` target: executor `nx:run-commands`, `options.commands` = an ARRAY (RESEARCH Q3) running, in order, `tsc --noEmit -p packages/angular-typechecker/tsconfig.spec.json` (covers ~47 specs + the plugin vitest.config.mts), `tsc --noEmit -p packages/angular-typechecker/tsconfig.drift.json` (the folded drift gate), `tsc --noEmit -p tsconfig.tools.json` (the workspace tooling configs), with `parallel:false` and `cwd:"."`. Set `cache:true`. Set `inputs` to: `"default"` (plugin src/specs/drift/eslint/vitest all live under projectRoot), `"{workspaceRoot}/tsconfig.base.json"`, `"{workspaceRoot}/tsconfig.tools.json"`, plus the NON-plugin tooling files so their edits bust this target's cache: `"{workspaceRoot}/eslint.config.mjs"`, `"{workspaceRoot}/vitest.workspace.ts"`, `"{workspaceRoot}/apps/ng-spike-app/eslint.config.mjs"`, `"{workspaceRoot}/libs/test-util/eslint.config.mjs"`, `"{workspaceRoot}/libs/test-util/vitest.config.mts"`, `"{workspaceRoot}/e2e/angular-typechecker-cache-e2e/vitest.config.mts"`, `"{workspaceRoot}/e2e/angular-typechecker-install-e2e/vitest.config.mts"`, `"{workspaceRoot}/e2e/angular-typechecker-matrix-e2e/vitest.config.mts"`, and `{ "externalDependencies": ["typescript", "@angular/compiler-cli"] }`. Leave `build`, `lint`, `nx-release-publish`, `scoped-name-guard`, `test` untouched (the `test`/`integration` split is Task 4).

3. RUN the new target and FIX every error it surfaces. Expect real type errors from the first-time spec type-check (RESEARCH gap #1) and possibly `checkJs` errors in flat ESLint configs (RESEARCH Q6 A4). Fix them in-place. If a SINGLE tooling config genuinely cannot type-check under checkJs (e.g. a non-iterable spread), remove ONLY that file from tsconfig.tools.json `include` with a comment explaining exactly why — do not weaken checkJs globally. Every edited file must stay Prettier- and lint-clean.
  </action>
  <verify>
    <automated>npx nx typecheck angular-typechecker --skip-nx-cache</automated>
  </verify>
  <done>`typecheck-drift` is gone; the plugin `typecheck` runs all three tsc --noEmit invocations serially and exits 0; tsconfig.tools.json type-checks the tooling configs; all surfaced errors fixed.</done>
</task>

<task type="auto">
  <name>Task 3: Add `typecheck` targets to the coverage-gap projects (app, consumer-dep, test-util)</name>
  <files>apps/ng-spike-app/project.json, libs/typecheck-consumer-dep/project.json, libs/test-util/project.json</files>
  <action>
Additive coverage only (D3) — do NOT add build/test/e2e to these projects; do NOT touch .nxignore or the release preVersionCommand.

1. ng-spike-app: add a `typecheck` target, executor `angular-typechecker:typecheck`, `options.tsConfig` = `apps/ng-spike-app/tsconfig.app.json`, leave `includeDeps` at default (false) per RESEARCH Q7. No custom inputs (the executor-keyed default's `{projectRoot}/**/*` is fine for a real project). This is the showcase dogfood: it adds the NG8xxx extended template diagnostics decoupled from the build.

2. typecheck-consumer-dep (currently zero targets, gap #4): add a `typecheck` target, executor `angular-typechecker:typecheck`, `options.tsConfig` = `libs/typecheck-consumer-dep/tsconfig.lib.json`. `dep.component.ts` is committed-clean (the e2e cache test mutates it at runtime only), so this is a green target. Its `.pristine` sidecar is not `.ts` and is excluded by the `src/**/*.ts` include.

3. test-util: add a `typecheck` target, executor `nx:run-commands` (RESEARCH Q1 — spec tsconfig has no single `main` entry, so the Angular executor is not appropriate and @nx/js:tsc cannot noEmit), `options.command` = `tsc --noEmit -p libs/test-util/tsconfig.spec.json`, `cwd:"."`. Set `inputs` = `["default", "{workspaceRoot}/tsconfig.base.json", { "externalDependencies": ["typescript"] }]`. This covers find-workspace-root.spec.ts (gap #2). Leave its `build`, `lint`, `test` untouched. Do NOT touch typecheck-consumer / typecheck-walk-consumer (already correct).

Run each new target and fix anything that surfaces.
  </action>
  <verify>
    <automated>npx nx run-many -t typecheck -p ng-spike-app typecheck-consumer-dep test-util --skip-nx-cache</automated>
  </verify>
  <done>All three new typecheck targets exist and exit 0; consumer libs unchanged.</done>
</task>

<task type="auto">
  <name>Task 4: 3-tier test split — reclassify 2 leaker specs + fast `test` / new `integration` target</name>
  <files>packages/angular-typechecker/src/core/gate-b.spec.ts, packages/angular-typechecker/src/core/gate-b.integration.spec.ts, packages/angular-typechecker/src/core/compiler-cli-types.runtime.spec.ts, packages/angular-typechecker/src/core/compiler-cli-types.runtime.integration.spec.ts, packages/angular-typechecker/vitest.config.mts, packages/angular-typechecker/vitest.integration.config.mts, packages/angular-typechecker/project.json, tsconfig.tools.json</files>
  <action>
Per D5 + RESEARCH Q4. No test CODE changes — mechanism only.

1. Reclassify the 2 cold-compile leakers by `git mv` (verified zero string references): `git mv packages/angular-typechecker/src/core/gate-b.spec.ts packages/angular-typechecker/src/core/gate-b.integration.spec.ts` and `git mv packages/angular-typechecker/src/core/compiler-cli-types.runtime.spec.ts packages/angular-typechecker/src/core/compiler-cli-types.runtime.integration.spec.ts`. Stage the renames with `git mv` (do NOT hand-copy).

2. Narrow the fast tier: in `packages/angular-typechecker/vitest.config.mts` exclude the integration specs. Import `configDefaults` from `vitest/config` and set `test.exclude = [...configDefaults.exclude, '**/*.integration.spec.ts']` (spread the defaults so node_modules/dist stays excluded). Leave `include`, `name`, timeouts, coverage untouched.

3. Create `packages/angular-typechecker/vitest.integration.config.mts` mirroring vitest.config.mts but with `test.name:'angular-typechecker:integration'`, `test.include:['src/**/*.integration.spec.ts']`, and a DISTINCT `coverage.reportsDirectory` (`../../coverage/packages/angular-typechecker-integration`) so v8 coverage does not overwrite the fast tier's. Keep the 30000ms testTimeout/hookTimeout (these are the cold-compiler specs that need it). Do NOT add this file to vitest.workspace.ts (its glob only matches `vitest.config.*`, so it stays isolated and integration specs never double-run).

4. In `packages/angular-typechecker/project.json` add an `integration` target: executor `@nx/vitest:test`, `dependsOn:["build"]`, `outputs:["{options.reportsDirectory}"]`, `options.configFile` = `packages/angular-typechecker/vitest.integration.config.mts`, `options.reportsDirectory` = `coverage/packages/angular-typechecker-integration`. The existing `test` target stays as-is (auto-discovers the narrowed vitest.config.mts); the executor-keyed `@nx/vitest:test` default applies to BOTH by executor (RESEARCH Q4).

5. Add `"packages/angular-typechecker/vitest.integration.config.mts"` to the `include` array of `tsconfig.tools.json` (created in Task 2) so the new config is itself type-checked.

Run both tiers. The fast `test` must NOT run the 2 reclassified specs; `integration` must run 18 files (16 existing + 2 reclassified). Confirm `scoped-name-guard` still resolves its `scoped-name-guard.spec.ts` (a fast-tier spec, still included). Fix anything that surfaces.
  </action>
  <verify>
    <automated>npx nx test angular-typechecker --skip-nx-cache && npx nx integration angular-typechecker --skip-nx-cache</automated>
  </verify>
  <done>2 specs renamed to *.integration.spec.ts; fast `test` excludes them; new `integration` target runs the 18 real-compiler specs with a distinct coverage dir; tools tsconfig covers the new config.</done>
</task>

<task type="auto">
  <name>Task 5: Rename e2e `test`->`e2e` and `typecheck-e2e`->`typecheck`; rewrite the ci coverage guards</name>
  <files>e2e/angular-typechecker-cache-e2e/project.json, e2e/angular-typechecker-install-e2e/project.json, e2e/angular-typechecker-matrix-e2e/project.json, packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts</files>
  <action>
Per D4 + RESEARCH Q3/Q4.

1. In each of the 3 e2e project.json files: rename the `test` target to `e2e` (keep executor `@nx/vitest:test`, its `outputs`, and `reportsDirectory` unchanged — the executor-keyed default still applies by executor). Rename the `typecheck-e2e` target to `typecheck` (keep executor `nx:run-commands`, its per-project `inputs`, and its `tsc --noEmit -p <e2e>/tsconfig.spec.json` command unchanged). Each project now has exactly `e2e` + `typecheck`.

2. Rewrite `packages/angular-typechecker/src/ci-e2e-coverage-guard.spec.ts` to the new vocabulary — this guard file HARD-ASSERTS ci.yml facts that just changed and will THROW/FAIL otherwise:
   - GUARD-01 (was: e2e job `-p` list == e2e/* set): the ci.yml e2e job drops the `-p` list (Task 6/7) because `nx run-many -t e2e` now matches ONLY the 3 e2e projects (unique target name). Repurpose GUARD-01 to preserve the anti-silent-skip guarantee under the new scheme: assert (a) every `e2e/<dir>/project.json` defines an `e2e` target (reusing `enumerateE2eProjects`), and (b) NO non-e2e project defines an `e2e` target (scan every `project.json` outside `e2e/` via the workspace project list / a glob and assert none has a target named `e2e`), so `run-many -t e2e` provably covers exactly the 3 e2e projects. Drop the old `-p`-list extraction (`extractE2ePList`) that would throw.
   - GUARD-01b (`--parallel=1`): KEEP unchanged — still valid and load-bearing.
   - GUARD-01c: update both axes from `typecheck-e2e` to `typecheck` — assert every e2e project defines a `typecheck` target AND ci.yml runs `nx run-many -t typecheck`. Keep the "run-many with zero matches exits 0" rationale in the comment.
   Keep the file a cheap READ-ONLY string/regex check (no YAML parser, no new deps); keep it a fast-tier `*.spec.ts` (NOT integration). Update the header comments to describe the new invariants accurately.

Note: this spec reads ci.yml, so its final green depends on Task 7's ci.yml edits; run it after Task 7 in the make-green pass. Its own type-check (Task 2's spec sweep) must still pass now.
  </action>
  <verify>
    <automated>node -e "for(const d of ['cache','install','matrix']){const p=require('./e2e/angular-typechecker-'+d+'-e2e/project.json');if(!p.targets.e2e||p.targets.e2e.executor!=='@nx/vitest:test')throw new Error(d+' e2e target wrong');if(!p.targets.typecheck||p.targets.typecheck.executor!=='nx:run-commands')throw new Error(d+' typecheck target wrong');if(p.targets.test||p.targets['typecheck-e2e'])throw new Error(d+' still has old target names');}console.log('OK')"</automated>
  </verify>
  <done>All 3 e2e projects expose `e2e` + `typecheck`; the guard spec is rewritten to the new vocabulary and no longer references the old `-p` list or `typecheck-e2e`.</done>
</task>

<task type="auto">
  <name>Task 6: Clean-fixtures coverage — empirically classify, build fixtures/tsconfig.clean.json + root `typecheck`</name>
  <files>fixtures/tsconfig.clean.json, project.json</files>
  <action>
Per D1 + RESEARCH Q5. CAVEAT (RESEARCH Q5/A2): the clean fixtures are ALREADY type-checked by the real compiler inside the 16 integration specs, so this target is redundant coverage — but the USER explicitly decided (D1) clean fixtures get their own separated coverage. KEEP it; note the redundancy in a comment in fixtures/tsconfig.clean.json.

1. CLASSIFY EMPIRICALLY (D1: by running, NOT by name — `ts-baseline`/`ng-baseline` are BROKEN despite the name; only some `-clean` dirs are truly clean). For each candidate fixture LEAF tsconfig (the ones that directly include/`files` source — tsconfig.app.json / tsconfig.lib.json / plain tsconfig.json that carry source, NOT the solution-style ones with their own `references[]`), run `angular-typechecker:typecheck` against it and record whether the verdict is GREEN (success, zero diagnostics — advisory warnings like the not-type-checked notices are still GREEN and count as clean) or RED. Easiest mechanism: iteratively build `fixtures/tsconfig.clean.json` (solution-style, `files:[]` + `references:[]`) with candidate leaves, run the root `typecheck` target (which resolves one level and runs performCompilation per leaf, unioning diagnostics), then REMOVE any leaf that reported errors; repeat until the target is fully GREEN. Keep only truly-clean leaves; broken siblings are excluded simply by omission (D1 satisfied). Place the solution tsconfig at `fixtures/` so the executor's module-boundary containment guard keeps leaves scoped under `fixtures/`. Do NOT reference nested solution-style tsconfigs (e.g. `fixtures/solution-style/tsconfig.json`) as leaves — reference concrete leaf tsconfigs the executor can compile directly.

2. Add a `typecheck` target to the ROOT project `project.json` (`@angular-typechecker/source`): executor `angular-typechecker:typecheck`, `options.tsConfig` = `fixtures/tsconfig.clean.json`. MUST OVERRIDE `inputs` (RESEARCH Q5 cache-poison pitfall — the root project's `{projectRoot}` is the repo root, so the executor default's `default`/`{projectRoot}/**/*` input would hash the entire repo incl. .planning): set `inputs` = `["{workspaceRoot}/fixtures/**/*", "{workspaceRoot}/tsconfig.base.json", { "externalDependencies": ["typescript", "@angular/compiler-cli"] }]`. Keep the existing `local-registry` target untouched. Do NOT add project.json to any fixture dir (keeps them off the graph — D3, .nxignore, release scoping all untouched).

3. Record the final classified-clean set as an inline comment in fixtures/tsconfig.clean.json so re-classification on future fixture additions is obvious.
  </action>
  <verify>
    <automated>npx nx typecheck @angular-typechecker/source --skip-nx-cache</automated>
  </verify>
  <done>fixtures/tsconfig.clean.json lists only empirically-classified clean leaves; the root project `typecheck` target exits 0 with overridden inputs; no fixture gained a project.json; broken fixtures excluded by omission.</done>
</task>

<task type="auto">
  <name>Task 7: Simplify ci.yml onto the unified verbs (preserve all security properties)</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Per D4/D5 + constraint #9. Change ONLY the `run:` step commands (and their explanatory comments) in the `test` and `e2e` jobs. Do NOT touch any security scaffolding: SHA-pinned actions, `persist-credentials:false`, top-level `permissions: contents: read`, the `changes` path-gating job + its `predicate-quantifier: every`, the `if:` path-gates, `NX_DAEMON:false`, `concurrency`, the `ci` aggregate gate, the fallow/format-lint/act-compat/lint-workflows/scoped-name-guard jobs, and the matrix.

1. `test` job: replace the single `npx nx run-many -t typecheck-drift test -p angular-typechecker test-util` step with three sequential steps (fast tier fails first): `npx nx run-many -t typecheck`, then `npx nx run-many -t test`, then `npx nx run-many -t integration`. Update the step comments: typecheck now folds the old drift gate and covers all projects; `test` = fast unit (plugin + test-util); `integration` = the real-compiler tier.

2. `e2e` job: replace `npx nx run-many -t typecheck-e2e` with `npx nx run-many -t typecheck` (folds the old typecheck-e2e; runs before the multi-minute install so a type error fails fast). Replace the `nx run-many -t test -p <3 projects> --parallel=1` step with `npx nx run-many -t e2e --parallel=1` (no `-p` list — only the 3 e2e projects define `e2e`; `--parallel=1` preserves the shared-tarball serialization). Update the comments to match (drop the `-p`-list-preservation note; the GUARD spec was repurposed in Task 5).

3. Because ci.yml changed, re-run the workflow gates locally: `bash tools/act/act-compat.sh` and `./actionlint` (or `actionlint`) — install per the ci.yml pins if not present. Do NOT edit AGENTS.md: these are vocabulary-only changes that preserve every documented CI/release property; if you believe a material CI-semantics change is unavoidable, STOP and flag it (AGENTS.md edits require a code review per its own rule).
  </action>
  <verify>
    <automated>bash tools/act/act-compat.sh</automated>
  </verify>
  <done>ci.yml test job runs typecheck+test+integration; e2e job runs typecheck+e2e --parallel=1; act-compat + actionlint pass; all security properties intact; no AGENTS.md edit.</done>
</task>

<task type="auto">
  <name>Task 8: Make it green locally — run every CI gate and fix all surfaced errors</name>
  <files>packages/angular-typechecker/src, apps/ng-spike-app, libs, e2e, fixtures, tsconfig.tools.json, .github/workflows/ci.yml</files>
  <action>
Run the full CI-equivalent battery on the branch and FIX every failure before the PR (CONTEXT "make it green locally"). Order:
1. `npx nx run-many -t typecheck` (unified — the biggest new surface; fix the first-time-checked specs + tooling configs; loading the `angular-typechecker:typecheck` executor across app/consumer-dep/root uses the source plugin via swc-node/register — if the executor fails to LOAD (not a type error), that is a discovery, not expected; resolve by ensuring the plugin is discoverable rather than by weakening the target).
2. `npx nx run-many -t test`
3. `npx nx run-many -t integration`
4. `npx nx run-many -t e2e --parallel=1` — HEAVY (tarball pack + multi-PM install; needs corepack/pnpm/yarn provisioning as in ci.yml). If it cannot fully run locally on Windows arm64, run what you can, then explicitly STATE that the e2e tier is CI-verified only and rely on the PR's `e2e` job. Do NOT fake it green.
5. `npx nx run-many -t lint` (maxWarnings:0 is baked into the @nx/eslint:lint default — every edited file must be lint-clean).
6. `npx nx format:check --base=main --head=HEAD` (changed-files scope, matching CI; do NOT use `--all` — CI never does, and it needlessly scans the whole repo). Run `npx nx format:write` on any file it flags.
7. `npx nx scoped-name-guard angular-typechecker`.
8. `bash tools/act/act-compat.sh` + `actionlint` (ci.yml was edited).
Re-run the guard spec (`ci-e2e-coverage-guard.spec.ts`) here specifically — it reads the just-edited ci.yml and must be green against it. Iterate until every gate above is green (except a documented e2e-only-on-CI note).
  </action>
  <verify>
    <automated>npx nx run-many -t typecheck && npx nx run-many -t test && npx nx run-many -t integration && npx nx run-many -t lint && npx nx scoped-name-guard angular-typechecker && npx nx format:check --base=main --head=HEAD && bash tools/act/act-compat.sh</automated>
  </verify>
  <done>Every CI-equivalent gate is green locally (typecheck/test/integration/lint/format:check/scoped-name-guard/act-compat/actionlint); e2e is green locally or explicitly documented as CI-verified-only.</done>
</task>

<task type="auto">
  <name>Task 9: Atomic commits, open the PR into main, confirm CI green</name>
  <files>.git</files>
  <action>
Ensure the branch is `dogfood-typecheck-all-files` (create off up-to-date `main` if not already on it). Stage files BY NAME (never `git add .`/`-A`/`-u`); prefer the `git mv` renames already staged in Task 4.

Commit atomically with clean conventional scopes (NEVER plan-ids; scopes render in the public changelog). IMPORTANT: this task adds NO consumer-facing behavior to the published `angular-typechecker` package — it is repo tooling/CI dogfood — so use NON-releasing types to avoid an unintended version bump and changelog noise: prefer `test(...)` for the spec split/reclassify, `build(...)` or `chore(...)` for tsconfig/nx.json/project.json target config, `ci(...)` for ci.yml, `chore(...)` for root scripts. Do NOT use `feat`/`fix` (both bump the version in 0.x and appear in the changelog) unless the user explicitly wants a release. Suggested grouping: (a) `build(nx): ...`/`chore(nx): ...` for nx.json + root scripts (Task 1); (b) `build(typecheck): ...` for tsconfig.tools.json + plugin typecheck fold + gap-project targets + spec fixes (Tasks 2-3); (c) `test(...): ...` for the 3-tier split + reclassify + guard rewrite (Tasks 4-5); (d) `build(typecheck): ...` for the clean-fixtures target (Task 6); (e) `ci(...): ...` for ci.yml (Task 7); (f) any `fix`-of-tooling from Task 8 folded into the relevant commit. Keep commit messages end-user-neutral (no board/plan jargon). No AI attribution trailers.

Push the branch and open a PR into `main` (base is `main`; `main` is PR-only). Confirm the required `ci` aggregate check runs and goes GREEN on the PR. Do NOT merge and do NOT approve any environment/deployment gate — the task ends at a green `ci`. Report the PR URL and the `ci` status.
  </action>
  <verify>
    <automated>test "$(git rev-parse --abbrev-ref HEAD)" = dogfood-typecheck-all-files && test "$(gh pr view --json baseRefName -q '.baseRefName')" = main && echo OK</automated>
  </verify>
  <done>Atomic, clean-scoped, non-bumping commits staged by name; PR open into main; `ci` aggregate green (e2e caveat noted if applicable).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted PR -> ci.yml run steps | PR metadata (title/branch/author) must never reach a privileged run step |
| workspace -> npm registry (dev deps) | `npm ci` installs the lockfile-pinned toolchain in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-b9t-CI | Elevation/Tampering | .github/workflows/ci.yml | mitigate | Task 7 changes ONLY run-step commands; keeps SHA-pinned actions, persist-credentials:false, contents:read, path-gating, NX_DAEMON:false, the `ci` aggregate, and all sibling security jobs. No PR-metadata interpolation is introduced. Re-verified by act-compat + actionlint. |
| T-b9t-SC | Tampering | npm/pnpm/yarn installs | accept | No new dependencies added by this task (only Nx target config + tsconfigs + spec renames). Existing lockfile-pinned `npm ci` posture is unchanged; no legitimacy gate needed. |
| T-b9t-GUARD | Repudiation | ci-e2e-coverage-guard.spec.ts | mitigate | The e2e-coverage guarantee is preserved across the vocabulary change: Task 5 repurposes GUARD-01 to assert exactly-the-3-e2e-projects define `e2e` (and no others do), keeps GUARD-01b `--parallel=1`, and updates GUARD-01c to the `typecheck` name — so a silently-skipped e2e project or a dropped serialization still fails loudly. |
</threat_model>

<verification>
- `npx nx run-many -t typecheck` exits 0 and covers all projects (app, 3 consumer libs, plugin spec+drift+tools, test-util, root fixtures, 3 e2e).
- `npx nx run-many -t test` (fast) excludes the 2 reclassified specs; `npx nx run-many -t integration` runs 18 real-compiler specs; `npx nx run-many -t e2e --parallel=1` runs the 3 tarball projects (or documented CI-only).
- `npx nx run-many -t lint`, `npx nx format:check --base=main --head=HEAD`, `npx nx scoped-name-guard angular-typechecker` all green.
- `bash tools/act/act-compat.sh` + `actionlint` green after ci.yml edits.
- The rewritten `ci-e2e-coverage-guard.spec.ts` passes against the edited ci.yml.
- No project.json added to any fixture dir; .nxignore + release preVersionCommand untouched; no `feat`/`fix` commits; no AGENTS.md edit.
- PR open into `main`; `ci` aggregate green.
</verification>

<success_criteria>
Every non-intentionally-broken *.ts/*.mts/*.js/*.mjs file (incl. config files, excl. .planning) is
type-checked by some `typecheck` target reachable via `nx run-many -t typecheck`. Tests are split
into fast `test`, `integration`, and serialized `e2e`. Root package.json exposes the 6 scripts.
ci.yml is simplified onto the unified verbs with all security properties intact. The branch
`dogfood-typecheck-all-files` has a PR into `main` with a green `ci`.
</success_criteria>

<output>
Create `.planning/quick/260710-b9t-dogfood-angular-typechecker-add-typechec/260710-b9t-SUMMARY.md` when done.
</output>
