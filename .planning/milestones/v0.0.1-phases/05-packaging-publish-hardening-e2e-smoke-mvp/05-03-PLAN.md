---
phase: 05-packaging-publish-hardening-e2e-smoke-mvp
plan: 03
type: execute
wave: 3
depends_on: ['05-01', '05-02']
files_modified:
  - e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-app/project.json
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-app/package.json
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-app/nx.json
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-app/tsconfig.json
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-app/tsconfig.lib.json
  - e2e/angular-typechecker-install-e2e/fixtures/consumer-app/src/app.component.ts
autonomous: true
requirements: [TEST-05]
must_haves:
  truths:
    - 'The packed tarball installs into a CLEAN tmp workspace (no legacy-peer-deps) and a green run of nx run consumer-app:angular-typecheck exits 0 -- proving the executor resolves FROM the installed package'
    - "An injected TS2322 run exits non-zero, prints TS2322 to stdout, and prints NEITHER ERR_REQUIRE_ESM NOR the infra-error meta message -- proving the installed CJS executor's import() survived packaging and the check actually ran"
    - 'The committed fixture wires the executor by its PUBLISHED unscoped id angular-typechecker:angular-typecheck with includeDeps:true and NO tsconfig path-alias to plugin source'
    - 'If a clean install ERESOLVEs (B-03), the smoke surfaces it honestly rather than masking it with --legacy-peer-deps'
  artifacts:
    - path: 'e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts'
      provides: 'pack -> clean tmp install -> green run + injected-TS2322 run'
      min_lines: 80
    - path: 'e2e/angular-typechecker-install-e2e/fixtures/consumer-app/project.json'
      provides: 'Committed minimal consumer fixture wired with the PUBLISHED executor id + includeDeps:true'
      contains: 'angular-typechecker:angular-typecheck'
    - path: 'e2e/angular-typechecker-install-e2e/fixtures/consumer-app/src/app.component.ts'
      provides: 'Self-contained standalone component (no @fixtures/@angular-typechecker alias) that type-checks clean'
      min_lines: 5
  key_links:
    - from: 'e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts'
      to: 'the packed angular-typechecker .tgz'
      via: 'mkdtemp -> copy fixture -> npm install <tgz> (no --legacy-peer-deps)'
      pattern: 'npm install'
    - from: 'e2e/angular-typechecker-install-e2e/fixtures/consumer-app/project.json'
      to: 'angular-typechecker:angular-typecheck'
      via: 'PUBLISHED unscoped executor id (NOT the dev-scoped key)'
      pattern: 'angular-typechecker:angular-typecheck'
    - from: 'e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts'
      to: 'the injected TS2322'
      via: 'mutate the TMP-copied fixture source, re-run, assert non-zero + TS2322 + no ERR_REQUIRE_ESM'
      pattern: 'TS2322'
---

<objective>
Prove the whole vertical slice end-to-end: a clean `npm install <tarball>` resolves the executor and runs it. This is THE tracer bullet (D-22) -- after 05-01/05-02 made the artifact correct and audited, this plan installs the exact packed tarball into an isolated tmp workspace and runs the executor by its PUBLISHED id, both green and against a deliberately broken source.

Purpose: The audit gate (05-02) proves the tarball is shaped right; this smoke proves it actually WORKS from a consumer install -- the executor path resolves from `node_modules/angular-typechecker`, the CJS `import()` of ESM compiler-cli survives packaging (no `ERR_REQUIRE_ESM`), and a real type error is reported with a non-zero exit. The green+error pair is what distinguishes "the check ran and passed" from "a no-op exited 0" (a type-checker that lies is worse than none). The clean (no legacy-peer-deps) install also surfaces whether consumers hit ERESOLVE (B-03).

Output: A committed minimal consumer-app fixture wired with the PUBLISHED executor id + includeDeps:true (no source alias), and an `install-smoke.int.spec.ts` that packs, installs into a per-run tmp copy, and asserts the green + injected-TS2322 runs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-packaging-publish-hardening-e2e-smoke-mvp/05-CONTEXT.md
@.planning/phases/05-packaging-publish-hardening-e2e-smoke-mvp/05-RESEARCH.md
@.planning/phases/05-packaging-publish-hardening-e2e-smoke-mvp/05-PATTERNS.md

# The harness analog (run()/buildCleanEnv/inject-error patterns) + the fixture analog:

@e2e/angular-typechecker-cache-e2e/src/cache-busts-on-dep-error.int.spec.ts
@libs/typecheck-consumer/project.json
@libs/typecheck-consumer/tsconfig.lib.json
@libs/typecheck-consumer/src/lib/consumer.component.ts

# The install-e2e project scaffold this plan adds files into (created by 05-02):

@e2e/angular-typechecker-install-e2e/vitest.config.mts
@e2e/angular-typechecker-install-e2e/project.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the committed minimal consumer-app fixture (PUBLISHED id, no source alias)</name>
  <files>e2e/angular-typechecker-install-e2e/fixtures/consumer-app/project.json, e2e/angular-typechecker-install-e2e/fixtures/consumer-app/package.json, e2e/angular-typechecker-install-e2e/fixtures/consumer-app/nx.json, e2e/angular-typechecker-install-e2e/fixtures/consumer-app/tsconfig.json, e2e/angular-typechecker-install-e2e/fixtures/consumer-app/tsconfig.lib.json, e2e/angular-typechecker-install-e2e/fixtures/consumer-app/src/app.component.ts</files>
  <read_first>
    - libs/typecheck-consumer/project.json (the dev recipe -- but it uses the DEV-scoped key + @fixtures alias, which the fixture MUST NOT copy)
    - libs/typecheck-consumer/tsconfig.lib.json (the Angular tsconfig shape: noEmit, strict, strictTemplates -- mirror it but self-contained, not extending the workspace tsconfig.base.json)
    - libs/typecheck-consumer/src/lib/consumer.component.ts (the component shape -- but DROP the @fixtures dep import; the fixture is self-contained)
    - .planning/phases/05-packaging-publish-hardening-e2e-smoke-mvp/05-PATTERNS.md "fixtures/<consumer-app>/" section (the TWO critical D-18 divergences: PUBLISHED unscoped id + NO source alias)
    - .planning/phases/05-packaging-publish-hardening-e2e-smoke-mvp/05-CONTEXT.md D-18 + STATE [04-03] Rule-1/Rule-2 (published id + includeDeps:true)
  </read_first>
  <action>Create a COMMITTED minimal consumer-app fixture under `e2e/angular-typechecker-install-e2e/fixtures/consumer-app/` that is SELF-CONTAINED (it gets tmp-copied and installed-against, so it must not depend on the workspace tsconfig.base.json aliases -- A3). Files: (a) `project.json` -- `name: consumer-app`, `projectType: application`, an `angular-typecheck` target with `"executor": "angular-typechecker:angular-typecheck"` (the PUBLISHED UNSCOPED id -- NOT `@angular-typechecker/angular-typechecker:angular-typecheck`, which would not bind in a consumer -- D-18/STATE) and `options: { "tsConfig": "tsconfig.lib.json", "includeDeps": true }` (includeDeps:true per Phase-4 Rule-2; relative paths since the fixture is its own workspace root once copied); (b) `nx.json` -- a minimal consumer nx.json carrying the `targetDefaults` recipe for `angular-typechecker:angular-typecheck` (cache:true, outputs:[], the per-tsconfig + externalDependencies inputs as in the live workspace nx.json) so the installed plugin's cacheable target binds in the tmp workspace; (c) `package.json` -- the consumer manifest: `"private": true`, a `name` like `consumer-app`, Angular 22 stable runtime deps (`@angular/core`/`@angular/common`/`@angular/compiler`/`@angular/compiler-cli` at `22.0.4`, `typescript` at `6.0.3`) and the Nx bits the run needs (`nx`/`@nx/devkit` at `23.0.1`) so a clean `npm install <tgz>` resolves the peers -- the `angular-typechecker` tarball is added by the spec's install step, NOT pre-declared here; (d) `tsconfig.json` (references tsconfig.lib.json) + `tsconfig.lib.json` mirroring the dev consumer's Angular shape (`noEmit:true`, `strict:true`, `target:es2022`, `module:preserve`, `moduleResolution:bundler`, `angularCompilerOptions.strictTemplates:true`, `include:["src/**/*.ts"]`) but SELF-CONTAINED (no `extends` of the workspace base, NO `paths` alias to `@angular-typechecker/*` or `@fixtures/*` -- D-18); (e) `src/app.component.ts` -- a minimal standalone component that type-checks CLEAN (no committed error; mirror consumer.component.ts MINUS the `@fixtures` dep import -- it must be self-contained). This fixture's recipe (PUBLISHED id + includeDeps:true) IS the consumer README example proven by execution. Keep all files ASCII-only.</action>
  <verify>
    <automated>node -e "const p=require('./e2e/angular-typechecker-install-e2e/fixtures/consumer-app/project.json');const ex=p.targets['angular-typecheck'].executor;if(ex!=='angular-typechecker:angular-typecheck')throw new Error('wrong executor id: '+ex);if(p.targets['angular-typecheck'].options.includeDeps!==true)throw new Error('includeDeps not true');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/angular-typechecker-install-e2e/fixtures/consumer-app/project.json` target `angular-typecheck` uses executor `angular-typechecker:angular-typecheck` (the PUBLISHED unscoped id) and `options.includeDeps` is `true` (verify: the node -e check above exits 0).
    - The fixture does NOT use the dev-scoped key (verify: `git grep -c "@angular-typechecker/angular-typechecker:angular-typecheck" e2e/angular-typechecker-install-e2e/fixtures/consumer-app/project.json` returns 0).
    - The fixture tsconfig has NO alias to plugin source or @fixtures (verify: `git grep -c "@angular-typechecker/" e2e/angular-typechecker-install-e2e/fixtures/consumer-app/tsconfig.lib.json` returns 0 and `git grep -c "@fixtures" e2e/angular-typechecker-install-e2e/fixtures/consumer-app` returns 0).
    - `fixtures/consumer-app/package.json` declares `"private": true` (it is never published) and Angular 22.0.4 + typescript 6.0.3 stable peers (D-06; no -next/-rc anywhere).
    - `src/app.component.ts` exists, is a self-contained standalone component, and contains no committed type error.
    - All fixture files are ASCII-only and contain no `consensus.dk`/work-email reference (privacy).
  </acceptance_criteria>
  <done>A committed, self-contained minimal consumer fixture exists that wires the PUBLISHED executor id with includeDeps:true and no source alias -- the exact shape a real consumer would author.</done>
</task>

<task type="auto">
  <name>Task 2: Author the install smoke (pack -> clean tmp install -> green + injected-TS2322)</name>
  <files>e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts</files>
  <read_first>
    - e2e/angular-typechecker-cache-e2e/src/cache-busts-on-dep-error.int.spec.ts (the run()/RunResult helper lines 94-123, buildCleanEnv/NX_RUNNER_ENV_KEYS lines 59-89, the injected-error JSON.stringify idiom lines 188-191, the assertion trio lines 200-210)
    - .planning/phases/05-packaging-publish-hardening-e2e-smoke-mvp/05-RESEARCH.md "TEST-05 smoke shape" Code Example + "Common Pitfalls" (NEVER --no-color; NEVER copy the repo .npmrc; clean install no --legacy-peer-deps -- B-03) + Assumptions A3
    - .planning/phases/05-packaging-publish-hardening-e2e-smoke-mvp/05-PATTERNS.md "install-smoke.int.spec.ts" + "Crash-safe fixture mutation" (D-18 simplifies: mutate the TMP copy, no .pristine sidecar needed) sections
    - .planning/phases/05-packaging-publish-hardening-e2e-smoke-mvp/05-CONTEXT.md D-17/D-18/D-19/D-20/D-21
    - e2e/angular-typechecker-install-e2e/fixtures/consumer-app/ (the fixture from Task 1 to copy + install against)
  </read_first>
  <action>Create `e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts` implementing the D-17/D-18/D-19/D-20 smoke. Structure (clone the cache-e2e harness shape): resolve `workspaceRoot` (3 dirs up); `distDir = join(workspaceRoot, 'dist/packages/angular-typechecker')`; `fixtureDir = join(workspaceRoot, 'e2e/angular-typechecker-install-e2e/fixtures/consumer-app')`. In `beforeAll`: `execSync('npx nx build angular-typechecker', { cwd: workspaceRoot, env })` (buildCleanEnv: strip NX_RUNNER_ENV_KEYS, `NX_DAEMON:'false'`, `FORCE_COLOR:'0'` -- Pitfall 6 fresh dist) then `npm pack --json` in distDir to capture the ABSOLUTE `.tgz` path. Provide a `run(cwd, extra='')` helper cloned from the cache-e2e `run()` (execSync `npx nx run consumer-app:angular-typecheck --output-style=static`, try/catch concatenating stdout+stderr, reading `error.status`; NEVER `--no-color` -- Nx forwards `color:false` and the schema's `additionalProperties:false` rejects it; use FORCE_COLOR=0/--output-style=static). The smoke body (one `it(...)` or two): (1) `const tmp = mkdtempSync(join(tmpdir(), 'atc-smoke-'))`; copy the committed `consumer-app` fixture into `tmp` (do NOT copy any repo `.npmrc` -- D-20 honesty; the tmp workspace must have NO legacy-peer-deps); (2) `execSync('npm install ' + absoluteTgz, { cwd: tmp, env: cleanInstallEnv })` WITHOUT `--legacy-peer-deps` (B-03) -- if this ERESOLVEs, let the test FAIL surfacing the real finding (do NOT auto-add the legacy flag; the remediation is escalated per B-03); (3) GREEN: `const green = run(tmp)`; `expect(green.code).toBe(0)`; (4) mutate the TMP copy's `src/app.component.ts` injecting `const x: number = 'str';` via the JSON.stringify idiom (inherently crash-safe -- the tmp copy is discarded, no .pristine sidecar needed -- D-18); (5) INJECTED: `const bad = run(tmp)`; `expect(bad.code).not.toBe(0)`; `expect(bad.stdout).toContain('TS2322')`; `expect(bad.stdout).not.toMatch(/ERR_REQUIRE_ESM/)` (the installed CJS executor's import() survived packaging -- D-19); `expect(bad.stdout).not.toContain('infrastructure error')`. In `afterEach`/`afterAll`: `rmSync(tmp, { recursive:true, force:true })` and rm the `.tgz`. Optionally also assert a `require()`-the-installed-executor check (discretion -- D-18). Hoist the injected code token `'TS2322'` to one const (IN-02). Keep ASCII-only; never pipe nx/npm through head/rg (masks exit code).</action>
  <verify>
    <automated>npx nx run angular-typechecker-install-e2e:test</automated>
  </verify>
  <acceptance_criteria>
    - `npx nx run angular-typechecker-install-e2e:test` exits 0 with BOTH the audit gate (from 05-02) AND the smoke green (the serialized config runs all `*.int.spec.ts`).
    - The smoke installs WITHOUT `--legacy-peer-deps` (verify: `git grep -c "legacy-peer-deps" e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts` returns 0) and into a per-run mkdtemp tmp dir (verify: `git grep -c "mkdtempSync" e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts` >= 1).
    - The green run asserts `exit 0` and the injected run asserts non-zero exit + `TS2322` in stdout + NOT `ERR_REQUIRE_ESM` + NOT `infrastructure error` (verify: `git grep -c "ERR_REQUIRE_ESM" e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts` >= 1 and `git grep -c "TS2322" e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts` >= 1).
    - The smoke runs `nx run consumer-app:angular-typecheck` via the PUBLISHED id and `--output-style=static` (NOT `--no-color`) (verify: `git grep -c "no-color" e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts` returns 0; `git grep -c "output-style=static" e2e/angular-typechecker-install-e2e/src/install-smoke.int.spec.ts` >= 1).
    - The tmp workspace is cleaned up (`rmSync(... recursive, force)`) and the `.tgz` is removed in teardown.
    - If a clean install ERESOLVEs, the SUMMARY records it as a B-03 finding for human escalation (do NOT auto-patch with --legacy-peer-deps).
  </acceptance_criteria>
  <done>A clean install of the packed tarball into an isolated tmp workspace runs the executor green (exit 0) and reports an injected TS2322 with a non-zero exit and no ERR_REQUIRE_ESM -- the tracer bullet proving the package installs-and-runs from a clean install.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                                | Description                                                                                                                                         |
| ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| packed tarball -> consumer install                      | The smoke installs the exact artifact `nx release publish` would ship; a packaging defect (broken import(), unresolved executor path) crosses here. |
| repo `.npmrc` (legacy-peer-deps) -> smoke tmp workspace | Inheriting the repo's legacy-peer-deps would mask a real consumer ERESOLVE (B-03); the tmp workspace must not inherit it.                           |
| injected source -> type-check exit code                 | A no-op that exits 0 on broken source would be a lying type-checker; the injected-error run is the honesty check.                                   |

## STRIDE Threat Register

| Threat ID | Category  | Component                                         | Disposition           | Mitigation Plan                                                                                                                                                                                |
| --------- | --------- | ------------------------------------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-08   | Tampering | packaged executor import() survival               | mitigate              | Injected-error run asserts NO `ERR_REQUIRE_ESM` -- proves the CJS executor's dynamic import() of ESM compiler-cli survived packaging (D-19).                                                   |
| T-05-09   | Spoofing  | dev-scoped vs published executor id               | mitigate              | Fixture wires the PUBLISHED unscoped id `angular-typechecker:angular-typecheck`; the dev key would not bind in a consumer (D-18/STATE) -- a green run proves real resolution from the install. |
| T-05-10   | Tampering | honest peer resolution (B-03)                     | accept-with-surfacing | Clean install with NO `--legacy-peer-deps`; an ERESOLVE is surfaced as a real finding for human remediation (B-03), never auto-masked.                                                         |
| T-05-SC   | Tampering | npm install in the smoke (the tarball + its deps) | mitigate              | The only install is the just-built local tarball + the fixture's pinned Angular 22.0.4 / Nx 23.0.1 stable deps into a discarded tmp dir; no untrusted external package is introduced.          |

</threat_model>

<verification>
- `npx nx run angular-typechecker-install-e2e:test` exits 0 (audit gate + smoke green).
- The smoke installs clean (no legacy-peer-deps) into a tmp copy, runs green, and reports the injected TS2322 with a non-zero exit and no ERR_REQUIRE_ESM.
- `git grep` confirms the PUBLISHED executor id, no `--no-color`, no legacy-peer-deps, no source alias.
</verification>

<success_criteria>

- A clean `npm install <tarball>` into an isolated tmp workspace resolves the executor and runs `nx run consumer-app:angular-typecheck` green (exit 0) -- the tracer bullet.
- An injected TS2322 run exits non-zero with `TS2322` in stdout and neither `ERR_REQUIRE_ESM` nor the infra-error message -- the check ran and the packaged import() survived.
- The committed fixture uses the PUBLISHED unscoped executor id + includeDeps:true and carries no source path-alias.
- A clean (no legacy-peer-deps) install honestly surfaces any ERESOLVE (B-03) rather than masking it.
  </success_criteria>

<output>
Create `.planning/phases/05-packaging-publish-hardening-e2e-smoke-mvp/05-03-SUMMARY.md` when done. If a clean install ERESOLVEs, record it under a "B-03 finding" heading for human escalation.
</output>
