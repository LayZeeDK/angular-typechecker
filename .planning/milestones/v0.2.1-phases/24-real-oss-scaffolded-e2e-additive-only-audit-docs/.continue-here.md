---
context: phase
phase: 24-real-oss-scaffolded-e2e-additive-only-audit-docs
plan: 6
task: 1
total_tasks: 3
status: paused
last_updated: 2026-07-12T12:33:26.742Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each was discovered through a spike failure or is the load-bearing correctness gate. Acknowledge
> before executing 24-06.

- [ ] CONSTRAINT: **Option D (lazy-require `@nx/devkit`) is REFUTED — do NOT use it.** The compiled
      `ng-add` `schematic.js` MUST contain ZERO `@nx/devkit` (no import, no require) in its load OR
      execution path. That is the entire fix (Option C): the yarn-4 `chalk.blue` crash is
      PROCESS-WIDE (nx's CJS `log-symbols@4` resolving Angular schematics' ESM `chalk@5`), so deferring
      the `@nx/devkit` load past the CLI probe still throws at `executeSchematic`. Task 2's acceptance
      greps the compiled dist to enforce this; the eslint `core/**` block also bans `@nx/*`.
- [ ] CONSTRAINT: **Keep the Nx `configuration` generator BYTE-IDENTICAL — extract, do NOT rewrite.**
      Move the wiring decision logic into the shared `src/core/angular-cli-wiring.ts` and route the
      generator's CLI-branch through it with byte-preserved output + error strings. The 4 configuration
      specs (`configuration.spec`, `configuration-angular-cli.spec`, `configuration-matrix.spec`,
      `schema-parity.spec`) + the init specs are the pass/fail gate; the Nx else-branch stays untouched.

**Do not proceed until both boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| lazy-require-the-fix | Deferring `@nx/devkit` out of the ng-add module top-level (Option D) "fixed" the probe but `executeSchematic` still threw `chalk.blue` — the chalk breakage is process-wide. | blocking | The ng-add module must be VANILLA `@angular-devkit/schematics` with NO `@nx/devkit` anywhere; enforce via the compiled-dist grep in Task 2 + the eslint `core/**` ban. |
| rewrite-not-extract | Rewriting the configuration generator's wiring (instead of extracting a shared pure core) would drift from the collision-fixed behavior and break shipped output. | blocking | Extract pure functions (plain data + injected `exists` callback); keep the generator's observable output byte-identical; the 4 configuration specs gate it. |
| worktree-for-the-e2e | A worktree branches from origin/HEAD without `node_modules` and cannot run the Verdaccio yarn/npm/pnpm e2e. | advisory | Single-plan wave -> run on the MAIN checkout, no `isolation="worktree"` (per AGENTS.md + the plan's isolation note). |
| blind-slow-e2e | `nx e2e angular-typechecker-ng-cli-e2e` is several minutes (Verdaccio build+publish + yarn/npm/pnpm installs). | advisory | Use a generous Bash timeout or `run_in_background`; don't declare failure on a slow run. |

<current_state>
Phase 24 is code-complete for v0.2.1; gaps 24-04 + 24-05 landed and the phase was re-verified/secured/
validated + re-completed. A deep investigation then pinned the yarn-4 `ng add` no-auto-wire root cause,
REFUTED Option D (lazy-require), and CONFIRMED Option C (a vanilla nx-free ng-add auto-wires first-run
under yarn 4). The user chose to fold Option C into v0.2.1 as phase-24 gap 24-06. `24-06-PLAN.md` is
COMMITTED (HEAD 3495ec6) and plan-check PASSED (no blockers, 3 low-severity advisories). Working tree is
clean. THIS pause is BEFORE executing 24-06 — the user's approval to execute is this handoff.
</current_state>

<completed_work>
See `.planning/HANDOFF.json` `completed_this_session`. Headlines: 24-04 (nx direct dep) + 24-05 (yarn+pnpm
e2e) executed + all four completion gates re-run (verify 5/5, secure 0 threats, validate compliant) +
learnings + phase re-complete; vanilla-repro quick task 260712-ft9 (yarn failure = NX-SPECIFIC, no
upstream issue); Option D spike REFUTED + Option C spike CONFIRMED + exhaustive web research; 24-06
planned + plan-checked PASSED. All on `gsd/v0.2.1-angular-cli-workspace-support`; package.json stays 0.2.0.
</completed_work>

<remaining_work>
Execute 24-06 (3 tasks) then the post-execution gates. See HANDOFF.json `remaining_tasks` +
`next_action`:
- Task 1: extract `src/core/angular-cli-wiring.ts` + route the Nx configuration generator through it
  byte-identically (relocate + re-export the 2 shared constants from init).
- Task 2: vanilla nx-free ng-add + migrate the 14 ng-add tests + delete the orphaned `ngAddGenerator`
  (keep its schema files) + add `@angular-devkit/schematics` to eslint `ignoredDependencies`.
- Task 3: flip the yarn e2e to assert first-run `ng add` auto-wires (drop the `ng g` fallback +
  no-wire quirk-lock); resolve the README-yarn-caveat todo (README already claims universal auto-wire).
- Post-gates (dedicated agents): additive re-audit (24-ADDITIVE-AUDIT.md) -> gsd-code-review ->
  gsd-verifier -> gsd-secure-phase -> gsd-validate-phase -> gsd-extract-learnings -> phase.complete +
  global-learnings bridge.
</remaining_work>

<decisions_made>
See HANDOFF.json `decisions`. Headlines: (a) Option C (vanilla nx-free ng-add) is the fix, folded as
phase-24 gap 24-06; (b) single-source-of-truth via a shared framework-agnostic core, configuration+init
stay convertNx; (c) Option D lazy-require is refuted (process-wide chalk load).
</decisions_made>

<blockers>
- None. 24-06 is a real refactor (generator core + ng-add test suite), not a trivial edit — see the
  BLOCKING CONSTRAINTS above.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — full structured state + next_action.
2. `.planning/phases/24-real-oss-scaffolded-e2e-additive-only-audit-docs/24-06-PLAN.md` — the
   checker-PASSED plan (3 tasks, interface-first, must_haves, threat_model).
3. `.planning/debug/resolved/cli-yarn-e2e-wrong-version.md` — "Option C spike" (CONFIRMED design) +
   "Web research + consolidated fix ranking" (the authoritative root cause + why no cheaper fix exists).
4. `packages/angular-typechecker/src/generators/configuration/generator.ts` — the wiring logic to
   extract (resolveTsConfigLeaves + collision + targetName guard); its 4 specs are the byte-identity gate.
5. `packages/angular-typechecker/src/generators/ng-add/generator.ts` + `ng-add/ng-add.spec.ts` — the
   orphaned generator to delete + the 14 tests to migrate to the vanilla Rule.
6. `e2e/angular-typechecker-ng-cli-e2e/src/ng-add-ng-run-yarn.e2e.spec.ts` — the e2e to flip.

## Infrastructure State
- Verdaccio: stood up per-run by the ng-cli-e2e globalSetup (build dist ONCE + publish 0.2.0 ONCE,
  angular-typechecker no-proxy). Not running now.
- Branch `gsd/v0.2.1-angular-cli-workspace-support`, well ahead of origin/main, tree clean. package.json
  stays 0.2.0 (Release-PR is human-gated + not cut; complete-milestone deferred).
- Toolchain: @angular/cli 22.0.6, yarn 4.17.0 (corepack), nx 23.0.1, node 24. A ready Ng22 CLI workspace
  exists at D:/projects/sandbox/angular220 (scratch). The vanilla-ng-add + Verdaccio harness spikes live
  in the job tmp (external, uncommitted) + D:/projects/sandbox/vanilla-ng-add-repro.
- The local `angular-cli` clone (D:/projects/github/angular/angular-cli) was left on branch `22.0.x`
  (checked out for the investigation; `git checkout main` to restore).

<context>
The product is correct; this is a targeted architecture change so `ng add` auto-wires uniformly across
npm/pnpm/yarn. Mental model: the yarn probe/execution can't load nx's CJS log-symbols@4 (it misresolves
Angular schematics' ESM chalk@5 under yarn's hoist), so the ng-add path must be fully nx-free while
sharing the collision-fixed wiring core with the Nx generator (single source of truth). Windows arm64:
corepack yarn; git grep/rg NEVER grep; ASCII only; Write tool not heredocs; stage files by name;
committer email MUST be the public gmail. Do NOT file any upstream GitHub issue (not warranted; and it
would need explicit AskUserQuestion confirmation).
</context>

<next_action>
Start with: /gsd-execute-phase 24 --gaps-only . It runs the checker-PASSED plan 24-06 on the MAIN
checkout (single-plan wave, NO worktree). Then run the post-execution gates via dedicated agents in
order: additive re-audit -> gsd-code-review -> gsd-verifier -> gsd-secure-phase -> gsd-validate-phase ->
gsd-extract-learnings -> phase.complete + global-learnings bridge.
</next_action>
