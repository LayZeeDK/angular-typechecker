---
phase: 260719-k2i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - DX-fallow-script
must_haves:
  truths:
    - "`npm run fallow` runs the exact CI new-only gate (`fallow audit --format human --base origin/main`) and exits 0 on a clean tree."
    - "The script uses the bare `fallow` bin (not `npx`), matching the existing `nx`-based scripts (npm puts node_modules/.bin on PATH); fallow is already a pinned root devDependency."
  artifacts:
    - package.json
  key_links:
    - "The command mirrors ci.yml:350 verbatim so a local run reproduces the CI `fallow` job / `.fallowrc.jsonc` `audit.gate: new-only`."
---

<objective>
Add a `fallow` npm script to the root `package.json#scripts` so the CI-only code-quality
gate can be reproduced locally with one command. fallow is not run by `nx lint`, which is
why complexity/duplication regressions can pass local checks yet fail CI (see quick task
260719-iib). This is a developer-experience convenience — no product/source change.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add the `fallow` script mirroring the CI gate</name>
  <files>package.json</files>
  <action>
    Add `"fallow": "fallow audit --format human --base origin/main"` to `scripts` (after
    `format:check`). Bare `fallow` bin (matches the existing `nx`-based scripts; npm adds
    node_modules/.bin to PATH). Command is byte-identical to ci.yml:350.
  </action>
  <verify>
    <automated>npm run fallow &amp;&amp; npx prettier --check package.json</automated>
  </verify>
  <done>`npm run fallow` exits 0 ("No issues"); package.json is Prettier-clean.</done>
</task>

</tasks>

<success_criteria>
- `npm run fallow` resolves + runs the new-only gate and exits 0.
- No source/product/version change; package.json scripts-only edit.
</success_criteria>
