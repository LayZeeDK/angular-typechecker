---
phase: 09-resilience-per-file-fault-isolation-boundary-robustness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/angular-typechecker/src/core/filter-diagnostics.ts
  - packages/angular-typechecker/src/core/filter-diagnostics.spec.ts
autonomous: true
requirements: [RES-03]
tags: [angular, filter, realpath, resilience, boundary-filter]

must_haves:
  truths:
    - 'A throwing options.realpath() in the project-boundary filter is caught and falls back to the unresolved raw path; the type-check pass does not abort (RES-03 / SC3 / D-08).'
    - "The fallback path is STILL normalized (\\\\ -> /) and case-folded, so it classifies consistently with a resolved path (D-08)."
    - 'The happy path (a non-throwing realpath) is unchanged: in-project kept, out-of-project/node_modules suppressed; the per-input memoization cache still applies.'
    - 'The fallback is SILENT — no logging, no process access (core is PURE; eslint bans no-console and process.exit in **/src/core/**).'
  artifacts:
    - path: 'packages/angular-typechecker/src/core/filter-diagnostics.ts'
      provides: 'createCanonicalizer with the realpath try/catch + raw-path fallback (RES-03)'
      contains: 'catch'
    - path: 'packages/angular-typechecker/src/core/filter-diagnostics.spec.ts'
      provides: 'The throwing-realpath unit case proving in-project diagnostics are still kept and the pass does not abort'
      contains: 'RES-03'
  key_links:
    - from: 'filter-diagnostics.spec.ts'
      to: 'filterDiagnostics'
      via: "injected realpath: () => { throw new Error('EACCES'); } in FilterOptions"
      pattern: "throw new Error\\('EACCES'\\)"
    - from: 'filter-diagnostics.ts createCanonicalizer'
      to: 'the unresolved raw filePath'
      via: 'try/catch fallback (resolved = filePath)'
      pattern: 'catch'
---

<objective>
RES-03: Wrap the `options.realpath(filePath)` call inside `createCanonicalizer` (`filter-diagnostics.ts:127`) in try/catch so a throwing `realpath()` (EACCES / broken junction / permission-denied symlink) falls back to the unresolved raw path instead of propagating out of `filterDiagnostics` and aborting the WHOLE type-check pass.

Purpose: A filesystem realpath failure must degrade gracefully (one path classified on the raw value) rather than crash the pass (Denial of Service hardening). The fallback still normalizes + case-folds so the path classifies consistently; the cache and happy path are untouched; the fallback is SILENT (core is pure).
Output: A modified `createCanonicalizer` + an extended `filter-diagnostics.spec.ts` with the throwing-realpath case.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-CONTEXT.md
@.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-RESEARCH.md
@.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-PATTERNS.md

<interfaces>
<!-- The FilterOptions contract the spec injects against. Extracted from filter-diagnostics.ts. -->
From packages/angular-typechecker/src/core/filter-diagnostics.ts:
```ts
export interface FilterOptions {
  basePath: string;
  includeDeps: boolean;
  useCaseSensitiveFileNames: boolean;
  realpath: (filePath: string) => string;   // <- the injected impl; a test stub throws here
}
export interface FilterResult {
  kept: ts.Diagnostic[];
  suppressedCount: number;
}
export function filterDiagnostics(
  diagnostics: readonly ts.Diagnostic[],
  options: FilterOptions,
): FilterResult;
```
The RES-03 edit is INSIDE `createCanonicalizer` (filter-diagnostics.ts:115-136), the single place `options.realpath` is called (line 127). The per-input `cache` Map and the `\\` -> `/` normalize + case-fold logic stay exactly as-is.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wrap createCanonicalizer's realpath in a silent try/catch with raw-path fallback</name>
  <files>packages/angular-typechecker/src/core/filter-diagnostics.ts, packages/angular-typechecker/src/core/filter-diagnostics.spec.ts</files>
  <read_first>
    - packages/angular-typechecker/src/core/filter-diagnostics.ts (lines 115-136 — createCanonicalizer; the bare `options.realpath(filePath).replace(/\\/g, '/')` at line 127 the edit wraps; the per-input cache; the case-fold)
    - packages/angular-typechecker/src/core/filter-diagnostics.spec.ts (lines 14-30 — the diag() literal builder and the `base` shared fixture; lines 88-106 — the injected-realpath idiom to mirror with a THROWING stub)
    - packages/angular-typechecker/eslint.config.mjs (lines 16-63 — the **/src/core/** override banning no-console + process.exit; the fallback catch MUST be silent)
    - .planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-RESEARCH.md ("Code Examples — RES-03" — the exact try/catch shape; the Anti-Pattern: no process/logging in the fallback)
    - .planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-PATTERNS.md ("filter-diagnostics.ts (MODIFY, RES-03, line 127)" — the D-08 shape; "filter-diagnostics.spec.ts (EXTEND, RES-03)" — the test idiom)
  </read_first>
  <behavior>
    - Test 1 (RES-03): with `realpath: () => { throw new Error('EACCES'); }`, an in-project diagnostic at a path under `basePath` is STILL kept (`kept.length === 1`, `suppressedCount === 0`) — the throw is caught and the raw path classifies in-project. No exception escapes `filterDiagnostics`.
    - Test 2 (happy-path regression): the existing resolving-realpath cases (symlink-under-basePath kept; case-fold; node_modules/out-of-project suppressed) still pass unchanged.
  </behavior>
  <action>
    In `createCanonicalizer` (filter-diagnostics.ts), replace the single bare call `const real = options.realpath(filePath).replace(/\\/g, '/');` (line 127) with a try/catch that resolves into a local `resolved` variable: `try { resolved = options.realpath(filePath); } catch { resolved = filePath; }`, then `const real = resolved.replace(/\\/g, '/');` and the existing case-fold. The `catch` block is SILENT (no `console`, no `process`) — add ONLY a comment explaining D-08 (a throwing realpath must not abort the pass; fall back to the unresolved raw path; still normalize + case-fold; core is PURE). The per-input `cache` Map, the `useCaseSensitiveFileNames` case-fold, and the `cache.set`/early-return are unchanged. Follow CLAUDE.md JS/TS style: braces always; blank line around control flow.

    Extend `filter-diagnostics.spec.ts`: add an `it('RES-03: a throwing realpath is caught; the in-project diagnostic is still kept', ...)` that injects a throwing `realpath` (mirroring the :88-106 idiom but with a stub that throws `new Error('EACCES')`), reusing the `diag()` builder and `...base`, and asserts `result.kept` has length 1 and `result.suppressedCount` is 0.

  </action>
  <verify>
    <automated>npx nx test angular-typechecker -- filter-diagnostics.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `filter-diagnostics.ts` `createCanonicalizer` contains a `try { ... options.realpath(filePath) ... } catch { ... = filePath; }` whose `catch` body has NO `console`/`process` reference.
    - The fallback path is still passed through `.replace(/\\/g, '/')` and the `useCaseSensitiveFileNames` case-fold (consistent classification).
    - `filter-diagnostics.spec.ts` contains an `RES-03` test injecting `realpath: () => { throw new Error('EACCES'); }` that asserts `kept.length === 1` and `suppressedCount === 0`.
    - `npx nx test angular-typechecker -- filter-diagnostics.spec.ts` exits 0 (the new case AND every pre-existing case pass).
    - `npx nx lint angular-typechecker` exits 0 (the pure-core eslint override is satisfied — the catch is silent).
  </acceptance_criteria>
  <done>A throwing realpath in the boundary filter is caught and falls back to the raw path (still normalized + case-folded), proven by a unit test, with the happy path and cache unchanged and the fallback silent.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                         | Description                                                                                                                             |
| ------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| filesystem (realpath syscall) -> boundary filter | `options.realpath` (prod: `ts.sys.realpath`) crosses into the OS; a hostile/broken symlink target or a permission-denied path can throw |

## STRIDE Threat Register

| Threat ID | Category          | Component                                       | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------- | ----------------- | ----------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-09-02   | Denial of Service | createCanonicalizer (filter-diagnostics.ts:127) | mitigate    | RES-03 / D-08: wrap `options.realpath(filePath)` in try/catch and fall back to the unresolved raw path (still normalized + case-folded), so an EACCES / broken-junction / permission-denied realpath cannot abort the whole type-check pass. Proven by the injected throwing-realpath unit test (ASVS V12 Files/Resources — graceful degradation, no path-traversal surface: the filter only CLASSIFIES paths, never reads/writes them). |
| T-09-SC   | Tampering         | npm/pip/cargo installs                          | accept      | No package installs in this phase (RESEARCH "Package Legitimacy Audit: Not applicable").                                                                                                                                                                                                                                                                                                                                                 |

</threat_model>

<verification>
- `npx nx test angular-typechecker -- filter-diagnostics.spec.ts` exits 0 (RES-03 case + all pre-existing cases green).
- `npx nx lint angular-typechecker` exits 0 (pure-core eslint: the fallback is silent).
- Full suite green: `npx nx test angular-typechecker` (no regression in the boundary-filter coverage).
</verification>

<success_criteria>

- SC3 satisfied: a throwing `options.realpath()` is caught and falls back to the unresolved path; a filesystem realpath failure cannot abort the pass; the happy path is unchanged (proven by a realpath-throws unit test).
- No package added; core remains pure (no logging/process in the fallback).
  </success_criteria>

<output>
Create `.planning/phases/09-resilience-per-file-fault-isolation-boundary-robustness/09-03-SUMMARY.md` when done.
</output>
